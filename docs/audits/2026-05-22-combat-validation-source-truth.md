# Combat Validation Source-Truth Cross-Check

## Verdict

The three-lane validation direction is still right: catalog contracts, behavior-class tests, and representative runner/interactive scenarios. The correction is that catalog presence is not rule truth. A row is only "integrated" when it has both local implementation evidence and an explicit authority boundary.

## Authority Model

- Rulebook authority: Total Warfare / BattleMech Manual style rules define the tabletop mechanic.
- MegaMek source authority: use local MegaMek as executable reference for exact behavior only after reading the relevant source.
- MekHQ behavior authority: use only for campaign/maintenance behavior, not tabletop combat effects.
- MekStation deviation: UI, networking, event ordering, target filters, objectives, and turn-rotation semantics are product contracts layered over tabletop outcomes.

## Cross-Checked Findings

- Secondary target penalties and Multi-Tasker are source-backed by MegaMek: `E:/Projects/megamek/megamek/src/megamek/common/compute/Compute.java` computes +2 secondary target, -1 for front arc/Battle Armor, and -1 for `GUNNERY_MULTI_TASKER`; `OptionsConstants.java` defines `multi_tasker`.
- Local Multi-Target is not the MegaMek source-backed SPA. It must stay unsupported unless a separate rule source is found. Source-backed behavior should continue through Multi-Tasker/multi_tasker.
- Called shots are source-backed as TacOps called shots with +3 high/low/left/right modifiers in `ComputeAttackerToHitMods.java`. MekStation now carries called-shot intent through runner `AttackDeclared` events and event-sourced `declareAttack` to-hit math.
- Marksman and Sharpshooter called-shot reductions are local helper behavior, not MegaMek-verified. MegaMek has Sharpshooter commented out and no Marksman SPA hit in the searched source.
- Heat-induced ammo explosions are source-backed by MegaMek: `HeatResolver.java` checks heat >= 19 and calls `explodeAmmoFromHeat`; `TWGameManager.java` chooses the most destructive valid ammo bin and routes it through explosion damage.
- The `HEAT_RULE_COMBAT_SUPPORT['heat-induced-ammo-explosion']` row now carries structured `sourceRefs` for the MegaMek `HeatResolver` heat-threshold roll and `TWGameManager.explodeAmmoFromHeat` ammo-bin selection path, so heat ammo cookoff parity is pinned in the executable catalog instead of only this audit.
- Ejection lifecycle is now source-backed at the original-unit removal boundary: MegaMek `MovePathHandler` routes `MoveStepType.EJECT` into `TWGameManager.ejectEntity`, which marks the crew ejected, creates the ejected pilot/crew entity for campaign tracking, damages the cockpit/head, destroys the original entity, and removes the original unit with `REMOVE_EJECTED` for manual ejection. `ServerReportsHelper` then separates active combat units, ejected-unit counts, and ejected crew counts. The `ejection-lifecycle` requirement row now carries structured `sourceRefs` for those three MegaMek anchors. MekStation still intentionally keeps ejection as a product contract: `UnitEjected` preserves the mech's current damage snapshot, does not spawn an ejected-pilot unit, and removes the unit from actions, target filters, objectives, and survivor counts.
- TAG marker application is source-backed by MegaMek: `TAGHandler.java` creates `TagInfo`, calls `setTaggedBy`, and also marks the attacker as spotting the target for indirect fire. MegaMek clears per-turn TAG info in phase preparation via `resetTagInfo`. TAG designation, marker event, and clear support rows now carry structured `sourceRefs` for the MegaMek handler and phase-reset anchors.
- Standard NARC marker application is source-backed by MegaMek: `NarcHandler.java` attaches a `NarcPod` to the hit target location. The NARC marker attachment and lifecycle-event rows now carry structured `sourceRefs` for that handler anchor. iNARC has separate pod variants (ECM, Haywire, Nemesis, Homing) and stays helper-only with its own source anchor until represented explicitly in runner missile resolution.
- Artemis cluster modifiers are source-backed by MegaMek: `MissileWeaponHandler.java` and `LRMHandler.java` gate the bonus on linked Artemis FCS plus matching Artemis-capable ammo, suppress it for ECM/attacker stealth and indirect LRM fire, apply Artemis IV as +2, prototype Artemis IV as +1, and Artemis V as +3 without stacking IV/V. MekStation now approximates Artemis IV/prototype IV/V flag hydration, resolves their direct-fire cluster modifiers, and applies source-backed indirect-fire, ECM, active-probe countering, and active-attacker-stealth suppression, while exact FCS linking, Nova CEWS networking, and FCS/ECM/probe/stealth mode or damage lifecycle remain gaps.
- Artemis ECM/stealth edges are now split in the validation catalog instead of folded into the broad Artemis row: ECM suppression is integrated because `createInitialState` seeds standard Guardian/Angel/Clan ECM plus Watchdog/Nova CEWS equipment and critical-slot ECM into `IGameState.electronicWarfare`, hydrates active probes/CEWS for ECM countering, and `runAttackPhase` derives target ECM coverage into missile cluster resolution. Attacker stealth suppression is integrated because runner state now preserves BattleMech stealth armor and suppresses Artemis IV/prototype IV/V only when the attacking unit has active own ECM support.
- Active probe and CEWS hydration is source-backed by MegaMek: `MiscType` marks Beagle, Bloodhound, Clan Active Probe, light active probe, Watchdog CEWS, and Nova CEWS with `F_BAP`, Watchdog/Nova also carry `F_ECM`, and `Entity.getBAPRange` treats Clan Active Probe, Watchdog, and Nova as 5-hex probes. The `artemis-ecm-suite-hydration` and `active-probe-counter-hydration` rows now carry structured `sourceRefs` for those equipment and BAP-range anchors. This intentionally follows MegaMek over the local Watchdog catalog prose that says "4 hex probe range"; that catalog/source mismatch remains a data-audit item, not a runner behavior blocker.
- Physical weapon damage and to-hit modifiers are source-backed for the integrated runtime set: MegaMek commit `6ca1867` applies `ClubAttackAction.getDamageFor(...)` formulas for hatchet/lance, sword, and mace, and `ClubAttackAction.getHitModFor(...)` returns hatchet `-1`, sword `-2`, mace `+1`, and lance `+1`. Official BattleMech Manual errata v7.01 also changes the lance physical weapon table modifier from 0 to +1. MekStation now uses source-backed sword ceil(weight / 10) + 1 damage, mace ceil(weight / 4) damage, and +1 lance to-hit.
- Push and charge displacement are source-backed by local MegaMek: `TWGameManager.resolvePushAttack` displaces the pushed target in attacker-facing direction and then moves the attacker into the target's original hex when the target actually moved; the charge resolution path uses the same displacement shape on successful charge. MekStation runner and event-sourced physical resolution now record displacement payloads and apply target movement plus attacker follow-through for valid push/charge displacement.
- Charge-miss displacement is source-backed by local MegaMek: `Compute.getMissedChargeDisplacement` displaces the attacker into one of the two side hexes 60 degrees off the charge direction, preferring the higher-elevation side hex and otherwise choosing randomly. MekStation runner and event-sourced physical resolution now emit and replay `charge_miss` attacker-only displacement payloads when a legal side hex exists.
- Death-from-above displacement is source-backed by local MegaMek: `TWGameManager.resolveDfaAttack` uses `Compute.getValidDisplacement` on a hit to move the target out of the target hex, then displaces the attacker into that original target hex; on a miss it uses `Compute.getPreferredDisplacement` for the target before the attacker falls into the target hex. MekStation now emits/replays valid DFA hit and miss displacement payloads in runner and event-sourced physical resolution. Impossible-displacement destruction and exact fall/prone side effects remain gaps.
- MegaMek physical action class scope is now explicit in MekStation. `PHYSICAL_ACTION_CLASS_SCOPE_SUPPORT` covers the concrete MegaMek action classes found under `megamek/common/actions`: runtime-integrated BattleMech classes (`PunchAttackAction`, `KickAttackAction`, `PushAttackAction`, `ChargeAttackAction`, `DfaAttackAction`, `ClubAttackAction`), unsupported BattleMech-applicable classes (`BrushOffAttackAction`, `ThrashAttackAction`, `TripAttackAction`, `GrappleAttackAction`, `BreakGrappleAttackAction`, `JumpJetAttackAction`), and non-BattleMech classes split out of this matrix (`AirMekRamAttackAction`, `BAVibroClawAttackAction`, `LayExplosivesAttackAction`, `ProtoMekPhysicalAttackAction`, `RamAttackAction`).
- Push, charge, and DFA legality gates are now source-checked against local MegaMek action classes and cataloged in `PHYSICAL_LEGALITY_GATE_SUPPORT`. MekStation currently integrates shared target existence, shared friendly-target rejection, shared self-target rejection, shared adjacent target range, push destination openness, push attacker/target Mek unit-type rejection, push both-arms-present, push No Arms, push same elevation, push target directly ahead of attacker feet facing, push attacker/target not prone, charge requires-run, charge standing-Mek target rejection, charge non-Mek-to-infantry/ProtoMech target rejection, charge elevation-overlap rejection, charge target movement-complete/immobile rejection, DFA requires-jump, DFA attacker not prone, and DFA infantry-family attacker rejection; remaining shared physical impossibility gates and stricter charge/DFA/building/displacement legality gates remain explicit unsupported rows instead of implicit omissions.
- Destroyed-target physical targetability is now explicit: MegaMek `Game.getValidTargets` filters through `Entity.isTargetable`, and `Entity.isTargetable` excludes destroyed/doomed/off-board/non-deployed units. MekStation now rejects destroyed physical targets at helper and event-sourced declaration time, resolves stale declared attacks against destroyed targets as `TargetDestroyed` without side effects, and runner physical target selection already excludes destroyed enemies.
- Push arm-presence legality is source-backed by MegaMek: `PushAttackAction.toHit` rejects when either arm location is bad. MekStation now passes unit `destroyedLocations` into push restriction helpers from eligibility, event-sourced declaration/resolution, and runner physical resolution, rejecting missing left or right arm as `LimbMissing`.
- Push unit-type legality is source-backed by MegaMek: `PushAttackAction.toHit` rejects non-Mek attackers and non-Mek targets. MekStation now carries explicit `unitType` into `IUnitGameState`, passes attacker/target unit types through push restriction helpers from eligibility, event-sourced declaration/resolution, and runner physical resolution, and rejects explicit non-Mek values as `AttackerNotMek` or `TargetNotMek` while preserving legacy undefined BattleMech fixtures.
- Charge standing-Mek target legality is source-backed by MegaMek: `ChargeAttackAction.toHit` rejects a Mek attacker's charge against targets that are not Meks or are prone. MekStation now passes attacker/target unit types and target prone state through charge restriction helpers from eligibility, event-sourced declaration/resolution, and runner physical resolution, rejecting explicit non-Mek targets as `TargetNotMek` and prone targets as `TargetProne`.
- Charge non-Mek target-class legality is source-backed by MegaMek: `ChargeAttackAction.toHit` rejects non-Mek attacker charges against Infantry and ProtoMek targets. MekStation now maps explicit Infantry, Battle Armor, and ProtoMech target unit types into `TargetInfantryOrProtoMek` rejection for explicit non-Mek attackers through helper, eligibility, event-sourced declaration/resolution, and runner physical resolution.
- Charge elevation-overlap legality is source-backed by MegaMek: `ChargeAttackAction.toHit` computes attacker and target elevation bands with normal standing Mek `height()` equal to 1 and rejects when the bands do not overlap. MekStation now applies the same default standing-Mek band check from `elevationDifference`, rejecting non-overlap as `ElevationMismatch` through helper, eligibility, event-sourced declaration/resolution, and runner physical resolution.
- Charge target movement-complete legality is source-backed by MegaMek: `ChargeAttackAction.toHit` rejects charge targets that have not completed movement unless the target is immobile. MekStation now passes optional movement-complete state through charge restriction helpers from eligibility and event-sourced declaration/resolution, preserves immobile targets via shutdown-derived target immobility, and treats runner physical resolution as post-movement complete.
- DFA infantry-family attacker legality is source-backed by MegaMek: `DfaAttackAction.toHit` rejects `Infantry` attackers before hit resolution. MekStation now maps explicit Infantry and Battle Armor unit types into `AttackerInfantry` rejection through helper, eligibility, event-sourced declaration/resolution, and runner physical resolution, keeping those non-BattleMech paths from silently using BattleMech DFA behavior.
- Push arm-fired weapon legality is source-backed by MegaMek: `PushAttackAction.toHit` rejects when `weaponFiredFrom(Mek.LOC_RIGHT_ARM)` or `weaponFiredFrom(Mek.LOC_LEFT_ARM)` is true. MekStation now rejects push when arm-fired weapon ids are supplied to the physical restriction input, event-sourced declarations reject before scheduling, and runner injected push declarations conservatively thread the flat `weaponsFiredThisTurn` list through the same gate. Exact runner parity still needs weapon-location hydration so torso-fired weapons are not treated as arm-fired weapons.
- Every `PHYSICAL_LEGALITY_GATE_SUPPORT` row now carries structured `sourceRefs` to local MegaMek commit `325b2504c7b7750ecdcb85468621fb2de2ad8e60`, including shared physical impossibility gates, destroyed-target targetability, push, charge, and DFA legality ranges. Unsupported physical gaps therefore have line-anchored authority before any future implementation promotes them to integrated.

## Council Cross-Exam Findings

- OMO-native council seats were unavailable in this Codex app runtime, so the review fell back to an engineering-council shape with real read-only subagents for source truth, invariants, and critique.
- The current push/charge displacement implementation is aligned with the verified MegaMek source shape, but the surrounding requirement rows must not read as full physical-combat parity.
- Interactive/event-sourced physical resolution now emits and replays successful push/charge displacement payloads when a grid is supplied, closing the runner-vs-interactive split-brain for that successful-hit shape.
- Push/charge/DFA legality gates are now cataloged against MegaMek source, and the representable push/DFA posture/elevation/facing/unit-type gates have started moving into shared restriction helpers. MegaMek still checks target/entity type, quads, airborne state, flipped arms, missing arms, arm-fired weapons, range, movement state, displacement conflicts, and building/invalid target shapes that MekStation does not yet enforce.
- Charge-miss and valid DFA hit/miss displacement now emit from runner and event-sourced resolution; impossible-displacement destruction, exact DFA fall/prone timing, and blocked charge/DFA semantics are still open validation gaps.
- Multi-unit physical phases can validate later displacement against stale `grid.hexes.occupantId` because runner displacement updates unit positions but not the grid occupancy map.
- MegaMek's physical target probe includes additional physical classes such as brush-off, thrash, lay explosives, trip, grapple, break grapple, jump-jet attacks, ProtoMek physical, and Battle Armor vibro-claw paths. These are now explicitly cataloged as integrated, unsupported BattleMech-applicable, or non-BattleMech scope rather than silently absent.
- MekHQ should remain low priority for combat mechanics. Use it only when a campaign, maintenance, or quirk lifecycle is specifically in scope.

## Gates To Keep

- Do not promote a requirement to integrated if any support reference is helper-only or unsupported.
- Do not use OpenSpec alone as external rule authority; it is the project contract.
- Do not use broad known-limitation filters to certify combat coverage.
- Do not claim non-BattleMech coverage in the BattleMech matrix. Vehicles, aerospace, infantry, battle armor, and ProtoMechs need separate matrices or explicit out-of-scope rows.
- For lifecycle claims, require command/intent/wire coverage, event emission, state mutation, target filtering, and terminal summary evidence.
- Feature support rows that put `source-backed` in their evidence must carry structured `sourceRefs` with pinned rulebook or MegaMek/MekHQ authority links; prose-only authority claims are not enough.
- Helper-only or unsupported combat classes must have a named authority row, an expected simulation scenario, and an explicit reason for exclusion from the BattleMech matrix.
- Physical legality rows must carry commit-pinned MegaMek `sourceRefs`; unsupported rows may stay unsupported, but they cannot stay source-ambiguous.

## Source References

- MegaMek commit `6ca1867`: [`MovePathHandler.java` ejection movement branch](https://github.com/MegaMek/megamek/blob/6ca18676725d273f6b96a3fe5bdd9ecda22c2811/megamek/src/megamek/server/totalWarfare/MovePathHandler.java#L177-L215).
- MegaMek commit `6ca1867`: [`TWGameManager.ejectEntity`](https://github.com/MegaMek/megamek/blob/6ca18676725d273f6b96a3fe5bdd9ecda22c2811/megamek/src/megamek/server/totalWarfare/TWGameManager.java#L28991-L29232).
- MegaMek commit `6ca1867`: [`ServerReportsHelper` unit/ejected-crew counts](https://github.com/MegaMek/megamek/blob/6ca18676725d273f6b96a3fe5bdd9ecda22c2811/megamek/src/megamek/server/ServerReportsHelper.java#L46-L89).
- BattleMech Manual Errata v7.01: [`Physical Weapon Attacks Table` lance modifier correction](https://battletech.com/wp-content/uploads/2025/06/78_BattleMech-Manual-2023-09-17-v7.01-3.pdf).
- MegaMek commit `6ca1867`: [`ClubAttackAction.getDamageFor` physical weapon formulas](https://github.com/MegaMek/megamek/blob/6ca18676725d273f6b96a3fe5bdd9ecda22c2811/megamek/src/megamek/common/actions/ClubAttackAction.java#L91-L182).
- MegaMek commit `6ca1867`: [`ClubAttackAction.getHitModFor` physical weapon modifiers](https://github.com/MegaMek/megamek/blob/6ca18676725d273f6b96a3fe5bdd9ecda22c2811/megamek/src/megamek/common/actions/ClubAttackAction.java#L218-L251).
- MegaMek commit `6ca1867`: [`ClubAttackAction.toHit` applies physical weapon hit modifiers](https://github.com/MegaMek/megamek/blob/6ca18676725d273f6b96a3fe5bdd9ecda22c2811/megamek/src/megamek/common/actions/ClubAttackAction.java#L521-L529).
- MegaMek commit `6ca1867`: [`MiscType.createISLance` declares `toHitModifier = 1`](https://github.com/MegaMek/megamek/blob/6ca18676725d273f6b96a3fe5bdd9ecda22c2811/megamek/src/megamek/common/equipment/MiscType.java#L3798-L3813).
- Local MegaMek source cross-check: `E:/Projects/megamek/megamek/src/megamek/server/totalWarfare/TWGameManager.java` resolves push displacement through `resolvePushAttack`, target `doEntityDisplacement`, and attacker follow-through into the target's original hex.
- Local MegaMek source cross-check: `E:/Projects/megamek/megamek/src/megamek/server/totalWarfare/TWGameManager.java` resolves successful charge displacement by moving the target to the next hex and the attacker into the target's former position.
- Local MegaMek source cross-check: `E:/Projects/megamek/megamek/src/megamek/common/compute/Compute.java` resolves missed charge displacement through `getMissedChargeDisplacement`.
- Local MegaMek source cross-check: `E:/Projects/megamek/megamek/src/megamek/server/totalWarfare/TWGameManager.java` resolves death-from-above displacement in `resolveDfaAttack`, including target `doEntityDisplacement`, attacker fall/follow-through, and impossible-displacement destruction branches.
- Local MegaMek source cross-check: `E:/Projects/megamek/megamek/src/megamek/common/compute/Compute.java` resolves DFA hit/miss destination choice through `getValidDisplacement` and `getPreferredDisplacement`.
- Local MegaMek source cross-check: `E:/Projects/megamek/megamek/src/megamek/common/actions/` contains the concrete physical action classes now mirrored by `PHYSICAL_ACTION_CLASS_SCOPE_SUPPORT`.
- Local MegaMek source cross-check: `E:/Projects/megamek/megamek/src/megamek/common/actions/PhysicalAttackAction.java` applies shared physical attack impossibility gates now cataloged by `PHYSICAL_LEGALITY_GATE_SUPPORT`.
- Local MegaMek source cross-check: `E:/Projects/megamek/megamek/src/megamek/common/game/Game.java` builds physical display target lists through `getValidTargets`, which filters `Entity.isTargetable`; `E:/Projects/megamek/megamek/src/megamek/common/units/Entity.java` defines destroyed/doomed units as not targetable.
- Local MegaMek source cross-check: `E:/Projects/megamek/megamek/src/megamek/common/actions/PushAttackAction.java` applies broader push legality gates than MekStation currently maps.
- Local MegaMek source cross-check: `E:/Projects/megamek/megamek/src/megamek/common/actions/PushAttackAction.java` rejects push when either arm location is bad/missing.
- Local MegaMek source cross-check: `E:/Projects/megamek/megamek/src/megamek/common/actions/PushAttackAction.java` rejects push when attacker is not a Mek or target is not a Mek.
- Local MegaMek source cross-check: `E:/Projects/megamek/megamek/src/megamek/common/actions/PushAttackAction.java` rejects push when either arm location fired a weapon this turn.
- Local MegaMek source cross-check: `E:/Projects/megamek/megamek/src/megamek/common/actions/ChargeAttackAction.java` applies broader charge legality gates than MekStation currently maps.
- Local MegaMek source cross-check: `E:/Projects/megamek/megamek/src/megamek/common/actions/ChargeAttackAction.java` rejects a Mek attacker's charge against a non-Mek or prone target.
- Local MegaMek source cross-check: `E:/Projects/megamek/megamek/src/megamek/common/actions/ChargeAttackAction.java` rejects non-Mek attacker charges against Infantry and ProtoMek targets.
- Local MegaMek source cross-check: `E:/Projects/megamek/megamek/src/megamek/common/actions/ChargeAttackAction.java` rejects charges when attacker and target elevation bands do not overlap; `E:/Projects/megamek/megamek/src/megamek/common/units/Mek.java` returns normal standing Mek `height()` as 1.
- Local MegaMek source cross-check: `E:/Projects/megamek/megamek/src/megamek/common/actions/ChargeAttackAction.java` rejects charges against targets that are not done with movement unless the target is immobile.
- Local MegaMek source cross-check: `E:/Projects/megamek/megamek/src/megamek/common/actions/DfaAttackAction.java` applies broader DFA legality gates than MekStation currently maps.
- Local MegaMek source cross-check: `E:/Projects/megamek/megamek/src/megamek/common/actions/DfaAttackAction.java` rejects `Infantry` attackers before DFA hit resolution.
- Local MegaMek source cross-check: `E:/Projects/megamek/megamek/src/megamek/common/compute/Compute.java` enumerates additional physical target/action checks that must be explicitly scoped in or out.
- Local MegaMek source cross-check: `E:/Projects/megamek/megamek/src/megamek/server/totalWarfare/TWPhaseEndManager.java` resolves physical attacks and chain-whip grapple checks through phase-end handling.
- MegaMek commit `325b2504`: [`MissileWeaponHandler` Artemis IV/prototype IV/V modifiers](https://github.com/MegaMek/megamek/blob/325b2504c7b7750ecdcb85468621fb2de2ad8e60/megamek/src/megamek/common/weapons/handlers/MissileWeaponHandler.java#L144-L188).
- MegaMek commit `325b2504`: [`LRMHandler` skips Artemis bonuses for indirect mode](https://github.com/MegaMek/megamek/blob/325b2504c7b7750ecdcb85468621fb2de2ad8e60/megamek/src/megamek/common/weapons/handlers/lrm/LRMHandler.java#L159-L207).
- MegaMek commit `325b2504`: [`MiscType.createISProtoArtemis` defines Prototype Artemis IV FCS](https://github.com/MegaMek/megamek/blob/325b2504c7b7750ecdcb85468621fb2de2ad8e60/megamek/src/megamek/common/equipment/MiscType.java#L6276-L6295).
- MegaMek commit `325b2504`: [`ComputeAttackerToHitMods` applies called-shot +3 modifiers](https://github.com/MegaMek/megamek/blob/325b2504c7b7750ecdcb85468621fb2de2ad8e60/megamek/src/megamek/common/actions/compute/ComputeAttackerToHitMods.java#L108-L138).
- Local MegaMek source cross-check: `E:/Projects/megamek/megamek/src/megamek/common/equipment/MiscType.java` creates Watchdog/Nova CEWS with `F_ECM` and `F_BAP`.
- Local MegaMek source cross-check: `E:/Projects/megamek/megamek/src/megamek/common/equipment/Sensor.java` classifies Watchdog, light AP, Nova CEWS, and Beagle prototype as BAP sensors.
- Local MegaMek source cross-check: `E:/Projects/megamek/megamek/src/megamek/common/units/Entity.java` returns 5-hex BAP range for Clan Active Probe, Watchdog, and Nova CEWS.
- Local MegaMek source cross-check: `E:/Projects/megamek/megamek/src/megamek/common/weapons/handlers/MissileWeaponHandler.java` suppresses Artemis IV, prototype Artemis IV, and Artemis V when `attackingEntity.isStealthActive()`.
- Local MegaMek source cross-check: `E:/Projects/megamek/megamek/src/megamek/common/weapons/handlers/lrm/LRMHandler.java` repeats the same attacker-stealth Artemis suppression in the LRM-specific path.
- Local MegaMek source cross-check: `E:/Projects/megamek/megamek/src/megamek/common/units/Mek.java` considers Mek stealth active only when stealth equipment is on and the unit has active ECM.

## Next Best Gap

Secondary-target declaration state, called-shot declaration state, TAG/NARC marker events, Artemis IV/prototype IV/V flag hydration, Artemis indirect-fire suppression, automatic ECM/probe/CEWS hydration for Artemis ECM suppression and countering, attacker-stealth Artemis suppression, ejection lifecycle validation, source-backed hatchet/sword/mace/lance physical weapon formulas, successful runner/event-sourced push/charge/DFA displacement, runner/event-sourced charge/DFA miss displacement, explicit MegaMek physical action class scoping, source-backed push/charge/DFA legality gate cataloging, and the first shared push/DFA legality gates including target existence, friendly-target rejection, self-target rejection, adjacency, push unit-type rejection, charge standing-Mek target rejection, charge non-Mek-to-infantry/ProtoMech target rejection, charge elevation-overlap rejection, charge target movement-complete/immobile rejection, DFA infantry-family attacker rejection, and direct-ahead push facing are now present. The clean next implementation gap is source-backed but narrower: choose between impossible-displacement destruction, exact DFA fall/prone timing, implementing the remaining cataloged but unsupported push/charge/DFA legality gates, implementing unsupported BattleMech physical classes, FCS/ECM/probe/stealth damage or mode lifecycle, Nova CEWS network behavior, or runtime support for official physical weapons still cataloged as unsupported.
