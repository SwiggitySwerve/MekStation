# Lane A review: U96

reviewedHead: bbdb6a07cb2f01ec2f162bb2dc17a64774ef4504
baseline: 2edfbb3b6f40f04c641dbc8530afd5ad6df73d2a
reviewerModel: claude-sonnet (Agent model: sonnet, lean-worker)
Verdict: APPROVE

## Setup

- `git -C E:/Projects/MekStation fetch origin` — exit 0.
- `git -C E:/Projects/MekStation rev-parse bbdb6a07cb2f01ec2f162bb2dc17a64774ef4504` and `...2edfbb3b6f40f04c641dbc8530afd5ad6df73d2a` both resolve (both commits present after fetch).
- Detached worktree created: `git worktree add --detach .sisyphus/roadmap-completion-20260912/worktrees/u96-review bbdb6a07cb2f01ec2f162bb2dc17a64774ef4504` — HEAD is now at bbdb6a07c.
- `node_modules` junctioned via PowerShell `New-Item -ItemType Junction` — succeeded.
- `export PATH=/c/Users/wroll/AppData/Local/nvm/v22.22.0:$PATH; node -v` → v22.22.0; `export npm_config_dry_run=true` set for every npm/npx call. No install/ci/prune/build/Playwright/server run performed by this lane.
- Root checkout and worktrees `u40`/`u92` were not touched (only `u96-review` was created/used).

## Files on the range

`git diff --stat 2edfbb3b6f40f04c641dbc8530afd5ad6df73d2a..bbdb6a07cb2f01ec2f162bb2dc17a64774ef4504`: 8 files changed, 941 insertions(+), 120 deletions(-) — identical file list to the local receipt's `changedFiles`:
`e2e/coop-campaign-two-browser-journey.spec.ts` (+4/-2 net, 4 lines), `src/__tests__/api/campaigns/campaignPutLiveHost.test.ts` (new, 352), `src/__tests__/integration/coopMutationPersistence.test.ts` (+13/-1), `src/lib/multiplayer/server/CampaignHostRegistry.ts` (+13), `src/lib/multiplayer/server/CampaignMatchHost.ts` (+27), `src/pages/api/campaigns/[id].ts` (+73/-3), `src/stores/campaign/useCampaignStore.__tests__/useCampaignStore.coopDayAdvanceOrder.test.ts` (new, 261), `src/stores/campaign/useCampaignStore.dayActions.ts` (+199/-115). `useCampaignStore.actions.ts` shows no diff (`git diff --stat ... -- .../useCampaignStore.actions.ts` produced no output) — confirmed unchanged as claimed.

The 4 product files' sha256 in the worktree matched `local` receipt's `changedFiles[].sha256After` exactly before I touched anything (verified below in Gates/Findings 6-7), so the reviewed head is the exact code the receipts describe.

## Findings

1. **[INFO] Adoption correctness — same lock, checkpoint fold explains the fix's mechanism.** `CampaignMatchHost.runExclusive` (`CampaignMatchHost.ts:212-221`) chains every call on one `this.commitChain` promise; `applyHostIntent` (`:363-365`), `open` (`:235-236`) and the new `adoptSavedCheckpoint` (`:382-392`) all go through it, so a host command and an adoption cannot interleave — same lock, verified by reading the shared chain, not inferred. `adoptSavedCheckpoint` reads `this.log.nextSequence()` before `save()`, then folds `this.log.getCampaignEvents(before)` with `applyCampaignEvent` into `this.state` and publishes each (`:383-391`); the log is the same `CampaignEventLog` the route's checkpoint write appends to (same SQLite journal), so the head the next command's `nextSequence()` sees is the checkpoint's, not a separately-tracked value (`CampaignMatchHost.ts:150,207,384,541`). The `route`'s live-session gate at `src/pages/api/campaigns/[id].ts:253-256` runs `getCampaignHostRegistry().liveEntryFor(id)` and refuses non-host PUTs *before* `save()` is even called; a host-own PUT instead reaches `await live.host.adoptSavedCheckpoint(save)` at `:288-289`. Stale-base rollback is not possible: `campaignRecordRow.plan` (`CampaignPersistenceService.ts:186-188`) does `if (baseVersion !== currentVersion) return conflictFromStoredRow(...)` before any row write, and this check runs *inside* `save()`, which itself runs inside `adoptSavedCheckpoint`'s lock — a stale-base PUT (correct coopSession) returns `{kind:'conflict'}` from `save()` with nothing appended to the journal, so the fold loop in `adoptSavedCheckpoint` iterates zero events and nothing is rolled back. I additionally confirmed the mechanism this closes: `FundsChanged` carries an **absolute** post-balance computed from `host.readState()` at intent time (`applyCampaignEvent.ts:80-83,138-142`, comment: "the event carries the resulting balance...rather than re-deriving from the delta"), which is why the baseline bug was a durable erasure (a stale in-memory balance minted a wrong absolute event that journal replay then trusts) and why folding `this.state` before the next intent (not just publishing) is the load-bearing part of the fix — proven with my own mutant in Finding 7.
2. **[INFO] Identity is an unauthenticated body claim, confirmed no new trust.** `src/pages/api/campaigns/[id].ts` on the baseline (`git show 2edfbb3b6f40f04c641dbc8530afd5ad6df73d2a:...`) has no `Authorization`/session check anywhere in the PUT handler — grep for `auth|token|session|header` turns up only the pre-existing `authority` (D2 source/replica) concept, unrelated to caller identity. U96 adds `isLiveHostSave` (`[id].ts:134-140`), which reads `envelope.body.coopSession` — a claim already carried by the unauthenticated envelope before this unit, so it adds no new trust surface, matching FN-u96-put-host-identity-is-a-body-claim. Grepped `createGuestCoopSession`/`createHostCoopSession` call sites: only `CampaignCoopEntryPanel.tsx:258` calls `createHostCoopSession` (the host-creation flow, right after the match is created); the only client call site that produces a guest session is `useCampaignStore.actions.ts:136`, which calls `createGuestCoopSession` (`mode: 'guest'`). No shipped code path lets a guest's own store stamp `mode: 'host'`.
3. **[INFO] 409 envelope keeps every field the client reads; `current` is the stored record.** `refuseForLiveSession` (`[id].ts:147-171`) reads `readCampaign(id)` fresh and returns `{kind:'conflict', reason: LIVE_COOP_SESSION_REASON, recoveryAction: EXPECTED_HEAD_RESYNC_ACTION, conflictingFields: [], currentVersion: stored.record.version, current: stored.record, matchId}` (or the `blocked` shape when the record isn't readable). The client's `readConflictBody` (`useCampaignPersistenceStore.ts:624-651`) only requires `kind==='conflict'`, `reason` (any string), `recoveryAction`, `currentVersion` (number) and `current` (defined) — every one of those is present, and it sets `record = typed.current`, i.e. the just-read stored row, not the refused envelope. No code branches on the specific `reason` string besides the toast text (grep of `saveConflict`/`conflictServerRecord` usage), so the existing rollback + "changed elsewhere" toast path (`:852-883`) fires uniformly, as admission's risk note says. Closed detection is `entry.host.isClosed()` reading `private closed = false` set only by `close()` (`CampaignMatchHost.ts:115,245,250`); `pauseUntilGmReturns` (`CampaignSyncSession.ts:316-317`) sets a separate `paused` flag and never touches `closed`, so a paused (GM-absent) host is still live per `liveEntryFor`'s `!entry.host.isClosed()` filter (`CampaignHostRegistry.ts:15-20`) — confirmed, not inferred.
4. **[INFO] Client ordering matches the sentence; the no-timeout gap is real and disclosed (FN-u96-resent-advanceday-waits-without-timeout).** `sendAdvanceDay` (`dayActions.ts:397-429`) resolves on: an `Error` frame whose `intentId` matches (`answer(message.reason ?? message.code)`), a `CampaignEvent` of type `CampaignDayAdvanced` for the same campaign (`answer(null)`), a transport `onError`, or a synchronous throw from `sendHostIntent` — and explicitly never on a timeout (comment at `:395`: "There is no timeout: an unanswered intent leaves it pending"). On refusal, `advanceCoopDayThroughHost` (`:365-382`) returns `null` immediately after the toast with no `set()` and no `saveCampaign()` call — no date roll-forward, no partial effects, verified both by reading the function body and by the R2-refusal jest row (`record` stays `{version:1, currentDate: dateBefore}`, `dateAfter === dateBefore`). This is a real change from the baseline, which never waited at all (`emitCoopDayAdvancedEvent` on `2edfbb3b6f40f...:` fires `transport.sendHostIntent` synchronously and returns without registering any frame listener) — so the baseline could not hang on a missing frame, and this code can (a resent identical `AdvanceDay` for an already-committed day gets no answer per `campaignHostBatchCommit.ts:172-174`'s "duplicate command returns the prior receipt WITHOUT publishing"). This is already filed as FN-u96-resent-advanceday-waits-without-timeout with a recommended follow-on; I did not find a UI guard (button disable) in the owned paths that prevents a double-send, and the dashboard component that calls `advanceDay()` (`CampaignDashboard.tsx:62-65`) is out of this unit's ownership paths.
5. **[INFO] Journey edit is exactly the disclosed one line plus two comment lines.** `git diff .../e2e/coop-campaign-two-browser-journey.spec.ts` shows only: two added comment lines citing `OD-u35g-coop-outcome-rewrites-record`, and `.toBe(0)` → `.toBe(1)` at the post-destroyed-unit expectation (was line 475). `OD-u35g-coop-outcome-rewrites-record` is confirmed as U35g's ruling in `units.json` (U35g's `behavior` field: "A destroyed unit is recorded as a status change (Destroyed), not a removal"; and finding `FN-u35d-...` at line 10020: "the co-op guest's roster count includes them"), so the cited ruling is real and matches the edit's rationale.
6. **[VERIFIED] Red reproduces exactly.** Restored the baseline blobs (`git show 2edfbb3b6f40f04c641dbc8530afd5ad6df73d2a:<path>`) for all 4 product files (confirmed post-restore sha256 differs from head, e.g. route `e087d830a1...` vs head's `38f598c1c4...`), ran `npx jest src/__tests__/api/campaigns/campaignPutLiveHost.test.ts src/stores/campaign/__tests__/useCampaignStore.coopDayAdvanceOrder.test.ts` against baseline product code + head test files: **5 failed, 2 passed, 7 total**, failures at exactly the described assertions — `hostLiveBalance` expected 382500 received 380000 (R1, `:182`), `put.status` expected 409 received 200 (R3, `:237`), `hostLiveBalance` for the day row expected 378500 received 380000 (`:284`), and both R2 commit/refusal timeline mismatches (PUT lands before `intent:AdvanceDay`, matching the red receipt's captured order). Restored all 4 files with `git checkout bbdb6a07c... -- <paths>`; sha256 afterward matched the head exactly (`38f598c1c4a8...`, `dd3524f45102...`, `9ced07a13b65...`, `1bbecd420c2c...`), `git status --short` clean. Reran the same two test files on the restored head: **7 passed, 7 total**.
7. **[VERIFIED] Independent mutant (not M1-M5): "adoption publishes but does not fold."** Removed `this.state = applyCampaignEvent(this.state, event);` from `adoptSavedCheckpoint`'s fold loop (`CampaignMatchHost.ts:387-390`) while leaving `this.publish(event)` intact, so the guest still gets the checkpoint frame but the host's own in-memory state never advances. `npx jest .../campaignPutLiveHost.test.ts -t R1`: **1 failed** — `expect(afterPut.hostLiveBalance).toBe(382_500)` received `380000` (`:182`) — reproducing exactly the erasure mechanism from Finding 1 (the next host intent would still compute its absolute-balance event from the stale 380000). Restored the file via `git checkout bbdb6a07c... -- src/lib/multiplayer/server/CampaignMatchHost.ts`; sha256 afterward `9ced07a13b652b9d908f37c5ec1ee91ad0262c3a6035975177fcaa784824226c`, matching the head exactly; `git status --short` clean.
8. **[VERIFIED] Gates.** All run from the `u96-review` worktree with Node 22 and `npm_config_dry_run=true`, after `node scripts/qc/machine-idle.mjs --wait --timeout-ms 1800000` printed `MACHINE_IDLE` before the full-directory jest run. See Gates section below — every command's last line and exit code matched the local receipt's recorded results exactly.
9. **[VERIFIED] Scope and cap.** Product diff (`git diff --numstat`) is `[id].ts` +73/-3, `CampaignHostRegistry.ts` +13/-0, `CampaignMatchHost.ts` +27/-0, `dayActions.ts` +199/-115 → sum 430, under the 500 cap; 8 files total on the range, under the 15-file cap. `useCampaignStore.actions.ts` has a zero-line diff — confirmed unchanged. `git diff -w --numstat` on `dayActions.ts` gives +138/-54 = 192, versus the raw +199/-115 = 314 the receipt counts against the cap: the receipt is honest about this (`"the day body moved into advanceDayLocally and lost one indent level"`) — the raw-numstat method is what the cap and validator use, and 430 is still the correct total under that method; the whitespace-insensitive number (222 fewer lines) shows the real behavioral diff is smaller, which is a disclosure, not a discrepancy. No AI attribution or absolute machine paths found in the diff (`grep -niE "claude|anthropic|generated by|co-authored|C:\\\\Users|E:\\\\Projects|/c/Users"` on the full diff: no hits). Comment audit: every added/changed function comment I checked (`liveEntryFor`, `adoptSavedCheckpoint`, `isLiveHostSave`, `refuseForLiveSession`, the route's header block, `advanceDayAction`, `advanceCoopDayThroughHost`, `sendAdvanceDay`, `warnCoopDayAdvanceWithoutHost`) states exactly what the code does and no more — `sendAdvanceDay`'s comment even documents the no-timeout gap explicitly rather than glossing over it. One pre-existing (unedited) type comment is now slightly imprecise: `ConflictResponse`'s JSDoc still reads "The typed stale-write refusal" (`[id].ts:60-62`, unchanged bytes — not part of this diff's hunks) even though the type's `reason` union now also carries `live-coop-session`, an authority conflict rather than a stale write. This is outside the charter's "added or changed" comment rule (the JSDoc text itself wasn't touched) and is not a defect this unit introduced, but is worth a follow-on note since the type it documents changed shape.

## Gates

- `npx jest src/__tests__/api src/lib/multiplayer/server src/lib/campaign src/lib/api src/stores/campaign src/__tests__/integration/coopMutationPersistence.test.ts` — exit 0. Last lines: `Test Suites: 455 passed, 455 total` / `Tests: 5317 passed, 5317 total` (matches local receipt exactly).
- `npx tsc --noEmit` — exit 0, no output.
- `npx oxlint` — exit 0. Last line: `Found 84 warnings and 0 errors.` (matches receipt's `versus84: equal (84)`).
- `npx oxfmt --check` on the 8 changed files — exit 0. Last line: `All matched files use the correct format.`
- `npm run lint:units` — exit 0. Last line: `LINT_UNITS_PASS 100/100`.
- `npm run qc:openspec-ci:validate` — exit 0. Last line: `[qc:openspec-ci] workflowContracts=8/8 workflowJobContracts=1/1 aggregatorNeeds=19/19 protectedContexts=4 packageScripts=3/3 activeOpenSpecChanges=9 accountedActiveOpenSpecChanges=9 errors=0`.
- `node openspec/planning/2026-09-12-roadmap-completion/validate-roadmap.mjs` — exit 0. Last line: `ROADMAP VALIDATION PASSED: 75 nodes, 13 packages, 376 tasks, 40 triage rows`.
- Build, Playwright and any live server were **not** run by this lane (charter forbids it); the local receipt's own build/Playwright/live-driver runs were read and cross-checked for consistency (Findings 1 and 4) but not reproduced.

## Cap

Product: 430 of 500 lines (4 files: `[id].ts` +73/-3, `CampaignHostRegistry.ts` +13, `CampaignMatchHost.ts` +27, `dayActions.ts` +199/-115), 8 files of 15 on the whole range. Both within the unit's caps. No AI attribution, no absolute machine paths in the diff.

## Verdict rationale

The diff on the exact reviewed head reproduces the receipts byte-for-byte (file list, line counts, sha256, gate outputs) and I independently reproduced red (baseline product code + head tests fail exactly as recorded), green (head passes 7/7), and a mutant of my own choosing outside the implementer's M1-M5 set (killed, restored). The single-writer lock claim, the checkpoint-fold mechanism, the unauthenticated-identity claim, the 409 envelope shape, the closed/paused distinction, and the client's no-timeout wait are all confirmed by reading the exact cited lines rather than inferred. The two live risks the implementer already disclosed (snapshot frames never acked; a resent AdvanceDay can hang with no timeout) are real, are correctly scoped as follow-on work rather than blocking defects (neither is reachable through this unit's own new code paths under normal single-shot use, and the no-timeout pattern already exists elsewhere in this codebase for the same class of host-intent wait), and are filed as findings rather than silently absorbed. Scope, caps, formatting, lint, types and the roadmap/openspec validators are all green on the exact head. No required edits identified.
