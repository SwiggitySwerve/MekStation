# U1b Lane A Review — PR #1788

Reviewer: Lane A (cross-model, independent). Base `291eb81e05709b34c5087a7fe3ef4cfd111eedc6`, head `2e127fd693f09054d722f54affdb15c11ea3269b`.

## Verdict

**APPROVE**

## Findings

- **Diff scope (exactly 10 files, nothing under `src/`):** `git diff --name-status <base> <head>` lists exactly 10 files: `e2e/fixtures/gmTwoPlayerEvidence.ts` (M), `e2e/gm-two-player-evidence.pack.spec.ts` (A), `e2e/helpers/gmTwoPlayerAuthorityRecovery.ts` (M), `openspec/changes/harden-gm-two-player-campaign-sessions/tasks.md` (M), the three `openspec/planning/2026-09-12-roadmap-completion/evidence/u1b-{admission,red,local}-20260916.json` (A), `openspec/planning/2026-09-12-roadmap-completion/units.json` (M), `scripts/__tests__/gm-two-player-campaign-qc.test.ts` (M), `scripts/qc/gm-two-player-campaign-core.cjs` (M). 6 product/test/spec-package files + units.json + 3 receipts = 10. No `src/` path present.
- **Line count claim verified:** `git diff --numstat` gives 353 new lines (`e2e/gm-two-player-evidence.pack.spec.ts`) + insertions of 56+8+1+28+9 = 102 across the five modified non-ledger files = 455, matching the PR's claimed "353 new + 102 insertions".
- **tasks.md change is one PROGRESS bullet, no checkbox touched:** `git diff -U0 -- tasks.md` shows a single added line (`@@ -587,0 +588 @@`) — one long PROGRESS bullet under row 22.3. No `- [ ]`/`- [x]` line was added, removed, or toggled.
- **Spec fidelity — genuine strict 27-cell bundle:** `e2e/gm-two-player-evidence.pack.spec.ts` drives `openRecoverableCampaign` (real Playwright browser contexts for `gm`/`playerOne`/`playerTwo`, confirmed via `e2e/helpers/gmTwoPlayerAuthorityRecovery.ts`), arms a `page.on('websocket')` frame tap plus Playwright tracing per role, re-joins each role through the product's own `joinUntilSnapshot` path, captures a real durable-row export via a read-only SQL probe (`PARTICIPANT_SQL`), writes all 9 declared kinds × 3 declared roles = 27 cells from live-observed data (no synthetic bodies), and calls `bundle.finalize(environment)` with **no** `allowIncompleteEvidence` argument — confirmed by reading the full spec file and the `finalize` signature (`IFinalizeOptions` is optional and omitted at the call site).
- **`attach()` is additive and preserves both gates:** `e2e/fixtures/gmTwoPlayerEvidence.ts` refactors `write`/new `attach` onto one shared `record()` closure that still runs `assertNoSecrets` (binary payloads scanned as latin1) and `assertRunOwnedPath` before every write, for both text and binary paths. `SECRET_PATTERNS` and `listMissingEvidenceCells`/`finalize`'s incomplete-bundle throw are untouched by the diff — verified by diffing the full file and re-reading the unchanged sections in the head blob. The pre-existing `src/__tests__/unit/evidence/gmTwoPlayerEvidence.test.ts` (12 rows, outside `ownedPaths`, not edited) still passes, which is real evidence the refactor is byte-identical for `write()`.
- **Transcript redaction is narrow and declared:** `redactCredentials` in the new spec file replaces only 4 named key VALUES (`token`, `wireToken`, `password`, `privateKey`), leaving JSON shape intact; the row additionally asserts the artifact body does not contain the literal `wireToken` value (a property the shared regex-based `SECRET_PATTERNS` scan cannot see because the frame's quotes arrive JSON-escaped — confirmed measured/explained in the receipts, not inferred).
- **Projection non-claim is visible to a reader, not buried:** stated in the file's header docblock, inline in the `projection` artifact's own JSON body (`nonClaim: 'POST-WIRE...'`), in the tasks.md PROGRESS bullet, in `units.json.nonClaims20260916`, and in the PR body. Consistent across all four surfaces.
- **Red receipt (`u1b-red-20260916.json`):** R1 is a live `finalize` on shipped code (base commit) inside the strict `fixture-smoke` sandbox with the `allowIncompleteEvidence` argument removed as a reverted probe; `exitCode: 1`, and `evidenceIncompleteTextVerbatim` names exactly 24 cells (8 kinds × 3 roles, everything but `environment/*`), matching the stated `8 × 3 = 24` arithmetic. R2 shows the `evidence-smoke` group as `NOT_IMPLEMENTED` (exit 3) pre-PR. Tree-state block confirms the probe was reverted (`git status --porcelain` shows only ledger files after).
- **Mutants (`u1b-local-20260916.json`):** Four listed mutants each target a distinct assertion with plausible failure text — M1 (drop trace attach for one role) → `EVIDENCE_INCOMPLETE missing cells: trace/future-player-1`; M2 (relabel a role to a non-matrix literal) → `missing cells: projection/future-player-2`; M3 (drop a kind AND reopen the escape hatch) → gate bypassed, but the row's own manifest set-equality assertion still fails naming `trace/future-player-1`; M4 (neutralize the redactor) → `future-gm transcript carried no credential to redact`. The JSON-escaped-credential residual (M4a) is recorded honestly as a **survived-then-fixed** mutant with root cause measured directly (`SECRET_PATTERNS[1]` needs an unescaped quote before the colon; the nested JSON string escapes it), the fix taken (row asserts absence of the literal wire token), and an explicit `residualForASuccessor` note — not hidden, and repeated consistently in `units.json.residuals20260916`, the tasks.md bullet, and the PR body.
- **Registration and gates (rerun in a throwaway worktree at head, junctioned to the shared `node_modules`):**
  - `npx tsc --noEmit -p tsconfig.json` → clean, no diagnostics (exit 0).
  - `npx oxlint` → "Found 84 warnings and 0 errors" — matches the receipt's baseline-equality claim exactly.
  - `npx oxfmt --check` on all 5 changed non-JSON files → "All matched files use the correct format."
  - `node node_modules/jest/bin/jest.js --maxWorkers=1 --coverage=false scripts/__tests__/gm-two-player-campaign-qc.test.ts src/__tests__/unit/evidence/gmTwoPlayerEvidence.test.ts` → both suites pass, 22 tests total (10 + 12, matching the receipt's per-suite counts).
  - `SPEC_BY_GROUP['evidence-smoke']` maps to `['e2e/gm-two-player-evidence.pack.spec.ts']`; `GROUP_CATALOG`'s `evidence-smoke:27` entry is untouched by the diff (was already reserved pre-PR). `evidence-smoke` is confirmed **not** present in `RESPAWNING_GROUPS` (`Set(['restart-pack','resilience-pack','authority-recovery','authority'])`). The qc pin test's `NOT_IMPLEMENTED`-expectation loop no longer includes `evidence-smoke` (moved into the implemented-group pin), and `all` now includes the new spec file.
- **Ledger (`units.json`):** U1b's block changes from `state: "planned"` / null receipts to `state: "local-verified"` with populated `admission`/`red`/`local` `stageReceipts` (review/merge/mainProof/tick correctly left `null`), `taskKeys: []` confirmed, plus `nonClaims20260916`, `authoredLines20260916`, and `residuals20260916` folding the parent/admission rulings into the ledger entry. Byte-diffed the U1 block (lines 71–159 of both blobs) — **identical**, confirming U1's "complete" state was left untouched.
- **Roadmap validator:** `node openspec/planning/2026-09-12-roadmap-completion/validate-roadmap.mjs` → `ROADMAP VALIDATION PASSED: 73 nodes, 13 packages, 376 tasks, 40 triage rows` (exact match to the required 73/13/376/40). `--next` → `NONE-ADMISSIBLE: 0 owner-gated, 4 blocked`.
- **No AI attribution:** `git log -1 --format=%B` on the head commit and `gh pr view 1788 --json body` both checked — neither contains any AI/Claude/Codex attribution line.

## Required edits

none

reviewedHead: 2e127fd693f09054d722f54affdb15c11ea3269b
