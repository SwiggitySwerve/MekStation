# Lane A review: U17
reviewedHead: dd404f9ba9cd285ad80627281f8a0afb169aabc0
baseline: 6eff49a2d05a6d0e6db8e3b145adadf21bf122ad
reviewerModel: claude-sonnet (Agent model: sonnet)
Verdict: APPROVE

## Setup

- `git -C E:/Projects/MekStation fetch origin` — ran clean, no output (already current).
- `git -C E:/Projects/MekStation worktree add --detach .sisyphus/roadmap-completion-20260912/worktrees/u17-review dd404f9ba9cd285ad80627281f8a0afb169aabc0` — succeeded; `git log --oneline -1` in the worktree shows `dd404f9ba chore(qc): the roadmap loop's fold and park generators move into the repository`.
- No leftover `worktrees/u17` directory existed at review start (`ls .sisyphus/roadmap-completion-20260912/worktrees/` showed only `u3-diagnosis/` and `u4/`); nothing to avoid touching.
- `git merge-base --is-ancestor 6eff49a2d... dd404f9ba...` exits 0 — baseline is an ancestor of the reviewed head, confirmed.
- node_modules junctioned via PowerShell `cmd /c mklink /J <worktree>\node_modules E:\Projects\MekStation\node_modules` — junction created.
- `export PATH=/c/Users/wroll/AppData/Local/nvm/v22.22.0:$PATH` then `node --version` → `v22.22.0`. `npm_config_dry_run=true` exported before every npm/npx call; no `npm install`/`ci`/`prune`/build/Playwright/commit was run anywhere; the root checkout was never touched (all commands used `-C`/`cwd` inside the review worktree, except read-only `Read`/`git diff --stat` calls against the root checkout, which mutate nothing).
- All generator invocations in this review ran against throwaway temporary copies of the ledger directory created under the *review worktree's* `openspec/planning/`, never against the root checkout's or the review worktree's live `openspec/planning/2026-09-12-roadmap-completion/units.json`. Both temp copies (`lane-a-fidelity-*`, `lane-a-full-*`) were deleted before finishing; `git status --porcelain` in the worktree is clean.
- Cleanup performed at the end of this review (see Gates section closing note).

## Files on the range

`git diff --numstat 6eff49a2d... dd404f9ba...` (run from the root checkout, read-only):

```
207  0  openspec/planning/2026-09-12-roadmap-completion/evidence/u17-admission-20260917.json
240  0  openspec/planning/2026-09-12-roadmap-completion/evidence/u17-local-20260917.json
 80  0  openspec/planning/2026-09-12-roadmap-completion/evidence/u17-red-20260917.json
  2  0  package.json
455  0  scripts/__tests__/roadmap-loop-scripts.test.ts
249  0  scripts/qc/roadmap-ledger-lib.mjs
 84  0  scripts/qc/roadmap-unit-fold.mjs
126  0  scripts/qc/roadmap-unit-park.mjs
```

8 files changed, 1443 insertions, 0 deletions, all pure additions. 5 non-receipt files touched (`package.json`, the three `scripts/` files) plus 3 receipts = 8 total — inside the 15-file cap. Everything is inside `scripts/qc`, `scripts/__tests__`, `package.json`, or `openspec/planning/2026-09-12-roadmap-completion/evidence/` (the standard receipt path). Nothing outside that set. `package.json`'s diff is exactly two added lines (`roadmap:fold`, `roadmap:park`), no removals.

Commit `dd404f9ba` author/committer: `Wes Rollings <wrollings@gmail.com>` on both lines; no `Co-Authored-By` or other AI-attribution trailer in the commit message. PR #1837 body (`gh pr view 1837`, read-only) also carries no AI attribution, `headRefOid` equals the reviewed head, `baseRefName: main`.

## Findings

1. **[Informational] Fidelity reproduced independently — key-for-key/type-for-type match, one documented extra key.** On a full temporary copy of the real ledger directory (`cp -r openspec/planning/2026-09-12-roadmap-completion → openspec/planning/lane-a-full-250198` inside the review worktree), I seeded a fabricated `planned` unit `ZLA1` and three lane receipts, then ran `node scripts/qc/roadmap-unit-fold.mjs --unit ZLA1 --date 20260917 --receipts-dir <evidence> --red-summary ... --local-summary ... --ledger-dir <tmp>`. Result: exit 0, `unit.state` → `local-verified`, `unit.baseline` set from the admission receipt, and `validator: ROADMAP VALIDATION PASSED: 75 nodes, 13 packages, 376 tasks, 40 triage rows exit 0` printed by the fold itself. Comparing `ZLA1.stageReceipts` against `U14.stageReceipts` on main (read from the real `units.json`): `red` and `local` have identical key sets (`path`, `at`, `summary`) with matching types; `admission` has U14's key set (`path`, `baseline`, `at`, `decision`) plus exactly one extra key, `parentDecisionRecordedHere: true` — the same difference the pin's own test asserts explicitly (`roadmap-unit-fold.mjs`'s doc comment and the reference `unit-fold-stages-1-3-v3.js` both document this as the deliberate v3 behavior: the admission receipt file is never rewritten, and the parent decision is recorded on the unit entry instead). No unexplained drift found.
2. **[Informational] Admission receipt never rewritten — confirmed by hash.** `sha256sum` of the fabricated unit's admission receipt file before the fold (`9680637e...484d1c`) and after (`9680637e...484d1c`) are identical. The fold's `foldStages` in `roadmap-ledger-lib.mjs` only `fs.copyFileSync`s a receipt into the evidence dir when `path.resolve(file) !== target` (i.e., when `--receipts-dir` differs from the ledger's own evidence dir); when they're the same path, as in my probe and in normal use, the file is never touched. Confirmed by direct hash comparison, not by reading the code alone.
3. **[Informational] Fold refusals confirmed, ledger untouched.** Re-running the fold on the now-`local-verified` `ZLA1` refuses: `UNIT_NOT_PLANNED: ZLA1 is local-verified, not planned`, exit 1. A second fabricated unit (`ZLA2`, `planned`, admission+local receipts present, red receipt deliberately omitted) refuses: `RECEIPT_MISSING: .../zla2-red-20260917.json does not exist`, exit 1, and `ZLA2.state` stayed `planned` afterward. `units.json`'s sha256 was identical before and after both refusals plus the safety-probe refusals in finding 4 (`97da51bb...d86530d` both times).
4. **[Informational] Safety refusals confirmed for both generators.** `--bogus-flag zzz` on the fold → `INVALID_ARGUMENT: unknown flag --bogus-flag` (exit 1). Fold with `--date` omitted → `INVALID_ARGUMENT: --date is required` (exit 1). Park with `--pr-head notahexsha` → `INVALID_ARGUMENT: --pr-head notahexsha` (exit 1). All three refusals left `units.json`'s hash unchanged. `parseFlags`/`requireFlags` refuse before any ledger read in these cases (confirmed by the error firing before any file write). The library's only subprocess call, `runValidator` in `roadmap-ledger-lib.mjs:141-155`, uses `spawnSync(process.execPath, [fixedPath, ...extraArgs], {...})` with an array argument list and no `shell: true` — no untrusted string is ever concatenated into a shell command; `grep -n "exec(\|shell:"` over the three product files found only the single `spawnSync` import/call.
5. **[Informational] Park reproduced against U12's shape.** Fabricated unit `ZLA3` (`planned`, three receipts with `local.status: 'BLOCKED'`) run through `roadmap-unit-park.mjs` with a fabricated finding: exit 0, both `validator: ROADMAP VALIDATION PASSED ... exit 0` and `--next: NONE-ADMISSIBLE: 0 owner-gated, 12 blocked exit 3` printed (matching `u12-park.js`'s dual validator+`--next` call, which `roadmap-unit-park.mjs`'s `printValidator(ledgerDir, { withNext: true })` reproduces). Resulting `blocked20260917` block keys (`cause`, `parkedPr`, `nextIndependentAction`, `proof`) are identical in name and count to U12's on main; the new finding's keys (`at`, `class`, `evidence`, `foundBy`, `id`, `ownership`, `recommendedSuccessor`, `status`, `summary`) are identical in name and count to `FN-u12-root-preinstall-runs-after-reify` on main. `proof` in the new code is always the fixed string `evidence/<lc>-local-<date>.json findingsReportedNotFixed` (no bracketed sub-finding id appended, unlike U12's hand-written `findingsReportedNotFixed[F1] (npm source lines cited with sha256)`) — a cosmetic difference from ad hoc prose the reference script's author added by hand, not a shape mismatch; the required keys and their types match.
6. **[Informational, confirms charter's split accounting] Exports independently recounted.** `grep -c '^export (const|function|class)'` over `roadmap-ledger-lib.mjs` → 26 exports, matching the receipt's claim. The union of names imported by `roadmap-unit-fold.mjs` (11 names) and `roadmap-unit-park.mjs` (14 names) is exactly 14 unique names (`DATE8, HEX40, UNIT_ID, foldStages, loadUnits, nowIso, parseFlags, printValidator, readJson, refuse, requireFlags, resolveLedgerDir, runCli, saveUnits`), matching the receipt's "14 are called by the fold and the park" claim exactly. The remaining 12 split into 10 used only internally by the library (`REPO_ROOT, LEDGER_RELATIVE, DEFAULT_LEDGER_DIR, LoopScriptError, posix, writeJson, unitsFile, evidenceDirOf, findUnit, runValidator` — each traced by grep to a caller inside `roadmap-ledger-lib.mjs` itself) and exactly 2 (`sha256`, `lastLine`) with no caller anywhere in the three shipped product files (confirmed: `grep -n sha256` over all three `.mjs` files hits only `sha256`'s own definition; `lastLine` is absent from `roadmap-unit-fold.mjs` and `roadmap-unit-park.mjs`).
7. **[Informational, answers charter Q3] `sha256`/`lastLine` are dead code for this head, justified by the staged successor.** Reading the staged U17b modules' import lists (read-only, `.sisyphus/roadmap-completion-20260912/u17b-staging/`, not part of this head): `roadmap-unit-closure.mjs` imports both `sha256` and `lastLine` from `./roadmap-ledger-lib.mjs`; `roadmap-main-proof.mjs` imports neither. All names both staged modules import (`DATE8, HEX40, LEDGER_RELATIVE, REPO_ROOT, UNIT_ID, evidenceDirOf, findUnit, lastLine, loadUnits, nowIso, parseFlags, posix, printValidator, readJson, refuse, requireFlags, resolveLedgerDir, runCli, saveUnits, sha256, writeJson`) exist in the current library's export list — none is missing. `grep -rn "u17b-staging\|roadmap-unit-closure\|roadmap-main-proof"` over `scripts/qc`, `scripts/__tests__`, `package.json` in this head finds only doc-comment mentions inside `roadmap-loop-scripts.test.ts` (naming what was staged and disclaiming it) — no product code imports or references the staged files. So: dead code for this head (nothing here calls either export), but a correct anticipation of the successor's real needs rather than speculative/unjustified surface.
8. **[Low, matches receipt's own F5] Temp-ledger-copy leak on a killed pin run is a pre-existing, disclosed risk, reproduced.** `roadmap-loop-scripts.test.ts`'s `beforeAll` creates its temp ledger via `fs.mkdtempSync(path.join(repoRoot, 'openspec/planning', 'u17-pin-'))` and only removes it in `afterAll`; a killed/crashed jest process would leave a `u17-pin-*` directory under the real `openspec/planning/`, visible in `git status`. I hit the identical structural need myself (my first probe copy, minimal-file, also had to live under `openspec/planning/` for `validate-roadmap.mjs`'s `path.resolve(scriptDir, '..', '..', '..')` repo-root derivation to resolve correctly) and manually removed both of mine after use. This is exactly finding F5 in `evidence/u17-local-20260917.json`, self-disclosed with a rejected-for-now mitigation ("a `u17-pin-*` .gitignore entry... neither was added because .gitignore is not an owned path of this unit"). Independently reproduced, not newly discovered; no `.gitignore` change is in scope for this unit and I did not make one.
9. **[Low] Only one of the six "green-first" library-level pin cases has a mutant behind it in the receipts; the other five are asserted, not mutant-proven, as load-bearing.** The `roadmap-ledger-lib` describe block's six cases are the *only* pin coverage of `parseFlags`'s bare-boolean path, and of the refusal branches in `parseFlags` (unknown flag, valueless flag), `requireFlags` (missing flag) and `resolveLedgerDir` (no `units.json`) — none of these five branches is exercised by any fold/park CLI-level test in this file (confirmed by reading all fold/park test cases: none passes an unrecognized or boolean flag, an empty required flag, or a bad `--ledger-dir`). Only mutant M6 (parseFlags skips an unknown flag) is recorded against this block, killing 1 of the 6 cases. The other five (parseFlags happy path with a boolean; refuses valueless flag; refuses missing required flag; refuses ledger dir with no `units.json`; the two `lastLine` assertions) have no corresponding mutant in `evidence/u17-local-20260917.json`'s `mutants` array. The PR body's claim that these six cases were "made load-bearing by a library mutant" is true only for the one M6-covered case; for the rest, "load-bearing" is a reasonable inference (they are each the sole test of a real refusal branch that fold.mjs/park.mjs's own `parseFlags`/`requireFlags`/`resolveLedgerDir` calls could hit on a real bad invocation) but is not itself proven by a reproduced mutant. Not a defect — the tests do guard real branches — but the receipt's "made load-bearing by a library mutant" phrasing overstates what was actually mutant-tested.
10. **[Informational] Independent mutant reproduction.** Mutation: `scripts/qc/roadmap-unit-fold.mjs` line `unit.state = 'local-verified';` → `unit.state = 'complete';` (chosen independently, not copied from the receipt's mutant list before I ran it — the result was then cross-checked against the receipt's M3 entry and matches exactly). `sha256sum` before: `6aff2d01...48fdf156`(truncated: `6aff2d0133652c37f61b08c8780523a4ea9b40be2abb942f5dad7f95b148ad36`). Pin result: `Tests: 2 failed, 11 passed, 13 total` — the two failures are `roadmap-unit-fold › folds the three lane receipts and leaves the unit local-verified` (`expect(unit.state).toBe('local-verified')` got `'complete'`) and `roadmap-unit-fold › refuses a unit that is not planned` (`UNIT_NOT_PLANNED: U91 is complete, not planned` no longer contains the substring `'local-verified'` the test expects). File restored with `sed`; `sha256sum` after: `6aff2d0133652c37f61b08c8780523a4ea9b40be2abb942f5dad7f95b148ad36` — identical to before. Re-ran the pin after restoring: `Tests: 13 passed, 13 total`. This independently matches the receipt's own M3 entry (file, mutation, `2 failed, 11 passed, 13 total`, same two killedBy names, `restoredSha256Matches: true`) exactly.
11. **[Informational] No absolute machine paths.** `grep -rniE "C:\\\\Users|E:[\\\\/]Projects|/home/|/Users/wroll"` over the five changed product/test files returns no matches (exit 1 = no match). `REPO_ROOT` is derived from `fileURLToPath(import.meta.url)`, and the pin derives its paths from `process.cwd()`; the only literal paths in the product files are repo-relative (`../..`, `evidence/...`, the ledger-relative constant).
12. **[Informational] Serialization with U13 honored.** U17's admission baseline (`6eff49a2d05a6d0e6db8e3b145adadf21bf122ad`) is the commit immediately after U13's own closure-documentation commit merged (`git log --oneline`: `6eff49a2d docs(roadmap): U13 closure, review through tick, unit complete (#1836)` sits directly on top of `cfa943780 chore(qc): preserve a run's logs with sha256 before a worktree is removed (#1835)`, U13's product merge). U17 did not start against a stale or concurrent baseline while U13's `scripts/qc`/`scripts/__tests__`/`package.json` work was still in flight, consistent with council decision C.5's serialization instruction for units sharing those paths.

No blocking or required-edit findings. All numbered items above are informational or low-severity and already self-disclosed in the unit's own receipts (F1-F7 in `evidence/u17-local-20260917.json`) except items 9 and 12, which are original observations from this review and do not change the verdict.

## Gates

All run in the review worktree, Node `v22.22.0`, `npm_config_dry_run=true` exported:

| Gate | Command | Last line | Exit |
|---|---|---|---|
| Pin | `npx jest scripts/__tests__/roadmap-loop-scripts.test.ts` | `Tests:       13 passed, 13 total` | 0 |
| Typecheck | `npx tsc --noEmit` | (no output) | 0 |
| Format | `npx oxfmt --check` on the 5 changed files | `All matched files use the correct format.` | 0 |
| Units lint | `npm run lint:units` | `LINT_UNITS_PASS 100/100` | 0 |
| OpenSpec CI quality | `npm run qc:openspec-ci:validate` | `[qc:openspec-ci] workflowContracts=8/8 workflowJobContracts=1/1 aggregatorNeeds=19/19 protectedContexts=4 packageScripts=3/3 activeOpenSpecChanges=9 accountedActiveOpenSpecChanges=9 errors=0` | 0 |
| Roadmap validator | `node openspec/planning/2026-09-12-roadmap-completion/validate-roadmap.mjs` | `ROADMAP VALIDATION PASSED: 75 nodes, 13 packages, 376 tasks, 40 triage rows` | 0 |

All six gates match the counts/verdicts claimed in `evidence/u17-local-20260917.json`'s own `gates` array.

**Cleanup performed at the end of this review:** removed both temporary ledger copies (`openspec/planning/lane-a-fidelity-249828`, `openspec/planning/lane-a-full-250198`) from the review worktree; `git status --porcelain` and `git diff --stat` in the worktree are clean (matches the pristine reviewed tree). Node_modules junction removed via `[System.IO.Directory]::Delete('<worktree>\node_modules')` and the worktree removed via `git worktree remove --force <worktree>` immediately after this file was written (see final response for confirmation these ran without error).

## Cap

`wc -l` in the review worktree: `roadmap-ledger-lib.mjs` 249, `roadmap-unit-fold.mjs` 84, `roadmap-unit-park.mjs` 126, `roadmap-loop-scripts.test.ts` 455. `git diff --numstat` confirms the same four counts plus `package.json` +2/-0. Product total: 249+84+126+2 = 461 (claimed 461, matches). Pin total: 455 (claimed 455, matches). 461 is inside the 500-line cap by 39; 7 files touched (4 non-receipt + 3 receipts) is inside the 15-file cap.

Six shared-library ("green-first") cases: see Finding 9 — five of the six guard real refusal/parsing branches that `roadmap-unit-fold.mjs`/`roadmap-unit-park.mjs` actually call (`parseFlags`, `requireFlags`, `resolveLedgerDir`) and that no CLI-level test in this file otherwise exercises, so they are load-bearing in the sense that removing them would silently drop the only coverage of those branches; only one of the six (the unknown-flag refusal) has a reproduced mutant behind it in the receipts. The sixth case (`lastLine`) tests an export not called by either shipped CLI, load-bearing only for the not-yet-shipped U17b successor.

Scope check: nothing outside `scripts/qc`, `scripts/__tests__`, `package.json` (exactly 2 added lines, 0 removed) and the three `evidence/u17-*-20260917.json` receipts. No AI attribution in the commit message or the PR #1837 body. The receipts (`u17-admission`, `u17-red`, `u17-local`) describe the split (1121 lines measured pre-split → 461 shipped + 660 staged), the U17b staging location, and findings F1 through F7 exactly as the charter describes; I independently re-derived the export-count arithmetic in Finding 6 and the split-line arithmetic here rather than trusting the receipt's own numbers.

## Verdict rationale

Every claim in the unit's own receipts that this review could independently probe — the fold's fidelity against U14's stage-receipt shapes, the admission receipt's immutability, both generators' refusal behavior, the six required gates, the 461/455 line counts, the export-usage arithmetic (14 direct / 10 internal / 2 unused), the absence of absolute paths and AI attribution, and one independently chosen mutant — reproduced exactly as claimed. The two findings this review adds beyond what the unit already disclosed (Finding 9: five of six library-level cases are asserted rather than mutant-proven load-bearing; Finding 12: serialization with U13 confirmed) are informational/low severity and do not indicate a defect in the shipped behavior. The split from U17 to U17b is honest: nothing in this head references or claims the staged closure/main-proof modules, the receipts say so explicitly and repeatedly, and the two currently-dead exports are a correct, minimal-diff anticipation of the successor rather than unused surface invented speculatively. No forbidden move (narrowed task row, edited admission snapshot, self-approved Lane B, flipped production flag, ticked without a tick receipt) is present — this unit holds no task keys and made no such edits. Verdict: **APPROVE**.
