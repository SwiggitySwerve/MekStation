# Roadmap completion handoff (published, 2026-09-15)

Prior handoffs preserved under .sisyphus/roadmap-completion-20260912/handoff-20260915/. Authoritative state: roadmap.json (nodes, receipts, PR/merge/main-proof records, ownerDecisions, infraNotes, ledgerReconciliation, taskReconciliationSnapshot, cleanupInventory, completionStatement20260915). PROGRESS.md carries the dated checkpoints. Root checkout dirty and preserved (after the user-directed pull and the post-publication fast-forward: 44 entries outside the planning directory, root node_modules untouched). origin/main at refresh: b668d659a49fcd1aba9f08b7fafdb6f2eefadf52. Open PRs: only the accounting PR carrying this handoff. Owned worktrees: main-proof (removed after the accounting proof).

## Terminal state
The admitted roadmap is complete at the tier agents can reach. 86 PRs (#1667-#1757) merged today as single-parent squashes, each with a merge receipt and an exact-main proof receipt under evidence/ (main-proof-index-20260915.json indexes them). Six reconciliation batches merged; 179 of the 376 admitted task rows are checked on main (4 belong to the archived tab-precedence package), and every open row that has a main-verified receipt carries a NOTE naming the unmet clause.

Node states (73): 15 main-verified, 8 complete, 1 archived, 13 main-verified except an owner gate, 2 main-verified except an owner ruling, 1 measured-verified except an owner gate, 1 main-verified with residuals, 1 local-verified, 1 local-verified with publication owner-gated, 1 owner-blocked, 1 blocked, 28 planned. Every planned node depends on an owner decision: R2.camp-8 (three witnesses, non-author APPROVED review), R3.activation (cross-stream effect receipts), the production journal-authority flag flip and the single-player seat (R2.journey, R5.E1-E10, R6.B4-umbrella, R7, R8, R9.triage/close), S7-d (OD-journal-writer-authority), OD-rewind-branch-reader.

## What landed today (by package)
- Campaign authority (design-campaign-authority-and-sync): P0b S1-S3; 6.2a prefixes and source-row stack; 6.3 rows; 6.1 fence (A+B); 6.4 CAS bridge (A+B); client crash-window stack; host live fold (single squash #1733); cutover CO0 marker wiring, CO1/CO2 parity driver and rollback surface, CO3 two-process convergence and durable restart proof (#1747-#1750); authority-live revocation and redaction rows (#1753-#1755) and the F-LIVE-1 fix that detaches a revoked seat's live socket (#1756). Owner-gated: 6.2b routing, the flag flip, single-player seat, OD-launch-head-gate.
- Journal cutover (adopt-combat-journal-cutover-and-gm-rewind): S3b, S4, S5, S6 stack, S7-a mirror strip and create-path seeding (S6-R1 closed), S7-b marker retirement (task 1.3 flipped), S7-c checkpoint-door routing (S4-R4 closed). Owner-ruled: S7-d; residuals S7b-Q6, S7a-Q2/Q3/Q4, S7-c OQ-1/OQ-2/OQ-4, OQ-2 marker table.
- Saved custom-unit combat: S5/S6 rows, recovery-redaction row (#1726), production combat launch browser proof (#1731; line 8 flipped). Owner decision: OD-rewind-branch-reader for the rewind clause.
- CAMP: command-level receipts for every wave incl. camp-01b/c/d re-run on main after the guest share-panel fix (#1720); controller-registered and PR-provenanced tiers owner-blocked.
- Roster damage (R9.roster-damage): maxima-derived damage bar and source-authority population (#1734/#1735); spec migration item 3 closed.
- CI reliability: selected-paper print settle (#1741) and fixture-port range (#1742); seventeen consecutive PRs without a rerun afterwards.
- Reconciliation batches 1-6 (#1718, #1727, #1736, #1743, #1752, #1757).

## Proof limits (actual)
- Exact-main proofs: blob equality of every merged file against the reviewed head, scoped jest, tsc, the four openspec validators; runtime proofs (E2E standalone build plus Playwright on the merge commit) for #1731, #1741, #1750, #1755, #1756. Scoped jest is not the full battery.
- Two-process proofs use a second server process on the same SQLite file and a directly constructed journal-native marker; no shipped path puts a campaign on journal authority (flag hardcoded false). Scoped read proven for one seat; typed socket close (Q1) and F-LIVE-2 (RemoveParticipant 422 host-only via /commands) remain open.
- CAMP receipts are command-level only. Roster-card and acknowledged-refresh proofs are jsdom-level. The flake fixes rest on CI log evidence and code reading; the races do not reproduce on this 16-core machine.
- Receipts written before the afternoon use older field names (commitTested/prReference); the parent self-review verified ancestry, single-parent squash, no AI attribution and receipt presence for all 86 merged PRs independently of the schema.

## Owner decisions preserved (roadmap.json ownerDecisions; none worked around)
Single-player identity/seat (6.2b, OB-2, flag flip); CAMP non-author review and three witnesses; R3.activation 2.4-2.7; R7/R8 D8 order; OD-flush-mechanism; OD-proof02-empty-cause-graph; OD-camp00-controller-env; OD-journal-writer-authority (S7-d); OD-rewind-branch-reader; OD-launch-head-gate (/head answers no-authoritative-stream for a journal-only campaign; launch gate ungated). Recorded, not fixed: fire-and-forget terminal archive writes can double-fire; host F-R1 in-mount reconnect cursor; #1724 refreshAfterCommittedCommand as a second writer (ordering owed at 5.7); cutover CLI untyped/unlinted; S4-N1 consult DB handle; RE-3 requestActivity unconditional .json().

## Document publication (done)
The roadmap planning directory and the customizer spec reconciliation audit are published: #1758 and #1761 merged as reviewed prefixes, then the user directed one consolidated PR for the rest (OD-publication-single-pr), merged as #1776 (b668d659a49fcd1aba9f08b7fafdb6f2eefadf52) from the sixteen reviewed heads with the sixteen superseded PRs closed. #1758 and #1761 have sonnet review, premerge and merge receipts; #1776 has a sonnet review receipt (required edit applied in the accounting PR), a merge receipt carrying the parent's content guard (it was merged directly by the parent once its checks passed, so it has no premerge receipt), and all three have exact-main proofs; the roadmap validator passes on the combined merge commit with its full input set. Seven planning raw logs and fifteen audit raw logs stay gitignored and are identified by sha256 (evidence/r0-local-evidence-hashes-20260915.json). Publication events not in the exact payload: DELIVERY.md's state enum is unchanged and the ledger's compound states were normalised into state plus stateQualifier; three archive-path links in the audit inventory and overview were corrected to the verified archive paths; six CRLF receipts are stored LF per .gitattributes with both hashes in their batch manifests. The customizer-browser commit 77d9a9480 on codex/roadmap-customizer-browser-20260912 remains unpublished and unretried (separate approval gate).

Worker routing note: the Cursor Agent Grok 4.6 route could not run shell or git in this environment (every shell invocation rejected; one run looped on connection loss), so its twelve review placeholders are retained as evidence only and all reviews ran on sonnet lanes.

## Cleanup performed (identity-verified at execution; logs cleanup-pass1..4-20260915.log and cleanup-publication-20260915.log)
Removed 22 owned worktrees (every branch PR-merged or content byte-identical on main; branch-integrity-repair's uncommitted 2026-09-13 draft preserved as cleanup-20260915/branch-integrity-repair-uncommitted.patch first); deleted 70 merged local codex/roadmap-* branches; unlinked five node_modules junctions without touching their targets. Retained on purpose: codex/roadmap-r2-authority-host-h0-20260915 (remote and local, red-first evidence, #1732 closed superseded), nine backup/* and one tmp/* snapshot refs, seven superseded draft branches whose published content landed via rebuilt stacks, and codex/roadmap-r2-p1-62b-accept-routing-20260915 (owner-blocked 6.2b draft). Non-owned worktrees (C:/tmp/*, ~/.codex/worktrees/*, E:/CodexWorktrees/*, E:/Projects/mekstation-l*) untouched. Stashes untouched. Publication cleanup: 16 superseded PRs closed; worktrees publish, publish-b removed; 14 owned publication branches deleted after verifying every added file byte-identical on main; kept: codex/roadmap-r0-publish-ledger-20260915, codex/roadmap-r0-publish-progress-20260915.

## Environment notes carried forward
NEXT_PUBLIC_* are inlined at build time (the pre-commit hook's bare build replaces an E2E bundle; rebuild before any post-commit live run). Husky is absent in fresh worktrees until `npx husky`. A red-first test-only PR cannot pass the protected checks (ship it inside the product PR with the harness-floor arithmetic). Rebuild stacked children onto main by cherry-pick to avoid add/add conflicts after a squash. Cursor Agent Grok 4.6 route: `cursor-agent -p --model grok-4.6 --trust --output-format text` (its sandbox blocks git fetch/gh network calls; suitable for local read-only indexing).
