Every PR in this change MUST stay under 1000 non-generated changed lines and 15 files, own one behavior seam, and split through an OpenSpec update before implementation if the cap cannot be met. (Cap follows wave-map rule 7; line cap raised 500 -> 1000 on 2026-08-28.)

Work-path trace: this leaf follows `add-authority-audit-and-privacy-proof`, may proceed independently of campaign adoption, and must finish before `add-cross-stream-effect-receipts` in `../harden-gm-two-player-campaign-sessions/event-history-wave-map.md`.

## 0. Privacy-Gate Admission — Pre-Implementation Receipt

- [x] 0.1 Before PR 1, verify on fetched exact main that `add-authority-audit-and-privacy-proof` is synced and archived, its task 10.4 and post-merge terminal evidence pass, its active ledger entry/directory are absent, and its merged branch/worktree are pruned. Record the predecessor merge SHA and fail closed on missing or contradictory evidence.
  - Receipt (2026-08-24, admission PASSES on exact main `63ebb57e6`): `add-authority-audit-and-privacy-proof` is archived at `openspec/changes/archive/2026-08-22-add-authority-audit-and-privacy-proof/` and its ACTIVE directory is absent; its task 10.4 ("After merge, rerun exact-main evidence, record authority/store/reload proof, and prune the merged branch/worktree before any combat or campaign journal cutover") is checked, and the archived leaf carries 0 open tasks; no `*authority-audit*` or `*privacy-proof*` branch or worktree remains. Predecessor merge SHA recorded: **`fb3ccd16d`** (`docs(openspec): archive add-authority-audit-and-privacy-proof (#1312)`). Each of the five conditions was checked against the working tree rather than read off the wave map, which is a claim rather than evidence.
- [x] 0.2 Before PR 1, re-review every PR seam in this file against the then-current codebase for the 500-line/15-file cap and one-behavior-seam rule, and re-confirm seam ownership against `../harden-gm-two-player-campaign-sessions/event-history-wave-map.md`; if any seam no longer fits or ownership has drifted, split or reassign it through a spec-only OpenSpec update before implementation, and fail closed on unresolved ownership conflicts.
  - Receipt (2026-08-24): re-reviewed PR 1's seam against the current codebase before implementing. `IMatchStore.ts` has NO batch, journal, expected-revision, or command-identity surface today (grep for all four returns nothing), so PR 1 is greenfield on that boundary rather than an adaptation. Measuring the implementation proved the seam does NOT fit the cap — 563 lines across 4 files — so it is split into PR 1a/1b above through this spec-only update BEFORE review, per the no-waiver rule. Ownership re-confirmed against `../harden-gm-two-player-campaign-sessions/event-history-wave-map.md`: this leaf owns umbrella section 4 and the combat portions of 5, 7, 14, and 24; no drift found. Campaign adoption (leaf 5, `design-campaign-authority-and-sync`) completed 26/26 on 2026-08-24 and is independently gated, so it does not block this leaf.

## 1. Atomic Match Store Contract — PR 1

> Seam split (2026-08-24, required by the 500-line/15-file cap in this file's preamble and wave-map rule 7 — there is no waiver path). Implementing 1.1-1.2 as one PR measured **563 non-generated changed lines across 4 files**, so the seam is split before review into:
> - **PR 1a — the contract.** `matchCommandBatch.ts` (batch/receipt/result types, command fingerprint, contiguity rule) plus the optional `appendCommandBatch` capability on `IMatchStore`. Owns the contract-visible seam "a command has a stable identity and a contiguity rule", with pure tests. No store behaviour changes.
> - **PR 1b — the durable adapter.** `DurableMatchStore.appendCommandBatch`, the `mp_command_receipts` table, and the RED store tests from 1.1. Owns the behaviour seam "a command commits atomically or not at all".
> Task 0.2's pre-implementation seam re-review is what surfaced this; 1.3's legacy import/baseline tuple is re-measured against the cap before PR 1b.


- [x] 1.1 Add failing `IMatchStore`/`DurableMatchStore` tests for multi-event atomicity, no-gap revisions, event identity, command retry, and restart.
- [x] 1.2 Adapt the match store to the journal batch API while preserving current match metadata and legacy completed-log reads.
  - Receipt (2026-08-24, PR 1a #1342 + PR 1b): the contract seam and the durable adapter landed as two PRs after the combined diff measured 563 lines against the no-waiver 500 cap. IDENTITY covers command id, actor, starting revision, and every event id AND type - a retry reproduces all of them, a different command reusing the id does not, which is what makes `integrity-conflict` a distinct answer from `duplicate-command` rather than a silent overwrite that would attribute one player's command to another. CONTIGUITY names the offending revision rather than failing vaguely, and rejects a REPEATED revision the same way as a skipped one (two events claiming one place in history are equally unreadable). ATOMICITY is one SQLite transaction covering the identity lookup, revision check, every INSERT, the receipt, and the `updated_at` bump; identity is checked FIRST because a retry arriving after the stream moved is still a retry, and calling it a revision conflict would send the caller to rebuild state it already holds. **A VACUOUS TEST WAS CAUGHT HERE:** the original atomicity row offered an out-of-order batch and asserted nothing was written - it passed WITH THE TRANSACTION REMOVED, because the pure contiguity guard rejects that batch before the insert loop runs. It proved the guard, not the rollback. Replaced with both rows, each labelled: an out-of-order row that states in-file that it is NOT an atomicity proof, and a real rollback row where a well-formed batch fails PARTWAY through the loop (second event carries an unserializable payload), which also asserts no receipt survives - a receipt without its events would make a later retry look like a duplicate and silently skip work that never happened. Falsified 1:1: removing the transaction turns ONLY the rollback row red. Verification: tsc clean; oxlint 0 errors; batch suite 8/8, contract suite 11/11; multiplayer battery 68 suites / 481 tests; FULL unit suite 2404 suites / 33001 passed / 0 failed. NON-CLAIM: 1.3 (legacy event import with source identities plus the immutable baseline tuple) is NOT included - it is re-measured against the cap and lands separately; `InMemoryMatchStore` is untouched, so it still has no command boundary and callers there keep using `appendEvent`.
- [x] 1.3 Import retained legacy events with source identities and explicit baseline metadata for any missing prefix; persist an immutable baseline tuple containing stream/branch/revision/digest/effective-generation identity.
  - Receipt (2026-08-28, PR #1426 - the EVENT half; the TUPLE half was migration 18, receipted under 1.2): `importLegacyMatchEvents` copies a retained log into the durable match store under three laws, each mutation-proven and independently re-verified. NEVER INVENT A PREFIX: a log retained from revision K imports exactly K onward - the backfill-from-zero mutation reds as `non-contiguous` because the expected revision was still K, the guard catching the fabrication rather than the test knowing about it. IMPORT ONCE: a completion marker inside the same transaction makes a second import a typed `already-imported` answer, count unchanged. ALL OR NOTHING: one transaction covers events, source identities, and the marker; unwrapping it reds both the atomicity row and the uniqueness row (a leftover event INSERT surviving its source-row failure - two visible corruptions from one dropped transaction, which is why it is one transaction). Every imported event carries an `imported-legacy` source identity: adoption is visible in the record, never disguised as journal-native history. The control row proves `appendCommandBatch` continues at the right revision with the contiguity guard intact after an import. PLACEMENT, because it was a decision: events land in the match store's own revision space (`mp_match_events`), beside `appendCommandBatch` and the baseline - the shared `event_journal` cannot represent a missing prefix (an empty stream must start at revision 1), and an import must not become a third writer re-litigating this leaf's 1.1/1.2 architecture. Gates: typecheck 0; oxlint 0 errors / 77 warnings; persistence+multiplayer battery 106 suites / 834 tests; full suite 2442 suites with a single simulation perf-budget row over threshold under parallel worker load and green on a quiet re-run. NOT DONE HERE, per the 1.2 receipt's own boundary: production cutover does not call the import - task 4.x owns the cutover readers; `InMemoryMatchStore` still has no import path, matching its absent command boundary.

  - Progress (2026-08-25, STAYS OPEN — the baseline TUPLE half is done; the EVENT-IMPORT half is not): `match_authority_baseline` (SQLite migration 18) persists exactly the tuple D4 names - `(streamType, streamId, branchId, revision, digest, effectiveGeneration)` - plus the two facts that keep it honest: a `source` label and the `first_retained_revision` the log actually carries. TWO RULES DO THE LOAD-BEARING WORK. **Never invent a prefix**: a retained log that does not begin at the start of the stream is recorded as a `legacy-baseline` with the first surviving revision, because filling the gap would put events into history that nobody committed and every later digest would then agree with a past that never happened. **Never rewrite a baseline**: a second import READS the stored tuple and returns it unchanged, and the table has no UPDATE path at all - the primary key makes the second write fail rather than replace. Immutability that lives only in a code path is a convention; this is a constraint, and it is proven against real SQLite rather than asserted. The digest is over IDENTITY (revision + event type), not payload: two imports of the same retained log must agree, and a payload whose serialisation shifts between versions would otherwise make the same history digest differently and look like tampering. An empty retained log writes NOTHING and returns `empty-log` - deliberately not a revision-0 baseline, which would claim a revision that was never committed. An event with no integer sequence THROWS rather than defaulting to 0, because treating it as 0 silently claims it is the first event. Falsified 1:1 six times, each neutralisation reddening exactly one row and only that row: the missing-prefix label, the already-imported read, the empty-log branch, the sequence guard, the table's PRIMARY KEY, and the `source` CHECK constraint. Migration-ladder pins moved together: MAX(version) 17->18 and COUNT 16->17. WHAT IS NOT DONE, STATED EXACTLY: (1) the retained EVENTS are not copied into the journal with per-event source identities - this records what the retained log CONTAINS (its head revision and digest) rather than importing its contents, and the import proper is a separate seam because doing both here exceeds the 500-line cap; (2) NOTHING READS THE BASELINE YET - task 4.3 ('rollback selection from the durable cutover marker') is its first consumer, and this is foundational by the task's own wording rather than speculative. Verification: typecheck 0 errors; oxlint 0 errors / 78 warnings unchanged; new suites 11/11 (5 import rows, 2 digest rows, 4 schema rows); persistence migrations 8 suites / 87 tests; FULL unit suite 2417 suites / 33136 passed / 0 failed.
- [ ] 1.4 Run focused durable-store tests on Node 22, typecheck/lint/format, strict OpenSpec validation, and independent durability/security review.
- [ ] 1.5 After merge, rerun the real SQLite receipt on exact main and prune the merged branch/worktree.

## 2. Decide, Commit, Apply, Publish — PR 2

- [x] 2.1 Lock existing command-to-`IGameEvent` and post-state digests before refactoring the engine/host boundary.
  - CORRECTION (2026-08-26 — the receipt below asserts a defect that DOES NOT EXIST; read this first): the bolded claim "INITIATIVE ON THE SERVER-AUTHORITATIVE PATH IS NOT DETERMINISTIC UNDER A FIXED SEED" is FALSE, and so is its stated mechanism ("`rollInitiative` defaults its `diceRoller` to `defaultD6Roller` (`Math.random`) ... the seeded stream handed to `ServerMatchHost.create` is not reaching it"). The roller DOES reach `rollInitiative`: `ServerMatchHostBootstrap` passes `engineCallback` into the session unconditionally and `InteractiveSession.phases.ts` forwards it. What the host builds WITHOUT a `diceSeed` is a `CryptoDiceRoller`, not `Math.random`. THE DISCRIMINATING EXPERIMENT, run before anything was changed: adding `diceSeed: 42` to the same bootstrap makes the outcome identical across 8 runs, while omitting it flips between exactly two values - so the roller arrives and the variance was crypto dice working as specified. Crypto-by-default is a live SHALL (`openspec/specs/multiplayer-server/spec.md:345-351`: the server "SHALL use `crypto.randomBytes` ... AND it SHALL NOT use `Math.random`"), with reproducibility scoped to the opt-in seeded debug mode at :353-356. The replay worry does not hold either: replay reads back the rolls the server STAMPED onto events (`stampRollsOnNewEvents` writes `rolls: captured` before persistence), never re-deriving them from a seed. WHAT WAS ACTUALLY WRONG was the test seeding the wrong knob - `random` is the engine's general RNG (in production a hardcoded `SeededRandom(0xc0ffee)`), `diceSeed` is what initiative consumes. FIXED: `diceSeed: 42` added, `initiativeWinner`/`firstMover` restored to the post-state digest, literal re-pinned and verified identical across 5 separate processes, falsified 1:1 (removing `diceSeed` reds the restored pin). THE LESSON, which is why this correction is appended rather than the receipt being edited: a value that varies is not automatically a defect, and "the seeded stream is not reaching it" is a claim that must be MEASURED before it is written down. TWO GENUINELY REAL defects were found while verifying this and are filed separately: (1) `?seed=N` is parsed in the production entry point (`server.js:700-706`) with no environment gate, letting a client select the server's dice stream; (2) recovered MP matches pass `sourceRoller: undefined` and never adopt a capture ref, so they roll on `Math.random` AND stamp no `payload.rolls` - which is the receipt's original mechanism, true for a path it was not testing.
  - Receipt (2026-08-25 — CLOSES this box, and surfaced two defects while doing it): `commandDigestLock.test.ts` pins, for a seeded `ServerMatchHost` driven through three `AdvancePhase` commands, the exact ordered event signature (`sequence:type`, eight events) and a digest of the derived post-state. WRITTEN AS LITERALS, NOT SNAPSHOTS, deliberately: a snapshot can be regenerated with `-u` and the change disappears into a file nobody reads, whereas a literal makes a behaviour change show up as a diff in the test itself - which is the entire job of a lock. DETERMINISM IS ASSERTED FIRST, before either literal, because a golden that varies per run is a flake rather than a lock. THAT ROW EARNED ITS PLACE IMMEDIATELY: the first digest pinned flipped between two values across runs. Measured rather than guessed - four runs of the identical seeded setup produced `initiativeWinner`/`firstMover` as `opponent`/`player` once and `player`/`opponent` three times, with every other field stable. `rollInitiative` (`src/utils/gameplay/gameSessionCore.ts:283`) defaults its `diceRoller` to `defaultD6Roller` (`Math.random`), and its tie-break IS deterministic, so the variance is in the ROLLS - the seeded stream handed to `ServerMatchHost.create` is not reaching it. **INITIATIVE ON THE SERVER-AUTHORITATIVE PATH IS NOT DETERMINISTIC UNDER A FIXED SEED**, which the codebase already treats as a bug class (`InteractiveSession.resolvers.ts:39` calls reaching `Math.random` from an engine-created session a bug the determinism audit catches). The two fields therefore sit OUTSIDE the digest, with the reason recorded at the exclusion rather than quietly worked around, and the narrowed digest is stable across four runs. Filed separately with the evidence and an explicit instruction to re-include the fields and re-pin once fixed - that is the acceptance check for that work. SECOND OBSERVATION, LOCKED AS IS: events 6 and 7 are phase advances landing AFTER `game_ended` at 5. Probed directly - status is `completed` after the first command and the next `AdvancePhase` is still accepted, commits, and broadcasts. NOT claimed as a defect: `handleIntent` guards the HOST's `closed` flag, which is a different thing from the game's `completed` status, and that may be deliberate so post-match corrections work. Filed as a question, with the prior questions to answer before any guard is written. Characterisation records what the system DOES; a task about locking behaviour is the wrong place to change it. Sensitivity is proven too - a shorter command sequence moves the digest, so it is not a value that would agree with any refactor. Verification: typecheck 0 errors; oxlint 0 errors / 78 warnings unchanged; the new suite 4/4 and stable across three consecutive runs; FULL unit suite 33158 passed / 0 failed. PR 2's remaining tasks (2.2 decision seam, 2.3 batch append at the expected revision) now have the comparison they need.
- [x] 2.2 Add the smallest decision seam that produces an ordered event batch without advancing the authoritative live engine.
  - Receipt (2026-08-28, PR #1430 - CLOSES this box, and its discovery is 2.3's law): `decideCommandBatch` builds a SCRATCH engine session from the live session's current event log via a new in-pattern `InteractiveSession.fromHydratedSession` factory (RNG and dice INJECTED rather than re-seeded; the constructor's throwaway create+start log replaced with the real history), dispatches through the SAME dispatch table - no second dispatch map to drift - and returns the ordered batch plus a post-state digest. THE LIVE SESSION IS UNTOUCHED, proven byte-identical (state digest and event count) after every decide, including one the engine REFUSES: deciding is not committing, and a refusal is also a batch. Equality is anchored to 2.1's digest lock for its exact seeded scenario, with the lock file untouched. Mutations, worker-run then reviewer-re-run: dispatching on the LIVE session kills all five rows (reviewer reproduced all five); a stale-prefix scratch kills equality, fidelity, and isolation while untouched and determinism stay green; an empty-batch neutralization reds exactly the three rows that consume the batch. THE DISCOVERY, worth more than the seam and binding on 2.3: the dice cursor is NOT reconstructable from stamped `payload.rolls`, so the APPLY side must CONSUME THE DECIDED BATCH rather than re-dispatch the intent - a re-dispatch after a decide advances the dice stream twice and every later dice-consuming command diverges. Also recorded for 2.3: bootstrap adapted units must ride the decide deps for catalog-unit commands (empty maps match only the lock fixtures). Wired into nothing, deliberately - the seam ships callable and proven, as 1.2 shipped the store capability before its consumer. Gates: typecheck 0; oxlint 0 errors / 77 warnings; multiplayer+engine battery 127 suites / 1157 tests; full suite 2444 suites / 33360 passed / 0 failed. 3 files, +311.

  - Progress (2026-08-25, STAYS OPEN — feasibility is established and the DESIGN CONSTRAINT is now measured; the seam itself is not built): today deciding and advancing are THE SAME ACT. The host dispatches straight into the live `InteractiveSession` and reads back whatever appeared past its broadcast cursor, so there is no point at which a batch exists but has not been applied - which is exactly what this task has to create. THE OBVIOUS SUBSTITUTE IS A SCRATCH PROJECTION (`InteractiveSession.fromSessionAsync` over a snapshot of the live session), and it was worth establishing whether that is sound BEFORE starting a refactor that would be wasted if it is not. IT IS SOUND FOR SHAPE AND NOT FOR ROLLS. Measured, not reasoned about: the same command decided on a scratch and applied on the live engine produced identical event types, order, turns, phases and visibility, and DIFFERENT dice - one run gave `opponentRoll: 8` on the scratch against `opponentRoll: 5` live. `fromSessionAsync` RE-SEEDS its dice stream rather than continuing the live one, which is right for recovery and fatal for 'decide here, then re-dispatch there'. **THE DESIGN CONSTRAINT THAT FALLS OUT: a decided batch MUST CARRY ITS OWN ROLLS and the apply must CONSUME them.** A seam that decides on a scratch and then re-dispatches on the live engine would commit one set of numbers and apply another - precisely the post-apply divergence task 2.4 exists to catch, arriving by construction rather than by accident. Two rows pin what is established: a scratch decision produces a non-empty batch while the live engine's event count and phase do not move, and the applied batch matches the decided one in shape. A CORRECTION MADE BEFORE SHIPPING, because the obvious explanation was wrong: the first version of the isolation row claimed the deep copy was what kept the live session safe. Falsifying it - handing the LIVE session object straight to `fromSessionAsync` - did NOT red the row. Isolation actually comes from the engine REPLACING the session object on each step rather than mutating it in place, so the scratch swaps its own reference and the host keeps its old one. The copy is kept as defence against a future handler that mutates a nested unit state in place, and the comment now says which of those two things is load-bearing. SCOPE, STATED: zero units on both sides. `fromSessionAsync` re-derives adapted-unit maps from the catalog, and whether a scratch projection stays faithful with units DEPLOYED is NOT established - that is the next thing to measure before the seam is written. Verification: typecheck 0 errors; oxlint 0 errors / 78 warnings unchanged; new suite 2/2, stable across three consecutive runs; FULL unit suite 33160 passed / 0 failed.
- [x] 2.3 Change `ServerMatchHost` to append the batch and expected post-state digest at the expected revision; the first journal-authority batch transaction also writes the immutable one-time started fact containing command ID, event range, and resulting head tuple. Apply only the committed batch, verify its digest, and only then publish projections.
  - Receipt (2026-08-28, PR #1432 - CLOSES this box): with the per-host flag ON, a command's path is decide -> append-at-revision -> CONSUME the committed batch -> verify the applied digest -> only then publish; with the flag OFF (the default) behavior is byte-identical, proven by the digest lock running 4/4 under BOTH flag states with the same literal digest, and the flag-on receipt carrying that digest as its expectedPostStateDigest. THE EARNED LAWS ARE LOAD-BEARING: apply consumes the decided batch (2.2's dice-cursor law - the re-dispatch mutation reds the consume row via dice divergence, exactly the failure the law predicted); the first journal-authority commit writes the immutable one-time STARTED FACT (command id, event range, resulting head tuple) in the same transaction, and a second write fails constraint-style like the baseline's no-UPDATE law; divergence blocks publication, quarantines the process-local projection, rebuilds from the durable journal, and NEVER deletes the committed batch (the delete-on-divergence mutation reds the batch-survives row; publish-before-verify reds the no-Event-on-the-wire row). A stale expected revision surfaces the store's typed sequence-conflict with live state untouched. The divergence row corrupts the APPLY pass per the campaign sibling's mutant lesson (#1264) - a derive-pass corruption makes the rebuild mutant equivalent. Reviewer re-verification: divergence-check falsification re-run (exactly the divergence row red), both-flag lock re-run, independent full suite 2446 suites / 33373 passed / 0 failed matching the worker's numbers. Gates: typecheck 0; oxlint 0 errors / 77 warnings; battery 129 suites / 1170. 14 files, +983/-6. LEFT FOR 2.4, recorded by the worker: persistence-failure injection after decide (the shared dice roller has already advanced - the characterization is 2.4's), the crash-between-commit-and-publish duplicate-command path (must consume the outbox resume machinery), one typed recovery result folding persistence-failure / revision-conflict / digest-divergence. The production cutover flag is task 4.1's; this per-host switch is the test seam.

- [x] 2.4 Inject revision conflict, persistence failure, and post-apply digest divergence; prove no premature success frame, quarantine plus deterministic journal rebuild on divergence, and a typed recovery result without deleting the commit.
  - Receipt (2026-08-28, PR #1434 - CLOSES this box): one discriminated RECOVERY RESULT - persistence-failure (carrying the store's answer), revision-conflict (both revisions), digest-divergence (both digests plus the rebuilt flag) - and nothing swallowed: the mutation that turns a store throw into a silent empty batch reds the persistence row. PERSISTENCE FAILURE AFTER DECIDE: typed result, no frame on any socket, live engine byte-identical, retry commits. THE DICE TRUTH, characterized rather than assumed: the shared roller has already advanced when the decide ran, so a post-failure retry decides a DIFFERENT but internally consistent batch - acceptable by design because rolls are stamped in the committed batch and a retry is a NEW decision; the test's comment says exactly this. CRASH BETWEEN COMMIT AND PUBLISH: the store's identity law returns the prior receipt, no event is appended twice, and the committed events reach viewers through the EXISTING outbox resume rather than a sibling mechanism. THE CONTRACT DISCOVERY, load-bearing for 2.5 and named there as contract rather than regression: on this path IDENTITY-FIRST PRECEDES DUPLICATE-INTENT TRACKING - a recovered host rebuilds its accepted-intent tracker from stamped ids, so D7's DUPLICATE_INTENT would fire before the identity law, and re-deciding would mint new event UUIDs and turn an honest retry into integrity-conflict. Flag-off D7 unchanged. THE MUTATION LEDGER SEPARATES THE CRASH FAILURE MODES PRECISELY: skipping identity-first yields integrity-conflict (delivery reds, no duplication); the mutation that actually double-writes is a blind append of the prior range, and it reds the no-duplicate row; dropping the resume consumption reds delivery - re-run independently by the reviewer (1 red, restored green). Neutralizing only the union's divergence arm reds 2.4's divergence row while 2.3's stays green; neutralizing the digest check itself reds both - shared coverage of the check, named as such. Also fixed in passing, caused by this slice's own full-suite run: gitignore now covers __snapshots__/test-failed/ (a red run could previously stage 36k lines of snapshot noise). Gates: typecheck 0; oxlint 0 errors / 77 warnings; battery 130 suites / 1175; full suite 2447 suites / 33378 passed / 0 failed, reviewer's independent run matching. 6 files, +623/-34. Worker relaunched once after a silent death (no output, no diff, no process - the standing recovery rule). LEFT FOR 2.5/2.6: production recover() stays flag-off until 4.x; skip-publish approximates a crash whose RAM is gone - resume is what delivers either way.

- [x] 2.5 Run focused engine/host/store suites plus multiplayer contracts; independently review authority ordering and scope.
  - Receipt (2026-08-28 - CLOSES this box on an independent fresh-context review, verdict CLOSE, tree left byte-clean): a read-only reviewer walked section 2 as one contract and looked for the holes the receipts claim do not exist. AUTHORITY ORDERING - HOLDS: every early-return and error branch of the journal-authority handler walked; no path publishes a frame before verify or advances live state before commit. FLAG-OFF SCOPE - HOLDS: every new call site guarded; nothing outside the locked scenario touches the new seams unguarded. CONTRACT COHERENCE - HOLDS with the composition spelled out: identity-first-before-D7 is bounded to the journal path; the import and appendCommandBatch share one revision space with NO production second writer (the import is called by nothing in the host/registry/recovery, per 1.3's own boundary); and the dice-truth vs determinism claims are consistent statements about DIFFERENT MOMENTS - the clean seeded first attempt is locked, a failed decide never became a fact, and replay trusts stamped rolls rather than re-deriving (the 2.1 correction's law). Identity-first-before-revision-conflict verified in BOTH stores at the receipt-lookup-precedes-head-check line. SUITES, real numbers from the reviewer's own run: focused engine/host/store plus multiplayer contracts 139 suites / 1266 tests / 0 failed; typecheck 0 errors; oxlint 0 errors / 77 warnings. RESIDUALS CARRIED, NOT CLOSE-BLOCKERS, each with an owner: production recover() stays flag-off until 4.x (already 2.4's record); the main D7 spec text does not yet state the journal-path identity-first exception - the spec-delta child that merges at Code Complete owns wording it; lobby ForfeitMatch is still live-first if a host flips the per-host flag - task 4.1 owns routing EVERY intent class through the executor, and this is a named input to it rather than a section-2 ordering break.

- [x] 2.6 After merge, rerun exact-main command/restart authority proof and prune the merged branch/worktree.
  - Receipt (2026-08-28 - CLOSES this box, executed by the orchestrator at landing): the command/restart authority proof - the seven journal suites (path, recovery, started-fact sqlite, digest lock, decide seam, legacy import unit + sqlite) - reran green on EXACT MAIN twice: 31/31 at 08c79cfa9 (the section's merge base, #1434) and 31/31 at d2bbdbda3 (the tip after the docs receipts, delta docs-only). Merged branches deleted, worktrees removed and pruned. Section 2 is COMPLETE: contract locked (2.1), decision seam (2.2, #1430), journal-authority path behind its flag (2.3, #1432), one recovery contract (2.4, #1434), independent ordering/scope review with verdict CLOSE (2.5), and this exact-main proof (2.6).


## 3. Recovery and Client Mirror — PR 3

- [x] 3.1 Resume combat replay/live delivery from durable committed history and acknowledge only successfully applied contiguous projected events through the per-viewer delivery mapping the umbrella shipped (`ViewerDeliveryCursors` + the `deliverySequence` protocol fields and delivery-space replay bounds); do not mint a SECOND sequence allocator or expose a raw-journal cursor. Epoch identity enters with branches (task 14.x world): a branch change is the epoch boundary, and the mapping gains its epoch dimension there rather than here.
  - Receipt (2026-08-28/29, PR #1438 - CLOSES this box as amended): the shipped per-viewer delivery mapping is now DURABLE. `mp_viewer_delivery` persists the in-memory record exactly - index is the deliverySequence, value the authority sequence, -1 for send-failure slots (index drift is why the slot must persist; the mutation that skips it reds the exact-record row). Recovery loads records back into `ViewerDeliveryCursors`, so a resume against a rebuilt host receives the FIRST MISSED frame, proven by event id (red: a from-zero replay's first event; green: live[1]); a viewer with no rows keeps the full-replay fallback, the #1399 control. One optional-port writer, log-and-continue by design with the rationale at the code: better no row than a wrong row. SCHEMA HOME, a reported deviation with its reasoning accepted: the table lives on DurableMatchStore beside the outbox and command tables, NOT the campaign DB's versioned ladder - recovery rebuilds from IMatchStore, and combat resume state where recovery does not read would recreate the two-world split the amendment forbids; the schema pin is the PK + match-isolation row. Mutations: skip-failure-slots, cross-match bleed, and skip-restore each red their named rows; the reviewer independently re-ran skip-restore (1 red on the host-rebuild row, restored green). FLAKE, handled by instrumentation rather than reruns: the PK-pin row failed twice across full-scale runs (duplicate insert did not throw) and never reproduced - 25 isolated iterations, the whole server battery in-band (655/655), and a fresh full suite (2448 suites / 33384 / 0) all green, :memory: stores ruling out sharing. The row is now SELF-DIAGNOSING: it asserts the pre-duplicate state and reports the rows it saw on any future non-throw. Landed on a fully green CI run. Gates: typecheck 0; oxlint 0 errors / 77 warnings; battery 112 suites / 872. 7 files, +675/-8 (+ the instrumented row). LEFT FOR 3.2/3.3: the IndexedDB browser mirror and prefix detection; the recovery coverage matrix; the outbox still does not consult per-viewer delivery (pre-existing, its comment says so).

  - Spec amendment (2026-08-28, BEFORE implementation per this file's preamble rule): the original text mandated the privacy-owned `(deliveryEpochId, deliverySequence)` mapping and forbade a combat-specific allocator. It was written (2026-08-20 council era) before the umbrella built combat's per-viewer delivery numbering - `ViewerDeliveryCursors` (#1399/#1406), the `deliverySequence` wire fields, the delivery-space replay envelope bounds (#1423/#1429), and the inverted concealment guard - all shipped, mutation-proven, and load-bearing. The privacy-owned epoch mapping exists only in the CAMPAIGN delivery world (`src/lib/campaign/delivery/`), scoped to grant streams over the campaign journal. TODAY THERE IS EXACTLY ONE combat allocator; building the campaign-epoch variant into combat would CREATE the two-allocator duplication the original text guarded against, and rebuilding proven combat delivery on campaign-stream tables fails simplest-solution for no privacy gain - the spirit (no raw authority/journal cursor reaches a player) is already enforced by the projection boundary and its negative sweeps. The letter is amended to name the shipped mapping as THE mapping; the epoch dimension is deferred to the branch work that creates epochs. The wave-map delegation is unchanged.

- [x] 3.2 Preserve IndexedDB as a browser recovery mirror; detect truncated/replaced immutable prefixes and request authoritative resync.
  - Receipt (2026-08-29, PR #1440 - CLOSES this box): the IndexedDB match-log mirror proves itself a cache on every reconnect - the authoritative stream is compared position-by-position against the stored prefix BY EVENT IDENTITY, first divergence yields a typed verdict (replaced, naming the position and both ids; truncated, naming the position), the match's mirror rows are deleted, and rehydration proceeds on the authoritative stream alone. A matching prefix keeps behavior byte-identical; an absent mirror touches nothing. IDENTITY, NEVER SEQUENCE, and the test proves why: the post-cutover player stream carries no event.sequence, and the mutation that keys the comparator on it reds the sequence-free row - stored 0 versus a missing field reads as divergence at position zero, exactly the false alarm the id key avoids. Mutations, worker-run then reviewer-re-run: length-only comparison reds replaced and sequence-free; shorter-stream-as-match reds truncated (reviewer re-run: 3 red across unit and reconcile rows, restored 189/189); sequence-keying reds sequence-free. Gates: typecheck 0; oxlint 0 errors / 77 warnings; battery 160 suites / 1406; full suite 2449 suites / 33400 passed / 0 failed. 7 files, +766/-4. CARRIED TO 3.3, named: the suffix-only P2P resume cannot see replaced-at-0 (an unknown head reads as a suffix, not a rewritten prefix; closing it means requesting a full replay when a mirror exists), and the multiplayer client does not yet call the verification seam - its consumer is the P2P reconnect hook.

- [x] 3.3 Add cold reload, process restart, replay/live overlap, duplicate delivery, and legal post-recovery command coverage.
  - Receipt (2026-08-29, PR #1442 - CLOSES this box, plus the two residuals 3.2 carried by name): the coverage matrix runs five recovery shapes, each ending in a LEGAL POST-RECOVERY COMMAND - cold reload with a rewritten head, process restart with the tail delivered from the first missed frame, a live event during a replay window buffered and applied exactly once, duplicate delivery applied once (by delivery number when present, by identity when not) and stored once in the mirror, and a normal command round-tripping after every one of them. R1 CLOSED: when a mirror exists, the P2P reconnect requests the FULL authoritative replay instead of the suffix, so a rewritten head is visible - the replaced-at-0 row was the load-bearing red (with suffix quoting, the hook asked from sequence 2 and the verification had nothing to compare); the typed replaced verdict at position 0 now discards the mirror and recovery completes on the stream. The no-mirror path quotes exactly as before. R2 RESOLVED AS NON-APPLICABLE, with the assertion that proves it: the multiplayer client keeps no IndexedDB mirror - matchLogStorage is never read on its connect/join/command path - so there is nothing to verify there; a fact with a test, not a wire-up postponed. Mutations, worker-run then reviewer-re-run: emitting live during replay reds the overlap row; dropping identity-dedup on append reds the duplicate row; disabling the prefix-snapshot assumption reds replaced-at-0 (reviewer re-run: 1 red, restored 200/200); reverting the full-replay-when-mirror condition reds both the full-replay and replaced-at-0 rows. Gates: typecheck 0; oxlint 0 errors / 77 warnings; battery 160 suites / 1415; full suite 2449 suites / 33409 passed / 0 failed, the reviewer's independent run matching. 9 files, +472/-60. RECORDED FOR 3.4: answerReconnectRequest in gameSessionChannel still quotes lastLocalSeq + 1 - only this hook's host responder compensates at zero; the browser QC commands and the independent browser/security review are 3.4's own clauses.

- [x] 3.4 Run `verify:qc:multiplayer:browser`, `verify:qc:replay-recovery`, `qc:command:browser:quick`, and independent browser/security review with store-backed evidence.
  - Receipt (2026-08-29, verification vs main 11dec7e4a; one fixture fix shipped as PR #1444 - CLOSES this box): all three QC commands green, each spec run solo. HARNESS DISCOVERY that forced the solo runs: scripts/playwright/run-playwright.mjs silently forwards only ONE spec file of a multi-file invocation, so the packaged QC commands under-run while reporting success (the 4-spec multiplayer command executed 5 tests instead of 20); chip filed to fix the runner, every spec here re-verified individually. Command 1, verify:qc:multiplayer:browser: p2p-sync 12 passed / 3 skipped; playtest-mp-smoke 5; playtest-coop-route-smoke 2; multiplayer-live-vault-auth 1 (a real two-context host/join/launch/advance fog match) - 20 passed, 0 failed. Command 2, verify:qc:replay-recovery: the jest trio 3 suites / 18 passed; active-session-recovery 3; replay-player 12. Command 3, qc:command:browser:quick: went RED at the starmap travel journey - Save-now produced NO PUT and the persist waiter starved. Root cause is deliberate product design, not a product bug: since the adoption change a store-seeded campaign whose first server contact happens AFTER a full reload is a rehydrated legacy copy (the 404'd load sets legacyUnadopted and runSave skips instead of birthing the record), while the product's create wizard persists in the SAME page context before navigating. The e2e helper now mirrors that contract with one in-context genesis save before the dashboard visit (PR #1444); this also removed the persist-flush race that let the other two helper consumers pass on luck. Post-fix solo: starmap 1, customizer-handoff 5, feature-screens 1, gm-campaign-ledger 2 - all green. Independent defensive-QA review (fresh-context grok-4.6 worker, read-only worktree at 37a5befc2, six evidence-demanding questions; two citations spot-checked verbatim by the orchestrator): verdict CLOSE - the reconnect-success mirror path, delivery-cursor resume, delivery-row write/load rule, decide/append/verify publish gate, and all three recovery union arms hold with file:line evidence. FIVE RESIDUALS CARRIED INTO 4.x, none close-blocking: (1) answerReconnectRequest in gameSessionChannel still quotes lastLocalSeq + 1 uncompensated - only the hook's host responder compensates at zero; fix or delete the helper before any new caller, and never treat ServerMatchHost.handleReconnectRequest as a full-replay API. (2) The reconnect TIMEOUT path hydrates the IndexedDB mirror into live state with no prefix verification (host-absent degraded mode, not the success resync) - hostPending local hydrate must never be treated as authority. (3) The live dice cursor advances during decideCommandBatch when d6Roller is the host capture - already the characterized design (stamped rolls, apply-consumes-batch, a failed decide is not a fact) but it IS a live-store mutation before verify; keep the law. (4) P2P defaultGetHostEventsFromSeq returns the raw unfiltered session log (no fog filter, no sequence strip) - inert for server GM+2P sessions, but never reuse this reader for server-authoritative reconnect. (5) hasDetectedDivergence is diagnostic-only: after a journal rebuild the host keeps serving; whether divergence should freeze the match is a 4.x ops decision, not a missing fail-close of the commit.

- [ ] 3.5 After merge, rerun the combat journey on exact main, archive journal/IndexedDB/reload proof, and prune the merged branch/worktree.

## 4. Shadow Cutover — PR 4

- [ ] 4.1 Add a reviewed feature flag for new match journal authority and shadow state/event digest comparison without dual-authoring.
- [ ] 4.2 Enable journal authority only for new controlled matches after shadow equality and the active-membership, server-derived-viewer, action-audit, private-record, and pre-serialization privacy gates pass; preserve schema-compatible legacy reads.
- [ ] 4.3 Implement rollback selection from the durable cutover marker: allow the compatible legacy reader only before the first journal-authored command, otherwise require a journal/upcaster/effective-generation-compatible reader or typed blocked state while preserving rows, receipts, head, generation, and recovery state.
- [ ] 4.4 Prove pre-cutover legacy reopen, exact-tuple baseline-only cutover rollback, crash/reopen immediately after the first batch transaction, post-command compatible rollback, and post-command incompatible rollback without silent snapshot/log substitution or new command/effect admission.
- [ ] 4.5 Run applicable combat, multiplayer, replay, command-browser, and long-browser gates plus final independent visual/authority review.
- [ ] 4.6 After merge, run the exact-main regression suite, record the merge SHA and evidence, and prune the merged branch/worktree before combat closeout or cross-stream effects; campaign adoption remains independently gated.
