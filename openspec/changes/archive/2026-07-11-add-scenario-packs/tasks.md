# Tasks: add-scenario-packs

> Execution is **Codex-first and strictly sequential** — parallel agent fan-out has failed on this repo. One group at a time; each lands with its verification before the next. Claude orchestrates, packages context, and final-reviews. Pre-commit runs a full `next build` (~3–5 min) — commit via background shell. **Hard ordering gate: implementation of this change starts only after the `add-campaign-fast-forward-api` (W3) IMPLEMENTATION merges to main** (ladder sequencing); mechanically, groups 1–3 depend only on merged production surface, group 4 carries its own W2 gate check (task 4.0), and group 5 its own W3 gate check (task 5.0) — both STOP on absence, never stub (design D10). **Non-goals are load-bearing: modify NO existing production file (this is a zero-production-touch change — `isValidPutBody`, the read-side casts, the migration ladder, the campaign shell, and the recovery factory are consumed as-is); NO store injection anywhere in pack loaders or pack specs; NO hand-authored or hand-edited pack payloads, ever, including bootstraps for gated packs; NO PR-gate or required-check wiring (the nightly recapture job is triage only); NO W5/W6 work (no viewport sweeps, no project rationalization beyond this change's `testIgnore` globs, no subsystem lanes); NO edits to anchor specs, audit journeys, or the seven-journey catalog; NO golden traces; NO `Date.now()` id uniquing in the pack loader (the stamped template uses `crypto.randomUUID()` — design D4's recorded deviation).** File paths and line numbers below were verified on `claude/scenario-packs@dc84728d8` (= main + the W2/W3 declarative folders) — re-verify against the then-current tree before editing (the W2 and W3 implementations will have landed in between). After any spec-delta edit, run the formatter + `npx openspec validate add-scenario-packs --strict` (design R4). Ledger registration ships with this declarative PR, not with these implementation tasks. Estimated total: ~3–4 dev-days.

## 1. Ground-truth re-verification (read-only)

- [x] 1.1 Re-verify the campaign-pack load path end to end: the PUT guard and handler (`src/pages/api/campaigns/[id].ts:51-69, 98-125`), whether `saveCampaign(envelope, baseVersion)` creates a row for a previously unseen campaign id (and which `baseVersion` a fresh-id PUT requires) or whether creation needs a different call first, the shell's route-loader chain (`campaignPageShell.tsx:120-170`, `useCampaignPersistenceStore.ts:441-483`), the literal campaign shell route template for `targetRoute`, and a mount-completion signal the loader can await (a loaded-surface testid or exposed-store predicate — record which).
  - Files: read-only.
  - Acceptance: a written note naming the create-vs-update PUT semantics for fresh ids, the exact route template, and the load-completion signal `loadCampaignPack` will await.
- [x] 1.2 Resolve design R10 and the remap/strip scope: determine whether the campaign surfaces the pilot packs target resolve wholly from the envelope's inline `rosterProjection` (client-side `restoreRosterProjection`, `useCampaignPersistenceStore.ts:222`) or additionally fetch `/api/pilots` rows by pilot id; inventory every id-bearing envelope location against `SerializedCampaign.ts:111-281` — the inventory range STARTS at the roster-projection interfaces (`SerializedCampaignRosterEntry`:116, `SerializedCampaignRosterMissionRecord`:129-140, `SerializedCampaignRosterState`:148), NOT at the body interface — plus `Force.ts:33-63` / `CampaignRosterEntry.ts:45-67`, and classify every field three ways: joins the remap table (leaks into shared server tables), stays row-internal, or is a **cross-store reference** into the encounter store or the match-log IndexedDB that joins design D11's strip list. The two verified strip entries are the roster mission records' `encounterId`/`gameSessionId` (`SerializedCampaign.ts:134-135`); sweep the remaining body fields (`pendingBattleOutcomes`, `processedBattleIds`, `dailyBattleAudit`, salvage records, …) for further instances and record a verdict for each (strip vs campaign-internal idempotency marker).
  - Files: read-only.
  - Acceptance: a written remap/strip-scope note the stamper and canonicalizer implementation (2.2) cites: the definitive remap-id list, the row-internal list, the cross-store strip list (the two verified fields plus any further instances, each with its verdict), and the R10 verdict (with the front-door pilot-row creation plan if rows are needed).
- [x] 1.3 Re-verify the encounter-pack substrate: IndexedDB names/version/stores/keyPaths (`src/lib/p2p/matchLogStorageSchema.ts:1-38`, `matchLogStorage.helpers.ts:20-45`), the `GameCreated`-first rule (`InteractiveSession.persistence.ts:57-81`), `GameEventType.GameCreated === 'game_created'` (`GameSessionCoreTypes.ts:65`), and where the finite `config.seed` sits on the `GameCreated` payload post-W1 (`GameSessionUnitTypes.ts:81`, `GameSessionLifecycleEvents.ts:56-58`).
  - Files: read-only.
  - Acceptance: a written schema/seed note the encounter zod schema (2.1) cites verbatim.
- [x] 1.4 Re-verify the registration surfaces: the three responsive projects' `testIgnore` arrays and the smoke project's `grep` (`playwright.config.ts`), the flow-audit manifest's subsystem vocabulary and the three genesis checkpoints (`e2e/flows/manifest.ts` — `campaign-create-to-launch@contract-accepted`, `personnel-hiring@roster-updated`, `pilot-xp-progression@xp-surface-viewed`, all `holdSafe: true`), and the nightly workflow's job/`needs` anchors (`.github/workflows/nightly-validation.yml` — `install-deps`, `notify-on-failure.needs`).
  - Files: read-only.
  - Acceptance: a written anchors note with current line numbers for every surface groups 3 and 6 will edit.

## 2. Pack library — schemas, stamper, manifest types (`src/lib/scenarioPacks/`)

- [x] 2.1 Create `src/lib/scenarioPacks/packSchemas.ts`: zod schemas per design D3 — the campaign envelope skeleton + **the full deserializer-required field set** (design D3's enumerated floor: `body.id`, `name`, `currentDate` as a *parseable* date-time, `factionId`, `forces`, `rootForceId`, `missions`, `finances` with required `transactions` array + `balance`, `factionStandings`, `options`, `campaignType`, `createdAt`, `updatedAt` — everything `deserializeCampaignBody`/`deserializeFinances`, `serializeCampaign.ts:209-270`/`:82`, hard-reads) + load-bearing id fields + full rosterProjection linkage (unknown keys pass through; date-valued strings validated parseable — `new Date(garbage)` is silently `Invalid Date`), the matchlog pack schema (ordered events, sequence-0 type `'game_created'`, finite `payload.config.seed`, matches-row fields), and the manifest entry schema (fields per design D2, closed six-tag subsystem enum, closed `postLoadActions` vocabulary of exactly `['advance-day']`, `parityAnchorJourney` format `flow:<id>@<checkpoint>` | `anchor:<basename>` | `fast-forward:<fixtureId>`). Export the `manifestVersion` constant. Every schema failure surfaces the offending path (zod default behavior — do not catch-and-soften).
  - Files: `src/lib/scenarioPacks/packSchemas.ts` (new), colocated `__tests__/packSchemas.test.ts` (new).
  - Acceptance: unit tests cover accept + reject cases per schema (drifted body shape names its path; a payload missing `finances.transactions` rejected naming the path; an unparseable `currentDate` rejected naming the path; future `schemaVersion` rejected; unseeded matchlog rejected; misordered events rejected; unknown additive key accepted); a guard test asserts the pack subsystem literals equal the flow-audit `FlowSubsystem` literals (import `e2e/flows/manifest.ts` from the test only — no src→e2e import in library code).
- [x] 2.2 Create `src/lib/scenarioPacks/packStamping.ts` with BOTH halves of the remap machinery (design D4 + D11). **Load side**: `stampPackIds(payload, {packId, workerIndex})` builds the remap table per design D4 (template `${packId}-w${workerIndex}-${crypto.randomUUID()}`; campaign remap set per 1.2's note — at minimum `envelope.campaignId`, `body.id`, `body.rosterProjection.campaignId`, rosterProjection mission records' `campaignId`; encounter remap = `matchId` across matches row, every event record, every payload field carrying it; session unit ids never remapped), applies it at every occurrence, then runs the zero-residue scan: the serialized output contains no occurrence of any mapped-from id, else throw naming the residue path. **Mint side**: `canonicalizePackPayload(payload, {packId})` (design D11) remaps the same id families to deterministic role-ordinal template ids (`pack:<packId>:<role>[:<NN>]` — missions in `missionNumber` order), strips cross-store references per 1.2's strip list (at minimum roster mission records' `encounterId`/`gameSessionId`), and runs the mint-side residue scan (no mapped-from id survives; no `campaign-|force-|mission-<epoch>-`-shaped wall-clock id survives in a campaign payload's remapped families).
  - Files: `src/lib/scenarioPacks/packStamping.ts` (new), colocated `__tests__/packStamping.test.ts` (new).
  - Acceptance: tests prove (a) two stamps of one payload yield distinct ids; (b) internal graph consistency (envelope/body/rosterProjection ids all equal post-stamp); (c) the adversarial fixture — a remapped id planted in a field the schema doesn't enumerate — trips the residue scan with the path named (design R3's regression gate); (d) deployed session unit ids in a matchlog payload are untouched; (e) canonicalizing the same captured payload twice yields byte-identical output (determinism); (f) a canonicalized campaign payload contains no wall-clock-shaped id in the remapped families and no `encounterId`/`gameSessionId` on any roster mission record; (g) stamping a canonicalized payload remaps cleanly FROM the canonical template ids (the two halves compose).
- [x] 2.3 QA — `npm run typecheck` clean; new colocated suites green under the jest unit lane (they live under `src/`, matched by the unit project's `__tests__` glob — jest ignores `e2e/`, `jest.config.js:46`, which is why the library is here; design D5).

## 3. Campaign loader, flow-checkpoint minter, and the three ungated pilot packs

- [x] 3.1 Create `e2e/helpers/scenarioPackLoading.ts`: `loadCampaignPack(page, packId, {workerIndex})` — resolve the manifest entry, zod-parse the payload (fail-loud pre-PUT), stamp via 2.2, PUT `{envelope, baseVersion}` per 1.1's create semantics through a request context, `goto` the stamped target route, then determine the mount-time load outcome from the persistence store's own state (design D3): success = `saveState: 'saved'` with the loaded campaign id matching the stamped id; any reported `errorMessage` (the catch-all `'error'` state, `useCampaignPersistenceStore.ts:477-481`, or the not-found `'idle'` path, `:450-453`) throws immediately naming the store's message — never a generic mount-signal timeout. Then run declared `postLoadActions` as front-door UI interactions, and return the stamped ids for teardown. Track created rows in a module-level set with a `test.afterAll` teardown deleting them via a standalone `APIRequestContext` (the `e2e/campaign-flow.spec.ts` afterAll pattern, extended per 1.2's R10 verdict if pilot rows are created).
  - Files: `e2e/helpers/scenarioPackLoading.ts` (new).
  - Acceptance: loader imports only `src/lib/scenarioPacks/` + Playwright (no store writes anywhere — grep `page.evaluate` uses to confirm none touch application stores); an intentionally corrupted fixture throws before any PUT; a forced client-side load failure (e.g. `goto` a stamped route whose row was deleted pre-navigation) throws naming the store's reported message (`campaign not found`), not a timeout.
- [x] 3.2 Create the flow-checkpoint minter (`scripts/qc/mint-scenario-pack.mjs` or `e2e`-side equivalent — implementer picks the home that can drive a flow to its checkpoint): drives the named flow-audit flow to the named `holdSafe: true` checkpoint through the existing flow machinery, captures the campaign envelope via live `GET /api/campaigns/:id`, runs 2.2's `canonicalizePackPayload` (design D11 — canonical template ids, cross-store refs stripped, mint-side residue scan), writes `e2e/scenario-packs/campaign/<id>.campaign.json` pretty-printed, and stamps the manifest provenance fields (genesis source, minted-at, base commit). The minter is the ONLY way payloads are written (spec: generator-minted).
  - Files: minter script (new), `e2e/scenario-packs/manifest.ts` (new — registry module with `manifestVersion`, typed per 2.1's manifest schema).
  - Acceptance: minting the navigation pack produces a payload that round-trips 2.1's campaign schema, carries only canonical template ids in the remapped id families, and has no cross-store references; the manifest entry validates and a guard test asserts the registry's declared `manifestVersion` equals the library's exported constant (spec: registry validation); re-running the minter yields an **identical canonical id graph** and a payload satisfying the checkpoint's invariant predicates — run-varying production content (in-fiction calendar anchor, `rngSeed`-derived market content) is expected to differ and is NOT compared structurally (design D7/D11 — proves the recapture job's invariant-level comparison is quiet on a no-change re-mint).
- [x] 3.3 Mint the three ungated pilot packs and author their parity specs: `navigation` (from `campaign-create-to-launch@contract-accepted` — state stops strictly BEFORE the launch click; that crossing is W2 seam (b) territory), `personnel` (from `personnel-hiring@roster-updated`), `experience` (tier (a), from `pilot-xp-progression@xp-surface-viewed`). Each parity spec (`e2e/scenario-packs/<id>.parity.spec.ts`) loads its pack via `loadCampaignPack` and hard-asserts the genesis checkpoint's invariants at the target route (spec: Parity Binding) — blocking `expect`s, no capture-tolerant findings, no `@smoke` tags.
  - Files: three payloads + three manifest entries + three parity specs (new).
  - Acceptance: all three parity specs green at `--project=chromium --workers=1`; each manifest entry carries provenance + pins + `parityAnchorJourney`; teardown leaves no rows (verify via API after a green run).
- [x] 3.4 Registration and scoping: add the `'**/scenario-packs/**'` glob to the three responsive projects' `testIgnore` arrays (`playwright.config.ts`, lines per 1.4); add `verify:qc:scenario-packs` to `package.json` (pack-library jests by path, then the parity specs sequentially via `node scripts/playwright/run-playwright.mjs test --project=chromium <spec> --workers=1` — the `verify:qc:seam-anchors` precedent).
  - Files: `playwright.config.ts`, `package.json`.
  - Acceptance: `npx playwright test --list e2e/scenario-packs/` schedules under chromium ONLY (spec scenario "Pack specs are excluded from viewport multiplication"); the verify lane runs green end-to-end from a clean shell; it is NOT wired into `verify:qc`'s chain or any CI workflow.
- [x] 3.5 QA — each parity spec green 3× consecutively at `--project=chromium --workers=1`; one deliberate parallel double-load of the navigation pack (two workers, same pack) lands two distinct campaign rows (spec scenario "Parallel loads never share a row"); `git diff` confirms zero edits to existing production files.

## 4. Encounter side — GATED: after the W2 implementation merges

- [x] 4.0 **Gate check (STOP on failure)**: verify on the current tree that the W2 implementation has landed — `e2e/helpers/matchLogSeeding.ts` exists and exports the parameterized seeder (W2 task 2.1's contract: match id + event stream + matches-row fields), and `e2e/seam-fresh-construction-no-instant-defeat.spec.ts` exists (the combat pack's genesis + parity anchor). If either is absent, STOP this group and report naming the missing artifact — do not stub, inline a substitute seeder, or capture from a different journey.
  - Files: read-only grep.
  - Acceptance: a written gate verdict naming both verified artifacts (or the STOP report).
- [x] 4.1 Extend `e2e/helpers/scenarioPackLoading.ts` with `loadEncounterPack(page, packId, {workerIndex})`: zod-parse (fail-loud — GameCreated-first + finite seed per 1.3), assert the manifest's `MATCH_LOG_DB_VERSION` pin **strictly equals** the live `matchLogStorageSchema.ts:4` constant (design D3 — no ladder exists for the match-log store; mismatch in either direction throws pre-seed naming the pin), stamp the matchId graph via 2.2, seed through the shared `matchLogSeeding` helper (never a private re-implementation), cold `goto` the gameplay route with the stamped match id, and await recovery completion via the exposed gameplay store (the recovery anchor's assertion surface). IndexedDB is per-browser-context — no server rows, no teardown surface.
  - Files: `e2e/helpers/scenarioPackLoading.ts`.
  - Acceptance: an unseeded or misordered fixture throws pre-seed; a pin-mismatched fixture (manifest pin ≠ live `MATCH_LOG_DB_VERSION`) throws pre-seed naming the mismatch (spec scenario "A stale encounter pack pin fails loud"); the loader contains no `indexedDB.open` of its own (grep — the shared helper owns the mechanism).
- [x] 4.2 Create the matchlog capture minter + mint the `combat` pilot pack: capture the real IndexedDB event stream (both stores) from the fresh-construction anchor's launched-and-advanced session — mid-battle, post-Initiative, mirrored canonical `unitRef` pair on the player side per the anchor's own fixture — write `e2e/scenario-packs/encounter/combat-midbattle.matchlog.json` + manifest entry (`parityAnchorJourney: anchor:seam-fresh-construction-no-instant-defeat`, `MATCH_LOG_DB_VERSION` pin). Author its parity spec: `loadEncounterPack` + hard-assert the recovery-anchor invariant set on the recovered session (loading complete, no error, active status, every deployed id resolves a movement capability, the bare canonical ref does not, full roster count, no manufactured terminal outcome).
  - Files: capture minter (new or a mode of 3.2's), payload + manifest entry + `e2e/scenario-packs/combat-midbattle.parity.spec.ts` (new).
  - Acceptance: parity spec green 3× at `--project=chromium --workers=1`; the payload's sequence-0 event is `game_created` with a finite `config.seed`; the capture is canonicalized (matchId → `pack:<packId>:match` at every occurrence — design D11); a re-capture run passes the matchlog schema + pin and satisfies the anchor's seed-agnostic invariant set — structural equality with the committed payload is NOT asserted (the engine seed is wall-clock-fresh per capture, `GameEngine.ts:97`; design D7).
- [x] 4.3 QA — group-4 additions green 3×; `npm run verify:qc:scenario-packs` (now including the combat parity spec) green end-to-end; grep confirms the pack tree still contains no store injection.

## 5. Fast-forward-minted packs — GATED: after the W3 implementation merges

- [x] 5.0 **Gate check (STOP on failure)**: verify on the current tree that the W3 implementation has landed — `src/lib/campaign/fastForward/` exports `fastForwardCampaign` returning the run report (W3 tasks 2.1/3.3 contract), and the live-parity acceptance suite (`src/__tests__/integration/fastForwardLiveParity.test.ts`, W3 task 5.3) exists; record its current green/failing status into the two packs' triage-only standing (design D9 — a failing or unproven acceptance keeps fast-forward-minted packs triage-only, it does not block their minting). If the driver or the acceptance suite is absent, STOP this group and report — do not mint campaign packs from any substitute generator.
  - Files: read-only grep + one targeted jest run.
  - Acceptance: a written gate verdict naming the verified exports and the acceptance status (or the STOP report). **Verdict**: both artifacts verified present — `src/lib/campaign/fastForward/fastForwardCampaign.ts` exports `fastForwardCampaign(campaign, params): Promise<FastForwardRunReport>` matching the W3 tasks 2.1/3.3 contract (`daysAdvanced`/`drainDays`/`dayReports`/`scenariosBridged`/`battles`/`outcomesApplied`), and `src/__tests__/integration/fastForwardLiveParity.test.ts` exists. Ran it (`npx jest src/__tests__/integration/fastForwardLiveParity.test.ts --ci`): **3/3 GREEN** ("parity holds between fast-forward and the identical-ordering discrete-step live-equivalent", "a deferred-fight ordering variation stays on the seam invariant set", "the live-equivalent does not reuse the code under test"). Per design D9, a GREEN acceptance means the two fast-forward-minted packs are **NOT** presently constrained to triage-only standing — recorded as such in both manifest entries' provenance/comments.
- [x] 5.1 Add the fast-forward mint mode to the minter: run a W3 fixture through `fastForwardCampaign()` in jest (economy shape: ≥1 contract paid, mixed transaction types; maintenance shape: post-battle damaged units), dump via `buildSerializedCampaign` (`campaignEnvelope.ts:32` — the pre-existing `campaign-persistence` builder), canonicalize via 2.2 (design D11 — post-battle dumps are exactly where roster mission records carry `encounterId`/`gameSessionId`; the strip is load-bearing here), write the payload + provenance recording the fixture id and run-report summary (the invariant summary the parity spec asserts against).
  - Files: minter (fast-forward mode), `src/lib/scenarioPacks/` additions if the dump needs a helper.
  - Acceptance: minted payloads pass 2.1's campaign schema and carry no cross-store references; two mints of the same fixture are equal on W3's enumerated **invariant-level field set** and carry identical canonical id graphs (rides W3's determinism contract, which promises invariant-level equality and nothing more — any invariant-level diff is a W3 bug to report, never absorbed); structural payload equality is NOT asserted (design D7). Implemented as `src/lib/scenarioPacks/mintFastForwardPack.ts` (the roster-projection-snapshot + dump-and-canonicalize helper) plus the env-gated minter/regression suite `src/lib/scenarioPacks/__tests__/mintFastForwardPack.test.ts` (`MEKSTATION_MINT_FASTFORWARD_PACK_ID`, invoked via `mint-scenario-pack.mjs`'s new jest transport). Extended `packStamping.ts`'s canonicalizer with `stripDanglingMatchReferences` (design D11.2's expanded strip inventory, task 1.2: `unitCombatStates[].lastCombatOutcomeId`, `repairQueue[].matchId` nulled; `dailyBattleAudit[].matches` emptied) — the first genesis class whose captured state carries real combat/repair data. Verified: 5 always-on jest assertions green (schema round-trip, no cross-store refs, two-mint determinism + identical canonical id graph, for both fixtures).
- [x] 5.2 Mint the `economy` pilot pack + parity spec: mid-campaign day N with a paid contract and mixed-type ledger; parity spec loads it and hard-asserts the route-rendered economy surfaces agree with the mint-time invariant summary (balance, transaction-type presence, contract status).
  - Files: payload + manifest entry + `e2e/scenario-packs/economy-midcampaign.parity.spec.ts` (new).
  - Acceptance: parity spec green 3× at `--project=chromium --workers=1`; manifest binds `fast-forward:<fixtureId>` and records the acceptance-status standing from 5.0.
- [x] 5.3 Mint the `maintenance` pilot pack + parity spec (design D8): post-battle damaged-units state; manifest declares `postLoadActions: ['advance-day']`; the parity spec asserts AFTER the post-load advance — repair tickets present in the repair bay (populated by the production CLEANUP processor, never written directly), damaged units listed, and the documented day-N+1 state (one day's costs posted). The pack's manifest documents the target state as minted-state-plus-one-production-day.
  - Files: payload + manifest entry + `e2e/scenario-packs/maintenance-repairbay.parity.spec.ts` (new).
  - Acceptance: parity spec green 3×; grep proves neither loader nor spec writes `campaignInventory` or any store; removing the post-load action makes the repair-bay assertion fail (proving the projection really comes from the production pipeline); neither this parity spec nor 5.2's asserts encounter-link or replay affordances on mission history (stripped at mint — design D11, spec scenario "Cross-store references never dangle"). **Drift correction discovered during authoring**: `repairQueueBuilderProcessor` is same-day-coupled to `recentlyAppliedOutcomes`/`pendingBattleOutcomes` — it never retroactively builds tickets from pre-existing `unitCombatStates` on a later day, so the minted payload's tickets are genuinely built during the mint's own drain (via a manual bridge→inject-`unitMaxStates`→drain loop, since production has no fast-forward writer for `unitMaxStates`), captured UNTOUCHED (`status: 'parts-needed'`); a matching `partsInventory` is stitched in (never consumed at mint time) so the loader's `advance-day` post-load action is what causes `repairProgressProcessor` to apply real progress for the FIRST time — proven by a dedicated negative-control test in the parity spec (PUT+goto without running `postLoadActions`) confirming every ticket stays at its minted status. **One additional sanctioned touch**: `e2e/helpers/scenarioPackLoading.ts`'s `loadCampaignPack` ran `postLoadActions` AFTER navigating to `targetRoute` — harmless for groups 3/4 (`postLoadActions: []` on every prior pack) but broke here, since the `advance-day` control (`day-advance-one-day`/`advance-day-btn`) is a dashboard-only surface absent from `repair-bay`/`finances`/etc. Reordered so post-load actions run on the dashboard, before the hop to `targetRoute` — verified no regression on all four prior packs (navigation/personnel/experience/combat-midbattle, 3× green each).
- [x] 5.4 QA — all six pilot packs registered with one subsystem tag each covering the full six-tag vocabulary (spec: Pilot Pack Coverage); `npm run verify:qc:scenario-packs` green end-to-end; parallel double-load spot-check repeated on one fast-forward pack. **Verified**: manifest now carries navigation/combat/economy/maintenance/personnel/experience (all six); `npm run verify:qc:scenario-packs` green end-to-end 3× consecutively (pack-library unit suites + 8 Playwright specs, 41 tests, 2 intentionally-skipped env-gated mint tests); a 2-worker parallel double-load of `economy-midcampaign` landed two distinct campaign rows (`economy-midcampaign-w0-...`/`economy-midcampaign-w1-...`), no collision.

## 6. CI re-capture-and-diff (nightly, triage-only)

- [x] 6.1 Create the recapture script (`scripts/qc/recapture-scenario-packs.mjs`): for every manifest entry — **(a)** re-validate the committed payload against the current pack schemas and all three pins; **(b)** re-mint from the recorded genesis source (flow-checkpoint packs replay their flow; fast-forward packs re-run their fixture in jest; the combat pack replays its capture journey at `--project=chromium --workers=1`) and validate the fresh mint identically; **(c)** compare fresh mint vs committed pack **at the invariant level per genesis class** (design D7: checkpoint invariant predicates / W3's invariant-level field set / the anchor's seed-agnostic invariant set) — never byte-structural payload equality, which the tree makes unachievable (wall-clock ids, random `rngSeed`, wall-clock engine seeds); exit nonzero naming pack + violated invariant or path on any failure, and report payload sizes (design R6).
  - Files: `scripts/qc/recapture-scenario-packs.mjs` (new) — a thin dispatcher (the `run-flow-audit.mjs`/`validate-flow-audit.mjs` tsx-subprocess precedent) spawning `scripts/qc/recapture-scenario-packs-core.ts` (new — the typed core importing the real pack schemas, the manifest, and W3's `comparableRunState` comparator directly; `scripts/**` is outside both `tsconfig.json`'s and `.oxlintrc.json`'s coverage, the same pre-existing convention `list-flow-audit.ts`/`validate-nonbattlemech-scope-matrix.ts` already sit under).
  - **Verified**: ran the full script against the real registry three ways — (1) all 6 packs unmutated, single invocation, exit 0, every pack size-reported ("6/6 pack(s) passed", sizes from 7.8 KB to 38.1 KB — design R6); (2) a schema-breaking mutation (dropped `body.finances.transactions` on economy-midcampaign) → exit 1 naming the path (`body.finances.transactions: Invalid input: expected array, received undefined`), caught pre-remint; (3) two invariant-breaking mutations — dropped navigation-briefing's sole mission record → exit 1 naming "navigation-briefing invariant violated — expected >=1 mission..., found 0" (caught pre-remint), and corrupted economy-midcampaign's committed `provenance.json` `invariantSummary.balanceDeltaCents` → a REAL jest re-mint ran and `assertRunStatesEqual` correctly diverged naming the pack + field (`seam invariant "balanceDeltaCents" diverged between economy-midcampaign fresh re-mint and economy-midcampaign committed`). Every genesis class exercised with a real re-mint at least once (navigation-briefing/personnel-roster/experience-pilot via the Playwright `scenario-pack-mint` project; combat-midbattle via the same, driving a full battle-launch + phase-advance journey; economy-midcampaign/maintenance-repairbay via the jest fast-forward transport). `git status` confirmed clean after every run (pass or fail) — the backup/restore `finally` block holds. No structural-diff normalization list exists anywhere in the script.
- [x] 6.2 Add the `scenario-pack-recapture` job to `.github/workflows/nightly-validation.yml`: `needs: install-deps`, checkout + the shared setup-node-and-install steps (mirror the existing jobs' pattern per 1.4's anchors), run 6.1's script, and append the job to `notify-on-failure.needs` so drift lands on the existing notification convention. NOT added to `pr-checks.yml` or any required check (spec scenario "Recapture is not a gate").
  - Files: `.github/workflows/nightly-validation.yml`.
  - **Verified**: job added with `needs: install-deps`, the same checkout + `setup-node-and-install` + Playwright chromium browser-cache steps `e2e-full` uses (needed since 4/6 packs re-mint via a live Playwright journey), then `node scripts/qc/recapture-scenario-packs.mjs`; `notify-on-failure.needs` now reads `[full-statistical-proofs, e2e-full, coverage, scenario-pack-recapture]`. YAML parse-verified via `js-yaml` (`YAML parses OK`, all 6 jobs present, needs array correct). `grep -rln "scenario-pack-recapture|recapture-scenario-packs" **/*.yml` across the repo returns only `nightly-validation.yml` — no reference in `pr-checks.yml` or anywhere else.
- [x] 6.3 QA — full local recapture pass green; `git diff` on the workflow shows only the one job + the `needs` append.
  - **Verified**: `node scripts/qc/recapture-scenario-packs.mjs` (no `--pack` filter, full registry) → `6/6 pack(s) passed`, exit 0, `git status --short` clean immediately after. `git diff --stat -- .github/workflows/nightly-validation.yml` shows one file, +65/-1 lines; the single `-` line is the `notify-on-failure.needs` array being replaced by its 4-entry form — every other changed line is a pure addition (the new job block's comment header + steps).

## 7. Verification battery + spec truth

- [x] 7.1 `npm run typecheck && npm run lint && npm run format:check` clean across all new files.
  - **Verified** (run twice — pre- and post-7.6 rebase, identical results both times): `npm run typecheck` → `tsc --noEmit --skipLibCheck` clean, 0 errors. `npm run lint` → `Found 1935 warnings and 0 errors`; targeted grep of the full lint output for `scenarioPack`/`scenario-packs` returns zero matches — none of the pre-existing warnings (all `no-console`/`no-unused-vars`/`max-lines` on unrelated pre-existing files) touch this change's files. `npm run format:check` → `All matched files use the correct format` across 6799 files.
- [x] 7.2 Full unit lane green (`npx jest --selectProjects unit --ci`) including the pack-library suites; `npm run verify:qc:scenario-packs` green end-to-end; each parity spec's 3×-green evidence recorded in the PR description.
  - **Verified**: pack-library suites 3/3 passed (`packSchemas.test.ts`, `packStamping.test.ts`, `mintFastForwardPack.test.ts` — 39 passed, 2 intentionally env-gated-skipped, 41 total). `npm run verify:qc:scenario-packs` green end-to-end, run twice (pre-7.6-rebase and post-7.6-rebase): identical both times — jest 3 suites passed (39 passed/2 skipped/41 total) + Playwright 9/9 (`manifest.guard.spec.ts` 2, `navigation-briefing` 1, `personnel-roster` 1, `experience-pilot` 1, `combat-midbattle` 1, `economy-midcampaign` 1, `maintenance-repairbay` 2). Each parity spec's 3×-green evidence is already recorded in tasks.md from prior groups (3.5 "each parity spec green 3× consecutively"; 4.3 "group-4 additions green 3×"; 5.4 "`npm run verify:qc:scenario-packs` green end-to-end 3× consecutively") — this pass adds two further full-green confirmations (pre/post rebase).
  - The full `npx jest --selectProjects unit --ci` lane, run from a **neutrally-named path** (a throwaway `git worktree add --detach` outside `.claude/worktrees/`, node_modules junction-linked): 2210/2211 suites passed, 30744/30779 tests passed, 34 intentionally skipped, **one** failure — `src/simulation/__tests__/integration.test.ts`'s `PROFILE_AVG_GAME_BUDGET_MS` perf assertion (`integration.chunk-1.test-helpers.ts:347`), unrelated to and untouched by every scenario-packs commit (`git log` confirms), which measured 1592–1684ms against its 1500ms budget under this session's heavy concurrent background load and passed cleanly in isolation (2ms) on a dedicated re-run — a confirmed contention flake, the same class the repo's own 2026-07-11 council decision (`openspec/council-decisions/2026-07-11-nightly-green-preemption.md`) already documents and 3×-widens for three sibling budgets in this exact file family (this specific chunk-1 budget wasn't one of the three widened; out of scope to touch here).
  - **Worktree-path-collision finding (documented, not a defect in this change)**: running the identical unit lane directly from this session's implementation worktree (`.claude/worktrees/scenario-packs`) additionally surfaces 3 spurious `desktop/electron/__tests__/*.test.ts` suite failures ("Cannot find module 'electron'"). Root-caused: `jest.config.js`'s pre-existing `testPathIgnorePatterns: ['<rootDir>/desktop/', ...]` (untouched by this branch — `git log -- jest.config.js` shows no scenario-packs commit) fails to exclude `desktop/` only when `<rootDir>` itself resolves to a path containing the literal segment `scenario-packs` (this session's worktree directory name) — confirmed by control experiment: substituting an unrelated segment that also appears in the cwd (`**/worktrees/**`, absent from any real e2e file path) reproduces the identical breakage, and running from a neutrally-named path makes the exclusion work correctly (`--listTests | grep -c desktop` → 0) with zero related failures. The `electron` package itself is absent from `node_modules` on the MAIN checkout too (pre-existing, unrelated dev-env gap). Both findings are artifacts of this session's temporary worktree name, not defects in the shipped code; the neutral-path run above is the authoritative evidence for this task.
- [x] 7.3 Discovery proof: `npx playwright test --list` shows scenario-pack specs under chromium only, and the other projects' scheduled counts are unchanged from the pre-change baseline (design R8).
  - **Verified** from the same neutrally-named worktree used for 7.2 (the CURRENT worktree's own name, `scenario-packs`, makes Playwright's `testIgnore: ['**/scenario-packs/**']` match every file's *absolute* path — Playwright matches testIgnore against the full resolved path, not testDir-relative — confirmed via control pattern `**/worktrees/**`, which reproduces the identical "0 tests scheduled" breakage for Mobile Chrome/Tablet Portrait/Tablet Landscape on this worktree; a session-environment artifact of the worktree's chosen name, never a defect in the shipped `playwright.config.ts`):
    - `npx playwright test --list e2e/scenario-packs/` → all 9 scenario-pack tests scheduled under `[chromium]` only, 0 under any other project.
    - Per-project totals, PRE-CHANGE (`428f63aa9`) vs POST-CHANGE (current HEAD), both captured from the identical neutral path: chromium 712→721 (**+9**, exactly the new pack tests); Mobile Chrome 697→697 **UNCHANGED**; Tablet Portrait 697→697 **UNCHANGED**; Tablet Landscape 697→697 **UNCHANGED**; smoke 181→181 **UNCHANGED**. Design R8 satisfied.
- [x] 7.4 Spec-coverage self-audit: walk `specs/scenario-packs/spec.md` requirement by requirement and name the artifact delivering each SHALL (registry/manifest incl. the manifestVersion guard: 3.2/2.1; generator-minted: 3.2/4.2/5.1 + provenance fields; front-door loading: 3.1/4.1/5.3; fail-loud validation incl. the deserializer-floor schema, the encounter pin assertion, and the post-`goto` named-error surfacing: 2.1/3.1/4.1; id templating + mint-time canonicalization + cross-store strip: 2.2/3.2/3.5/5.1; version pins + invariant-level recapture: 2.1/3.2/4.1/6.1/6.2; parity + gate rule: 3.3/4.2/5.2/5.3 + absence of any PR-gate wiring; pilot coverage: 5.4; registration/scoping: 3.4/7.3). Any uncovered SHALL blocks completion.
  - **Verified** — every requirement traces to a real, inspected artifact: **Registry and Manifest** — `e2e/scenario-packs/manifest.ts` (`export const manifestVersion = MANIFEST_VERSION` at `MANIFEST_VERSION = 1` in `packSchemas.ts:427`); `packSchemas.test.ts`'s registry guard asserts the two constants match (3.2/2.1). **Generator-Minted** — `scripts/qc/mint-scenario-pack.mjs` (flow-checkpoint + capture-matchlog modes, 3.2/4.2) and `src/lib/scenarioPacks/mintFastForwardPack.ts` (fast-forward mode, 5.1); every manifest entry carries `provenance: { genesisSource, mintedAt, baseCommit }` (inspected `navigation-briefing`'s entry directly). **Front-Door Loading** — `loadCampaignPack` (`e2e/helpers/scenarioPackLoading.ts:361`, PUT + `goto` riding the shell's own GET, 3.1) and `loadEncounterPack` (`:576`, IndexedDB seed via the shared `matchLogSeeding` helper + cold `goto`, 4.1); `postLoadActions` driven as real UI clicks (5.3). **Fail-Loud Validation** — `packSchemas.ts`'s zod deserializer-floor schema (2.1), pre-PUT/pre-seed validation in both loaders (3.1/4.1), the `MATCH_LOG_DB_VERSION` strict-equality pin assertion in `loadEncounterPack`, and the post-`goto` named-error surfacing reading `campaignPersistence.getState()` via the sanctioned `storeExposure.ts` touch (D3 amendment, inspected directly — additive-only diff confirmed against `428f63aa9`). **Id Templating + Canonicalization + Strip** — `packStamping.ts`'s `stampPackIds`/`canonicalizePackPayload` + `stripDanglingMatchReferences` (2.2/5.1), zero-residue scan proven by adversarial fixture tests, 3.2's mint-side canonicalization, 3.5's parallel-double-load spec. **Version Pins + Invariant Recapture** — `schemaVersion`/`MATCH_LOG_DB_VERSION`/`manifestVersion` pins (2.1), minter provenance pins (3.2), the loader's pin assertion (4.1), `scripts/qc/recapture-scenario-packs-core.ts`'s per-genesis-class invariant predicates + `comparableRunState` comparator (6.1), the nightly job wiring (`.github/workflows/nightly-validation.yml` — `scenario-pack-recapture` job present, `notify-on-failure.needs` includes it, absent from `pr-checks.yml`, 6.2). **Parity + Gate Rule** — six parity specs (3.3/4.2/5.2/5.3), zero pack references in `pr-checks.yml` or any required-check config (grep-confirmed). **Pilot Coverage** — all six subsystem tags present in the manifest (5.4). **Registration/Scoping** — `testIgnore: ['**/scenario-packs/**']` on the three responsive projects + `verify:qc:scenario-packs` package script (3.4), discovery proof (7.3). No uncovered SHALL found.
- [x] 7.5 `npx openspec validate add-scenario-packs --strict` passes after any spec-delta edits (formatter first — design R4).
  - **Verified** (run twice — pre- and post-7.6 rebase): `npx openspec validate add-scenario-packs --strict` → `Change 'add-scenario-packs' is valid`, both times. No spec-delta edits were made during this verification group, so no formatter re-run was required (design R4's ordering was already satisfied by 7.1's clean `format:check`).
- [x] 7.6 Ship-time ledger reconciliation (design R5): rebase onto current `origin/main`, confirm `openspec/active-change-ledger.json` lists exactly the then-active change folders including `add-scenario-packs`, and confirm `node scripts/qc/validate-openspec-ci-quality.mjs` (the `qc:openspec-ci:validate` gate) passes.
  - **Verified**: `origin/main` had advanced 3 commits past this branch's cut point (`428f63aa9`) — `6e0ed7868` (W5 `add-viewport-layout-sweep`, declarative-only), `9865ba260` (perf-budget/CI triage fix, #1049), `d26fd385b` (W6 `add-subsystem-lanes-and-ci`, declarative-only). `git diff --stat` confirmed zero file overlap with anything this change touches (no `playwright.config.ts`, `package.json`, `.github/workflows/nightly-validation.yml`, or `src/lib/scenarioPacks/**`/`e2e/scenario-packs/**` in the upstream diff), so `git rebase origin/main` completed cleanly with **zero conflicts** (new HEAD `e7a5ddc3f`). Post-rebase: `openspec/active-change-ledger.json` lists exactly 3 entries (`add-scenario-packs`, `add-viewport-layout-sweep`, `add-subsystem-lanes-and-ci`), matching exactly the 3 non-archived folders under `openspec/changes/` (`ls` confirms). `node scripts/qc/validate-openspec-ci-quality.mjs` → `workflowContracts=8/8 aggregatorNeeds=17/17 protectedContexts=4 packageScripts=3/3 activeOpenSpecChanges=3 accountedActiveOpenSpecChanges=3 errors=0`. Post-rebase re-verification of 7.1/7.2/7.5 (typecheck, format:check, lint, `verify:qc:scenario-packs`, `openspec validate --strict`) all reconfirmed clean/green above.
