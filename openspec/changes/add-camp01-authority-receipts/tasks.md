CAMP-PROOF is implemented as six ordered focused PRs — PROOF-1 contract/tests, PROOF-2 writer/validator, PROOF-3 exact-SHA controller/worktree lifecycle, PROOF-4 runner integration, PROOF-5 adversarial verification, PROOF-6 receipt handoff — one per numbered section below, in order. Each PR MUST stay under 500 non-generated changed lines and 15 files, own its one behavior seam, pass focused tests plus Node 22 typecheck/lint/format and strict OpenSpec/QC, receive sequential independent review, merge with a SHA guard, verify on exact main, and prune its branch/worktree before the next seam begins. The former single-PR shape is retired: it could not credibly satisfy the cap across six subsystems.

## 1. Immutable Contract — PR PROOF-1
- [x] 1.1 Add `camp01-authority-receipt.contract.mjs` with strict v1 schema, canonical repository/fetch identity, frozen ten-child set, twelve logical `@node|@npm` command rows/run roots and reviewed digests, frozen CAMP-01E/H command-index/invocation/PNG capture contracts, the CAMP-01F persistence and CAMP-01G Mech Bay reporter/authority contracts, canonical PROOF/H repair-row extensions/virtual gates, predecessors, caps, H reporters, bootstrap ref, artifacts, and assertions.
  - Receipt (recorded posthumously 2026-08-02): PR #1134 merged head `b416c84466b3b7790725f534a50f23712227af0d` as `196661d4009e34254805f0b5e7317f41451cf88a`; `scripts/qc/camp01-authority-receipt.contract.mjs` and its contract tests are present on main.
- [x] 1.2 Add contract tests that recompute every digest and reject unknown fields/ids, caller/other executable tokens, root/argv/order/chaining drift, missing/extra/unknown-wave/reordered command-index/invocation/PNG capture contracts, F/G reporter/artifact/test/identity drift, invalid repair declarations/rows/manifests/sources, wrong PROOF lexical or H lowest-rank roots, non-null root aliases, cyclic/self/chained/multi-root/nonterminal causes, gate/cleanup/cap/provenance gaps, unsafe retained values, and H observation/final/report drift.

## 2. Writer and Validator — PR PROOF-2

PROOF-2 is delivered as two declared ordered sub-PRs under the cap-driven split rule (wave-map rule 7): PROOF-2A writer baseline (merged, receipt below) and PROOF-2B validator completion — the spec-pinned `scripts/qc/validate-camp01-authority-receipt.mjs` CLI entry, the `qc:camp01-authority-receipt:{write,validate}` package scripts, and every task 2.1-2.4 obligation not already covered by the 2A baseline — each sub-PR within the 500-line/15-file cap. Tasks 2.1-2.4 close only when PROOF-2B merges with an exact-main receipt; if PROOF-2B cannot fit the cap, declare a further split here before implementing it.

- [x] 2.0 PROOF-2A writer baseline.
  - Receipt (recorded posthumously 2026-08-02): PR #1137 merged head `d3ef15817c002ceb46d384b2ad3e879dca443e2a` as `851c1d180789f9d00f370445e68aee3446c04152` (`feat(camp): add authority receipt writer`); delivered `scripts/qc/camp01-authority-receipt.mjs` (writer, receipt-directory validation, H identity issuance, PROOF-02 normalization), `scripts/qc/camp01-authority-receipt.schemas.mjs`, focused writer tests, and two package entries within the cap (4 files, +494 lines).
- [x] 2.1 Implement strict argument/provenance parsing, random parent run identity, exclusive staging, immutable sequence execution, writer-owned `command-result.json`, closed D10 artifact/manifest/cap schemas and canonical bytes, kind/source-bound exact-set digest-only identity registry, atomic single-child publication, and privacy allowlisting.
- [x] 2.2 Implement exact producer/reporter/schema/source/status/id/path/digest-owner validation; require CAMP-01F production-PUT/accepted-response/server-read identity equality and recovery facts plus CAMP-01G cold-reload/source/cached-metadata/BV/unresolved/no-substitution facts, parent/witness/execution/label/status, observation fact-wrapper/report-fingerprint, final complete fact/evidence, combat, reconciliation, exact-main-to-reviewed-head cap, manifest-file-type, observed entity-kind/source equality, and sorted exact-PNG-set capture attestations with matching artifact/manifest digests, invocation, equal pre/post state/counter digests, and pinned allowlist/barrier digests; reject stale, cross-run, duplicate, omitted, reordered, substituted, tampered, incomplete, abnormal-exit, predicate, and witness drift.
- [x] 2.3 Implement complete PROOF-02 reproduction publication for exit 0/1, null fingerprints only for passed observations and writer-derived non-null fingerprints for failed/missing observations, self-digest-free exact-byte artifacts, plus zero-command triage with exact observation-set equality and outcome-specific disposition validation.
- [x] 2.4 Implement CAMP-01H parent/three-child identity issuance, follower-ineligible observations with complete observed/unavailable typed facts after six ordinary exit-0/1 attempts, exact one-to-many failed-report/finding/cause-row mapping with canonical source id/fingerprint, reconciliation, and fresh finals whose complete authority plus six invocations/reports/wave/assertions pass after every repair/cleanup or blocker.
  - Receipt (PROOF-2B, closes 2.1-2.4 per the sub-seam declaration): PR #1148 merged head `b8af6b149cc9e00125b515e12d145a4230c2b7c5` as `eb2aa17486d759554c552fbfe04da9616d9ba478` (`feat(camp): complete PROOF-2B public authority receipt validator`); delivered the spec-pinned `scripts/qc/validate-camp01-authority-receipt.mjs` public CLI, the receipt invocation identity-drift guard in `validateReceiptDirectory`, the `qc:camp01-authority-receipt:validate` package script repoint with the legacy low-level validate op removed, and the writer-backed validator test suite. The PROOF-2B audit confirmed the 2A baseline plus this seam satisfy every validator-layer-testable 2.1-2.4 obligation; controller/runner/adversarial obligations remain PROOF-3/4/5. Exact-main proof on `eb2aa1748…` (Node 22.22.0): focused Jest 18/18 across writer+validator suites, strict OpenSpec 236/236, QC 19/19 with zero errors; 29/29 PR checks; three sequential independent reviews approved with zero P0/P1 findings and the single P2 fixed in-PR; merged branch/worktree pruned.

## 3. Exact-SHA Controller — PR PROOF-3

PROOF-3 is delivered as four declared ordered sub-PRs under the cap-driven split rule (wave-map rule 7), mapped one-to-one to tasks 3.1-3.4: PROOF-3A controller CLI/state core (task 3.1), PROOF-3B pinned-environment construction (task 3.2), PROOF-3C Git/GitHub identity verification (task 3.3), PROOF-3D durable export and cleanup lifecycle (task 3.4). Each sub-PR stays under the 500-line/15-file cap, owns its one behavior seam, invokes receipt validation only through the public `validate-camp01-authority-receipt.mjs` entry, and merges with review, SHA guard, exact-main verification, and prune before the next begins. A task closes only when its sub-PR merges with an exact-main receipt; if a sub-seam cannot fit the cap, declare a further split here before implementing it.
- [x] 3.1 Add `run-camp01-authority-receipt.mjs` and its `register-pr-target`, `proof`, and `cleanup` CLI/state contract; implement subject-specific product/audit/none provenance, merged-spec PROOF/H repair registration plus exact closed source receipt/observation/cause and registry-set equality, the ten-child/predecessor/cap preflight, reviewed-head observation/final and exact-main detached worktrees, clean manifests, and non-reparse row-root confinement.
  - Receipt (PROOF-3A): PR #1150 merged head `72b88da7ff50aefc4d4c1b3a8e8ce9068aea949e` as `90f1570c4d5528e9b70813e09de4213dc4269ac7`; controller core with fail-closed 3B/3C/3D dependency seams, provenance-bound exact-main gate, and public-validator-only routing; three sequential independent reviews approved after one provenance-binding fix and one test-rigor remediation; exact-main proof passed camp01 suites 36/36, strict OpenSpec 236/236, QC 19/19; the one CI shard failure was verified as an unrelated port-fixture flake (test passes 7/7 locally on merged main; CI rerun green on record); branch/worktree pruned.
- [x] 3.2 Pin Node/npm/Git/System32 absolute binaries, expand only `@node|@npm`, run fixed bootstrap, construct child environments from zero with writer temp/profile and empty npm configs, bind tool/config/lockfile/environment digests, and reject PATH/CWD, credentials, proxies, Git/Node/npm/CI/cloud/product/Playwright ambient drift.
  - Receipt (PROOF-3B): PR #1151 merged head `6c4e5ec991fa397508279e0252f74a96a63b2cde` as `82391552c635233dde34c3cf55f71b62dd91750a`; pinned proof environment with validated SystemRoot resolution, zero-built child environments, digest-bound execution identity, and real-writer executeReceipt; Git binary and writer context remain fail-closed PROOF-3C seams; three sequential independent reviews approved after SystemRoot hardening, Linux-CI portability restructure, and hermeticity/rejection-coverage remediation; exact-main proof passed camp01 suites 58/58, strict OpenSpec 236/236, QC 19/19; branch/worktree pruned.
- [x] 3.3 Use the verified Git binary, literal HTTPS URL, disabled config/credentials/proxies/prompts, and a writer-owned empty bare proof repository; require fetched head/main OIDs equal pinned GitHub API identity before verifying PR heads/merges, approvals, ledger, and ancestry with mocked GitHub/transport adapters.
  - PROOF-3C is delivered as three declared ordered sub-PRs (cap-driven split per the 2026-08-03 pre-implementation audit, which estimated the unsplit seam at 745-940 changed lines against comparable merged slices and stopped fail-closed without code): **PROOF-3C1 Git trust foundation** — verified absolute Git resolution pinned to the schema version, hardened `shell:false` invocation, literal frozen HTTPS URL, disabled config/credentials/proxies/rewrites/prompts, writer-owned empty bare session repository, fetched head/main OID equality against injected GitHub API identity, exposing `resolveVerifiedGit`; **PROOF-3C2 GitHub provenance** — frozen repository id/node/name/base validation, PR origin/base/head/merge checks, exact-head non-dismissed non-author approval with reviewer-permission verification, ledger and ancestry verification, injectable production transport with mock-only tests, owning `resolveRepairRegistration`, `verifyPreflight`, and `resolveWriterContext`; **PROOF-3C3 local target authority** — real-Git owned-target inspection, detached proof-worktree record construction, canonical non-reparse identity, HEAD/tree/index/tracked clean observation with sorted ignored/untracked manifests, owning `inspectOwnedTarget`, `createProofTarget`, and `observeCleanState`. Each sub-PR stays under the 500-line/15-file cap with its own review, SHA-guarded merge, exact-main receipt, and prune, in order; task 3.3 closes only when all three have exact-main receipts; a further split must be declared here first if any sub-seam cannot fit. Fact-resolver ownership (declared 2026-08-04 per 3C2 review): the injected `resolveRepairSource`, `resolveWriterInputs`, and `resolvePreflightFacts` seams consumed by `camp01-github-provenance.mjs` are owned as follows — PROOF-3C3 owns the target-derived facts (tree SHA, cap file/line/binary provenance from owned-target inspection); PROOF-3D owns the durable-receipt-derived facts (predecessor receipt/cleanup waves, repair sources and gates, receipt identity registry, reviewed-head records) and the production composition of all three resolvers wiring the `createGitHubProvenance` factory into the controller.
  - Receipt (PROOF-3C1): PR #1153 merged head `c38da440c` as `7169f2d543c731b9b396e829a8bff00f1587582a`; git trust foundation (verified absolute Git discovery pinned to the schema version, hardened zero-base `invokeGit`, frozen HTTPS URL policy with narrow test-pinned local-remote escape, writer-owned empty bare sessions, fetched head/main OID equality vs injected API identity, `resolveVerifiedGit` wired as the 3B production default); three sequential independent reviews approved (review 1 live-probed the real frozen-URL fetch under the hardened environment); exact-main proof passed camp01 suites 73/73 (pre-QC-suite baseline), strict OpenSpec 236/236, QC 19/19; branch/worktree pruned.
  - Receipt (PROOF-3C2): PR #1154 merged head `476857aa1` as `9633ca78f7c3259957201964f74fd6672e600ac3`; GitHub provenance (frozen repository identity gate before any PR conclusion, exact-head non-dismissed non-author approval with separate permission lookup, ledger read from verified history at the merge SHA, same-session real-git ancestry, hardened never-in-tests HTTPS transport, consumer seams with preflight-to-writer provenance binding); full P.2 drift matrix 9/9 exact-message tested plus stale-approval and binding-mismatch guards; three sequential independent reviews approved after remediation with closures trace-verified; exact-main proof passed camp01 suites 155/155, strict OpenSpec 236/236, QC 19/19; branch/worktree pruned.
  - Receipt (PROOF-3C3, closes 3.3): PR #1156 merged head `a323619ce` as `9b036c6c982e670cb8f612577c9bc4069a74e7e6`; local target authority (real-Git owned-target inspection with porcelain worktree identity, detached proof-worktree records with post-creation re-verification and pre-write reparse rejection, lstat-guarded non-reparse clean-state observation, code-unit-sorted ignored/untracked SHA-256 manifests, target-derived writer facts with schema-proven fail-closed reviewed-head placeholders); three sequential independent reviews approved after two P1 module repairs (consumer-contract sort order; run-root terminal reparse blind spot) with pins empirically verified; exact-main proof passed the camp01 eight-suite baseline 167 passed/3 host-skips, strict OpenSpec 236/236, QC 19/19; branch/worktree pruned.
- [x] 3.4 Implement exclusive durable export, non-self-referential command-result/receipt-manifest digest ownership and recomputation, atomic rename, reopen validation, separate detached-proof and subject-bound owned target records, the single-use exact-reviewed-head CAMP-PROOF bootstrap, strict pre-edit creation for PR-owning rows, no owned target for reproduction, independent non-force removal, owned-ref exact-OID compare-delete, and writer-owned follower cleanup receipts.
  - Receipt (PROOF-3D1): PR #1159 merged head `c3256a705` as `823018b36a13473684c458b819442c2f2bebd09f`; durable export/reopen with destination-time proof-worktree disjointness, finalized-set fidelity, staged recomputation, atomic publication, and stage-localized public-validator invocation; three sequential independent reviews approved after one P2 fix; exact-main proof passed the nine-suite baseline 183/5-skips, strict OpenSpec 236/236, QC 19/19; pruned.
  - Receipt (PROOF-3D2): PR #1160 merged head `75a55165f` as `89aebee78f21d2ffce44b9991cf657d641e8f5fe`; cleanup authority with durable-revalidation-first ordering, creation-bound reinspection immediately before every non-force mutation, exact-old-OID compare-delete, reality-probed vacuous reproduction, exactly-recorded orphan recovery, and writer-owned cleanup receipts; reviews fixed one P1 (dead evidence-tree exclusion masked by harness complicity) and seven P2 hardenings, all confirmed closed; no reachable wrong-deletion path found under the P.4 threat model; exact-main proof passed the ten-suite baseline 221/6-skips, strict OpenSpec 236/236, QC 19/19; pruned.
  - Receipt (PROOF-3D3, closes 3.4): PR #1161 merged head `0cba6ad44` as `57b86f711f810c8438bd3363fd3188e2b76154b0`, repaired fix-forward by PR #1162 merged head `6a110e57e` as `df64dc5602112fbe397e7be1a01c4afe5b6a5646` (a dependency-threading gap made the new writer tests fail on the Linux CI shard; #1161 was merged against a red shard through an orchestration gating fault, disclosed in the external receipt; the repair's green Linux shard is the decisive oracle); durable facts with public-validator-gated reader/index, the three fact-resolvers, crash-replayable single-use bootstrap, and production composition with shared state store and index-backed cross-process durable validation; reviews fixed two P1 composition defects (production cleanup deadlock; state plumbing masked by a state-injecting oracle), confirmed closed; exact-main proof passed the eleven-suite baseline 234/6-skips, strict OpenSpec 236/236, QC 19/19; pruned.
  - Section-3 closure blockers (fail-closed, named owners; verified real by two independent reviews) — BOTH NOW CLOSED, see the PROOF-5E1/PROOF-5E2 receipts under task 5.3: (1) CLOSED by PROOF-5E1 (#1186 `8180672cc`, which added `runId`/`parentRunId` to the triage schema and the `triage parent run drift` guard) and (2) CLOSED by PROOF-5E1's `resolveReceiptRow` boundary completed by PROOF-5E2 (#1187 `2987aa0e8`); the historical statements follow verbatim for provenance: (1) the merged writer/schema surface cannot produce a public-valid `proof-02-triage` receipt — the triage schema exposes neither `runId` nor `parentRunId` while artifact validation requires the run binding ('parent run drift'); owner: the PROOF-2 writer/schema surface, to be repaired as a declared follow-up seam before PROOF-02 triage runs. (2) Literal `WAVE_CONTRACTS` resolution in the writer and public validator excludes dynamically named repair rows from durable validation; owner: the PROOF-1 contract surface, to be declared before repair-row receipts are produced. Neither is waived by this closure; both reject rather than weaken. PROOF-5 adversarial ledger additionally carries the accumulated deferred negative-branch coverage recorded in the external receipts (#1148-#1162). PROOF-3D additionally owns (per the fact-resolver ownership declaration above) the durable-receipt-derived facts and the production composition of `resolvePreflightFacts`/`resolveWriterInputs`/`resolveRepairSource` wiring the `createGitHubProvenance` factory into the controller, and must account for orphaned failed-creation proof worktrees, the transient-root localization of the controller's public-validator payload, and prepared-environment runtime-root retry residue (review carryovers from 3A/3B/3C3). A pre-implementation cap audit is mandatory; if 3.4 cannot fit one 500-line/15-file PR, declare its sub-split here before implementing.
  - PROOF-3D is delivered as three declared ordered sub-PRs (cap-driven split per the 2026-08-04 pre-implementation audit, which estimated the unsplit seam at 1,180-1,420 changed lines against the five merged comparable slices and stopped fail-closed without code): **PROOF-3D1 durable export and reopen** — exclusive initiating-checkout durable root with non-reparse confinement, finalized-set-only copy, non-self-referential digest ownership with byte/manifest recomputation, collision-safe staging inside the durable destination parent with atomic rename after full validation, transient and durable public-validator invocation with stage-localized roots, reopen validation, and the export drift/collision/partial-copy/tamper rejection matrix; **PROOF-3D2 cleanup authority and cleanup receipts** — separate detached-proof and subject-bound owned-target lifecycle records with creation-time cleanup bindings, strict pre-edit target creation, reproduction with no owned target, orphaned failed-creation worktree recovery, exact porcelain/HEAD/clean-manifest reinspection immediately before independent non-force removal, owned-ref exact-old-OID compare-delete, removal ordered strictly after durable reopen/revalidation, writer-owned follower cleanup receipts, and the complete P.4 rejection matrix; **PROOF-3D3 durable facts and production composition** — durable receipt reader/index, `resolvePreflightFacts`, completion of `resolveWriterInputs` over 3C3 target facts plus durable registry/reviewed-head/reproduction/triage inputs, `resolveRepairSource` from reopened durable receipts only, the single-use exact-reviewed-head CAMP-PROOF bootstrap, production composition of `createGitHubProvenance` and all three resolvers into the controller, and prepared-environment runtime-root retry-residue cleanup. Order is dependency-driven (3D1 establishes the durable authority 3D2 protects during removal; 3D3 consumes only durable validated receipts). Each sub-PR stays under the 500-line/15-file cap with its own review, SHA-guarded merge, exact-main receipt, and prune, in order; task 3.4 closes only when all three have exact-main receipts; a further split must be declared here first if any sub-seam cannot fit.

## 4. Runner Integration — PR PROOF-4

PROOF-4 is delivered as five declared ordered sub-PRs (cap-driven split per the 2026-08-04 pre-implementation audit, which estimated the unsplit section at 1,680-2,160 changed lines across five independent behavior seams against the eight merged 300-470-line comparables and stopped fail-closed without code): **PROOF-4A guarded capture transaction and PNG attestation core** — CAMP-01E/H capture-contract admission by exact invocation/index/path, fixture-only seeding, monotonic DOM/app-write counters with the capture-relevant mutation barrier, canonical pre/post digest equality, exact PNG digest binding, sorted digest-only attestations, deletion on drift; **PROOF-4B CAMP runner isolation and runtime cleanup** — fail-closed CAMP context validation from writer-issued identities, fresh browser temp/profile/storage and invocation-local runtime databases, CAMP-only routing of every transient output below the declared invocation runtime, normalization-before-cleanup ordering with exact non-reparse invocation-runtime removal only, and unchanged non-CAMP defaults; **PROOF-4C PROOF-02 and CAMP-01F/G Playwright authority reporters** — opt-in JSON collection without raw retention, complete normalized observed-test inventories with writer-derived bounded fingerprints, trusted run-bound F (production PUT/accepted/readback) and G (cold-reload Mech Bay) fact ingress, observed-id propagation into the writer, and rejection of missing/duplicate/stale/caller-selected/cross-run facts; **PROOF-4D CAMP-01H runner identities and six normalized reports** — writer-issued label/id-map consumption only, the six exact report normalizations, witness mapping (02,04 / 01,05 / 03,06), fresh context/execution/witness binding with disjoint report paths/digests, ordinary-exit observation completeness, and no output outside the writer-issued directory; **PROOF-4E viewport orchestrator and package command surface** — `run-viewport-sweep.mjs` importing the contract's final three H logical arrays through the shared verified resolver with sequential `shell:false` spawning and first-failure propagation, replacement of the chained `verify:qc:viewport-sweep` script, and the task 4.3 package commands wiring the production controller entry per the PROOF-3D3 deferral, with no dependency change. Order is dependency-driven (4A's attestations and 4B's runtime feed 4C/4D's reporters; 4E consumes 4D's arrays). Each sub-PR stays under the 500-line/15-file cap with its own pre-implementation cap audit, review, SHA-guarded merge, exact-main receipt, and prune, in order; tasks 4.1-4.3 close only against the sub-seam receipts covering their obligations; a further split must be declared here first if any sub-seam cannot fit.
  - Receipt (PROOF-4A): PR #1165 merged head `07aab17fe` as `5d6cd0d7c9b5956024756a5458bdc0c9a996b78b`; guarded capture transaction and PNG attestation core (exact CAPTURE_CONTRACTS admission, fixture-only seeding, ten-channel counter/barrier instrumentation with bidirectional GET/HEAD exemption pins, canonical pre/post digest equality, exact PNG digest binding, codepoint-sorted digest-only attestations the real writer/validator accept as oracle, sibling-preserving delete-on-drift, structurally un-stubbable production helper); three sequential independent reviews approved after remediation — review 1 removed production guard-bypass knobs and patched IDBCursor/sendBeacon/WebSocket barrier gaps in the real instrumentation, review 3 empirically proved and then killed a fixture-policy mutation survivor; exact-main proof passed the thirteen-suite baseline 264 passed/6 host-skips, strict OpenSpec 236/236, QC 19/19; pruned. Deferred negatives recorded in the external receipt for PROOF-5.
  - Receipt (PROOF-4B): PR #1166 merged head `d309bb738` as `323d14997543113fb6ba2ad032803dc41224e612`; CAMP runner isolation and runtime cleanup (fail-closed context validation cross-checked against the writer's own issuedCommandIdentity derivation with exactly-one-match, `.stage-<runId>`/runRootTemplate artifact binding, fresh invocation-local runtime with all transient classes routed below it, creation-bound non-reparse cleanup, nested-owner adoption, lease-gated inline config routing with byte-equivalent non-CAMP legacy literals pinned by a config load test, repo-root-confined distDir); reviews caught and closed a P0 (a config-level module import broke every Playwright invocation — fixed with inline env consumption plus a now-mandatory browser boot-proof gate) and two mutation-surviving artifact-binding guards (killed with per-guard rows); orchestrator additionally ran a live end-to-end launcher probe with real writer-derived identities (activation, routing, exit-time cleanup all green); exact-main proof passed the fourteen-suite baseline 283 passed/6 host-skips, boot proof exit 0, strict OpenSpec 236/236, QC 19/19; pruned. CAMP catalog transience and the profile-dir consumer surface are declared 4C/4D obligations in the external receipt.
  - PROOF-4C is delivered as three declared ordered sub-PRs (cap-driven split per the 2026-08-04 pre-implementation audit, which projected the unsplit reporter seam at 862-1,178 changed lines across 10 files against the ten merged 312-475-line comparables and stopped fail-closed without code; the audit also established that the frozen CAMP-01F test id `creates a saved custom unit campaign through accepted server persistence`, the frozen CAMP-01G test id `cold reloads a saved custom unit into Mech Bay without source substitution`, and the CAMP-01F `CreateCampaignPage.submitPersistence.test.tsx` unit surface do not yet exist — they are owned by the unimplemented `persist-saved-custom-unit-campaign-creation` and `resolve-saved-custom-units-in-mech-bay` product children, so reporter seams observing them cannot truthfully precede them): **PROOF-4C1 JSON collection, PROOF-02 normalization, and writer observation binding** — opt-in built-in Playwright JSON output routed beneath the 4B invocation runtime, complete normalized observed-test inventories over the three existing proof02 anchor tests, static source-controlled known-failure mapping with writer-derived bounded fingerprints, exclusive-creation publication of the normalized report at the writer-allowlisted path during `finish(normalize)` before 4B cleanup, writer derivation of observed ids from reopened validated reports only (no caller sidecar/env ids), and byte-equivalent non-CAMP reporter configuration; **PROOF-4C2 CAMP-01F authority ingress and reporter** — run-bound structured-attachment emission from the exact frozen F journey (production PUT/accepted/readback/retry/conflict facts), normalization to the canonical `camp01-campaign-persistence-authority/v1` report, and the F rejection matrix, sequenced strictly after the `persist-saved-custom-unit-campaign-creation` child implements the frozen F journey and unit surface; **PROOF-4C3 CAMP-01G authority ingress and reporter** — run-bound attachment emission from the exact frozen G journey (resolved/unresolved cold-reload facts), normalization to `camp01-mech-bay-authority/v1`, and the G rejection matrix, sequenced strictly after the `resolve-saved-custom-units-in-mech-bay` child implements the frozen G journey. PROOF-4 section order continues 4C1 → 4D → 4E; 4C2 and 4C3 land during the product wave series, each immediately after its owning child's journey merges and strictly before that wave's receipt is produced; placeholder or non-passing journeys must never be merged to unblock a reporter seam; task 4.1's F/G reporter obligations close only against the 4C2/4C3 receipts. Fact-ingress design (binding): facts flow only through writer-declared normalized files under `CAMP01_ARTIFACT_DIR` produced by the `finish(normalize)` consumer with full writer-tuple cross-checks — never through caller environment values, caller file drops, or `runCommand().observedTestIds`.
  - Receipt (PROOF-4C1): PR #1169 merged head `a98240ff9` as `23cfbe09cf45b2a1ab8023a9c136ce1fc28d1d77`; Playwright JSON collection, PROOF-02 normalization, and writer observation binding (identity-re-deriving collection prepare with exclusive fresh raw directory, config-embedded metadata tuple cross-checks, exact source-multiset completeness, contiguous-retry resolution, design-doc-grounded static known-failure map, wx exclusive publication during `finish(normalize)`, writer observed-id derivation from reopened validated reports only with caller-id rejection); three sequential independent reviews approved — review 3's empirical mutation matrix surfaced a sourceIds-completeness suite-survivor, killed in-seam with a mutation-verified partial-run row; exact-main proof passed the fifteen-suite baseline 295 passed/6 host-skips, boot proof exit 0, strict OpenSpec 236/236, QC 19/19; pruned.
  - Receipt (PROOF-4D): PR #1170 merged head `c775241de` as `c9c59096f8ab2b3b267a7cea44a7edc312f9921b`; CAMP-01H runner identities and six normalized reports (verbatim CAMP01_H_IDENTITIES consumption with exact label/role/format checks and executionId cross-check, contract-sourced witness mapping, H_TEST_IDS inventory-exact observations with missing-fill completeness, wx publication confined to the writer-issued reports directory, ordinary-exit normalize selection, UX-manifest report closing the 4B catalog-transience obligation); reviews approved after review 3's mutation matrix caught a false-green witness corridor in the UX ok-to-passed mapping — killed in-seam with a per-position per-id observation pin, mutant-verified; forward obligation recorded: extend `H_TEST_IDS['02-command-browser-quick']` in a declared contract seam when the frozen CAMP-01F/G journeys land, else the H wave rejects fail-closed at 'observed report input drift'; exact-main proof passed the sixteen-suite baseline 306 passed/6 host-skips, boot proof exit 0, strict OpenSpec 236/236, QC 19/19; pruned.
  - Receipt (PROOF-4E, closes 4.2 and 4.3): PR #1171 merged head `aa865fdeb` as `77a9054bc4a5984b7b9d3824addbf202748c6d7c`; viewport orchestrator and package command surface (contract-final-three logical arrays with literal non-circular test pins, shared verified resolver via a read-only-additive `resolveVerifiedLogicalCommand` export delegating to the writer's own `expandLogicalCommand`, sequential shell:false spawning with first-failure propagation and typed invalid-exit rejection, chained `verify:qc:viewport-sweep` replaced by the single command, `qc:camp01-authority-receipt:controller` wiring `run-camp01-production.mjs` per the PROOF-3D3 deferral — superseding design.md D9's literal module wording — and the self-covering seventeen-suite `qc:camp01-authority-receipt:test` command, no dependency change); reviews approved after review 3 empirically proved two false-green survivors (an unconditional `--list` mutant hollowing the gate; a null/null exit returning success) — both killed in-seam and mutant-re-verified; merged on the fourth CI attempt after a declared GitHub Actions major outage (three setup-stage infra failures, never a seam failure) via the hardened bucket-asserted merge gate; exact-main proof passed the seventeen-suite family 313 passed/6 host-skips, sweep --list exit 0, boot proof exit 0, strict OpenSpec 236/236, QC 19/19; pruned.
- [ ] 4.1 Add opt-in routing for fresh empty browser profile/storage/runtime database, Next/results/HTML/snapshots/traces/videos/screenshots, the CAMP-01F persistence and CAMP-01G Mech Bay authority reporters, UX/H runners, and six normalized H reports; PNG rows seed only exact fixtures and use a guarded capture transaction with monotonic DOM/app-write counters, a capture-relevant mutation barrier, fixture-only allowlist, equal canonical pre/post state digests, and digest-only attestation bound to invocation/path/PNG, remove declared runtime only, and preserve defaults without CAMP.
- [x] 4.2 Add `run-viewport-sweep.mjs` that imports H's final three logical arrays and the shared verified resolver, expands `@node` to the verified absolute executable before sequential `shell:false` spawning, propagates the first failure, and tests both logical and expanded equality; replace the chained package script with this single command.
- [x] 4.3 Add package commands for controller, focused receipt writing, validation, and tests without adding a dependency.

## 5. Adversarial Verification — PR PROOF-5

PROOF-5 is delivered as eight declared ordered sub-PRs (cap-driven split per the 2026-08-06 pre-implementation audit, which projected the unsplit section at 3,100-3,775 changed lines against the fourteen merged 308-475-line comparables, aggregated the complete deferred-negative ledger from the external receipts of PROOF-2B through PROOF-4E into a deduplicated per-surface inventory with covered/re-deferred dispositions, and stopped fail-closed without code): **PROOF-5A hermetic Git/provenance/environment adversarial and production adapter closure** — the 5.1 injected-adapter matrix (verified executable, literal fetch URL, disabled config/credentials/proxies/rewrites, API OID equality/mismatch), remaining provenance/environment negative branches, and closure of the `run-camp01-production.mjs` public dependency-adapter argument so production cannot accept the test adapter; **PROOF-5B real repository/export/cleanup lifecycle and recovery races** — real temporary repositories/worktrees for proof/export/reopen/cleanup, dirty/reparse/ref/OID no-delete races, cap/compare-delete/export collisions, staging/crash-residue recovery, durable post-removal validation; **PROOF-5C capture transaction state machine and durable attestation reopen** — phase-drift/duplicate/tamper attestation adversarial with no-finalized-publication oracles; **PROOF-5D live capture sentinels, runner isolation, normalizer, and sweep fault adversarial** — the rendered-sentinel browser proof (visible DOM/storage/database sentinels injected before the guard and via the post-guard pre-screenshot seam, rejected by barrier/counters/post-state with no PNG/export even when raw bytes are clean), isolation/normalizer/H-confinement fault rows, the re-assigned sweep CLI/resolver jest rows in `run-viewport-sweep.test.ts`, plus the declared live-browser/live-CI tier for branches that cannot be honestly reduced to Jest (real browser capture, full production CAMP routing, live Playwright failures, OS signal/permission/ENOSPC/concurrency races) — the live tier publishes machine-readable pass/fail artifacts and is not optional manual evidence; **PROOF-5E1 dynamic repair-row validation, writer publication, and public validator** — repairs the disclosed PROOF-triage parent-run-drift blocker (owner: the PROOF-2 writer/schema surface) and admits declaration-verified repair rows through a closed row-resolution boundary without mutating `WAVE_CONTRACTS`; **PROOF-5E2 durable repair registry, reopen, cleanup, and gate equality** — repairs the disclosed dynamic-repair-row durability blocker (owner: the PROOF-1 contract surface): durable registration/discovery/cleanup binding with exact required/registered/reviewed/exact-main/cleanup set equality and chain-forgery rejection; **PROOF-5F independent approvals and PROOF pass/fail/missing triage topology** — approvals independence, cause-graph self-link/cycle/alias-chain/multi-root/nonterminal-root rejection, end-to-end durable triage oracle; **PROOF-5G H partial-observation, canonical repair, and fresh-final adversarial loop** — the six persistence-stage failure simulations with canonical repair sourcing and fresh complete finals, plus real six-report writeReceipt composition. Order is dependency-driven (5A then 5B then 5C then 5D; 5E1 then 5E2 after 5A/5B; 5F after 5A/5E; 5G last); explicitly re-deferred inventory items (diagnostic wording, macOS tmpdir, maintainability duplications, defense-in-depth-only branches) are named with justification in the audit and the receipts; each sub-PR carries its own cap audit, three sequential reviews, SHA-guarded merge, exact-main receipt, and prune; a further split must be declared here first if any sub-seam cannot fit. Tasks 5.1-5.3 close only against the sub-seam receipts (plus the live-tier artifacts) collectively covering every non-re-deferred inventory row.

- [x] 5.1 Use the test-only injected Git process/transport adapter to hermetically assert the verified executable, literal fetch URL, disabled config/credentials/proxies/rewrites, and API-returned OID equality/mismatch behavior while proving production cannot accept the adapter; use real temporary Git repositories/worktrees for exact proof/export/reopen/cleanup, dirty/reparse/ref/OID races with no deletion, caps/compare-delete/export collisions, and durable post-removal validation. Inject visibly rendered non-fixture profile/database/storage sentinels before the guard and through a test seam after the initial guard but before screenshot; prove the barrier/counters or unequal post-state reject and publish no PNG/export even when a raw byte scan would not, then reject missing/extra/reordered/substituted/tampered durable capture attestations on reopen. Mock only GitHub/API and the test transport response.
- [x] 5.2 Prove independent approvals and PROOF pass/fail/missing triage; reject cause self-links/cycles/alias chains/multiple or nonterminal roots; simulate failures before save, readiness, session, command, terminal, and post-battle persistence so each partial observation sources its canonical repair then a fresh complete final passes; reject unavailable finals, invalid failures/ranks/relationships/identity/caps/manifests, and nondurable consumers.
- [x] 5.3 Run focused Jest, Node 22 nonincremental typecheck, lint, format check, strict OpenSpec/QC validation, and applicable viewport/runner smoke checks; keep each product PR below 15 files/500 changed lines.
  - PROOF-5D sub-split declaration (cap-driven, per the 2026-08-07 pre-implementation audit which projected the declared 5D scope at a minimum honest 795-1,143 changed lines across 11-14 files against the 500-line cap and stopped fail-closed without code): PROOF-5D is delivered as six ordered sub-PRs. **PROOF-5D1 rendered-sentinel Jest kill rows** (band 170-245; `camp01-capture-transaction.test.ts` only) — the real `createBrowserCaptureInstrumentation` seedFixtures/arm/snapshot callbacks driven against rendered DOM/local-session-storage/IndexedDB state; pre-guard DOM/storage/database sentinels rejected by `validateSnapshot`/sentinel sets, post-guard pre-screenshot sentinels with clean PNG bytes rejected by mutation counters/barrier/post-state inequality, every row proving no adopted PNG and no attestation. **PROOF-5D2 runner isolation fault adversarial** (band 190-275; isolation fixture + `camp01-runner-isolation.test.ts`) — entropy, creation-failure rollback, rollback-failure typing, normalizer unavailability/ordering, cleanup-failure sibling preservation, adopted-runtime lease/routing/storage-state/lease-file drift, and non-ENOENT/canonical/reparse fault rows. **PROOF-5D3 Playwright normalizer and H confinement faults** (band 205-295; the two normalizer suites) — raw-directory/entry-kind/reporter/retry/status/identity/SHA-binding/filesystem-fault rows plus H reports-directory symlink/escape/partial-publication rows and the documented defense-in-depth disposition of the frozen `reporter.normalizedPath` lexical branch. **PROOF-5D4 sweep CLI/resolver and live-tier publication shell** (band 250-355; `run-viewport-sweep.test.ts`, new `scripts/qc/run-camp01-live-adversarial.mjs`, package.json, live CI workflow) — the re-assigned N14 sweep CLI/resolver rows (invalid-argument exit, real `--list` resolver traversal, missing-npm-CLI `required tool unavailable`) and the machine-readable live-tier core: closed result fields, passed/failed/skipped-with-reason, per-probe host gates, atomic artifact publication, summary exit code, outside the Jest umbrella. **PROOF-5D5 live real-browser and production-routing adversarial** (band 300-425) — real Chromium pre-guard and post-guard/pre-screenshot DOM/storage/database sentinel captures through the production instrumentation/transaction recording rejected/no-PNG/no-attestation, full production CAMP routing boot when repository/GitHub context exists (explicit skip otherwise), a real Playwright failure traversing launcher/normalization/cleanup/publication, and the 5B F1 disposition (the `reopenPublished` manifest-byte sub-guard is verified equivalent-mutant defense-in-depth: `readManifest` canonicalizes, `verifyArtifactBytes` binds every artifact, and the public durable revalidation uniquely determines the canonical manifest — downgraded, no tautological row added). **PROOF-5D6 live OS fault and race adversarial** (band 285-405) — real permission-denial, ENOSPC (`/dev/full` or explicit quota root, never filling the workspace disk), signal termination with no-normalizer-publication, same-ownership two-process races with exactly one authoritative outcome, and the carried Windows `rmSync` retry-timing obligation; every host branch publishes pass/fail or skipped-with-reason, never silently absent. Order is 5D1→5D2→5D3→5D4→5D5→5D6 (the live shell precedes the live probes that publish through it); each sub-PR carries its own cap audit, review, SHA-guarded merge, exact-main receipt, and prune; a further split must be declared here first if any sub-seam cannot fit.
  - Receipt (PROOF-5A): PR #1173 merged head `069067ad3` as `368ee1b46507ba68057f04bb70d56f088b4bd98f`; hermetic Git/provenance/environment adversarial (43 kill rows) plus closure of the `run-camp01-production.mjs` public dependency-adapter argument (arity guard, dependencies parameter removed); three sequential reviews approved (closure traced complete; audit inventory E01-E07 gap list empty; 10/10 single-guard mutants killed empirically); band 417/320-420; N14's sweep CLI rows re-assigned to PROOF-5D by ledger amendment; exact-main proof 17-suite umbrella 356 passed/6 host-skips, boot proof exit 0; pruned. Deferred/residual items recorded in the external receipt.
  - Receipt (PROOF-5B): PR #1174 merged head `60a4aed34` as `72038995f01542cb6daa497ae28f6801a09d6b02`; real repository/export/cleanup lifecycle oracle (new cross-module adversarial suite) with 24 bounded production recovery lines (created-directory tracking, byte-identical destination reopen, runId-anchored staging-residue recovery, receipt-before-git removal ordering with post-removal absence check, hard-link publication rollback); review 1 established the Windows MAX_PATH root cause; review 3 ran 11 mutants with the F2 foreign-residue destruction-safety fold confirmed closed; band 423/500; exact-main proof 18-suite umbrella 391 passed/7 host-skips, boot proof exit 0; pruned. F1/F3/F4/F5 dispositions recorded in the external receipt (F1 subsequently verified equivalent-mutant in the PROOF-5D audit).
  - Receipt (PROOF-5C): PR #1176 merged head `6969411a0` as `a7d643ca7526c453f4d8a636a7b3dd74aa8233cd`; capture transaction state machine and durable attestation reopen adversarial with ZERO production changes (the frozen guards rejected every row); C01/C02/C03 inventory covered including four byte-canonical structural attestation tampers, a control-rowed 10-mutation durable reopen oracle with exact-message pins and the production public-validator reopen refusing each mutated receipt, four field-level digest-tamper rows killing the previously unpinned `validateCaptures` state/counter/allowlist/barrier-policy clauses, and the C05 flat-artifact invariant across both frozen waves; two sequential reviews (test-only tier) both approved after mutation-verified remediation; band 299 (below the 380-470 projection — the guards held); exact-main proof 19-suite umbrella 419 passed/7 host-skips, boot proof exit 0, OpenSpec strict 237/237, QC 20/20; pruned.
  - Receipt (PROOF-5D1): PR #1178 merged as `5a8935069`; rendered-sentinel Jest kill rows C04-J0..J7 through the real `createBrowserCaptureInstrumentation` against simulated rendered DOM/storage/IndexedDB state — pre-guard sentinels rejected by snapshot collection, post-guard clean-PNG sentinels caught only by the production instrumentation hooks with publication itself as the failure oracle; 176 lines/1 file, zero production changes; orchestrator-personal review with a four-mutant empirical matrix (all 1:1); exact-main proof umbrella 427/7, boot 0; pruned.
  - Receipt (PROOF-5D2): PR #1180 merged as `9387af723`; runner-isolation fault adversarial C06-J1..J7 (15 rows over a deterministic injectable-fault fixture: entropy admission, creation-failure rollback with sibling preservation, rollback/cleanup failure typing with retryable ownership, four adopted-runtime drift rejections, non-ENOENT/canonical/reparse fail-closed); 243 lines/2 files, zero production changes; five personal mutants killed 1:1 against production; one branch-protection rebase; exact-main proof 442/7; pruned.
  - Receipt (PROOF-5D3): PR #1181 merged as `1fbba243e`; Playwright-normalizer and H-confinement fault rows C07-J1..J3 + C08-J1..J3 with C08-D1 documented defense-in-depth; FOUND AND REPAIRED a real production defect — the H normalizer leaked raw Errors on mkdir/realpath faults; 31-line typed repair (shared resolver + creation wrap, causes preserved, no guard logic altered) proven by revert-red/restore-green; 212 lines/3 files; personal mutation matrix incl. the Windows entry-kind shared-closure disposition (symlink half pinned by the POSIX-gated rows on ubuntu CI); exact-main proof 466/9; pruned.
  - Receipt (PROOF-5D4): PR #1182 merged as `1a8c163c6`; re-assigned N14 sweep CLI/resolver rows (real-CLI subprocess surface, invalid-argument closure, real --list resolver traversal, copied-Node @npm required-tool kill) plus the C09-L0 machine-readable live-tier shell (`qc:camp01-live-adversarial`, closed probe records, atomic artifact publication, failed-probe-still-publishes semantics, explicit skipped-with-reason registration points) and the C09-CI evidence workflow (push-to-main + dispatch, never a merge gate); 364 changed lines/4 files (measured `git diff --numstat 1a8c163c6^1 1a8c163c6`; the earlier 355+12 figure double-counted the reviewer fold); one Linux CI red (Windows npm-layout assumption in the row's non-interference stat) reviewer-folded to a host gate; ledger: `resolveVerifiedLogicalCommand` '@npm' fails closed on POSIX toolchains; exact-main proof 470/9 + live artifact exit 0; pruned.
  - Receipt (PROOF-5D5): PR #1183 merged as `406f7eb0a`; live real-browser adversarial C04-L1..L6 — headless Chromium driving the PRODUCTION instrumentation/transaction on a real rendered page, pre-guard and post-arm clean-PNG sentinels rejected with no publication, probes proven guard-death oracles (neutering the production non-fixture rejection failed the live tier on exactly the C04-L1 oracle); C09-L1 production routing behind an honest machine-readable context skip; C09-L2 real Playwright failure traversing launcher/normalizer/writer with no partial publication; F1 cited as dispositioned; 379 lines/3 files (+ubuntu Chromium install); exact-main proof 470/9; pruned. LIVE-TIER CORRECTION (recorded 2026-08-07 after a council audit): the originally-recorded "live artifact 4 passed/2 skips" was the AUTHOR'S LOCAL Windows run, not CI. The actual CI evidence for this merge (run 31170491634) is Linux `{passed:4, failed:0, skippedWithReason:2, exitCode:0}` and Windows `{passed:3, failed:0, skippedWithReason:3, exitCode:0}` — the Windows leg skips the flagship rendered-sentinel probe with BROWSER_UNAVAILABLE because this seam installs Chromium on the Linux leg only. The C04-L1..L6 guard-death evidence stands (it was reproduced on the Linux leg and locally); what was overstated is the breadth of hosts on which it ran.
  - Receipt (PROOF-5D6): PR #1184 merged as `9d6744e39`; live OS fault and race adversarial C09-L3..L7 — real icacls/chmod permission denial typed closed with ACL-restoration ladder, honest ENOSPC mechanism gating (/dev/full on the ubuntu leg), SIGKILL'd child with normalizer non-selection and no report, two-process isolation race with exactly one owner and zero loser residue, and the carried 5B Windows locked-handle obligation CLOSED (rmSync EBUSY in 0.34ms at maxRetries:0, ownership retryable, full cleanup after release); 317 lines/2 files, no production repair; personal ACL-safety audit + live guard-death on the locked-handle oracle; worker-process reset incident recovered via reflog (standing rule: kill the worker at REPORT-DONE); exact-main proof 470/9, strict 238/238, QC 21/21; pruned. LIVE-TIER CORRECTION (recorded 2026-08-07 after a council audit): the originally-recorded "live artifact 8 passed/0 failed/2 skips" was the AUTHOR'S LOCAL Windows run and is CONTRADICTED BY CI. The actual CI evidence for this merge (run 31173800715) is Linux `{passed:8, failed:0, skippedWithReason:2, exitCode:0}` but Windows `{passed:6, failed:1, skippedWithReason:3, exitCode:1}` — `proof5d6-two-process-isolation-race` FAILED with `PROOF5D6_RACE_ORACLE_FAILED`. That failure went unread for six consecutive pushes because the live tier is a non-gating evidence publisher and nothing was checking it. ROOT CAUSE (diagnosed and repaired in PR #1190, merge `2ff3b0e83`): a PROBE defect, not a production defect — the probe fixture built its artifactDirectory with `path.join` and used it raw while production returns `realpath`-canonicalized paths, so on a host whose temp dir carries an 8.3 short-name alias (Windows CI) `path.relative` walked out of the tree and fabricated exactly 11 phantom residue entries. Production invariants held in every failing run (typed loser, empty artifact tree after cleanup, both workers exit 0), independently corroborated by the sibling locked-handle probe passing on the same host. Reproduced locally under a forced 8.3 temp dir: pre-fix `{passed:7, failed:1}`, post-fix `{passed:8, failed:0}`. The C09-L7 rmSync obligation closure is unaffected and stands. The PROOF-5D sub-split is complete; C09-L1's provisioned production-context execution remains the one open live-evidence item.
  - Receipt (PROOF-5E1): PR #1186 merged as `8180672cc`; REPAIRS the first section-3 closure blocker (owner: the PROOF-2 writer/schema surface) — the writer built the proof-02-triage artifact without its run binding while artifact validation required it, so the public validator rejected every triage receipt with `parent run drift` (reproduced empirically pre-repair by both the worker and the reviewer). Triage schema gains `runId`/`parentRunId`; the writer publishes `runId` from writer entropy and `parentRunId` from the validated reproduction; triage-parent/reproduction equality is required. Adds the closed `resolveReceiptRow(wave, declaration, source)` boundary — frozen rows admitted only by their exact `WAVE_CONTRACTS` object with repair metadata rejected as `fixed wave repair collision`, dynamic rows admitted only through the frozen contract's own `assertRepairDeclaration` and returned recursively frozen; `WAVE_CONTRACTS` byte-identical. 77 changed lines/4 files (33 production, 44 proving rows); K01-K11 kill rows; three sequential reviews with a 7-mutant matrix plus three independent reviewer mutants (fixed-collision, unfrozen-return, parentRunId-equality) each killing exactly its row; exact-main proof umbrella 478 passed/9 skipped, boot 0, strict 238/238, QC 21/21; pruned.
  - Receipt (PROOF-5E2): PR #1187 merged as `2987aa0e8`; REPAIRS the second section-3 closure blocker (owner: the PROOF-1 contract surface) — a dynamic repair-row receipt validated while its declaration was in memory but failed `unknown receipt wave` in a fresh process, so durable receipts of dynamic rows could not be discovered, reopened, or cleaned up after context loss (reproduced empirically pre-repair). Adds `scripts/qc/camp01-repair-registry.mjs`: immutable ordinal event directories `<8-digit ordinal>-<sha256(event.json)>`, canonical declaration/source payloads, `previousDigest` chaining, replay validation of ordinal/name-digest/prior-link/payload-digests/unique-wave/exact-field-set/canonical-bytes, registration validated through `assertRepairDeclaration` BEFORE publication, discovery resolving through 5E1's boundary, and a `--repair-registration=sha256:...` public-CLI path; atomicity and cleanup reuse the durable-export patterns. 358 changed lines/9 files, umbrella 20 suites. Review ran a five-mutant matrix (chain-link, payload-digest, order each 1:1); TWO survivors were adjudicated empirically rather than assumed equivalent and BOTH proved GENUINE gaps, now pinned: `K18 stale-name` (the LAST event has no successor to back-link it, so its directory-name digest is its only integrity commitment — an in-place tamper with a stale name was accepted, escalating a registered repair's commandSequence and caps) and `K17 duplicate-live` (the mutant rejects with the IDENTICAL message but publishes a poison event first, permanently bricking the registry; the load-bearing assertion is `events: 1`). Exact-main proof umbrella 497 passed/9 skipped, boot 0, strict 238/238, QC 21/21; pruned.
  - Receipt (PROOF-5F): PR #1188 merged as `ddb841696`; approvals independence, PROOF pass/fail/missing triage topology, cause-graph rows, and the end-to-end durable triage oracle. FOUND AND REPAIRED a real production gap (10 changed lines): nothing bound a non-reporter artifact's PATH to its schema — `validateArtifact` dispatched purely on the artifact's self-declared `schema` field, so a GENUINE, fully-valid artifact republished at a different allowlisted path validated, which would have hollowed out this seam's own durable-triage oracle; repaired with a frozen `ARTIFACT_PATH_SCHEMAS` map plus `assertArtifactPathSchema` consulted during receipt validation, pinned by a kill row. A 5-reader survey plus two adversarial critiques established that much of the declared 5F surface is ALREADY pinned by merged seams (approvals by PROOF-3C2/5A, cause-graph self-link/alias-chain/multi-root/nonterminal by PROOF-1's task-1.2 contract tests, the triage round-trip by 5E1) and killed two proposed rows as DEAD before any code, so the honest seam is 137 lines/5 files rather than a re-derivation of existing coverage. FIRST seam implemented without a Sol worker (quota exhausted): orchestrator-authored, and verified SERIALLY after a first parallel mutation pass was DISCARDED WHOLESALE as untrustworthy (two batches sharing one worktree contaminated each other and a peer session; rules adopted — mutation testing requires an exclusively-owned worktree and a committed restore point, and the production file must be hashed immediately before AND after every mutation). Serial re-run: M-F1a, M-F1b, M-F8e and M-SCHEMA-BIND each reddened exactly their own row, 4/4, zero collateral, production restored byte-identical each time. Frozen contract byte-identical; no umbrella or sweep-pin edit needed. Exact-main proof umbrella 537 passed/9 skipped, boot 0, strict 238/238, QC 21/21; pruned.
  - PROOF-5G sub-split declaration (cap-driven, per the 2026-08-07 pre-implementation audit which re-measured the declared 5G scope against every merged PROOF-5 slice by `--numstat` this session and stopped fail-closed without code): twelve PROOF-5 slices are merged at `ddb841696`, of which the eleven test-tier slices (5A 417, 5B 423, 5C 299, 5D1 176, 5D2 243, 5D3 212, 5D4 364, 5D5 379, 5D6 317, 5E2 358, 5F 137 changed lines) set the aggregate packing rate of 80 characters per changed line, while PROOF-5E1 (#1186 `8180672cc`, 77 changed lines across 4 files — 33 production, 44 proving rows) is excluded from the rate as a production-repair slice and is instead the pricing comparable for this seam's disclosed-blocker repair below; every projection here converts content at that measured rate and is apples-to-apples only if 5G is written at that density. The declared 5G scope projects at 1,053-1,367 changed lines (central ~1,211) across 65 composition kill rows plus 30 tabular schema rows plus a ~115-line shared fixture, so PROOF-5G is delivered as three ordered sub-PRs (FOUR after the PROOF-5G0 repair seam declared in the blocker-II amendment below, which lands strictly before 5G1 and does not alter the three bands or file plans stated here); the whole-seam figure is the sum of the three sub-bands, so summing them back is an internal consistency check and not independent corroboration. **PROOF-5G1 H composition fixture, composed-receipt identity binding, and the declared contract repair** (band 390-488, central ~451; 7 files against the camp-proof 15-file row: new `scripts/__tests__/support/camp01-h-composition.fixture.ts` ~115 lines, new `scripts/__tests__/camp01-h-composition.test.ts`, `package.json`, `scripts/__tests__/run-viewport-sweep.test.ts`, `scripts/qc/camp01-authority-receipt.contract.mjs`, `openspec/changes/add-camp01-authority-receipts/design.md`, `scripts/__tests__/camp01-authority-receipt-qc.test.ts` — this is 5G1's whole file plan, repair included, and no other count of it is authoritative) — the shared fixture driving the real `writeReceipt` at wave `camp-01h` (6 logical commands, 21 frozen artifacts after the repair below, 6 normalized reports, 3 witness authority/experience pairs, 2 real PNG capture transactions, re-deriving the writer-private `deriveEvidenceId` at `scripts/qc/camp01-authority-receipt.mjs:67` because only `issueHIdentities` at `:56` is exported), plus 23 rows: composition observation/final/reopen/public-validator happy paths with `H phase drift` x3, per-report witness binding (`H report witness drift` x4, `H issued identity drift` x3, `session identity reuse`, `H report digest set drift`, `unknown H reporter`), and inventory/registry binding (`H observation inventory drift` x2, `H source set drift` x2, `H observed identity registry drift` x2). The entire camp-01h branch of `validateCrossArtifacts` — `camp01-authority-receipt.mjs:91-104`, fourteen distinct failure messages across the branch body and its three helpers `validateHBindings`/`validateHIdentityRegistry`/`reporterPath` — is unexercised end to end today: every one of those fourteen messages returns zero hits under `scripts/__tests__/`, `run-camp01-authority-receipt.test.ts:42` stubs `executeReceipt` in its dependencies object (`:55` is the `observation` action that consumes it), and no test anywhere calls `writeReceipt` with wave `camp-01h`. 5G1 alone pays both coherence costs of a new jest suite — the `qc:camp01-authority-receipt:test` umbrella in `package.json` (20 to 21 suites, counted this session) and the `camp01TestFiles` pin at `run-viewport-sweep.test.ts:155-176` asserted at `:382` — a measured 3 changed lines off the 5E2 diffstat (`package.json` 1+1, `run-viewport-sweep.test.ts` 1+0); a `support/*.fixture.ts` is not collected under `jest.config.js` `testMatch` and costs zero umbrella churn, precedent `scripts/__tests__/support/camp01-runner-isolation.fixture.ts`. **PROOF-5G2 H authority/experience binding, six-stage partial-observation repair loop, fresh finals, and finding identity** (band 385-490, central ~437; `camp01-h-composition.test.ts` plus a stage-mutation hook in the fixture if one is needed — 1-2 files, no umbrella churn) — 35 rows: authority/experience artifact binding (`H authority drift` x4, `H experience witness drift` x3), the six persistence-stage failure simulations (before save, readiness, session, command, terminal, post-battle) each sourcing its canonical repair through `registryContext.repairSources` and then passing a fresh complete final, unavailable-final rejection, ranks/relationships (`H repair source drift` x4, `H reconciliation drift` x3 over `backlogRank`/`rankedFindingIds`/critical-major dispositions), and the 5 re-assigned `finding identity drift` rows named in the tail ledger below; `witnesses/*/experience.json` is where findings, dispositions and `repairRowId` live and `combat-authority.json` mirrors the post-battle witness, so the authority binding is the repair loop's data plane and is not separable from it. **PROOF-5G3 durable H receipt envelope, H durable consumers, and the re-assigned cap/manifest/observation schema matrices** (band 278-389, central ~323; `camp01-h-composition.test.ts`, `camp01-durable-facts.test.ts`, `camp01-repair-registry.test.ts` — 3 files, no umbrella churn) — 12 composition rows (nested `reports/` and `witnesses/<label>/` manifest drift, artifact-set drift, camp-01h cap provenance, and the camp-01h branch of the durable consumers: `camp01-durable-facts.mjs:236` `repairSources` reading `witnesses/*/experience.json` into `findings[].disposition.repairRowId`, the `camp-01h-required-repairs` `repairGate` at `:238`, and the non-proof arm of `resolveRepairSource` at `:70` — every hit in the durable/repair suites today is `proof-02-*` and the H arm has zero coverage) plus the ~25 tabular cap-clause, manifest-shape and observation-shape rows assigned in the tail ledger below. Row counts are 23 + 35 + 37 = 95, i.e. 65 composition kill rows (23+30+12) plus 30 tabular schema rows (5+25). **Re-deferred inventory (named owners with justification, per the 5B F1 and 5D3 C08-D1 precedent):** *5G-D1 durable-validator identity sub-guards* — `camp01-durable-facts.mjs:147` (`proof target validator missing` / `durable validator identity missing`) and the `typeof context.invokePublicValidator!=='function'` arm of `camp01-cleanup-authority.mjs:77` (`durable validator unavailable`) are message-unpinned but reachable only when a caller passes a structurally wrong validator handle, a state the `lifecycle` row already exercises positively with real production dependencies; recorded as a documented defense-in-depth disposition in the PROOF-5G3 receipt rather than a tautological row, owner PROOF-5G3. Stated plainly and unlike the 5B F1 downgrade at :59: **no equivalent-mutant check was run** — this disposition rests on structural reachability alone, and if 5G3's implementer wants it downgraded on the 5B F1 standard the mutant must be run and recorded first. Carried but explicitly NOT deferred: whether production producers exist for the remaining H artifacts was not audited — no producer exists for `audit-reconciliation.json` anywhere in `scripts/`, `e2e/` or `src/`, the H artifact production layer appears unbuilt generally, 5G1's fixture is the declared test-side producer for all 17 remaining JSON artifacts and both PNG capture transactions, and if a production producer is required instead that is a new declared seam here, never a silent fold. **Task 5.2 tail obligations (assignment declared here; 5.2 closes only against sub-seam receipts, so every tail item is dispositioned by name):** *unavailable finals* → PROOF-5G2, NEEDS-ROW ~10-18 lines — `validateAuthority`'s final branch (`camp01-authority-receipt.schemas.mjs:147`) requires non-empty evidence lists (`final authority evidence missing`) and routes every fact through `validateFactValue` (`:167`), while the identical `IUnavailableFactV1` wrapper is ACCEPTED at `status:'observation'` through `validateFactWrappers` (`:149`); coverage today is ZERO — `final authority evidence missing`, `authority identity drift`, `authority label drift`, `unavailable fact drift`, `observed fact source drift` and `fact source drift` each return zero hits under `scripts/__tests__/`, and the nearest neighbour `camp01-authority-receipt-writer.test.ts:58` (`h-bindings`) builds its witnesses with `facts:{}` and would stay green if the whole final branch were deleted; the observation/final asymmetry is `openspec/changes/add-camp01-authority-receipts/specs/journey-qc/spec.md:184`. *invalid failures* → split by guard, both NEEDS-ROW, both at ZERO coverage today (verified by grep over `scripts/__tests__/`): `finding identity drift` (`schemas.mjs:151` — category, severity, `backlogRank<1`, dimension values, `causeFingerprint` digest) → **PROOF-5G2**, 5 rows ~14-20 lines, cohesive with the findings/dispositions group its stage simulations construct; `observation fingerprint drift` (`schemas.mjs:181` — unknown status, passed-with-fingerprint / failed-with-null, non-digest fingerprint) → **PROOF-5G3**, 3 rows ~8-12 lines, a pure tabular observation-shape matrix cohesive with the schema-matrix group. Both guards are silently removable today, which is precisely the false-green class this ledger exists to kill. *invalid ranks* → PROOF-5G2, ALREADY-IN-SCOPE — `H reconciliation drift` (`camp01-authority-receipt.mjs:96`: `findings.some((entry,index)=>entry.backlogRank!==index+1)` and the `rankedFindingIds` equality) plus the `backlogRank<1` clause of the `finding identity drift` rows above. *invalid relationships* → PROOF-5G2, ALREADY-IN-SCOPE — `H repair source drift` x4 and `H reconciliation drift` x3 (mapped-failure set equality, repair-row set equality, critical-major disposition equality), all `camp01-authority-receipt.mjs:96`; the cause-graph topology half of 5.2 (self-links, cycles, alias chains, multiple or nonterminal roots) is already discharged by PROOF-5F (#1188 `ddb841696`) and is cited here, never duplicated. *invalid identity* → PROOF-5G1, ALREADY-IN-SCOPE — discharged by `H issued identity drift` x3 (`:94`), `session identity reuse` (`schemas.mjs:157`), `H observed identity registry drift` x2 (`:102`), `unknown H reporter` (`:104`) and `H report witness drift` x4 (`:93`), all at zero coverage today. *invalid caps* → PROOF-5G3, NEEDS-ROW ~25-35 lines — `validateCap` (`schemas.mjs:122`) carries the eleven OR'd clauses of `cap provenance drift` plus the entire exact-main linkage block, and the only cap mutations that exist today are the `changedLineCount = 501` and `mode = 'exact-main'` entries of `camp01-authority-receipt-writer.test.ts:248`, which assert only `{ok:false,commandCount:0,runRootExists:false}` and pin no message at all; `camp01-target-authority.test.ts:314` is the sole message-pinned cap row (`reviewed cap linkage drift`), `cap linkage drift` and `reviewed head cap drift` return zero hits under `scripts/__tests__/`, and `camp01-authority-receipt-writer.test.ts:257` exercises the exact-main linkage round-trip positively only. *invalid manifests* → PROOF-5G3, NEEDS-ROW ~15-22 lines — `validateManifest` (`schemas.mjs:179`) has zero coverage: `invalid manifest entry` and `manifest paths` both return zero hits under `scripts/__tests__/`, and the adjacent rows are all different guards (`camp01-cleanup-authority.test.ts:161` is the durable byte guard `manifest digest drift` at `camp01-authority-receipt.mjs:52`; the `camp01-capture-reopen-adversarial.test.ts:98` matrix is capture attestations; the `camp01-durable-export.test.ts:204` matrix is export-layer), so the `entry.type !== 'file'` and path-uniqueness clauses are masked by no sibling guard and are silently removable today. *nondurable consumers* → ALREADY-PINNED, no new row — `camp01-durable-facts.mjs:242` is killed by `camp01-durable-facts.test.ts:111` (`it.each(['tamper','unvalidated'])` producing `CAMP01_FACTS_INVALID: public validator rejected durable receipt`, the `unvalidated` variant recomputing the manifest entry's size and digest so only re-running the real public validator over the reopened durable bytes rejects it); `camp01-cleanup-authority.mjs:77` is killed by `camp01-cleanup-authority.test.ts:265`/`:267` and the four-shape result matrix at `:292` (`durable receipt revalidation failed` with `calls === ['validate:durable']` and nothing published), `:166` (`durable receipt digest drift` from a tampered durable `receipt-manifest.json`) and `:92`/`:97` (`validate:durable` ordering and exact validator inputs); `camp01-durable-facts.mjs:70` is killed by `camp01-durable-facts.test.ts:165` (`declared repair source absent from durable receipts`) and `camp01-repair-registry.test.ts:71` (K01 public durable round trip). The 5G receipt cites these rather than duplicating them; PROOF-5G3's H-branch consumer rows are a distinct uncovered surface required by this declaration's canonical-repair-sourcing scope, not a restatement of this obligation. Order is dependency-driven 5G1 then 5G2 then 5G3: 5G1 lands the shared fixture nothing else can compose without and executes the declared contract repair every later row is downstream of; 5G2 depends on 5G1's fixture, phase parameterization, and pinned witness identity so an authority failure is never ambiguous with an identity failure; 5G3 depends on 5G2 because `camp01-durable-facts.mjs:236` reads exactly the finding-carrying `repairRowId` artifact shape 5G2's stage simulations produce, and landing it earlier would force it to hand-forge the artifact 5G2 exists to prove. Each sub-PR carries its own pre-implementation cap audit, three sequential reviews, SHA-guarded merge, exact-main receipt, and prune, exactly as declared for PROOF-5 above — recorded as fact and not as a relaxation: the Sol worker quota is exhausted, so all three reviews on each sub-PR are orchestrator-run sequential independent passes rather than dispatched worker reviews, and the count is not reduced for any sub-seam including the test-only ones. Task 5.2 closes only when all three sub-seams have exact-main receipts collectively covering every obligation named above; a further split must be declared here first if any sub-seam cannot fit.
  - PROOF-5G disclosed blocker and declared repair (fail-closed, named owner; PROVEN empirically on `ddb841696` with the real `writeReceipt` and the real schemas, mocking only `randomBytes` and `runCommand` per `scripts/__tests__/camp01-authority-receipt-writer.test.ts:35-52`, every probe written outside the worktree with `git status --porcelain` empty before and after): a `camp-01h` receipt cannot be published today in either controller phase. camp-01h's frozen artifact set (`camp01-authority-receipt.contract.mjs:21` `hArtifacts`, bound to the row at `:42-46`) contains `proof02-triage.json`, so `writeReceipt`'s retained-artifact loop (`camp01-authority-receipt.mjs:42`, set by `retainedArtifacts` at `:77`) validates it through `validateTriageArtifact` (`camp01-authority-receipt.schemas.mjs:165`), whose first guard `if (!provenance||!reproduction) fail('verified reproduction context missing')` rejects. Evidence: with all 20 retained camp-01h artifacts present on disk the real writer throws `CAMP01_RECEIPT_INVALID: verified reproduction context missing`, while the identical harness publishes valid `proof-02-reproduction` and `proof-02-triage` receipts as controls, so the failure is camp-01h-specific and not harness error; a no-op camp-01h run fails earlier at `CAMP01_WRITER_INVALID: missing artifact audit-reconciliation.json`, and no producer for `proof02-triage.json` or `audit-reconciliation.json` exists anywhere in `scripts/`, `e2e/` or `src/` (the sole triage writer, `camp01-authority-receipt.mjs:41`, is gated on `row.wave==='proof-02-triage'`). The blocker is phase-independent — it lives in the retained-artifact loop and the triage guard, and `mode` appears zero times on `camp01-authority-receipt.mjs:42`, `:77` and `schemas.mjs:165` — so neither reviewed-head observation nor exact-main final can produce a receipt; stated fail-closed, the exact-main path was NOT exercised end to end and that confirmation is 5G1's first act. **TWO independent structural impossibilities are proven, not three:** (i) `reproduction` cannot enter the camp-01h write request at all, because `exactKeys` at `camp01-authority-receipt.mjs:36` admits `reproduction`/`triage` for exactly one wave, yielding `CAMP01_WRITER_INVALID: write request fields drift` against a baseline that reaches `run root mismatch`; and (ii) camp-01h may not legally carry the `proof-02-reproduction` execution evidence that `validateReproduction` (`schemas.mjs:133`) demands, because that wave is not among camp-01h's predecessors (`contract.mjs:35` gives `["camp-01g","proof-02-triage","proof-02-required-repairs","camp-01h-required-repairs"]`), so `prepareValidationContext` rejects at `camp01-authority-receipt.mjs:59` with `CAMP01_WRITER_INVALID: unissued external evidence`, mirrored by the `[row.wave,...row.predecessors]` evidence gate at `schemas.mjs:161`. The provenance vise is a third *clause*, not a third impossibility, and the earlier claim that it is closed against all context data is WITHDRAWN: `validateRegistryContext` (`schemas.mjs:161`) constrains `entry.wave` to `[row.wave,...row.predecessors]` for the EVIDENCE registry only — the provenance-member gate applies only `PROVENANCE_ID.test`, `PROVENANCE_KINDS.includes`, subject membership and `bounded(entry.wave,...)` — so a camp-01h provenance registry may legally carry extra `proof-02-reproduction` and `proof-02-triage`/`audit-pr` members, and the vise exists solely because of the equality `value.reproductionReceiptId!==provenance.predecessorReceiptIds[0]||value.auditTupleId!==provenance.ownedPrTupleId` at `schemas.mjs:165` colliding with `validateProvenance`'s positional wave binding at `:163`; that clause is closed by the same wave-aware change as the identity source and is not independent of it. **Declared repair (decided here, not a fork): drop `proof02-triage.json` from `hArtifacts`** — `contract.mjs:21` (1 source line), the `H_ARTIFACTS` mirror at `openspec/changes/add-camp01-authority-receipts/design.md:81`, and the source-pinning artifact-set row that does not exist today (the QC freeze row at `scripts/__tests__/camp01-authority-receipt-qc.test.ts:124` pops the last element of a runtime snapshot and `assertFixedContract` at `contract.mjs:83` compares that snapshot to `contractSnapshot()`, i.e. to the live source, so the artifact set is currently mutable without any test going red — that gap is closed in the same PR with a literal 21-entry pin); honest cost 10-18 changed lines across 3 files, carried inside PROOF-5G1's declared band and file plan, owner: the PROOF-1 contract surface, declared here before it is taken. Justification, not convenience: camp-01h's frozen set names an artifact with no producer and a validator that structurally cannot accept it in camp-01h's write context, while the triage facts camp-01h actually consumes reach it through its declared `proof-02-triage` predecessor (`validateProvenance`, `schemas.mjs:163`), through `validateHBindings`' `sourceObservationReceiptIds` equality (`camp01-authority-receipt.mjs:100`) and through the durable repair-source resolver (`camp01-durable-facts.mjs:236`, which reads `record.wave==='proof-02-triage'` into `record.artifacts['proof02-triage.json'].dispositions`) — never through a copy in camp-01h's own set, which `validateCrossArtifacts`' camp-01h branch (`:91-104`) never reads. No spec requirement in `specs/journey-qc/spec.md` places the triage artifact in H's set. Removing it therefore restores the contract rather than weakening it, and it changes no validator, no key set and no predecessor list. **STRUCK alternative, recorded so the strike is auditable:** the composite wave-aware route — make `validateTriageArtifact` source camp-01h's expected `{reproductionReceiptId, auditTupleId}` from the `proof-02-triage` predecessor's own receipt (`schemas.mjs:165`), admit `reproduction` on the camp-01h write request (`camp01-authority-receipt.mjs:36`), extend the already-existing receipt-derived read in `camp01-durable-facts.mjs` `contextFor` (`:224`) and `writerContext` off its `row.wave==='proof-02-triage'` gate, close the evidence-wave gate at `camp01-authority-receipt.mjs:59` (either by lengthening camp-01h's `predecessors` 4 to 5 at `contract.mjs:35`, which shifts `validateProvenance`'s positional `predecessorReceiptIds[index]` binding and every camp-01h provenance construction, or by relaxing `validateReproduction`), and follow the 5E1 precedent into `scripts/qc/validate-camp01-authority-receipt.mjs` for the reopen/public-validator leg. The two candidate routes the task named are not alternatives to each other: the receipt-derived resolver route already exists at `camp01-durable-facts.mjs:224` and its output must still reach the validator via `request.reproduction` into `externalContext` (`camp01-authority-receipt.mjs:37`), so it DEPENDS on opening the request key set rather than avoiding it. The composite is struck on three grounds: it opens `reproduction` on camp-01h's write request, contradicting the deliberately closed key set (`REQUEST_KEYS` at `camp01-authority-receipt.mjs:25` plus the single-wave ternary at `:36`); it additionally requires either a predecessor-length contract change or a validator relaxation, both strictly larger weakenings than removing one redundant name; and priced honestly against the 5E1 comparable (#1186, 77 changed lines / 4 files for a strictly simpler one-clause repair) this four-clause repair across five files lands at 110-160 changed lines, which on top of 5G1's ~451 central test content breaches both the sub-seam band and task 5.3's 15-file/500-line row and would force a fourth sub-seam for work the decided route makes unnecessary. **Prior-blocker reconciliation:** the blocker declared here is INDEPENDENT of both section-3 closure blockers, which are now closed — blocker (1), the `proof-02-triage` parent-run-drift blocker owned by the PROOF-2 writer/schema surface, was CLOSED by PROOF-5E1 (#1186 `8180672cc`), which added `runId`/`parentRunId` to `SCHEMA_KEYS['camp01-proof02-triage/v1']` (`schemas.mjs:40`) and the `triage parent run drift` guard (`schemas.mjs:165`), and blocker (2), dynamically named repair rows excluded from durable validation and owned by the PROOF-1 contract surface, was closed by the same PR's `resolveReceiptRow` boundary (`schemas.mjs:71`) and completed by PROOF-5E2 (#1187 `2987aa0e8`); this blocker lives in a different guard of the same function — the `!provenance||!reproduction` clause, not the run binding. tasks.md carried no PROOF-5E1, PROOF-5E2 or PROOF-5F receipt line at `ddb841696`, so a reader saw three open blockers on one surface with no way to tell two were closed; that gap is CLOSED in this same branch — the three receipts are recorded under task 5.3 above and the section-3 blockers bullet in section 3 now marks both closed with its historical text preserved verbatim for provenance — and that bookkeeping is not counted against any sub-seam band. Neither this blocker's repair nor the struck alternative is waived by this declaration; the decided route rejects rather than weakens.
  - PROOF-5G acceptance decoupling declaration (binding, fail-closed; scope, evidence, ownership and ordering stated here so that no later reader can mistake a closed task 5.2 for an obtained acceptance witness, per the 2026-08-07 scoping workflow and its adversarial pass, with every figure below independently re-measured against `382641ee9` in this session and the scoping pass corrected by name where it was wrong): **PROOF-5G1/5G2/5G3 proceed EXACTLY AS DECLARED above and are worth their cost as adversarial coverage** — measured this session, the camp-01h branch body of `validateCrossArtifacts` (`scripts/qc/camp01-authority-receipt.mjs:92-96`) carries exactly NINE distinct failure messages (`H phase drift`, `H report witness drift`, `H issued identity drift`, `H authority drift`, `H experience witness drift`, `H reconciliation drift`, `H repair source drift`, `empty H observation`, `H disposition reconciliation drift`) and its three helpers carry five more (`H observation inventory drift`, `H source set drift`, `H report digest set drift`, `H observed identity registry drift`, `unknown H reporter`), which is the fourteen-message figure declared above split 9 + 5 and re-confirmed at zero hits under `scripts/__tests__/` for all fourteen; the nine are the stronger case because `validateCrossArtifacts` is unexported with exactly one call site (`:52`, inside `validateReceiptDirectory`) and no test drives `writeReceipt` or `validateReceiptDirectory` at wave `camp-01h`, so they are UNREACHABLE in the suite rather than merely message-unpinned and are silently removable today, whereas the five helper messages are reached behaviorally by the eight-mutation row at `scripts/__tests__/camp01-authority-receipt-writer.test.ts:254` calling the exported `validateHBindings` directly and are unpinned by message only (the camp-01f/g sibling `authority identity registry drift` at `:98` is a fifteenth zero-hit message, outside this branch, named only so the ledger is complete). **Tasks 5.1-5.3 MAY close against the 5G1/5G2/5G3 receipts exactly as the sub-split declaration above provides.** **What those receipts do NOT establish, declared here because it is otherwise invisible: a camp-01h receipt produced by that work proves ONLY that the receipt machinery composes, validates, reopens and cleans up end to end. It does NOT constitute the three authority-evidenced play sessions that task 5.2 and camp-01h's own `threeSessionWitnessCount===3` name. The long-horizon CAMP-01 acceptance goal is explicitly NOT closed by PROOF-5, NOT closed by PROOF-6, and NOT closed by the completion of this change; a future reader must not infer otherwise from a green 5G receipt, a checked 5.2, or a checked section 6.** Evidence, measured: of camp-01h's twenty-one post-repair artifacts exactly SIX have real production producers (`reports/01-ux-audit-deep.json` through `reports/06-viewport-layout-sweep.json`, via `prepareCamp01HReport` at `scripts/qc/camp01-h-report-normalizer.mjs:33` consumed by `scripts/qc/run-ux-walkthrough.mjs` and `scripts/playwright/run-playwright.mjs`), exactly TWO are writer-derived (`command-result.json` composed at `camp01-authority-receipt.mjs:43`, `receipt-manifest.json` written at `:44`), and the remaining THIRTEEN have none; the DECIDING FACT is that the nine artifacts which constitute the acceptance claim — the three `witnesses/<label>/authority.json`, the three `witnesses/<label>/experience.json`, `combat-authority.json`, `session-authority-map.json`, `audit-reconciliation.json` — are nine-for-nine producer-less AND the app behavior they attest does not exist: `rosterInstanceId`, `unitSource` and `RosterUnitSource` each return ZERO tracked files under `src/` and zero under `e2e/`, and the alternation `savedDesignId|serverSessionId|terminalResultId|postBattleConsequenceId|readinessBlockerId` returns zero under `src/` and zero under `e2e/`, every tracked hit lying inside the receipt machinery's own contract/schema/writer/test surface (four files under `scripts/`) or the design documents that declare it (two under `openspec/`) — correcting the scoping pass, which recorded that alternation as one file repo-wide; it is six, none of them product code. Nine of the ten `PROGRAM_CHILD_CHANGES` sit at zero tasks done (0 of 71 aggregate: 0/14, 0/7, 0/7, 0/6, 0/6, 0/7, 0/8, 0/7, 0/9); the tenth is this change at 13 of 19. Commissioning real producers is therefore NOT a serializer project and is blocked one wave earlier than camp-01h: `prepareCamp01HReport` binds `ROW = WAVE_CONTRACTS['camp-01h']` at `camp01-h-report-normalizer.mjs:11` and resolves a reporter only from `ROW.reporterContracts` at `:35-40`, so camp-01f's `reports/campaign-persistence-authority.json` and camp-01g's `reports/mech-bay-authority.json` have no authority-report producer either — both names appear in tracked source only as validator entries in `camp01-authority-receipt.schemas.mjs` — while camp-01h's predecessor list (`camp-01g`, `proof-02-triage`, `proof-02-required-repairs`, `camp-01h-required-repairs`) requires a registered camp-01g receipt; the program's own frozen caps budget 3,460 changed lines across 82 files for the nine product waves BEFORE any producer work and before any sub-split, and every merged PROOF section has needed three to eight declared sub-PRs to fit one cap, so the scoping pass' figure of roughly eight-to-ten feature seams plus seven-to-nine producer seams (16-20 seams, 8,000-10,000 lines) is recorded here as a PROJECTION and never as a measurement. **THE REASON this distinction is load-bearing rather than pedantic — the indistinguishability result:** a fixture-authored receipt and a real-producer receipt are BYTE-INDISTINGUISHABLE to every validator in the chain. Of the twenty-three acceptance fact slots in `FACT_KEYS` (`camp01-authority-receipt.schemas.mjs:23`; 8 + 7 + 8 across the three witness labels) exactly SEVENTEEN fall through `validateFactValue`'s catch-all at `:167` and pass on any `sha256:` plus 64 hex with no binding whatsoever to app output, the other six binding only to registry membership (`saveReceiptId`, `reloadReceiptId`, `readinessBlockerId`), an integer floor (`savedDesignVersion`), or the frozen literals `true` (`savedDesignUnchanged`) and `'custom'` (`unitSource`). All EIGHTEEN trace-evidence ids (`H_TRACE_KEYS`, six keys at `camp01-authority-receipt.mjs:32`, times three labels) are minted by hash derivation inside the writer — `deriveEvidenceId` at `:67` is a sha256 over `camp01-evidence/v1`, runId, wave, label, kind and key, reached through `issuedEvidence` at `:61` — whether or not a route was ever hit. And, correcting the scoping pass upward, it is not roughly twenty-six of the wave assertions that are self-declared but ALL FORTY-SIX: `validateWave` (`schemas.mjs:131`) is their only validator and checks only that the id carries a recognized suffix, that the value's type matches it, and that the value satisfies the id's own literal, while the camp-01h branch of `validateCrossArtifacts` reads `assertions` zero times; the count is 46 (42 `===true`, 2 `>=N`, 2 `===N`) and the earlier 45 is corrected. Consequently the fixture authors NINETEEN of the twenty-one artifacts, not thirteen: mocking `writeReceipt`'s `runCommand` seam per `camp01-authority-receipt-writer.test.ts:37-52` means the six real producers never run, and that same harness writes `wave-result.json` at `:43` with every assertion defaulted to `true` at `:42`. **Declared future program (named owner, never a silent fold, following the disclosed-blocker precedent of the bullet above and the 5B F1 / 5D3 C08-D1 re-deferral precedent):** genuine acceptance requires, first, the unbuilt FEATURE work of the nine unstarted product child changes — the roster-instance, unit-source, saved-design, server-session, terminal-result and post-battle-consequence entities the twenty-three fact slots name, none of which exist in `src/` — and second, an authority-report PRODUCER layer for the thirteen producer-less camp-01h artifacts plus camp-01f's and camp-01g's reports, sourcing every fact from observed app output instead of a fixture; owner: the CAMP-01 product wave series `camp-00` through `camp-01h`, sequenced strictly after CAMP-PROOF closes, each wave carrying its own cap audit, reviews, SHA-guarded merge, exact-main receipt and prune exactly as declared for the PROOF sections, and a sub-split declared here first if any wave cannot fit. Until that program produces them, the three witnesses are FIXTURE-AUTHORED and every artifact attesting them is evidence about the machinery, never about the game. **PROOF-6 constraint (declared, not a rewrite):** section 6 is NOT amended by this declaration and its task text stands as written; but read as written, tasks 6.1 and 6.2 produce a `camp-proof` receipt and 6.2 closes with "block PROOF-02 until every cleanup predicate is true", which is the last instruction in the file, so the section heading "Receipt Handoff" plus a green terminal receipt is exactly what a future reader would mistake for the program's acceptance. The correction, binding on any PROOF-6 implementer and on any reader of its receipts: PROOF-6's receipts attest the CAMP-PROOF receipt machinery's own reviewed-head and exact-main reproduction, export, reopen and cleanup, and NO PROOF-6 artifact — receipt, cleanup predicate, or closure note — may be cited as evidence that the three authority-evidenced play sessions occurred. That evidence can come only from the declared future program above.
  - PROOF-5G1 unreachable-guard disposition (binding correction to the tail ledger of the acceptance-decoupling bullet above; raised by an adversarial review of the delivered seam and settled by execution, not by reading): the tail ledger records *invalid identity* as ALREADY-IN-SCOPE and discharged in part by `H issued identity drift` x3 (`camp01-authority-receipt.mjs:95`) and `unknown H reporter` (`:105`). **Both are structurally unreachable and neither has, or can have, a covering row.** Unlike the 5G-D1 disposition at :72 — which rests on structural reachability alone and says so — this disposition meets the 5B F1 standard: the equivalent mutants were RUN and are recorded here. **M1**, deleting the entire `witness.witnessId!==issued.witnessId||...` clause at `:95`, leaves all 23 rows of `camp01-h-composition.test.ts` GREEN. **M2**, replacing `?.normalizedPath??fail('unknown H reporter')` at `:105` with `?.normalizedPath??''`, leaves all 23 rows GREEN. Both guards are therefore silently removable today, and no row this seam could have added would change that. MECHANISM, identity: `validateAuthority` (`camp01-authority-receipt.schemas.mjs:147`) binds each witness id to its label — `requireEvidence(context,value.witnessId,['witness'],label)` and likewise for `executionId` and `contextId` — and the per-artifact validation pass at `camp01-authority-receipt.mjs:43` runs strictly BEFORE `validateCrossArtifacts` at `:95`. `issueHIdentities` mints exactly one id per (label, role), so every possible drift is consumed upstream: a duplicate fails `session identity reuse` (`schemas.mjs:157`), another label's valid id fails the label-bound `evidence registry membership drift`, and an unissued id fails the same guard on lookup. There is no fourth case. Proven by probe this session — swapping `contextId` between witnesses (the one drift that survives the distinctness check) yields `CAMP01_RECEIPT_INVALID: evidence registry membership drift`, never `H issued identity drift`. MECHANISM, reporter: `validateReport` (`schemas.mjs:135`) fails `reporter identity drift` on an `invocationId` mismatch before `reporterPath` at `:105` is ever reached. WHAT THE DELIVERED ROWS DO PIN, verified rather than asserted: the three `pins the independently derived H <role> identity` rows are real kill rows for the production derivation itself — **M17**, changing the `camp01-evidence/v1` domain string in `deriveEvidenceId` at `:68`, reddens all three plus every happy path, because the fixture re-implements the formula independently and compares against what the writer actually wrote; and `rejects an unknown H reporter before cross-artifact lookup` is a real kill row for `schemas.mjs:135`. Every row title states the guard it pins and none claims the unreachable one, so the 23-row count and the ledger agree once this bullet is read with them. OWNER: none — unreachable by construction, not deferred work. RE-OPEN TRIGGER, stated so a future change cannot silently resurrect the gap: if the label argument is ever dropped from any `requireEvidence` call in `validateAuthority`, or if the per-artifact pass is ever reordered after `validateCrossArtifacts`, `H issued identity drift` becomes reachable and owes a row at that time. CARRIED, with a named owner rather than folded: the `source-reconciliation H source set drift` row reaches `validateHBindings` by direct call with a locally mutated `reconciliation` object (`camp01-h-composition.fixture.ts` `action:'bindings'`), not by composing a receipt whose on-disk `audit-reconciliation.json` carries the drift. It is the only argument of `validateHBindings` with no composed route — `command`, `sessions` and `reports` all have one — so a future call-site normalization at `camp01-authority-receipt.mjs:93` could vacate the `sourceObservationReceiptIds` clause of `:101` while every row stays green. The row is genuine coverage of the guard's logic and is kept; what is missing is proof that the composed path routes the on-disk artifact into it. Owner PROOF-5G2, which already owns the reconciliation surface (`H reconciliation drift` x3 and `H repair source drift` x4 are declared there), cost ~1 fixture line plus a row re-route from `action:'bindings'` to `action:'mutate'`. `source-command` is NOT carried: composition genuinely cannot reach that clause, because `:44` derives `observedTestIds` from the same reports the guard compares against.
  - PROOF-5G1 disclosed blocker II and declared repair (fail-closed, named owner, binding; this AMENDS the disclosed-blocker bullet above rather than replacing it — that bullet's decided removal of `proof02-triage.json` from `hArtifacts` stands unchanged, stays inside PROOF-5G1, and is still necessary; PROVEN empirically on `219f45159` this session against the real `validateArtifact` and the real `SCHEMA_KEYS`, every probe executed from outside the worktree with `git status --porcelain` carrying nothing but the worker's untracked audit-stop report before and after): **removing `proof02-triage.json` does NOT clear camp-01h — `proof02-repairs.json` is a second name in the same frozen set that no schema-valid instance can survive, and it is the LAST blocker of its class, established by exhaustive audit rather than by sampling.** Mechanism, each leg re-read this session: `contract.mjs:21` `hArtifacts` carries 22 names today and 21 after the declared removal, and `proof02-repairs.json` is among the survivors; `writeReceipt`'s retained-artifact loop (`camp01-authority-receipt.mjs:42`, its set built by `retainedArtifacts` at `:77`) validates every non-PNG retained artifact through `validateArtifact(...,{...artifactContext(row,runId,request.sha,name),...receiptContext,provenance:request.provenance})`, and `artifactContext` at `:75` returns `{row,runId,sha,reporter}`, so `context.runId` is ALWAYS populated on that pass — `prepareValidationContext` at `:59` returns `{...context,registryContext}` and carries no `runId` key of its own, so the later spread cannot unset it; the generic artifact guard at `schemas.mjs:97` is `if (context.runId !== undefined && (value.runId ?? value.parentRunId) !== context.runId) fail('parent run drift')` and runs BEFORE the schema's handler; and `SCHEMA_KEYS['camp01-proof02-repairs/v1']` at `schemas.mjs:47` is exactly `['schema','triageReceiptId','repairs']` — no `runId`, no `parentRunId`. The result is a closed vise, and it was reproduced with four probes over the real module on Node 22: the canonical artifact `{schema:'camp01-proof02-repairs/v1',triageReceiptId:'receipt-2222222222222222',repairs:[]}` validated against `{runId:'camp01-11111111111111111111111111111111',registryContext}` returns `CAMP01_RECEIPT_INVALID: parent run drift`; the identical artifact and identical registry with `context.runId` omitted is ACCEPTED, so the schema is satisfiable and the harness is not at fault; and adding either binding to satisfy the guard returns `CAMP01_RECEIPT_INVALID: camp01-proof02-repairs/v1 fields drift` for `runId` and for `parentRunId` alike, because `exactKeys` runs first at `schemas.mjs:96`. The guard demands a binding the schema forbids publishing, so EVERY schema-valid `proof02-repairs.json` is rejected in a camp-01h retained-artifact pass. Phase-independent for the same reason the first blocker was: `mode` appears zero times on `camp01-authority-receipt.mjs:42`, `:52`, `:75` and `:77`, zero times on `schemas.mjs:97`, and zero times in `validateRepairs` (`schemas.mjs:153`), and the reopen path at `:52` supplies `context.runId` from `command.runId` through the same `artifactContext` — so neither reviewed-head observation nor exact-main final can publish, and no already-written receipt could be revalidated either. **The class audit is exhaustive and this is the last one, which matters because it means the remaining blocker inventory for camp-01h composition is now EMPTY rather than unknown:** of camp-01h's 21 post-repair artifacts, 19 are retained and 17 are JSON-validated (the two PNGs skip `validateArtifact` on the `!name.endsWith('.png')` arm), and resolving all 17 through the production resolver `assertArtifactPathSchema` (`schemas.mjs:58`) and reading each resolved schema's `SCHEMA_KEYS` row gives 16 with a run binding and exactly one without — `wave-result.json` binds `runId`; `session-authority-map.json`, `combat-authority.json`, `audit-reconciliation.json`, all six reports (`reports/01-ux-audit-deep.json` on `camp01-ux-audit-report/v1` and `reports/02`–`06` on `camp01-normalized-test-report/v1`) and all six `witnesses/<label>/{authority,experience}.json` bind `parentRunId`; `proof02-repairs.json` binds neither. **Declared repair (decided here, not a fork): REPAIR the binding, do NOT drop the artifact — the removal precedent above does NOT transfer.** Unlike `proof02-triage.json`, which belongs to the `proof-02-triage` wave and reaches camp-01h through its predecessor rather than through a copy in camp-01h's own set, `proof02-repairs.json` GENUINELY BELONGS to camp-01h on four independently verified legs: `validateProofInputs` (`run-camp01-authority-receipt.mjs:84`) gates repair tuples to camp-01h EXCLUSIVELY — `row.wave!=='camp-01h'&&arguments_.repairs.length` fails `mode-specific inputs drift`, the exact mirror of the `proof-02-triage` disposition gate in the same expression; `parseProof` (`:56`) parses `--repair` as 17-field tuples (`parseTuple(tuple,17,'repair')`, against 13 for dispositions); `validateRepairs` (`schemas.mjs:153`) is real validation and not a stub — predecessor-receipt provenance on `triageReceiptId`, sorted-unique `repairRowId`, repair-receipt and cleanup-receipt provenance per entry, and `tuple17.length!==17` failing `repair tuple drift`; and camp-01h's predecessor list carries the virtual `camp-01h-required-repairs` gate (`contract.mjs:35`), served by `repairGate` and by the `record.wave==='camp-01h'` arm of `repairSources` in `camp01-durable-facts.mjs`. The program spec requires the artifact by name — `openspec/changes/add-saved-custom-unit-campaign-roster/specs/journey-qc/spec.md:5` makes H completion require "writer-generated `proof02-repairs.json` containing zero or one verified 17-field repair tuple per required distinct cause", and that parent change's `design.md:92` names H as its producer from the repeated `--repair=` tuples — which is the exact inverse of the triage finding, where no spec requirement placed the artifact in H's set. Dropping it would discard a declared capability, so the decided route repairs rather than removes. **The repair is the PROOF-5E1 pattern applied to the sibling schema, and it has TWO legs, not one.** Schema leg: add `runId`/`parentRunId` to `SCHEMA_KEYS['camp01-proof02-repairs/v1']` exactly as #1186 added them to `camp01-proof02-triage/v1` at `schemas.mjs:40`, add the sibling of its `triage parent run drift` clause inside `validateRepairs`, and mirror `IProof02RepairsV1` at `openspec/changes/add-camp01-authority-receipts/design.md:194`. Writer-composition leg: `writeReceipt` must COMPOSE the artifact for camp-01h from the accepted repair tuples and publish it, exactly as `:37` now composes `triageArtifact={schema:'camp01-proof02-triage/v1',runId,parentRunId:request.reproduction.parentRunId,...request.triage}` and `:41` writes it — and this leg is strictly larger than 5E1's, because NO `repairInputs` composer exists today: `triageInputs` (`camp01-durable-facts.mjs:256`, called at `:230` under `row.wave==='proof-02-triage'`) is the analogue that DOES exist for triage, while `arguments_.repairs` dead-ends after the gate — its only two references anywhere under `scripts/qc/` are its `parseProof` construction and `validateProofInputs`, so the tuples are parsed, gated to camp-01h, and then consumed by nothing. Measured rather than assumed, because it decides what the writer must publish: `runId` alone is the load-bearing binding, since `(value.runId ?? value.parentRunId)` short-circuits — probed on `camp01-proof02-triage/v1`, a matching `runId` beside a differing `parentRunId` clears the guard and fails later inside its own handler, while a differing `runId` beside a matching `parentRunId` fails at the guard — so the writer SHALL publish `runId` from writer entropy, and `parentRunId`, which would bind to the `proof-02-triage` predecessor receipt that `triageReceiptId` already names, is a sibling-symmetry choice the repair seam must declare explicitly rather than a guard requirement. **Ownership stated honestly: this is the PROOF-2 writer/schema surface again — the same owner as the first section-3 closure blocker that PROOF-5E1 repaired — and it is the THIRD instance of one root pattern**, a frozen artifact set naming an artifact for which the writer cannot produce a valid instance in that wave's write context: instance (1) `proof02-triage.json`'s missing run binding, closed by #1186 `8180672cc`; instance (2) `proof02-triage.json`'s unsatisfiable `!provenance||!reproduction` context inside camp-01h, decided above as the removal; instance (3) this one. Instances (1) and (3) are the identical run-binding shape and both land on PROOF-2's writer/schema surface; the pattern is recorded as a pattern and not as a coincidence, and any later seam adding a name to a frozen artifact set is on notice to check its schema against `schemas.mjs:97` first. **Priced against the measured 5E1 comparable and the camp-proof cap row, with the recommendation following from the arithmetic rather than from taste:** PR #1186 is 77 changed lines across 4 files, independently re-measured this session by `git show --numstat 8180672cc` (44+/0- `camp01-authority-receipt-writer.test.ts`, 10+/4- `camp01-authority-receipt.mjs`, 9+/4- `camp01-authority-receipt.schemas.mjs`, 3+/3- `validate-camp01-authority-receipt.mjs`, i.e. 66 added and 11 deleted), which corroborates the 77/4 figure the 5E1 receipt above records. This repair is 5E1 minus its `resolveReceiptRow` boundary but plus the composer 5E1 got for free, so it prices at 50-82 changed lines across 6-7 files (central ~66): production ~18-34 across `schemas.mjs` (the `SCHEMA_KEYS` row plus the `validateRepairs` binding clause), `camp01-authority-receipt.mjs` (request-key admission on the `exactKeys` ternary at `:37`, the compose/validate block, the publication), `camp01-durable-facts.mjs` (the new `repairInputs` composer and its `:230` call site), `run-camp01-authority-receipt.mjs` (threading `arguments_.repairs` into the write request), `validate-camp01-authority-receipt.mjs` (the public-validator leg 5E1 paid 6 changed lines for) and the `design.md:194` mirror, plus 32-48 proving-row lines at 5E1's measured four-lines-per-kill-row over 8-12 rows. **Recommendation, decided here: its OWN declared seam `PROOF-5G0`, ordered strictly BEFORE 5G1 — 5G1 does NOT absorb it and its band is NOT widened.** The arithmetic decides it: 5G1 is declared at 390-488 (central ~451) across 7 files against camp-proof's `maxFiles:15`/`maxChangedLines:500` row (re-read this session from `WAVE_CONTRACTS['camp-proof']`), so its declared ceiling already sits 12 lines under the hard cap; folding this repair in gives 440-570 across 11-13 files — the ceiling breaches 500 by 70 and the CENTRAL estimate breaches it by 17 (451+66=517), which is a cap breach at the EXPECTED outcome and not merely at the pessimistic tail, and the file count would land at 11-13 of 15 with four production files (`schemas.mjs`, `camp01-authority-receipt.mjs`, `camp01-durable-facts.mjs`, `run-camp01-authority-receipt.mjs`) that 5G1's authoritative 7-file plan does not contain. Widening the band cannot rescue that, because 500 is a cap and not a band: a wider declaration buys no headroom and would only make the breach declared instead of accidental. As its own seam the repair is 50-82 lines across 6-7 files, comfortably inside 15/500, and 5G1 then proceeds at 390-488 across its declared 7 files completely unchanged — which is also why the already-declared triage removal STAYS in 5G1 rather than migrating into 5G0: it is owned by the PROOF-1 contract surface where this repair is owned by PROOF-2's, the 5E1/5E2 precedent splits repair seams by owner, its 10-18 lines are already carried inside 5G1's band and its 3 files are already in 5G1's plan, and moving it would re-open a declared file plan for zero cap benefit. Named `PROOF-5G0` and not `PROOF-5E3` because this ledger is ordered and the seam must land before 5G1, while the 5E repair family closed at `2987aa0e8` with 5F and both 5G declarations already recorded after it. 5G0 proves its repair the way 5E1 did — unit-level kill rows over the real schemas and the real writer — and does NOT require a composed camp-01h receipt, which is 5G1's deliverable and depends on both repairs landing first; order is therefore 5G0, then 5G1, then 5G2, then 5G3, and 5G0 carries its own pre-implementation cap audit, three sequential orchestrator-run independent reviews, SHA-guarded merge, exact-main receipt and prune exactly as every seam above. **Recorded because the declared fail-closed behavior belongs in the ledger and not only in a worker report:** the Sol worker dispatched for 5G1 reproduced this blocker against the real writer, REMOVED its partial implementation rather than leaving false kill rows or a knowingly red worktree behind, and stopped without code — which is exactly the declared behavior, and is independently confirmed by the worktree carrying zero implementation changes at `219f45159`. Nothing above is waived by this amendment: the acceptance-decoupling declaration stands unchanged, the 5G1/5G2/5G3 bands and file plans stand exactly as declared, and task 5.2 still closes only against sub-seam receipts — now four of them.
  - Receipt (PROOF-5G0): PR #1198 merged head `e6b90341d` as `a7ff70e0debb948cfac5409b6f0347bd937c9d32`; REPAIRS disclosed blocker II (owner: the PROOF-2 writer/schema surface; third instance of the frozen-set/unproducible-artifact pattern) — `proof02-repairs.json` gains its run binding (schema keys `runId`/`parentRunId`/`triageReceiptId`; the `repairs parent run drift` clause in `validateRepairs`) and the writer now COMPOSES it for camp-01h from accepted `--repair` tuples through the new `repairInputs` composer (`camp01-durable-facts.mjs:259`) threaded via `run-camp01-authority-receipt.mjs` and a wave-gated spread in `camp01-proof-environment.mjs`; 54 changed lines across 7 files (20 production, 32 proving rows, 2 spec mirror), measured from the merge commit and inside the declared 50-82 band; serial sha256-guarded mutation matrix with the parent-run clause killed 1:1; exact-main proof on the merged SHA; external receipt `~/.codex/evidence/MekStation/2026-08-08-proof5g0-a7ff70e0d/`; pruned.
  - Receipt (PROOF-5G1): PR #1200 merged as `c87dcc4267d71e0d35654bf03f9abbcf3c5e9971`; the camp-01h cross-artifact branch made reachable end to end — the shared composition fixture (`scripts/__tests__/support/camp01-h-composition.fixture.ts`) drives the real `writeReceipt` at wave `camp-01h` mocking only `randomBytes` and `runCommand`, the declared `proof02-triage.json` removal lands (`hArtifacts` 22→21) with the literal 21-entry artifact-set pin closing the QC freeze gap, and the declared 23 rows land (composition observation/final/reopen/public-validator happy paths, `H phase drift` x3, per-report witness binding, inventory/registry binding); 400 changed lines across 8 files against the declared 390-488 band; M17 (domain-string mutation in `deriveEvidenceId`) reddens all three identity rows plus every happy path, proving the fixture re-derives the writer's formula independently; the adversarial review's "tautological rows" and "misleading titles" findings were refuted by execution before anything was deleted; umbrella 20→21 suites, 572 passed / 9 skipped; exact-main proof on the merged SHA; external receipt `2026-08-08-proof5g1-c87dcc426`; pruned. The unreachable-guard disposition bullet above (M1/M2 run and recorded) is part of this seam's record.
  - Receipt (PROOF-5G2): PR #1201 merged as `cc54d0ac3d85249820179fb590cad2bfd5180328`; H authority/experience binding, the six persistence-stage failure simulations (before save, readiness, session, command, terminal, post-battle) each sourcing its canonical repair through `registryContext.repairSources` and then passing a fresh complete final, unavailable-final rejection, the finding-identity rows, and the carried source-reconciliation composed-route row; suite 23→57 rows; three declared rows dispositioned by execution rather than written — a removal mutant CANNOT redden a positive/acceptance row because `validateCrossArtifacts` is called for side effect only and its return is discarded (`camp01-authority-receipt.mjs:53`), so the equivalent test for a positive row is corrupting the expectation source, and the worker's twelve-row credit to a clause via an over-rejecting mutant was refuted on this ground (that clause had zero kill rows and the receipt says so); umbrella 606 passed / 9 skipped; exact-main proof on the merged SHA; external receipt `2026-08-08-proof5g2-cc54d0ac3`; pruned.
  - Receipt (PROOF-5G3, closes PROOF-5): PR #1202 merged as `c1dae23e53b50c5ff46ecb75ae8657072fe38660`; durable H receipt envelope, H durable consumers — the H arm of `repairSources` (`camp01-durable-facts.mjs:237`), the `camp-01h-required-repairs` `repairGate` (`:239`), the non-proof arm of `resolveRepairSource` (`:70`) — and the re-assigned cap-clause/manifest-shape/observation-shape tabular matrices; camp-01h coverage 0→125 rows across three suites; umbrella 21 suites / 643 passed / 9 skipped (549→643 across the 5G session); the 5G-D1 durable-validator identity sub-guards carry their documented defense-in-depth disposition exactly as the declaration assigns; exact-main proof on the merged SHA; external receipt `2026-08-08-proof5g3-c1dae23e5`, which also records the PROOF-5 section close-out; pruned. C09-L1's provisioned production-context execution remains the one open live-evidence item.

## 6. Receipt Handoff — PR PROOF-6

PROOF-6 is delivered as four declared ordered sub-PRs (declared 2026-08-08, per the program owner's fabrication fork — "dedicated anchoring seam before PROOF-6" — scoped by the same owner's answer to the scope question as "envelope anchors only", and the same-day pre-implementation audit, which read every consuming surface named below in this session and stopped fail-closed without code): **PROOF-6A1 validation-time Git envelope anchoring**, **PROOF-6A2 CI-run anchoring and the live anchor probe**, **PROOF-6B reviewed-head `camp-proof` receipt** (task 6.1 exactly as written), **PROOF-6C exact-main `camp-proof` receipt, export/reopen, cleanup publication, and prune** (task 6.2 exactly as written). **Why an anchoring seam precedes the handoff, measured:** at validation/reopen time every check in the chain is INTERNAL consistency — `command.sha` and `command.treeSha` are regex-checked only (`camp01-authority-receipt.schemas.mjs:11`, `:97`), `validateCap` (`schemas.mjs:122`) bounds the declared numbers against the row caps and checks reviewed-head linkage but never recomputes anything from a repository, and the validation context is caller-supplied verbatim (`validate-camp01-authority-receipt.mjs:18` parses `CAMP01_VALIDATION_CONTEXT`; the durable index composes it from the on-disk candidate records themselves at `camp01-durable-facts.mjs:225`) — so a fabricated receipt directory with well-formed random SHAs, a self-declared cap and self-consistent digests passes the public validator end to end and enters the durable index that feeds every successor predecessor gate (`camp01-durable-facts.mjs:66`). PROOF-3's construction-time verification is real, but a fabricated receipt never went through construction; the reopen path re-verifies bytes against digests and digests against nothing. **PROOF-6A1** adds `scripts/qc/camp01-anchor-authority.mjs` — an anchor authority with an injectable verified-Git dependency following the 3C adapter precedent, wired into durable record admission (after `invokeValidator` accepts and before the record is pushed, `camp01-durable-facts.mjs:64`) and composed with the already-resolved verified Git in `createProductionDependencies` (`:84`) — carrying three anchor classes, each with per-clause kill rows over real temporary repositories (the 5B precedent): **A1 commit/tree resolution** — `command.sha` must resolve as `^{commit}` and `command.treeSha` must equal `sha^{tree}` under the verified Git, values a writer cannot mint without a real object store containing them; **A2 ref reachability** — an exact-main record's `sha` must be an ancestor of (or equal to) the fetched `refs/camp01/fetched-main` OID obtained through the existing 3C1 fetch-equality route (`fetchAndVerifyOids`, `camp01-git-trust.mjs:54`), and a cap-owning record's `baseSha` must be an ancestor of its `headSha` (`merge-base --is-ancestor`, the `resolveTargetFacts` precedent at `camp01-target-authority.mjs:56`); **A3 cap recomputation** — `fileCount`, `changedLineCount`, `binaryEntries` and `changedTreeManifestDigest` must equal the values re-derived from `git diff --numstat -z --no-renames baseSha headSha` by THE SAME derivation `resolveTargetFacts` uses (`camp01-target-authority.mjs:57-58`), reusing its parse/digest helpers as exports and never a reimplementation, so a cap claiming 400 changed lines against a diff of 700 rejects at reopen exactly as it would at construction. Band 240-340, central ~290, across 6-8 files (new module ~70-100, durable admission wiring ~8-15, production composition ~4-8, kill rows ~130-190 at the measured 80-characters-per-changed-line rate, umbrella coherence 3 measured lines, `design.md` mirror ~8-15), priced against PROOF-5E2 (358/9, the new-module comparable) and PROOF-5F (137/5). **PROOF-6A2** adds the fourth class: **A4 CI-run existence** — a successful check run must exist for the cap's `headSha` on the frozen `REPOSITORY_IDENTITY` via the injectable transport seam (`camp01-github-provenance.mjs:160` `fetchGitHubResource`), mocked in hermetic rows exactly as 3C2 mocks it, plus a live anchor probe registered through the machine-readable live tier (`run-camp01-live-adversarial.mjs`, the C09-L0 shell) exercising the production transport and the production Git anchors against this repository's real history — the live tier is the honest home for the only legs Jest cannot reach. Band 110-190, central ~150, across 4-6 files (transport anchor ~25-40, hermetic rows ~60-90, live probe ~30-50, mirror ~5). **Forward-only scope, stated so no prior artifact is silently re-blessed:** zero durable machinery receipts exist to grandfather — verified this session, the evidence root carries only `2026-07-07-live-playtest/` — so no compatibility path exists or is added, and the anchor authority applies to every receipt that enters a durable index from its merge onward; the 38 external process receipt directories under `~/.codex/evidence/MekStation/` (2026-08-02 `proof02-save-reload-77fb47ef` through 2026-08-08 `proof5g3-c1dae23e5`, enumerated this session) are receipts of the PR process itself, were issued under the pre-anchor contract, and are recorded here as such — the anchor authority makes no claim about them and this declaration does not re-bless them. **Declared NOT taken, with the reason on the record:** the 17 catch-all fact slots, 18 writer-minted trace ids and 46 self-declared wave assertions named by the acceptance-decoupling declaration under task 5.3 remain unanchored by this seam — their referents are app entities that do not exist (measured there: `rosterInstanceId`, `unitSource`, `RosterUnitSource` and the id alternation each return zero tracked product files), so any anchor a fixture supplied for them would itself be fixture-chosen, which is anchoring theater rather than anchoring; their binding is owned by the CAMP-01 product wave series exactly as the acceptance-decoupling declaration assigns, and a green PROOF-6A1/6A2 receipt MUST NOT be read as closing that gap. **Process:** order is 6A1, 6A2, 6B, 6C; implementation is dispatched to a Sol worker when the quota admits one and is orchestrator-authored at full rigor otherwise (the standing fallback decided 2026-08-08); each sub-PR carries its own pre-implementation cap audit, three sequential independent reviews, a serial sha256-guarded mutation matrix run by the orchestrator, SHA-guarded merge, exact-main proof on the merged SHA, external receipt, and prune, exactly as every PROOF-5 seam above; tasks 6.1/6.2 close only against the 6B/6C receipts; the PROOF-6 constraint of the acceptance-decoupling declaration binds every sub-seam here — no PROOF-6 artifact may be cited as evidence that the three authority-evidenced play sessions occurred; a further split must be declared here first if any sub-seam cannot fit. The PROOF-5G0-5G3 receipt lines recorded under task 5.3 in this same branch are bookkeeping under the same-branch precedent of the disclosed-blocker bullet and are not counted against any band.

- Receipt (PROOF-6A1): PR #1205 merged as `9b9a773965dfe85783c5855407ceea1fb89aff6c`; validation-time Git envelope anchoring — `scripts/qc/camp01-anchor-authority.mjs` invoked at durable record admission (after the public validator accepts, before the record is pushed) and composed with the verified Git in `createProductionDependencies`; A1 commit/tree resolution, A2 fetched-main reachability through the 3C1 route plus cap base→head ancestry, A3 cap recomputation through the exported `parseNumstat` derivation; 333 changed lines across 8 files against the declared 240-340/6-8 (the two files beyond the code plan are the lifecycle row's explicit anchor injection and the design mirror); orchestrator mutation matrix 9 kills 1:1 including **M-A1a, which SURVIVED the worker's 12 rows** — deleting `^{commit}` resolution stayed green because a tree OID posing as a commit passed A1 — remediated in-seam with the `tree-object` kill row (12→13 rows) and re-proven 1:1; M-W2/M-W3 dispositioned defense-in-depth by executed probe (both degrade fail-closed downstream); the dispatched Sol review worker was killed mid-hunt by an upstream content filter with no findings produced, and reviews 2-3 were orchestrator-run per the standing fallback (recorded rule: phrase review charters as defensive QA); exact-main proof on the merged SHA umbrella 22 suites / 656 passed / 9 skipped, boot proof exit 0, strict 238/238, QC 21/21; external receipt `2026-08-09-proof6a1-9b9a77396`, which also records the seam's explicit non-claims — envelope anchors do not resist replay of a REAL historical envelope (owned by the predecessor/provenance/runId chain) and do not anchor fact slots (owned by the product waves); pruned.
- PROOF-6A2 transport reconciliation (declared amendment to the sub-split declaration above, prompted by the 2026-08-08 requirement handoff's instruction to reuse the live tier's `runGh` transport pattern rather than invent a second transport): the ADMISSION leg of A4 stays on the in-process injectable REST seam — `fetchGitHubResource` gains a `check-runs` resource (`/repos/SwiggitySwerve/MekStation/commits/<sha>/check-runs`, OID-validated parameter) in `resourceEndpoint` (`camp01-github-provenance.mjs:191`) — because durable admission runs inside the 3B pinned-environment philosophy where spawning an unpinned ambient `gh` binary would widen the trusted surface, and the REST seam is already the production transport 3C2 composes; the `runGh` seam (`report-camp01-live-status.mjs:83`) remains the live tier's evidence-retrieval transport, which is where the handoff's reuse instruction is honored. Amended mechanics, declared before implementation: the anchor authority gains an injectable `fetchCheckRuns` dependency that is MANDATORY for cap-owning candidates (absent → fail-closed `anchor dependency invalid`; no successful completed check run for `cap.headSha` on the frozen repository → `anchor ci run drift`), with the 6A1 suite's harness updated to inject a canonical fake and its existing rows unchanged; `resolveFetchedMainOid` is exported read-only-additively from `camp01-durable-facts.mjs` so the live probe can drive the production A2 composition; the 6A2 live probe registers INLINE in the C09-L0 shell (the 5D4 precedent for shell-owned probes) with capability gate `hosts:['win32']` (the verified-Git default resolver is Windows-only, so the ubuntu skip is EXPECTED), closes the 6A1-deferred `resolveFetchedMainOid` coverage item with live evidence, splits transport outcomes honestly — network-layer failures skip with reason, HTTP/shape/verification failures FAIL — and probes A4 against the most recent prior main commit carrying a completed successful check run via a bounded walk-back, because the triggering push's own check runs are still in progress at probe time. File plan: 6 code files (`camp01-github-provenance.mjs`, `camp01-anchor-authority.mjs`, `camp01-durable-facts.mjs`, `run-camp01-live-adversarial.mjs`, the anchor suite, the provenance suite) plus this tasks.md bookkeeping and the design.md mirror; band unchanged at 110-190.
- Receipt (PROOF-6A2): PR #1206 merged as `b69382fd43b26a37de18dcebcbeb9e5231f74da0`; CI-run anchoring and the live anchor probe exactly per the transport-reconciliation amendment above — A4 requires an injectable `fetchCheckRuns` at construction (`anchor dependency invalid` when absent) and, for cap-owning candidates, at least one completed successful check run whose `head_sha` equals `cap.headSha` (`anchor ci run drift`), composed in production from `fetchGitHubResource` with the OID-validated `check-runs` resource; the live probe `proof6a2-envelope-anchor-production` drives the production anchor module plus the newly exported `resolveFetchedMainOid` end to end against real history (orchestrator's own run: fetched-main OID = initiating HEAD = anchored SHA; nine successful check runs among sixteen on the prior main commit at walk-back depth 1), closing the 6A1-deferred coverage item; 177 code lines across the six declared files; orchestrator matrix six kills 1:1 including **M-B1b, which SURVIVED the worker's rows** — a completed FAILING-conclusion run satisfied A4 — remediated in-seam with the `ci-failed-conclusion` row and re-proven 1:1 (pattern recorded: BOTH 6A seams' worker suites missed exactly one conjunction-clause kill row; every conjunction clause gets a personal mutant); **M-B4** (hardcoding success in `productionAnchor`'s composed closure) proven a hermetic survivor at 89/89 green and dispositioned with a named re-open trigger — the composed REST leg's falsification surface is 6B/6C's real cap-owning durable admissions, and if the 6B receipt does not traverse one through `createProductionDependencies` this disposition re-opens and owes a hermetic composition row; check-runs pagination rejects fail-closed beyond page one (P2); exact-main proof on the merged SHA umbrella 22 suites / 664 passed / 9 skipped, boot proof exit 0, strict 238/238, QC 21/21, live tier 9/0/2-expected exit 0; external receipt `2026-08-09-proof6a2-b69382fd4`; pruned.
- H-witness reduction (declared amendment, user-approved 2026-08-08, executed 2026-08-09; the spec deltas ride this same branch): the three CAMP-01H acceptance witnesses are REDEFINED against behavior the application ships today in `openspec/changes/prove-saved-custom-unit-campaign-journey/specs/journey-qc/spec.md` (requirement paragraph plus the two rewritten scenarios) and in the H-completion clauses of `openspec/changes/add-saved-custom-unit-campaign-roster/specs/journey-qc/spec.md` (the `a real canonical production combat/post-battle chain` completion clause becomes the reloaded ten-contract checkpoint chain, and the appended REDUCED CLAIM paragraph defines all three redefinitions). Both amendments state outright, in RFC-2119 text, that this is a REDUCTION of the original claim and not satisfaction of it: the saved-custom-unit boundary attestations remain unbuilt and owned by the CAMP-01 product wave series, the machinery fact-slot contract still names the unreduced entities and stays fixture-satisfiable only until that series re-points it, and no green three-witness run under the reduced claims may be cited against the unreduced goal. The long-horizon CAMP-01 acceptance goal remains OPEN exactly as the acceptance-decoupling declaration records.
- PROOF-6B0 solo-provenance reduction (declared amendment and seam, decided 2026-08-09 by the program owner after the 6B pre-flight audit surfaced a fail-closed external impossibility; the fork options — second collaborator account, reduce the clause, park PROOF-6 — were put to the owner explicitly and "reduce the approval clause" was chosen): the live 3C2 preflight requires an exact-head non-dismissed NON-AUTHOR approval with `WRITE|MAINTAIN|ADMIN` on every cited spec PR, every program-spec citation, and the owned PR (`verifyCitation`, `camp01-github-provenance.mjs:140`), and that requirement is UNSATISFIABLE in this repository as measured this session: zero reviews exist on every sampled PR (#1134, #1203, #1206, #1207 all return empty review lists), `SwiggitySwerve` is the sole collaborator, and GitHub forbids both self-approval and approving already-merged PRs, so no historical citation can ever gain an approval. **Declared reduction:** citations MAY carry the sentinel approval tuple `solo-maintainer|<owner-login>`; `verifyCitation` gains a solo arm that, for sentinel citations only, verifies via the live API that the repository's collaborator set is EXACTLY `[reviewer]` (new OID-less `collaborators` resource in `resourceEndpoint`), that `pull.user.login === reviewer` (owner-authored), that merged citations carry `merged_by.login === reviewer`, and that the reviewer holds `ADMIN` permission — the strict arm stays byte-identical for real approvals, and the solo arm REJECTS the moment more than one collaborator exists, so the reduction self-invalidates exactly when an independent reviewer becomes possible. The spec-text reductions in `add-camp01-authority-receipts/specs/journey-qc/spec.md` (the provenance requirement and its reviewed-head scenario) and `add-saved-custom-unit-campaign-roster/specs/journey-qc/spec.md` (the authors-may-be-same clause) ride this same branch and state outright, in normative text, that receipts produced under the sentinel attest SELF-REVIEWED provenance — a reduction of the independent-review claim, never satisfaction of it. File plan for the code seam: `camp01-github-provenance.mjs` (solo arm ~15-30 lines, `collaborators` endpoint ~1-2), the provenance suite (solo-accept, multi-collaborator reject, author-mismatch reject, merged-by-mismatch reject, endpoint row — ~5-8 rows), `design.md` tuple-doc mirror; band 90-160, central ~120, across 4-5 files; standard process (cap audit, orchestrator reviews, serial sha256-guarded matrix, SHA-guarded merge, exact-main proof, external receipt, prune). **Execution plan for tasks 6.1/6.2 once 6B0 merges:** the 6B0 implementation PR's merge serves as the ledgered citation for the `camp-proof` spec tuple and all ten `--program-spec` tuples (every program child change is ledger-accounted at any recent main SHA); a small owned product PR branched from that merge carries the camp-proof cap; the controller produces the reviewed-head receipt against it (task 6.1), the PR merges under the standard gate, and the exact-main receipt, export/reopen, cleanup publication, and prune close task 6.2.
- Receipt (PROOF-6B0): PR #1209 merged as `ecb67e15825ed25dd80e8407328be78e1ad46a5e`; the solo-maintainer sentinel provenance arm exactly per the declaration — collaborator set API-verified as exactly the cited owner, owner-authored and owner-merged pulls, ADMIN-only permission, strict arm byte-identical, sentinel rejecting whenever a second collaborator exists; 122 code lines across the declared files; orchestrator matrix six kills all 1:1 with ZERO survivors and no remediation required (the first seam this arc whose worker suite matrixed complete); provenance suite 63/63, umbrella 22 suites / 671 passed / 9 skipped, strict 238/238, QC 21/21, oxfmt/oxlint green, both commits verified non-empty (the worker report swept into the first commit was removed by the second); pruned. This same branch is the camp-proof OWNED PRODUCT PR: it carries the branch name the contract's `bootstrapProductRef` declares (`codex/implement-camp01-authority-receipts`), its head is the reviewed-head SHA task 6.1's receipt proves, and its citation/product tuples use the sentinel per PROOF-6B0.

- [ ] 6.1 Produce and validate a reviewed-head `camp-proof` receipt with exact spec/product provenance, then obtain independent review and merge with a SHA guard.
- [ ] 6.2 Produce and validate a fresh exact-main `camp-proof` receipt, export/reopen it, publish/revalidate cleanup, prune only the creation-bound branch/worktrees, and block PROOF-02 until every cleanup predicate is true.
