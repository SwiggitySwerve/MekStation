CAMP-PROOF is implemented as six ordered focused PRs — PROOF-1 contract/tests, PROOF-2 writer/validator, PROOF-3 exact-SHA controller/worktree lifecycle, PROOF-4 runner integration, PROOF-5 adversarial verification, PROOF-6 receipt handoff — one per numbered section below, in order. Each PR MUST stay under 500 non-generated changed lines and 15 files, own its one behavior seam, pass focused tests plus Node 22 typecheck/lint/format and strict OpenSpec/QC, receive sequential independent review, merge with a SHA guard, verify on exact main, and prune its branch/worktree before the next seam begins. The former single-PR shape is retired: it could not credibly satisfy the cap across six subsystems.

## 1. Immutable Contract — PR PROOF-1
- [ ] 1.1 Add `camp01-authority-receipt.contract.mjs` with strict v1 schema, canonical repository/fetch identity, frozen ten-child set, twelve logical `@node|@npm` command rows/run roots and reviewed digests, frozen CAMP-01E/H command-index/invocation/PNG capture contracts, canonical PROOF/H repair-row extensions/virtual gates, predecessors, caps, H reporters, bootstrap ref, artifacts, and assertions.
- [ ] 1.2 Add contract tests that recompute every digest and reject unknown fields/ids, caller/other executable tokens, root/argv/order/chaining drift, missing/extra/unknown-wave/reordered command-index/invocation/PNG capture contracts, invalid repair declarations/rows/manifests/sources, wrong PROOF lexical or H lowest-rank roots, non-null root aliases, cyclic/self/chained/multi-root/nonterminal causes, gate/cleanup/cap/provenance gaps, unsafe retained values, and H observation/final/report drift.

## 2. Writer and Validator — PR PROOF-2
- [ ] 2.1 Implement strict argument/provenance parsing, random parent run identity, exclusive staging, immutable sequence execution, writer-owned `command-result.json`, closed D10 artifact/manifest/cap schemas and canonical bytes, kind/source-bound exact-set digest-only identity registry, atomic single-child publication, and privacy allowlisting.
- [ ] 2.2 Implement exact producer/reporter/schema/source/status/id/path/digest-owner validation; require parent/witness/execution/label/status, observation fact-wrapper/report-fingerprint, final complete fact/evidence, combat, reconciliation, exact-main-to-reviewed-head cap, manifest-file-type, observed entity-kind/source equality, and sorted exact-PNG-set capture attestations with matching artifact/manifest digests, invocation, equal pre/post state/counter digests, and pinned allowlist/barrier digests; reject stale, cross-run, duplicate, omitted, reordered, substituted, tampered, incomplete, abnormal-exit, predicate, and witness drift.
- [ ] 2.3 Implement complete PROOF-02 reproduction publication for exit 0/1, null fingerprints only for passed observations and writer-derived non-null fingerprints for failed/missing observations, self-digest-free exact-byte artifacts, plus zero-command triage with exact observation-set equality and outcome-specific disposition validation.
- [ ] 2.4 Implement CAMP-01H parent/three-child identity issuance, follower-ineligible observations with complete observed/unavailable typed facts after six ordinary exit-0/1 attempts, exact one-to-many failed-report/finding/cause-row mapping with canonical source id/fingerprint, reconciliation, and fresh finals whose complete authority plus six invocations/reports/wave/assertions pass after every repair/cleanup or blocker.

## 3. Exact-SHA Controller — PR PROOF-3
- [ ] 3.1 Add `run-camp01-authority-receipt.mjs` and its `register-pr-target`, `proof`, and `cleanup` CLI/state contract; implement subject-specific product/audit/none provenance, merged-spec PROOF/H repair registration plus exact closed source receipt/observation/cause and registry-set equality, the ten-child/predecessor/cap preflight, reviewed-head observation/final and exact-main detached worktrees, clean manifests, and non-reparse row-root confinement.
- [ ] 3.2 Pin Node/npm/Git/System32 absolute binaries, expand only `@node|@npm`, run fixed bootstrap, construct child environments from zero with writer temp/profile and empty npm configs, bind tool/config/lockfile/environment digests, and reject PATH/CWD, credentials, proxies, Git/Node/npm/CI/cloud/product/Playwright ambient drift.
- [ ] 3.3 Use the verified Git binary, literal HTTPS URL, disabled config/credentials/proxies/prompts, and a writer-owned empty bare proof repository; require fetched head/main OIDs equal pinned GitHub API identity before verifying PR heads/merges, approvals, ledger, and ancestry with mocked GitHub/transport adapters.
- [ ] 3.4 Implement exclusive durable export, non-self-referential command-result/receipt-manifest digest ownership and recomputation, atomic rename, reopen validation, separate detached-proof and subject-bound owned target records, the single-use exact-reviewed-head CAMP-PROOF bootstrap, strict pre-edit creation for PR-owning rows, no owned target for reproduction, independent non-force removal, owned-ref exact-OID compare-delete, and writer-owned follower cleanup receipts.

## 4. Runner Integration — PR PROOF-4
- [ ] 4.1 Add opt-in routing for fresh empty browser profile/storage/runtime database, Next/results/HTML/snapshots/traces/videos/screenshots, UX/H runners, and six normalized H reports; PNG rows seed only exact fixtures and use a guarded capture transaction with monotonic DOM/app-write counters, a capture-relevant mutation barrier, fixture-only allowlist, equal canonical pre/post state digests, and digest-only attestation bound to invocation/path/PNG, remove declared runtime only, and preserve defaults without CAMP.
- [ ] 4.2 Add `run-viewport-sweep.mjs` that imports H's final three logical arrays and the shared verified resolver, expands `@node` to the verified absolute executable before sequential `shell:false` spawning, propagates the first failure, and tests both logical and expanded equality; replace the chained package script with this single command.
- [ ] 4.3 Add package commands for controller, focused receipt writing, validation, and tests without adding a dependency.

## 5. Adversarial Verification — PR PROOF-5

- [ ] 5.1 Use the test-only injected Git process/transport adapter to hermetically assert the verified executable, literal fetch URL, disabled config/credentials/proxies/rewrites, and API-returned OID equality/mismatch behavior while proving production cannot accept the adapter; use real temporary Git repositories/worktrees for exact proof/export/reopen/cleanup, dirty/reparse/ref/OID races with no deletion, caps/compare-delete/export collisions, and durable post-removal validation. Inject visibly rendered non-fixture profile/database/storage sentinels before the guard and through a test seam after the initial guard but before screenshot; prove the barrier/counters or unequal post-state reject and publish no PNG/export even when a raw byte scan would not, then reject missing/extra/reordered/substituted/tampered durable capture attestations on reopen. Mock only GitHub/API and the test transport response.
- [ ] 5.2 Prove independent approvals and PROOF pass/fail/missing triage; reject cause self-links/cycles/alias chains/multiple or nonterminal roots; simulate failures before save, readiness, session, command, terminal, and post-battle persistence so each partial observation sources its canonical repair then a fresh complete final passes; reject unavailable finals, invalid failures/ranks/relationships/identity/caps/manifests, and nondurable consumers.
- [ ] 5.3 Run focused Jest, Node 22 nonincremental typecheck, lint, format check, strict OpenSpec/QC validation, and applicable viewport/runner smoke checks; keep each product PR below 15 files/500 changed lines.

## 6. Receipt Handoff — PR PROOF-6

- [ ] 6.1 Produce and validate a reviewed-head `camp-proof` receipt with exact spec/product provenance, then obtain independent review and merge with a SHA guard.
- [ ] 6.2 Produce and validate a fresh exact-main `camp-proof` receipt, export/reopen it, publish/revalidate cleanup, prune only the creation-bound branch/worktrees, and block PROOF-02 until every cleanup predicate is true.
