# Tasks: add-subsystem-lanes-and-ci

> Execution is **Codex-first and strictly sequential** — parallel agent fan-out has failed on this repo. One group at a time; each lands with its verification before the next. Claude orchestrates, packages context, and final-reviews. Pre-commit runs a full `next build` (~3–5 min) — commit via background shell. **Hard ordering gate: implementation of this change starts only after the `add-viewport-layout-sweep` (W5) IMPLEMENTATION merges to main** (ladder sequencing); mechanically, groups 2–6 depend only on merged W1/W2/W3 surface, group 7 carries its own W4 gate check (task 7.0), and group 8 its own W5 gate check (task 8.0) — both STOP on absence, never stub (design D9). **Non-goals are load-bearing: NO branch-protection changes (the required-context flip is a manual GitHub settings/API action by a human admin — documented in 5.1, deliberately never performed; the required-context list stays exactly `Lint and Test` + the three `Build Test / *` names); NO renaming or repurposing of any existing job or check name (`E2E Smoke` keeps its exact scope; the 5.3 aggregator hardening keeps `lint-and-test`'s job id, check name, and `needs` shape byte-stable — soundness, not repurposing); NO PR-gating of anything beyond the three seam anchors (mini-journeys, lanes, packs, flow audits gate nothing); NO new Playwright projects and no project selecting `@subsystem:` tags; NO edits to the `add-scenario-packs` or `add-viewport-layout-sweep` change folders, to anchor specs' assertions, to `mekstation-qc-registry.json`, or to `run-qc-lanes.mjs`/`select-qc-lanes.mjs` filter keys; NO vocabulary growth (six tags, closed); NO docblock-comment tags as selection anywhere; NO store injection, `test.skip`-on-missing-data, or golden traces in the mini-journeys; the seam-anchor invocation carries an explicit `--retries=0` — the config's CI default is `retries: 2` (`playwright.config.ts:46`; GitHub runners set `CI=true`), so retry-freedom must be forced, never assumed inherited (flake is an anchor bug, never masked); NO claiming the gate blocks without the 5.5 red-path proof (a failed fan-out job skips the unhardened aggregator, and a skipped required check counts as passing — verified, design R10).** File paths and line numbers below were verified on `claude/subsystem-lanes-and-ci@6e0ed7868` — re-verify against the then-current tree before editing (the W4 and W5 implementations will have landed in between; both touch surfaces named here). After any spec-delta edit: formatter, then `npx openspec validate add-subsystem-lanes-and-ci --strict` (design R1). Ledger registration ships with this declarative PR, not with these implementation tasks. Estimated total: ~2–3 dev-days.

## 1. Ground-truth re-verification (read-only)

- [x] 1.1 Re-verify the PR-gate surface and record the pre-change baseline: the full check-run name inventory (22 job blocks → 29 checks: unit-test-shards ×6, build-test ×3) from `.github/workflows/pr-checks.yml`; the `lint-and-test` aggregator's `needs` array (17 entries, lines ~573-599) and its comment; the `E2E Smoke` job's exact shape (lines ~476-531: `needs: [detect-changes, install-deps]`, the per-step `if: needs.detect-changes.outputs.code == 'true' || needs.detect-changes.outputs.e2e == 'true'` guards, browser-cache steps, failure-only report upload); and the live branch-protection snapshot via `gh api repos/<owner>/MekStation/branches/main/protection` (expected: exactly 4 required contexts — `Lint and Test`, `Build Test / win|mac|linux`; record the verbatim JSON as the rollback reference for 5.1's documentation). Also record the aggregator's failure-path facts for group 5: `lint-and-test` carries no `if:` condition and NO job in the workflow carries a job-level `if:` (per-step guards only — 5.3's strict every-result-`success` assertion depends on this; if a job-level `if:` has appeared on a needed job, STOP and resolve its result policy before 5.3); the skipped-aggregator evidence (run 29059132882: `Unit Tests (4/6)` concluded `failure`, `Lint and Test` concluded `skipped` — the "before" half of 5.3's hardening rationale); and the CI retry default 5.1's `--retries=0` overrides (`playwright.config.ts:46` — `retries: process.env.CI ? 2 : 0`).
  - Files: read-only.
  - Acceptance: a written baseline note with the 29 check names, the aggregator `needs` list, the guard expressions verbatim, the protection snapshot, the no-job-level-`if` verdict, the skipped-aggregator run reference, and the retry-default line — this is the "before" half of group 5's proofs.
  - Baseline (2026-08-21, verified on `main@83675c9a9`): 22 job blocks in `pr-checks.yml` producing 29 checks — Detect Changes, Install Dependencies, Lint, Format Check, Type Check, Unit Tests (1/6)…(6/6), Perf Smoke Tests, Statistical Proof PR, Perf Budget PR, Coverage Floor, A11y Tests, Validate BV Parity, Validate Combat Suite, Build Storybook, Schema Bridge, Determinism Audit, E2E Smoke, Desktop Type Check, Desktop Tests, Lint and Test, Prepare Build Artifacts, Build Test / {win,mac,linux}. Aggregator `lint-and-test`: flow-style `needs` of 17 entries `[lint, format-check, typecheck, unit-test-shards, perf-smoke-tests, statistical-proof-pr, perf-budget-pr, coverage-floor, a11y-tests, validate-bv, validate-combat, storybook-build, schema-bridge, determinism-audit, e2e-smoke, desktop-typecheck, desktop-tests]`, `runs-on: ubuntu-latest`, `timeout-minutes: 1`, echo step, NO `if:`. Job-level `if:` count across the whole workflow = 0 (grep) — 5.3's strict every-result-`success` assertion is safe. `E2E Smoke`: `needs: [detect-changes, install-deps]`; per-step guard verbatim `if: needs.detect-changes.outputs.code == 'true' || needs.detect-changes.outputs.e2e == 'true'` (with the `!=`/`&&` inverse on the no-op step) at lines 458/462/466/472 — the task's ~476-531 cite drifted, the shape is unchanged. Branch protection (gh api, live): required contexts exactly `["Lint and Test","Build Test / win","Build Test / mac","Build Test / linux"]`, `strict: true`, `enforce_admins: true`, `required_approving_review_count: 0`, `dismiss_stale_reviews: true` — the 5.1 rollback reference. Skipped-aggregator evidence re-fetched live: run 29059132882 shows `Unit Tests (4/6): failure` and `Lint and Test: skipped`. Retry default: `retries: process.env.CI ? 2 : 0` now at `playwright.config.ts:99` (the :46 cite drifted).
- [x] 1.2 Re-verify the nightly surface: job ids/names and `needs` wiring in `.github/workflows/nightly-validation.yml` (`install-deps`, `full-statistical-proofs`, `e2e-full`, `coverage`, `notify-on-failure` with `needs: [full-statistical-proofs, e2e-full, coverage]` + `if: failure()`); whether the W4 implementation's `scenario-pack-recapture` job has landed (design R5 — record which state the tree is in); the current red/green state of the last 3 scheduled runs and the failing-spec set (the 8 pre-existing reds are context for lane triage, not this change's scope).
  - Files: read-only.
  - Acceptance: a written note naming the job anchors group 6 will edit and the recapture-job presence verdict.
  - Baseline (2026-08-21): `nightly-validation.yml` job anchors — `install-deps` (:38), `full-statistical-proofs` (:65), `e2e-full` (:132), `coverage` (:195), `scenario-pack-recapture` (:249, `needs: install-deps`), `notify-on-failure` (:293) with `needs: [full-statistical-proofs, e2e-full, coverage, scenario-pack-recapture]` + `if: failure()`. R5 verdict: the W4 `scenario-pack-recapture` job HAS landed and is already unioned into `notify-on-failure.needs` — 6.1's append is a second union member, not the first. Last 3 scheduled runs ALL `failure`: 32354845862 (08-20), 32238524072 (08-19), 32122525924 (08-18); in the latest every job is green except `Full E2E Suite (chromium)` — 781 passed / 8 failed / 11 skipped in 43.8m. Failing set (08-20): `campaign-flow.spec.ts:710`, `campaign-organic-contract.spec.ts:171`, `campaign-round-trip.spec.ts:57`, `encounter-combat-continuity.spec.ts:967` and `:1084`, `integration.spec.ts:332`, `playable-command-feature-screens.spec.ts:65`, `playtest-coop-route-smoke.spec.ts:154`. The 08-18 run failed the same set plus `campaign-subroute-recovery.spec.ts:402` (9 → 8; that one recovered). Stable pre-existing reds — context for lane triage per this task's own scoping note, not this change's scope.
- [x] 1.3 Re-verify the anchor and invariant surfaces: the three anchor specs exist unmodified (`e2e/active-session-recovery.spec.ts`, `e2e/seam-roster-materialization-handoff.spec.ts`, `e2e/seam-fresh-construction-no-instant-defeat.spec.ts`); the two invariant jest suites (`src/engine/__tests__/InteractiveSession.recovery.test.ts`, `src/__tests__/integration/campaignMissionEncounterLaunchIntegrity.test.ts`) are matched by the unit project and NOT by any exclusion pattern (`jest.config.js` — the perf-sensitive pattern names only `src/simulation/__tests__` files); run them once (`npx jest --selectProjects unit --ci --runTestsByPath <both>`) and record the timing; the current Playwright topology (pre- or post-W5 — record which; the anchors must list under chromium only either way, `npx playwright test --list <anchor spec>`).
  - Files: read-only.
  - Acceptance: a written note with the invariant-lane verdict, the jest timing, and the anchors' `--list` proof.
  - Baseline (2026-08-21): all three anchor specs present at their canonical paths. Both invariant suites present and matched by the unit project — `jest.config.js` exclusions are the a11y pattern (`\.a11y\.test\.(ts|tsx)$`, :58) and the env-gated perf-sensitive simulation pattern (`src/simulation/__tests__` files only, :61-62); neither matches. Run: `npx jest --selectProjects unit --ci --runTestsByPath` on both suites = 7 passed / 7 total in 27.5s. Topology is post-W5 tag-then-scope: `playwright.config.ts` registers `chromium` (:169), `mobile-touch` (:216), env-gated `flow-audit` (:247) plus W4's env-gated `scenario-pack-mint` (:267). Anchors' `--list` proof: 5 tests in 3 files, all `[chromium]` only.
- [x] 1.4 Re-verify the subsystem surfaces the mini-journeys and ledger cite: the three stub specs and their seeding/testids (`e2e/playtest-subsystem-loans.spec.ts` — `take-loan-form`/`loan-submit` targeting `src/components/campaign/command/FinancesPanel.tsx`; `e2e/playtest-subsystem-hiring.spec.ts` — `seedHiringHall` from `e2e/helpers/campaignSeeders.ts`, `hiring-panel-grid`, `candidate-hire-{id}`; `e2e/playtest-subsystem-contracts.spec.ts` — the accept affordance's actual wiring); the store exposure surface (`src/lib/e2e/storeExposure.ts` — which stores ride `window.__ZUSTAND_STORES__`; `e2e/helpers/store.ts` readers); `PilotAbilitiesPanel` mounts (`src/pages/gameplay/pilots/create.tsx`, `src/components/campaign/personnel/PersonnelSidePanel.tsx`) and testids (`add-ability-btn`, `owned-spa-{id}`); **the spendable-XP front-door verdict per design D6's ordered decision rule** (create-page starting XP → seeded campaign personnel → STOP-and-report if neither yields an affordable purchase); the catalog/validator anchors (`docs/qc/mekstation-journey-scenarios.json` 7 entries; `journey-qc-core.mjs:51-52` allowed-Sets, `:357` `validateJourneyCatalog`; the query command's option parsing); `FlowSubsystem` literals (`e2e/flows/manifest.ts:25-31`); the legacy title-tag vocabulary (`playwright.config.ts:14-32`) and the native-tag precedent (`e2e/playtest-sp-smoke.spec.ts:287`).
  - Files: read-only.
  - Acceptance: a written note with per-surface verdicts, including the XP start-state decision (which mount, which precondition mechanism) that task 4.4 implements.
  - Baseline (2026-08-21) per-surface verdicts:
    - loans: spec drives `take-loan-form`/`loan-submit` (spec :31/:36); testids live at `FinancesPanel.tsx:229`/`:288`. VERIFIED.
    - hiring: `seedHiringHall` imported from `e2e/helpers/campaignSeeders` (spec :20, used :39); `hiring-panel-grid` per the spec docblock (:10/:52). VERIFIED.
    - contracts: spec present and documents the accept wire-up (docblock :7); the accept affordance's live semantics are 4.3's implementation-time verification.
    - store exposure: `window.__ZUSTAND_STORES__` (`storeExposure.ts:33-51`) carries campaign (StoreApi via the lazy wrapper — PT-004), campaignPersistence, campaignRoster, force, pilot, encounter, gameplay, quickGame, repair, award, tabManager; plus `__AEROSPACE_REGISTRY__`/`__VEHICLE_REGISTRY__`/`__UNIT_REGISTRY__`, `__UNIT_TEMPLATES__`, `__EVENT_STORE__`.
    - `PilotAbilitiesPanel` mounts — DRIFT: `/gameplay/pilots/create` does NOT mount the panel directly; the wizard redirects on success to `/gameplay/pilots/{id}?creating=1`, where the DETAIL page's `PilotAbilitiesPanel` opens in creation flow (`create.tsx:26-38`). `PersonnelSidePanel.tsx:20` mounts it directly. Testids confirmed: `add-ability-btn` (`PilotAbilitiesPanel.tsx:306`), `owned-spa-${spa.id}` (:109).
    - Spendable-XP front-door verdict (D6 ordered rule): path (1) VIABLE — no STOP. `availableXp = pilot.career?.xp ?? 0` (:192) with the insufficient-XP guard at :219-221; flaws with negative `xpCost` GRANT XP front-door (:235-237). Catalog: cheapest positive SPA `xpCost` = 15; flaws exist at -15 (`unofficialSPAs.ts:49`) and -25 (`miscAndInfantrySPAs.ts:113`). Even a 0-XP fresh creation therefore reaches spendable XP ≥ the cheapest SPA entirely through the UI (add a flaw, then purchase); whether creation itself grants nonzero starting XP is left to 4.4's implementation-time recording — the flaw-grant mechanism makes the front-door path deterministic either way. Chosen mount for 4.4: the create-flow redirect (`?creating=1`) as the true front door; `PersonnelSidePanel` over a seeded campaign is the fallback.
    - catalog/validator anchors — DRIFT (modularization): the allowed-sets now live at `journey-qc-catalog-validator.mjs:1-2` (`allowedTiers`/`allowedModes`; core keeps its own run-tier copy at `journey-qc-core.mjs:50`) and `validateJourneyCatalog` at `journey-qc-catalog-validator.mjs:264`, re-exported via `journey-qc-core.mjs:397` (import :5). 2.2's real edit targets the catalog-validator module plus `parseArgs` (`journey-qc-core.mjs:133`). Catalog: 7 entries (character-build, mek-build, combat-1v1, combat-4v4, contract-campaign, campaign-short, campaign-long).
    - `FlowSubsystem` literals VERIFIED at `e2e/flows/manifest.ts:25-31` (navigation, combat, economy, maintenance, personnel, experience — six, closed).
    - title-tag vocabulary: the tag docs comment moved (now ~`playwright.config.ts:44-48`; the :14-32 cite drifted). Native-tag precedent VERIFIED at `playtest-sp-smoke.spec.ts:287` (`test.describe(..., { tag: [...] }, ...)`).

## 2. Journey-catalog subsystem facet + query mode

- [x] 2.1 Add the facet data: each of the 7 entries in `docs/qc/mekstation-journey-scenarios.json` gains `subsystems` per design D4's seed mapping (`character-build` → `["personnel","experience"]`, `mek-build` → `[]` with the construction-taxonomy rationale in the entry's existing notes convention, `combat-1v1`/`combat-4v4` → `["combat"]`, `contract-campaign` → `["navigation","economy","combat"]`, `campaign-short` → `["navigation","economy","personnel"]`, `campaign-long` → `["navigation","economy","personnel","maintenance","experience"]`) — finalize each against the journey's own step list and record any deviation from the seed mapping.
  - Files: `docs/qc/mekstation-journey-scenarios.json`.
  - Acceptance: catalog parses; the mapping-deviation note (possibly empty) recorded.
  - Receipt (2026-08-21): all 7 entries carry `subsystems` exactly per the D4 seed mapping — no deviations. Finalization notes: contract-campaign keeps `navigation` (contract selection rides the mission/starmap surface; the step list itself names no navigation-specific step); campaign-short keeps `personnel` (the resolution pipeline applies pilot effects; its steps are contract/economy-named). mek-build's `[]` rationale rides its `knownLimitations` entry (the entry's notes convention — no dedicated notes field exists). `qc:journeys:validate` green: `catalog=7 journeys graph=259 nodes errors=0 warnings=0`.
- [x] 2.2 Validate the facet: add `allowedSubsystems` (a Set of the six literals beside `allowedModes`/`allowedTiers`, `journey-qc-core.mjs:51-52`) and extend `validateJourneyCatalog` (`:357`) to reject a missing `subsystems` field, a non-array, an unknown tag, or a duplicate — naming journey id and offending value in the existing issue format. Extend the graph query command with `--subsystem=<tag>`: validate against `allowedSubsystems` (unknown tag fails naming the six allowed values), resolve matching journeys by the catalog facet at query time, and return their existing graph joins. NO field added to `mekstation-qc-validation-graph.json` and NO edit to `mekstation-qc-registry.json` or the lane scripts (design D4.4).
  - Files: `scripts/qc/journey-qc-core.mjs` (+ the query entry script if option parsing lives there).
  - Acceptance: `npm run qc:journeys:validate` green; a mutation test (unknown tag planted in the catalog → validator fails naming it; prove once, revert); `--subsystem=economy` returns the expected journeys; `--subsystem=bogus` fails naming the vocabulary.
  - Receipt (2026-08-21): `allowedSubsystems` lives in `journey-qc-catalog-validator.mjs` (exported, beside `allowedTiers`/`allowedModes` — the task's `journey-qc-core.mjs:51-52` cite predates the validator modularization recorded in 1.4); `validateJourney` rejects missing/non-array, unknown tag, and duplicate in the existing issue format. Mutation proven: planting `warcrimes` on combat-1v1 fails with `ERROR: combat-1v1: invalid subsystem tag warcrimes.` (reverted). Query: `parseArgs` gains the `subsystem` value handler (`journey-qc-core.mjs`); `resolveSubsystemJourneyNodeIds(catalog, tag)` resolves the facet at query time and `queryGraph` gains an exact-id `matchIds` path; `--subsystem=economy` matches exactly `journey:contract-campaign`, `journey:campaign-short`, `journey:campaign-long` (+ their existing joins); `--subsystem=bogus` exits 1 with `Unknown subsystem tag bogus. Allowed: navigation, combat, economy, maintenance, personnel, experience.` No field added to `mekstation-qc-validation-graph.json`; no edit to `mekstation-qc-registry.json` or the lane scripts (the commit's file set is the proof: catalog, catalog-validator, core, query entry, manifest, guard test).
- [x] 2.3 Vocabulary guard: a jest test in `scripts/__tests__/` asserting the `.mjs` `allowedSubsystems` set equals the TS `FlowSubsystem` literal union (import `e2e/flows/manifest.ts` from the test only) and that the `@subsystem:` tag literals used by group 4/6 derive as `'@subsystem:' + <tag>` from the same set. Update `scripts/__tests__/journey-qc.test.ts` fixtures for the new required field.
  - Files: `scripts/__tests__/` (new guard + fixture updates).
  - Acceptance: guard green and runs under `npx jest --selectProjects unit` (PR-lane membership proven via `--listTests`); removing a literal from either side fails the guard (prove once, revert).
  - Receipt (2026-08-21): `scripts/__tests__/subsystem-vocabulary.test.ts` reads the .mjs set through the established spawn-harness ESM-import pattern and compares against `FLOW_SUBSYSTEMS` — a runtime const added to `e2e/flows/manifest.ts` type-preservingly (`FlowSubsystem` is now derived `(typeof FLOW_SUBSYSTEMS)[number]`, literal-for-literal identical); the `@subsystem:` literals are asserted as pure derivations of the same set. `npx jest --selectProjects unit --listTests` matches the suite (PR-lane membership). Kill proofs: removing `'maintenance'` from the .mjs set fails the guard (1 failed, reverted); removing it from `FLOW_SUBSYSTEMS` fails the guard (proven post-commit, reverted). `journey-qc.test.ts` needed no fixture edits — its fixtures copy the real catalog, which now carries the field; both suites green together (34 tests).

## 3. Subsystem coverage ledger (the honest replacement for the 19-spec fiction)

- [x] 3.1 Author `docs/qc/mekstation-subsystem-coverage.json`: one row per fine-grained subsystem across the full 19-id set (hiring, XP/leveling, medical, salvage, repair, refit, loans, contract market, contract negotiation, faction reputation, random events, morale, force hierarchy, turnover, aging, awards, rank+pay, unit market, maintenance), each with `id`, `facet` (one of the six tags), `coveringSpecs` (paths; empty only for `deferred`), `status` (`asserting` | `render-only` | `deferred`), and `followUpRef` (mandatory for `deferred` — the Wave-6.1 Track-B/C provenance, `openspec/changes/archive/2026-05-20-add-subsystem-validation-specs/tasks.md`, or a successor ref). Statuses ship truthful to the then-current tree: hiring/loans/contract-market/XP-leveling flip to `asserting` only when group 4's specs land (author this row set with group-4 foresight but validate against reality at each commit); awards and unit-market rows reflect the re-verified surfaces (an `e2e/awards.spec.ts` exists; the acquisitions route exists) rather than the stale 2026-05-20 BLOCKED ratings — record each drift correction.
  - Files: `docs/qc/mekstation-subsystem-coverage.json` (new).
  - Acceptance: 19 rows, six-tag facets, every non-deferred row's spec paths exist on disk; drift corrections recorded.
  - Receipt (2026-08-21): 19 rows shipped. Drift corrections vs the 2026-05-20 audit: **awards** BLOCKED → `asserting` (`e2e/awards.spec.ts` drives the award engine's primary actions — stat recording, auto-grant, direct grant — and asserts observable state change through the exposed award store across 13 tests; the award UI component family now exists under `src/components/award/`; nuance recorded: the assertions are store-observable, not rendered-surface); **unit-market** BLOCKED → `asserting` (`/gameplay/campaigns/[id]/acquisitions` route exists; `e2e/campaign-acquisition-browser.spec.ts` drives add-request → advance-day → delivery with UI testid assertions and reload persistence). hiring/loans/contract-market ship `render-only` (their stub specs render the primary output; group 4 flips them). xp-leveling `deferred` with followUpRef → task 4.4. Facet finalizations grounded in source: faction-reputation → economy (standing feeds contract offer generation — `contractMarket.ts` `factionStandingMod`); random-events → personnel (life events act on pilots — `lifeEvents.ts`). Native `@subsystem:` tags added to the five covering specs here (the validator's linkage rule requires them; this pre-satisfies the tag half of 4.1-4.3).
- [x] 3.2 Author `scripts/qc/validate-subsystem-coverage.mjs` + its jest wrapper in `scripts/__tests__/`: fail on missing/extra/duplicate rows vs the 19-id set, invalid facet/status, dangling `coveringSpecs` path, a non-deferred row whose named spec file does not contain the row's `@subsystem:<facet>` tag literal, or a `deferred` row without `followUpRef`. Include the group-6 lane cross-check (design D8): parse `nightly-validation.yml`'s subsystem-lane matrix list and fail when a listed tag has no tagged covering spec or a tag with tagged non-deferred rows has no lane (tolerate the job being absent entirely until group 6 lands — the check activates when the job id exists).
  - Files: `scripts/qc/validate-subsystem-coverage.mjs` (new), `scripts/__tests__/subsystem-coverage.test.ts` (new).
  - Acceptance: validator green on 3.1's ledger; each failure class proven by one deliberate mutation naming the row/tag (prove, revert); wrapper listed by `npx jest --selectProjects unit --listTests` (the PR-lane enforcement the old requirement never had).
  - Receipt (2026-08-21): validator core is a pure function over injected probes (`specExists`/`specContains`/`nightlyMatrixTags`); the CLI wires real fs + the nightly-matrix parse (`parseNightlyMatrixTags` returns null while the `subsystem-lanes` job id is absent — the D8 cross-check ships dormant and activates when group 6 lands the job). Every failure class is pinned PERMANENTLY in `scripts/__tests__/subsystem-coverage.test.ts` as in-memory mutations naming the row/tag (missing row `morale`, unknown row `warcrimes`, duplicate row, invalid facet `vibes` + status `aspirational`, dangling path, missing `@subsystem:experience` tag, deferred without followUpRef, dormant-lane no-op, and BOTH cross-check directions via a simulated `['combat']` matrix) — stronger than prove-once-revert: the proofs re-run in every PR's unit shards. CLI green on the real tree: `[qc:subsystem-coverage] rows=19 lanes=dormant errors=0`. Wrapper listed by `--listTests`.
- [x] 3.3 Demote the stale matrix: add a dated header note to `playtest/phase-6/SUBSYSTEM_UI_AUDIT.md` naming `docs/qc/mekstation-subsystem-coverage.json` as the living coverage source and this audit as a 2026-05-20 snapshot (do not rewrite its rows — point-in-time evidence; the full re-audit is a recorded follow-up).
  - Files: `playtest/phase-6/SUBSYSTEM_UI_AUDIT.md` (header only).
  - Acceptance: header present; no row content edited.
  - Receipt (2026-08-21): dated blockquote header added under the title naming the ledger as the living source, the 2026-05-20 snapshot status, the known drift (awards, unit market), and the re-audit follow-up; zero row edits (diff touches the header only).

## 4. Write-flow mini-journeys + native tags

- [x] 4.1 Extend `e2e/playtest-subsystem-loans.spec.ts` in place (design D6): keep the render assertion, then drive the write flow — fill `take-loan-form`, click `loan-submit`, hard-assert the loan row renders (`loan-row-*`), the campaign balance is credited by exactly the entered principal, and a `LoanDisbursement`-typed transaction posts with `dailyRepayment` recorded (store reads via `e2e/helpers/store.ts` over the exposed campaign store — never a private `page.evaluate` store write). Tag the describe natively: `test.describe(title, { tag: ['@subsystem:economy'] }, …)`. Update the docblock to describe what the spec now actually does.
  - Files: `e2e/playtest-subsystem-loans.spec.ts`.
  - Acceptance: green 3× at `--project=chromium --workers=1 --retries=0` (explicit — local runs inherit 0 anyway, but the evidence claim must not depend on the environment; design R4); the assertions fail when the submit click is removed (prove once, revert); no `test.skip` guards; no `@smoke` tag.
  - Receipt (2026-08-21): write flow drives `loan-input-principal` (250,000) -> `loan-submit`; asserts `loan-row-*` renders, balance credited exactly `principal * 100` cents, a `loan_disbursement` transaction posts for the principal, and the appended loan carries positive `dailyRepayment`. Store reads via `getStoreState` over the exposed campaign store (Money serializes as `{cents}` through page-evaluate). Native `@subsystem:economy` tag (landed in group 3); docblock rewritten. 3x green first-attempt at `--project=chromium --workers=1 --retries=0` (4-spec battery, runs 11.1s/10.8s/11.1s, zero flaky); submit-click removal killed the spec (proven in the 4-mutant battery, restored from byte-copies — not git checkout, the group-2 lesson).
- [x] 4.2 Extend `e2e/playtest-subsystem-hiring.spec.ts` in place: after its existing `seedHiringHall` pre-hire seed, click `candidate-hire-{id}` and hard-assert the roster gained exactly one entry with the hired candidate's id (roster store read), the ledger was debited the hire cost (cross-subsystem propagation), and the UI feedback surface renders. The W4 personnel pack is NOT consumed (post-hire genesis — design D6's recorded rejection). Tag `@subsystem:personnel`; fix the overclaiming docblock.
  - Files: `e2e/playtest-subsystem-hiring.spec.ts`.
  - Acceptance: green 3×; removing the hire click fails the new assertions (prove once, revert).
  - Receipt (2026-08-21): after the existing `seedHiringHall` pre-hire seed, clicks `candidate-hire-hire-offer-test-1`; asserts roster +1 with `hired-hire-offer-test-1` present (campaignRoster store), ledger debited exactly the hire cost (5,000 -> 500,000 cents; cross-subsystem propagation), and the candidate card leaving the grid (UI feedback). W4 personnel pack NOT consumed (post-hire genesis, D6 rejection stands). 3x green; hire-click removal killed it.
- [x] 4.3 Extend `e2e/playtest-subsystem-contracts.spec.ts` in place: drive the accept affordance on a rendered offer card and hard-assert the observable accept semantics verified in 1.4 (contract status transition in the store + its downstream ledger/mission surface). Tag `@subsystem:economy`; fix the docblock.
  - Files: `e2e/playtest-subsystem-contracts.spec.ts`.
  - Acceptance: green 3×; accept-click removal fails (prove once, revert).
  - Receipt (2026-08-21): accept semantics verified at implementation: `acceptContractOffer` routes through `acceptContract` which sets `MissionStatus.ACTIVE` (`'Active'`) on the contract and lands it in `campaign.missions` (a Map — read via a read-only page evaluate, since Maps do not survive JSON serialization; the D6 store-read obligation is met read-only). Spec accepts the first auto-seeded offer and asserts the status transition, the offer leaving the market pool, and the card leaving the grid. 3x green; accept-click removal killed it.
- [x] 4.4 Author `e2e/playtest-subsystem-xp-purchase.spec.ts` (new — the genuinely zero-e2e hole): establish the spendable-XP pilot through the front-door path 1.4 verified (create page or seeded campaign personnel panel; the pack upgrade is 7.2's), open the abilities editor via `add-ability-btn`, select an affordable SPA, confirm, and hard-assert `owned-spa-{id}` renders AND the pilot's XP decremented by exactly the ability's cost (pilot store read — the same math `spaAcquisition.test.ts` proves headlessly). Record the chosen start-state path + evidence in the spec header. Tag `@subsystem:experience`.
  - Files: `e2e/playtest-subsystem-xp-purchase.spec.ts` (new).
  - Acceptance: green 3× at `--project=chromium --workers=1 --retries=0` (explicit — local runs inherit 0 anyway, but the evidence claim must not depend on the environment; design R4); XP-delta assertion fails if the cost constant is perturbed in the assertion (sanity-prove once, revert); no store injection (grep: no `page.evaluate` writes to stores), no `test.skip`.
  - Receipt (2026-08-21): start-state = D6 path 1, recorded in the spec header: the creation wizard redirects to `/gameplay/pilots/{id}?creating=1` where the detail page's `PilotAbilitiesPanel` opens in creation flow. Fresh creation grants 0 XP (verified live), so the spec first takes the flaw `atow_combat_paralysis` (+25 XP) THROUGH THE PICKER — a front-door XP grant, not store injection — then purchases `foot_cav` (15) and asserts XP decremented by exactly the cost, `owned-spa-foot_cav` renders, and repeat purchase is not offered (picker excludes owned ids). Implementation traps recorded: `waitForURL(/\/gameplay\/pilots\/[^/?#]+/)` matches the `/create` route itself — the negative lookahead `(?!create)` is required; picker items select via their inner `Select` button, not the container. 3x green; perturbing the cost constant (15 -> 16) killed it. The only `page.evaluate` reads state (readPilotXp via getStoreState).
- [x] 4.5 Flip the four ledger rows to `asserting` (hiring, loans, contract market, XP/leveling) with the specs' paths; add the parameterized local lane script to `package.json`: `"qc:subsystem-lane": "node scripts/playwright/run-playwright.mjs test --project=chromium --workers=1 --grep"` (invoked `npm run qc:subsystem-lane -- "@subsystem:economy"`). NOT added to `verify:qc`'s chain or any workflow.
  - Files: `docs/qc/mekstation-subsystem-coverage.json`, `package.json`.
  - Acceptance: 3.2's validator green with the flipped rows; `npm run qc:subsystem-lane -- "@subsystem:economy"` runs exactly the economy-tagged specs (recorded `--list` proof); grep proves the script is referenced by no workflow and no `verify:qc` chain.
  - Receipt (2026-08-21): four rows flipped to `asserting` with their spec paths (xp-leveling's followUpRef dropped — no longer deferred); coverage validator green post-flip (`rows=19 lanes=dormant errors=0`; the in-lane wrapper re-proves it, 10/10). `qc:subsystem-lane` added to package.json; `npm run qc:subsystem-lane -- "@subsystem:economy" --list` selects exactly 3 tests in 3 files (loans, contracts, acquisition-browser — the economy-tagged set; NOTE: npm forwards args after a single `--`, a second `--` breaks the grep). Grep proof: zero references from `.github/workflows/` and zero from the `verify:qc` chain.

## 5. The per-PR seam-anchor gate (workflow group — proofs are the acceptance; branch protection is explicitly OUT)

- [ ] 5.1 Add the `seam-anchors` job (`name: Seam Anchors`) to `.github/workflows/pr-checks.yml`, mirroring the `E2E Smoke` job byte-for-byte in shape (per 1.1's verbatim baseline): `needs: [detect-changes, install-deps]`, `timeout-minutes: 20`, the identical per-step `code`/`e2e` guard expressions (design R9 — copied, not paraphrased), the browser cache steps, and ONE invocation — `npx playwright test e2e/active-session-recovery.spec.ts e2e/seam-roster-materialization-handoff.spec.ts e2e/seam-fresh-construction-no-instant-defeat.spec.ts --project=chromium --retries=0` (the explicit `--retries=0` overrides the config's CI-default `retries: 2` recorded in 1.1 — a gate must never retry a flaky anchor to green; design R4) — plus a failure-only report upload under its own artifact name. **Do NOT add it to `lint-and-test.needs` in this task** (design D2.4's staged landing: advisory → burn-in → harden → wire → red-path proof). **Explicitly excluded, documented in the PR description: no branch-protection change accompanies this group** — designating a check as independently required is a manual GitHub settings/API action by a human admin (`Settings → Branches → main` or `gh api --method PUT …/branches/main/protection`), with rollback = restoring the 4-context list recorded in 1.1; this change relies on aggregator membership instead and never performs the flip. Add the one-sentence note to `.github/BRANCH_PROTECTION.md` documenting that the seam gate rides the `Lint and Test` aggregator.
  - Files: `.github/workflows/pr-checks.yml`, `.github/BRANCH_PROTECTION.md`.
  - Acceptance: **the before/after check-name diff proof** — the full check-run name list from 1.1 (29 names) vs the post-change list (30): every pre-existing name byte-identical, `Seam Anchors` the only addition, `lint-and-test.needs` unchanged at 17 entries in this commit; the invocation line carries `--retries=0` (grep proof); workflow YAML parses; recorded in the PR description.
- [ ] 5.2 Burn-in (design D2.4/D3, STOP on failure): observe the advisory job on ≥3 real code-touching PR runs (this change's own implementation PRs qualify — the workflow-file edit sets `code=true`) plus one docs-only PR (or dispatch equivalent) proving the no-op path completes in seconds with the check green. **Green means first-attempt green**: each run's Playwright report must record zero `flaky`/retried results (with `--retries=0` in place a retry-pass is structurally impossible — the report check catches the flag being dropped). Record each run's job wall-clock against design D3's thresholds: ≤6.5 min → proceed to 5.4; 6.5–10 min → proceed only with the measured workflow-total regression recorded and an anchor-perf follow-up filed; >10 min or ANY flake → STOP — do not wire 5.4, file the anchor stability/perf finding (journey-qc's 3×-green obligation owns the fix), and report (the job stays advisory; the blocking step becomes a follow-up decision).
  - Files: read-only (evidence gathering).
  - Acceptance: a written burn-in table (run URL, trigger class, job wall, flaky-result count = 0, verdict) in the PR description; the D3 threshold verdict named.
- [ ] 5.3 Harden the aggregator, name-stably (the soundness precondition for 5.4 — design D2.3/R10): add `if: always()` to the `lint-and-test` job and replace its echo step with an explicit result assertion over `${{ toJSON(needs) }}` that fails — printing every non-success job id — unless every needed job's `result` is `success` (strictness is safe per 1.1's no-job-level-`if` verdict: `skipped`/`cancelled`/`failure` results are all defects). Keep the job id, `name: Lint and Test`, and the flow-style `needs: [...]` array byte-identical (the name is the branch-protection currency; `parseAggregatorNeeds` in `scripts/qc/validate-openspec-ci-quality.mjs` regex-reads that array shape); update the job's comment to state it always runs and fails on any non-success need. Extend 5.1's `.github/BRANCH_PROTECTION.md` note: the aggregator runs unconditionally and asserts every fan-out result, because a `needs`-skipped required check counts as passing (the 1.1-recorded run-29059132882 hole). Without this task, 5.4 would wire the seam gate into a blocking mechanism whose failure path is fictional — and the hole it closes covers all 17 pre-existing needs, not just the new 18th.
  - Files: `.github/workflows/pr-checks.yml` (`lint-and-test` block), `.github/BRANCH_PROTECTION.md` (note extension).
  - Acceptance: workflow YAML parses; the check-name diff is unchanged (the hardening adds no check run and renames none); `node scripts/qc/validate-openspec-ci-quality.mjs` still green (`aggregatorNeeds` parse intact); the landing PR's own run shows `Lint and Test` green through the assertion step (the all-success path); the red path is proven live in 5.5, never simulated here.
- [ ] 5.4 Wire the gate and ratchet it (only after 5.2's burn-in verdict permits AND 5.3 has landed): add `seam-anchors` to `lint-and-test`'s `needs` array (17 → 18 — the `e2e-testing` "PR-Gated Coverage Floor" mechanism) AND append `'seam-anchors'` to `requiredAggregatorNeeds` in `scripts/qc/validate-openspec-ci-quality.mjs:32` (the existing per-PR wiring pin — a future refactor dropping the `needs` entry then fails `qc:openspec-ci:validate` instead of silently un-gating the anchors; design D2.3). Re-run the check-name diff (still 30 names, all pre-existing byte-identical); re-confirm branch protection untouched (`gh api` snapshot equals 1.1's). Blocking semantics are NOT claimed in this task — all evidence here is green-path; 5.5's red-path proof is the only evidence that the gate blocks.
  - Files: `.github/workflows/pr-checks.yml` (one line), `scripts/qc/validate-subsystem-coverage.mjs` untouched — the ratchet edit is in `scripts/qc/validate-openspec-ci-quality.mjs` (one line).
  - Acceptance: diff shows exactly the two one-line additions; `node scripts/qc/validate-openspec-ci-quality.mjs` reports `aggregatorNeeds=18/18 errors=0`; the post-merge protection snapshot is byte-identical to 1.1's; the burn-in evidence + the wiring are cross-referenced in the PR description.
- [ ] 5.5 Red-path proof of blocking authority (STOP on failure — design D2.4/R10; the spec's "anchor failure blocks the pull request" scenario is demonstrated, never assumed): from a throwaway branch, open a **draft** PR carrying a deliberate single-anchor failure (a temporary always-failing assertion appended to one anchor spec — the edit lives only on the never-merged draft branch; main's anchors stay untouched per the non-goals). Record, at that head SHA: (a) the `Seam Anchors` job concluded `failure` on first attempt (no retries — `--retries=0`); (b) `Lint and Test` concluded `failure`, NOT `skipped`, with the assertion step naming `seam-anchors` as the non-success need (the hardened aggregator ran); (c) `gh pr view --json mergeable,mergeStateStatus` reporting the PR un-mergeable (`mergeStateStatus: BLOCKED`). Close the draft unmerged and delete the branch. If ANY observation deviates — above all a mergeable merge box — STOP: revert 5.4's one-line `needs` entry (the gate returns to advisory), file the finding, and report; the gate is undelivered until a passing red-path run exists.
  - Files: read-only on main (the draft branch is discarded, never merged).
  - Acceptance: the recorded red-path table (draft PR URL, run URL, the three conclusions, the merge-state JSON) in the wiring PR's description; the draft closed unmerged; the STOP path never reached, or fully reported.

## 6. Nightly subsystem lanes

- [ ] 6.1 Add the `subsystem-lanes` matrix job to `.github/workflows/nightly-validation.yml` (anchors per 1.2): `strategy: { fail-fast: false, matrix: { subsystem: [economy, personnel, experience] } }` (the group-4 tenant tags — navigation/combat/maintenance join in 7.1), `name: Subsystem Lane (${{ matrix.subsystem }})`, `needs: install-deps`, the E2E-Smoke-pattern setup steps, then `npx playwright test --grep "@subsystem:${{ matrix.subsystem }}" --project=chromium`; append `subsystem-lanes` to `notify-on-failure.needs` (union with W4's recapture append if it landed first — design R5). NOT referenced by `pr-checks.yml` or any required check.
  - Files: `.github/workflows/nightly-validation.yml`.
  - Acceptance: YAML parses; `notify-on-failure.needs` includes the job; grep proves zero references from any PR workflow; one `workflow_dispatch` run recorded with per-lane check runs visible and each lane selecting ≥1 test.
- [ ] 6.2 Activate the lane cross-check in 3.2's validator (the workflow job id now exists): matrix list ↔ ledger tag tenancy in both directions.
  - Files: `scripts/qc/validate-subsystem-coverage.mjs` (activation only, if gated), else read-only.
  - Acceptance: validator green; removing a matrix tag (prove once, revert) and planting an untenanted tag (prove once, revert) each fail naming the tag.

## 7. Pack-adjacent work — GATED: after the W4 implementation merges

- [ ] 7.0 **Gate check (STOP on failure)**: verify on the current tree that the W4 implementation has landed — `e2e/helpers/scenarioPackLoading.ts` exports `loadCampaignPack`/`loadEncounterPack`, `e2e/scenario-packs/manifest.ts` exists with pilot-pack entries and their declared `subsystems`, and the parity specs exist under `e2e/scenario-packs/`. If any artifact is absent, STOP this group and report naming it — never tag non-existent specs, never hand-fake a pack, never bind 7.2 to a missing loader.
  - Files: read-only grep.
  - Acceptance: a written gate verdict naming the verified exports, manifest entries, and parity specs (or the STOP report).
- [ ] 7.1 Tag each pack parity spec with its manifest entry's subsystems (native `test.describe` tags, additive title-tag-free edits to W4-owned spec files post-merge — no manifest/loader/payload edits); extend 2.3's guard with the manifest↔tag equality assertion (spec scenario "Pack parity spec tags mirror the manifest"); extend 6.1's matrix with the newly tenanted tags (expected: navigation, combat, maintenance — exactly the tags whose only tenants are pack parity specs) and re-run 6.2's cross-check.
  - Files: `e2e/scenario-packs/*.parity.spec.ts` (tags only), `scripts/__tests__/` guard, `.github/workflows/nightly-validation.yml` (matrix list), `docs/qc/mekstation-subsystem-coverage.json` if any row's covering set grows.
  - Acceptance: guard green; a deliberate tag/manifest mismatch fails naming the pack (prove once, revert); one dispatch run shows the expanded lane set green (or red only on documented pre-existing failures).
- [ ] 7.2 Evaluate the experience-pack start-state upgrade for the XP mini-journey (design D6 path 3): load the experience pack once and verify its pilot holds spendable XP ≥ the cheapest SPA cost. If yes, add the pack-loaded variant as the spec's start state (front-door loader only), keeping the 4.4 path as documented fallback; if no, record the verdict in the spec header and the ledger row — the 4.4 path stands. Either outcome is acceptance-valid; faking XP via store writes is not.
  - Files: `e2e/playtest-subsystem-xp-purchase.spec.ts` (conditional), or read-only + recorded verdict.
  - Acceptance: written verdict with evidence; spec green 3× in whichever configuration ships.

## 8. Topology-dependent proofs + the W5-deferred review — GATED: after the W5 implementation merges

- [ ] 8.0 **Gate check (STOP on failure)**: verify `playwright.config.ts` registers exactly the tag-then-scope project set (chromium, `mobile-touch`, env-gated `flow-audit`). If the responsive/smoke projects still exist (W5 impl unlanded), STOP this group and report — ladder sequencing should make this impossible; the gate verifies rather than assumes.
  - Files: read-only.
  - Acceptance: a written gate verdict naming the registered projects.
- [ ] 8.1 Topology proofs on the real project set: `npx playwright test --list` proves every `@subsystem:`-tagged spec schedules exactly once (chromium only — no project greps the namespace, and none of the tagged specs carries `@mobile-touch`); the three anchors still list under chromium only; record the listing table.
  - Files: read-only; proof recorded in the PR description.
  - Acceptance: the recorded table with zero double-scheduled tagged specs.
- [ ] 8.2 Perform the mobile-touch tenancy review W5 deferred to W6: count the project's scheduled tests (`--list --project=mobile-touch`) vs the two describes W5 migrated; record the verdict (retain vs recommend-deletion) as a written note + follow-up recommendation. **Do not delete the project** — that requires a delta to W5's topology requirement after its archive (design D1/D9).
  - Files: read-only; verdict recorded.
  - Acceptance: the written tenancy verdict with the `--list` evidence and, if deletion is recommended, the named follow-up.

## 9. Verification battery + spec truth

- [ ] 9.1 `npm run typecheck && npm run lint && npm run format:check` clean across all new/modified files.
- [ ] 9.2 Full unit lane green (`npx jest --selectProjects unit --ci`) including the new guards/wrappers; `npm run qc:journeys:validate` green; each mini-journey's 3×-green evidence (first-attempt green — `--retries=0`) and the seam job's burn-in table (zero flaky results per run) recorded in the PR description.
- [ ] 9.3 Final CI proofs re-run on the final tree (post any rebase): 5.1/5.4's check-name diff (30 names, 29 byte-stable), the branch-protection snapshot equality, 5.5's red-path evidence (seam job `failure` → aggregator `failure`, never `skipped` → merge state `BLOCKED` at the forced-failure SHA), 6.1's dispatch evidence, and 8.1's `--list` table — the numbers shipped are the numbers proven.
- [ ] 9.4 Spec-coverage self-audit: walk both delta files requirement by requirement and name the artifact delivering each SHALL — journey-qc: Seam Anchor Pull-Request Gate (5.1–5.5 + 1.3's invariant-lane pin), MODIFIED Required Journey Scenario Catalog (2.1/2.2/2.3), MODIFIED QC Graph Query Command (2.2); e2e-testing: Subsystem Tag Taxonomy (4.1–4.5/7.1 + 2.3's literal-derivation guard), Nightly Subsystem Audit Lanes (6.1/6.2/7.1), MODIFIED Subsystem Validation Coverage (3.1/3.2/3.3/4.1–4.5). Any uncovered SHALL blocks completion.
- [ ] 9.5 `npx openspec validate add-subsystem-lanes-and-ci --strict` passes after any spec-delta edits (formatter first — design R1).
- [ ] 9.6 Ship-time ledger reconciliation (design R2): rebase onto current `origin/main`, confirm `openspec/active-change-ledger.json` lists exactly the then-active change folders including `add-subsystem-lanes-and-ci`, and confirm `node scripts/qc/validate-openspec-ci-quality.mjs` (the `qc:openspec-ci:validate` gate) passes.
