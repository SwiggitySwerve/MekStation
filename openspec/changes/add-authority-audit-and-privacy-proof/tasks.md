Every PR in this change MUST stay under 500 non-generated changed lines and 15 files, own one behavior seam, and split through an OpenSpec update before implementation if the cap cannot be met.

Work-path trace: this leaf follows `add-replay-schema-and-checkpoint-safety` and precedes the independently gated combat and campaign authority leaves in `../harden-gm-two-player-campaign-sessions/event-history-wave-map.md`.

## 0. Replay-Safety Admission Gate — Pre-Implementation Receipt

- [x] 0.1 Before PR 1, verify on fetched exact main that `add-replay-schema-and-checkpoint-safety` is synced and archived, its task 21.3 and separately recorded post-merge terminal evidence pass, its active ledger entry/directory are absent, and its merged branch/worktree are pruned. Record the predecessor merge SHA and fail closed on missing or contradictory evidence.
  - Receipt (2026-08-21, fetched exact main 8f084e4be): `add-replay-schema-and-checkpoint-safety` is synced and ARCHIVED as `openspec/changes/archive/2026-08-22-add-replay-schema-and-checkpoint-safety/` (closeout PR #1300, squash-merged SHA-guarded); its 21.3 receipt records the exact-main battery on 243a4f657 (focused sweep 1456/1456, replay + multiplayer QC gates green) and the post-merge terminal evidence was separately verified on 8f084e4be: archive directory present, active-change-ledger entry ABSENT (10 active, `validate-openspec-ci-quality` 10/10 accounted, errors=0), change directory absent from `openspec/changes/`, and zero `mekstation-rs-*` worktrees/branches remaining. Predecessor merge SHA: 8f084e4be (#1300); final implementation PR chain #1279-#1299 with the reviewed inventory amendment #1287. No missing or contradictory evidence.
- [x] 0.2 Before PR 1, re-review every PR seam in this file against the then-current codebase for the 500-line/15-file cap and one-behavior-seam rule, and re-confirm seam ownership against `../harden-gm-two-player-campaign-sessions/event-history-wave-map.md`; if any seam no longer fits or ownership has drifted, split or reassign it through a spec-only OpenSpec update before implementation, and fail closed on unresolved ownership conflicts.
  - Receipt (2026-08-21): all ten PR seams re-reviewed against main 8f084e4be. Ownership vs `../harden-gm-two-player-campaign-sessions/event-history-wave-map.md` row 3 (waves 11/12/18: membership-gated projection, action/private audit, privacy evidence) is UNCHANGED - the predecessor wave's only adjacent landings were consumer-side: replay-safety PR 19B gated the CLIENT mirror (`buildMirrorSessionGated`) and PR 18/19A gated library/recovery CONSUMPTION, while this change's seams own the SERVER production surfaces (socket attachment, baseline/replay frame publication, recipient selection, projection service, delivery epochs) - complementary, no seam absorbed or split. Cap review: each seam remains one behavior (viewer contract; admission routing; one authorization gate; one additive audit table; private storage; projection service; delivery-epoch store; live/replay adoption; historical adoption; evidence journey) and none obviously exceeds 500 lines/15 files given the current surface sizes (ServerMatchHost* modules are already decomposed; SQLite migrations follow the small per-migration module idiom at v10). Two watch-items recorded, neither a conflict today: (a) PR 8's server frame routing must compose with the PR-19B client gate (the mirror will consume projected objects - the match-broadcast host-stamp destamp contract must be preserved or explicitly superseded by projected objects); (b) PR 7's delivery-epoch schema is the named dependency of `design-campaign-authority-and-sync`'s delivery side (its tasks preamble consumes the (deliveryEpochId, deliverySequence) cursor mapping) - schema decisions there must be made with that consumer named.

## 1. Authorized Viewer Contract and Resolver — PR 1

- [x] 1.1 Add failing unit tests proving a known campaign/match ID, client role/ownership claim, or non-human principal claim cannot construct an authorized viewer without verified identity plus active durable membership.
  - Receipt (2026-08-21): failing-first negative set (red before the module existed, green after): a structurally identical client claim with known campaign/match/participant ids AND role/ownership fields fails `isAuthorizedViewer`; a bare `{principalId}` fails `isVerifiedPrincipal`; resolution with an unminted principal fails typed `unverified-identity`; a verified principal without an active durable membership (inactive row AND unknown principal - same code, no membership-existence oracle) fails typed `no-active-membership`; a service-kind membership row fails typed `non-human-principal`.
- [x] 1.2 Add the server-internal `IAuthorizedViewer` contract, membership resolver, and revision-bound cache with no transport deserializer or public constructor.
  - Receipt (2026-08-21): `src/lib/multiplayer/server/authorization/AuthorizedViewer.ts` - `IAuthorizedViewer` exactly per design D1; NO public constructor and NO transport deserializer: viewers are minted only inside the module and registered in a module-private WeakSet, so spreads, escalated copies, JSON round-trips, and client claims are not authority; `JSON.stringify(viewer)` THROWS (typed) so a viewer context cannot cross a transport boundary; `AuthorizedViewerResolver` derives every field from the durable membership row alone via the server-internal `IMembershipSource` port (durable store binding is PR 2's seam) with a revision-bound cache; `mintVerifiedPrincipal` is the server session layer's identity proof.
- [x] 1.3 Prove membership revision changes invalidate cached contexts and internal non-human jobs cannot construct, serialize, borrow, or convert into human viewer authority; keep one healthy authorized control.
  - Receipt (2026-08-21): revision-bound invalidation proven - cached resolve reuses the same context with ONE source lookup; a membership change (ownership shrink + revision bump) flips `isCurrent(old)` false and re-resolution mints a fresh context carrying the new row; revocation (active=false) removes the context entirely with the typed failure. Non-human non-convertibility: a service principal fails typed while a healthy authorized HUMAN control resolves in the same test; forged/copy objects fail `isCurrent` outright; the viewer and its ownedForceIds are frozen.
- [x] 1.4 Run focused resolver/type tests, typecheck/lint/format, strict OpenSpec validation, and independent security review.
  - Receipt (2026-08-21): focused resolver/type suite 8/8; typecheck/oxlint/format clean; strict OpenSpec green; independent fresh-context SECURITY review (Grok 4.6, attack-minded charter: forgery via prototype/proxy, serialization escapes, mint export scope + client-bundle runtime question, cache TOCTOU, membership-existence oracles) recorded in the PR description - initial verdict REJECT on a REAL blocker (the resolver minted whatever row the source returned; verified identity was only a lookup key), fixed by binding the row to the verified principal + requested session with typed `membership-source-integrity` failure and treacherous/cross-session-source tests; re-review APPROVE with both residual hardenings folded (explicit consumer contract: every PR 2/3 trust boundary MUST call `isAuthorizedViewer`, never authorize from property reads; single-snapshot row reads defeating getter-based TOCTOU). Seam cap: one behavior, 2 files, well under limits.
- [x] 1.5 After merge, rerun exact-main resolver receipts and prune the merged branch/worktree.
  - Receipt (2026-08-21): PR 1 merged as #1302 (squash, SHA-guarded; the REJECT-then-fix-then-re-review APPROVE security cycle recorded); resolver receipts reran within this PR's battery on the post-merge main (AuthorizedViewer suite 10/10 green); worktree + branches pruned; PR 2 branched from the post-merge main e37a81c29.

## 2. Socket, Replay, and Publication Admission — PR 2

- [x] 2.1 Add failing admission tests proving attachment, baseline, replay, reconnect, and publication send no protected payload before the resolver returns an active viewer.
  - Receipt (2026-08-21): failing-first admission set (red before `admitSocket` existed): a verified NON-MEMBER principal is refused with typed `AUTH_REJECTED` Error + Close as the ONLY frames ever sent - no baseline, replay, lobby, or event payload precedes the resolver verdict - and never attaches; a seated member admits, attaches, and carries seat-derived scope (participant slot + owned playing slots); the binder (`bindMultiplayerSocketConnection`) routes through `admitSocket`, so replay/reconnect/publication all sit BEHIND admission by construction (replay only flows on the post-attach SessionJoin path).
- [x] 2.2 Route socket attachment, baseline/replay recovery, and publication recipient selection through the authorized-viewer resolver; ignore client authority/role/ownership fields.
  - Receipt (2026-08-21): `MatchSeatMembershipSource` derives membership ONLY from durable state (HUMAN/SPECTATOR-kind seat occupancy in `IMatchMeta.seats`, or the durable `playerIds` roster for pre-lobby matches); client authority/role/ownership fields have no path in (the resolver takes only the upgrade handler's verified player id + the match id). The session membership EPOCH derives deterministically from the durable membership-relevant state itself (canonical hash of seat occupancy/roster), satisfying the PR-1 port invariant with no schema change and restart-stable epochs. `ServerMatchHost.admitSocket` is the production attach path; `revalidateAttachedViewers` runs after EVERY lobby intent dispatch.
- [x] 2.3 Prove revocation closes subsequent publication/reconnect access and non-human capabilities cannot attach or receive replay, with a healthy authorized reconnect control.
  - Receipt (2026-08-21): revocation proven end-to-end on a REAL host + in-memory store - POST-LOBBY (occupancy IS membership once the match starts; during lobby a verified room-code joiner is an INVITEE member with no owned forces, because OccupySeat necessarily arrives on an already attached socket - the review's chicken-egg blocker), durably vacating a member's seat then revalidating closes that member's socket with typed `AUTH_REJECTED` (subsequent publication impossible - the socket is detached and closed) and their reconnect is refused at admission, while the healthy authorized member stays attached AND can re-admit (control); non-human capabilities cannot attach because no human/spectator seat row exists for them (typed refusal). Epoch-change test pins that a durable seat change flips the session revision (status participates in the epoch so the lobby-to-active transition invalidates invitee contexts); an AI seat with a leftover occupant never mints; an infrastructure failure reading membership is DISTINCT from revocation (typed MembershipSourceUnavailableError): revalidation keeps attached members while new admission fails closed with an INTERNAL_ERROR close - proven with a broken-store test.
- [x] 2.4 Run focused multiplayer/recovery tests and independent admission/security review.
  - Receipt (2026-08-21): admission suite 9/9 (re-review LOW residuals folded: attached-invitee kicked on lobby fill; status-only epoch change) + full multiplayer battery 377/377 (incl. the binder suite updated to the admission-mirroring mock); `verify:qc:multiplayer-reliability` 16 passed / 3 skipped-as-designed, with ONE pre-existing failure reproduced byte-identically on UNMODIFIED main e37a81c29 (playtest-coop-route-smoke host-mode: campaign PUT 409 retry-noise - the known residual owned by design-campaign-authority-and-sync, not caused by this change); typecheck/lint/format clean; strict OpenSpec green; independent admission/security review (Grok 4.6): initial REJECT on the REAL lobby chicken-egg blocker (room-code joiners attach before OccupySeat - seat-gated admission would have bricked lobby join on the wire) plus the store-error mass-revoke MAJOR; both fixed and the focused re-review returned APPROVE with the production lobby client flow read end-to-end; the intent-dispatch recheck residual is exactly task 3.2 (next PR).
- [x] 2.5 After merge, rerun exact-main attachment/replay receipts and prune the merged branch/worktree.
  - Receipt (2026-08-21): PR 2 merged as #1303 (squash, SHA-guarded); attachment/replay receipts reran on exact post-merge main a6810272b - admission battery 24/24 green (MatchAdmission 9 + AuthorizedViewer 10 + binder 5); worktree and branch pruned from the main checkout.

## 3. Human Command and Read Authorization — PR 3

- [x] 3.1 Add failing tests for command, raw/history read, branch operation, timeline, export, and private-audit entrypoints without a current authorized viewer.
  - Receipt (2026-08-21): `HumanActionAuthorization.test.ts` pins ALL SIX entrypoint kinds (command, history-read, branch, timeline, export, private-audit) refusing with typed `HumanActionAuthorizationError('no-viewer')` for a principal with no current viewer, with constant id-free messages (unknown vs inactive vs non-human are not distinct oracles; refusal text contains neither principal nor session ids).
- [x] 3.2 Add one application authorization gate that rechecks viewer membership and requested entity/stream scope before those entrypoints reach authority or storage services.
  - Receipt (2026-08-21): `HumanActionAuthorizationGate.ts` is THE single application gate: fresh resolve through `AuthorizedViewerResolver` (epoch/revocation applies per call), `isAuthorizedViewer` brand check (consumer contract honored), then server-derived scope law - command force claims must be a subset of `viewer.ownedForceIds`, a participant claim must equal the server-derived `participantId` (mismatch refuses, never falls back to the claim), and stream/entity ids must name the viewer's own session/match (compared against viewer fields only - the foreign session is never loaded, so wrong-session cannot become an existence oracle). Wired at the production command path: the binder passes the CONNECTION'S admitted principal into `handleIntent`; an envelope whose `playerId` differs refuses with `AUTH_REJECTED player-mismatch` before any integrity gate, and the gate recheck runs before rate-limit/pause/dice so an unauthorized command learns nothing about match state; refusals append no event and never dispatch to the engine. The auth-vs-infra split is preserved end-to-end (`MembershipSourceUnavailableError` maps to `INTERNAL_ERROR`, never an auth verdict).
- [x] 3.3 Prove client scope escalation and non-human capabilities fail closed with no gameplay mutation or disclosure, while authorized GM/player controls remain healthy.
  - Receipt (2026-08-21): proven on a REAL `ServerMatchHost` + store - a seated member sending an engine-mutating intent as the OTHER player is refused with zero events appended and no Event frame delivered; an admitted-then-revoked member (seat vacated + match started) with a MATCHING wire principal is refused by the gate's fresh membership recheck (`command refused: no-viewer`), store unchanged; force-scope escalation (foreign seat slot) refuses while the member's own slot passes; non-human capabilities (no membership row AND a service-kind row) refuse for all six kinds with byte-identical messages; the healthy control - the same seated member acting as themselves - dispatches, appends, and broadcasts.
- [x] 3.4 Run focused command/history authorization tests and independent code/security review.
  - Receipt (2026-08-21): focused authorization battery 10/10 (`HumanActionAuthorization`), full multiplayer battery 66 suites / 495 tests green, `tsc --noEmit` clean, oxfmt clean, strict OpenSpec valid. Cross-model split per worker policy: implementation by a Grok 4.6 worker from a bounded charter; independent line-by-line code/security review by the orchestrator (Claude) - review found one gap (no wire-path proof that a revoked-but-admitted member's command is refused by the gate rather than the mismatch check) and folded the missing regression test; reliability gate result recorded in the PR description.
- [x] 3.5 After merge, rerun exact-main command/read receipts and prune the merged branch/worktree.
  - Receipt (2026-08-21): PR 3 merged as #1304 (squash, SHA-guarded); command/read receipts reran on exact post-merge main c6a9d7eaa - 29/29 green (HumanActionAuthorization 10 + MatchAdmission 9 + AuthorizedViewer 10); worktree and branch pruned from the main checkout.

## 4. Action Audit Lifecycle — PR 4

- [x] 4.1 Add the additive action-audit table and server-internal repository with command identity/digest, server-derived actor/authority, lifecycle, safe reason, correlation, timestamps, and published receipt identity.
  - Receipt (2026-08-21): SQLite migration v11 `action_audit_schema` (additive; CREATE IF NOT EXISTS; no FKs into journal/checkpoint tables) + server-internal `SQLiteActionAuditRepository` behind `IActionAuditRepository` (no IAuthorizedViewer anywhere in the API, per design D2). One row captures session identity, command id + 64-hex digest, SERVER-derived actor/authority (principal, participant, closed gm/player role), lifecycle, closed id-free safe reason code, correlation, created/updated timestamps, published receipt identity, and the committed batch link (first/last revision + count with the journal batch law last = first + count - 1 enforced in BOTH the SQL CHECK and the TS guard). PK is the journal global command identity. Append-once at the SCHEMA level: no-delete trigger, no-rewrite trigger whose only legal UPDATE is the accepted-to-published stamp with every identity column pinned unchanged, and an insert-not-published trigger.
- [x] 4.2 Record accepted, rejected, vetoed, timed-out, and published lifecycle provenance idempotently; prove terminal failures create no gameplay event, outbox, projection mutation, or player delivery identity.
  - Receipt (2026-08-21): inserts admit only terminal accepted/rejected/vetoed/timed-out; `published` is a one-time stamp on an accepted row (same-value re-link idempotent, different value typed receipt-conflict, non-accepted typed refusal). Retrying the same terminal identity returns the EXISTING record (typed created-vs-existing result; row count pinned at 1), a conflicting retry (same command id, different digest/state/identity attribute) is a typed identity-conflict with the stored row proven unchanged, including the unique-violation race path. Failure lifecycles CANNOT carry a committed range or receipt (CHECK + guard). Isolation proven against a REAL SQLiteEventJournal in the same database file: rejected/vetoed/timed-out audit writes leave the journal event/batch/ref/causation/head tables, store state, and getCommandReceipt byte-identical (outbox/projection/delivery tables do not exist yet, so no path can mutate them).
- [x] 4.3 Run real-SQLite restart/idempotency tests and independent history review.
  - Receipt (2026-08-21): real-SQLite restart proof - terminal rows for all four insert lifecycles survive a database file close/reopen with identical content; crash-rerun migration proof (v11 record dropped, migration re-run clean); migration-pin moves recorded (event-journal latest-version pin 10 to 11, count 9 to 10; replay-checkpoint trigger catalog gains exactly the three action_audit guards). Batteries (orchestrator-run): audit + migration + journal slice 20 suites / 198 tests green; tsc clean; oxfmt clean; strict OpenSpec valid. Cross-model split: Grok 4.6 worker implemented from a bounded charter; orchestrator (Claude) line-reviewed the migration SQL (trigger identity-pin list, lifecycle CHECK law), the guard/identity comparison, and the repository transaction paths; history review recorded in the PR description.
- [ ] 4.4 After merge, rerun exact-main lifecycle receipts and prune the merged branch/worktree.

## 5. Private Record Storage and Retention — PR 5

- [x] 5.1 Add separate private-record, private-access-audit, and retention-state tables plus server-internal repositories.
  - Receipt (2026-08-21): SQLite migration v12 `private_records_schema` adds the separate access-controlled storage class (design D4): `private_record` (opaque 32-hex crypto-random ref PK never derived from content or command identity; payload/payload_state CHECK law present/erased/redacted; retention_class closed set; identity-pinned triggers - no delete ever, UPDATE legal only for the present-to-erased and present-to-redacted transitions), `private_access_audit` (append-only, structurally payload-free - no payload column exists; no-update/no-delete triggers), and `private_retention_state` (per-class keep / erase-on-expiry policy). Server-internal repositories behind `IPrivateRecordRepository` (contract + guards + borrowed-handle adapter, mirroring the action-audit folder). No FKs into journal or action_audit; command linkage is by identity value only.
- [x] 5.2 Keep private payload content out of player-safe rows/digests and require current membership/role for every private lookup.
  - Receipt (2026-08-21): player-safe rows/digests carry only the opaque ref - proven content-free: two records with IDENTICAL payload mint DIFFERENT refs, and neither ref contains any substring of the payload or its sha256 hex. Every private lookup/export/erase/redact rechecks the PR 3 gate (`authorizeHumanAction` kind 'private-audit' - membership + stream scope) THEN requires server-derived `viewer.role === 'gm'`; players receive role-denied in the access log and the SAME public refusal as an absent ref. Absent-ref and wrong-scope denials are BYTE-IDENTICAL in code and message (asserted equal), so neither existence nor content is disclosed; denied lookups still append a safe denied access row; the private payload never appears in any thrown error or audit row (JSON scans asserted).
- [x] 5.3 Add export-deny default, configured retention, audited access, and erasure/redaction tests that preserve only the player-safe fact and unavailable-detail marker.
  - Receipt (2026-08-21): export-deny default - the default export shape has NO payload key at all (asserted via Object.keys) even for payload_state present; includePrivate requires the gm gate pass and appends an export-attempt audit row. Retention is injected-clock minimal: configureRetention per class; runRetention(cutoffAt, occurredAt, job principal) erases only expired erase-on-expiry present rows, audited as retention-action with the job principal and NULL role. Erasure/redaction are gm-gated one-time transitions (second call typed already-terminal), and after erase the row retains ONLY the safe fact + unavailable-detail marker (opaque_ref/kind/session kept, payload NULL, state erased) while action_audit rows and journal tables are byte-identical before/after (count + content probes); DELETE on private_record aborts at the trigger.
- [x] 5.4 Run real-SQLite restart/privacy tests and independent security/history review.
  - Receipt (2026-08-21): real-SQLite restart proof - private records, access-audit rows, and retention rows survive database close/reopen; migration crash-rerun clean; pin moves recorded (journal latest 11 to 12, count 10 to 11; trigger catalogs gain the five private-record guards; action-audit crash-rerun now clears version >= 11). Batteries (orchestrator-run): persistence + audit + privacy + journal slice 22 suites / 216 tests green; multiplayer battery 66 suites / 495 tests green; tsc clean; strict OpenSpec valid. Cross-model split: Grok 4.6 worker implemented from a bounded charter; orchestrator (Claude) security/history line review covered the gate coupling in privateRecordAccess (denied-row actor sourcing, single constant public refusal), the export default path, the erase/redact transition guards, and the migration trigger identity pins; review narrative in the PR description.
- [x] 5.5 After merge, rerun exact-main private-lifecycle receipts and prune the merged branch/worktree.
  - Receipt (2026-08-21): PR 5 merged as #1306 (squash, SHA-guarded); private-lifecycle receipts reran on exact post-merge main 3cfb4bcfc - 18/18 green (privateRecord migration + repository suites); worktree and branch pruned from the main checkout.

## 6. Viewer Projection Service — PR 6

- [x] 6.1 Add an application projection service that requires an authorized viewer, authorizes the requested entity/stream, and returns only versioned viewer-safe projection objects.
  - Receipt (2026-08-21): `ViewerProjectionService` (src/lib/multiplayer/server/projection/) is the application projection chokepoint with a BINDING order: (1) `isAuthorizedViewer` brand check - a spread structural clone of a real resolver-minted viewer refuses `not-a-viewer` (property reads are never authorization); (2) stream scope compared against viewer fields ONLY (the named foreign stream is never loaded; constant refusal message shared with the human-action gate); (3) versioned audience-projector registry lookup; (4) journal read INSIDE the service; (5) per-event explicit audience decision (public / owner-only / gm-only / hidden). Output is ONLY the versioned viewer-safe shape: projectorVersion + viewerScope + facts of {factType, sequenceHint, payload}, where sequenceHint is a LOCAL gapless ordinal over visible facts (explicitly non-durable - PR 7 owns delivery identity), so hidden events are uncountable. GM sees owner-only facts for all owners; players match owner-only facts only via payload participant/force identity within their server-derived scope, fail-closed when the payload names none.
- [x] 6.2 Prohibit raw journal/private rows from crossing the service boundary and prove membership, audience, or projection failure yields a typed failure with no raw fallback.
  - Receipt (2026-08-21): raw rows cannot cross the boundary - no IStoredEvent field (eventDigest, commitPosition, streamRevision, commandId, recordedAt, previousStreamEventDigest) appears anywhere in any successful response (deep JSON scans asserted against the appended events' actual values); output payload objects are never reference-identical to stored payloads (the service constructs every fact wrapper itself and projectors must return fresh objects); the projection folder has NO import of src/lib/events/privacy and its public types never mention IStoredEvent (static boundary scan test, incl. a no-Date.now/new-Date production-source scan). Failures are typed with NO raw fallback: a projector decision that throws mid-stream or a missing decision refuses the WHOLE request with constant-message `projection-failed` (no partial facts, inner error not attached as cause); journal infrastructure errors map to the same constant so storage/zod messages cannot leak; `wrong-session` is byte-identical whether or not the foreign stream exists (probe journal proves the read never happens).
- [x] 6.3 Run focused projection-object/privacy tests and independent code/security review.
  - Receipt (2026-08-21): focused ViewerProjection suites 13/13 green; combined multiplayer + journal battery 80 suites / 644 tests green (orchestrator-run); tsc clean; oxfmt + oxlint clean; strict OpenSpec valid. Cross-model split: Grok 4.6 worker implemented from a bounded charter; orchestrator (Claude) security line review covered the binding order in project(), the mirrored stream-scope law, the owner-audience matching rule (payload identity keys vs viewer scope, gm-sees-all-owners), the silent-skip vs fail-closed split (hidden/out-of-audience skip silently keeping hints gapless; unknown decision fails the whole request), and the error-code separation between request refusals and registry-construction faults; review narrative in the PR description.
- [x] 6.4 After merge, rerun exact-main projection-service receipts and prune the merged branch/worktree.
  - Receipt (2026-08-21): PR 6 merged as #1307 (squash, SHA-guarded); projection-service receipts reran on exact post-merge main f2c8fac13 - 13/13 green (ViewerProjection suites); worktree and branch pruned from the main checkout.

## 7. Durable Viewer Delivery Epochs — PR 7

- [x] 7.1 Add the delivery-epoch and event-mapping schema keyed by principal, campaign session, participant, membership revision, stream type/ID, projector version, and effective generation, with unique epoch/event and epoch/sequence constraints.
  - Receipt (2026-08-21): SQLite migration v13 `delivery_epochs_schema`: `delivery_epoch` (opaque 32-hex crypto-random id never derived from key material; UNIQUE over the full 8-column tuple principal/campaign-session/participant/membership-revision/stream-type/stream-id/projector-version/effective-generation; append-only triggers), `delivery_event_mapping` (UNIQUE (epoch, projectedEventIdentity) AND UNIQUE (epoch, deliverySequence); sequence >= 1 CHECK; no-update/no-delete triggers - durable delivery identity is append-only), `delivery_generation` (UNIQUE stream triple; INSERT only at 1; UPDATE only +1 enforced by trigger - the schema-owned MONOTONIC axis, deliberately separate from the non-monotonic membership-revision hash from PR 2). projectedEventIdentity is the stored event CONTENT digest (eventDigest), not a positional revision - stable across reconnect/retry/replay and reconstructions that keep identical bytes at new positions; pinned by test.
- [x] 7.2 Derive and compare the complete epoch key on every cursor request; reject cross-principal/session/participant/stream or stale revision/version/generation cursors without revealing epoch existence.
  - Receipt (2026-08-21): `resolveEpoch`/`validateCursor` freshly derive the complete 8-tuple from the BRANDED viewer + requested stream + current generation on EVERY call (the request carries only streamType/streamId/projectorVersion; principal/participant/revision come from the viewer alone). Cursor validation loads the cursor epoch row and compares the full tuple: unknown id, foreign principal, moved membership revision, different stream/version, and bumped generation ALL return ONE identical stale-epoch shape (asserted toEqual across mismatch classes) whose baseline is the caller's OWN freshly resolved epoch - existence of foreign epochs never leaks and foreign mappings are never read.
- [x] 7.3 Atomically allocate a gapless sequence only after successful visible projection; prove hidden/failed events assign nothing and concurrent reconnect/retry/replay returns one mapping without duplicates or reserved gaps.
  - Receipt (2026-08-21): sequences are assigned only through projectWithDelivery AFTER the PR 6 projection succeeded, and only for VISIBLE facts - hidden and out-of-audience events produce no fact, no identity, and no sequence, with adjacent visible events taking adjacent sequences (spec scenario pinned); each viewer epoch owns an independent gapless 1..n space. `assignSequences` runs in ONE IMMEDIATE better-sqlite3 transaction: existing (epoch, identity) mappings are REUSED (reused: true, no new rows on reconnect/retry/replay/pagination), new identities take MAX+1, and a thrown transaction rolls back every row leaving MAX(sequence) and row count unchanged (no reserved gaps), with a follow-up assign continuing gaplessly. Interleaved assigns on one epoch produce a disjoint gapless union with one durable mapping each.
- [x] 7.4 Prove cold reopen, pagination, same-epoch replay stability, explicit new baselines, and typed stale-epoch rejection with real SQLite.
  - Receipt (2026-08-21): real-SQLite proofs - cold reopen keeps epochs/mappings/generations with sequences continuing unrenumbered; pagination via readMappings(afterSequence) returns durable identities ascending with journal positions structurally absent from output types; same-epoch replay reuses identical sequences; bumpGeneration (+1 one-way, bootstrap insert-1-then-bump so the first explicit rebaseline is 2) makes old cursors return typed stale-epoch with an explicit NEW baseline - never silent continuation or renumbering; migration crash-rerun clean.
- [x] 7.5 Run focused delivery-store/privacy tests and independent concurrency/security review.
  - Receipt (2026-08-21): focused delivery suites 28/28 across 3 suites; combined delivery + projection + multiplayer + journal + migration battery 93 suites / 752 tests green (orchestrator-run); tsc clean; oxfmt + oxlint clean; strict OpenSpec valid; migration pins moved (latest 12 to 13, count 11 to 12, trigger catalogs). Cross-model split: Grok 4.6 worker implemented from a bounded charter; orchestrator (Claude) concurrency/security line review covered the IMMEDIATE-transaction allocation loop and its race/reuse paths, the stale-epoch uniformity law, the eventDigest identity decision, the generation bootstrap semantics, and the projectWithIdentities pairing seam (identities beside - never inside - the byte-identical PR 6 fact objects); review narrative in the PR description.
- [x] 7.6 After merge, rerun exact-main delivery-epoch receipts and prune the merged branch/worktree.
  - Receipt (2026-08-21): PR 7 merged as #1308 (squash, SHA-guarded); delivery-epoch receipts reran on exact post-merge main 45ad78fb8 - 28/28 green (3 delivery suites); worktree and branch pruned from the main checkout.

## 8. Live, Replay, and Snapshot Projection Adoption — PR 8

- [x] 8.1 Route live publication, replay/recovery frames, and snapshots through the projection service plus delivery-epoch boundary.
  - Receipt (2026-08-21): the match host's three viewer-facing frame producers now route through `ViewerPublicationBoundary` bound to each socket's ADMITTED viewer: live broadcast (`broadcastEvent` resolves every attached socket's viewer per broadcast, cached by playerId; fog filtering still runs first; the guard runs before every send; a socket whose membership cannot be verified receives NOTHING - the spec's membership-failure-sends-no-raw-fallback law), SessionJoin replay frames, and the SessionJoin baseline (`guardReplayFrames`/`guardBaseline`). The versioned `MatchWireAudienceCatalog` v1 declares EVERY GameEventType (all 81) as an explicit identity-public decision - compile-locked exhaustive via satisfies Record over the union, runtime-pinned all-public-at-v1, so the wire format is UNCHANGED and any future hidden/gm-only match event is a deliberate reviewed projectorVersion bump. Delivery-epoch adoption: `projectWithCursor` is the reusable journal-backed consumption shape - null cursor resolves a fresh baseline; a cursor validates FIRST (stale/foreign returns the typed stale-epoch result with the caller's fresh baseline, NO projection, NO sequence assignment - a stale cursor cannot mint delivery identity); valid cursors page facts by durable deliverySequence. The PR-19B host-stamp contract and campaign sync are untouched.
- [x] 8.2 Add projected-object and raw-frame parity tests with hidden-gap, projection-failure, reconnect, and stale-cursor cases.
  - Receipt (2026-08-21): parity - on a REAL host with two admitted members and a real engine intent, the raw Event frames each socket received deep-equal the guardLiveEvent output for each member's REAL resolved viewer (projected == raw for the public catalog, intentId stamps preserved; both sockets byte-identical). Hidden-gap - a synthetic test catalog with a hidden type between two public types delivers the public frames adjacent with no hidden marker anywhere in serialized output, and rebuilt replay chunks report totalEvents = visible count; gm-only reaches the gm viewer and not the player (healthy control). Projection-failure - a throwing decision yields typed projection-failed, NOTHING is sent, error JSON carries no payload fragment. Reconnect - SessionJoin replay equals the store's events for a member; a NON-MEMBER join closes with typed AUTH_REJECTED and zero baseline/replay frames while a BROKEN membership store closes with INTERNAL_ERROR (auth-vs-infra split preserved on the join path - orchestrator review FOLDED this split: the worker's first cut conflated revocation with infra under one INTERNAL_ERROR close). Stale-cursor - valid cursors page after afterSequence; foreign/unknown cursors return one identical stale shape with mapping row count unchanged; reconnect on the returned baseline reuses sequences.
- [x] 8.3 Run focused multiplayer/replay/recovery contracts and independent code/security review.
  - Receipt (2026-08-21): multiplayer + projection + delivery + publication battery 74 suites / 546 tests green; replay/recovery contracts 57 suites / 901 tests green; tsc clean; oxfmt clean; strict OpenSpec valid; `verify:qc:multiplayer-reliability` 16 passed / 3 skipped-as-designed with the ONE pre-existing coop-route-smoke campaign PUT 409 flake (signature identical to unmodified main; owned by design-campaign-authority-and-sync). Cross-model split: Grok 4.6 worker implemented from a bounded charter; orchestrator (Claude) security line review verified the wire-format pin, the broadcast fail-closed law, the per-broadcast viewer cache, and CAUGHT + FOLDED the join-path auth-vs-infra conflation (revoked member now gets typed AUTH_REJECTED per the spec scenario; infra failure keeps INTERNAL_ERROR) with regression tests for both closes; review narrative in the PR description.
- [ ] 8.4 After merge, rerun exact-main live/replay/snapshot parity and prune the merged branch/worktree.

## 9. History, Timeline, and Export Projection Adoption — PR 9

- [ ] 9.1 Route history, timeline, and export serialization through the projection service and enforce private-export denial by default.
- [ ] 9.2 Add history/timeline/export byte parity tests with cross-entity authorization, hidden-gap, projection-failure, and stale-cursor cases.
- [ ] 9.3 Run focused history/timeline/export privacy contracts and independent code/security review.
- [ ] 9.4 After merge, rerun exact-main historical-surface parity and prune the merged branch/worktree.

## 10. Three-Context Privacy Evidence — PR 10

- [ ] 10.1 Add one isolated GM/Player 1/Player 2 browser journey covering live action, rejection, reconnect, replay, history, timeline, and export.
- [ ] 10.2 Inspect pre-serialization objects, raw frames, snapshots, browser history/storage, exported bytes, and DOM with positive authorized controls and negative private-data searches.
- [ ] 10.3 Run applicable GM sandbox, replay/recovery, deep-audit, viewport/accessibility, and long-browser gates plus independent visual/security review.
- [ ] 10.4 After merge, rerun exact-main evidence, record authority/store/reload proof, and prune the merged branch/worktree before any combat or campaign journal cutover.
