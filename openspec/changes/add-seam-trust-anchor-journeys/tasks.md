# Tasks: add-seam-trust-anchor-journeys

> Execution is **Codex-first and strictly sequential** — parallel agent fan-out has failed on this repo. Run one group at a time; each lands with its verification before the next. **Hard ordering gate: implementation of this change starts only after the `add-sp-combat-determinism` (W1) IMPLEMENTATION merges to main.** At this authoring's base (`main@4bc1597bd`) only W1's declarative folder is merged — `fromSessionAsync` still hardcodes `SeededRandom(0xc0ffee)` with no `d6Roller` (`src/engine/InteractiveSession.ts:322-346`), `IGameConfig` has no `seed` field, and no SP surface parses `?seed=N`. Groups 1–3 have no runtime dependency on W1 mechanics; group 4 consumes W1's contracts directly and carries its own mechanical gate check. **Non-goals are load-bearing: NO CI workflow or branch-protection edits (W6), NO scenario-pack or fast-forward work (W4/W3), NO viewport-project rationalization beyond the three anchors' `testIgnore` entries (W5), NO changes to the deep-play/walkthrough audit journeys or their capture-tolerant contract, NO tightening of `quick-play.spec.ts` / `playtest-sp-smoke.spec.ts` / `encounter-combat-continuity.spec.ts`, NO product behavior changes (additive `data-testid` hooks are the ONLY sanctioned product-file touch), NO additions to the seven-journey catalog (`requiredJourneyIds` stays exactly seven), and NO golden traces (same-seed runs compare to each other, never to pinned values).** File:line references below were verified on `main@4bc1597bd` — re-verify against the then-current tree before editing (W1's implementation will have landed in between). After any spec-delta edit, run the formatter + `npx openspec validate add-seam-trust-anchor-journeys --strict` (design R4). Pre-commit runs a full `next build` (~3–5 min) — commit via background shell. Ledger registration ships with this declarative PR, not with these implementation tasks.

## 1. Ground-truth re-verification (read-only)

- [x] 1.1 Re-verify the seam (b)/(c) click-path testids live in `src`: `mission-launch-briefing`, `mission-readiness-panel`, `launch-mission-direct`, `pre-battle-page`, `player-force-card`, `player-unit-list`, `player-force-bv` (`src/pages-modules/gameplay/campaigns/missionLaunchPage.surfaces.tsx`, `missionLaunchReadinessPanel.tsx`, `src/pages/gameplay/encounters/[id]/pre-battle.tsx`) and the battle unit-identity testids `unit-token-${id}` / `rail-unit-${id}`. **Audit whether the per-unit pilot-assignment indicator — the literal `(pilot assigned)` marker span on each pre-battle unit row (`src/components/gameplay/pages/PreBattlePage.sections.tsx:133-137`, no testid as of `main@4bc1597bd`) — exposes a stable testid** — record the answer; if absent, group 3 adds one on that EXISTING marker (additive only). NOTE: the pre-battle rows render NO per-unit pilot NAME (only unit name + marker + position; names first render post-launch in battle) — the DOM assertion targets the indicator, and pilot identity is asserted at the data layer (1.3 / spec scenario "Force assignments verified at the data layer"), never by adding a pilot-name element.
  - Files: read-only grep across `src/`.
  - Acceptance: a written testid inventory including the pilot-assignment-marker verdict; any drifted selector recorded with its replacement.
- [x] 1.2 Re-verify the recovery seeding contract before writing the shared seeder: DB name/version/stores/keyPaths/indexes (`src/lib/p2p/matchLogStorageSchema.ts:3-38`), the `GameCreated`-first rule (`src/engine/InteractiveSession.persistence.ts:57-81`), the store exposure surface (`src/lib/e2e/storeExposure.ts:29-51`), and — post-W1 — where `config.seed` sits in the `GameCreated` payload so group 4's fixture can persist it.
  - Files: read-only.
  - Acceptance: a written schema/exposure note the seeder implementation cites; the `config.seed` payload location recorded.
- [x] 1.3 Re-verify the cleanup/data-layer API shapes: `GET/DELETE /api/forces/:id` (`src/pages/api/forces/[id].ts` — GET returns `{ force: IForce }`), `DELETE /api/encounters/:id` (`src/pages/api/encounters/[id]/index.ts:65`), how the pre-battle route/encounter resolves its player force id, and the exact assignments field shape on `IForce` the data-layer cross-check will count.
  - Files: read-only.
  - Acceptance: a written route/shape note naming the force-id resolution path and the assignments field; DELETE handlers confirmed for both row types.

## 2. Recovery rehydration trust anchor (seam a — structural phase, no W1 mechanics)

- [x] 2.1 Extract the inline `seedMatchLog` machinery from `e2e/active-session-recovery.spec.ts` into `e2e/helpers/matchLogSeeding.ts` as a pure move (same records, same raw `indexedDB.open` mechanism, parameterized over match id + event stream + matches-row fields). The existing test keeps passing unchanged.
  - Files: `e2e/active-session-recovery.spec.ts`, new `e2e/helpers/matchLogSeeding.ts` (+ `e2e/helpers/index.ts` export if that barrel is the convention).
  - Acceptance: existing spec green with zero assertion changes; the helper is consumed by both the existing test and 2.2's new test.
- [x] 2.2 Add the mirrored-roster anchor test to `e2e/active-session-recovery.spec.ts`: seed a `GameCreated` (sequence 0) whose units array fields the SAME canonical `unitRef` on both sides under distinct deployed ids (e.g. `player-1-atlas-as7-d` / `opponent-1-atlas-as7-d`) plus at least one more unit per side, a follow-on event (sequence 1), and an active `matches` row; cold `goto('/gameplay/games/:matchId')`; hard-assert via the exposed gameplay store: `isLoading === false`, `error === null`, `session.id === matchId`, `currentState.status === 'active'`, `interactiveSession` non-null, `getMovementCapability(deployedId)` non-null for EVERY seeded deployed id, `getMovementCapability(bareCanonicalRef)` null, unit count === full seeded roster count, and no terminal-outcome event beyond the seeded log; then `page.reload()` and re-assert everything (spec scenarios: "Cold route mount recovers the seeded match", "Mirrored canonical refs survive recovery without id collision", "Recovery does not manufacture a terminal outcome", "Reload re-recovers idempotently"). Drive NO dice-consuming continuation in this task (pre-W1-safe structural assertions only — design D1).
  - Files: `e2e/active-session-recovery.spec.ts`, `e2e/helpers/matchLogSeeding.ts`.
  - Acceptance: the new test fails if the #1019 recovery-side aliasing fix (`src/engine/InteractiveSession.recovery.ts`, `adapted.push({ ...adaptedUnit, id: gameUnit.id })`) is reverted (verify once by local revert-and-run, then restore); no server-side rows created (IndexedDB only — no cleanup surface).
- [x] 2.3 QA — `node scripts/playwright/run-playwright.mjs test --project=chromium e2e/active-session-recovery.spec.ts --workers=1` green 3× consecutively; `npm run verify:qc:replay-recovery` still green end-to-end.

## 3. Roster materialization handoff trust anchor (seam b — no W1 mechanics)

- [x] 3.1 Create `e2e/helpers/seamCampaign.ts`: campaign-via-wizard creation with a unique `Date.now()`-suffixed name (mirroring the `campaignSeeders.ts:163` uniquing pattern), contract acceptance + mission-launch-briefing opening, select-all-roster (returning the selected count), and launch-click-to-pre-battle — mirroring the verified click-paths of `e2e/ux-deep-play-audit.spec.ts:423-513` WITHOUT importing from or modifying that spec file (design D2 boundary).
  - Files: new `e2e/helpers/seamCampaign.ts`.
  - Acceptance: helpers are consumed by 3.2 (and group 4); `git diff` shows `e2e/ux-deep-play-audit.spec.ts` untouched.
- [x] 3.2 Create `e2e/seam-roster-materialization-handoff.spec.ts`: drive wizard → contract → briefing → select ALL roster units (capture N) → click `launch-mission-direct` → assert URL lands on `/gameplay/encounters/:id` then pre-battle; HARD assertions (spec scenario "Full roster selection reaches pre-battle intact"): `player-unit-list` rendered unit count `=== N` (blocking `expect`, replacing the C4 soft-finding class of `ux-deep-play-audit.spec.ts:515-536` — which stays untouched), `player-force-bv` nonzero, the pilot-assignment indicator present on every rendered unit row (using 1.1's testid; add the additive testid on the existing `(pilot assigned)` marker first if 1.1 found none — the only sanctioned product touch; the pre-battle page renders no pilot names, so pilot identity is asserted only by the data-layer cross-check below); data-layer cross-check (spec scenario "Force assignments verified at the data layer"): resolve the player force id per 1.3's note, `GET /api/forces/:id`, assert exactly N assignments with N distinct unit ids and pilots preserved.
  - Files: new `e2e/seam-roster-materialization-handoff.spec.ts` (+ the additive testid file from 1.1 if needed).
  - Acceptance: the spec FAILS (does not soft-record) when the rendered count ≠ N or BV is zero; API-layer and DOM-layer counts both asserted.
- [x] 3.3 Cleanup: track created campaign/encounter/force ids in module-level sets; `test.afterAll` deletes them via a standalone `APIRequestContext` (`DELETE /api/campaigns/:id`, `/api/encounters/:id`, `/api/forces/:id`), asserting 2xx per delete — extending the `e2e/campaign-flow.spec.ts:637-658` pattern (afterAll, not afterEach; standalone request context, never page fixtures) to the two new row types (spec scenario "Handoff journey cleans up its materialized rows").
  - Files: `e2e/seam-roster-materialization-handoff.spec.ts` (or shared teardown util in `e2e/helpers/seamCampaign.ts`).
  - Acceptance: after a green run, the created campaign, encounter, and forces are absent from the API; teardown failures surface as test errors, not silence.
- [x] 3.4 QA — the new spec green 3× consecutively at `--project=chromium --workers=1`; `git diff` confirms zero edits to the audit specs and zero non-additive product edits.

## 4. Seed-dependent determinism (seam c + recovery continuation) — GATED: after W1 implementation merges

- [x] 4.0 **Gate check (STOP on failure)**: verify on the current tree that the W1 implementation has landed — `IGameConfig` has a `seed` field (`src/types/gameplay/GameSessionUnitTypes.ts`), `pre-battle.tsx` parses `?seed=N` into the launch hooks (W1 task 4.1), and `fromSessionAsync` re-seeds from `config.seed` (W1 task 2.1). If any is absent, STOP this group and report — do not stub, shim, or partially implement W1's contract here.
  - Files: read-only grep.
  - Acceptance: a written gate verdict naming the three verified contracts (or the STOP report).
- [x] 4.1 Create `e2e/seam-fresh-construction-no-instant-defeat.spec.ts`: materialize ONE fresh NvN encounter via the group-3 helpers with a roster whose player force fields ≥2 units sharing one canonical `unitRef` (design D3 — the per-instance id path must be exercised player-side, not left to opponent-selection data); `goto('/gameplay/encounters/:id/pre-battle?seed=1337')` and launch; HARD assertions (spec scenario "Seeded launch constructs a full battle with distinct unit identities"): 2N distinct per-instance unit identities counted by distinct ids from `unit-token-${id}` / `rail-unit-${id}` testids (never DOM node totals); advance out of Initiative and through ≥2 further rounds, asserting after each advance no terminal outcome absent justifying events and both sides ≥1 alive unit (spec scenario "Initiative advance does not instant-defeat"); then a second independent page load of the SAME encounter with the SAME `?seed=1337`, identical advance sequence, asserting per-round outcome markers (phase, status, alive counts per side) equal between the two runs — equality between runs only, no pinned literals (spec scenario "Same seed twice yields equal outcome markers"). Reuse group-3 teardown for the created rows.
  - Files: new `e2e/seam-fresh-construction-no-instant-defeat.spec.ts`, `e2e/helpers/seamCampaign.ts`.
  - Relaunch-navigation gotcha: the second same-seed load navigates away from an active interactive battle — `useGameSessionLifecycle` registers a `beforeunload` handler for active battles (`GameSessionPage.lifecycle.ts`), so with sticky user activation the `goto` can hang on the unload dialog. Either run the second load in a fresh browser context, or register `page.on('dialog', d => d.accept())` before the relaunch navigation — the precedent is `e2e/active-session-recovery.spec.ts` (its dialog-accept handler).
  - Acceptance: the spec fails on N-not-2N collapse, on an unjustified terminal outcome within the driven rounds, and on same-seed marker divergence; no golden values anywhere in the file.
- [ ] 4.2 Recovery continuation determinism at the route level (spec scenario "Seeded recovered continuations are deterministic at the route level"): extend `e2e/active-session-recovery.spec.ts` with a test that seeds a match log whose `GameCreated` config persists a finite `seed` (location per 1.2's note), recovers it through TWO independent cold page loads, drives each recovered session through an identical short continuation (e.g. one phase advance), and asserts the two normalized post-recovery event streams equal each other (normalize wall-clock timestamps and generated ids — the W1 normalizer contract). Run-to-run equality ONLY: no roll value, outcome, or uninterrupted-run claim pinned (the "Recovery is re-seed, not continuation" scenario in W1's delta forbids it).
  - Files: `e2e/active-session-recovery.spec.ts`, `e2e/helpers/matchLogSeeding.ts`.
  - Acceptance: seedless seeded-log recovery tests from group 2 still green (legacy path untouched); the new test green; a divergence here is a W1 wiring bug to report, never a normalizer to widen.
- [ ] 4.3 QA — both group-4 specs/tests green 3× consecutively at `--project=chromium --workers=1`.

## 5. Registration and scoping

- [x] 5.1 Add `verify:qc:seam-anchors` to `package.json` following the `verify:qc:replay-recovery` precedent (`package.json:181`): targeted jests first (`npm.cmd test -- --watchAll=false --runTestsByPath src/engine/__tests__/InteractiveSession.recovery.test.ts src/__tests__/integration/campaignMissionEncounterLaunchIntegrity.test.ts`), then the three anchor specs sequentially via `node scripts/playwright/run-playwright.mjs test --project=chromium <spec> --workers=1`.
  - Files: `package.json`.
  - Acceptance: the lane runs end-to-end green from a clean shell; it is NOT added to `verify:qc`'s chain or any CI workflow (W6 scope).
- [x] 5.2 Add the three anchor spec files to the `testIgnore` arrays of the Mobile Chrome, Tablet Portrait, and Tablet Landscape projects (`playwright.config.ts:102/117/130`), mirroring the two existing audit-spec exclusions. Note the intended behavior change: `active-session-recovery.spec.ts` stops running under the responsive projects (design D7).
  - Files: `playwright.config.ts`.
  - Acceptance: `npx playwright test --list <each anchor spec>` schedules tests under chromium ONLY (spec scenario "Anchors are excluded from viewport multiplication"). Also confirm no anchor spec title carries the `@smoke` tag — the smoke project greps `/@smoke/` on Desktop Chrome and would double-schedule a tagged anchor.
- [x] 5.3 Guard sweep: confirm `requiredJourneyIds` in `scripts/qc/journey-qc-core.mjs` is untouched (exactly-seven contract) and `npm run qc:openspec-ci:validate` passes with this change's ledger entry accounted.
  - Files: read-only + validator run.
  - Acceptance: both validators green; no catalog or validation-graph edits present in the diff.

## 6. Verification battery

- [x] 6.1 `npm run typecheck && npm run lint && npm run format:check` clean across touched files.
- [x] 6.2 Targeted regression jests green: `InteractiveSession.recovery.test.ts` and `campaignMissionEncounterLaunchIntegrity.test.ts` (the two suites the lane fronts).
- [x] 6.3 Flake-resistance proof (spec scenario "Anchor stability is proven by repetition"): each of the three anchor specs run 3× consecutively green locally at `--project=chromium --workers=1`; record the three-run evidence in the PR description.
- [x] 6.4 `npm run verify:qc:seam-anchors` green end-to-end; `npm run verify:qc:replay-recovery` still green (the recovery anchor serves both lanes).
- [x] 6.5 Fidelity diff (design R6): the MODIFIED requirement's three untouched scenarios in `specs/journey-qc/spec.md` remain byte-identical to canonical `openspec/specs/journey-qc/spec.md:249-266`; only the requirement paragraph and the readiness scenario differ.
- [ ] 6.6 `npx openspec validate add-seam-trust-anchor-journeys --strict` passes; spec-delta requirements carry backing evidence from 2.2/2.3 (recovery anchor + revert-proof), 3.2-3.4 (handoff anchor + cleanup), and 4.1-4.3 (construction anchor + route-level recover-twice).
