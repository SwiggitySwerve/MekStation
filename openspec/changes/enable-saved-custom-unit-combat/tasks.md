## Implementation and acceptance

- [x] Define strict supported-construction projection, server reader, custom combat catalog endpoint, and saved-design picker merge.
  - Receipt (2026-09-15, FLIP): PROVEN at exact main `4d7d17c3def5aa1989bd4819654f3eeec1fcc400` (the merge commit of PR #1713) by the consolidated S6 receipt `openspec/planning/2026-09-12-roadmap-completion/evidence/r2-combat-s6-consolidated-20260915.json`, which records no residual for this row. The strict projection and exact-identity rejection, the picker merge preferring server metadata for the same exact id, and the eligibility refusals for missing construction / unsupported chassis topology are pinned by `customCombatDefinition.test.ts`, `listCampaignSavedDesigns.test.ts` and `customCombatDefinition.integration.test.ts` in that receipt's single 13-suite / 69-test jest invocation (exit 0), and the catalog endpoint plus the picker merge are additionally proven end to end against the hydrated standalone production server (live GET `/api/units/custom/combat-catalog` returns the saved design ref; the wizard picker adds it). Owner-visible design note carried, NOT a gap against this text: `src/pages/api/units/custom/combat-catalog.ts` adds no auth and the delta spec does not require any.
- [ ] Adapt exact custom references and carry their immutable snapshot through seeded setup and GameCreated.
  - Progress (2026-09-15, PARTIAL - box stays OPEN): PROVEN-WITH-RESIDUAL at exact main `4d7d17c3def5aa1989bd4819654f3eeec1fcc400` (the merge commit of PR #1713) per `openspec/planning/2026-09-12-roadmap-completion/evidence/r2-combat-s6-consolidated-20260915.json`. The single-player half of this row is directly asserted - a detached snapshot is copied onto `GameCreated` units with the roster-instance id kept distinct from `unitRef`, a construction snapshot attached to a canonical unit reference is refused, and the browser proof re-asserts the distinct roster-instance id in the live page and again in the server readback. The residual is a clause of this box, which is why it does not flip: CO-OP HAPPY-PATH SNAPSHOT CARRIAGE IS STRUCTURAL, NOT ASSERTED - co-op's inheritance of snapshot-carrying seeding rests on `EncounterService` being the single materializer, and no test in the 13-file matrix asserts that a valid co-op seed carries the immutable snapshot onto `GameCreated`. The S5 work (#1712 / #1713) does not close it: it proves co-op REJECTS malformed / foreign / stale / missing custom-source references, which is row 7's denial concern, not a positive carriage assertion. Severity recorded low: an unproven inheritance, not observed misbehaviour.
- [ ] Recover and rewind from validated recorded construction; wire server readers and preserve canonical behavior.
  - Progress (2026-09-15, PARTIAL - box stays OPEN): PROVEN-WITH-RESIDUAL at exact main `4d7d17c3def5aa1989bd4819654f3eeec1fcc400` (the merge commit of PR #1713) per `openspec/planning/2026-09-12-roadmap-completion/evidence/r2-combat-s6-consolidated-20260915.json`. "Recover" is thoroughly proven - `customCombatRecovery.durableAuthority.test.ts` recovers the original construction against REAL SQLite through `DurableMatchStore` after a store reopen and a library edit/delete and refuses a present invalid recorded snapshot; `customCombatRecovery.recordedSnapshot.test.ts` refuses a present invalid snapshot without a live-lookup fallback, refuses a snapshot whose identity is not the source `unitRef`, and never silently skips a custom unit missing both snapshot and definition; canonical behaviour is preserved by `InteractiveSession.recovery.test.ts`. The residual is the box's own "rewind" clause and it has ZERO named coverage, grep-verified at this SHA: `rewind` appears in none of the four recovery/replay suites, and the intersection of src test files containing `rewind` with those containing `customConstruction|recordedConstruction|CustomCombatSnapshot` is EMPTY - `ServerMatchHostRewindRebuild.ts` is in this change's own production inventory but no test binds rewind to recorded custom construction. Severity recorded moderate (a literal clause with no coverage, newly surfaced by the S6 slice). Held rather than checked, now as an owner decision (OD-rewind-branch-reader, 2026-09-15, `evidence/r2-combat-residual-rows-1726-main-proof-20260915.json`): the rewind clause is structurally unreachable for any match stream on the current head, not merely uncovered - the journal mirror appends onto the baseline branch id while the match-store segment reader serves only `root`, so an activated rewind branch throws in `tryFoldActivatedRewindBranch` and the match lands in `recoverActiveMatches` failures (reproduced with a unit-free probe, not committed). This belongs to `adopt-combat-journal-cutover-and-gm-rewind` and needs a product change there either way; independently re-verified by the reviewer against all five cited files/lines (`evidence/r2-combat-residual-rows-review-20260915.json`, verdict APPROVE, no required edits).
- [x] Extend replay gating and verify private construction redaction for owners, opponents, spectators, and recovery streams.
  - Receipt (2026-09-15, FLIP): owners, opponents and spectators were already directly asserted at exact main `4d7d17c3def5aa1989bd4819654f3eeec1fcc400` per `openspec/planning/2026-09-12-roadmap-completion/evidence/r2-combat-s6-consolidated-20260915.json` (`customConstructionVisibility.test.ts` redacts both snapshots for spectator and unknown audiences and preserves the full-visibility contract when fog is off; `customConstructionTransport.test.ts` keeps custom construction private through live, replay and reconnect publication; `CustomConstructionReplay.test.ts` covers the replay gating). The fourth stream this box names, RECOVERY STREAMS, is now closed: PR #1726 `8c8f8221c4c1882f671d8fad7c2386ef9d10ca44` (exact-main proof `evidence/r2-combat-residual-rows-1726-main-proof-20260915.json`) adds `customCombatRecovery.recoveryDelivery.test.ts`, which launches two distinct custom saved designs through `ServerMatchHost.create` on a real `DurableMatchStore` (fog on, 1v1 plus a spectator seat), captures live per-viewer join-replay deliveries, deletes both library entries, closes and reopens the store/`SQLiteService`/`UnitRepository` on the same files, rebuilds the host via `recoverActiveMatches`, and asserts the recovered host's delivery to the same three viewers against the redacted literal first and the live delivery second. Mutation-proven and independently reproduced by the reviewer: removing the owner check in `fogOfWar.ts:131` fails the recovered-delivery literal, and replacing `snapshotOnlyReader(snapshot)` with `readCustom` in `InteractiveSession.recovery.ts` leaves `recovered.failed` non-empty. Gates: scoped jest `src/lib/multiplayer/server` + `src/engine` 180 suites / 1523 tests, tsc, oxfmt, openspec strict 232/0, pre-commit build ok; independent review verdict APPROVE, no required edits (`evidence/r2-combat-residual-rows-review-20260915.json`). Non-claim carried: the row covers the join-replay channel, not the recovered host's live broadcast channel.
- [x] Wire shared catalog admission and campaign consumers; reject malformed, unsupported, missing, or mismatched references before materialization.
  - Receipt (2026-09-15, FLIP): PROVEN at exact main `4d7d17c3def5aa1989bd4819654f3eeec1fcc400` (the merge commit of PR #1713). The co-op launch revalidates custom-source authority before composition (S5 PR #1712 `3323b6995051fe660794f854dd1174ac55805321`, exact-main proof `evidence/r2-combat-s5-1712-main-proof-20260915.json`, blob-identical, 15 suites / 255 tests) and the co-op snapshot denials are pinned at the unit level (S5 follow-up PR #1713 `4d7d17c3def5aa1989bd4819654f3eeec1fcc400`, exact-main proof `evidence/r2-combat-s5-rows-1713-main-proof-20260915.json`, blob-identical, all gates exit 0); across both reviews 11 mutations were run with no survivors, M3 revealing the `matchId` clause had zero prior coverage, and the follow-up review verdict is "taskLine7 PROVEN for co-op". Shared catalog admission and the campaign consumers are covered by `canonicalCatalogAdmission.test.ts`, `useCombatCatalog.test.tsx` and `listCampaignSavedDesigns.test.ts`, with the campaign-create roster step admitting the catalog-eligible design and persisting it through PUT/GET in the browser proof. Three reviewed-and-accepted residuals carried, none of them a missing proof of this text: (1) "stale" and "revision-mismatched" share one product branch and therefore one denial code, so the four adjectives map to three codes - a product shape, not a coverage hole; (2) browser rows 1 and 2 remain branch duplicates, renamed by OE-1 rather than repointed; (3) OE-2 - the browser rows prove rejection "before encounter launch" directly and "before composition" only structurally, which is exactly what the unit-level rows added by #1713 assert literally.
- [x] Verify focused tests, persisted SQLite reopen/recovery after library mutation, browser handoff with server readback, and required static/build/spec checks.
  - Receipt (2026-09-15, FLIP): PROVEN at exact main `4d7d17c3def5aa1989bd4819654f3eeec1fcc400` (the merge commit of PR #1713); the consolidated S6 artefact `openspec/planning/2026-09-12-roadmap-completion/evidence/r2-combat-s6-consolidated-20260915.json` IS this row's evidence. Focused tests: one `--runTestsByPath` jest invocation, 13 suites / 69 tests, exit 0 (`gate-jest-matrix.log`, sha256 `ab25fc78...`). Persisted SQLite reopen/recovery after library mutation: `customCombatRecovery.durableAuthority.test.ts` against real `DurableMatchStore`, PASS. Browser handoff with server readback: `e2e/saved-custom-combat.spec.ts`, 1 passed, retries 0, first attempt, against the hydrated standalone production server on 127.0.0.1:3639, with the `GET /api/campaigns/:id` readback byte-equal before AND after a cold reload (sha256 `3cc554ac...`). Required static/build/spec checks: typecheck 0 (`d140a6ce...`), lint 0 errors / 84 pre-existing repo-wide `max-lines` warnings (`032cd525...`), format:check 0 (`75b4aa1e...`), build exit 0 with BUILD_ID 1789482231591 and the standalone server hydrated (`747d25ed...`), `openspec validate --all --strict` 232 passed / 0 failed (`a6f1b01b...`). Two declared boundaries carried rather than resolved: (a) BROWSER SCOPE - the spec proves persistence, catalog admission, picker, identity separation, Mech Bay and cold reload with server readback but NOT a production COMBAT LAUNCH or terminal/post-battle flow, which the admission brief assigns to R2.combat-browser and which is outside this row's text; (b) BUILD ENV, corrected 2026-09-15: the "one build served both" framing is now stale - R2.combat-browser's production combat-launch proof, PR #1731 `392c207de703049565b70ca6fe17005aca8f0641` (exact-main proof `evidence/r2-combat-browser-launch-1731-main-proof-20260915.json`), rebuilt independently at the merge commit (`NEXT_PUBLIC_E2E_MODE=true NEXT_PUBLIC_E2E_TEST=true npm run build`, exit 0, BUILD_ID `1789488081479`, distinct from this row's own BUILD_ID `1789482231591`) rather than sharing this row's build artefact. The residual that remains open, unchanged: every build proven on main for either row still carries the E2E public flags, and a flag-free vanilla production build has still never been run separately. REPORT.md's self-reported 21-scenario / 88-assertion aggregate is NOT cited as evidence for this row.

Acceptance evidence is recorded separately; these checkboxes remain unchanged until explicitly requested. Requested and reconciled 2026-09-15 against merged exact-main receipts, at exact main `4d7d17c3def5aa1989bd4819654f3eeec1fcc400` (the merge commit of PR #1713): three rows flipped and three held with the residual named on the row. The S6 receipt proposed CHECK-WITH-NOTE for the three held rows; they are held rather than checked because each residual is a clause of the row's own literal text (co-op snapshot carriage, "rewind", "recovery streams") rather than a boundary outside it. Reconciled again 2026-09-15 (batch 3) against PRs #1726 and #1731, merged after the above SHA: the "recovery streams" clause is now covered (PR #1726) and its row flipped, so four rows are flipped and two held; the "rewind" residual on the recover/rewind row is now recorded as owner decision OD-rewind-branch-reader rather than an open coverage gap, and the verification row's build-env note is corrected to reflect that R2.combat-browser (PR #1731) rebuilt independently rather than sharing this row's build.
