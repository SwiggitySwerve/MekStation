# Tasks: polish-command-surfaces-wave3

> Execution is **Codex-first and strictly sequential** — parallel agent fan-out has failed on this repo. The three item groups are independent and may land in any order, but run one at a time; each lands with its own verification before the next. Claude orchestrates, packages context, and final-reviews. Pre-commit runs a full `next build` (~3–5 min) — commit via background shell. After the spec-delta edits, run `oxfmt` + `openspec validate --strict` (formatter/oxfmt quote race, R4). These are **display / UX-layer** changes only — no store, pipeline, or transport edits.

## 1. GM-TIME-CASCADE — per-day breakdown in the day report (day-progression)

- [ ] 1.1 Extract the existing single-day body of `DayReportPanel` (costs grid / healed-personnel list / turnover departures / expired contracts) into a `DayReportDayBody` subcomponent, unchanged for the single-day render path.
  - Files: `src/components/campaign/DayReportPanel.tsx`
  - Acceptance: single-day (`reports.length === 1`) output is byte-for-byte equivalent to today; no store/hook/pipeline change.
- [ ] 1.2 In the `isMultiDay` branch, keep the aggregate totals header **verbatim** — the string is `` `Day Report — ${reports.length} days processed` `` (em dash; existing tests and the delta scenarios depend on it) — AND render a collapsible per-day list mapping over `reports`, each row rendering `DayReportDayBody` for that day. **Key each row by `report.date.toISOString()`** — `DayReport.date` is a `Date` object, and a `Date` used directly as a React key coerces to a locale string (unstable across environments), so call `.toISOString()`. Compress or skip days with no events (no costs, no healed personnel, no expired contracts, no turnover) so a 30-day advance does not render a section per empty day; default per-day rows to collapsed.
  - Files: `src/components/campaign/DayReportPanel.tsx`
  - Acceptance: a multi-day report shows the aggregate header plus per-day sections carrying each day's date and its own event lists; event-less days are compressed; the aggregate totals are retained.
- [ ] 1.3 Add / extend a `DayReportPanel` component test with a multi-day case (e.g. a 3-day report where days 1 and 3 have events and day 2 is empty) asserting: the aggregate header still renders, per-day sections appear for the event days, and the event-less day is compressed. Update any existing test that asserts the aggregate-only shape.
  - Files: `src/components/campaign/__tests__/DayReportPanel.test.tsx` (new if absent), and `src/components/gameplay/pages/campaigns/dashboard/__tests__/CampaignDashboardPage.reactivity.test.tsx` if it asserts panel text.
  - QA: `npm.cmd test -- --watchAll=false --runTestsByPath src/components/campaign/__tests__/DayReportPanel.test.tsx --runInBand` green; `npm run typecheck` clean.

## 2. ForceCard custom-unit names (unit-services + force-management)

- [ ] 2.1 Add `getAllUnits(): readonly IUnitIndexEntry[]` and `getUnitById(id: string): IUnitIndexEntry | undefined` to `IUnitSearchService` / `UnitSearchService`, returning entries from the merged canonical + custom map already built in `initialize()` (do not add a second fetch/merge). **Both accessors MUST gate on the private `this.initialized` flag** — the private `allUnits` map fills *incrementally* during `doInitialize()` (canonical then custom) before the flag flips at `UnitSearchService.ts:114`, and `rebuildIndex()` resets the flag — so a caller during init or rebuild would otherwise read a partial map. Return an empty result (`[]` / `undefined`) until `this.initialized === true`, matching the delta's "reflects the merged index only after initialization" scenario.
  - Files: `src/services/units/UnitSearchService.ts`
  - Acceptance: after `initialize()`, `getAllUnits()` includes both canonical and `custom-*` entries; `getUnitById('custom-…')` returns the entry with its `${chassis} ${variant}` name; while `this.initialized` is false (pre-init or mid-`rebuildIndex`) the accessors return empty, not a partial map and not throwing.
- [ ] 2.2 Resolve assigned unit names through the merged lookup in the two force-display surfaces: `ForceDetailPage` seeds `allUnits` (and the derived `unitMap` / `availableUnits`) from the merged list instead of the canonical index only; the pre-battle force-card name hook (`useCanonicalUnitNameById` in `PreBattlePage.sections.tsx`) resolves against the merged source. Keep the pre-battle hook's capture-tolerant swallow so a custom-unit API failure degrades to the existing `Slot N` placeholder rather than crashing.
  - **`ForceDetailPage` degrade decision (required):** `customUnitApiService.list()` **throws** on a non-ok response (`CustomUnitApiService.ts:117-119`), so `unitSearchService.initialize()` **rejects** when the custom-unit API is down. `ForceDetailPage`'s init effect (`ForceDetailPage.tsx:69-80`) currently has **no catch**, so a rejection would leave the page hung on `ForceLoadingState` forever. Wrap the merged-list seeding so an `initialize()` rejection **falls back to the current canonical-index path** (`getCanonicalUnitService().getIndex()`, `ForceDetailPage.tsx:73-76`) and still calls `setIsInitialized(true)`. This is a page-local resilience wrap only — **do NOT change `UnitSearchService`'s own failure semantics** (`initialize()` still rejects; task 2.1 is untouched).
  - Files: `src/components/gameplay/pages/forces/detail/ForceDetailPage.tsx`, `src/components/gameplay/pages/PreBattlePage.sections.tsx`
  - Acceptance: an assigned `custom-*` unit renders its chassis + variant name on the pre-battle force card and renders as assigned (not an empty `+ Select Unit` slot) on the force detail page; canonical-unit behavior is unchanged; when the custom-unit API is down, `ForceDetailPage` renders (canonical-only names) instead of hanging on the loading state.
- [ ] 2.3 Extend the `UnitSearchService` test with the merged-accessor cases, and add a jest test asserting a `custom-*` assignment resolves its chassis + variant name in both surfaces and appears in `availableUnits`. Add a `ForceDetailPage` degrade assertion: when `unitSearchService.initialize()` rejects (custom-unit API down), the page still renders (canonical-only) rather than hanging on `ForceLoadingState`.
  - Files: `src/__tests__/service/units/UnitSearchService.test.ts`, plus a force-display resolution test (new, colocated with the touched surface).
  - QA: `npm.cmd test -- --watchAll=false --runTestsByPath src/__tests__/service/units/UnitSearchService.test.ts --runInBand` green; `npm run typecheck` clean.

## 3. M5 co-op onboarding affordances (coop-campaign-sync)

- [ ] 3.1 Add a static skip-notice under the Create Co-op button in `CampaignCoopEntryPanel`: copy describing the applied defaults (Mercenary faction, Standard preset, empty roster) and that name/preset/roster can be configured from the campaign dashboard after creation. Keep existing data-testids (`create-coop-campaign-btn`) stable.
  - Files: `src/pages-modules/gameplay/campaigns/CampaignCoopEntryPanel.tsx`
  - Acceptance: the notice renders adjacent to the create control on the co-op entry surface.
- [ ] 3.2 On a vault-identity (token-mint) failure, append a link to `/settings#vault` ("Set up your vault identity in Settings") to the error notice in both the create-error and join-error paths. Gate the link to the token-mint step only — invite-lookup 404s and match-POST failures keep the existing generic copy.
  - **Gating mechanism (required — structural, NOT message-matching):** today both handlers funnel mint / invite-lookup / match-POST failures into one `catch` that stores only `e.message` (`CampaignCoopEntryPanel.tsx:210-214` create, `:293-298` join), so the failing step is not recoverable at render. Distinguish the mint step **structurally**: either have `mintToken` throw a **typed error** (e.g. `error.name === 'VaultIdentityError'`) that the catch inspects, or wrap the mint call in its own try/catch that sets an `errorKind: 'vault-identity' | 'generic'` state. Render the link only when the recorded kind is the identity step. **Do NOT gate on error-message string matching** (`e.message.includes('token')` etc.) — brittle and forbidden here.
  - **Testid (required):** the real create-error element is **`create-coop-unavailable`** (`CampaignCoopEntryPanel.tsx:327`, asserted by deep-play at `e2e/ux-deep-play-audit.spec.ts:726,738`) — keep it stable; **do NOT invent `create-coop-error`.** Keep `join-coop-error` stable too.
  - **Pin:** the empty-password early-returns (`CampaignCoopEntryPanel.tsx:147-150` create, `:251-254` join) are **NOT** mint failures — they must render generic copy with no vault link.
  - Files: `src/pages-modules/gameplay/campaigns/CampaignCoopEntryPanel.tsx`
  - Acceptance: a mint failure shows the vault-settings link; a non-identity error (invite lookup, match POST, empty password) shows generic copy with no link.
- [ ] 3.3 Add a `CampaignCoopEntryPanel` test covering: skip-notice present under the create button; vault link appears on a mint failure; vault link absent on a non-identity (invite/match-POST) failure.
  - Files: `src/pages-modules/gameplay/campaigns/__tests__/CampaignCoopEntryPanel.test.tsx` (new)
  - QA: `npm.cmd test -- --watchAll=false --runTestsByPath src/pages-modules/gameplay/campaigns/__tests__/CampaignCoopEntryPanel.test.tsx --runInBand` green; `npm run typecheck` clean.

## 4. Verification

- [ ] 4.1 Typecheck + lint + format clean across touched files: `npm run typecheck && npm run lint && npm run format:check`.
- [ ] 4.2 Per-item jest green (the three group QA commands above): `DayReportPanel` multi-day, `UnitSearchService` merged accessor + force-display resolution, `CampaignCoopEntryPanel` onboarding.
- [ ] 4.3 GM-surfaces walkthrough re-run acceptance: `npm run qc:ux-audit` — the day-report / GM and co-op surfaces render the new per-day breakdown, custom-unit names, and onboarding copy with no screen-fit / clickability regression; the deep-play journey testids stay stable.
- [ ] 4.4 `npx openspec validate polish-command-surfaces-wave3 --strict` passes; the four delta requirements (`day-progression` MODIFIED, `unit-services` ADDED, `force-management` ADDED, `coop-campaign-sync` ADDED) each carry backing test/QA evidence from tasks 1–3.
