# Tasks: add-viewport-layout-sweep

> Execution is **Codex-first and strictly sequential** — parallel agent fan-out has failed on this repo. One group at a time; each lands with its verification before the next. Claude orchestrates, packages context, and final-reviews. Pre-commit runs a full `next build` (~3–5 min) — commit via background shell. **Non-goals are load-bearing: modify NO production `src/` file (zero-production-touch — `src/constants/layout.ts` is imported, never edited; overlap opt-outs are inventory allowlist entries, never production attributes); NO screenshot-diff assertions anywhere in the sweep; NO PR-gate or required-check wiring (`verify:qc:viewport-sweep` stays out of `verify:qc`'s chain and all workflows); NO runtime `test.skip`-on-missing-dependency guards (design D7 — the pack gate is authoring-time STOP, and `e2e-testing`'s no-skip scenario binds the sweep); NO store injection or hand-seeded state for pack-seeded screens (front-door loaders only); NO edits to the seven-journey catalog (`requiredJourneyIds` stays exactly seven), to anchor specs' assertions, or to the in-flight `add-scenario-packs` change folder; NO layout-bug fixes beyond D5 quarantine bookkeeping.** File paths and line numbers below were verified on `claude/viewport-layout-sweep@428f63aa9` — re-verify against the then-current tree before editing (the W4 implementation may have landed in between; design R1). After any spec-delta edit: formatter, then `npx openspec validate add-viewport-layout-sweep --strict` (design R4). Ledger registration ships with this declarative PR, not with these implementation tasks. Estimated total: ~2 dev-days (groups 1–4) + a short gated follow-on (group 5).

## 1. Ground-truth re-verification (read-only)

- [x] 1.1 Re-verify the project matrix and record the pre-change baseline: the six project blocks (`playwright.config.ts:92-203`) including each `testIgnore` array's current contents (W4's implementation may have added `'**/scenario-packs/**'` globs — record which state the tree is in, design R1); the scheduled-count baseline via `npx playwright test --list` (default run) and `--list --project=<name>` for chromium, Mobile Chrome, Tablet Portrait, Tablet Landscape, and smoke (baseline on the authoring tree: 2984 default = 712 + 697×3 + 181), plus `MEKSTATION_FLOW_ID=list-check npx playwright test --list --project=flow-audit`.
  - Files: read-only.
  - Acceptance: a written baseline note with the per-project counts, the default-run total, and the verbatim `testIgnore` arrays — this is the "before" half of task 4's proof.
- [x] 1.2 Re-verify every project-selecting consumer (the design D2 inventory) on the then-current tree: grep `--project` and `playwright test` across `.github/workflows/*.yml`, `package.json`, and `scripts/qc/*.mjs`; confirm the only unflagged invocations remain `test:e2e`/`test:e2e:ui`/`test:e2e:headed`/`test:e2e:debug`, and that zero hits reference `Mobile Chrome`, `Tablet Portrait`, `Tablet Landscape`, or `smoke` by name.
  - Files: read-only.
  - Acceptance: a written consumer table (site → invocation → project) matching design D2, or a STOP-and-reconcile note if a new consumer has appeared since authoring.
- [x] 1.3 Re-verify the sweep inputs: the helper promotion sources and their current line ranges (`coop-campaign-ui-audit.spec.ts` `expectNoHorizontalOverflow`/`expectClickable`; `tactical-map-visual-smoke.spec.ts` `expectNonBlankRender` + `measureRenderedContrast` + its 7 call sites); `BREAKPOINTS` (`src/constants/layout.ts:63-74`) and the `flow-audits.spec.ts:37,82-94` import precedent; `e2e/app-shell-route-manifest.json`'s route arithmetic (28+2+34+5=69 on the authoring tree); the touch/viewport self-scoping of all 7 `hasTouch`/`isMobile`/`tap`-referencing e2e files (design D3's evidence); and audit `useDeviceType`/`matchMedia`/`responsive.ts` consumers for mount-bound (non-reactive) viewport branching — the input to design D5's `remountPerViewport` flags.
  - Files: read-only.
  - Acceptance: a written note naming the helper source ranges, the manifest arithmetic, the touch-consumer verdict per file, and the list of screens (if any) needing `remountPerViewport: true`.

## 2. Shared layout helpers (`e2e/helpers/layout.ts`) + self-test

- [ ] 2.1 Create `e2e/helpers/layout.ts`: move `expectNoHorizontalOverflow(page, label)` and `expectClickable(locator, label)` near-verbatim from `coop-campaign-ui-audit.spec.ts` (same thresholds: +1px scroll tolerance, up-to-8-offender reporting, 32×32px box floor, 20s expect timeouts); move `expectNonBlankRender` from `tactical-map-visual-smoke.spec.ts` with the signature generalized to `(locator, label)` (the hardcoded `page.getByTestId('hex-map-container')` becomes the caller's argument) together with `measureRenderedContrast` and its `sharp` import; implement the net-new `expectNoOverlap(targets, label, { tolerance = 1, allowedOverlaps = [] })` per design D6 (pairwise AABB disjointness on `boundingBox()` rects, ancestor-descendant pairs excluded structurally, failure naming both elements and their boxes; a passed target resolving to no visible element fails naming the missing target — the helper never visibility-filters, per designs D6/D9: per-viewport exclusions are inventory applicability data, not helper behavior); export the `SWEEP_VIEWPORTS` constant per design D4 (375×667 literal with the below-SM doc comment; `BREAKPOINTS.MD`/`LG`/`XL` imported from `../../src/constants/layout`, heights 1024/768/720).
  - Files: `e2e/helpers/layout.ts` (new).
  - Acceptance: `npm run typecheck` clean; no import from `src/components/**` (the `LayoutValidator.boxesOverlap` math is reimplemented, never imported — grep proves no cross-domain import); `SWEEP_VIEWPORTS` widths bind MD/LG/XL by import (grep shows no hardcoded 768/1024/1280 in the constant).
- [ ] 2.2 Rewire the promotion sources: `coop-campaign-ui-audit.spec.ts` deletes its local `expectNoHorizontalOverflow`/`expectClickable` and imports from `./helpers/layout`; `tactical-map-visual-smoke.spec.ts` deletes local `expectNonBlankRender`/`measureRenderedContrast` and updates its 7 call sites to pass `page.getByTestId('hex-map-container')` explicitly.
  - Files: `e2e/coop-campaign-ui-audit.spec.ts`, `e2e/tactical-map-visual-smoke.spec.ts`.
  - Acceptance: both specs pass under `--project=chromium --workers=1` (behavior-identical promotion — same assertions, same messages); repo-wide grep shows exactly one definition of each helper name.
- [ ] 2.3 Author the helper self-test spec `e2e/layout-sweep/layout-helpers.selftest.spec.ts` using `page.setContent` synthetic fixtures (no app routes, no server state): overflow true-positive (element wider than `innerWidth` fails naming the offender) and full-bleed-fixed pass; overlap true-positive (two absolutely-positioned overlapping siblings fail naming both), containment pass (parent+child not flagged), 1px-abutting pass (no ε flap), `allowedOverlaps` pass, a declared-but-hidden target failure (`display:none` target fails naming the missing target — the no-silent-filter rule, designs D6/D9), and a realistic flex-wrap fixture pass; clickable failure on a 16×16px target and on a hidden target; non-blank-render failure on a flat-color canvas fixture and pass on a multi-color one.
  - Files: `e2e/layout-sweep/layout-helpers.selftest.spec.ts` (new).
  - Acceptance: self-test green 3× consecutively at `--project=chromium --workers=1` — this is design D6's false-positive budget made mechanical.

## 3. Screen inventory + standalone sweep

- [ ] 3.1 Create `e2e/layout-sweep/screenInventory.ts` per designs D5/D9/D10: import `e2e/app-shell-route-manifest.json`; classify every route/pattern as `standalone` (28) | `recovery` (2) | `static-catalog` (3, with concrete slugs — the `compendium.spec.ts` `/compendium/rules/structure` precedent) | `pack-seeded` (18, each naming its pack and navigation mechanism: navigation-pack for the 17 campaign subroutes — 16 direct goto, the launch screen flagged for in-page discovery per design D10 — combat pack for `/gameplay/games/[id]`) | `excluded` (13, each with a non-empty reason — including `/gameplay/pilots/[id]` with design D10's contract-gap reason and its two documented unlock paths — and, where an existing seeder precedent applies, a follow-up note); per swept entry declare the goto target and per-viewport check targets per design D9: primary-affordance locator(s) with viewport applicability (every swept screen declares at least one applicable primary affordance at each of the four sweep viewports — the shared app-shell chrome group with its hamburger/tablet/desktop variants from the verified TopBar breakpoint profile is defined once and referenced, never re-declared per screen), overlap check-target group (default: the route's *unconditional* `app-shell-route-proof` affordance set filtered through viewport applicability — `stateAffordances` never default in; a state-conditional target is declared explicitly naming the deterministic state it assumes), optional canvas locator, optional `remountPerViewport`, and `quarantine` entries (initially empty).
  - Files: `e2e/layout-sweep/screenInventory.ts` (new).
  - Acceptance: `npm run typecheck` clean; classification counts sum to the manifest's route arithmetic.
- [ ] 3.2 Author the inventory guard test (Playwright spec or a node-script jest-free check runnable inside the verify lane — implementer picks, but it must run in `verify:qc:viewport-sweep`): every manifest primary/recovery/delegated route has exactly one inventory classification; every `excluded` entry has a non-empty reason; every `quarantine` entry has non-empty `reason` and `followUp`; every swept entry declares at least one viewport-applicable primary affordance for each of the four sweep viewports (design D9); harness routes are auto-excluded.
  - Files: guard test (new, under `e2e/layout-sweep/`).
  - Acceptance: guard green; deleting any classification line, blanking a reason, or removing a viewport's last applicable primary affordance makes it fail naming the route (prove once, revert).
- [ ] 3.3 Author `e2e/layout-sweep/viewport-layout-sweep.spec.ts` for the swept-now classes (standalone + recovery + static-catalog, 33 screens): one `test()` per entry; `goto` once (or per-viewport when `remountPerViewport`), loop `SWEEP_VIEWPORTS` with `setViewportSize` → `expectNoHorizontalOverflow` → `expectNoOverlap` → `expectClickable` → `expectNonBlankRender` where declared, resolving the viewport-applicable target set from the inventory at each width (design D9: an applicable target resolving to no visible element fails naming target + viewport; the sweep never filters targets by runtime visibility; by-design responsive collapse is expressed as applicability data, never quarantined); honor quarantine entries as annotated, visible skips of the named assertion only. Populate initial quarantine entries for every real violation found, each with reason + follow-up reference (design R2 — the change lands green without layout fixes).
  - Files: `e2e/layout-sweep/viewport-layout-sweep.spec.ts` (new).
  - Acceptance: sweep green at `--project=chromium --workers=1`; no `@smoke` or `@mobile-touch` tag in any sweep/selftest title; wall-clock recorded and within ~2× of design D8's standalone estimate (~2.2 min single-worker warm) — a gross overshoot is investigated, not accepted.
- [ ] 3.4 Add `verify:qc:viewport-sweep` to `package.json`: the inventory guard, the helper self-test, and the sweep, sequentially, each via `node scripts/playwright/run-playwright.mjs test --project=chromium <spec> --workers=1` (the `verify:qc:app-shell` shape).
  - Files: `package.json`.
  - Acceptance: lane green end-to-end from a clean shell; NOT referenced by `verify:qc`'s chain, `verify`, or any workflow (grep proof).

## 4. Project-matrix rationalization (the D2 edit — proofs are the acceptance)

- [ ] 4.1 **Pre-deletion baseline (STOP on unexplained red)**: run the three responsive projects once each in full (`npx playwright test --project="Mobile Chrome"`, `--project="Tablet Portrait"`, `--project="Tablet Landscape"`). Any test failing under a responsive project while passing under chromium is triaged BEFORE deletion: fix it, tag it `@mobile-touch` (if genuinely touch/viewport-dependent), or document why it is safe to lose — design R9. An all-green (or fully-triaged) baseline is the precondition for 4.2.
  - Files: read-only (plus any triage outcomes).
  - Acceptance: a written baseline verdict per project (green, or itemized triage with resolutions).
- [ ] 4.2 Rewrite the projects array in `playwright.config.ts`: delete the `Mobile Chrome`, `Tablet Portrait`, `Tablet Landscape`, and `smoke` blocks (their `testIgnore` arrays go with them — including W4's `'**/scenario-packs/**'` globs if the W4 implementation landed first, design R1); add the thin `mobile-touch` project per design D3 (`grep: /@mobile-touch/`, Desktop Chrome device, 375×667, `hasTouch: true`, `isMobile: true`) with a comment stating the tag-then-scope contract and that anchors/deep-play/walkthrough/sweep/pack specs must never carry the tag; keep `chromium` and the env-gated `flow-audit` byte-identical; update the config header comment's tag/usage documentation (`@smoke` now documents `--grep @smoke --project=chromium` as the smoke invocation) and the stale project-name references in comments (`e2e/flow-audits.spec.ts` project-list comment).
  - Files: `playwright.config.ts`, comment-only edits in `e2e/flow-audits.spec.ts`.
  - Acceptance: config typechecks; `chromium` block diff shows no change beyond (if applicable) nothing at all.
- [ ] 4.3 Tag the existing touch describes `@mobile-touch`: `mobile-navigation.spec.ts` "Touch Interactions" describe and `tactical-map-visual-smoke.spec.ts` isometric pointer/touch describe append the tag to their titles; their `test.use({...})` blocks stay untouched (the tag is selection, not behavior — design D3).
  - Files: `e2e/mobile-navigation.spec.ts`, `e2e/tactical-map-visual-smoke.spec.ts` (title strings only).
  - Acceptance: `npx playwright test --list --project=mobile-touch` schedules exactly the tagged describes' tests and nothing else.
- [ ] 4.4 **The before/after `--list` count proof**: re-run the full listing set from 1.1 and record the after-table beside the before-table — (a) default `npx playwright test --list` total drops from the baseline (~2984) to chromium + sweep/selftest/guard additions + the mobile-touch tagged set (expected ~770; the exact number is recorded, not guessed); (b) `--list --project=chromium` equals the baseline chromium set plus exactly the new `e2e/layout-sweep/` tests; (c) `--list --project=mobile-touch` equals 4.3's tagged set; (d) `MEKSTATION_FLOW_ID=list-check --list --project=flow-audit` is byte-identical to baseline; (e) if the W4 implementation has landed, `npx playwright test --list e2e/scenario-packs/` still schedules under chromium only (re-proving W4's scoping scenario post-rewrite).
  - Files: read-only; proof table recorded in the PR description.
  - Acceptance: the recorded table, with every delta explained by an intended addition/deletion — one unexplained count change blocks the group.
- [ ] 4.5 **The CI/consumer grep proof**: re-run 1.2's grep across `.github/workflows/*.yml`, `package.json`, `scripts/qc/*.mjs` — every `--project=` reference resolves to a still-registered project (`chromium` | `flow-audit` | `mobile-touch`); zero references to the four deleted names remain anywhere in workflows/scripts/config (comments included); `pr-checks.yml` and `nightly-validation.yml` diffs are empty (no workflow file was touched).
  - Files: read-only; proof recorded in the PR description.
  - Acceptance: grep output recorded; any surviving stale reference blocks the group.

## 5. Pack-seeded sweep entries — GATED: after the W4 implementation merges

- [ ] 5.0 **Gate check (STOP on failure)**: verify on the current tree that the W4 implementation has landed — `e2e/helpers/scenarioPackLoading.ts` exists and exports `loadCampaignPack` and `loadEncounterPack` (W4 tasks 3.1/4.1 contracts), and the `navigation` and `combat` pilot packs exist in `e2e/scenario-packs/` with manifest entries (the experience pack is out of this change's scope — design D10). If any artifact is absent, STOP this group and report naming it — do not stub a loader, hand-seed campaign/match rows, or store-inject a substitute state (design D7; the 18 `pack-seeded` inventory entries simply remain visibly un-swept). Additionally verify design D10's discovery precondition: load the navigation pack once and confirm the missions screen surfaces the mission launch-briefing affordance — if it does not, reclassify the launch entry to `excluded` with that documented reason (D10's recorded fallback; a reclassification, not a STOP) and never reach the launch route via a constructed mission id, an uncontracted loader return value, or pack payload internals.
  - Files: read-only grep (plus, on the fallback path only, the inventory reclassification).
  - Acceptance: a written gate verdict naming the verified exports and packs plus the discovery-precondition verdict (or the STOP report).
- [ ] 5.1 Extend the sweep spec with the pack-seeded entries per designs D5/D9/D10: one `loadCampaignPack(navigation)` per worker amortized across the 17 campaign-subroute screens — the stamped campaign id read from the loader's post-navigation URL (the W4-spec-promised observable, design D5; never from a loader return value or pack payload internals), 16 subroutes by direct `goto`, and the launch screen by in-page discovery (goto the missions subroute, click the mission's launch-briefing affordance; never actuate the launch control itself; no encounter-link/replay check targets on pack-loaded mission history — design D10) — plus `loadEncounterPack(combat)` for `/gameplay/games/[id]`. All state enters through the W4 loaders exclusively; teardown rides the loaders' own row-tracking contract. Flip the 18 inventory entries from `pack-seeded` (un-swept) to swept, keeping the guard arithmetic green; quarantine any newly-revealed violations per D5.
  - Files: `e2e/layout-sweep/viewport-layout-sweep.spec.ts`, `e2e/layout-sweep/screenInventory.ts`.
  - Acceptance: full sweep (51 screens) green 3× consecutively at `--project=chromium --workers=1`; grep proves the sweep contains no direct store writes and no `indexedDB.open`/PUT of its own (loaders only), and reads no pack payload file from the sweep spec or inventory; wall-clock recorded against D8's ~4-min estimate.
- [ ] 5.2 QA — `npm run verify:qc:viewport-sweep` green end-to-end including the pack entries; one default-run (`npx playwright test`, parallel workers) observation of the sweep for contention flake (design R8) — flake here is triaged, never retried-away.

## 6. Verification battery + spec truth

- [ ] 6.1 `npm run typecheck && npm run lint && npm run format:check` clean across all new/modified files.
- [ ] 6.2 Stability: helper self-test, inventory guard, and the full sweep each green 3× consecutively at `--project=chromium --workers=1`; the two rewired source specs (`coop-campaign-ui-audit`, `tactical-map-visual-smoke`) green once each; evidence recorded in the PR description.
- [ ] 6.3 Final proofs re-run: 4.4's `--list` table and 4.5's consumer grep re-executed on the final tree (post any rebase) and re-recorded — the numbers shipped are the numbers proven, not stale drafts.
- [ ] 6.4 Spec-coverage self-audit: walk both delta files requirement by requirement and name the artifact delivering each SHALL (Shared Layout Assertion Helpers incl. the no-silent-filter rule: 2.1/2.2/2.3; Viewport Layout Sweep incl. inventory exhaustiveness, per-viewport applicability + per-viewport primary-affordance coverage (design D9), quarantine discipline, front-door-only pack seeding + observable-only dynamic segments (design D10), no-pixel-diff, chromium-only scheduling: 3.1–3.4/5.0/5.1; Playwright Project Topology incl. the selection-unchanged proofs and tag opt-in rules: 4.1–4.5; MODIFIED Deep-Play scoping: 4.2/4.4; MODIFIED journey-qc anchors: 4.2/4.4 — anchors listed chromium-only post-rewrite). Any uncovered SHALL blocks completion.
- [ ] 6.5 `npx openspec validate add-viewport-layout-sweep --strict` passes after any spec-delta edits (formatter first — design R4).
- [ ] 6.6 Ship-time ledger reconciliation (design R5): rebase onto current `origin/main`, confirm `openspec/active-change-ledger.json` lists exactly the then-active change folders including `add-viewport-layout-sweep`, and confirm `node scripts/qc/validate-openspec-ci-quality.mjs` (the `qc:openspec-ci:validate` gate) passes.
