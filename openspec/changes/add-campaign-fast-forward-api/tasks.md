# Tasks: add-campaign-fast-forward-api

> Execution is **Codex-first and strictly sequential** — parallel agent fan-out has failed on this repo. One group at a time; each lands with its verification before the next. Claude orchestrates, packages context, and final-reviews. Pre-commit runs a full `next build` (~3–5 min) — commit via background shell. **Non-goals are load-bearing: modify NO production file except the ONE sanctioned pilot-attribution fix (task 3.2, design D9 — three files, additive + fallback-preserving)** — `materializeCampaignMissionEncounter`, `scenarioEncounterBridgeProcessor`, the day pipeline, `GameEngine`, and the outcome bus are consumed as-is (the only other sanctioned exception is an additive `export` keyword if a needed function is module-private, recorded in the task that needs it). **No `Date.now()` seed derivation anywhere in `src/lib/campaign/fastForward/`; no seed→outcome golden values in any test; no direct writes to `campaign.pendingBattleOutcomes` from fast-forward code or its tests (design D5); no CI workflow edits.** File paths and line numbers below were verified on `main@bea54767c` — re-verify against the current tree before editing. After any spec-delta edit, run the formatter + `npx openspec validate add-campaign-fast-forward-api --strict` (design R6). Ledger registration ships with the declarative PR stage, not with these implementation tasks. Estimated total: ~4–5 dev-days.

## 1. In-process API router + campaign fixture builder (~1d)

- [ ] 1.1 Grep-inventory the exact handler modules the materializer's five call shapes hit (`/api/forces` POST, `/api/forces/assignments/:id` PUT, `/api/encounters` POST, `/api/encounters/:id` GET/PATCH, `/api/encounters/:id/player-force|opponent-force` PUT — see `src/lib/campaign/encounter/materializeCampaignMissionEncounter.ts:94-285`) plus `/api/forces/:id` GET (the runner's force read-back, design D2). Record the file list and each handler's default-export signature.
  - Files: read-only grep across `src/pages/api/forces/`, `src/pages/api/encounters/`.
  - Acceptance: a written route→module table covering all shapes above; any missing route surfaced now, not mid-implementation.
- [ ] 1.2 Create `src/lib/campaign/fastForward/inProcessApiRouter.ts` — `createInProcessApiFetch(): typeof fetch` with a static route table mapping method+path patterns to the imported real handler modules from 1.1; each call builds `createMocks({method, body, query})` (`node-mocks-http`, the `src/__tests__/api/forces/forceMultiAssignment.real.test.ts:1-78` pattern), awaits the handler, and adapts the mock response to the minimal `Response` surface `readApiJson` consumes (`status`, `ok`, `json()`). Unmatched method+path throws naming both (design D3 — never a silent 404). Include SQLite bootstrap helpers: real service at `:memory:` via the `routeHelpers` init path (`src/pages-modules/api/routeHelpers.ts:24-40`, `getSQLiteService({ path: ':memory:' }).initialize()` + `installNodeCanonicalUnitStatsResolver()`).
  - Files: `src/lib/campaign/fastForward/inProcessApiRouter.ts` (new), colocated `__tests__/inProcessApiRouter.test.ts` (new).
  - Acceptance: unit test drives every route in the table through the router against `:memory:` SQLite and asserts real rows land (create force → assign unit → create encounter → patch → attach both sides → read back); an unmatched route throws with method+path; no handler module is mocked anywhere in the test.
- [ ] 1.3 Create the shared fixture builder `src/lib/campaign/fastForward/fastForwardFixture.ts` — builds a campaign with pinned `rngSeed`, pinned start date (window parameterized to cover ≥1 Monday), `options.useAtBScenarios: true` (default is false — `src/types/campaign/options/combatForceOptions.ts:76-83` — generation never fires without it), an explicit `useRoleBasedSalaries` parameter (no silent default reliance — design D8), an active contract + combat teams whose `forceId`s resolve in `campaign.forces` (so `buildEncounterFromScenario` marks bridged encounters `Ready`, not `Draft`), **explicit `campaign.combatTeams`** (beats FRONTLINE-only force derivation, `scenarioGenerationProcessor.ts:115-124`) with the highest-base-chance role (`PATROL` = 60, `battleChance.ts:50-58`; `checkForBattle` reads `BASE_BATTLE_CHANCE[team.role]`, ignoring the `battleChance` field) and a team-count × Monday-count roll budget making P(zero battles) negligible for any seed (doc-comment the arithmetic, e.g. 8 PATROL rolls ⇒ 0.4^8 ≈ 0.07% — design D8: the seed serves determinism, never battle occurrence; re-pin freely after upstream roll-stream changes), **bounded contract windows** (contract `endDate` / Monday coverage set so no scenario can generate for a contract after it first reaches terminal status — design D8 + R9), and roster seeding via `useCampaignRosterStore.setState` (the `src/__tests__/integration/campaignCombatLoop.test.ts:150-174` `seedRoster` precedent) with canonical `unitRef`s + assigned pilots whose `pilotId`s are vault-shaped — NEVER session-unit-id-shaped strings (spec: XP invariants ban the rig).
  - Files: `src/lib/campaign/fastForward/fastForwardFixture.ts` (new), colocated test (new).
  - Acceptance: fixture round-trips one manual `advanceDay()` on a Monday and yields ≥1 `scenario_generated` event and ≥1 `bridgedEncounters` entry (proves all THREE gates — Monday, AtB option, battle-chance roll — are actually open before the driver exists); the colocated test documents that the pass is roll-budget-backed (not seed-cherry-picked) by asserting the fixture's declared team count × per-roll chance.
- [ ] 1.4 QA — `npm run typecheck` clean; new colocated suites green via the unit lane (match the jest environment docblock of `forceMultiAssignment.real.test.ts` so `better-sqlite3` loads — design D8).

## 2. Day-loop driver `fastForwardCampaign()` (~0.5d)

- [ ] 2.1 Create `src/lib/campaign/fastForward/fastForwardCampaign.ts` — the orchestrator: (a) touch `useCampaignStore()` so the combat-outcome-bus subscription is live BEFORE any battle (design D5 rule 1; `src/stores/campaign/useCampaignStore.ts:135-148`); (b) loop the store's `advanceDay` action (`useCampaignStore.dayActions.ts:181-252` — the registered-pipeline path, never `src/lib/campaign/dayAdvancement.ts`'s registry-free fallback) for the requested day count, collecting day reports; (c) after each day, diff `campaign.bridgedScenarioIds` (or consume `scenario_encounter_bridged` day events) to find newly bridged scenarios and hand each to the combat runner (group 3 — stub the hand-off behind an injected callback until group 3 lands); (d) after the final scheduled day, advance additional drain day(s) until `pendingBattleOutcomes` is empty (design D5 rule 2), recording them; (e) return the run report `{daysAdvanced, drainDays, dayReports, scenariosBridged, battles: [{scenarioId, contractId, encounterId, matchId, seed}], outcomesApplied}`; (f) accept `expectations: {minScenariosBridged?, minBattles?}` and THROW on any miss (spec: Fixture Expectations Fail Loud).
  - Files: `src/lib/campaign/fastForward/fastForwardCampaign.ts` (new), colocated test (new).
  - Acceptance: with the group-1 fixture (roll budget per 1.3 — the ≥1-bridged report holds for any seed, not a cherry-picked one) and a battle-runner stub, an 8-day run crossing one Monday reports ≥1 bridged scenario; the same run with `useAtBScenarios` unset throws on `minScenariosBridged: 1` instead of returning green; the driver provably ran the registered pipeline (bridged entries exist — only `scenarioEncounterBridgeProcessor` writes them).
- [ ] 2.2 QA — `npm run typecheck` clean; driver suite green; grep proves `dayAdvancement` (the legacy fallback module) is not imported by the fastForward tree.

## 3. Combat runner: real handlers → real engine → production bus (~1–1.5d)

- [ ] 3.1 Determinism preflight (design D4): read `src/simulation/core/SeededRandom.ts` and confirm no internal `Math.random` fallback; then create `src/lib/campaign/fastForward/deriveBattleSeed.ts` — deterministic seed from `resolveCampaignSeed(campaign)` (`src/lib/campaign/utils/campaignRng.ts`) + the scenario's stable id (FNV-style mix, doc-commented). The scenario id already embeds generation date + contract + force (`buildScenarioId`, `scenarioGenerationProcessor.ts:149-156`) — the seed MUST NOT key on the day the battle is fought (design D4: deferral-stable seeds are load-bearing for the 5.3 ordering variation). Unit test: same inputs ⇒ same seed; distinct scenarios ⇒ distinct seeds; same scenario "fought" on different days ⇒ SAME seed; no wall-clock input anywhere.
  - Files: `src/lib/campaign/fastForward/deriveBattleSeed.ts` (new) + colocated test; read-only on `SeededRandom.ts` (if a `Math.random` fallback IS found inside it, STOP and surface — that invalidates the design's determinism claims and needs its own decision).
  - Acceptance: helper test green; written confirmation of the `SeededRandom` read in the PR description.
- [ ] 3.2 **Sanctioned production fix (design D9, `campaign-combat-loop` ADDED requirement "Engine-Derived Outcome Pilot Attribution")**: (a) add optional `readonly pilotRef?: string | null` to `IUnitCombatDelta` (`src/types/combat/CombatOutcome.ts:81-101`); (b) populate it in `unitToDelta` from `IGameUnit.pilotRef` (`src/lib/combat/outcome/combatOutcome.ts:153-173` — the unit is in hand); (c) in `applyOutcomeDeltas` (`src/lib/campaign/processors/postBattleProcessor.ts:57-112`), resolve `entriesByPilotId` / `pilotsByPilotId` by `delta.pilotRef ?? delta.unitId` (prefer linkage, preserve the legacy unit-id fallback); kill attribution lookup (`reportUnitsById.get(delta.unitId)`) stays session-id-keyed. NO other keying changes — `unitCombatStates` stays as-is (design Open Questions).
  - Files: `src/types/combat/CombatOutcome.ts`, `src/lib/combat/outcome/combatOutcome.ts`, `src/lib/campaign/processors/postBattleProcessor.ts` (the three sanctioned touches) + new unit tests covering linkage-present / linkage-absent / linkage-unresolvable resolution.
  - Acceptance: new unit tests green; **every pre-existing suite passes unmodified** — `campaignCombatLoop.test.ts`, `phase4CampaignRoundTrip.test.ts`, `postBattleProcessor.retryIdempotency.test.ts` (their linkage-free hand-built outcomes must hit the fallback byte-for-byte, design R10); an engine-shaped fixture (session unit ids ≠ pilot ids, `pilotRef` carrying vault ids) yields nonzero `pilotsUpdated`.
- [ ] 3.3 Create `src/lib/campaign/fastForward/fastForwardCombatRunner.ts` — per bridged scenario: (a) build the `IRosterUnitProjection[]` roster from the campaign roster/force assigned to the scenario's team; (b) synthesize the `CampaignMissionSource` one-mission view and call `materializeCampaignMissionEncounter({campaign: view, missionId: scenarioId, rosterUnits, fetchImpl: router})` UNCHANGED (design D2); (c) read the created player/opponent forces back through the router (GET `/api/forces/:id`); (d) build units via `buildPreparedBattleData` (`src/components/gameplay/pages/preBattleSessionBuilder.ts` — the real catalog-adapted path; layering trade-off recorded in design D8); (e) `new GameEngine({ seed: deriveBattleSeed(...) })` → `runToCompletion(playerUnits, opponentUnits, gameUnits, linkage)` with linkage `{campaignId, contractId, scenarioId}` from the bridged encounter's `campaignMeta`; (f) `deriveCombatOutcome(session, {contractId, scenarioId})` + `publishCombatOutcome(event)` from the lib/engine layer — NOT imported from `usePreBattleLaunch` (design D5). If `deriveCombatOutcome`/`publishCombatOutcome` need an export tweak, that is the sanctioned additive-export exception — record it here.
  - Files: `src/lib/campaign/fastForward/fastForwardCombatRunner.ts` (new), colocated test (new); wire the group-2 callback to the real runner.
  - Acceptance: one bridged scenario runs end-to-end in jest — SQLite rows exist for both forces + encounter, the session's `config.seed` equals the derived seed (W1 stamping, `GameEngine.ts:141`), the published outcome lands in `pendingBattleOutcomes` via the store subscription (asserted through the store, never injected), and the NEXT `advanceDay()` applies it — roster XP fields move through 3.2's pilot attribution, with fixture pilot ids that are NOT session-unit-id-shaped.
- [ ] 3.4 QA — `npm run typecheck` clean; runner + driver suites green; grep proves no `Date.now` in `src/lib/campaign/fastForward/` and no import of `usePreBattleLaunch` from the fastForward tree.

## 4. `assertSessionInflictedDamage` + invariant assertion modules (~0.5–1d)

- [ ] 4.1 Create `src/lib/campaign/fastForward/assertSessionInflictedDamage.ts` per design D6: throws with per-unit diagnostics on (a) any deployed unit starting at zero total armor+structure (#998 shape — sum via the `sumLocations` pattern of `src/__tests__/integration/campaignMissionEncounterLaunchIntegrity.test.ts:64-66`); (b) a completed battle where every unit's remaining armor+structure (outcome `unitDeltas`) equals its starting totals — **deltas-unchanged trips alone, never AND-ed with the event log**; when damage-bearing events ARE present alongside unchanged deltas the thrown diagnostics additionally name the phantom-damage signature (events against ids that never mutated state — the dual-id class); (c) a terminal outcome with no justifying combat/withdrawal/concession events (#1019 shape). Wire it into the combat runner unconditionally for every completed battle.
  - Files: `src/lib/campaign/fastForward/assertSessionInflictedDamage.ts` (new) + colocated test (new); one-line wire-in in `fastForwardCombatRunner.ts`.
  - Acceptance: unit tests cover all three failure modes (fixture sessions/outcomes constructed to trip each) plus the healthy-battle pass plus the **phantom-damage case** — damage events present, deltas unchanged ⇒ throws AND names the phantom signature (this case existing and failing loud is the finding-3 regression gate); the runner test from 3.3 still passes with the guard active.
- [ ] 4.2 Create the invariant assertion modules — `src/lib/campaign/fastForward/invariants/ledger.ts` (balance reconciliation with EXPLICIT per-`TransactionType` sign mapping, failing on unknown types — design R4; contract-payout exactly-once **per terminal status flip and under same-matchId re-publication** — production's real guarantee, asserted by counting `tx-contract-close-<contractId>` postings + `processedFulfilledContractIds` membership under the D8 bounded-contract-window fixture; the invariant MUST NOT claim per-contract exactly-once beyond that bound — terminal-to-terminal re-payout is the R9 production gap, out of scope; salary-path mutual exclusion per `useRoleBasedSalaries`), `invariants/xp.ts` (per-pilot `xp`/`campaignXpEarned` non-decreasing across the run; apply-once on re-published duplicate `matchId`; `campaignMissions`/`campaignKills`/scenario-XP increments matching outcome participation per `postBattleProcessor.helpers.ts:84-166`, resolved through 3.2's pilot attribution — no id-rigged fixtures), `invariants/maintenance.ts` (per-day repair hours ≤ the daily budget, `repairProgressProcessor.ts:21`; armor/structure/ammo never exceed `unitMaxStates` caps; no duplicate tickets or double part consumption on day re-run, `repairQueueBuilderProcessor.ts` deterministic ticketIds).
  - Files: three new modules under `src/lib/campaign/fastForward/invariants/` + colocated tests (new).
  - Acceptance: each invariant has a passing case and a deliberately-violated fixture case proving it actually fails (no assertion theater); ledger reconciliation test includes at least Income + a subtractive type in one run.
- [ ] 4.3 QA — `npm run typecheck` clean; all group-4 suites green.

## 5. Integration suites: capstone, determinism, live-parity acceptance (~1d)

- [ ] 5.1 Capstone integration test `src/__tests__/integration/fastForwardCampaign.test.ts` — the full chain on the group-1 fixture: N days crossing ≥1 Monday, `expectations: {minScenariosBridged: 1, minBattles: 1}` (satisfiable for any seed via the fixture's roll budget — task 1.3), all battles pass `assertSessionInflictedDamage`, all three invariant modules pass over the end state, `outcomesApplied === battles.length`, and an XP assertion proving engine-driven combat moved `campaignXpEarned` **through 3.2's production pilot attribution** — this test must NOT hand-build any `ICombatOutcome` and must NOT seed roster pilot ids as session-unit-id-shaped strings (closing the continuous-chain gap for real). Follow the `phase4CampaignRoundTrip.test.ts` singleton-reset pattern (5 resets in `beforeEach`/`afterEach`, design D8) plus SQLite `:memory:` re-init.
  - Files: `src/__tests__/integration/fastForwardCampaign.test.ts` (new).
  - Acceptance: green under the unit lane; deliberately breaking the fixture (AtB off) makes it fail loud via expectations, not pass empty.
- [ ] 5.2 Determinism test `src/__tests__/integration/fastForwardDeterminism.test.ts` — run the identical fixture + day count TWICE (full singleton + DB reset between runs) and assert invariant-level equality per design D4's enumerated field set (survival sets, armor/structure deltas, balance delta, transaction types+amounts, XP counters, scenario/contract statuses, repair-ticket counts/kinds), with wall-clock-bearing fields excluded by the comparator. No golden values for any seed. A divergence is a determinism bug to fix (trace to the offending RNG consumer), never a comparator widening (design R3).
  - Files: `src/__tests__/integration/fastForwardDeterminism.test.ts` (new) + a shared comparator in `src/lib/campaign/fastForward/invariants/` if it earns reuse from 5.3.
  - Acceptance: run-twice equality green; the comparator's exclusion list is exactly the documented wall-clock set (reviewed, not open-ended).
- [ ] 5.3 Live-parity acceptance `src/__tests__/integration/fastForwardLiveParity.test.ts` (design D7 anti-tautology rules) — same fixture + seeds through (a) `fastForwardCampaign(N)` and (b) the hand-cranked discrete-step live-equivalent loop: advance one day; materialize+fight each newly bridged scenario; advance to apply; repeat — **hand-inlined against the production primitives; MUST NOT import `fastForwardCampaign` or `fastForwardCombatRunner`; shared surface limited to the fixture builder, the in-process router, and `deriveBattleSeed`** (D7 rule 1). Both sides advance the SAME total in-fiction days including drain days and the test asserts equal final in-fiction dates BEFORE comparing invariants (D7 rule 4 — a day-count mismatch is a guaranteed spurious balance divergence). Include the **deferred-fight ordering variation** (D7 rule 3): a live-equivalent run fighting a bridged scenario ≥1 day later within the same window, asserted equal on the seam invariant set (deferral-stable seeds from 3.1 make this orchestration-only); if it legitimately diverges, STOP and narrow the spec requirement to identical-ordering-only via an explicit delta — never a silent skip. Then assert equality on the seam invariant set. The test's failure message MUST name the diverging field and cite the council consequence (packs stay triage-only) so a red run cannot be mistaken for flake.
  - Files: `src/__tests__/integration/fastForwardLiveParity.test.ts` (new).
  - Acceptance: parity green on the seam invariant set for both identical-ordering and deferred-ordering runs (or the documented narrowing delta exists); grep proves the live-equivalent imports neither the driver nor the runner; the test contains no golden traces and no byte-level state-dump comparison.
- [ ] 5.4 QA — the three integration suites green in one `npx jest --selectProjects unit --runInBand` invocation scoped to the fastForward paths (no cross-test singleton bleed).

## 6. Verification battery + spec truth (~0.5d)

- [ ] 6.1 `npm run typecheck && npm run lint && npm run format:check` clean across all new files.
- [ ] 6.2 Full unit lane green: `npx jest --selectProjects unit --ci` including every new suite; pre-existing unrelated failures (if any) triaged against the unmodified base before being declared out of scope.
- [ ] 6.3 Spec-coverage self-audit: walk `specs/campaign-fast-forward-api/spec.md` AND the `specs/campaign-combat-loop/spec.md` delta requirement by requirement and name the test/file delivering each SHALL (driver: 2.1/5.1; expectations + seed-independent battle guarantee: 1.3/2.1/5.1; real-handlers+engine: 1.2/3.3; pilot attribution: 3.2; damage guard incl. phantom-damage case: 4.1; ledger/XP/maintenance: 4.2/5.1; determinism: 3.1/5.2; parity incl. independence grep + ordering variation + day-count pin: 5.3; complements-never-replaces: prose + absence of anchor edits). Any uncovered SHALL blocks completion.
- [ ] 6.4 `npx openspec validate add-campaign-fast-forward-api --strict` passes after any spec-delta edits (run the formatter first — design R6).
- [ ] 6.5 Ship-time ledger reconciliation (design R7): rebase the PR onto current `origin/main`, confirm `openspec/active-change-ledger.json` lists exactly the then-active change folders including `add-campaign-fast-forward-api`, and confirm `node scripts/qc/validate-openspec-ci-quality.mjs` (the `qc:openspec-ci:validate` gate) passes.
