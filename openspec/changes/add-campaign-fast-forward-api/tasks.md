# Tasks: add-campaign-fast-forward-api

> Execution is **Codex-first and strictly sequential** — parallel agent fan-out has failed on this repo. One group at a time; each lands with its verification before the next. Claude orchestrates, packages context, and final-reviews. Pre-commit runs a full `next build` (~3–5 min) — commit via background shell. **Non-goals are load-bearing: modify NO production file except the ONE sanctioned pilot-attribution fix (task 3.2, design D9 — three files, additive + fallback-preserving)** — `materializeCampaignMissionEncounter`, `scenarioEncounterBridgeProcessor`, the day pipeline, `GameEngine`, and the outcome bus are consumed as-is (the only other sanctioned exception is an additive `export` keyword if a needed function is module-private, recorded in the task that needs it). **No `Date.now()` seed derivation anywhere in `src/lib/campaign/fastForward/`; no seed→outcome golden values in any test; no direct writes to `campaign.pendingBattleOutcomes` from fast-forward code or its tests (design D5); no CI workflow edits.** File paths and line numbers below were verified on `main@bea54767c` — re-verify against the current tree before editing. After any spec-delta edit, run the formatter + `npx openspec validate add-campaign-fast-forward-api --strict` (design R6). Ledger registration ships with the declarative PR stage, not with these implementation tasks. Estimated total: ~4–5 dev-days.

## 1. In-process API router + campaign fixture builder (~1d)

- [x] 1.1 Grep-inventory the exact handler modules the materializer's five call shapes hit (`/api/forces` POST, `/api/forces/assignments/:id` PUT, `/api/encounters` POST, `/api/encounters/:id` GET/PATCH, `/api/encounters/:id/player-force|opponent-force` PUT — see `src/lib/campaign/encounter/materializeCampaignMissionEncounter.ts:94-285`) plus `/api/forces/:id` GET (the runner's force read-back, design D2). Record the file list and each handler's default-export signature.
  - Files: read-only grep across `src/pages/api/forces/`, `src/pages/api/encounters/`.
  - Acceptance: a written route→module table covering all shapes above; any missing route surfaced now, not mid-implementation.
  - Done: route→module table recorded as the doc-comment in `src/lib/campaign/fastForward/inProcessApiRouter.ts` (task 1.1 table) — all 8 routes present (5 materializer call shapes + `GET /api/forces/:id` read-back); no missing route.
- [x] 1.2 Create `src/lib/campaign/fastForward/inProcessApiRouter.ts` — `createInProcessApiFetch(): typeof fetch` with a static route table mapping method+path patterns to the imported real handler modules from 1.1; each call builds `createMocks({method, body, query})` (`node-mocks-http`, the `src/__tests__/api/forces/forceMultiAssignment.real.test.ts:1-78` pattern), awaits the handler, and adapts the mock response to the minimal `Response` surface `readApiJson` consumes (`status`, `ok`, `json()`). Unmatched method+path throws naming both (design D3 — never a silent 404). Include SQLite bootstrap helpers: real service at `:memory:` via the `routeHelpers` init path (`src/pages-modules/api/routeHelpers.ts:24-40`, `getSQLiteService({ path: ':memory:' }).initialize()` + `installNodeCanonicalUnitStatsResolver()`).
  - Files: `src/lib/campaign/fastForward/inProcessApiRouter.ts` (new), colocated `__tests__/inProcessApiRouter.test.ts` (new).
  - Acceptance: unit test drives every route in the table through the router against `:memory:` SQLite and asserts real rows land (create force → assign unit → create encounter → patch → attach both sides → read back); an unmatched route throws with method+path; no handler module is mocked anywhere in the test.
  - Done: `createInProcessApiFetch()` + `initializeInProcessApiDatabase()` implemented; colocated test drives create-force → assign-unit → create-encounter → patch → attach player+opponent forces → read-back through real `:memory:` SQLite rows, plus an unmatched-route case asserting the thrown method+path message. 2/2 tests green.
- [x] 1.3 Create the shared fixture builder `src/lib/campaign/fastForward/fastForwardFixture.ts` — builds a campaign with pinned `rngSeed`, pinned start date (window parameterized to cover ≥1 Monday), `options.useAtBScenarios: true` (default is false — `src/types/campaign/options/combatForceOptions.ts:76-83` — generation never fires without it), an explicit `useRoleBasedSalaries` parameter (no silent default reliance — design D8), an active contract + combat teams whose `forceId`s resolve in `campaign.forces` (so `buildEncounterFromScenario` marks bridged encounters `Ready`, not `Draft`), **explicit `campaign.combatTeams`** (beats FRONTLINE-only force derivation, `scenarioGenerationProcessor.ts:115-124`) with the highest-base-chance role (`PATROL` = 60, `battleChance.ts:50-58`; `checkForBattle` reads `BASE_BATTLE_CHANCE[team.role]`, ignoring the `battleChance` field) and a team-count × Monday-count roll budget making P(zero battles) negligible for any seed (doc-comment the arithmetic, e.g. 8 PATROL rolls ⇒ 0.4^8 ≈ 0.07% — design D8: the seed serves determinism, never battle occurrence; re-pin freely after upstream roll-stream changes), **bounded contract windows** (contract `endDate` / Monday coverage set so no scenario can generate for a contract after it first reaches terminal status — design D8 + R9), and roster seeding via `useCampaignRosterStore.setState` (the `src/__tests__/integration/campaignCombatLoop.test.ts:150-174` `seedRoster` precedent) with canonical `unitRef`s + assigned pilots whose `pilotId`s are vault-shaped — NEVER session-unit-id-shaped strings (spec: XP invariants ban the rig).
  - Files: `src/lib/campaign/fastForward/fastForwardFixture.ts` (new), colocated test (new).
  - Acceptance: fixture round-trips one manual `advanceDay()` on a Monday and yields ≥1 `scenario_generated` event and ≥1 `bridgedEncounters` entry (proves all THREE gates — Monday, AtB option, battle-chance roll — are actually open before the driver exists); the colocated test documents that the pass is roll-budget-backed (not seed-cherry-picked) by asserting the fixture's declared team count × per-roll chance.
  - Done: `buildFastForwardFixture()` implemented (8 explicit PATROL combat teams, `0.4 ** 8 ≈ 0.0655%` roll budget documented + asserted; bounded one-Monday contract window; vault-shaped roster pilot ids). Colocated test proves the three gates open across 10 arbitrary (non-cherry-picked) seeds via `getDayPipeline().processDay()`, plus dedicated cases for the roll-budget arithmetic, Ready-not-Draft resolution, bounded contract window, roster shape, and the explicit `useRoleBasedSalaries` contract. 7/7 tests green.
- [x] 1.4 QA — `npm run typecheck` clean; new colocated suites green via the unit lane (match the jest environment docblock of `forceMultiAssignment.real.test.ts` so `better-sqlite3` loads — design D8).
  - Done: `npm run typecheck` clean (`tsc --noEmit --skipLibCheck`, zero errors); `npx jest --selectProjects unit --runInBand src/lib/campaign/fastForward/` → 2 suites, 9/9 tests green. No jest environment docblock override needed — `forceMultiAssignment.real.test.ts` carries none; the `unit` project's default `testEnvironment: 'jsdom'` already loads `better-sqlite3` (drift correction confirmed, no action required).

## 2. Day-loop driver `fastForwardCampaign()` (~0.5d)

- [x] 2.1 Create `src/lib/campaign/fastForward/fastForwardCampaign.ts` — the orchestrator: (a) touch `useCampaignStore()` so the combat-outcome-bus subscription is live BEFORE any battle (design D5 rule 1; `src/stores/campaign/useCampaignStore.ts:135-148`); (b) loop the store's `advanceDay` action (`useCampaignStore.dayActions.ts:181-252` — the registered-pipeline path, never `src/lib/campaign/dayAdvancement.ts`'s registry-free fallback) for the requested day count, collecting day reports; (c) after each day, diff `campaign.bridgedScenarioIds` (or consume `scenario_encounter_bridged` day events) to find newly bridged scenarios and hand each to the combat runner (group 3 — stub the hand-off behind an injected callback until group 3 lands); (d) after the final scheduled day, advance additional drain day(s) until `pendingBattleOutcomes` is empty (design D5 rule 2), recording them; (e) return the run report `{daysAdvanced, drainDays, dayReports, scenariosBridged, battles: [{scenarioId, contractId, encounterId, matchId, seed}], outcomesApplied}`; (f) accept `expectations: {minScenariosBridged?, minBattles?}` and THROW on any miss (spec: Fixture Expectations Fail Loud).
  - Files: `src/lib/campaign/fastForward/fastForwardCampaign.ts` (new), colocated test (new).
  - Acceptance: with the group-1 fixture (roll budget per 1.3 — the ≥1-bridged report holds for any seed, not a cherry-picked one) and a battle-runner stub, an 8-day run crossing one Monday reports ≥1 bridged scenario; the same run with `useAtBScenarios` unset throws on `minScenariosBridged: 1` instead of returning green; the driver provably ran the registered pipeline (bridged entries exist — only `scenarioEncounterBridgeProcessor` writes them).
  - Done: `fastForwardCampaign()` implemented exactly per (a)-(f) — touches `useCampaignStore()` and loads the run's campaign via the real `switchCampaign` action before the day loop (never a raw `setState` bypass); loops `store.getState().advanceDay()` (imports `DayReport` from `dayReportTypes.ts`, never `dayAdvancement.ts`); detects newly bridged scenarios by diffing `campaign.bridgedScenarioIds` day over day and hands each to the injected `FastForwardBattleRunner` callback (group-3 stub point, returns `null` to skip); drains `pendingBattleOutcomes` to zero after the scheduled days (bounded by `maxDrainDays`, fails loud instead of looping forever on a stuck outcome); returns the exact report shape; throws a combined message on any declared `expectations` miss. Colocated test (`fastForwardCampaign.test.ts`) covers: 8-day run crossing the fixture's Monday reports ≥1 bridged scenario and the store's own campaign state (not just the report) carries the bridge entries; the same fixture with `useAtBScenarios` forced off throws `minScenariosBridged: expected >= 1, got 0` instead of returning green; a publishing battle-runner stub is handed every bridged scenario, publishes through the real `publishCombatOutcome` bus, and the drain-day loop (exercised with `days: 1` so scheduled days can't accidentally cover the drain) applies every battle (`outcomesApplied === battles.length`, `getPendingOutcomeCount() === 0`); `maxDrainDays: 0` proves the safety-net throw; a runner returning `null` records zero battles without erroring. 6/6 tests green.
- [x] 2.2 QA — `npm run typecheck` clean; driver suite green; grep proves `dayAdvancement` (the legacy fallback module) is not imported by the fastForward tree.
  - Done: `npm run typecheck` (`tsc --noEmit --skipLibCheck`) clean, zero errors. `npx jest --selectProjects unit --runInBand src/lib/campaign/fastForward/` → 3 suites, 15/15 tests green (`fastForwardCampaign.test.ts` 6 new, `inProcessApiRouter.test.ts` 2, `fastForwardFixture.test.ts` 7 — the latter two are group 1's pre-existing suites, re-run clean alongside the new driver suite). `grep -rn "from '@/lib/campaign/dayAdvancement'" src/lib/campaign/fastForward/` → zero matches (exit 1) — the module's doc comments name `dayAdvancement.ts` only to explain why it is avoided; no import statement references it anywhere in the tree. `npx oxlint`/`npx oxfmt --check` clean on both new files.

## 3. Combat runner: real handlers → real engine → production bus (~1–1.5d)

- [x] 3.1 Determinism preflight (design D4): read `src/simulation/core/SeededRandom.ts` and confirm no internal `Math.random` fallback; then create `src/lib/campaign/fastForward/deriveBattleSeed.ts` — deterministic seed from `resolveCampaignSeed(campaign)` (`src/lib/campaign/utils/campaignRng.ts`) + the scenario's stable id (FNV-style mix, doc-commented). The scenario id already embeds generation date + contract + force (`buildScenarioId`, `scenarioGenerationProcessor.ts:149-156`) — the seed MUST NOT key on the day the battle is fought (design D4: deferral-stable seeds are load-bearing for the 5.3 ordering variation). Unit test: same inputs ⇒ same seed; distinct scenarios ⇒ distinct seeds; same scenario "fought" on different days ⇒ SAME seed; no wall-clock input anywhere.
  - Files: `src/lib/campaign/fastForward/deriveBattleSeed.ts` (new) + colocated test; read-only on `SeededRandom.ts` (if a `Math.random` fallback IS found inside it, STOP and surface — that invalidates the design's determinism claims and needs its own decision).
  - Acceptance: helper test green; written confirmation of the `SeededRandom` read in the PR description.
  - Done: `SeededRandom` read confirmed — Mulberry32 implementation, `next()`/`nextInt()`/`nextRange()` derive exclusively from `this.state` seeded once in the constructor via `seed >>> 0`; zero `Math.random` fallback found. `deriveBattleSeed(campaignSeed, scenarioId)` implemented exactly per design D4's literal signature (`deriveBattleSeed(resolveCampaignSeed(campaign), scenarioId)`), with a local FNV-1a mix (the same algorithm `campaignRng.ts`'s private `fnv1a` uses, duplicated rather than reaching into that module's non-exported internals). 6/6 colocated tests green: determinism, distinct-scenario/distinct-seed divergence, same-seed-on-deferred-fight, uint32 range, and a source-inspection test proving no `Date.now`/`new Date(` appears in the function body.
- [x] 3.2 **Sanctioned production fix (design D9, `campaign-combat-loop` ADDED requirement "Engine-Derived Outcome Pilot Attribution")**: (a) add optional `readonly pilotRef?: string | null` to `IUnitCombatDelta` (`src/types/combat/CombatOutcome.ts:81-101`); (b) populate it in `unitToDelta` from `IGameUnit.pilotRef` (`src/lib/combat/outcome/combatOutcome.ts:153-173` — the unit is in hand); (c) in `applyOutcomeDeltas` (`src/lib/campaign/processors/postBattleProcessor.ts:57-112`), resolve `entriesByPilotId` / `pilotsByPilotId` by `delta.pilotRef ?? delta.unitId` (prefer linkage, preserve the legacy unit-id fallback); kill attribution lookup (`reportUnitsById.get(delta.unitId)`) stays session-id-keyed. NO other keying changes — `unitCombatStates` stays as-is (design Open Questions).
  - Files: `src/types/combat/CombatOutcome.ts`, `src/lib/combat/outcome/combatOutcome.ts`, `src/lib/campaign/processors/postBattleProcessor.ts` (the three sanctioned touches) + new unit tests covering linkage-present / linkage-absent / linkage-unresolvable resolution.
  - Acceptance: new unit tests green; **every pre-existing suite passes unmodified** — `campaignCombatLoop.test.ts`, `phase4CampaignRoundTrip.test.ts`, `postBattleProcessor.retryIdempotency.test.ts` (their linkage-free hand-built outcomes must hit the fallback byte-for-byte, design R10); an engine-shaped fixture (session unit ids ≠ pilot ids, `pilotRef` carrying vault ids) yields nonzero `pilotsUpdated`.
  - Done: all three sanctioned touches landed exactly as specified — `IUnitCombatDelta.pilotRef?: string | null` (additive), `unitToDelta` populates it from `unit.pilotRef`, `applyOutcomeDeltas` resolves `entriesByPilotId`/`pilotsByPilotId`/`pilotPatches` keying by `delta.pilotRef ?? delta.unitId` (`resolvedPilotId`), kill attribution (`reportUnitsById.get(delta.unitId)`) untouched. New suite `postBattleProcessor.pilotAttribution.test.ts` (5/5 green): linkage-present (engine-shaped, session unitId ≠ pilotId) yields nonzero `pilotsUpdated`; linkage-absent falls back byte-for-byte; linkage-unresolvable (opponent/NPC `pilotRef: 'Unknown'`) skips without throwing; kill attribution stays `unitId`-keyed even when `pilotRef` is present; `pilotRef: null` falls back identically to `undefined`. Pre-existing suites re-run and pass UNMODIFIED: `campaignCombatLoop.test.ts`, `phase4CampaignRoundTrip.test.ts`, `postBattleProcessor.retryIdempotency.test.ts`, `postBattleProcessor.test.ts` (43/43 combined, zero edits to any of the four files).
- [x] 3.3 Create `src/lib/campaign/fastForward/fastForwardCombatRunner.ts` — per bridged scenario: (a) build the `IRosterUnitProjection[]` roster from the campaign roster/force assigned to the scenario's team; (b) synthesize the `CampaignMissionSource` one-mission view and call `materializeCampaignMissionEncounter({campaign: view, missionId: scenarioId, rosterUnits, fetchImpl: router})` UNCHANGED (design D2); (c) read the created player/opponent forces back through the router (GET `/api/forces/:id`); (d) build units via `buildPreparedBattleData` (`src/components/gameplay/pages/preBattleSessionBuilder.ts` — the real catalog-adapted path; layering trade-off recorded in design D8); (e) `new GameEngine({ seed: deriveBattleSeed(...) })` → `runToCompletion(playerUnits, opponentUnits, gameUnits, linkage)` with linkage `{campaignId, contractId, scenarioId}` from the bridged encounter's `campaignMeta`; (f) `deriveCombatOutcome(session, {contractId, scenarioId})` + `publishCombatOutcome(event)` from the lib/engine layer — NOT imported from `usePreBattleLaunch` (design D5). If `deriveCombatOutcome`/`publishCombatOutcome` need an export tweak, that is the sanctioned additive-export exception — record it here.
  - Files: `src/lib/campaign/fastForward/fastForwardCombatRunner.ts` (new), colocated test (new); wire the group-2 callback to the real runner.
  - Acceptance: one bridged scenario runs end-to-end in jest — SQLite rows exist for both forces + encounter, the session's `config.seed` equals the derived seed (W1 stamping, `GameEngine.ts:141`), the published outcome lands in `pendingBattleOutcomes` via the store subscription (asserted through the store, never injected), and the NEXT `advanceDay()` applies it — roster XP fields move through 3.2's pilot attribution, with fixture pilot ids that are NOT session-unit-id-shaped.
  - Done: `runFastForwardBattle()` implements (a)-(f) exactly, reading the live campaign from `useCampaignStore()` rather than accepting one as a param (matches the driver's own subscription-liveness ownership); `createFastForwardCombatRunner()` adapts it to the `FastForwardBattleRunner` shape for direct wiring into `fastForwardCampaign()`'s `runBridgedScenario`. No export tweak was needed — `deriveCombatOutcome`/`publishCombatOutcome` were already exported. Two drift corrections surfaced during implementation (both scoped to this change's own module tree, never a sanctioned-file touch): (1) the group-1 fixture's combat-team `campaign.forces[].unitIds` referenced synthetic placeholder ids that never matched the roster store's real `unitId`s — `fastForwardFixture.ts` restructured so forces reference real roster-projection unit ids (roster built first, teams cycle through it); no assertion in `fastForwardFixture.test.ts`/`fastForwardCampaign.test.ts` depended on the old placeholder shape, both suites re-run green unmodified. (2) `PUT /api/forces/assignments/:id` → `ForceService.assignPilotAndUnit` validates the assignment's `pilotId` against a REAL SQLite `PilotRepository` row (`getPilotRepository().getById`) — the fixture's Zustand-only `vault-pilot-NNN` ids have no such row, so materialization failed "Pilot not found"; added `ensureRealPilotIds()` to the runner, which creates a real pilot via the exact `forceMultiAssignment.real.test.ts`-precedented `getPilotRepository().create()` call and renames the roster store's pilotId (idempotent, self-healing) so every downstream lookup — including 3.2's D9 resolution — references the same real id consistently. Also found and fixed a latent test-isolation gap: `EncounterRepository` guards its DDL behind its own `this.initialized` instance flag (same pattern as `ForceRepository`) and needs its own `resetEncounterRepository()` between tests — missing in the group-1 `inProcessApiRouter.test.ts` reset helper (latent there since it only exercises encounters once); fixed in both that file and the new suite. Colocated `fastForwardCombatRunner.test.ts` (3/3 green, stable across repeated runs): full end-to-end assertion (SQLite force + encounter rows, `session.config.seed === deriveBattleSeed(...)`, outcome observed in `pendingBattleOutcomes` via the store before the NEXT `advanceDay()` applies it, post-drain roster XP/`campaignMissions` movement with the resolved pilot id asserted NOT session-unit-id-shaped); a Draft-status (no resolvable player force) case returns `null` without throwing; a wiring smoke test drives `createFastForwardCombatRunner()` through `fastForwardCampaign()` end-to-end.
- [x] 3.4 QA — `npm run typecheck` clean; runner + driver suites green; grep proves no `Date.now` in `src/lib/campaign/fastForward/` and no import of `usePreBattleLaunch` from the fastForward tree.
  - Done: `npm run typecheck` (`tsc --noEmit --skipLibCheck`) clean, zero errors. `npx jest --selectProjects unit --runInBand` scoped to `src/lib/campaign/fastForward/` plus all group-3-touched/pinned suites (`postBattleProcessor.pilotAttribution.test.ts`, `postBattleProcessor.test.ts`, `postBattleProcessor.retryIdempotency.test.ts`, `campaignCombatLoop.test.ts`, `phase4CampaignRoundTrip.test.ts`) → 10 suites, 67/67 tests green. `grep -rn "Date\.now" src/lib/campaign/fastForward/` → matches only inside doc-comment prose explaining the footgun being avoided (`deriveBattleSeed.ts`'s module doc); zero executable `Date.now()` calls. `grep -rn "^import.*usePreBattleLaunch\|from '.*usePreBattleLaunch'" src/lib/campaign/fastForward/` → zero matches (exit 1) — the two prose mentions in `fastForwardCombatRunner.ts`'s doc comments name the module only to explain why the publish step is NOT imported from it. `npx oxlint`/`npx oxfmt --check` clean on all group-3 files.

## 4. `assertSessionInflictedDamage` + invariant assertion modules (~0.5–1d)

- [x] 4.1 Create `src/lib/campaign/fastForward/assertSessionInflictedDamage.ts` per design D6: throws with per-unit diagnostics on (a) any deployed unit starting at zero total armor+structure (#998 shape — sum via the `sumLocations` pattern of `src/__tests__/integration/campaignMissionEncounterLaunchIntegrity.test.ts:64-66`); (b) a completed battle where every unit's remaining armor+structure (outcome `unitDeltas`) equals its starting totals — **deltas-unchanged trips alone, never AND-ed with the event log**; when damage-bearing events ARE present alongside unchanged deltas the thrown diagnostics additionally name the phantom-damage signature (events against ids that never mutated state — the dual-id class); (c) a terminal outcome with no justifying combat/withdrawal/concession events (#1019 shape). Wire it into the combat runner unconditionally for every completed battle.
  - Files: `src/lib/campaign/fastForward/assertSessionInflictedDamage.ts` (new) + colocated test (new); one-line wire-in in `fastForwardCombatRunner.ts`.
  - Acceptance: unit tests cover all three failure modes (fixture sessions/outcomes constructed to trip each) plus the healthy-battle pass plus the **phantom-damage case** — damage events present, deltas unchanged ⇒ throws AND names the phantom signature (this case existing and failing loud is the finding-3 regression gate); the runner test from 3.3 still passes with the guard active.
  - Done: `assertSessionInflictedDamage(session, outcome)` implemented exactly per (a)-(c), checked in that order; starting totals sourced from `IGameUnit.armorByLocation`/`.structureByLocation` (the construction-time values `gameUnitsWithAdaptedCombatSeeds` splices onto `session.units` before `createInitialUnitState` seeds `currentState`, confirmed by reading `utils/gameplay/gameState/initialization.ts:139-140`) — documented as BattleMech-scoped (the only unit type the fast-forward runner constructs), non-Mek unit types out of scope; remaining totals from `outcome.unitDeltas[].armorRemaining`/`.internalsRemaining`; condition (b) trips alone (never AND-ed with the event log) and on trip, `DamageApplied` events (if any) are named as the phantom-damage signature since condition (b) having tripped already proves their target ids never mutated state; condition (c) checks session.events for a fixed `COMBAT_EVENT_TYPES`/`WITHDRAWAL_EVENT_TYPES` set, with `endReason === Concede` self-justifying (no separate "concede declared" event type exists in this codebase). Thrown as `SessionDamageInvariantError` (`reason` discriminator + `diagnostics` + optional `phantomDamageUnitIds`) for programmatic assertions. Wired into `fastForwardCombatRunner.ts` with a genuine one-line call (`assertSessionInflictedDamage(session, outcome);`) right after `deriveCombatOutcome`, before `publishCombatOutcome` — diff is exactly the import line + the 3-line call site, no other production edits. Colocated `assertSessionInflictedDamage.test.ts` (6/6 green): all three failure modes, the phantom-damage case (damage events present, deltas unchanged ⇒ throws AND names `PHANTOM-DAMAGE SIGNATURE` + `phantomDamageUnitIds`), a concession self-justification case, and the healthy-battle pass.
  - Drift correction surfaced by wiring in the guard (scoped to this task's own test file, no production touch beyond the sanctioned one-liner above): the 3.3 runner test (`fastForwardCombatRunner.test.ts`) called the REAL `adaptUnit`/`CompendiumAdapter` unmocked; `CanonicalUnitService.getById()` loads bundled unit data via `fetch()` with no filesystem fallback (`services/units/CanonicalUnitService.ts:161`), which always resolves to `null` in a plain jest/node environment — every OTHER jest suite in this repo that exercises `adaptUnit` mocks `CompendiumAdapter` (confirmed by grep), this was the first to call it unmocked. Unmocked, every battle ran with empty `armorByLocation`/`structureByLocation` (zero real armor), invisible to the test's pre-4.1 assertions (`campaignMissions`/`xp` increment unconditionally per applied outcome regardless of real combat, `postBattleProcessor.helpers.ts:158-159`) but caught immediately by the new guard as a zero-armor-start battle — exactly the class it exists to catch. Fixed by mocking `@/engine/adapters/CompendiumAdapter` in that test file, mirroring the repo's own established precedent (`campaignMissionEncounterLaunchIntegrity.test.ts`'s `CANONICAL_COMBAT_SHEETS`/`makeAdaptedUnit`) plus real weapon data (that precedent never advances past Initiative so never needed any) so `runToCompletion` genuinely inflicts damage. All 3/3 pre-existing 3.3 tests pass unmodified in assertion content — only the catalog-lookup mocking boundary was added.
- [x] 4.2 Create the invariant assertion modules — `src/lib/campaign/fastForward/invariants/ledger.ts` (balance reconciliation with EXPLICIT per-`TransactionType` sign mapping, failing on unknown types — design R4; contract-payout exactly-once **per terminal status flip and under same-matchId re-publication** — production's real guarantee, asserted by counting `tx-contract-close-<contractId>` postings + `processedFulfilledContractIds` membership under the D8 bounded-contract-window fixture; the invariant MUST NOT claim per-contract exactly-once beyond that bound — terminal-to-terminal re-payout is the R9 production gap, out of scope; salary-path mutual exclusion per `useRoleBasedSalaries`), `invariants/xp.ts` (per-pilot `xp`/`campaignXpEarned` non-decreasing across the run; apply-once on re-published duplicate `matchId`; `campaignMissions`/`campaignKills`/scenario-XP increments matching outcome participation per `postBattleProcessor.helpers.ts:84-166`, resolved through 3.2's pilot attribution — no id-rigged fixtures), `invariants/maintenance.ts` (per-day repair hours ≤ the daily budget, `repairProgressProcessor.ts:21`; armor/structure/ammo never exceed `unitMaxStates` caps; no duplicate tickets or double part consumption on day re-run, `repairQueueBuilderProcessor.ts` deterministic ticketIds).
  - Files: three new modules under `src/lib/campaign/fastForward/invariants/` + colocated tests (new).
  - Acceptance: each invariant has a passing case and a deliberately-violated fixture case proving it actually fails (no assertion theater); ledger reconciliation test includes at least Income + a subtractive type in one run.
  - Done: `invariants/ledger.ts` — `TRANSACTION_TYPE_SIGN: Record<TransactionType, 1|-1>` built from AUDITED call-site evidence (not `FinanceService`'s binary Income/Salvage-only rule, which a real call site — `campaignCommandActions.ts:406-417`'s `LoanDisbursement` via direct `.add()` — already contradicts); `signFor()` throws on an unrecognized type; `assertLedgerReconciles(startingBalance, transactions, endingBalance)` folds signed cents and compares exactly; `assertContractPayoutExactlyOnce` counts `tx-contract-close-<contractId>-*` postings + `processedFulfilledContractIds` membership, both required to equal exactly 1; `assertSalaryPathMutualExclusion` checks the legacy `Expense`/`tx-salary-*` path and the role-based `Salary` path never both post. `invariants/xp.ts` — `assertXpNonDecreasing` (day-snapshot sequence, per-pilot pairwise non-decreasing check on `xp`/`campaignXpEarned`), `assertXpApplicationUnchangedByDuplicate` (before/after-duplicate roster snapshots must match on all four counters), `assertAwardsMatchParticipation` (resolves `delta.pilotRef ?? delta.unitId` exactly as `applyOutcomeDeltas` does — design D9 — asserting `campaignMissions` +1 and `campaignKills` matching the outcome's per-unit kill attribution unconditionally, mirroring that the raw kill counter, unlike the kill XP bonus, is not side/win-gated in production). `invariants/maintenance.ts` — `assertDailyRepairHoursBounded` diffs before/after ticket `remainingHours` (mirrors `DEFAULT_DAILY_REPAIR_HOURS = 8`, duplicated from `repairProgressProcessor.ts:21` since it's module-private and an export-only touch is out of this task's sanctioned scope), `assertRepairsWithinMaxCaps` (armor/structure/ammo vs `unitMaxStates`), `assertNoDuplicateRepairTickets` (ticketId uniqueness) + `assertRepairQueueIdempotentAcrossRerun` (before/after re-run snapshot equality on ticket state + per-part inventory totals). Colocated suites: `ledger.test.ts` (13/13, includes an Income+Maintenance-in-one-run reconciliation case), `xp.test.ts` (9/9), `maintenance.test.ts` (14/14) — every invariant has both a passing case and a deliberately-violated fixture proving the failure path fires (no assertion theater), 36/36 total.
- [x] 4.3 QA — `npm run typecheck` clean; all group-4 suites green.
  - Done: `npm run typecheck` (`tsc --noEmit --skipLibCheck`) clean, zero errors, full repo. `npx jest --selectProjects unit --runInBand src/lib/campaign/fastForward/` → 9 suites, 66/66 tests green (groups 1-4 combined: the new `assertSessionInflictedDamage.test.ts` (6) + `invariants/__tests__/{ledger,xp,maintenance}.test.ts` (36) alongside groups 1-3's 5 pre-existing suites, all re-run clean). Group-3-pinned suites re-verified green after the guard wire-in and the 3.3 test's catalog-mocking fix: `postBattleProcessor.pilotAttribution.test.ts`, `postBattleProcessor.test.ts`, `postBattleProcessor.retryIdempotency.test.ts`, `campaignCombatLoop.test.ts`, `phase4CampaignRoundTrip.test.ts`, `campaignMissionEncounterLaunchIntegrity.test.ts` → 6 suites, 44/44 green, zero edits to any of those files. `npx oxlint`/`npx oxfmt --check` clean on every group-4 file plus the two group-3 files this task touched (`fastForwardCombatRunner.ts`'s one-line wire-in, `fastForwardCombatRunner.test.ts`'s catalog-mocking fix). `grep -rn "console\."` on all new/touched production files → zero matches.

## 5. Integration suites: capstone, determinism, live-parity acceptance (~1d)

- [x] 5.1 Capstone integration test `src/__tests__/integration/fastForwardCampaign.test.ts` — the full chain on the group-1 fixture: N days crossing ≥1 Monday, `expectations: {minScenariosBridged: 1, minBattles: 1}` (satisfiable for any seed via the fixture's roll budget — task 1.3), all battles pass `assertSessionInflictedDamage`, all three invariant modules pass over the end state, `outcomesApplied === battles.length`, and an XP assertion proving engine-driven combat moved `campaignXpEarned` **through 3.2's production pilot attribution** — this test must NOT hand-build any `ICombatOutcome` and must NOT seed roster pilot ids as session-unit-id-shaped strings (closing the continuous-chain gap for real). Follow the `phase4CampaignRoundTrip.test.ts` singleton-reset pattern (5 resets in `beforeEach`/`afterEach`, design D8) plus SQLite `:memory:` re-init.
  - Files: `src/__tests__/integration/fastForwardCampaign.test.ts` (new); `src/__tests__/integration/fastForwardTestSupport.ts` (new, shared by 5.1-5.3 — see 5.4).
  - Done: 2-day run crossing the fixture's Monday, fighting every bridged scenario through `createCapturingRunner()` (a read-only wrapper around the real `runFastForwardBattle`, capturing each battle's actually-published `ICombatOutcome` for assertions — never a hand-built outcome, never a `pendingBattleOutcomes` write). Asserts `assertLedgerReconciles`, `assertSalaryPathMutualExclusion`, `assertContractPayoutExactlyOnce` (conditionally, only for a contract that actually reached terminal status), `assertXpNonDecreasing` (2-point baseline→final), `assertAwardsMatchParticipation` + `assertXpApplicationUnchangedByDuplicate` (bracketing one isolated battle's outcome application, republishing it and re-advancing to prove apply-once), and `assertRepairsWithinMaxCaps`/`assertNoDuplicateRepairTickets` over the real end state. Second case: forcing `useAtBScenarios: false` on the fixture throws `minScenariosBridged: expected >= 1, got 0` instead of returning green. 2/2 tests green. Drift correction surfaced during authoring (scoped to this test's own fixture parameterization, no production or group-1-file touch): the group-1 fixture's default `combatTeamCount: 8` attaches every combat team to the SAME single contract (`getCombatTeamsForContract`: "all teams apply to every contract for now") — with 8 teams, multiple can independently win/lose against that one contract on the SAME Monday, and `postBattleProcessor`'s sequential apply loop lets a differing second result flip an already-terminal contract to a different terminal status (`applyContractDelta`'s `previousStatus !== nextStatus` gate has no non-terminal precondition), producing a real double payout — empirically reproduced against the pinned default seed. This is the SAME-DAY sibling of design R9's documented multi-Monday gap, not a flake; fixing it in production is out of scope (matches R9's own disposition). This capstone uses `combatTeamCount: 1` (documented in the test file's module doc) to make the shape structurally unreachable and to guarantee the isolated single-fight bracket the XP assertions need; `fastForwardDeterminism.test.ts` and `fastForwardLiveParity.test.ts` (5.2/5.3) use the true group-1 default (8 teams) since they never assert per-contract exactly-once, only cross-run equality.
- [x] 5.2 Determinism test `src/__tests__/integration/fastForwardDeterminism.test.ts` — run the identical fixture + day count TWICE (full singleton + DB reset between runs) and assert invariant-level equality per design D4's enumerated field set (survival sets, armor/structure deltas, balance delta, transaction types+amounts, XP counters, scenario/contract statuses, repair-ticket counts/kinds), with wall-clock-bearing fields excluded by the comparator. No golden values for any seed. A divergence is a determinism bug to fix (trace to the offending RNG consumer), never a comparator widening (design R3).
  - Files: `src/__tests__/integration/fastForwardDeterminism.test.ts` (new); shared comparator `src/lib/campaign/fastForward/invariants/comparableRunState.ts` (new — earned reuse from 5.3 per the task hint, both suites import `buildComparableRunState`/`assertRunStatesEqual` so they compare on the literal same field set by construction).
  - Done: two independent `fastForwardCampaign()` runs of the group-1 default fixture (`combatTeamCount: 8`, 2-day window) against a fully reset world (SQLite/repositories/Zustand stores/buses/day-pipeline registration + re-mocked catalog adapter) produce byte-for-byte-equal `ComparableRunState` snapshots. Identifier hygiene documented + implemented in the comparator: `matchId` (`session.id`, `uuidv4()`-random per `gameSessionCore.ts:103`) and SQLite `pilotId` (`pilot-${uuidv4()}` per `PilotRepository.ts:98`) are NEVER compared literally — battles correlate by the deterministic `scenarioId`, XP counters correlate by the stable roster-projection `unitId`, resolving each run's current `pilotId` at snapshot time. Confirmed real activity occurred before trusting the equality check (`battles.length > 0`, `transactions.length > 0`). 1/1 test green, passed on first run (no widening needed).
- [x] 5.3 Live-parity acceptance `src/__tests__/integration/fastForwardLiveParity.test.ts` (design D7 anti-tautology rules) — same fixture + seeds through (a) `fastForwardCampaign(N)` and (b) the hand-cranked discrete-step live-equivalent loop: advance one day; materialize+fight each newly bridged scenario; advance to apply; repeat — **hand-inlined against the production primitives; MUST NOT import `fastForwardCampaign` or `fastForwardCombatRunner`; shared surface limited to the fixture builder, the in-process router, and `deriveBattleSeed`** (D7 rule 1). Both sides advance the SAME total in-fiction days including drain days and the test asserts equal final in-fiction dates BEFORE comparing invariants (D7 rule 4 — a day-count mismatch is a guaranteed spurious balance divergence). Include the **deferred-fight ordering variation** (D7 rule 3): a live-equivalent run fighting a bridged scenario ≥1 day later within the same window, asserted equal on the seam invariant set (deferral-stable seeds from 3.1 make this orchestration-only); if it legitimately diverges, STOP and narrow the spec requirement to identical-ordering-only via an explicit delta — never a silent skip. Then assert equality on the seam invariant set. The test's failure message MUST name the diverging field and cite the council consequence (packs stay triage-only) so a red run cannot be mistaken for flake.
  - Files: `src/__tests__/integration/fastForwardLiveParity.test.ts` (new); `src/__tests__/integration/fastForwardLiveEquivalent.ts` (new — the independent hand-inlined day loop + per-battle sequence, isolated into its own module specifically so the D7-rule-1 grep check can target ONLY it, not the test file which legitimately imports `fastForwardCampaign` for the other side of the comparison).
  - Done: `fastForwardLiveEquivalent.ts` independently re-implements `resolveTeamRoster`/`ensureRealPilotIds`/the materialize→read-back→build→engine→derive→guard→publish sequence (mirroring, never importing, `fastForwardCombatRunner.ts`) plus its own discrete-step day loop (`runLiveEquivalent`: advance one day, fight every bridged scenario whose `deferFightsByDays` threshold has elapsed, advance to apply, repeat; drains any still-pending fights/outcomes after the scheduled window, bounded by `maxDrainDays`). Both a live-equivalent that fights immediately (`deferFightsByDays: 0`) and one that defers by one extra day-advance (`deferFightsByDays: 1`) are compared against a fresh `fastForwardCampaign()` reference run — both hold `totalDays`/final-in-fiction-date equality (asserted BEFORE the invariant comparison, D7 rule 4) and pass `assertRunStatesEqual` on the seam invariant set with the council-consequence text appended to the failure message, on the FIRST run (no legitimate divergence found — the deferred-ordering narrowing-delta escape hatch was not needed). Third test greps `fastForwardLiveEquivalent.ts`'s own source (`from '@/lib/campaign/fastForward/fastForwardCampaign'` / `...fastForwardCombatRunner'` patterns, matching only actual import statements, not the module's own prose doc comments that reference those names by word) and asserts zero matches. 3/3 tests green.
- [x] 5.4 QA — the three integration suites green in one `npx jest --selectProjects unit --runInBand` invocation scoped to the fastForward paths (no cross-test singleton bleed).
  - Done: `npx jest --selectProjects unit --runInBand src/__tests__/integration/fastForwardCampaign.test.ts src/__tests__/integration/fastForwardDeterminism.test.ts src/__tests__/integration/fastForwardLiveParity.test.ts` → 3 suites, 6/6 tests green, no cross-test singleton bleed (each suite's own `beforeEach`/`afterEach` runs the full `resetWorld()` sweep from `fastForwardTestSupport.ts`). Extended re-run scoped to the full `src/lib/campaign/fastForward/` tree + `src/__tests__/integration/fastForward*` + every group-3/4-pinned regression suite (`phase4CampaignRoundTrip.test.ts`, `campaignCombatLoop.test.ts`, `campaignMissionEncounterLaunchIntegrity.test.ts`, `postBattleProcessor.test.ts`, `postBattleProcessor.retryIdempotency.test.ts`, `postBattleProcessor.pilotAttribution.test.ts`) → 18 suites, 116/116 tests green, zero edits to any pinned file. `npx tsc --noEmit --skipLibCheck` (full repo) clean, zero errors. `npx oxlint`/`npx oxfmt --check` clean on all six new/touched files. `grep -rn "console\."` on all new files → zero matches. `grep -rn "Date\.now"` on all new files → zero matches. `grep -rn "pendingBattleOutcomes"` → the one hit is a doc-comment explaining why the wrapper does NOT write it directly, not an actual write.

## 6. Verification battery + spec truth (~0.5d)

- [ ] 6.1 `npm run typecheck && npm run lint && npm run format:check` clean across all new files.
- [ ] 6.2 Full unit lane green: `npx jest --selectProjects unit --ci` including every new suite; pre-existing unrelated failures (if any) triaged against the unmodified base before being declared out of scope.
- [ ] 6.3 Spec-coverage self-audit: walk `specs/campaign-fast-forward-api/spec.md` AND the `specs/campaign-combat-loop/spec.md` delta requirement by requirement and name the test/file delivering each SHALL (driver: 2.1/5.1; expectations + seed-independent battle guarantee: 1.3/2.1/5.1; real-handlers+engine: 1.2/3.3; pilot attribution: 3.2; damage guard incl. phantom-damage case: 4.1; ledger/XP/maintenance: 4.2/5.1; determinism: 3.1/5.2; parity incl. independence grep + ordering variation + day-count pin: 5.3; complements-never-replaces: prose + absence of anchor edits). Any uncovered SHALL blocks completion.
- [ ] 6.4 `npx openspec validate add-campaign-fast-forward-api --strict` passes after any spec-delta edits (run the formatter first — design R6).
- [ ] 6.5 Ship-time ledger reconciliation (design R7): rebase the PR onto current `origin/main`, confirm `openspec/active-change-ledger.json` lists exactly the then-active change folders including `add-campaign-fast-forward-api`, and confirm `node scripts/qc/validate-openspec-ci-quality.mjs` (the `qc:openspec-ci:validate` gate) passes.
