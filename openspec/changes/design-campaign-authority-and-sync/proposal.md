# Proposal: Campaign Authority and Synchronization

## Why

The 2026-08-07 live visual audit produced hard evidence of the boundary this change was opened to fix (historical motivation, not current route behavior): a campaign created in one browser context returned "Campaign not found" on every deep link from any other context. At that date campaign runtime state lived in the browser (Zustand `persist` → client storage), so the source device was the only place the campaign existed, deep links and cross-device continuity failed, and co-op was structurally constrained. The 2026-07-07 live playtest's multiplayer socket failures (dual in-memory match stores) and the silent `/api/campaigns` 500 were the same root cause surfacing elsewhere. As of 2026-09-12 the server already owns campaign read/save and route lookup: `CampaignPersistenceService` (`src/services/campaignPersistence/CampaignPersistenceService.ts`) reads and saves the server record; GET `/api/campaigns/:id` (`src/pages/api/campaigns/[id].ts`) returns 404 only for a missing row; `useCampaignPersistenceStore` (`src/stores/campaign/useCampaignPersistenceStore.ts`) loads through that API; and the campaign route shell (`src/pages-modules/gameplay/campaigns/campaignPageShell.tsx`) loads the route campaign through that store. Browser storage remains cache/legacy-adoption input until D8 migration. Production journal authority remains disabled (`CAMPAIGN_JOURNAL_AUTHORITY_ENABLED` is false in `src/lib/campaign/sync/JournalCampaignEventStore.ts` / `src/lib/campaign/sync/campaignJournalAuthorityEnabled.ts`).

The direction is confirmed: campaign state must be kept on the server — never authoritative in the browser — so it can be shared with peer players through a share function, synchronized constantly, and never lost on the source device. The source broadcasts events to every downstream consuming device (each of which runs its own local server and keeps its own durable copy). One source campaign can create many shared instances; shared instances know they are not the source; they see the actions taken over the campaign's whole history from their less-permissive perspective. The Game Master retains full view of everything; other participants must not even receive the events outside their access.

The substrate largely exists and is reused, not reinvented: the SQLite event journal (`src/lib/events/journal/`) already provides hash-chained events (`previous_stream_event_digest` → `event_digest`), stream/branch identity, canonical command identity, and actor/authority columns; `CampaignPersistenceService` already provides server-side campaign summaries/bodies; the WebSocket upgrade path and vault-identity token minting already exist for multiplayer. This change binds the campaign domain to that substrate and adds the authority, replication, and access-projection contracts on top.

## What Changes

- **NEW capability `campaign-authority`** — server-authoritative campaign state: every campaign is owned by exactly one source instance on a server; all mutations are commands validated at the source and appended durably to the campaign event stream before any acknowledgement or broadcast; browser storage is demoted to a cache; deep links resolve from the server.
- **NEW capability `campaign-replication`** — the share function issues grants; downstream devices instantiate replica instances that carry source identity and grant provenance (a replica always knows it is not the source); replicas receive the event stream over a persistent connection with cursor-resumable catch-up, persist their own durable local copy on their own local server, and route mutation intents back to the source.
- **NEW capability `campaign-access-projection`** — every campaign event carries an access scope at emission; the source projects a per-grant filtered stream; consumers never receive events outside their grant; the GM grant receives the full stream; filtered streams remain causally ordered, per-scope contiguous, and per-scope digest-chained so a replica can verify integrity without learning anything about withheld events.
- **MODIFIED capability `campaign-persistence`** — the server-side persistence contract is promoted from an optional save path to the authoritative home of every server-owned campaign: creation lands in the server store immediately, deep links resolve without client-side handoff, and a deleted server record cannot be resurrected from a client cache.
- **MODIFIED capability `coop-campaign-sync`** — the guest's read-only mirror is upgraded to a durable replica instance (persisted by the guest's own server, carrying replica authority metadata, surviving restart with offline reads), and guest hydration becomes scope-filtered at the host with per-grant cursor resume. The existing intent → validate → commit → broadcast loop, host-as-GM authority model, and player-safe projections are preserved and generalized, not replaced.

## Impact

- Affected specs: `campaign-authority` (new), `campaign-replication` (new), `campaign-access-projection` (new), `campaign-persistence` (modified), `coop-campaign-sync` (modified).
- Affected code (at implementation time, not in this change): campaign Zustand stores and persistence wiring (`src/stores/campaign/`), `CampaignPersistenceService` and `/api/campaigns/*` routes, the event journal consumers (`src/lib/events/journal/` — reused as-is), the WebSocket server surface (`server.js` upgrade path + a campaign sync channel), vault-identity token minting for grant authentication.
- **BREAKING (spec-level):** browser-local storage ceases to be an authoritative home for campaign state. Existing browser-persisted campaigns require a one-time adoption migration into the server store (design D8); after adoption the client cache is derived state only.
- Relationship to `design-vault-campaign-separation-and-maps`: composes, does not overlap. The vault change governs what enters a campaign (immutable personal templates → instance copies with provenance); this change governs where campaign state lives and how it reaches other players. The vault remains personal and unsynchronized; campaign event streams are the shared unit.
- Sequencing: implementation starts only after CAMP-01F/G/H complete (same frozen-contract gate as the vault change). Spec-level work (this change) lands now.

## Admitted preparation sequence (2026-09-12)

Design-only freeze of current source-grounded requirements and the next admitted preparation order. It does not claim implementation, parent review, runtime acceptance, production cutover, or that all `SerializedCampaign` fields replay. `CAMPAIGN_JOURNAL_AUTHORITY_ENABLED` remains false (`src/lib/campaign/sync/JournalCampaignEventStore.ts:83`). Original absorbed PR1–PR3 (tasks 5.1–5.3) stay historically closed. CAMP exact-head eligible non-author APPROVED and all three witnesses remain the parent gate; this delta does not waive them. Current browser `acceptContractOffer` behavior is unchanged (`src/stores/campaign/campaignCommandActions.ts:454-494`).

**Current writer seam that P0a must extend, not replace.** `IEventJournal` exposes only a prebuilt `append` (`src/lib/events/journal/EventJournalContract.ts:140-158`). Concrete `SQLiteEventJournalWriter.append` delegates to `appendWithExtension` (`src/lib/events/journal/SQLiteEventJournalWriter.ts:36-40`). That method parses `raw` (`AppendEventBatchSchema.parse`, line 54), canonicalizes (`canonicalizeCommandIdentityV1`, line 55), rejects duplicate `eventId` with `throw new Error('Duplicate eventId')` (lines 56-61), then starts `this.db.transaction(...).immediate()` (lines 62-68). A baseline first read inside the existing `extend` callback cannot enter already-canonicalized payload bytes. `saveCampaign` starts its own transaction (`src/services/campaignPersistence/CampaignPersistenceService.ts:110-158`) and MUST NOT be nested. Public `envelopeOf` returns only `campaignEvent` (`src/lib/campaign/sync/JournalCampaignEventStore.ts:177-180`). Local planning-input provenance (ignored raw; not canonical published proof and not runtime acceptance): `.sisyphus/roadmap-completion-20260912/r2-source-contract-private-replay-20260913/` with immutable `report.json` sha256 `835380dfb33bd14a3ee437e5ba86a762ce4691586db7573c055e70a840e33912` as catalogued by that directory's `artifact-hashes.json`. Executable source anchors above remain the support for current writer behavior.

**P0a — smallest first implementation seam (preparation API only).** Add concrete `SQLiteEventJournalWriter.appendPreparedWithExtension`; keep it off generic `IEventJournal`. One immediate transaction SHALL: (1) run contractually read-only `prepare(db)` (source/head reads only; no SQL writes, external side effects, or retained statements/handles; P0a does not add a runtime read-only database proxy or SQL parser — trusted internal callbacks plus real tests are sufficient); (2) allow `{ kind: 'refused' }` to return without parse, canonicalize, append, or extension write, leaving every relevant table unchanged; (3) parse and canonicalize `prepared.raw` so any prepared baseline is inside command/event digests; (4) reject duplicate event IDs as the current writer; (5) run `extend(db, context, append)` on the same handle; (6) roll back receipt, events, head, and extension rows on thrown prepare/parse/canonicalize/append/extension failure. Both `prepare` and `extend` SHALL complete synchronously entirely during the transaction: no `await`, returned thenables, timers, microtasks, deferred work, external I/O, or retaining `db`/`context`/`append` for later use. For the `TResult` contract, the `result` field of a refused `prepare` return and the direct `extend` return SHALL be synchronous; the defensive guard SHALL inspect the direct `prepare` return before its discriminator, `prepared.result` when it is refused, and the direct `extend` return, rejecting any Promise or thenable before commit and rolling back. Scheduling remains prohibited. The thenable guard is defensive only and cannot guarantee rollback of arbitrary scheduled or external side effects; P0a does not promise a general runtime sandbox or detection of arbitrary scheduled closures. A source-command layer defines its dedicated rollback error; `extend` SHALL throw that error on post-append source-write failure; the concrete writer SHALL propagate it unchanged through and out of the immediate transaction. Only the source-command caller MAY catch AFTER rollback and map a typed refusal. A catch that returns a refusal inside the transaction is forbidden. P0a tests a dedicated fixture error plus a caller-side catch outside the writer; the writer SHALL NOT add a campaign producer or campaign error taxonomy. Nested `saveCampaign` remains forbidden. Existing `append` / `appendWithExtension` stay compatible, including `src/lib/campaign/sync/campaignCombatOutcomeInbox.ts:80`. Required characterizations (not claimed to exist): actual SQLite commit/reopen, snapshot read during prepare, rollback after append, refused prepare leaving every relevant table unchanged, malformed/duplicate IDs, returned-thenable rejection/rollback, and fixture rollback-error propagation with caller-side mapping outside the writer. Callback contract and review cover the scheduling prohibition; tests SHALL NOT claim automatic scheduled-work detection. No campaign producer or cutover is part of P0a.

**P0b (next prerequisite; P0a does not solve it).** Private envelope, full source baseline, and contract-only replay: capture the exact persisted source record, row version, body digest, and root public revision under the transaction; preserve unrelated source body as baseline input; store the full accepted contract and remaining market in the journal-private envelope only, never on public `campaignEvent` or `ICampaignAuthoritativeState`; conditional absence preserves legacy canonical hashes (`canonicalizeJsonV1` cannot represent `undefined`, `src/lib/events/journal/EventJournalCanonicalizer.ts:59-117`); borrowed-handle campaign write with CAS and `sourceReplayFence` rejecting later generic whole-envelope overwrites; source snapshot/cache version, original command receipt revision, and current journal revision remain distinct; private/public identity mismatch or unsupported replay schema blocks/rebuilds with no silent compact fallback. Compact genesis (`authoritativeStateFromSerializedCampaign`, `src/lib/campaign/authority/campaignSourceGenesis.ts:54-85`) is not that baseline.

**P1 (downstream of P0b; not implementation-ready).** Actual `acceptContractOffer` toolbar source command; stable `contractId` plus authenticated actor fingerprint excluding `expectedRevision`; duplicate lookup before offer read; server-derived name/employer from the stored offer using exact opaque `IContract.employerId` (`src/types/campaign/Mission.ts:140`) with no invented aliases. Current public head after later commands plus original receipt revision. Existing active-seat `POST /api/campaigns/[id]/commands` authorization remains (`src/pages/api/campaigns/[id]/commands.ts:103-120`); that response and guest/room-code data stay compact (`envelopeOf`; `src/lib/campaign/coop/roomCodeGuestHydration.ts:46-55`). Full private HTTP readback has no verified source-only audience contract and remains an explicit prerequisite, not permission to disclose private facts. Browser-generated offer persistence is not assumed (`src/pages/gameplay/campaigns/[id]/contract-market.tsx` seeds via `generateAtBContracts`). Do not replace the actual source command with a co-op archival API.
