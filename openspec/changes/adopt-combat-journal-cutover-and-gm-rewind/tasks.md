# Tasks

Successor of `harden-gm-two-player-campaign-sessions` for GM combat rewind (owner ruling 2026-09-04). Blocker name used by the umbrella: `journal-cutover`. Umbrella boxes this change closes by receipt: 14.1, 14.2, 14.4, 14.5. Umbrella rows this change un-gates: E2E-40, E2E-41, E2E-42, E2E-43, E2E-44, E2E-76 (`e2e/gm-two-player-rewind.pack.spec.ts`, group `rewind-pack`, strict `test.fail` tagged `@until-journal-cutover`).

Rules carried from the umbrella: one PR per seam, under the 500-line / 15-file cap; every seam lands with its own mutant matrix and a dated receipt; workers never edit `openspec/**`; nothing here un-gates a row until the SHALL it proves is discharged live.

## 0. Provenance and the honest count

- [ ] 0.1 Reconcile all 80 acceptance scenarios of the umbrella's `e2e-testing` delta to test identifiers and evidence paths (the umbrella's box 25.4 first half), recording which rows are real, which are strict expected failures, and which have no row. Deliverable: a table in this change's notepad and a receipt naming the reachable surface (34 of 80 at the split).
- [x] 0.2 Verify on fetched exact main that `add-authoritative-history-branches` exposes the branch port this change activates through (`hasHistoryBranchStore`, `readEffectiveHead`, supersession), and record the exact SHA and the port's shape.
  - Receipt (2026-09-15, verified as part of S1 PR #1684 merged `95042d9196795afe33662954686c57cd4b1749a2`; exact-main proof `openspec/planning/2026-09-12-roadmap-completion/evidence/r4-s1-journal-batches-1684-main-proof-20260915.json`, jest exit 0 / 207 suites / 1596 tests, typecheck 0, 4/4 blobs identical to the reviewed head `e1ccf668c`): the branch port `add-authoritative-history-branches` exposes was read on fetched exact main at SHA `b7d65099` - `hasHistoryBranchStore` and `readEffectiveHead` are present on the capability port; the supersession WRITE is NOT on the branch store, it lives in `EventHistoryActivation.ts:204`, and this change activates through it there. Verification only: no branch-port code was changed by this box.

## 1. Production match-stream journal writer (the cutover)

- [ ] 1.1 S1: append every committed combat batch to `event_journal_events` at the expected revision and install `event_journal_stream_heads` and the match's genesis / `event_history_effective_heads` row on the first batch. Both rewind routes answer a real preview on a live match. Receipt must state that no SHALL is discharged by this seam alone.
  - Progress (2026-09-15, S1 PR #1684 merged `95042d9196795afe33662954686c57cd4b1749a2`, exact-main proof `evidence/r4-s1-journal-batches-1684-main-proof-20260915.json`; box stays OPEN): the journal-writing half is delivered and proven on exact main - every committed combat batch appends to `event_journal_events` at the expected revision and the first batch installs `event_journal_stream_heads` plus the genesis / `event_history_effective_heads` row. The receipt states, as this box requires, that no SHALL is discharged by this seam alone. The REWIND-ROUTE clause is explicitly NOT claimed by that receipt ("no rewind-route acceptance; task 1.1 rewind preview not claimed"): neither rewind route has been shown answering a real preview on a live match, so the box does not close here. Carried residuals recorded with the receipt: no `busy_timeout` pragma (`SQLiteService.ts:83-86`), no mirror catch-up, and `ServerMatchHost.create` persists initial events outside the batch path so the mirror is not seeded at creation - create-path seeding is routed to 1.4/1.7. Mode stays off.
- [x] 1.2 S2: with S1's mirror admitting matches at mode `shadow` / `enabled`, prove `readEffectiveHead` and the live branch admission consult reachable against a REAL journal-backed head for a live match. Per History B's repair (PR #1674) exactly ONE `STALE_BRANCH` arm is reachable on the live path — a host still serving an identity (root / baseline, or a prior `servedBranchId`) the store has since superseded by activation (`!onLivePath`) — answered with the active head and a resync action. A persisted head naming a NON-EFFECTIVE (superseded) branch is a separate shape, unreachable by construction on that path (activation supersedes the prior branch and installs the replacement head in one transaction), and is refused `MATCH_QUARANTINED` (`history-integrity-blocked`) as persisted corruption, naming no head — not `STALE_BRANCH`. Prove the four illegal command shapes the umbrella's 14.2-a receipt names end to end through a journal head installed by S1's mirror, not a hand-constructed fixture head.
  - Receipt (2026-09-15, S2 PR #1691 merged `823bdf3acbaaa8a5d7ddccfff0f0f1a30e2688ee`; exact-main proof `evidence/r4-s2-live-head-1691-main-proof-20260915.json`, jest exit 0 / 168 suites / 1314 tests, `openspec validate --all --strict` 0, purpose 0, terminology 0, 3/3 blobs identical to the reviewed head `c315b9f99`): the live branch-admission consult is proven against a REAL journal-backed head installed by S1's mirror - not a hand-constructed fixture head - with the match admitted at a test-configured mode, and all four illegal command shapes the umbrella's 14.2-a receipt names are refused end to end. The box text above was amended in the same PR to the shipped behaviour (History B's repair leaves exactly one reachable `STALE_BRANCH` arm; a persisted head naming a superseded branch is `MATCH_QUARANTINED`, not `STALE_BRANCH`). Production mode remains off, so the consult does not fire in production; create-path mirror seeding is routed to 1.4/1.7.
- [x] 1.3 S3: retire the `mp_journal_authority_started` marker write; one source of "started".
  - Progress (2026-09-15, S3 delivered as three merged sub-prefixes - 3a PR #1693 `f8abc1844760137dcc9d4e4c6eaff17157052bee`, 3b-A PR #1702 `28e114300a8d3c4f980a4d43270636c52b352d76`, 3b-B PR #1703 `f6c5bd15a0e32bf404bd0662431f968cc27fd834`; exact-main proofs `evidence/r4-s3a-derived-started-1693-main-proof-20260915.json`, `evidence/r4-s3b-a-1702-main-proof-20260915.json`, `evidence/r4-s3b-b-1703-main-proof-20260915.json`; box stays OPEN): a derived "started" signal now comes from the S1 journal tables (`started` iff `readEffectiveHead(stream) !== null`) and both production call sites are repointed to it. The MARKER RETIREMENT this box names is NOT delivered and is not this slice: per the parent ruling recorded with 3b, S3 transition semantics are "derived head wins when present, marker honored when no head exists", the write-once marker guard is kept, and removing the `mp_journal_authority_started` write path is the cutover - owed by 1.7. The box text is to be amended at S7 admission to match. Also owed with the marker: `isMatchJournalAuthorityStartedDerived` has zero production consumers after the repoint and retires with the marker path.
  - Note (2026-09-15, ledger reconcile batch 4): sub-prefix 3 (marker retirement, S7-b) is IN REVIEW, NOT MERGED - `evidence/r4-s7b-local-20260915.json` records six commits on local branch `codex/roadmap-r4-s7b-marker-retirement-20260915` (stacked on PR #1738, unpushed, no PR opened yet); no receipt file records a merged PR number for this sub-prefix. Box stays OPEN.
  - Note (2026-09-15, ledger reconcile batch 5, FLIP): the S7-b stack rebased onto main as three PRs. #1744 `ca884fcc666cb7f3d7206f9cf8e5d2381d92133f` (recovery reads only the mirrored effective head) and #1745 `f8de824471ecb974eb8717a00a6a463392235940` (the command path writes no started marker) are both MERGED and exact-main-verified (`evidence/r4-s7b-1-1744-main-proof-20260915.json`, `evidence/r4-s7b-2-1745-main-proof-20260915.json`: jest exit 0 / 176-177 suites, tsc/openspec-strict/purpose/terminology/qc-openspec-ci all exit 0, blob-identical diffs against their reviewed PR heads); independent review APPROVE, zero required edits (`evidence/r4-s7b-review-20260915.json`). #1746 `b81242d1b668bc5d1a6e067c463cc5b332652a09` (retiring the dangling `isMatchJournalAuthorityStartedDerived` export) is confirmed MERGED and an ancestor of origin/main live this session (`gh pr view 1746` state MERGED; `git merge-base --is-ancestor b81242d1b origin/main` exit 0), and `r4-s7b-3-1746-main-proof-20260915.json` now records it exact-main-verified too (jest exit 0 / 177 suites, tsc/openspec-strict/purpose/terminology/qc-openspec-ci all exit 0, blob-identical against its reviewed PR head); it is still not cited toward this box's closure, which rests on #1744+#1745 alone, since #1746 only retires the already-dead `isMatchJournalAuthorityStartedDerived` surface and adds no clause of its own. This box's literal text names only the marker WRITE and "one source of started" - both satisfied in production by #1744+#1745 alone: no production code writes or reads `mp_journal_authority_started` (recovery consults only the derived/mirrored head; the command path never builds the marker record). FLIP. Residuals, none of them this box's own clause: OQ-2 (the table, `IMatchStore` read port and `matchCommandBatch` field remain as inert mechanism pending an owner ruling on whether to drop them - `evidence/r4-s7b-local-20260915.json`); the `unavailable` + marker -> `blocked` refusal S3-b once described was measured unsound (mutant M2) and is deliberately withdrawn, deferred to S7-d/task 1.7 (today `unavailable` still resolves to the legacy reader, which is what task 1.7's "legacy completed matches keep their reads" protects); S7b-Q6 (removing the marker read also removed a fail-closed catch on the command path for a corrupt persisted head - inert at the shipped mode 'off', carried for S7-d). Mode stays hardcoded `off` in production throughout.
- [ ] 1.4 S4: restart recovery rebuilds the live host from the journal head and the effective branch; prove with the umbrella's restart pack against a rewound match.
  - Progress (2026-09-15, S4 split A/B; both prefixes now merged and main-verified - A PR #1704 `8cacf20a65880e42304c4e885b395525c820f775` (exact-main proof `evidence/r4-s4-a-1704-main-proof-20260915.json`) and B PR #1705 `293d21bb0e3874768f3b0269115d465313230f4d` (exact-main proof `evidence/r4-s4-b-1705-main-proof-20260915.json`, blob-identical to the reviewed head `32c08e47b`, jest exit 0, typecheck 0, `openspec validate --all --strict` 0, qc:openspec-ci 0); box stays OPEN): A rebuilds the live host from the journal head and the effective branch and fixes a measured defect (the rewind fold declined every branch but root while the S1 backfill installs the mirrored head, so a healthy mirrored match failed to recover at boot; inert at mode off). B adds the divergence-refusal and create-path gap rows - the 1.7 seeding tripwire - and closes nothing else. The SECOND clause of this box - proving it with the umbrella's restart pack (E2E-05/06/15) against a REWOUND match - is explicitly NOT claimed by EITHER receipt (B's own non-claims repeat it verbatim), so the box does not close. Scope decision recorded: S4 restricts itself to head-and-branch reads; create-path mirror seeding is owed by 1.7. Reviewer-carried residuals for S5/S6: S4-R1 the divergence refusal also fires at mode `shadow` (unreachable by construction today; row 4 is the tripwire), S4-R2 the journal path records no checkpoint at the live head, S4-R4 the journal arm skips the whole `checkpointRecoveryPort` block (corrupt-empty-history, identity uniqueness, quarantine registration).
- [x] 1.5 S5-a: make the sequence-versus-revision offset explicit at every derivation site; re-pin the four suites that encode it.
  - Receipt (2026-09-15, S5 landed as two merged prefixes - A PR #1716 `faf03445d421fc2b9a52c20d5dddfbdb5f82cb02`, B PR #1717 `4e5a74b9fddda382945277b15d76e2e09397d56c`; exact-main proofs `evidence/r4-s5-a-1716-main-proof-20260915.json` and `evidence/r4-s5-b-1717-main-proof-20260915.json`, both blob-identical to their reviewed heads, jest exit 0 / 171 suites / 1341 tests, typecheck 0, `openspec validate --all --strict` 0, qc:openspec-ci 0): both clauses of this box are discharged. Every sequence-versus-revision derivation site is now named - `MATCH_FIRST_SEQUENCE`, `nextMatchSequenceAfter` and `journalHeadRevisionForNextMatchSequence` colocated with `revisionForMatchSequence`, with the `DurableMatchStore` commit-time check, `InMemoryMatchStore.liveHeadSequence`, `ServerMatchHostBranchAdmission` and the mirror's renamed `nextMatchSequence` input all routed through them - and the independent review (`evidence/r4-s5-explicit-offsets-review-20260915.json`) confirms the sweep is complete for match sequence-versus-revision arithmetic (the only surviving `sequence + 1` in production is the canonical definition itself) and that `grep -rln revisionForMatchSequence src --include=*.test.ts` returns exactly the four suites this box names, each re-pinned through the named helpers rather than through a literal. Residuals carried, none of them a clause of this box's text: the S5 mutant matrix is a SEAM obligation (design.md S5, "carries its own mutant matrix") deferred to 1.6, with kill-coverage checked by inspection instead under review finding RE-5; and S5-R1, the typed `rewound-stream` refusal the mirror now returns, is permanent for a rewound stream until 1.6 sources the expected revision from the journal head, so once 1.7 seeds the mirror at creation every rewound match pins the shadow mismatch counter above zero - the S7 receipt must say so rather than be surprised by it. Mode stays off, so neither the guard nor the refusal is reachable in production.
- [x] 1.6 S5-b: the expected revision for a commit comes from the journal head; sequence-conflict and duplicate-command refusals proven against races.
  - Receipt (2026-09-15, S6 landed as three merged prefixes - A PR #1728 `16e7346f20303acd518df8c21c7b2ff04a22fe0a`, B PR #1729 `423381ff0eb026bb4f3834949aae555cd70d0ca9`, C PR #1730 `00901f4e097d9973ba4d48369d343f93d52092c8`; exact-main proofs `evidence/r4-s6-a-1728-main-proof-20260915.json`, `evidence/r4-s6-b-1729-main-proof-20260915.json`, `evidence/r4-s6-c-1730-main-proof-20260915.json`, all blob-identical to their reviewed heads, jest exit 0, tsc 0, `openspec validate --all --strict` 0, spec:purpose:validate:strict 0, terminology:validate:strict 0, qc:openspec-ci 0): both clauses of this box are discharged. `resolveMatchCommitExpectedRevision` (`matchCommitJournalHead.ts`) answers the store-derived revision at mode `off`/`shadow` and, at mode `enabled`, reads `readEffectiveStreamHead` and returns the journal head's own revision instead - PR A. The race and duplicate-command refusals are proven with two writers consulting the same head before either appends on real SQLite: `MatchStreamJournalMirror` surfaces the loser as typed `revision-conflict` (the `sequence-conflict` reason this box names, once translated by `ServerMatchHostJournalAuthority.ts:111/258`) with expectedRevision/actualRevision and no partial write, and a same-command-id retry as typed `duplicate-command` with the journal rows byte-unchanged - PR B, with PR C restoring the never-rewound race control and the stale-by-one-revision replay-from-receipt row that proves identity is checked before revision. Independent review verdict APPROVE, zero required edits (`evidence/r4-s6-journal-head-revision-review-20260915.json`); axis 1 (design) and axis 2 (tests) both PASS, with the reviewer independently re-running the mutant matrix (8/8 killed) including M4 (the mutant that keeps the `journal` label while sourcing the number from the store, which survives everywhere except the one log-ahead row in PR B) and M1/M7, all matching the receipt's claims exactly. Two residuals carried, neither a clause of this box's own text: S6-R1 - at mode `enabled` the consult answers the head's revision unconditionally even when the match log holds events the journal never received (the create-path gap), so create-path seeding (R4.S7-a, in progress) must land before any cutover flip, an ordering constraint owned by S7 rather than this box; S6-R2 - M4 is killed by exactly one row (the log-ahead row), so any later change to the consult should re-run M4 specifically. Mode stays off throughout, so none of this is reachable in production; the S5 mutant matrix SEAM obligation task 1.5 deferred here is discharged by the same 8/8 re-run.
- [ ] 1.7 S6: reviewed cutover flag with shadow parity, migration states and rollback law in the campaign cutover's shape; new matches admit through the journal, legacy completed matches keep their reads. Rollback proof: revert the flag, no row erased.
  - Note (2026-09-15, ledger reconcile batch 4, no flip): S7-a1 PR #1737 `bb672249c36200987f13dcde2cf7dd2a410ef677` (mirror strips explicitly-undefined properties in `toMatchJournalBatch` before canonicalisation) and S7-a2 PR #1738 `a8ec5105d6411163311c44fc711332266972e1a7` (`seedJournalFromInitialEvents` seeds a match's opening two events on the create path) are both main-verified (`evidence/r4-s7a-a1-1737-main-proof-20260915.json`, `evidence/r4-s7a-a2-1738-main-proof-20260915.json`; independent review APPROVE, `evidence/r4-s7a-review-20260915.json`); S6-R1 (the create-path gap where the mode-`enabled` consult answered the head unconditionally even when the journal never received the log's prefix) is measured red-then-green by S7-a2 and closed for newly-created matches, with mode still hardcoded `off` in production (`matchJournalAuthority.ts`). S7-b (marker retirement) is under review, not merged (see task 1.3's note). S7-c (routing `MatchRecovery.ts`'s journal arm through `checkpointRecoveryPort`) is owed and untouched. S7-d (the per-match migration-state machine, rollback law and busy_timeout pragma) is owner-ruled as `OD-journal-writer-authority` and untouched. Carried open questions from S7-a: S7a-Q2 (seed idempotency rests on command-identity, would swallow a genuinely divergent retry - owner ruling wanted before any flip), S7a-Q3 (seeding at create makes S5-R1's shadow-mismatch warning MORE reachable for a rewound-then-seeded match - inherited, not closed), S7a-Q4 (a partial `persistInitialEvents` failure reports as legacy rather than damaged - low, scope note). None of this discharges this box's own clauses (shadow parity, migration states, rollback law, the flag review itself); box stays OPEN.
  - Note (2026-09-15, ledger reconcile batch 5, no flip): S7-b is now main-verified for its write/read retirement (task 1.3 FLIPPED on that basis - see its own note): #1744 and #1745 merged and exact-main-proven; #1746 merged (confirmed live via `gh pr view` and `git merge-base --is-ancestor` against origin/main) and is now also exact-main-verified (`r4-s7b-3-1746-main-proof-20260915.json`). S7-c (routing `MatchRecovery.ts`'s journal arm through `checkpointRecoveryPort`, S4-R4) is no longer untouched: PR #1751 `fix(recovery): route the journal arm through the checkpoint door` is OPEN (state confirmed live via `gh pr view 1751`, not merged) with independent review APPROVE, zero required edits (`evidence/r4-s7c-review-20260915.json`; all four S4-R4 properties - empty-history refusal, event-identity-uniqueness, quarantine eligibility, checkpoint-cache refresh - routed on the journal arm). No `r4-s7c-1751-main-proof-20260915.json` exists, so S7-c stays QUEUED (opened and reviewed, not merged, not main-verified) rather than closed. S7-d stays owner-ruled `OD-journal-writer-authority`, untouched. Residuals carried forward: S7b-Q6 (F2 - the command path's fail-closed catch for a corrupt persisted head was removed with the marker read; inert at mode `off`, a question for S7-d) and S7-c's OQ-1 (the consult's `diverged` verdict stays blocked, not quarantined - an owner call, not reversed here) and OQ-4 (S4-N1's trigger has fired now that S7-a made heads real, but the DB-handle-routing fix belongs to the consult, not to S7-c's recovery-routing change, and remains open). None of this discharges this box's own clauses; box stays OPEN.

## 2. Moved requirements (umbrella 14.1, 14.2, 14.4)

- [ ] 2.1 `Combat Intervention Has Distinct Preview and Commit Phases`: finalization commits actor, reason reference, deterministic replacement data and authorized player projections through the authoritative transaction; stale preview refuses typed and appends nothing. Closes umbrella 14.1 by receipt.
- [ ] 2.2 `Combat Rewind Is GM-Only and Append-Only` (replacement-branch clause): a GM commit before the receipt boundary creates a building replacement branch from a trusted checkpoint and preserves prior history.
- [ ] 2.3 `Superseded Combat Commands Are Rejected`: commands name branch and revision; an old-branch command returns `STALE_BRANCH` with the active branch and resync action and appends nothing. Closes umbrella 14.2 by receipt.
- [ ] 2.4 `Combat Rebuild Gates Commands and Activation` (activation clause): one branch-activation fact after replay, validation, fog, hidden-state and viewer-projection checks; a failed check leaves the candidate blocked.
- [ ] 2.5 `Combat Rewind Preserves Viewer-Specific Hidden State`: per-viewer fog, sealed choices, private GM records, owned-unit visibility and public facts rebuilt as of the checkpoint; the preview's `fog-preview-unsupported` refusal lifted by computing the rebuild. Closes umbrella 14.4 by receipt.

## 3. Verification (umbrella 14.5) and the un-gate

- [ ] 3.1 GM intervention suites, deterministic replay tests, fog integration tests and `npm.cmd run verify:qc:gm:campaign-ledger` green on the cutover. Closes umbrella 14.5 by receipt.
- [ ] 3.2 Remove the `test.fail` marks from E2E-40, E2E-41, E2E-42, E2E-43, E2E-44 and E2E-76 one row per seam as its SHALL lands (a strict mark goes red on its own the day the row passes; that red is the signal to remove it, never a reason to skip). Live proof on the production build per row; live mutant per row.
- [ ] 3.3 Update the umbrella's 25.4 count and the archive receipt language: the reachable surface after this change, and that the headline capability withdrawn on 2026-09-04 shipped here.
