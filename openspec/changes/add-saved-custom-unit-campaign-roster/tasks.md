## P. CAMP-PROOF — authority receipt contract
- [ ] P.0 Treat the merged `add-camp01-authority-receipts` `WAVE_CONTRACTS`, logical tokens, digests, resolver, Git/environment/privacy rules, and row commands as the sole authority; every later legacy `npm.cmd`/`node` spelling is a label only and SHALL resolve by command id through that child contract.
- [ ] P.1 Before code, author/review/merge and ledger-account all ten named child OpenSpec changes; then apply only `add-camp01-authority-receipts` to add strict `camp01-authority-receipt/v1` writer/validator in `scripts/qc/camp01-authority-receipt.mjs` and `validate-camp01-authority-receipt.mjs`, bounded run-aware assertion recorders, package scripts `qc:camp01-authority-receipt:{write,validate}`, and opt-in JSON reporter output in `scripts/playwright/run-playwright.mjs`. The controller SHALL create a new detached worktree at the verified reviewed-head/exact-main commit, run repo-local proof code only there, enforce HEAD/tree/index/tracked cleanliness, baseline a sorted recursive relative-path/type/size/SHA-256 manifest of all ignored/untracked entries after dependency bootstrap, require the identical post-run manifest outside one allowlisted finalized run subtree, confine that subtree below non-reparse repo-relative `.sisyphus/evidence/playtest`, and record commit/tree/manifest-match facts; after in-worktree validation it SHALL atomically export only the finalized artifact set to the same repo-relative `--run-root` identity under the initiating checkout's non-reparse durable `.sisyphus/evidence/playtest` tree outside the proof worktree, recompute and match every byte digest and manifest, reopen/revalidate the export, and remove the proof worktree only afterward. The writer SHALL generate/export run identity, resolve only `--command-id` against literal `WAVE_CONTRACTS`, recompute the UTF-8 `JSON.stringify` SHA-256 of its ordered per-platform `[executable,...args]` sequence, execute separate sequence elements with `shell:false`, atomically publish only a complete declared result, retain allowlisted command ids/digests rather than raw argv/environment/path/error/stack/reporter payloads, own `command-result.json`, bind declared reporter/test/status/safe-fingerprint contracts, and reject caller command text or substitutes. Focused mocked GitHub/Git-tree/filesystem tests SHALL reject dirty/reused worktrees, ignored or untracked output outside the finalized allowlist, extra/temp output within it, symlink/junction/traversal escapes, caller commands, shell chaining, altered/omitted/substituted sequences or digests, raw payload retention, unsafe/free-form fingerprints, stale/pre-created/cross-run artifacts, non-canonical/self/inexact reproduction/triage digests, missing/mismatched reproduction/triage/spec/product tuples, disposition set drift or invalid outcome fields, unledgered/post-SHA specs, products predating reproduction/specs, incomplete/duplicate reporter output, abnormal reproduction exits, untriaged observed findings, missing required repairs, durable-export collision/partial-copy/reparse/tamper/digest drift, foreign/unmerged/wrong-SHA PRs, self/wrong-head approvals, unrecognized artifacts, and SHA/tree/path/digest/run-id drift; tests SHALL also prove that fresh pass/fail/missing anchor status and unexpected test failures publish into the result for triage, all four disposition outcomes validate only with their required fields and exact observation-set equality, the same person MAY author a spec and product PR when each exact head has independent non-author approval, and later repair/H validation succeeds exclusively from the durable exports after the proof worktree is absent. The `camp-proof` row SHALL pin one command array for `npm.cmd test -- --watchAll=false --runTestsByPath scripts/__tests__/camp01-authority-receipt-qc.test.ts --runInBand`; run `npm.cmd run qc:camp01-authority-receipt:write -- --wave=camp-proof --run-root=.sisyphus/evidence/playtest/camp-proof-<sha> --sha=<sha> --command-id=camp-proof "--spec=add-camp01-authority-receipts|<spec-pr>|<spec-merge-sha>|<spec-approval-id>|<spec-reviewer>" "--product=<product-pr>|<sha>|<product-approval-id>|<product-reviewer>|pending"` and matching reviewed-head validation. After its exact-main rerun, the `proof-02-reproduction` row SHALL pin one command array for `npm.cmd run qc:command:browser:quick`; execute its writer/validator using only `--command-id=proof-02-reproduction` and merged CAMP-PROOF provenance. Its complete normalized aggregate SHALL publish for ordinary exit 0 or 1, record each historical anchor as passed/failed/missing plus every unexpected failure and writer-derived `sha256:<64-lowercase-hex>` fingerprint, keep the UTF-8 JSON SHA-256 only in `command-result.json`, and support later recomputation from the durable export. Before CAMP-00, merge an independently approved audit-only PR and run the zero-command `proof-02-triage` row with one repeated `--disposition="<observation-id>|<failure-fingerprint>|<severity>|<repair-required-or-external-blocker-or-lower-severity-or-not-distinct-cause>|<cause-fingerprint>|<resolution-ref>|<blocker-ref-or-none>|<backlog-rank-or-none>|<audit-anchor>|<audit-pr>|<audit-merge-sha>|<audit-approval-id>|<audit-reviewer>"` per non-pass observation plus the durable reproduction identity; require exact observation-set equality and a durable writer-owned `proof02-triage.json`. Author/merge one child spec and focused repair PR per distinct in-scope Critical/Major cause without an external blocker, retain explicit blockers and ranked lower-severity findings, and validate all required tuples. Publish CAMP-PROOF as the six ordered PR seams declared in `add-camp01-authority-receipts/tasks.md` (PROOF-1 contract/tests through PROOF-6 receipt handoff), each within 15 files/500 lines owning one behavior seam with its own review, merge, audit, and prune; complete all six before reproduction/triage/repair work.
- [ ] P.2 Pin GitHub provenance to repository id `1014984218`, node id `R_kgDOPH9uGg`, `SwiggitySwerve/MekStation`, and base `main`; query that identity directly, require every cited PR/merge to target it, and accept only a non-dismissed exact-head `APPROVED` review from a non-author with `WRITE`, `MAINTAIN`, or `ADMIN`. Mock-reject fork/wrong-base/retargeted-origin/API-identity drift, wrong merge/head, dismissed approval, unauthorized reviewer, and permission drift.
- [ ] P.3 Pin Windows proof bootstrap to `npm ci --fund=false --audit=false` over the target `package-lock.json`, Node `22.22.0`, npm `11.6.2`, resolved System32 `cmd.exe`, `process.execPath`, the verified npm CLI, and a row-declared child environment. Retain the lockfile/bootstrap/tool/version/allowlisted-environment digest; reject PATH/tool substitution, bootstrap omission/failure, version drift, `NODE_OPTIONS`, `NODE_PATH`, npm user-config/prefix overrides, and undeclared `MEKSTATION_*`, `NEXT_PUBLIC_*`, or `PLAYWRIGHT_*` inputs.
- [ ] P.4 State the trusted-local-controller receipt threat model and test only post-publication byte/manifest drift, not non-repudiation. Bind every cleanup target at creation to canonical non-reparse path, Git worktree id, expected HEAD, branch ref, and old OID; immediately before non-force removal/compare-delete require exact porcelain identity, clean state, and expected ignored/untracked manifest. Mock-reject force/glob/prefix/recursive deletion, initiating/durable/unrecorded/dirty/reparse targets, and path/ref/HEAD/OID races.
## 0. CAMP-00 — packaged loopback listener prerequisite
- [ ] 0.1 Add a red process-level test or bounded injected listener seam proving `HOSTNAME=127.0.0.1` currently binds the unspecified address.
- [ ] 0.2 Pass the configured hostname to the production HTTP listener and prove the actual bound address is `127.0.0.1`, not `0.0.0.0` or `::`.
- [ ] 0.3 Replace or supplement the packaged-security static environment assertion with the live listener-address gate.
- [ ] 0.4 The immutable `camp-00` row SHALL pin one command array for `npm.cmd run validate:multiplayer:packaged-socket`. Run `npm.cmd run qc:camp01-authority-receipt:write -- --wave=camp-00 --run-root=.sisyphus/evidence/playtest/camp00-loopback-<sha> --sha=<sha> --command-id=camp-00 "--spec=bind-packaged-server-to-loopback|<spec-pr>|<spec-merge-sha>|<spec-approval-id>|<spec-reviewer>" "--product=<product-pr>|<sha>|<product-approval-id>|<product-reviewer>|pending"` and `npm.cmd run qc:camp01-authority-receipt:validate -- --wave=camp-00 --run-root=.sisyphus/evidence/playtest/camp00-loopback-<sha> --expected-sha=<sha> --mode=reviewed-head`; after merge rerun both under a new exact-main run root with the exact product merge tuple and exact-main SHA/mode; review, publish within 4 files/180 lines, merge with SHA guard, audit, and prune before CAMP-01A.
  - Receipt tier reached (2026-09-15, box stays OPEN): COMMAND-LEVEL ONLY. The frozen `camp-00` command array ran green on exact main `819ff6fd86696f88b7a9f23540b5caa2ca0a5ace` and all 17 frozen assertions converted true - `openspec/planning/2026-09-12-roadmap-completion/evidence/camp00-receipt-run-main-819ff6f-20260915.json`, which also verifies the contract module is unmodified at that SHA and that the #1701 validator fix landed byte-identical. The controller-registered and PR-provenanced tiers this box's literal text also names are NOT produced: `qc:camp01-authority-receipt:write` / `:validate` were never invoked and the writer-owned artifacts (`command-result.json`, `receipt-manifest.json`) are absent, so nothing here is a receipt DIRECTORY; and the githubReviewGate - a non-dismissed exact-head APPROVED review by a NON-AUTHOR holding WRITE/MAINTAIN/ADMIN on repository id 1014984218, `soloException` false - is owner-blocked on a single-collaborator repository. No agent lane can close this box. Two further blockers recorded there: the `proof-02-triage` and `proof-02-required-repairs` predecessor receipts do not exist, and the post-merge exact-main rerun this box requires presupposes the merged, approved product PR that does not exist.
## 1. CAMP-01A — trusted catalog/readiness boundary
- [ ] 1.1 Add `canonical | custom` plus a raw `unknown` persistence parser returning valid/legacy/invalid resolution; prove only absent normalizes to canonical, while present unknown remains invalid/non-launchable and is never auto-rewritten or inferred.
- [ ] 1.2 Add a runtime-only `loading | ready | unavailable` canonical-combat catalog snapshot: validated browser loading from `/api/units?includeBV=true`, Node fast-forward loading from `NodeCanonicalUnitService`, explicit retryable failure, and no silent empty-success fallback.
- [ ] 1.3 Add one shared exact-ref guard to readiness and materializer input; validation is the first materializer operation before diagnostics, scenario lookup/reuse return, catalog I/O, or mutation, while readiness preserves visible/recoverable blockers.
- [ ] 1.4 Add direct loader/readiness/materializer regressions, including persisted `scenarioIds` reuse and downgrade attempts, proving custom/invalid/forged/stale/loading/unavailable inputs cause no lookup, reuse result, routing, or mutation.
- [ ] 1.5 The immutable `camp-01a` row SHALL pin one command array for `npm.cmd test -- --watchAll=false --runTestsByPath src/types/campaign/__tests__/RosterUnitSource.test.ts src/lib/campaign/readiness/__tests__/missionReadinessProjection.test.ts src/lib/campaign/encounter/__tests__/materializeCampaignMissionEncounter.test.ts --runInBand`. Run `npm.cmd run qc:camp01-authority-receipt:write -- --wave=camp-01a --run-root=.sisyphus/evidence/playtest/camp01a-catalog-<sha> --sha=<sha> --command-id=camp-01a "--spec=add-campaign-roster-source-readiness|<spec-pr>|<spec-merge-sha>|<spec-approval-id>|<spec-reviewer>" "--product=<product-pr>|<sha>|<product-approval-id>|<product-reviewer>|pending"` and `npm.cmd run qc:camp01-authority-receipt:validate -- --wave=camp-01a --run-root=.sisyphus/evidence/playtest/camp01a-catalog-<sha> --expected-sha=<sha> --mode=reviewed-head`; after merge rerun both under a new exact-main run root with the exact product merge tuple and exact-main SHA/mode; review, publish within 10 files/400 lines, merge, audit, and prune before CAMP-01B.
  - Receipt tier reached (2026-09-15, box stays OPEN): COMMAND-LEVEL ONLY. The frozen `camp-01a` command array ran green on exact main `aa26a94f11c0d497df7f62b685de8ab6f9fafaae` (exit 0, 3/3 suites, 29/29 tests) and the frozen suite self-wrote `wave-result.json` (schema `camp01-wave-result/v1`, status `passed`) with all 9 frozen assertions at their expected values and an exact key-set match - `openspec/planning/2026-09-12-roadmap-completion/evidence/camp01a-receipt-run-main-20260915.json`. The controller-registered and PR-provenanced tiers this box's literal text also names are NOT produced: `qc:camp01-authority-receipt:write` / `:validate` were never invoked and the writer-owned artifacts (`command-result.json`, `receipt-manifest.json`) are absent, so nothing here is a receipt DIRECTORY; and the githubReviewGate - a non-dismissed exact-head APPROVED review by a NON-AUTHOR holding WRITE/MAINTAIN/ADMIN on repository id 1014984218, `soloException` false - is owner-blocked on a single-collaborator repository. No agent lane can close this box. The controller tier additionally fails fast on its `camp-00` predecessor, which has no controller-recognized receipt in the durable evidence tree. The archived child change's claim of a prior receipt (PR #1230, the 2026-08-12 council review) was NOT relied upon: that council-review document is confirmed absent from origin/main at this SHA.

## 2. CAMP-01B — authoritative co-op snapshot
- [ ] 2.1 Extend `CampaignSync` with revision-bound source-bearing roster records and authoritative `forceId -> unitIds`; build them from real roster/forces in `CampaignCoopEntryPanel`, preserve them through match registration and `CampaignHostRegistry`, and hydrate the guest mirror.
  - Progress (2026-09-15, box stays OPEN): the coverage gap the R2.camp-2 admission brief named against this box is CLOSED. PR #1715 `273ed7883420d6198094d6d23abde855cc327510` pins the `buildCampaignAuthoritativeState` roster guards the co-op snapshot relies on - invalid persisted source, missing catalog reference, a force referencing a unit absent from the roster projection, a unit claimed by two forces, plus a happy-path row asserting the source identity and the exact force-membership map - with a one-mutation-at-a-time campaign in which every row has a distinct kill set. Exact-main proof `openspec/planning/2026-09-12-roadmap-completion/evidence/r2-camp-2-s2-1715-main-proof-20260915.json`: blob-identical to the reviewed head `fda777ff1`, jest 9 suites / 56 tests exit 0, typecheck 0, `openspec validate --all --strict` 0, qc:openspec-ci 0; independent review verdict APPROVE with no required edits, M3 and M5 reproduced. The box is not flipped: its closure rides the `camp-01b` receipt named in 2.2 below, which is owner-blocked.
- [ ] 2.2 Test host/guest canonical success, source/membership, bootstrap/registration, malformed projection, and stale/mismatched rejection. The immutable `camp-01b` row SHALL pin separate ordered command arrays for `npm.cmd test -- --watchAll=false --runTestsByPath src/types/campaign/__tests__/CampaignSync.test.ts src/pages-modules/gameplay/campaigns/__tests__/CampaignCoopEntryPanel.test.tsx src/lib/multiplayer/server/__tests__/CampaignHostRegistry.test.ts src/lib/campaign/sync/__tests__/sharedCampaignState.integration.test.ts --runInBand` and `npm.cmd run verify:qc:coop-campaign-journey`. Run the authority writer with `--wave=camp-01b`, its run root/SHA/command id, and exact spec/product tuples but no caller command text, then matching reviewed-head validation; after merge rerun both under a new exact-main run root with the exact product merge tuple and exact-main SHA/mode; review, publish within 14 files/480 lines, merge, audit, and prune before CAMP-01C.
  - Receipt tier reached (2026-09-15, box stays OPEN): NO command-level receipt is claimed for `camp-01b`. On exact main `83887b7467deba22c1b0b6aa6fb1be8ba6bc4600` the row's FIRST frozen command array (the 4-file jest run) is green (exit 0, 4/4 suites, 42/42 tests) and the frozen suite self-wrote `wave-result.json` status `passed` with all 6 frozen assertions at their expected values and an exact key-set match; the SECOND array, `npm run verify:qc:coop-campaign-journey`, exits 1 - its 9-suite jest leg (138/138) and `e2e/coop-campaign-two-browser-journey.spec.ts` pass while `e2e/coop-campaign-ui-audit.spec.ts` fails deterministically (re-run once, same signature) on a blanket console-error assertion collecting four 403 responses. Because the frozen array did not run green end to end, nothing above the partial tier exists - `openspec/planning/2026-09-12-roadmap-completion/evidence/camp01bc-receipt-run-main-20260915.json`. The controller-registered and PR-provenanced tiers this box's literal text also names are NOT produced: `qc:camp01-authority-receipt:write` / `:validate` were never invoked and the writer-owned artifacts (`command-result.json`, `receipt-manifest.json`) are absent, so nothing here is a receipt DIRECTORY; and the githubReviewGate - a non-dismissed exact-head APPROVED review by a NON-AUTHOR holding WRITE/MAINTAIN/ADMIN on repository id 1014984218, `soloException` false - is owner-blocked on a single-collaborator repository. No agent lane can close this box. The controller tier additionally fails fast on its `camp-01a` predecessor; the missing-receipt chain is unbroken back to `camp-00`.

## 3. CAMP-01C — participation authorization

- [ ] 3.1 Replace protocol/local-runtime participation with `{ missionId, forceId, choice }`; derive match/player/role in the binder from verified connection/registry state and reject full `IForce`, client-authored identity, foreign/missing force, and stale revision.
- [ ] 3.2 Test forged identity/role, full force, foreign/missing force, stale snapshot, and authorized choice. The immutable `camp-01c` row SHALL pin separate ordered command arrays for `npm.cmd test -- --watchAll=false --runTestsByPath src/types/multiplayer/__tests__/Protocol.test.ts src/lib/multiplayer/server/__tests__/bindCampaignSyncConnection.test.ts src/lib/campaign/coop/__tests__/coopRuntimeSession.test.ts --runInBand` and `npm.cmd run verify:qc:coop-campaign-journey`. Run the authority writer with `--wave=camp-01c`, its run root/SHA/command id, and exact spec/product tuples but no caller command text, then matching reviewed-head validation; after merge rerun both under a new exact-main run root with the exact product merge tuple and exact-main SHA/mode; review, publish within 12 files/450 lines, merge, audit, and prune before CAMP-01D.
  - Receipt tier reached (2026-09-15, box stays OPEN): NO command-level receipt is claimed for `camp-01c`. On exact main `83887b7467deba22c1b0b6aa6fb1be8ba6bc4600` the row's FIRST frozen command array (the 3-file jest run) is green and the frozen suite self-wrote `wave-result.json` status `passed` with all 7 frozen assertions at their expected values and an exact key-set match; the SECOND array is the SAME `npm run verify:qc:coop-campaign-journey` package script as `camp-01b` and fails identically (exit 1, same single spec, same four-403 signature) - one failing leg shared by three waves, and not in any wave's own assertion surface. See `openspec/planning/2026-09-12-roadmap-completion/evidence/camp01bc-receipt-run-main-20260915.json`. The controller-registered and PR-provenanced tiers this box's literal text also names are NOT produced: `qc:camp01-authority-receipt:write` / `:validate` were never invoked and the writer-owned artifacts (`command-result.json`, `receipt-manifest.json`) are absent, so nothing here is a receipt DIRECTORY; and the githubReviewGate - a non-dismissed exact-head APPROVED review by a NON-AUTHOR holding WRITE/MAINTAIN/ADMIN on repository id 1014984218, `soloException` false - is owner-blocked on a single-collaborator repository. No agent lane can close this box. The controller tier additionally fails fast on its `camp-01b` predecessor.

## 4. CAMP-01D — all launch-path enforcement

- [ ] 4.1 Inject the trusted snapshot into mission launch, dashboard, Mech Bay readiness, fast-forward, and every materializer caller with explicit loading/unavailable recovery.
- [ ] 4.2 Make co-op launch resolve force membership and unit sources through the accepted host snapshot before applying the guard inside `launchCoopMission`; directly test canonical success with one launch separately from custom/invalid/forged/stale/loading/unavailable blocked scenarios, each recording zero encounter lookup, reuse result, creation, or launch.
- [ ] 4.3 The immutable `camp-01d` row SHALL pin separate ordered command arrays for `npm.cmd test -- --watchAll=false --runTestsByPath src/lib/campaign/fastForward/__tests__/fastForwardCombatRunner.test.ts src/components/gameplay/pages/campaigns/dashboard/__tests__/CampaignDashboardPage.reactivity.test.tsx src/lib/campaign/coop/__tests__/launchCoopMission.test.ts --runInBand`, `npm.cmd run qc:command:readiness-stable:quick`, and `npm.cmd run verify:qc:coop-campaign-journey`. Run the authority writer with `--wave=camp-01d`, its run root/SHA/command id, and exact spec/product tuples but no caller command text, then matching reviewed-head validation; after merge rerun both under a new exact-main run root with the exact product merge tuple and exact-main SHA/mode; review, publish within 12 files/450 lines, merge, audit, and prune before CAMP-01E.
  - Receipt tier reached (2026-09-15, box stays OPEN): NO command-level receipt is claimed for `camp-01d`. On exact main `83887b7467deba22c1b0b6aa6fb1be8ba6bc4600` the FIRST frozen command array is green (exit 0, 3/3 suites, 14/14 tests) with all 7 frozen assertions at their expected values - including the four `===0` and one `===1` numeric keys - and the SECOND array, `npm run qc:command:readiness-stable:quick`, is green (exit 0, 4/4 suites, 44/44 tests); the THIRD array is the same shared `npm run verify:qc:coop-campaign-journey` script and fails identically. See `openspec/planning/2026-09-12-roadmap-completion/evidence/camp01bc-receipt-run-main-20260915.json`. The controller-registered and PR-provenanced tiers this box's literal text also names are NOT produced: `qc:camp01-authority-receipt:write` / `:validate` were never invoked and the writer-owned artifacts (`command-result.json`, `receipt-manifest.json`) are absent, so nothing here is a receipt DIRECTORY; and the githubReviewGate - a non-dismissed exact-head APPROVED review by a NON-AUTHOR holding WRITE/MAINTAIN/ADMIN on repository id 1014984218, `soloException` false - is owner-blocked on a single-collaborator repository. No agent lane can close this box. The controller tier additionally fails fast on its `camp-01c` predecessor.

## 5. CAMP-01E — saved-design picker and roster identity

- [ ] 5.1 Runtime-validate saved BattleMech index entries and test exact id/source/display/tonnage mapping plus honest invalid-record exclusion.
- [ ] 5.2 Render named Stock Templates and Saved Designs groups with loading, empty, failure/retry, keyboard, feedback, desktop, and 390×844 behavior.
- [ ] 5.3 Propagate stable source/fresh instance without substitution or construction payload and add the Saved Designs picker scenario to `e2e/campaign-customizer-handoff.spec.ts`. The immutable `camp-01e` row SHALL pin separate ordered command arrays for `npm.cmd test -- --watchAll=false --runTestsByPath src/components/gameplay/pages/campaigns/create/__tests__/savedCustomUnitCampaignAdapter.test.ts src/components/gameplay/pages/campaigns/create/__tests__/CreateCampaignPage.RosterStep.test.tsx src/components/gameplay/pages/campaigns/create/__tests__/CreateCampaignPage.rosterPersistence.test.ts --runInBand` and `node scripts/playwright/run-playwright.mjs test --project=chromium e2e/campaign-customizer-handoff.spec.ts --workers=1`. Run the authority writer with `--wave=camp-01e`, its run root/SHA/command id, and exact spec/product tuples but no caller command text, then matching reviewed-head validation; after merge rerun both under a new exact-main run root with the exact product merge tuple and exact-main SHA/mode; review, publish within 10 files/450 lines, merge, audit, and prune before CAMP-01F.
  - Receipt tier reached (2026-09-15, box stays OPEN): COMMAND-LEVEL ONLY. Both frozen `camp-01e` command arrays ran green on exact main `4d7d17c3def5aa1989bd4819654f3eeec1fcc400` and the frozen suite self-wrote `wave-result.json` status `passed` with all 7 frozen assertions at their expected values and an exact key-set match; the row's `canonicalArgvDigest` was recomputed at that SHA and matches the frozen value - `openspec/planning/2026-09-12-roadmap-completion/evidence/camp01efg-receipt-run-main-20260915.json`. The row's captureContract artifacts (`desktop.png`, `mobile-390x844.png`) were NOT produced, because only the writer can issue the bound artifact directory. The controller-registered and PR-provenanced tiers this box's literal text also names are NOT produced: `qc:camp01-authority-receipt:write` / `:validate` were never invoked and the writer-owned artifacts (`command-result.json`, `receipt-manifest.json`) are absent, so nothing here is a receipt DIRECTORY; and the githubReviewGate - a non-dismissed exact-head APPROVED review by a NON-AUTHOR holding WRITE/MAINTAIN/ADMIN on repository id 1014984218, `soloException` false - is owner-blocked on a single-collaborator repository. No agent lane can close this box. The controller tier additionally fails fast on its `camp-01d` predecessor.

## 6. CAMP-01F — durable creation commit

- [ ] 6.1 Require accepted production wizard persistence with honest error/conflict and same-id retry and extend `e2e/campaign-customizer-handoff.spec.ts` through real production submit. The immutable `camp-01f` row SHALL pin separate ordered command arrays for `npm.cmd test -- --watchAll=false --runTestsByPath src/components/gameplay/pages/campaigns/create/__tests__/CreateCampaignPage.submitPersistence.test.tsx src/stores/campaign/__tests__/useCampaignPersistenceStore.test.ts --runInBand` and `node scripts/playwright/run-playwright.mjs test --project=chromium e2e/campaign-customizer-handoff.spec.ts --workers=1`. Run the authority writer with `--wave=camp-01f`, its run root/SHA/command id, and exact spec/product tuples but no caller command text, then matching reviewed-head validation; after merge rerun both under a new exact-main run root with the exact product merge tuple and exact-main SHA/mode; review, publish within 8 files/400 lines, merge, audit, and prune before CAMP-01G.
  - Receipt tier reached (2026-09-15, box stays OPEN): COMMAND-LEVEL ONLY. Both frozen `camp-01f` command arrays ran green on exact main `4d7d17c3def5aa1989bd4819654f3eeec1fcc400` and the frozen suite self-wrote `wave-result.json` status `passed` with every frozen assertion at its expected value and an exact key-set match - `openspec/planning/2026-09-12-roadmap-completion/evidence/camp01efg-receipt-run-main-20260915.json`. The row's reporterContract reports were NOT produced. The controller-registered and PR-provenanced tiers this box's literal text also names are NOT produced: `qc:camp01-authority-receipt:write` / `:validate` were never invoked and the writer-owned artifacts (`command-result.json`, `receipt-manifest.json`) are absent, so nothing here is a receipt DIRECTORY; and the githubReviewGate - a non-dismissed exact-head APPROVED review by a NON-AUTHOR holding WRITE/MAINTAIN/ADMIN on repository id 1014984218, `soloException` false - is owner-blocked on a single-collaborator repository. No agent lane can close this box. The controller tier additionally fails fast on its `camp-01e` predecessor.

## 7. CAMP-01G — downstream Mech Bay resolution

- [ ] 7.1 Resolve by source/ref without stock borrowing or hidden unresolved rows and extend `e2e/campaign-customizer-handoff.spec.ts` through cold-reloaded Mech Bay. The immutable `camp-01g` row SHALL pin separate ordered command arrays for `npm.cmd test -- --watchAll=false --runTestsByPath src/components/campaign/bays/__tests__/MechBay.test.tsx --runInBand`, `npm.cmd run qc:command:readiness-stable:quick`, and `node scripts/playwright/run-playwright.mjs test --project=chromium e2e/campaign-customizer-handoff.spec.ts --workers=1`. Run the authority writer with `--wave=camp-01g`, its run root/SHA/command id, and exact spec/product tuples but no caller command text, then matching reviewed-head validation; after merge rerun both under a new exact-main run root with the exact product merge tuple and exact-main SHA/mode; review, publish within 7 files/350 lines, merge, audit, and prune before CAMP-01H. This validated exact-main receipt is also the cross-path admission evidence consumed by `adopt-campaign-event-journal-authority` PR 3 task 3.0; that PR proves journal-level lineage only and cannot claim or substitute for the CAMP-01H three-session witness.
  - Receipt tier reached (2026-09-15, box stays OPEN): COMMAND-LEVEL ONLY. All three frozen `camp-01g` command arrays ran green on exact main `4d7d17c3def5aa1989bd4819654f3eeec1fcc400` and the frozen suite self-wrote `wave-result.json` status `passed` with every frozen assertion at its expected value and an exact key-set match - `openspec/planning/2026-09-12-roadmap-completion/evidence/camp01efg-receipt-run-main-20260915.json` (three waves, 7 distinct pinned commands, 7 green, 27 of 27 frozen assertions converted, 0 failed). The row's reporterContract reports were NOT produced. The controller-registered and PR-provenanced tiers this box's literal text also names are NOT produced: `qc:camp01-authority-receipt:write` / `:validate` were never invoked and the writer-owned artifacts (`command-result.json`, `receipt-manifest.json`) are absent, so nothing here is a receipt DIRECTORY; and the githubReviewGate - a non-dismissed exact-head APPROVED review by a NON-AUTHOR holding WRITE/MAINTAIN/ADMIN on repository id 1014984218, `soloException` false - is owner-blocked on a single-collaborator repository. No agent lane can close this box. The controller tier additionally fails fast on its `camp-01f` predecessor. Because this box's validated exact-main receipt is also the cross-path admission evidence for `adopt-campaign-event-journal-authority` PR 3 task 3.0, that consumer is unblocked only at the receipt tier this run did NOT reach.

## 8. CAMP-01H — authority journey and audit receipt

- [ ] 8.1 Block CAMP-01H product implementation until the durable writer-owned triage receipt has exact set equality with every failed/missing historical PROOF-02 anchor and unexpected failure, and every required distinct in-scope Critical/Major repair is separately merged or its external blocker explicit; already-passing anchors require neither disposition nor repair, lower-severity findings stay ranked, shared-cause observations point to a primary disposition, and CAMP-01 SHALL NOT absorb or waive required work. Produce the maintained three-session audit inside H's one parent writer run, and block H completion/merge until it has exactly three evidence-linked witnesses—one each for custom save/reload, campaign/Mech Bay/readiness, and canonical combat/post-battle—with the common parent run id plus pairwise-distinct writer-issued child execution ids, fresh browser-context ids, isolated labeled artifact subtrees, and non-overlapping durable report digests; each witness's authority map SHALL bind its own non-empty route/API/store/persistence/navigation/cold-reload report ids plus type-specific identity/command/result facts, while shared entity ids MAY prove continuity. Also require independently reviewed accessibility, visibility, feedback, recovery, cognitive-load, playability, enjoyment, positive, categorized-finding, and Critical/Major disposition records. Extend `e2e/campaign-customizer-handoff.spec.ts` and the deep-play/long-signoff evidence so the production-created mixed roster visibly rejects the saved-custom row without side effects, launches canonical rows through the real UI/API into one server-issued game session, exposes and accepts a player command, restores the same session authority after navigation and cold reload, reaches a terminal production result, returns to the campaign, persists an accepted post-battle consequence, and cold reloads the same campaign/mission/session/result/consequence plus unchanged saved-custom identity. The immutable `camp-01h` row SHALL use the design's single canonical Windows sequence and digest, in exact order: `npm.cmd run qc:ux-audit:deep`, `npm.cmd run qc:command:browser:quick`, `npm.cmd run qc:campaign-long:browser`, then the three direct `node scripts/playwright/run-playwright.mjs` inventory/helper/sweep commands. CAMP-PROOF SHALL replace `verify:qc:viewport-sweep`'s chained package value with a tested single Node orchestrator that spawns those same three argv arrays sequentially with `shell:false`; the H PR verification transcript SHALL run `npm.cmd run verify:qc:viewport-sweep` verbatim only after that replacement. CAMP-PROOF SHALL also add and test opt-in `CAMP01_ARTIFACT_DIR` support for every H sub-runner, including `scripts/qc/run-ux-walkthrough.mjs`, generate the three labeled child execution ids, deliver only the immutable label/id map to trusted recorders, declare each sub-runner's report/artifact mapping, and reject caller ids, missing parent/assigned-child ids, label/context/report reuse, or any H-created ignored/untracked output outside the writer-issued directory. The valid zero-repair form is `npm.cmd run qc:camp01-authority-receipt:write -- --wave=camp-01h --run-root=.sisyphus/evidence/playtest/camp01h-journey-<sha> --sha=<sha> --command-id=camp-01h "--spec=prove-saved-custom-unit-campaign-journey|<spec-pr>|<spec-merge-sha>|<spec-approval-id>|<spec-reviewer>" "--product=<product-pr>|<sha>|<product-approval-id>|<product-reviewer>|pending" "--triage=<triage-run-root>|<triage-run-id>|<triage-receipt-digest>"` followed by matching reviewed-head validation. For each required distinct cause, append one additional quoted `--repair="<failing-test-id>|<sha256-cause-fingerprint>|<critical-or-major>|<reproduction-sha>|<reproduction-run-id>|<reproduction-receipt-digest>|<sha256-failure-fingerprint>|<child-change>|<spec-pr>|<spec-merge-sha>|<spec-approval-id>|<spec-reviewer>|<product-pr>|<product-head-sha>|<product-merge-sha>|<product-approval-id>|<product-reviewer>"` argument; omit it entirely when no repair is required. The writer SHALL derive complete aggregate counts/statuses, recompute immutable command-sequence/reproduction/triage digests, verify every disposition and required wave/repair spec/product tuple, enforce independent non-author approvals without requiring different spec/product authors, and reject missing route/API/session/command/store/persistence/navigation/reload/post-battle/experience coverage, reused child-execution/context/report evidence, missing/type-mismatched/per-witness authority evidence, three-session cardinality or witness-uniqueness drift, disposition drift, missing required repairs, or unexpected/nonzero final failures. After merge rerun under a new exact-main root with merged provenance and resolved repair evidence; review, publish within 5 files/300 lines, merge, audit, generate/revalidate the writer-owned cleanup receipt, remove the merged local wave branch and registered product/proof worktrees, and block the follower until those cleanup predicates pass.

## 9. Change completion

- [ ] 9.1 Reconcile the parent and all ten child OpenSpecs with their validated exact-main receipts, require CAMP-01H's exact assertion keys to prove all three independent sessions, custom save/reload identity, campaign/Mech Bay/readiness use, blocked-custom plus canonical production combat launch/action/navigation/reload/terminal/post-battle persistence, desktop/mobile/accessibility/visibility/feedback/recovery/cognitive-load/playability/enjoyment evidence and positives, every categorized audit/PROOF-02 observation's reviewed disposition, every required focused child spec/repair PR, observed aggregate count, zero failures, and all three retained regression scenarios; confirm no CAMP-01 Critical/Major remains silently pending, record tenant authentication/ownership as the remote/shared-deployment blocker, and archive only after the full authority journey is clean.
