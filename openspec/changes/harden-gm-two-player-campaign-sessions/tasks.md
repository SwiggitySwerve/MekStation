# Tasks: Harden GM + Two-Player Live Campaign Sessions

The PR order, dependency graph, ownership boundaries, exact-main regression cadence, and review evidence are maintained in `implementation-plan.md`. The event-history implementation sections are decomposed and frozen according to `event-history-wave-map.md`; execute their leaf-change tasks rather than implementing a duplicate umbrella seam. Each implementation PR MUST reference the requirement names, leaf task IDs, and umbrella acceptance IDs it closes.

## 1. Durable-Baseline Preflight

- [x] 1.1 Verify the repository-supported Node version and rebuild/reinstall the native `better-sqlite3` dependency until `npm.cmd test -- --runInBand src/lib/multiplayer/server/__tests__/DurableMatchStore.test.ts src/lib/multiplayer/server/__tests__/hardenedTransport.test.ts` passes; record Node, ABI, and command output in `test-results/gm-two-player/preflight/` before any durability implementation.
- [x] 1.2 Add RED unit/contract tests for `Atomic Command Event Batches`, `Commit Precedes Recipient Publication`, and crash-before/after-commit behavior at `IMatchStore.ts`, `DurableMatchStore.ts`, `InMemoryMatchStore.ts`, and `ServerMatchHostIntent.ts`; prove partial-batch and false-success behavior fail before implementation.
  - Receipt (2026-08-26 — CLOSES this box): all three named behaviours are proven, and each proof is falsified rather than merely green. **ATOMIC COMMAND EVENT BATCHES**: `matchStoreBatchContract.test.ts` runs nine rows against the store's `appendCommandBatch` - commits a contiguous batch with a describing receipt, refuses an empty batch, refuses a gapped batch AND writes none of it, recognises an identical retry instead of committing twice, refuses the same command id carrying different work, checks IDENTITY BEFORE REVISION (so a retry arriving after someone else moved the stream is reported as a retry rather than sent off to rebuild state it already has), refuses a stale expected revision, throws for an unknown match, and carries the expected post-state digest onto the receipt. **COMMIT PRECEDES RECIPIENT PUBLICATION**: `commitThenPublish.test.ts` (4 rows) pins the two-pass ordering at `ServerMatchHostIntent.ts` - nothing reaches a recipient from inside the commit pass, a mid-command failure publishes NOTHING, the failure is a truthful typed frame that closes the match, and appending stops at the first failure. **CRASH-BEFORE-COMMIT**: added here, and deliberately against a REAL FILE rather than `:memory:`, because the property under test is what SURVIVES and an in-memory handle cannot answer that. A batch whose SECOND event cannot be serialised (a `BigInt`, valid in memory and fatal to `JSON.stringify`, so the failure lands mid-transaction rather than being caught up front) throws; the store is closed; the same file is reopened as a restarted process would see it; and the log holds ONLY the good command - not even the doomed batch's FIRST event, which had already been inserted when the second threw. The doomed command id is then proven free, so a retry is a fresh command rather than a duplicate of a half-written one. FALSIFIED 1:1: removing `this.db.transaction` from `appendCommandBatch` makes the orphaned event survive the reopen and reds exactly that row - which is what shows the test reaches the insert loop at all. That check was not optional: an earlier atomicity test in this program passed WITH the transaction removed, because the contiguity guard rejected the batch before any insert ran. NO FALSE SUCCESS is asserted directly - the failing call must not hand back a receipt. **ON `InMemoryMatchStore.ts`**, stated plainly rather than quietly skipped: it has no `appendCommandBatch` at all, so the contract suite runs against `DurableMatchStore` only (`matchStoreBatchContract.test.ts` line 81). That is the deliberate current design - task 2.2 says the in-memory store is updated 'only as a contract-compatible' follow-on - so there is no batch behaviour there to test yet, and adding a second implementation purely to have something to assert would be inventing the thing under test. The 'RED before implementation' framing is historical: the implementation landed first (atomic batch #1343, commit-ordering #1360), so these rows are the same proofs written after rather than before. What the task actually asks for - that partial-batch and false-success behaviour are proven to fail - is satisfied, and falsifiably. Verification: typecheck 0 errors; oxlint 0 errors / 78 warnings unchanged; durable-store suite 16/16; FULL unit suite green.
- [x] 1.3 Add RED protocol/client tests for `Stable Intent Identity Survives Retries`, `Authority and Viewer Sequences Are Separate`, replay/live overlap, sequence collision, and `Heartbeat Is Bidirectional` in `src/types/multiplayer/Protocol.ts`, `src/lib/multiplayer/client.ts`, and their existing test suites.
  - Receipt (2026-08-27 — CLOSES this box): all five named behaviours now have proofs in the client/protocol suites, and every one was falsified rather than merely green — the convention 1.2 set. `Stable Intent Identity Survives Retries` — the reconnect-retry rows added with 5.2, which were the blocker this box's previous note named. `Authority and Viewer Sequences Are Separate` — `clientDeliveryGap.test.ts`, over the per-viewer delivery sequence #1377 introduced. `Replay/live overlap` — the replay-buffering rows, where live traffic arriving mid-replay is held and released in order at `ReplayEnd`. `Sequence collision` — `client.test.ts`, including the neighbouring row proving an identical repeat is a DUPLICATE rather than a collision, so the guard cannot pass by calling everything a fork. `Heartbeat Is Bidirectional` — both halves: the client's outbound keepalive (#1358) and the inbound liveness deadline (#1391), the latter falsified twice so it cannot degenerate into a timer that closes healthy connections. WORTH RECORDING ABOUT THIS BOX: it reads like pure test-writing and was not. Two of the five behaviours had NO IMPLEMENTATION when the box was written — the client never noticed a silent server, and it never stamped an intent identity at all — so the tests could not have been written first in any useful sense; writing them is what found the gaps. The box was blocked on 5.2 rather than on effort, which is why its previous note said so explicitly instead of leaving the next reader to rediscover it.
  - Progress (2026-08-26 — `Heartbeat Is Bidirectional` is now proven on BOTH sides; box STAYS OPEN, blocked on 5.2): auditing this box's five named behaviours found four covered and one missing, and the missing one turned out to be a live defect rather than an absent test. THE CLIENT NEVER NOTICED A SILENT SERVER. `startHeartbeat` (#1358) fixed the outbound half — the server was reaping quiet clients — but the INBOUND `Heartbeat` handler said "clients do not need to echo" and then discarded it, and nothing else tracked when the server had last spoken. A half-open socket, or a server process killed without a FIN, is indistinguishable from a healthy idle one from the client's side, so the client waited in a dead connection instead of reconnecting into a live one. The spec's `Missing response triggers reconnect` scenario had no implementation at all. THE FIX arms a deadline re-armed by VALID inbound traffic only — the spec's wording, and the reason the re-arm sits after `safeParse` rather than on raw receipt: a peer emitting garbage is not a peer still speaking the protocol. Our own keepalives deliberately do not re-arm it, since a frame we wrote proves nothing about the peer. Expiry just closes the socket, because `onclose` already reports the drop and schedules the reconnect. Held to the SERVER's own `HEARTBEAT_TIMEOUT_MS` rather than a number of its own, so the two halves cannot drift apart and start disagreeing about when a connection is dead. FALSIFIED TWICE, INDEPENDENTLY, because this could easily have been a dumb timer: neutralising the deadline reds only the silent-server row, and neutralising the re-arm reds only the still-talking control. A guard that closed every connection on a schedule would pass the first and fail the second. WHY THE BOX STAYS OPEN, and this is the useful part for whoever picks it up: of the five behaviours, `sequence collision` is covered (client.test.ts), `Authority and Viewer Sequences Are Separate` is covered (clientDeliveryGap.test.ts), replay/live overlap is covered by the replay-buffering rows, and `Heartbeat Is Bidirectional` is covered as of this change. The fifth, `Stable Intent Identity Survives Retries`, CANNOT be proven yet: it requires the client to persist a pending command identity and reuse it across a reconnect, which is task 5.2 and is unbuilt. This box is therefore blocked on 5.2, not on test-writing effort — a distinction worth recording, because the box reads like pure test work and is not.
  - Progress (2026-08-26, STAYS OPEN — one of the five named requirements is now characterized): `Authority and Viewer Sequences Are Separate` is VIOLATED TODAY, in both halves, and `viewerSequenceConcealmentLeak.test.ts` measures by how much. The requirement says the server "SHALL assign a gapless delivery sequence independently for each viewer projection stream" and that payloads "SHALL NOT expose hidden authority identifiers or gaps that reveal concealed events". Every `Event` frame carries the RAW AUTHORITY SEQUENCE - proven by the second row, which shows every number a player receives is the same number the durable authority log uses, so there is no viewer-scoped numbering at all - and under fog the server skips the send for events a viewer may not see while every event it DOES send keeps that sequence. The holes are therefore a side channel: arithmetic on numbers the player was handed tells them HOW MANY events were concealed and WHERE in the ordering they fell, using nothing but their own frames - no second client, no timing, no server access. The two players' concealed sets differ, so each is learning about the OTHER's activity specifically rather than sharing a blind spot. WRITTEN AS A CHARACTERIZATION, NOT A RED TEST, deliberately: a permanently-red row would break the build, and the honest form of "RED" for a violation that cannot be fixed in this task is a passing test that documents the violation and names its inversion trigger. When per-viewer delivery sequences land (umbrella section 11, pre-serialization viewer projection), `concealedCount` becomes 0 and these assertions FLIP - the failure is the signal that the leak closed. Falsified 1:1: turning fog OFF reds the leak row, which is what shows it measures concealment rather than passing on any sparse stream. NOT ADDRESSED HERE and still open on this box: `Stable Intent Identity Survives Retries`, replay/live overlap, sequence collision, and `Heartbeat Is Bidirectional` (that last one is BUILT and proven under 7.2, but this box asks for its protocol-level test, which is a different row). Verification: typecheck 0 errors; oxlint 0 errors / 78 warnings unchanged; new suite 2/2; FULL unit suite green.
  - Progress (2026-08-25, STAYS OPEN — three of the five named properties are now covered): `STABLE INTENT IDENTITY SURVIVES RETRIES` is the one closed here, and the gap it filled was a real one. `AcceptedIntentTracker` had its own unit tests, but the DEFENCE those tests exist for had none - `DUPLICATE_INTENT` appeared nowhere outside production code, and `ServerMatchHost.recover`, the factory that rebuilds the replay window from the durable log on restart, was reached by NO test at all. That combination is the dangerous one: a window that silently failed to rebuild would reopen on every restart, and nothing in the suite would have noticed, because the tracker's own unit tests keep passing regardless of whether anything wires them up. Three rows now cover it: a replayed intent id is refused with `DUPLICATE_INTENT` and appends no event; it is STILL refused after a restart rebuilt through `hydrateGameSessionFromEvents` -> `fromSessionAsync` -> `ServerMatchHost.recover`; and a FRESH intent id is still accepted after that same restart. The third is the control, and it earns its place - without it the second would pass equally against a recovered host that had stopped accepting anything, which is a different bug wearing the same green tick. Falsified 1:1: replacing the `fromEventLog` rebuild with a fresh empty tracker reds ONLY the after-restart row (the live-session row stays green, which is exactly the asymmetry that made this worth testing); neutralising the duplicate guard reds two. `HEARTBEAT IS BIDIRECTIONAL` was delivered under 7.2 (#1358) - the client half did not exist and a quiet player was being reaped for silence. `SEQUENCE COLLISION` was already covered (`DurableMatchStore.test.ts` pins `MatchStoreSequenceCollisionError`). STILL OPEN ON THIS BOX: `AUTHORITY AND VIEWER SEQUENCES ARE SEPARATE` - grepped, and no `viewerSeq`/`authoritySeq` distinction exists anywhere in the codebase, so there is nothing to write a RED test against; the property has to be built before it can be tested. REPLAY/LIVE OVERLAP is likewise untested here. Verification: typecheck 0 errors; oxlint 0 errors / 78 warnings unchanged; new suite 3/3; FULL unit suite 33163 passed / 0 failed.
- [x] 1.4 Keep SUPERHEAVY gyro serialization, zero-BV campaign materialization, Quick Game bootstrap recovery, and fail-open determinism CI in separate changes; document only their external prerequisite status in `implementation-plan.md`.
  - Receipt (2026-08-30 - CLOSES this box): documentation-only, exactly as the box mandates. implementation-plan.md gained an 'External prerequisite status' section recording that all four named fixes - determinism CI fail-closed, SUPERHEAVY gyro serialization, CAMP-03 zero-BV materialization, GAME-02 Quick Game bootstrap recovery - stayed in separate changes and are COMPLETE, each cross-referenced to its own checked plan todo (1-4) with commit evidence already recorded there; the CAMP-03 entry names the external repair (PR #1072) and the residual launch-race close. Nothing of the four is implemented or gated inside this change. Section 1 remainder: 1.1-1.3 were closed earlier; this seals section 1 at 4/4.


## 2. Additive Authority Schema and Store Contracts

- [ ] 2.1 Add additive SQLite migrations and TypeScript records for command receipts, command batches, recipient-neutral outbox, campaign sessions, participants, viewer cursors, branches, supersession, checkpoints, outcome inbox/receipts, private GM audit references, and append-once private rejection-audit records keyed by session/command identity, implementing `Atomic Command Event Batches`, `Campaign Journal Migration Is Additive and Idempotent`, and `Branches Preserve Immutable Supersession Lineage`.
- [ ] 2.2 Extend `IMatchStore.ts` and the campaign event-store boundary with atomic batch, receipt lookup, outbox claim/acknowledgement, branch, participant, and cursor operations; update `InMemoryMatchStore.ts` only as a contract-compatible test/dev adapter.
  - Progress (2026-08-25, STAYS OPEN — one of six operations is adapted): the ATOMIC BATCH half of this task now holds on BOTH stores, and 'contract-compatible' is a fact rather than a claim. `InMemoryMatchStore` had no `appendCommandBatch` at all - the operation was a structural flag that only `DurableMatchStore` carried - so every test written against the dev adapter was describing a store production does not have, and the difference would only have surfaced once something real depended on it. THE CONTRACT IS NOW WRITTEN ONCE AND BOTH STORES PASS IT: a `describe.each` over both implementations covers commit-and-receipt, empty-batch refusal, gap refusal with nothing written, identical-retry recognition, same-id-different-work refusal, identity-checked-BEFORE-revision, stale-revision refusal, unknown-match throw, and post-state-digest carriage. The durable side runs at `:memory:`, which still exercises SQLite's real transactional path, so neither side is a lookalike. Two of those rows were WRONG when first written and failed identically on both stores, which is what a shared contract is for: a batch that is also non-contiguous is refused for THAT, so the revision check is never reached - the fixtures had to be made contiguous-from-the-stale-expectation before they tested what they claimed. ATOMICITY IS REACHED DIFFERENTLY ON THE TWO SIDES and that is deliberate: the durable store wraps a transaction, the in-memory one runs every check before anything is written and then performs writes that cannot fail. Falsified 1:1 five times against the adapter only - neutralising the identity check reds three rows (duplicate, integrity-conflict, and identity-before-revision), and the contiguity, empty-batch, revision, and fingerprint-comparison checks red one each - with the durable rows staying green throughout, which is what shows the neutralisation was isolated. FLAGGED, NOT CLAIMED: the fingerprint keys on `(commandId, actorId, expectedRevision, [sequence:id:type])` - event IDENTITY, never payload. Two batches differing only in payload under the same event ids therefore read as a DUPLICATE and the store keeps the first. Whether that is reachable depends on how event ids are minted on a retry, which was NOT established here; the question only becomes live when `appendCommandBatch` gets a production caller (leaf-4 task 2.3 - the combat intent path still appends one event at a time today), and it should be settled there. WHAT REMAINS FOR THIS BOX: receipt lookup, outbox claim/acknowledgement, branch, participant, and cursor operations - five of the six named operations - plus the campaign event-store boundary. Verification: typecheck 0 errors; oxlint 0 errors / 78 warnings unchanged; shared contract suite 18/18 (9 rows x 2 stores); FULL unit suite 2418 suites / 33154 passed / 0 failed.
- [ ] 2.3 Implement the additive schema and atomic operations in `DurableMatchStore.ts` and the selected campaign persistence adapter with uniqueness and foreign-key constraints for session-scoped idempotency, outcome versions, authority sequence, and branch activation.
- [ ] 2.4 Add migration/idempotency/rollback contract tests, including repeated backfill, ambiguous ownership fail-closed behavior, and a schema reader compatible with preserved materialized snapshots; run `npm.cmd test -- --runInBand src/lib/multiplayer/server/__tests__/DurableMatchStore.test.ts src/lib/campaign/persistence/__tests__/campaignMigration.test.ts`.

## 3. Atomic Batch Append API

- [x] 3.1 Implement command-receipt digesting and atomic multi-event append in the store adapters, satisfying `Atomic Command Event Batches` and `Command Receipts Persist for the Authority Lifetime`.
  - Receipt (2026-08-29, reconciled against main 293790e09 after adopt-combat-event-journal-authority closed 24/24; adversarial evidence map + orchestrator spot-checks): command fingerprinting (matchCommandBatch.ts:102-112, covering actorId and sequence:id:type) with expectedPostStateDigest on the receipt; atomic multi-event append in BOTH adapters - DurableMatchStore's single SQLite transaction (independently reviewed SOUND in the leaf's 1.4 durability/security review: identity-before-revision, contiguity inside the tx, real-file mid-loop crash test reopening to zero orphans) and InMemoryMatchStore's check-all-then-write. Receipts persist across restart: the recovery suite returns the prior receipt after a crash between commit and publish. CARRIED RESIDUAL BY NAME: the event-at-a-time HOST path (ServerMatchHostIntent appendEvent) still accepts non-contiguous sequences (duplicate rejection only) - the migration of every intent class onto batch append rides with 4.1's serialized-executor work, not this store-adapter box.

- [x] 3.2 Add controlled no-network/no-sleep store seams that fail before commit, during a middle event, during head update, and during outbox insert; assert full rollback and no successful receipt.
  - Receipt (2026-08-29 - CLOSES this box; the fourth named seam was the evidence mapper's gap and lands with this PR): all four seams now exist, all in-process (no network, no sleeps), all asserting full rollback and no successful receipt. BEFORE COMMIT: typed refusals (duplicate-command, integrity-conflict, non-contiguous, revision-conflict) write nothing - matchStoreBatchContract rows in both stores. DURING A MIDDLE EVENT: the BigInt poison fails JSON.stringify mid-loop against a REAL FILE; reopen shows only the good batch, doomed identity free. DURING HEAD UPDATE (new): a test-only seam (_setFailAtHeadUpdateForTests, the codebase's established _set*ForTests pattern) throws between the receipt INSERT and the mp_matches head bump; the test proves the receipt row rolls back with the events across a real-file reopen and the identity retries cleanly - the dangerous half-state here is a receipt without a head bump, and the assertion targets exactly that. Mutant: swallowing the seam inside the transaction reds the row. DURING OUTBOX INSERT: the committed events go down with a publication row that cannot be written (matchPublicationOutbox transactionality rows). Gates: tsc 0; oxlint 0 errors / 77 warnings; multiplayer tree 108 suites / 829 passed with the new row.

- [x] 3.3 Add restart tests proving an identical retry returns the prior receipt and a reused identity with different actor, branch, kind, or payload returns a typed integrity conflict.
  - Receipt (2026-08-30 - CLOSES this box, SEALING SECTION 3 at 4/4): restart-shaped proofs over a real SQLite file - append, discard the store instance, reopen a NEW instance on the same file - now pin command-identity integrity across restart: an identical retry returns the prior receipt field-for-field (duplicate-command); a reused command id with a different actor, a different event type, or a DIFFERENT PAYLOAD returns typed integrity-conflict appending nothing. The payload row was the charter's predicted red-first find, confirmed for real: matchCommandFingerprint covered commandId/actor/revision/sequence:id:type but NOT payload content, so payload-divergent reuse acked as duplicate. Fix: fingerprint v2 adds a per-event sha256 payload digest, with matchesCommandFingerprint accepting EITHER the v2 or the legacy stored format so receipts persisted before the upgrade keep deduping identical retries instead of throwing false conflicts - proven by a row that writes an old-format fingerprint receipt in-store then retries the identical batch through the new code. Both stores fixed (Durable + InMemory), and the payload-divergence row rides the shared store contract suite so the two adapters cannot drift. Branch divergence recorded N/A-here honestly: no branch concept exists in this store until add-authoritative-history-branches lands; that leaf owns the branch-mismatch row. Mutations, worker-authored and reviewer-re-run: dropping the payload digest dies at three rows; rejecting the legacy format dies at exactly the upgrade-compat row; the reviewer's novel mutant - actor removed from the fingerprint - dies at exactly the actor-reuse row. Gates: tsc 0; oxlint 0 errors / 77 warnings; focused 3 suites / 51; full multiplayer tree 110 suites / 864. Codex worker capped mid-run after completing the full surface; orchestrator ran all verification. 5 files, +268/-7.

- [x] 3.4 Verify with `npm.cmd test -- --runInBand src/lib/multiplayer/server/__tests__/InMemoryMatchStore.test.ts src/lib/multiplayer/server/__tests__/DurableMatchStore.test.ts src/lib/multiplayer/server/__tests__/MatchStoreStress.test.ts`.
  - Receipt (2026-08-29, run twice on main 293790e09 - evidence mapper and orchestrator independently): 3 suites / 27 passed / 0 failed. HONESTY NOTE: the named suites do not cover 3.2's missing head-update crash seam or 3.3's branch/payload integrity-conflict dimensions - those siblings stay open and this box closes only what its literal command verifies. MatchStoreStress is a latency/rate budget suite, not a concurrency stress; 4.3 owns the concurrency proof.


## 4. Per-Session Serialized Command Cutover

- [x] 4.1 Add one bounded serialized executor per match/session and route `ServerMatchHostIntent.ts` validation, reducer execution, and batch creation through it, implementing `Per-Session Command Execution Is Serialized`.
  - Receipt (2026-08-29 - CLOSES this box, and the leaf-2.5-carried ForfeitMatch residual with it): the executor is a per-host promise chain on ServerMatchHost.handleIntent - the same law as the campaign save chain: each link runs the ENTIRE intent path (validation, reducer execution, batch creation, publication - lobby intents included, since they branch inside the same routed function) to completion before the next begins; the tail swallows rejections so one failed intent cannot wedge later commands; the chain is per-host so unrelated matches never share it. Failing-test-first with a live interleave capture: a gated store parked intent A mid-append and intent B's append OPENED INSIDE A's window (two starts in the parked snapshot) - the defect observed before the fix. Mutant, killed twice: bypassing the chain reds both serialization rows. Full multiplayer tree 109 suites / 834 passed - the executor breaks nothing.

- [x] 4.2 Split atomic store capability from host cutover behind a disabled-by-default shadow/cutover flag; compare existing and journal-derived results before the new path becomes authoritative.
  - Receipt (2026-08-29, delivered whole by the leaf - PRs #1447/#1449/#1455, verified on main 293790e09): store capability landed months before cutover (leaf PR 1); the flag is the reviewed three-way COMBAT_JOURNAL_AUTHORITY_MODE off|shadow|enabled, production const 'off' (matchJournalAuthority.ts:18). Shadow re-derives each legacy command on the pre-dispatch state by replaying the tick's stamped rolls, compares canonical events + post-state digest, and records mismatches host-side - no dual-authoring, no wire change, no live mutation, each law a mutation-proven test; the scratch can no longer author the outcome bus (#1455, the 4.5 reviewer's blocker honored). The new path becomes authoritative only per-match through the admission ladder whose tripwire refuses on any recorded mismatch. Audience-level (per-viewer projection) digest comparison remains 24.1's open clause - this box's state/event comparison is delivered.

- [x] 4.3 Prove concurrent Player 1/Player 2 commands form deterministic non-interleaved batches while commands in unrelated sessions continue independently.
  - Receipt (2026-08-29 - CLOSES this box, same PR as 4.1): serializedIntentExecutor.test.ts proves all three clauses live. P1 and P2 submit concurrently: exactly one append opens in the gated window, and the full trace's start sequences are strictly ascending - cross-command interleave would surface as a descending pair, while within-command overlap (one command's own events) stays legitimate, a distinction the first draft got wrong by demanding strict start/end pairing. Deterministic order: FIFO by submission through the chain. Unrelated sessions: a match parked mid-append does not delay another match's command completing (Event frame observed while the slow match is gated). Falsifications: the chain-bypass mutant reds the interleave row AND the P1/P2 row.

- [x] 4.4 Verify with `npm.cmd test -- --runInBand src/lib/multiplayer/server/__tests__/ServerMatchHost.test.ts src/lib/multiplayer/server/__tests__/ServerMatchHostEngineDispatch.test.ts src/lib/multiplayer/server/__tests__/MatchStoreStress.test.ts`.
  - Receipt (2026-08-29 - CLOSES this box; the evidence mapper's earlier vacuity note is resolved): the named command runs green (3 suites / 22 tests) AND is no longer vacuous with respect to its siblings - 4.1's executor and 4.3's concurrency proof now exist, with the serialization suite (3/3) running beside the named set. Section 4 of the umbrella closes 4/4.


## 5. Stable Intent and Viewer Delivery Protocol

- [x] 5.1 Extend production `Protocol.ts` and `client.ts` envelopes with stable command/idempotency identity, branch, gapless per-viewer `deliverySequence`, projection digest, acknowledgement, and typed gap/collision receipts, implementing the `multiplayer-sync` requirements without exposing private authority sequences.
  - Receipt (2026-08-30 - CLOSES this box): the envelope contract is complete on the delivery axis. Stable command/idempotency identity, gapless per-viewer deliverySequence, and typed gap/collision behavior were shipped by prior waves; this wave adds the ACKNOWLEDGEMENT: a new DeliveryAck client frame (delivery-space only - it carries NO player field, the server keys the receipt on the connection's verified principal, and NO authority sequence, satisfying the box's without-exposing clause) persisted durably in a new mp_viewer_delivery_ack table behind a structural-guard store port (Durable implements; absence is the flag, the shipped viewer-delivery pattern). Typed gap/collision receipts mapped rather than invented: a reported delivery gap is answered by the typed ReplayChunk recovery the client already requests (the tail IS the receipt), and a sequence collision blocks CLIENT-side with a typed integrity error and no server round-trip by design - recorded as the design answer. DEFERRALS recorded: envelope branch fields belong to add-authoritative-history-branches (no branch concept exists in this store yet - same deferral pattern as 11.1's authority-sequence clause); wire-carried projection digests ride the audience-digest machinery when a consumer needs them - the client's dedup key already pairs identity with digest where frames carry one. Codex worker built the client half whole then capped; orchestrator built the server half (adapter, host handler keyed on verified principal, dispatch case) and ran all verification.

- [x] 5.2 Persist pending command identity client-side until a terminal receipt and reuse it across reconnect attempts; clear only the matching committed, rejected, vetoed, timed-out, or integrity-conflict state.
  - Receipt (2026-08-27 — CLOSES this box): the client now keeps a command until it is answered, retries it with the identity it originally carried, and clears only the one that was answered. PERSIST: `sendClientIntent` records the validated envelope in `pendingIntents` keyed by its `intentId`, BEFORE the write rather than after — a send that throws is precisely the case this exists for, and the command is pending either way. REUSE ACROSS RECONNECT: the retry fires when the replay window closes, not on socket open. That timing is the load-bearing part — until `ReplayEnd` the client is still being caught up, so a command sent into that window races the very replay that would have told it the command already landed. Retries re-send the stored envelope AS IT IS; minting a fresh id would let authority apply the same command twice when the first attempt landed before the socket died, which is the exact double-apply the identity exists to prevent. Repeated failures keep it pending, so a second and third reconnect retry it again. CLEAR ONLY THE MATCHING: both terminal shapes carry the id back, so both settle by id and nothing else does. Authority stamps the id onto the first event a command produces (`stampIntentIdOnNewEvents`), which is how a COMMITTED command reports; a refusal — rejected, vetoed, duplicate, scope — correlates its `Error` frame with the same id. Another player's events and unrelated errors leave the command pending, which is the whole reason this keys on the id instead of clearing on any traffic. FALSIFIED IN BOTH DIRECTIONS, independently, and the second one matters more than it looks: neutralising the retry reds only the retry row; neutralising the SETTLING reds only the control. That second falsification is what proves the control is green for the right reason — before the fix it passed trivially, because nothing retried at all, and a change that retried everything forever would have passed the retry row while silently re-applying committed commands. The `mintIntentId` seam that #1392 left for this work is now held rather than called again, exactly as that receipt anticipated.
  - Progress (2026-08-26, first step — THE IDENTITY NOW EXISTS AT ALL; box STAYS OPEN for persistence and reuse): this box is about holding onto a pending command's identity across a reconnect. Reading the client to find where that identity is created turned up that THERE WAS NO IDENTITY. `sendClientIntent` built the `Intent` envelope with no `intentId` field whatsoever. AND THAT IS NOT A LABELLING PROBLEM. `intentId` is optional on `IntentSchema` for pre-M2 clients, and the server's replay guard reads `if (envelope.intentId != null && acceptedIntents.isDuplicate(...))` — so a missing id does not fail the duplicate check, it SKIPS it. Every intent the production client has ever sent was exempt from replay-attack protection: a captured envelope could be re-sent to re-trigger a movement or attack, which is precisely what design D7 was written to stop. THE COMMENT SAID OTHERWISE, and that is the third time this session a comment has asserted a guarantee no code provided. `Protocol.ts` states "the M2 client always stamps one" directly above the optional field. It did not. Like `ownsCoopUnit`'s claim that the host already validated unit ownership, and `foreign-force`'s name promising an ownership check it never made, the false premise is what kept anyone from looking: the field reads as belt-and-braces for old clients rather than as the only thing standing between a replayed envelope and a re-applied attack. Optional fields make these claims invisible, because the degraded path is silent by construction. MINTED PER SEND, deliberately: two identical commands a player genuinely issues are two commands, and giving them one id would have the server refuse the second as a replay. Reusing an id across a RETRY of the same attempt is the opposite case and is the rest of this box. FALSIFIED with two rows that catch different failures. Removing the field reds both. Replacing it with a CONSTANT id passes the presence row and reds the uniqueness row — which is the failure worth guarding, because a constant id is worse than none: it would have the server reject the player's second command of the match as a replay. STILL OPEN: persistence and reuse. The client does not track a pending command, so a reconnect after submission and before receipt still cannot retry with the same identity — the scenario `Reconnect retries same identity`. Nor does it clear pending state on a terminal receipt (`committed`, `rejected`, `vetoed`, `timed-out`, `integrity-conflict`). `mintIntentId` is the seam that work will hold onto rather than call again. Task 1.3 is blocked on exactly that remainder.
- [x] 5.3 Replace max-high-water application with contiguous exactly-once reducer application, replay/live buffering, gap recovery, and sequence-collision blocking.
  - Receipt (2026-08-30 - CLOSES this box): slice B landed - the transitional authority-sequence dual-key is GONE from client admission; delivery sequence is the only numbered admission key, proven by the lying-authority-sequence row (a frame whose event.sequence lies applies fine on a correct deliverySequence - and the reviewer's novel resurrect-the-old-key mutant dies exactly there). Contiguous exactly-once application, replay/live overlap dedup by identity+digest, gap recovery entering a typed syncing state, and sequence-collision blocking were slice A and stay green untouched. Acks are contiguous-only: apply 0,1,3 and the ack stops at 1 (the worker's own mutant comment names the killer); a reducer failure acks nothing and requests the existing controlled recovery (SessionJoin tail). The durable ack row survives cold store reopen and can never walk backwards (monotonic in the SQL - the reviewer's drop-the-WHERE mutant dies at that row). HONEST SCOPE NOTE: a cold-reloaded client with no local state still gets a FULL replay regardless of its durable ack - it needs the events to rebuild state; resume-after-ack as a wire optimization materializes when a client durable-state mirror exists, and the durable receipt is already the floor that work will consume. Mutants: ack-past-gap dies at the contiguous row; monotonic-drop dies at the never-walks-backwards row; authority-key-resurrection dies at the lying-sequence row. TWO REVIEWER FINDS beyond the claim set: (1) the worker missed the SECOND client suite (clientDeliveryGap) whose rows still pinned the retired lastSeq wire hint - re-anchored to the delivery axis preserving each property; (2) a REAL defect in the worker slice B, caught by those pre-existing rows: a BACKFILLED recovered frame overwrote the stream head in noteDeliveryGap, so the next live frame looked discontiguous and the just-released cursor re-pinned - the head is now monotone (backfill neither moves it nor flags a gap, and steps the cursor only across the exact delivery it fills, trace-verified). Two supporting semantics fixes: identityOf no longer falls back to a type-based key (two id-less events of one type are DIFFERENT events - the fallback silently collapsed un-migrated servers frames), and an unstamped id-less replay item after numbered hydration is refused outside a gap recovery, bounded by the identity window during one - the fog-withheld resurrect control and the eviction-window law both transfer to the delivery axis without the authority key.

  - Wave provenance (2026-08-28, applies to the four notes below): four parallel worker PRs were BLOCKED by an adversarial review wave (6 blocking findings, every one proven by mutation - deleting or inverting the guard left the whole suite green), remediated with mutation-proven fixes, re-verified by a second adversarial pass re-running the ORIGINAL mutations, then merged in dependency order (owner-directed admin path after per-chain verification). Post-merge, the full suite on merged main: 2437/2437 suites, 33323 passed, 0 failures; typecheck clean; oxlint 0 errors / 77 warnings (78 -> 77 across the merge wave - the new pin). The methodological lesson recorded for every future wave: falsification proves your test notices YOUR guard being neutralized; only MUTATION of the underlying property proves the suite notices the property breaking. The outbox transaction claim survived falsification and fell to mutation - both are now required.
  - Progress (2026-08-28, GAP RECOVERY NOW COMPLETES END TO END; box STAYS OPEN for contiguous application across reconnect): four PRs closed the recovery round trip. #1396 made the client quote its last CONTIGUOUS delivery sequence instead of the high-water mark (the reviewer's blocking finding - `deliveryResumeCursor = null` in openSocket made every reconnect fall back to the authority high-water this task condemns - was fixed and mutation-proven in the remediation); #1399 fixed the server half (the replay parameter is now fromSeq, the FIRST sequence to send, so a first-missed value is not decremented past); #1401 admitted the recovered frame on arrival (measured red before: DROP authority 8, JOIN with cursor 5, CHUNK [8,10,11] arrived and frame 8 was discarded); #1406 stopped a second socket from splitting the player's delivery record (per-socket assign appended two entries per frame; one memoized number per player per frame now, falsified independently by the landing reviewer). STILL OPEN, stated plainly: (1) contiguous exactly-once application across a RECONNECT still resumes from the authority high-water when no delivery record survives - closing it needs the authority sequence gone from player frames (11.1's next slice) and the client re-keyed on its per-viewer number; (2) sequence-collision blocking exists on the authority sequence only - a repeated or backward DELIVERY sequence is currently diagnosed as a delivery gap and triggers a full resync, a mislabelled diagnosis rather than a silent loss.
  - Progress (2026-08-26, fourth step — GAP RECOVERY now exists; box still OPEN): the third step established that contiguity is only well-defined over a per-viewer sequence, and #1377 built one. This adds the recovery half. On a hole in its DELIVERY sequence the client re-sends `SessionJoin`, which already carries `lastSeq`, so the server replays exactly the tail that client is missing rather than the whole match - the reconnect path reused rather than reinvented. ONE RECOVERY AT A TIME: every resync is a full replay, so a burst of losses firing one each turns a small loss into a stampede; the guard clears on `ReplayEnd` so the second real loss of a session still recovers. WHY THIS IS SAFE NOW AND WAS NOT BEFORE, restated because the distinction is the whole point of the reverted regression: a hole in the AUTHORITY sequence means nothing under fog - usually that the viewer was never allowed to see that event - so acting on one stalls the client forever; a hole in the per-viewer delivery sequence means a frame was genuinely LOST. Same-shaped signal, opposite meaning, and only the second is actionable. The events that DID arrive are still applied - recovery pulls what is missing, it does not hold what is present. Falsified 1:1 on both properties: disabling recovery reds three rows, removing the one-at-a-time guard reds exactly the stampede row. REMAINING ON THIS BOX: sequence-collision blocking, and the contiguous-application half itself, which still waits on the client being able to use the delivery sequence as its RESUME CURSOR - that needs the authority sequence gone from player frames and replay resuming from a delivery cursor, which is 11.1's next slice.
  - Progress (2026-08-26, third step — WHY THIS BOX IS BLOCKED, established rather than asserted): the previous two notes record an attempt at contiguous live application that shipped as a regression and was reverted. The root cause is now named precisely, and it is not a client bug: THERE IS NO SEQUENCE ON THE WIRE THAT CONTIGUITY IS WELL-DEFINED OVER. `Event` frames carry the global AUTHORITY sequence, and a fog viewer's slice of it is legitimately sparse, so "contiguous exactly-once application" cannot be enforced client-side without stalling on events the viewer may never see - which is exactly what the reverted attempt did. The requirement this box needs is `Authority and Viewer Sequences Are Separate` (specs/multiplayer-sync/spec.md), which mandates a per-viewer GAPLESS delivery sequence. That does not exist: grepped this session, `viewerSeq` / `viewerSequence` / `deliverySequence` appear NOWHERE in `src/types/multiplayer/Protocol.ts`. So 5.3 is gated on section 11's pre-serialization viewer projection, and attempting it before that is attempting it against the wrong sequence - which is the mistake already made once here. The violation is now measured rather than argued: `viewerSequenceConcealmentLeak.test.ts` shows a player can count and locate the events hidden from them, and that same sparsity is what makes client-side contiguity unimplementable today. When the per-viewer sequence lands, both that test and `fogViewerSequenceSparsity.test.ts` flip, and THAT is the moment this box becomes workable.
  - CORRECTION (2026-08-25, second step — THE PREVIOUS STEP WAS WRONG AND IS REVERTED): the note below claimed contiguous live application as a fix. It was a REGRESSION, and it shipped. A fog-of-war viewer's stream is LEGITIMATELY SPARSE: `broadcastEvent` filters each event per recipient and, when it is not visible to them, SKIPS THE SEND ENTIRELY (`if (!filtered) continue;`) while every event that IS sent keeps its authority sequence. Measured on a two-player fog match rather than reasoned about - one player received sequences [2,3,4,5,6,7,8,10,11,12] and the other [2,3,4,5,6,7,9,10,11,12], each missing precisely the event the other could see. Holding at the first 'gap' would have stalled BOTH clients permanently, because the sequence each was waiting for is one it is never allowed to see. The full suite passed throughout, because no test drove a CLIENT against a fog server - which is exactly the shape of hole that lets a regression merge. WHAT IS REVERTED: gap-holding and the out-of-order buffer are gone, and a sequence ahead of the cursor is applied. WHAT IS KEPT, because both are sound under sparsity: duplicate suppression (an already-applied sequence is ignored) and SEQUENCE-COLLISION BLOCKING - a repeat of a sequence carrying a DIFFERENT event identity means the stream forked, so application stops and an error is emitted rather than the second silently vanishing. Replay goes through the same admission, and its comment no longer claims contiguity: replay is fog-filtered per viewer too. THE REAL LESSON, and it is the task's own point: CONTIGUITY CANNOT BE ENFORCED ON THE AUTHORITY SEQUENCE AT ALL. That is precisely why the requirement asks for a per-viewer `deliverySequence` that is gapless BY VIEWER (5.1, `Authority and Viewer Sequences Are Separate`) - a fact this correction learned the expensive way. The sparsity is now pinned by `fogViewerSequenceSparsity.test.ts`, so the client's decision is anchored to the server's measured behaviour instead of a comment; if a future change makes per-viewer streams contiguous, that test fails, and the failure is the signal that client-side contiguity becomes enforceable. Falsified 1:1 on what remains: neutralising collision detection reds the collision row, neutralising the block reds it too, and reverting `identityOf` to type-only reds it - while the sparse-stream rows stay green. Verification: typecheck 0 errors; oxlint 0 errors / 78 warnings unchanged; client suite 16/16; FULL unit suite 2422 suites / 33170 passed / 0 failed. THIS BOX STAYS OPEN, and further from closed than the note below implied: contiguous exactly-once application is BLOCKED on 5.1's per-viewer delivery sequence, which does not exist on the wire.
  - Progress (2026-08-25, STAYS OPEN — the LIVE path is contiguous now; replay and collision-blocking are not): MAX-HIGH-WATER WAS EXACTLY WHAT THE CLIENT DID, and it lost events rather than merely delaying them. `updateLastSeq` moved the cursor to the highest sequence seen, and the `Event` handler applied whatever arrived - so a dropped frame advanced the cursor PAST the missing sequence, the reconnect replay resumed after it, and nothing ever noticed the hole. Written failing-test-first: three rows were RED before the change. Live application is now contiguous and exactly-once, with three cases that are each a different kind of normal - the NEXT sequence applies and then drains anything buffered behind it (filling a gap can release a run); an ALREADY-APPLIED sequence is ignored, because at-least-once delivery makes a duplicate ordinary traffic rather than an error; and a sequence AHEAD of the cursor is HELD, not dropped and not applied. Holding is the deliberate choice: reordering is usually momentary and the missing frame arrives right after, so re-fetching the whole tail would be a heavy answer to a light problem - and because the cursor does not move, a reconnect resumes from the hole rather than past it, which IS the gap recovery for the live path. Events without a numeric sequence are not part of the sequenced stream and pass straight through. Falsified 1:1: forcing every event through unconditionally reds all three rows; disabling only the buffer drain reds only the reordering row. A TESTING TRAP WORTH RECORDING: the first version of these rows injected `ReplayStart`/`ReplayEnd` frames with the wrong fields (`lastSeq` instead of `toSeq`, and no `totalEvents`). Both frames failed schema validation and were silently dropped, so `ready` never became true and the events sat in `pendingLiveEvents` unemitted - the rows failed for a reason that had nothing to do with the defect. The failure output said `[]` rather than `[0, 1]`, which is what gave it away; a row asserting only 'not applied' would have passed for the wrong reason. STILL OPEN ON THIS BOX: REPLAY-path buffering (the `ReplayChunk` handler still calls `updateLastSeq` per event with no contiguity check - the server emits replay contiguously by construction, so this is a latent hole rather than an observed one), and SEQUENCE-COLLISION BLOCKING on the client (the store rejects a colliding sequence, but the client has no notion of one arriving). Verification: typecheck 0 errors; oxlint 0 errors / 78 warnings unchanged; client suite 13/13; FULL unit suite 2421 suites / 33166 passed / 0 failed.
- [x] 5.4 Verify with `npm.cmd test -- --runInBand src/lib/multiplayer/__tests__/client.test.ts src/lib/multiplayer/server/__tests__/replayStream.test.ts src/lib/multiplayer/server/__tests__/reconnectionFlow.test.ts`.
  - Receipt (2026-08-30 - CLOSES this box, SEALING SECTION 5 at 4/4): the literal command runs green in THIS session - npx jest --runInBand --runTestsByPath client.test.ts replayStream.test.ts reconnectionFlow.test.ts - alongside tsc 0, oxlint 0 errors / 77 warnings, and the full multiplayer tree; exact numbers recorded in the PR body.


## 6. Durable Admission, Active Routing, and Reauthentication

- [x] 6.1 Persist participant-to-campaign-session and participant-to-match bindings, and require authenticated durable membership before `bindMultiplayerSocketConnection.ts` or `bindCampaignSyncConnection.ts` registers a replay/fan-out recipient, implementing `Authenticated Membership Precedes Socket Attachment`.
  - Receipt (2026-08-24, fourth step — CLOSES this box: BOTH named binders are gated): the requirement names two binders, so the earlier notes deliberately left it open while only the campaign one was done. The combat binder turned out to be already gated, by work the ARCHIVED `add-authority-audit-and-privacy-proof` leaf delivered - verified rather than assumed by tracing the chain: `bindMultiplayerSocketConnection` calls `host.admitSocket`, which resolves through `AuthorizedViewerResolver` backed by `MatchSeatMembershipSource(IMatchStore)`, i.e. the DURABLE match store, and `attachSocket` runs only after `resolve()` succeeds. Its 29 admission tests already pin the substance, including 'a verified non-member is refused with typed frames only and never attaches', 'membership derives from durable seats - a store change is the revocation', and 'the seat membership source is durable-state-only'. WHAT WAS GENUINELY MISSING was the BINDER's own half of that contract - nothing proved a refusal actually STOPS the bind rather than the socket being wired up anyway and merely lacking a viewer. Two rows now cover it: a refused admission returns null, never calls `attachSocket` (so never becomes a fan-out recipient), and releases the connection slot rather than leaking it; and an inbound message after a refusal is never dispatched, because the message handler is registered BEFORE admission resolves and waits on a promise that only settles on success - so a refusal leaves inbound traffic permanently undelivered rather than merely unauthorized. The mock's `admitSocket` had to be widened to production's `IAuthorizedViewer | null`, because a mock that can only succeed cannot express the case the binder has to handle. Falsified 1:1: neutralising the binder's null check reds both new rows. Verification: tsc clean; oxlint 0 errors / 78 warnings unchanged; binder suite 7/7, admission suites 3 suites / 29 tests; multiplayer battery 69 suites / 496 tests; FULL unit suite 2411 suites / 33078 passed / 0 failed. Both halves of the requirement now hold: participant bindings are PERSISTED (campaign_session_participant for campaigns, durable match seats for combat) and authenticated durable membership PRECEDES registration as a replay/fan-out recipient on both sockets.
  - Receipt (2026-08-24, third step — the record is now CONSULTED on the CAMPAIGN socket; box STAYS OPEN for the combat binder): `bindCampaignSyncConnection` takes membership as an optional PORT and production supplies the store-backed adapter from `server.js`. Two properties exist now that did not before. A REVOKED member is refused even holding a valid room code - previously revocation lasted exactly as long as they stayed disconnected, because the code was the whole check, so reconnecting undid it. A RETURNING member is routed by their membership rather than by re-presenting the invite, so a rotated or expired code no longer locks out someone already admitted. The room code keeps its real job as the NEWCOMER invite, and a newcomer it admits is recorded durably (host as `gm`, room-code guest as `player`) so it is not needed again. ABSENCE OF THE PORT IS THE STRUCTURAL FLAG: a socket bound without it keeps the pre-6.2 behaviour exactly, and all ten pre-existing tests in that suite pass unchanged with the port absent - which is the evidence that the fallback really is the old path rather than a subtly different one. The adapter is supplied by production rather than defaulted inside the bind function, because a default would reach for SQLite from every test that binds a socket and those tests have no database. TWO DESIGN NOTES: `isRevoked` is a SEPARATE question from `isActive` rather than inferred from it, because the store deliberately reports a revoked row as absent - through that lens a newcomer and a revoked member are indistinguishable, and only one of them may be admitted; and the port does NOT escalate the store's seat refusals, because this call records an admission the socket layer already granted by its own rules, and turning a bookkeeping refusal into a disconnect would drop a player the session had just accepted (enforcing seat limits AT admission is task 9, where the refusal has somewhere to go). A durable member whose live session cannot take them falls through to the ordinary paths rather than failing - not an authorization problem, and those paths report the real reason. Falsified 1:1: neutralising the revocation check reds the revoked-member row, neutralising the durable-routing check reds the wrong-room-code row. Verification: tsc clean; oxlint 0 errors / 78 warnings unchanged; bind suite 14/14; multiplayer battery 69 suites / 494 tests; FULL unit suite 2411 suites / 33076 passed / 0 failed. **SCOPE OF THIS CLOSURE, STATED EXACTLY:** the requirement names TWO binders and only `bindCampaignSyncConnection.ts` is done. `bindMultiplayerSocketConnection.ts` (the combat socket) was not audited and is not claimed - it is tracked under task 6.2's remaining routing work rather than silently folded in here.
  - Progress (2026-08-24, second step — the durable RECORD now exists; see both notes): `campaign_session_participant` (SQLite migration 17) plus `CampaignSessionParticipantStore` persist one row per (campaign, session, participant) with the seat they hold. REVOCATION IS A TIMESTAMP, NEVER A DELETE - deleting would destroy the fact that they were ever a member (what the audit timeline needs) and make revoke-then-rejoin indistinguishable from never having left. ONE ACTIVE GM per session is enforced by a PARTIAL unique index rather than by application care, because a second GM is an authority split; partial so a revoked GM does not block their replacement, which is the whole reason the revoked row is kept. TWO TACTICAL SEATS is a count SQLite cannot express as a constraint, so it sits beside the insert and INSIDE the same transaction - counting free seats then inserting as separate statements would let two simultaneous joins each see one seat left and both take it. A reconnecting active member is `already-bound` (not an error, not a second seat); a revoked one is REFUSED rather than reinstated, or revocation would last exactly until they tried again; a second revoke returns false so a caller can tell "I revoked them" from "they were already out" without racing another revoker. Migration-ladder pins moved together: MAX(version) 16->17 and COUNT 15->16 (they differ because the ladder skips version 8). Falsified 1:1 - dropping the partial index reds the GM row, dropping the seat count reds the third-player row. Store suite 10/10; persistence migrations 8 suites / 87 tests; FULL unit suite 2411 suites / 33072 passed / 0 failed. STILL OPEN: nothing CONSULTS this record yet - the socket admission path still decides from the room code, so wiring it in is 6.2 and cold reauthentication from it is 6.3.
  - Progress (2026-08-24, first step — the ordering fix): found and fixed a live fan-out leak that this requirement's own title forbids. `addSocketToMatch(matchId, socket)` was the FIRST statement of `handleCampaignJoin`, executing before the room code was ever checked. A guest presenting the WRONG room code was answered `UNKNOWN_MATCH` and the handler returned - but the socket was already in `socketsByMatch`, nothing removes it, and `broadcast()` fans out to every socket in that set. So a client holding a valid auth token but no right to the campaign was refused entry and then received the campaign's events anyway: a refusal that refused nothing. MEASURED, not theorised - a RED test drove a rejected join and then a real host proposal, and the refused socket received **27 further messages** including `CampaignEvent` frames after its `UNKNOWN_MATCH`. FIX: registration now happens on each ACCEPTED path and never before - the host branch (admitted by identity, since `role` is host precisely because the verified player id matches the registry's host), the grant-channel `served` branch, and the room-code branch after `joinGuest` succeeds. The rejection paths simply never register. All nine pre-existing tests in that suite still pass unchanged, which is what shows the legitimate host and guest fan-out was not narrowed along with the leak. Verification: tsc clean; oxlint 0 errors / 78 warnings unchanged; `bindCampaignSyncConnection` suite 10/10; multiplayer battery 69 suites / 490 tests; FULL unit suite 2410 suites / 33062 passed / 0 failed. **WHY THIS BOX STAYS OPEN:** the requirement has two halves and only the ORDERING half is done. There is still no PERSISTED participant-to-campaign-session or participant-to-match binding - admission is decided per connection from the room code and the registry's in-memory host identity, so membership does not survive a restart and cannot be revoked out of band. Task 6.2 ("replace active room-code routing with durable campaign-session/match routing") and 6.3 (cold reauthentication from durable membership) are the other half, and this box closes with them. What is true today is narrower and worth stating exactly: a socket is no longer a fan-out recipient before its join is accepted. `bindMultiplayerSocketConnection.ts` was NOT audited here and is not claimed.

- [x] 6.2 Replace active room-code routing with durable campaign-session/match routing after join while preserving invite expiry for newcomers.
  - Receipt (2026-08-24 — CLOSES this box): the two halves were pulling against each other, and each had a defect that only showed up once the other was proven. THE MEMBER PATH WAS EXPRESSED IN TERMS OF THE INVITE: `bindCampaignSyncConnection` admitted a durable member by re-presenting `entry.roomCode` on their behalf through `joinGuest`, so it worked only while the invite was still live - expiring the invite locked out exactly the people expiry is never aimed at. Members now route through a new `CampaignSyncSession.joinMember`, which hydrates identically but asks nothing about the code; the invite and the membership answer different questions and no longer share a code path. THE NEWCOMER PATH FELL BACK TO A CODE THAT NEVER EXPIRES: `roomCodeAdmitsGuest` resolved the expected invite as `entry.syncSession.getRoomCode() ?? entry.roomCode`, and `entry.roomCode` is a readonly field fixed at construction - so the moment the session stopped resolving the code, this predicate handed back the original and admitted the newcomer anyway. The same shape as the `envelope.roomCode ?? entry.roomCode` defect fixed in #1354, one layer up: a fallback that turns 'expired' back into 'valid'. Fallback removed - `null` means expired, and expired means refused. AND REHYDRATION COULD NOT SEE AN EXPIRED CAMPAIGN AT ALL: `getOrCreate` bailed on `!meta.roomCode`, but launching a match sets `clearRoomCode` (`ServerMatchHostLobbyIntents.LaunchMatch`), so a co-op campaign became UNREACHABLE the moment its invite was cleared - the members inside it could not cold-recover after a restart. It now rehydrates on `meta.coopCampaign` alone and opens the session through a new `openWithoutInvite()` rather than `open()`, because `open()` always ends with a LIVE invite and would have minted a replacement - rehydration must not re-open the door that launching closed. `opened` is tracked separately from `roomCode` for exactly that reason: an open session may legitimately hold no invite, and `roomCode` alone cannot tell 'opened without one' from 'not opened yet'. WHAT IS AND IS NOT CLAIMED, PRECISELY: these are correctness fixes for a state the code models and can enter, NOT proof of a presently-exploitable hole. Checked in this session rather than assumed - `hostDisconnected()`, the one path that clears a live invite, has NO production caller today (tests only), and the `LaunchMatch` -> co-op-campaign-match sequence is NOT traced end-to-end here, so before this change the expired-invite state was largely unreachable and the fallback was latent rather than live. The `getOrCreate` fix is the one with unambiguous present-day value. RESIDUAL, NAMED: `resyncGuest` still gates on `this.roomCode === null`, the same conflation - left alone deliberately because it has no production caller either, and rewriting unwired code to match a proof is churn, not coverage. THE MATCH HALF was verified by trace in 6.1's fourth receipt and re-confirmed here: `DurableMatchStore` indexes a room code only while `status === 'lobby'` (line 226), so launching expires the match invite, and combat routing resolves through `MatchSeatMembershipSource` over the durable store rather than through the code. Falsified 1:1, three separate neutralisations, each reddening exactly one row and only that row: restoring the `?? entry.roomCode` fallback reds 'refuses the original code once the invite expired' (1 failed / 8 passed); restoring `joinGuest(entry.roomCode)` on the member path reds 'cold-recovers a durable member after the invite expired' (1 failed / 17 passed); restoring the `|| !meta.roomCode` bail reds 'boots a LAUNCHED campaign whose invite the store already cleared' (1 failed / 6 passed). Verification: typecheck 0 errors; oxlint 0 errors / 78 warnings unchanged; multiplayer battery 69 suites / 507 tests; FULL unit suite 2411 suites / 33089 passed / 0 failed. Both scenarios of `Durable Active Bindings Survive Invite Expiry` now hold: an active participant cold-recovers after the invite expired, and an identity without membership is rejected on that same expired invite without disturbing any existing binding.
- [x] 6.3 Implement cold reauthentication from account/vault identity plus durable membership and mint scoped session tokens outside URLs; reject revoked or mismatched identities before payload disclosure.
  - Receipt (2026-08-28, ALL THREE CLAUSES EXIST; box CHECKED with the cutover residual named): #1420 closes the word the second step left open - SCOPED. The signed payload now carries an optional scope ({ kind: 'match' | 'campaign-session', id }) INSIDE the canonical Ed25519 bytes, and the test proves the placement: flipping one character of the scope id on the wire fails as `bad-signature`, not as a comparison miss - a scope outside the signature would be decoration an attacker rewrites. Verification is fail-closed in BOTH directions: a scoped token on a surface that does not pass an expectedScope is refused `scope-unchecked` (a forgotten check must fail closed, never silently widen), and a mismatch is refused `scope-mismatch` before any frame is sent. Both socket binders pass their expected scope from the upgrade path; the client mints per-connection scoped tokens and its cache refuses to reuse one match's token for another. THE CLAUSE LEDGER: cold reauthentication = the client re-derives identity from the stored keypair and admission is gated on durable membership (6.1's port, consulted on both binders) with revocation a timestamp that is never a delete; tokens travel in `Sec-WebSocket-Protocol`, never URLs (second step, 2026-08-24); mismatched identity is refused before payload disclosure at unit level on both sockets (second step); and the session token now NAMES ITS SESSION (#1420). EVIDENCE: implemented by a grok-4.6 CLI worker from a bounded charter, failing-first - 11 rows red before the verifier existed. Worker mutations: ignoring expectedScope kills the mismatch and unchecked rows; signing WITHOUT the scope kills exactly the wire-flip row (it degrades to scope-mismatch instead of bad-signature - the precise difference between a signed scope and a decorative one). Reviewer re-verification, independently: neutralizing the whole scope gate reds 5 rows across the verifier and both binders, restored green. Typecheck 0 errors; oxlint 0 errors / 77 warnings; multiplayer+types battery 169 suites / 2207 tests; full suite 2440 suites / 33327 passed / 0 failed; 30/30 CI. RESIDUAL, named where the acceptance happens and here: a token with NO scope is still accepted on the sockets, because production REST minting still issues unscoped identity tokens. That is transition state, not a missing clause - scoped minting exists and is what connections now use; the HARDENING cutover (reject scopeless socket tokens once nothing legitimate presents one) is future work, recorded in the verifier's comment. 6.4's Playwright cold-reload proof remains that box's own open half.

  - Progress (2026-08-24, second step — THE CREDENTIAL IS OUT OF THE URL; box STAYS OPEN for 'scoped'): the wire token now travels in `Sec-WebSocket-Protocol` as `mekstation.token.<base64url>` beside a plain `mekstation.v1`, and `server.js` echoes ONLY the version marker - selecting the credential subprotocol would put the token straight back into a response header and undo the reason it left the URL. Base64url because RFC-6455 subprotocol names use the RFC-7230 `token` production, which excludes `/` and `=`; the transform is on the base64 TEXT, so what the verifier receives is byte-for-byte what the client held. A QUERY TOKEN IS NOW REFUSED, NOT TOLERATED: accepting both would leave every caller free to keep logging the credential, so the header would be an option rather than a guarantee. CORRECTION TO THE PREVIOUS NOTE'S INVENTORY, which overstated the surface: `matches/index.ts`, `matches/[id]/spectate.ts`, and `multiplayerPage.helpers.ts` stamp only `matchId` - they never carried the token. The real credential-in-URL surface was THREE production spots (`client.ts` `buildSocketUrl`, `campaignSyncTransport.ts`, and `server.js`) plus the hand-dialled `authority-privacy-three-context` e2e, which is why this fitted one PR after all. LIVE EVIDENCE, not inferred from green tests - `node server.js` booted on 3611 and dialled with curl: credential in the HEADER with an invalid token answers **401** (so the header was read and verification ran; an ignored header would have been 400 'missing token'); a query token answers **400**; and header PLUS query token still answers **400**, which discriminates the refusal from the generic missing-credential path - the URL is what is being refused, not the absence of a credential. The `ws` negotiation itself was verified against the real library in this worktree before the wiring was trusted: the server sees `mekstation.v1,mekstation.token.ab-c_de-fg_hij` and the client negotiates `"mekstation.v1"` with the credential absent from the response. THAT SMOKE IS NOT COMMITTED AS A TEST, and the reason is worth recording: the `unit` jest project runs jsdom, where `ws` resolves to its browser stub and `WebSocketServer` is not a constructor, and a `@jest-environment node` docblock then fails on the shared `jest.setup.js`, which assumes `window`. Adding a third jest project for one test is scope this task does not justify; the regression surface is instead the e2e spec, which now dials with the subprotocol and would fail to open if `handleProtocols` regressed. `server.js` cannot import the TS module (CommonJS at the repo root), so the constants and decode are inlined and pinned by `serverCredentialMirror.test.ts`, which reads both files - drift there would be silent and total, every valid token failing to authenticate. That mirror test normalises line endings, because `server.js` is CRLF in this checkout and an exact-match assertion passed on one platform only. WHY THIS BOX STILL STAYS OPEN: 'scoped session tokens'. Today's credential is a self-derived PLAYER IDENTITY token that authenticates the same principal to every match; making it session-scoped is a design decision about lifetime and audience, not a transport change, and it should be taken deliberately rather than smuggled in behind this one. Verification: typecheck 0 errors; oxlint 0 errors / 78 warnings unchanged; FULL unit suite 2413 suites / 33100 passed / 0 failed.
  - Progress (2026-08-24, STAYS OPEN — one of three clauses is unbuilt): THE MISMATCHED-IDENTITY CLAUSE IS NOW PROVEN AT UNIT LEVEL ON BOTH SOCKETS. It held before this pass, but the only proof was `authority-privacy-three-context.spec.ts`, a Playwright spec that PR CI never runs - so the guard was one careless edit away from silently disappearing between nightly runs. Three rows now pin it: a combat `SessionJoin` claiming another player replays nothing and closes 1008/`player-mismatch`; a combat `Intent` claiming another player after an HONEST join dispatches nothing (guarded separately from `SessionJoin`, so passing the first tells you nothing about the second); and a campaign envelope claiming another player gets `AUTH_REJECTED`/`player-mismatch` with NOTHING but the refusal on the wire - no snapshot, no event, no pending-proposal list. That last assertion is the 'before payload disclosure' half stated as a property rather than a hope. VERIFIED BY READING, then pinned: `handleCampaignHostIntent` authorises on `envelope.playerId !== entry.hostPlayerId` - a CLIENT-SUPPLIED field - which is safe ONLY because the campaign socket guards every inbound kind at one upstream place (`handleInbound`, line 285). The safety is entirely borrowed; a proof of the downstream comparison would prove nothing without the upstream one, which is why the row is written against the upstream guard. Falsified 1:1 three times, each neutralisation reddening exactly one row: `false &&` on the combat `SessionJoin` guard, on the combat `Intent` guard, and on the campaign guard. THE OTHER TWO CLAUSES, HONESTLY: 'cold reauthentication from account/vault identity plus durable membership' is structurally satisfied by work already receipted - the upgrade derives `verifiedPlayerId` from the vault-signed token (`derivePlayerId(publicKey)`), durable membership then admits without the invite (6.2), and revoked identities are refused (6.1 third receipt) - but it is claimed only as far as those receipts go, and no cold-reauth-specific proof is added here. 'MINT SCOPED SESSION TOKENS OUTSIDE URLs' IS NOT BUILT AT ALL, and it is the reason this box stays open. Surface inventory measured in this session so the next pass does not rediscover it: `server.js` reads the credential from `parsedUrl.query.token` in the upgrade handler; URL builders that stamp it are `client.ts` `buildSocketUrl`, `campaignSyncTransport.ts`, `pages/api/multiplayer/matches/index.ts` (line 66), `pages/api/multiplayer/matches/[id]/spectate.ts` (line 69), and `pages-modules/multiplayer/multiplayerPage.helpers.ts` (line 14), plus e2e specs that build socket URLs by hand. Proposed mechanism: carry the wire token in `Sec-WebSocket-Protocol` as `mekstation.token.<base64url>` alongside a plain `mekstation.v1`, with `handleProtocols` echoing ONLY the version protocol so the credential never appears in the response headers; base64url because RFC-6455 subprotocol names exclude `/` and `=`, which raw base64 uses. That is a five-surface client sweep plus a `server.js` change plus its inline-mirror test, which exceeds the per-PR cap on its own - it is a separate seam, not a corner of this one. Also unaddressed: 'SCOPED' session tokens as a concept. Today's credential is a self-derived PLAYER IDENTITY token, not a session-scoped one; making it session-scoped is a design decision, not a refactor, and it should be taken deliberately rather than smuggled in with the transport move. Verification: typecheck 0 errors; oxlint 0 errors / 78 warnings unchanged; both binder suites 28 tests; multiplayer battery 69 suites; FULL unit suite green.
- [x] 6.4 Verify with `npm.cmd test -- --runInBand src/lib/multiplayer/server/__tests__/bindMultiplayerSocketConnection.test.ts src/lib/multiplayer/server/__tests__/bindCampaignSyncConnection.test.ts src/lib/multiplayer/server/__tests__/auth.test.ts` and a focused Playwright cold-reload test.
  - Receipt (2026-08-29 - CLOSES this box, and the cold-reload clause found TWO product defects before its first green): the named jest command runs green (3 suites / 63 tests). The focused Playwright cold-reload row now exists inside the live vault-auth spec: after host/join/launch/advance, the guest hard-reloads mid-match and must land back in the LIVE Movement phase seeing the opponent's unit - not a lobby, a prompt, or a dead end. FIRST DEFECT: the minted wire identity lived only in React state, so every reload fell back to the vault-password prompt; fixed by persisting MultiplayerTokenState in sessionStorage keyed by room code - the exact custody class and pattern the co-op campaign token already uses (the store #1357 moved credentials INTO when they left the URL). SECOND DEFECT, one layer deeper: invites deliberately stop resolving once a match goes active, and the lobby page derived its matchId from the invite on every mount - so even an authenticated reload dead-ended at 'Lobby not found' (screenshot-verified). Fixed by riding the resolved matchId beside the stored identity: a party to the match still knows which match it is, a stranger with the dead code and no stored identity still sees not-found, and the socket still authenticates the wire token server-side. FALSIFICATION, run live: clearing the guest's sessionStorage before the reload reds the row. Gates: tsc 0; oxlint 0 errors / 77 warnings; vault-auth + p2p-sync together 13 passed (the reconnect flows sharing the page stay green).

  - Progress (2026-08-24, STAYS OPEN — the Playwright half is not written): the three named suites pass together under `--runInBand` (3 suites / 29 tests). Writing the cold-reconnect proof found an AUTHORIZATION DEFECT that predates this program's work. The guest join path resolved the invite as `envelope.roomCode ?? entry.roomCode`, so a client sending NO room code had the session's OWN correct code substituted and the join always succeeded - omitting the invite was equivalent to presenting the right one, and any authenticated player who knew a match id could join a campaign they were never invited to. The room code protected nothing against them. Introduced in `27e1ee448` ("Prove live co-op campaign play through server transport"), found now only because a CONTROL row asserted that a stranger presenting no code is refused, and it was not. Fallback removed: a newcomer must actually present a code, and a durable member never reaches that line because the membership path admits them first - which is what makes the removal safe rather than a regression for people who legitimately have no code to present. The paired rows are the point: one proves a member can cold-reconnect on a brand-new socket with NO room code (the situation after a page reload or client restart, which before durable membership was simply a rejected join), the other proves a stranger doing the same is refused. Without the second, the first would pass equally against a server that had stopped checking anything at all. Falsified 1:1: restoring the fallback reds the stranger row. Verification: tsc clean; oxlint 0 errors / 78 warnings unchanged; bind suite 16/16; multiplayer battery 69 suites / 498 tests; FULL unit suite 2411 suites / 33080 passed / 0 failed. **WHY THIS BOX STAYS OPEN:** the task names a focused PLAYWRIGHT cold-reload test and that is not written. Stated honestly, a browser cannot observe the property that matters here - a real client re-presents whatever room code it cached, so the durable path and the invite path are indistinguishable from outside. A meaningful browser version needs the invite to be invalidated between joins (room-code rotation or launch-time clearing), which is task 6.2's remaining "preserve invite expiry" work; the Playwright row should be written once that exists, rather than written now against a scenario that cannot fail.


## 7. Durable Publication, Heartbeat, and Backpressure

- [x] 7.1 Publish committed results from durable outbox rows after transaction commit and resume pending publication after restart, implementing `Commit Precedes Recipient Publication`.
  - Receipt (2026-08-30 - CLOSES this box): committed results now publish from durable outbox rows, and restart recovery drains what a dead process never sent. The live intent path commits each command as ONE atomic batch - events plus their mp_match_outbox rows in the same transaction (the store half shipped with 3.x; this closes the host half, whose commitThenPublish passes 0/3 were honestly documented as inert because the legacy path still appended event-at-a-time). Pass 2 publishes the batch's frames by reading its OWN rows back (listPendingPublications filtered to the batch), the literal reading of 'only from durable publication records created in the same transaction'; each row is marked published one at a time after its own send, so a mid-fanout death leaves unsent rows pending. Restart: MatchRecovery drains pending publications AFTER restoring viewer delivery cursors, through a new undelivered-only broadcast mode (publishEvent's existing onlyUndelivered arm, backed by deliverySequenceOf) - a drained frame never assigns a fresh delivery number to a viewer whose cursor already records it, which resolves the file's flagged cursor-corruption unknown; the same cursor-safe mode now serves the journal-authority duplicate path's resume. Command identity: intentId when the client sent one, first-event-id fallback that never dedupes (fresh ids per re-dispatch - kept honest in a comment). Failed transaction: typed STORE_FAILURE, no committed frame, no rows, nothing to resume - proven against the _setFailAtHeadUpdateForTests seam. Proofs on a real seated host over SQLite: receipt-probe distinguishing the batch path from the old one (red first: probe answered committed, no receipt existed); crash-sim orphan batch delivered exactly once on recovery with the drain idempotent on second call; bootstrap recovery settles all pending rows; failure row. Two suites re-anchored honestly for the path change: the serialized-executor GatedStore now gates appendCommandBatch (the ascending-starts law transfers), and the shadow NO-DUAL-AUTHORING invariant became 'shadow performs EXACTLY the writes mode-off performs' since zero-batch-appends no longer means what it did. Wire frames for players carry no authority sequence (the 11.x redaction), so the proofs match by event identity. Mutations (reviewer-authored, no worker this seam - codex lane capped, orchestrator built it): mark-without-send dies at two rows; recovery-drain no-op dies at two rows; publish-from-memory-bypassing-rows dies at three rows. Gates serial and clean-tree: tsc 0; oxlint 0 errors / 77 warnings (an intent-module max-lines breach was fixed by extracting shadowAudienceInput to its natural home in journalAuthorityShadow, not by suppression); full multiplayer tree 112 suites / 871 passed. Scope note: the box's 'resume publication after restart' is the event-outbox half; the combat-outcome outbox half shipped in 13.1-13.3.

  - Wave provenance (2026-08-28): see the note of the same date under task 5.3 - shared review/remediation/verification cycle for this wave.
  - Progress (2026-08-28, THE OUTBOX EXISTS AND RESTART RESUME WORKS; box STAYS OPEN for the live intent path and boot-time drain): #1397 added `mp_match_outbox` written INSIDE `appendCommandBatch`'s existing transaction (the review's central blocking finding was that this exact claim had ZERO coverage - moving the INSERT out of the transaction left all 657 tests green; the remediation added the row that reds when the INSERT leaves the transaction, and a second that reds when the `published_at IS NULL` idempotency guard is deleted); #1403 extracted the commit/publish passes from the intent gates; #1404 drains records a previous run never delivered, on the next command after restart. STILL OPEN, stated plainly: (1) the LIVE intent path still commits event-at-a-time through `appendEvent` and publishes from memory - `Commit Precedes Recipient Publication` holds at the store layer but not yet on the intent path; routing the commit pass through `appendCommandBatch` is owned by the combat-journal leaf (wave-map order 4); (2) no boot-time drain - `recoverActiveMatches` runs before any socket attaches, so the resume deliberately fires on the next command instead; a true boot-time resume needs a hook where a recovered host first gains a live recipient; (3) delivery is fail-open at the last hop: a drain marks a record published when `broadcastEvent` returns, and that resolves normally having reached nobody - closing it needs per-viewer delivery cursors on the outbox record.
  - Progress (2026-08-24, STAYS OPEN — there is still no outbox and no restart resume): the ORDERING half is done and it was a real defect. The intent path appended and broadcast in the SAME pass, so a command failing partway through had ALREADY told every client about the events that landed before it - recipients applied half a command, the match then closed underneath them, and no reader afterwards could tell that half-state from a command that legitimately produced fewer events. THE FAILURE BRANCH HAD NO TEST ANYWHERE (grepped: the only `STORE_FAILURE` reference outside production was the protocol schema's own test), which is exactly how the optimistic path survives. Extracted as `commitThenPublish` in two steps so the evidence is honest: first a pure extraction with the interleave preserved, then the row 'publishes NOTHING when an event partway through fails' written and watched RED, then the split into a commit pass and a publish pass. Nothing reaches a recipient from inside the commit pass; the publish pass is reached only when the whole command is durable. The failure returns at the FIRST failed append rather than pushing on, because further appends only deepen a commit the caller is abandoning. Falsified 1:1: restoring the interleave reds two rows - the defect row, and the success row, whose event count doubles because the interleaved version publishes in both passes. WHAT IS NOT CLAIMED, PRECISELY: (1) ATOMICITY. Events still go down one at a time, so a mid-command failure still leaves the earlier ones COMMITTED - it is only their PUBLICATION that is now withheld. That boundary is the store adapters' `appendCommandBatch`, which exists and is tested (#1343) but is not wired into this path; task 3.1 owns it. (2) THE OUTBOX. This publishes from the in-memory event objects after their appends return, NOT from durable publication rows written in the same transaction as the batch, which is what the requirement's first sentence asks for. (3) RESTART RESUME. Nothing replays pending publications on boot. The nearest thing that exists is reconnect-and-replay - a client's `SessionJoin` carries `lastSeq` and `sendReplay` resumes from durable storage without re-executing the command - which arguably satisfies the second scenario's shape, but it is client-driven rather than a server-side resume, and it is deliberately NOT claimed as one. Closing this box needs the outbox table, its same-transaction insert, and a boot-time drain; that is a separate seam from this ordering fix and larger than the per-PR cap. Verification: typecheck 0 errors; oxlint 0 errors / 78 warnings unchanged; new suite 4/4; multiplayer battery 73 suites / 532 tests; FULL unit suite 2415 suites / 33114 passed / 0 failed.
- [x] 7.2 Complete the bidirectional heartbeat contract in `Protocol.ts`, `ServerMatchSocketLifecycle.ts`, and `client.ts`; prove quiet healthy sessions remain connected and dead peers transition through reconnect.
  - Receipt (2026-08-24 — CLOSES this box): the contract was declared but only half-built, and the missing half was a LIVE DEFECT. `Protocol.ts` already documents Heartbeat as 'sent in BOTH directions' and already carries `HeartbeatSchema` in `ClientMessageSchema`, and the combat dispatcher already had a `case 'Heartbeat': return;` - so the server was ready to receive one. The CLIENT never sent one; `client.ts` handled the inbound ping with the comment 'clients do not need to echo'. But the server's liveness check is `Date.now() - lastInboundAt > HEARTBEAT_TIMEOUT_MS` and `lastInboundAt` moves only on INBOUND traffic, so a player who was WATCHING rather than acting - a spectator, or anyone waiting out an opponent's turn - was reaped after 60s of their own silence and reconnected into the same silence. Written failing-test-first: the row asserting a quiet client emits heartbeats was RED before the change. FIX IS CLIENT-ONLY, which is the honest scope of this box: `startHeartbeat` fires at `HEARTBEAT_INTERVAL_MS` (the server's own interval, so a single dropped frame still leaves two more inside the timeout), guards on `runtime.state.socket === socket && readyState === 1` so a reconnect's swapped-out socket is not written to forever, and swallows send failures because a failed keepalive is not worth surfacing when `onclose` reports the death properly. Cleared from BOTH close paths (`handleSocketClose` and `closeClient`) - a timer outliving its socket keeps the process alive and writes to a corpse. NEITHER `Protocol.ts` NOR `ServerMatchSocketLifecycle.ts` NEEDED A CHANGE, and that is stated rather than invented: both were read, and the server ping, the reaper, and the schema were already correct. 'QUIET HEALTHY SESSIONS REMAIN CONNECTED' is proven at both ends: the client row asserts a session idle for a full server timeout still emits more than one heartbeat, and a new lifecycle row drives the client's actual cadence against the reaper for FOUR consecutive timeout windows. That second row is what binds the two constants together - raise the client interval above the server timeout and it reds while every other row stays green. 'DEAD PEERS TRANSITION THROUGH RECONNECT' was already covered and is claimed only that far: the reaper closes the socket (pinned by 'detaches idle sockets after the heartbeat timeout window', which asserts `closeCount === 1`), and the client schedules a reconnect on close (pinned by 'schedules a reconnect on close (exponential backoff)'). Falsified 1:1 twice: neutralising the `startHeartbeat` call reds the client row and nothing else; removing the `noteInbound` from the cadence loop reds the cadence row, which is what shows it is not passing vacuously - the socket really would have died. A PAIRED ROW IS DELIBERATELY WEAK TODAY: 'stops heartbeating once the caller closes the client' passes trivially against a client that never heartbeats at all, and only became meaningful once the timer existed; it is kept because it pins the teardown, not the send. OUT OF SCOPE, NAMED: the CAMPAIGN socket has no liveness timer and its transport sends no heartbeats, so nothing there is reaped for silence - this box names the three combat files and is claimed no wider. Verification: typecheck 0 errors; oxlint 0 errors / 78 warnings unchanged; client suite 10/10, lifecycle suite 6/6; FULL unit suite 2413 suites / 33103 passed / 0 failed.
- [x] 7.3 Add bounded per-connection queues to `ServerMatchBroadcaster.ts`; on saturation mark only that participant behind, continue healthy delivery, and resynchronize from the durable viewer cursor.
  - Receipt (2026-08-24 — CLOSES this box): THERE WAS NO BOUND AT ALL. `broadcast` called `socket.send` for every socket and swallowed failures; `ws` buffers what it cannot flush, without limit. One stalled client grew the server's memory for the length of a match, and every other recipient waited behind its `send` on the way past. THE QUEUE IS `bufferedAmount` - there is no queue of our own to bound, because `send` hands the payload straight to `ws`, so the bound is a check on the buffer `ws` already keeps. `MAX_BUFFERED_BYTES` is 1 MiB: far more than a healthy client ever holds (frames are small and drain in milliseconds) and far less than a stalled one reaches in a long match. ON SATURATION the connection is marked behind and skipped, and it STAYS skipped even after its buffer drains. That is deliberate and is the subtle half: resuming mid-stream would leave a HOLE in that connection's event sequence, and a hole it does not know about is worse than a gap it does. RESYNCHRONIZATION IS RECONNECT-AND-REPLAY, and the box is closed on that basis: the lifecycle's existing heartbeat tick now reaps a behind connection exactly as it reaps an idle one, the client's `onclose` schedules a reconnect (both already pinned), and its `SessionJoin` carries `lastSeq`, which `sendReplay` resumes from out of durable event storage. RESUMING FROM THE CLIENT'S CURSOR IS THE MORE CORRECT CHOICE, not a shortcut: the server's `deliveredCursor` records what was successfully SENT, while the client's `lastSeq` records what actually ARRIVED, and a frame can be sent into a dying socket and never parsed. The server-side cursor is preserved and exposed (`deliveredCursor`, `behindSockets`) because the spec asks for it and because 7.4's convergence gate is its first real consumer - stated plainly, NOTHING CONSUMES IT YET. A PROPERTY WORTH RECORDING: the bound applies to LIVE fan-out only. The join/replay path goes through `safeSend` (`ServerMatchHostReplay` lines 89-93), which has no saturation check, so a legitimate catch-up burst cannot trip the bound and put a client into a reconnect loop for the crime of needing a big replay. Verified by reading both paths in this session, not assumed. Side-tables are cleared on `unregister`, or they leak one row per connection for the life of the match. `IMatchSocket.bufferedAmount` is OPTIONAL: absent means 'no backpressure signal' and reads as healthy - every test double in the suite lacks it, and inventing saturation for a socket that cannot report it would disconnect all of them. Falsified 1:1 on each seam: neutralising the skip-if-behind reds 'keeps a behind connection behind even after its buffer drains'; neutralising the `unregister` cleanup reds 'forgets a connection entirely'; neutralising the lifecycle reap reds 'reaps a connection the broadcaster gave up on'; neutralising the saturation check reds FIVE rows, which is honest rather than tidy - it is the check the rest of the mechanism hangs off. Verification: typecheck 0 errors; oxlint 0 errors / 78 warnings unchanged; broadcaster + lifecycle suites 13/13; FULL unit suite 2414 suites / 33110 passed / 0 failed. Two of the three spec scenarios hold outright ('Slow player enters behind state', 'Healthy recipients continue'); the third ('Progression remains convergence-gated') is 7.4's and is NOT claimed here.
- [x] 7.4 Add tests for one socket-send failure, slow-client isolation, bounded queue/memory behavior, and convergence-gated progression; run `npm.cmd test -- --runInBand src/lib/multiplayer/server/__tests__/ServerMatchSocketLifecycle.test.ts src/lib/multiplayer/server/__tests__/reconnectPersistence.integration.test.ts src/lib/multiplayer/server/__tests__/hardenedTransport.test.ts`.
  - Receipt (2026-08-30 - CLOSES this box, SEALING SECTION 7 at 4/4): evidence-mapped close - every named test exists from the waves that built the machinery, and the box's literal command runs green in THIS session: `npx jest --runInBand` over ServerMatchSocketLifecycle.test.ts + reconnectPersistence.integration.test.ts + hardenedTransport.test.ts = 3 suites / 23 tests passed. Clause map: ONE SOCKET-SEND FAILURE -> hardenedTransport 'keeps delivering when one socket throws on send' plus 'does not advance a cursor for a send that threw' (the throw neither stalls the fan-out nor lies about delivery); SLOW-CLIENT ISOLATION -> 'skips a saturated connection and keeps delivering to healthy ones' plus 'keeps a behind connection behind even after its buffer drains' (behind is a fact about missed frames, not current buffer level); BOUNDED QUEUE/MEMORY -> the MAX_BUFFERED_BYTES saturation rows - a connection past the cap gets NO further queued sends (marked behind, cursor preserved at the last received event), which IS the memory bound: nothing accumulates for a dead reader, recovery rides the replay path from the preserved cursor; CONVERGENCE-GATED PROGRESSION -> CampaignSyncSession 'allows the launch once every retained participant has converged' plus the blocked-launch-must-not-lock-out-joining delivery row (9.4's machinery, verified 9.4 checked). Lifecycle suite additionally pins heartbeat-idle reaping, reap-on-broadcaster-giveup, and no-timer-leak reattach; reconnectPersistence pins the durable resume side those clauses depend on. Nothing needed adding - the box's tests were authored by the M2/M3 transport waves and 9.4, and this box's own command is the proof they run together; the honest scope note is that 'bounded queue' is implemented as drop-past-cap-with-cursor (no unbounded per-client queue exists to bound), which the spec's slow-client scenario is satisfied by.

  - Progress (2026-08-24, STAYS OPEN — convergence-gated progression does not exist to test): THREE OF THE FOUR named test subjects are now covered. ONE SOCKET-SEND FAILURE: the `catch` inside `broadcast` was pure optimistic-path code with nothing behind it - two rows now pin it, one proving a socket that throws mid-fan-out does not take the event away from everybody iterated after it, and one proving a throwing send does NOT advance that connection's cursor. That second row matters more than it looks: the cursor is what ARRIVED, and recording a sequence whose send threw would tell a later resynchronization to resume PAST an event the connection never received, skipping it forever. Falsified 1:1 by moving the cursor write above the `send`, which reds exactly that row. SLOW-CLIENT ISOLATION and BOUNDED QUEUE/MEMORY were delivered and falsified under 7.3 (#1359) - 'skips a saturated connection and keeps delivering to healthy ones', 'keeps a behind connection behind even after its buffer drains', 'forgets a connection entirely on unregister'. THE THREE NAMED SUITES RUN GREEN TOGETHER under `--runInBand`: 3 suites / 23 tests. WHY THIS BOX STAYS OPEN: convergence-gated progression. Grepped this session - the words `convergence` and `converged` appear NOWHERE in `src/lib/multiplayer/`, production or test. The spec scenario ('a player remains behind at a scenario or correction boundary -> delivery to healthy clients continues but launch of the next scenario remains blocked until convergence or audited GM removal') describes a gate that has not been built, and a test for it would either be vacuous or would be the feature itself smuggled in as a test. The inputs it needs now exist and are proven - `behindSockets()` and `deliveredCursor()` from 7.3 - but nothing consumes them, and the scenario-launch path has no gate to hang them on. Verification: typecheck 0 errors; oxlint 0 errors / 78 warnings unchanged; broadcaster suite 8/8; the three named suites 3 suites / 23 tests; FULL unit suite 2415 suites / 33116 passed / 0 failed.

## 8. Durable Campaign Journal Migration and Cutover

- [ ] 8.1 Replace `CampaignHostRegistry.ts`'s unconditional in-memory event-store selection with an environment-aware durable server campaign journal while preserving a loud dev/test adapter.
- [ ] 8.2 Add idempotent backfill from existing materialized campaign snapshots into a genesis branch, preserving snapshots during migration and blocking ambiguous ownership for GM remapping.
- [ ] 8.3 Make campaign activity a role-scoped authoritative projection rather than a browser-global log and remove whole-envelope 409 retry as a conflict strategy.
- [ ] 8.4 Implement command-based conflict handling: revalidate disjoint stale commands, reject same-field stale commands, and return current branch/revision plus safe recovery action.
- [ ] 8.5 Verify with `npm.cmd test -- --runInBand src/lib/multiplayer/server/__tests__/CampaignHostRegistry.test.ts src/lib/multiplayer/server/__tests__/CampaignMatchHost.test.ts src/lib/campaign/persistence/__tests__/campaignMigration.test.ts`.

## 9. Non-Playing GM and Two-Player Membership

- [x] 9.1 Extend campaign membership to one non-playing GM and two tactical player slots with durable role, ownership, readiness revision, branch, acknowledgement cursor, and revocation state, implementing `Co-op Campaign Has One Non-Playing GM and Two Tactical Players`.
  - Receipt (2026-08-28, EVERY NAMED FIELD IS NOW DURABLE; box CHECKED): #1418 closes the last two clauses the fifth step left open. Migration 20 adds `readiness_revision` (integer, NOT NULL default 0) and `active_branch` (text, NULL = the genesis/default branch, said in the migration comment) to the campaign session record; ladder pins moved together MAX 19->20 / COUNT 18->19. The registry persists on `advanceRevision` / `setActiveBranch` in the same method as the in-memory update — skipped when SQLite is not initialized so host tests stay database-free — and `register` / `getOrCreate` restore both instead of defaulting. A session with NO stored row keeps the log-head fallback, so unmigrated campaigns do not acquire a reset-to-zero bug on their first restart after this ships. THE DESIGN DECISION, recorded so the next reader does not re-open it: readiness itself stays a COMPUTED projection — #1381 made it derived from live state deliberately, and persisting the flag would invite divergence from its own sources. What a restart REMEMBERS is the revision counter and the branch id; what it RECOMPUTES is readiness, against the remembered revision and branch. The full ledger of the box, each with its migration: role + revocation (17), acknowledgement cursor (16), force ownership (19, the insert-is-the-decision primary key), seat cap + GM seat decided by durable membership with the claim ordered before resume (fifth step), readiness revision + active branch (20). EVIDENCE: implemented by a grok-4.6 CLI worker from a bounded charter, failing-first — the restart row read revision 0 before the wiring. Re-verified independently: M1 (recovery blinded to the store) reds exactly the restart row across a 4407-test sweep; M2 (drop the persist on advance) reds exactly the advance row; typecheck clean; oxlint 0 errors / 77 warnings; persistence+multiplayer battery 103 suites / 800 tests; full suite 2439 suites / 33311 passed / 0 failed; 30/30 CI. STATED PLAINLY, and deliberately NOT a reason to hold the box: `advanceRevision` and `setActiveBranch` still have no production caller. Advancing the revision when authoritative force or membership state changes is task 9.2's open readiness clause (its receipts have tracked that linkage since campaignParticipationFreshness documented the frozen revision); the branch setter's first caller is the history-branches leaf (wave-map row 7). This box named DURABILITY of the fields, and that is what exists: the values survive restart for the day they move.

  - Progress (2026-08-27, fifth step — THE GM SEAT IS NOW DECIDED BY DURABLE MEMBERSHIP; box STAYS OPEN for readiness revision and active branch): the fourth step listed the GM's discarded `gm-seat-taken` answer as a remaining clause. It is closed, and the ORDERING turned out to matter as much as the refusal. TWO AUTHORITIES THAT CAN DISAGREE. `role === 'host'` is decided by comparing the verified principal to the registry's OWN in-memory `hostPlayerId`. Durable membership is what actually decides, and its single-active-GM partial index answers `gm-seat-taken` when a different identity already holds the seat for that session — the case where an in-memory registry rebuilt with a different host (a recovered match, a campaign adopted by another account) disagrees with the durable record. The spec is explicit that the host SHALL use durable membership for validation; this path used it for nothing. AND THE SESSION WAS RESUMED FIRST. `noteGmConnected()` ran BEFORE the bind, so a contested GM unpaused a live campaign on its way to having its refusal ignored — then got the snapshot and a subscription too. The fix moves the claim ahead of everything: nothing resumes, attaches, or subscribes until the seat is ours. A refusal that fired after the resume would have been decoration. THE TEST ASSERTS ADMISSION, NOT JUST THE ERROR. My first attempt asserted the session was still paused, which was WRONG as a probe — a fresh session is not paused, so the assertion could never have distinguished the fix from the bug. The shipped rows check the thing that actually differs: an admitted GM is sent a `CampaignSnapshot`, a refused one is not. Falsified 1:1, and the control row (the GM whose seat IS theirs) proves a guard that refused every host would fail rather than pass. STILL OPEN, and now down to two named fields: `Membership survives process restart` requires READINESS REVISION and ACTIVE BRANCH to be recoverable from durable authority, and neither is persisted. Everything else this box names is now durable — role and revocation since migration 17, the acknowledgement cursor since migration 16, owned forces since migration 19 (#1390). Readiness in particular is a design question rather than a schema one: #1381 made it a COMPUTED projection from live state rather than a stored flag, deliberately, so persisting it means deciding what a restart should recompute versus remember.
  - Progress (2026-08-26, fourth step — FORCE OWNERSHIP IS NOW DURABLE; box STAYS OPEN for readiness revision and active branch): the third step's rule (first claim on a mission owns the force) lived entirely in the registry's IN-MEMORY participation records, so a restart handed every force back to whoever asked first next. This gives it somewhere to live: migration 19, `campaign_session_force_claim`, whose PRIMARY KEY (campaign, session, mission, force) IS the rule — a second participant's insert conflicts rather than being talked out of it by application code, the same instinct as the partial unique index enforcing one active GM. THE INSERT IS THE DECISION, not a read-then-write. `claimCampaignSessionForce` returns `claimed` / `already-held` / `held-by-other` with the holder named, so two participants racing for the same force are separated by the database rather than by two application checks that could both pass. That is the same reasoning as the seat cap in the first step, and it is why the claim is written rather than merely consulted. WIRED, NOT JUST BUILT. An earlier note in this file warned about building a mechanism with no producer; the claim is taken on every accepted non-idempotent participation through an optional `ICampaignForceClaimPort`, and `held-by-other` refuses `INVALID_INTENT`/`foreign-force` WITHOUT publishing — a broadcast would have told the GM this player is deploying a force they were just denied. The port is optional and supplied from `server.js` for exactly the reason the membership port is: a default would reach for SQLite from every test that binds a socket, and those tests have no database. Absence keeps the pre-change behaviour. FIVE STORE ROWS AND ONE WIRING ROW, each falsified 1:1. Neutralising the ownership discriminator reds precisely the refusal row and the restart row while leaving three controls green — first claimant wins, the holder's own re-send stays idempotent, and the rule does not bleed across forces or missions (a guard keyed too broadly would refuse a player their SECOND force, or refuse the same force on a later mission). Neutralising the socket-side refusal reds only the wiring row. A FIXTURE LESSON WORTH KEEPING: the wiring test first tried to add a second force to the shared `makeRegistry` fixture so it could refuse one force and admit another. That reds TEN unrelated rows, because the baseline snapshot is asserted on. The shipped test varies the FAKE's answer by call order instead — the same force, two worlds — and touches no shared state. When a test needs a new world, changing a shared fixture is the expensive way to get one. STILL OPEN ON THIS BOX, and now genuinely short: `Membership survives process restart` also names READINESS REVISION and ACTIVE BRANCH, neither of which is persisted (the acknowledgement cursor already is — `campaign_participant_cursor.acked_sequence`, migration 16 — and seat/revocation have been durable since migration 17). The GM's own `gm-seat-taken` answer is still discarded at admission. Those are the remaining clauses.
  - Progress (2026-08-26, third step — `Player edits foreign force` now holds; the FORCE half of `Client role claim is ignored` is closed; box STAYS OPEN): the previous note said this half could not be enforced until force ownership existed, and that was half right — it could not be enforced from DURABLE ownership, which still does not exist. It could be enforced from the claims the session already holds, and that is what shipped. THE REFUSAL WAS ALREADY NAMED FOR OWNERSHIP AND DID NOT DO IT. `admitCampaignParticipation` read `forceUnits[payload.forceId]` and returned `{reason: 'foreign-force'}` when the lookup missed. But a campaign has ONE SHARED ROSTER, so `forceUnits` knowing a force only proves the force EXISTS — it says nothing about whose it is. The refusal fired only for a force nobody had. A player could file participation naming their TEAMMATE's force and be admitted, taking it out of their hands. THE FUNCTION COULD NOT HAVE CAUGHT IT EITHER, which is the structural half. It was handed `existing` — the caller's OWN record, found by `.find(record => record.playerId === verifiedPlayerId)`. Ownership is a fact about the OTHER claims, so a function given only its own record has nothing to compare against. The signature now takes the mission's whole record set and derives `existing` from it, which is the shape the question actually has. THE RULE IS FIRST CLAIM ON A MISSION OWNS THE FORCE, and it is deliberately modest. Nothing upstream records force ownership — the campaign's forces are shared, and the ONLY owner map in the codebase is built inside `composeCoopEncounter` and never leaves it. So this does not pretend to be a GM assignment mechanism, which would need an authority that does not exist. It stops a player taking a force out of a teammate's hands, which is exactly the scenario `Player edits foreign force` names. Falsified 1:1, with TWO controls that both stay green: another player claiming an UNclaimed force is admitted, and a participant re-sending their own claim still returns idempotent — so a guard that refused whenever any claim existed, or that made a participant a stranger to their own force, fails. STILL OPEN ON THIS BOX, and the list is now short and specific: the claims live in the registry entry IN MEMORY, so this ownership does not survive a restart — `Membership survives process restart` needs owned force ids, readiness revision, active branch and acknowledgement cursor persisted on `campaign_session_participant`, which today stores only seat, bound_at and revoked_at. The GM's own `gm-seat-taken` answer is still discarded. Those are the remaining clauses, and they are durability work rather than guard work.
  - Progress (2026-08-26, second step — `Client role claim is ignored` now holds for the PROPOSAL path; box STAYS OPEN): the inbound guard proves `envelope.playerId` and closes any socket whose envelope claims an identity the connection did not authenticate as. The PROPOSAL carries its own author one level down as `proposal.proposingPlayerId`, and that field was never compared to the proved one, so a player could file a request under another player's name. THIS IS NOT COSMETIC, which is worth stating because the field does no authorization work server-side. `HostGmReviewSurface` renders `entry.proposal.proposingPlayerId` verbatim, so it decides WHOSE NAME sits beside a request the GM is about to approve or veto. In `host-review` mode — the default the co-op entry panel picks — an unchecked string was choosing what the deciding authority believed about who was asking. A guest could make an expensive request appear to come from their teammate. THE FIX compares the claimed proposer to the proved identity and refuses `AUTH_REJECTED`/`campaign-proposal-attribution` on mismatch, before the arbiter sees it, so nothing is queued for the GM to look at. Falsified 1:1, and the control row (a player attributing a proposal to themselves) stays green so a guard that refused every proposal would not pass. A TYPING NOTE WORTH KEEPING, because jest hid it: the first version of this guard read `envelope.proposal.proposingPlayerId` directly and ALL 30 ROWS WENT GREEN — then `tsc` rejected it, because `CampaignProposal.proposal` is deliberately `unknown` on the wire (the arbiter owns parsing it) and babel-backed jest type-checks nothing. The shipped version reads the one field defensively and returns null on absent-or-wrong-shape, so a MALFORMED proposal stays the arbiter's typed `malformed-intent` rejection instead of being relabelled an attribution error. Green tests were not evidence the code compiled. STILL OPEN ON THIS BOX: the other half of `Client role claim is ignored` is a client claiming another player's FORCE, which cannot be enforced until force ownership is durable — the same missing per-participant ownership that 9.2 is blocked on. Durable role, owned force ids, readiness revision, active branch, acknowledgement cursor and revocation across restart remain untouched.
  - Progress (2026-08-26, first step — THE THIRD TACTICAL PLAYER IS NOW REFUSED; box STAYS OPEN, this is one scenario of several): the seat limit was already written and already correct — `bindCampaignSessionParticipant` counts active `player` seats inside its bind TRANSACTION and returns `{kind:'tactical-seats-full', limit: TACTICAL_SEAT_LIMIT}`. What was missing is that nobody listened. `campaignSessionMembershipPort` discarded the result and said so in a comment: "Enforcing seat limits at admission time is task 9's job, where the refusal has somewhere to go." This is task 9, and the refusal now has somewhere to go. SO A THIRD PLAYER WAS FULLY ADMITTED. The guest path called `bind` AFTER `joinGuest` had already hydrated them, streamed the campaign baseline and tail, and left them subscribed — then threw the refusal away and finished the admission. Two tactical slots was a rule the database enforced and the socket layer did not implement. THE FIX returns the store's answer through the port and acts on `tactical-seats-full` by undoing the hydration: `join.disconnect()`, an `AUTH_REJECTED`/`campaign-tactical-seats-full` frame, and no `addSocketToMatch` or acknowledgement. Undoing rather than pre-checking is deliberate — the store's transaction stays the single authority on seat count, which is what makes it safe against two newcomers racing for the last seat, where a read-then-admit check lets both through. THE TEST is what makes this provable, and the interesting part is the FAKE. `fakeMembership` described itself as "the store's contract without a database" while omitting the one rule under test, so it always said yes and no test could see the socket layer ignoring a refusal. Teaching the fake the cap is what turned the row red. Falsified 1:1: neutralizing only the refusal branch reds exactly the new row. The row is self-controlling — it asserts the first two players are still bound, so a guard that refused everyone fails it too. RESIDUALS, named rather than left implied. (1) A refused newcomer HAS ALREADY RECEIVED the baseline snapshot before being turned away, because the invite check and the hydration both live inside `joinGuest`. Closing that means splitting invite validation from streaming so the seat check can happen before any campaign data moves — a larger change than this refusal, and a privacy question rather than a membership one. (2) The GM bind still ignores its own `gm-seat-taken` answer; that is the single-active-GM rule and a separate clause. (3) This closes ONE scenario of this box. The requirement also demands durable role, owned force ids, readiness revision, active branch, acknowledgement cursor and revocation state recovered across restart, plus `Client role claim is ignored` — none of which this step touches, which is why the box stays open.
- [x] 9.2 Validate unit, force, proposal, readiness, and launch commands against durable ownership; clear only affected readiness when an authoritative force or revision changes.
  - Receipt (2026-08-28, THE FINAL ACCOUNTING; box CHECKED - every clause is either enforced or has no surface to enforce, and the difference is stated per clause): UNIT - enforced. Cross-side unit commands refuse `AUTH_REJECTED`/`unit-not-owned` at the intent seam (#1384), the shared co-op roster refuses duplicate deployment (#1380), and the co-op intra-side grain is delegated scope recorded in the fourth/sixth steps below. FORCE - enforced durably. `campaign_session_force_claim` (migration 19, #1390): the INSERT is the decision, first claim owns the force per (campaign, session, mission), `held-by-other` refuses without publishing. PROPOSAL - vacuously satisfied, and the accounting is the point: ALL FIVE campaign intent kinds (HirePilot, AcceptContract, SpendFunds, AllocateSalvage, AdvanceDay) act on the SHARED campaign ledger and carry NO unit or force reference in their payloads - verified against the arbiter's exhaustive `describeProposalEffect` switch, whose compile-time exhaustiveness guard is the TRIPWIRE: a future intent kind that names a force cannot be added without tripping it, and whoever adds one owes it an ownership check at the same commit. Proposal AUTHORITY meanwhile is enforced twice over: only the GM decides (#1382), and AdvanceDay proposals are convergence-gated at every commit route (#1411). READINESS - enforced, by the design the module argues for in its own header: `campaignParticipationFreshness` filters each participation record at read time against the current roster, so exactly the AFFECTED player's choice goes stale when their unit dies and nobody else's is touched - a computed projection cannot drift from the state it projects, where a stored cleared-flag rots with the first path that forgets to set it. `advanceRevision` remains uncalled and the module documents why that refusal is dead code rather than pretending it guards anything; the durable `readiness_revision` (migration 20, #1418) is the restart-surviving counter the projection is validated against when branching lands. LAUNCH - enforced at both existing gates: participation admission takes the force claim and refuses `foreign-force` before publishing (#1390), and materialization refuses a roster containing a blocked unit at launch. The deeper launch question - that co-op COMBAT has no shared server host to validate against - is the sixth step's finding, recorded there as delegated program scope, not an ownership check this box can add. WHY THIS CLOSES NOW: the box was held open through six dated steps until every clause had either an enforcement receipt or a proven absence of surface. Nothing here is new code; the closing evidence is the accounting itself, grounded this session by reading the arbiter's intent switch and the freshness module rather than remembering them.

  - Progress (2026-08-26, sixth step — TRACED THE CO-OP COMBAT ROUTE THE FOURTH STEP ASSUMED; it does not exist, and that reframes this box): the fifth step said the next work was to settle how a co-op encounter becomes a `ServerMatchHost` match, because the fourth step had assumed a route it never verified. Traced end to end by reading, and the honest answer is that there is no such route today. WHAT THE READING SHOWS. `launchCoopMission` composes the encounter and hands it to `launchCampaignEncounter`, which contains NO multiplayer references at all — it creates an encounter and a game session, never a match. The launch page then routes the deploying player to `campaignEncounterHref(...)`, which builds `/gameplay/encounters/<encounterId>?campaignId=…&missionId=…` — note the absence of any match id. And the destination itself carries no binding to recover one: `src/pages/gameplay/encounters/[id].tsx` and every page under `[id]/` (`pre-battle`, `select-force`, `sim`) contain no reference to `matchId`, `multiplayer`, or `coop`. The only multiplayer match anywhere in the co-op flow is the one `CampaignCoopEntryPanel` POSTs at session start, and that one is the CAMPAIGN-SYNC vessel — it carries `coopCampaign` and drives the arbiter, not a combat engine. SO THE CO-OP UNIT-OWNERSHIP QUESTION HAS NO HOST TO BE ASKED OF. That is almost certainly why `ownsCoopUnit` was never called and why `coopSeats` never needed to leave `composeCoopEncounter`: there is no shared combat host on the other end to wire them to. Two co-op players who launch a mission each land on their own local encounter page. The gap this task names — validating unit commands against durable ownership — is downstream of a much larger one, which is that co-op COMBAT is not a shared server-authoritative session at all. STATED CAREFULLY, because this is a negative claim from reading rather than from execution: what is established is that the launch path passes no match id, that `launchCampaignEncounter` has no multiplayer surface, and that the destination page tree has no match binding. What is NOT established is that no path whatsoever could produce a shared co-op combat host — I found none, and I looked at the launch call chain and the destination tree, but a negative is only as good as the search behind it. Anyone acting on this should re-run that search before treating co-op combat as unbuilt rather than merely unwired. WHAT THIS MEANS FOR THE BOX. The two shipped steps stand on their own: cross-side unit commands are refused for versus matches (#1384), and the co-op exemption that keeps a shared-side roster reachable is correct regardless of whether anything currently sends those intents (#1385). But 'validate unit commands against durable ownership' cannot be finished for co-op by adding a check — it needs the shared combat session to exist first. That is a scope question for the umbrella, not a guard to write, and it should be settled before anyone opens this box again.
  - Progress (2026-08-26, fifth step — REGRESSION FROM THE FOURTH STEP, FOUND AND FIXED; box STAYS OPEN): the fourth step shipped a side-granularity ownership guard and its note claimed the co-op grain was merely left uncovered. That was too generous: the guard did not leave co-op alone, it BROKE it, and #1384 was already merged when this surfaced. WHAT WENT WRONG. Co-op pools every deploying player's units onto the SHARED `player` side — `composeCoopEncounter` says so in as many words — while the guest still occupies a `bravo` seat, and `lobbySideAssignments` resolves the second side name to `opponent`. So a co-op guest is `opponent` holding a roster that is entirely `player`-side, and a side check reads them as an intruder against EVERY unit. Measured on a co-op-shaped match (coopCampaign present, guest on bravo, all units Player-side): `refused=true, moved=false` — the guest was locked out of their own mechs. Not a narrowing of the guard's reach; a total lockout of one of the two players. HOW IT GOT PAST THE FOURTH STEP, which is the part worth keeping. The full suite was green — 2430 suites, 33209 tests — and stayed green, because NOTHING exercises a seated multiplayer host with real movable units end to end. The one test that dispatches Move through the host stubs `InteractiveSession` entirely and asserts only routing. The fourth step's own test was the first of its kind, and it covered the shape I had been probing (versus) rather than the shape the task was about (co-op). A green full suite said nothing about either. The lesson is not 'run the suite' — it was run — but that a guard keyed on a proxy MUST be tested against every shape where the proxy's meaning changes, and co-op was the shape whose meaning changes. THE FIX exempts co-op matches from the side check, because side is not an ownership signal there by construction. That restores the guest's pre-#1384 behaviour exactly, keeps the measured versus hole closed, and leaves co-op unit commands unconstrained — which is where they were before either step, and is honestly the state this task still has to close. Falsified 1:1: neutralizing only the co-op exemption reds exactly the co-op row and leaves the versus row green. The test now boots both shapes from one factory whose ONLY difference is the co-op campaign, so the next person changing this guard cannot cover one shape and miss the other the way I did. STILL OPEN, unchanged by either step: co-op per-unit ownership. `ownsCoopUnit` remains uncalled and `coopSeats` still never leave `composeCoopEncounter`, so the per-unit owner map still never reaches the host. Wiring it is a real slice, not a guard tweak — the co-op combat path does not create the match through `launchCampaignEncounter` (that function has no multiplayer references at all), so where the owner map would have to be persisted is itself unestablished. That trace is the next piece of work here, and it should start by settling how a co-op encounter becomes a `ServerMatchHost` match, because the fourth step assumed a route it never verified.
  - Progress (2026-08-26, fourth step — CROSS-SIDE UNIT COMMANDS ARE NOW REFUSED; box STAYS OPEN, the co-op grain is not covered): the previous note said a properly-seated co-op match had to be built before anything could be concluded, and that the earlier probe was unrepresentative. Both were right, and building the seated match turned up a live defect that is broader than the co-op question that prompted it. MEASURED, NOT INFERRED. On a properly-seated 1v1 — `meta.seats` occupied, server-derived ownership resolving to `ownedForceIds=["alpha-1"]` for one player and `["bravo-1"]` for the other — the player holding the BRAVO (opponent) seat issued a Move for `unit-A2`, a PLAYER-side mech, and the host committed it: `movement_declared` + `movement_locked`, position `(-1,5)` -> `(-1,4)`. The control in the same run (the owner moving their own unit) also committed, which is what makes the result readable: the harness is faithful, so the refusal is the thing that was missing. NEITHER EXISTING LAYER COULD HAVE CAUGHT IT, and this is structural rather than a missed branch. A Move names `unitId`, never `forceId`, so `commandRequestFromIntent` builds an EMPTY force claim and the subset check against `ownedForceIds` passes trivially — the comment right above it ("unit ids are not force ids") records the decision that left it that way. The engine cannot catch it either: `EngineIntentHandler` is `(session, intent) => void`, so the dispatch layer never learns who sent the command. Ownership had nowhere to be checked. WHY IT SURVIVED THIS LONG, which is the part worth keeping: `ownsCoopUnit` opened by asserting that "`ServerMatchHost` already validates unit ownership for any seated match". That was false when it was written. It made the co-op helper look like a refinement of a working base case instead of the only ownership check in the codebase, and the base case was never built. The comment is corrected in the same commit, because leaving it would re-seed the same mistake in the next reader. THE FIX is a side-granularity refusal at the intent seam, before rate-limiting and before any engine dispatch: resolve the actor unit's side from engine state, resolve the caller's side from `meta.sideAssignments`, refuse `AUTH_REJECTED`/`unit-not-owned` when they differ. Both halves are records the server already owns, so nothing new is threaded through the wire. The actor is `unitId`, or `attackerId` for Attack/Physical; `targetId` is pointedly NOT read, since shooting a unit you do not own is the entire game. Server-internal callers are exempt — the host acting is not a principal claiming scope. Falsified 1:1 (#PR): neutralizing only the side comparison reds exactly the refusal row and leaves the owner-control row green, and the row also asserts the unit DID NOT MOVE, so a guard that refused loudly while committing anyway would not pass. WHAT THIS DOES **NOT** CLOSE, stated plainly so the box is not read as done. (1) It separates OPPONENTS, not TEAMMATES. In a co-op match both players are assigned side `player`, so caller side and unit side match and the guard returns without refusing — player B commanding player A's mech is still accepted. That intra-side grain is exactly what `ownsCoopUnit` expresses, and it is STILL uncalled, because `coopSeats` still never leave `composeCoopEncounter` and so the per-unit owner map still never reaches the host. Nothing in this step changed that. (2) A match whose meta carries no `sideAssignments` resolves no caller side and is left exactly as it was rather than newly refused — deliberate, since making unresolved-ownership matches unplayable is a bet this slice did not verify across every creation path. (3) The proposal / readiness / launch clauses of this task are unaffected by this step. CORRECTION TO THE PREVIOUS NOTE, which got the reason right but the evidence wrong: it reported that the earlier probe saw own/teammate/enemy all "commit". They did not. Re-run with the effect measured instead of the frame kind, all three were `movement_invalid` events — engine refusals for `NoMovementCapability`, because the probe's units were `as IGameUnit` casts with no movement stats — and no position changed. `kind: 'Event'` was never evidence of success; an engine REFUSAL is also an Event. The empty `ownedForceIds` was real, but it was not what made that probe unreadable.
  - Progress (2026-08-26, third step — DECISION AUTHORITY closed; a LAUNCH-side gap is recorded but NOT claimed as an exploit): the decision clause is done and was a real hole - `handleCampaignDecision` never checked the sender, so in `host-review` mode (the default the co-op entry panel picks) a guest could submit a proposal and immediately approve their own, committing it with the GM never consulted. `handleCampaignHostIntent` had always compared against `entry.hostPlayerId`; the decision path fifty lines away simply never did. Fixed with the same comparison and `AUTH_REJECTED`/`campaign-decision-requires-gm`; the row also asserts the proposal is STILL PENDING after the refusal, so a guard that complained while deciding anyway would not pass. Falsified 1:1 (#1382). ON THE LAUNCH CLAUSE, stated carefully because I could not finish verifying it: three facts ARE established by reading. (1) `ownsCoopUnit` - the per-unit co-op ownership predicate - has NO production caller; only its own tests reference it. (2) `coopSeats`, which `composeCoopEncounter` tags with `ownerPlayerId`, never leave that module: `launchCoopMission` forwards only `composition.encounter` onward, so no per-unit ownership data reaches `ServerMatchHost` at all. (3) Unit-scoped intents carry no force identity - `MoveIntentSchema` is `unitId`/`to`/`facing`/`movementType` - while `commandRequestFromIntent` builds its authorization request from `claimedForceIds` and `claimedParticipantId`, so FORCE-SCOPE authorization has nothing to constrain a unit-scoped command with. WHAT IS **NOT** ESTABLISHED, and must not be read into the above: that a co-op player can actually command a teammate's or an opponent's unit in production. I probed it and the probe was NOT REPRESENTATIVE - the harness created a match with `sideAssignments` but no seats or server-derived force ownership, so `ownedForceIds` was empty and an empty claim passed trivially; all three of own/teammate/enemy committed, INCLUDING the enemy case, which is itself the signal that the harness rather than the product was permissive. `HumanActionAuthorization` has real coverage that contradicts a naive reading of that result ('command force scope must be a subset of server-derived ownedForceIds', 'an admitted member whose membership was revoked cannot command', 'a mismatched participant claim is scope-escalation, never a fallback'). ANYONE PICKING THIS UP SHOULD BUILD A PROPERLY-SEATED CO-OP MATCH FIRST - real seats, real server-derived force ownership, both players on the same side - and only then decide whether unit-scoped commands are constrained. The probe was deleted rather than merged, because a test that cannot tell its own subject apart from its harness is worse than none.
  - Progress (2026-08-26, STAYS OPEN — the UNIT half is done; proposal/readiness/launch are not): found and fixed a live defect on the way. A co-op campaign has ONE SHARED ROSTER, so both players pick from the same units, and `composeCoopEncounter` guarded `duplicate-player` but NOT duplicate unit - overlapping forces produced two `coopSeats` entries for one unit id carrying two DIFFERENT `ownerPlayerId` values. The same mech on the map twice, and an ownership question with two answers, in exactly the flow this program exists to make playable: two people share a roster and both take the good lance. Written failing-test-first (both duplicate rows RED before the guard, both controls already green). CHECKED ACROSS DEPLOYING CONTRIBUTIONS ONLY - a command-HQ player puts nothing on the map, so their force overlapping a deployer's is not a double-deployment and must not block the launch; there is a row for that, because a check that rejected it would be indistinguishable from a correct one on the two obvious cases. REFUSED RATHER THAN DE-DUPLICATED: silently dropping the second copy would quietly discard part of somebody's contribution, and the launch surface branches on a typed cause so it can say what happened. The exhaustive switch in `launchCoopMission` refused to compile until the new rejection carried a player-facing message - the type system asking the right question. Falsified 1:1: neutralising the guard reds exactly the two duplicate rows and leaves the controls green. WHAT REMAINS ON THIS BOX, stated so it is not mistaken for done: proposal, readiness and launch commands are NOT yet validated against durable ownership, and 'clear only affected readiness when an authoritative force or revision changes' is untouched. Also worth recording for whoever takes those: `admitCampaignParticipation`'s `foreign-force` refusal checks only that the force EXISTS in shared `forceUnits` - it does NOT check the sender owns it, which is defensible while the roster is genuinely shared but stops being so the moment per-player ownership is introduced. Verification: typecheck 0 errors; oxlint 0 errors / 78 warnings unchanged; coop + encounter suites 9 suites / 54 tests; FULL unit suite green.
- [x] 9.3 Implement durable GM-loss pause with no implicit player promotion; allow resume only by the same reauthenticated GM and participant removal only through an audited GM command.
  - Receipt (2026-08-30 - CLOSES this box, SEALING SECTION 9 at 5/5): the third clause - participant removal only through an audited GM command - now exists; the first two (durable GM-loss pause with no promotion; same-reauthenticated-GM-only resume) were already shipped and stand untouched. RemoveParticipant is a new host-only campaign intent: validateCampaignIntent rejects any non-host author with typed 'host-only' appending nothing, and the committed ParticipantRemoved event - actor, participant, reason, timestamp, append-only through the normal one-batch commit path - IS the audit record the spec scenario names. In the same operation the durable seat is revoked (revokeCampaignSessionParticipant, previously ZERO production callers) and the participant leaves the sync session's retained convergence map, so the launch acknowledgement requirement updates with the batch: a launch blocked on a departed player's convergence proceeds after removal (the exact scenario SHALL). A live removed socket detaches on its next frame; rejoin gets the existing membership-revoked refusal (readmission is a decision, not a retry). Crash-window semantics chosen commit-first and PROVEN: a death between event commit and durable revoke leaves the committed removal event, and the next authenticated join rescans committed removals and heals both the revocation and the retained set. Union ripple enumerated and complete: CampaignEventType 7->8 with the runtime-list completeness pin, zod baseline schema pack + strict mutant matrix (missing-participant / extra-property / non-string-reason all reject), campaignEventScope ('campaign' - a roster change is session-public), reducer, provenance manifest (NONE - pure command input), grant projection switch, provenance/exhaustive-switch consumers, membership-port revoke. HONEST CAP EXCESS: 21 files (worker BLOCKED at the 15-file bound after compile-time exhaustiveness demanded five more mechanical consumers; splitting would have left main broken between PRs, so the orchestrator completed the six - two intent-label switches, one projection row, one provenance member, the replay-registry count pin (retitled to the discriminant heads per the migration-head law), test fakes - and the excess is recorded here rather than hidden). REVIEWER RE-RUN CAUGHT A VACUOUS MUTANT CLAIM: the worker's guard-removal mutant 'killer' was a pre-existing TRANSPORT-layer test (AUTH_REJECTED at the socket) that never reaches the validator - the guard was real but unproven; a new CampaignMatchHost row drives the guest-facing handleIntent path with a RemoveParticipant intent and now reds when the guard is removed. Mutants, all reviewer-re-run: guard-removed dies at the new validator row; retained-delete-removed and durable-revoke-removed both die at the revoke/detach/launch-unblock integration row. Gates: tsc 0 across the 21-file ripple; focused 4 suites / 84; worker evidence multiplayer 112/873 + oxlint 77-pin, orchestrator serial re-run across multiplayer+campaign+events trees recorded in the PR. +~530/-11.

  - Progress (2026-08-25, second step — THE PAUSE NOW SURVIVES A RESTART; box still OPEN for audited removal): the previous note named non-durability as reason (1) for leaving this box open. That is closed, and WITHOUT a stored flag, which is the part worth recording. A stored `paused` column can disagree with reality - it is a claim about the world that has to be kept in step with it. What replaced it states the reality directly: a REBUILT session has no GM connection attached, therefore the GM is absent, therefore the campaign is paused. `CampaignHostRegistry.register` takes a `rebuilt` option and `getOrCreate` - the path that runs precisely when the entry was already gone, after a restart or an eviction - passes it. A freshly CREATED session deliberately does NOT: the GM is the one creating it and their socket follows immediately, and starting that case paused would refuse a guest who arrived in between. Three rows: a rebuilt campaign is paused, a fresh one is not, and the returning GM clears a rebuild pause (a pause nothing can lift is an outage, not a pause). Falsified 1:1 - neutralising the rebuilt branch reds exactly the rebuild row. WHAT REMAINS OPEN on this box, unchanged: AUDITED PARTICIPANT REMOVAL is not built - revocation exists in the participant store but no GM command performs it and no audit record says who removed whom - and the spec's 'MAY resume after cursor convergence' is still immediate rather than gated, which needs 9.4's convergence gate. Verification: typecheck 0 errors; oxlint 0 errors / 78 warnings unchanged; registry suite 10/10; FULL unit suite 33125 passed / 0 failed.
  - Progress (2026-08-24, STAYS OPEN — the pause is not DURABLE, and audited removal is unbuilt): GM LOSS DID NOTHING AT ALL BEFORE THIS. `CampaignSyncSession.hostDisconnected()` existed and read like the handler for it, but grepped this session it has NO production caller - only two tests - so a GM's socket dropping left the campaign running as if they were still there. It is also TERMINAL by design and by its own test ('pauses the session and closes the host' asserts `host.isClosed()`), so wiring it up would have made GM loss unrecoverable rather than paused. A recoverable pause now exists beside it: `noteGmConnected` / `noteGmDisconnected`, wired from the campaign socket's HOST branch and its cleanup. Only the GM's own connection can reach either, because `role` is host precisely when the connection's VERIFIED principal matches the registered host - a tactical player cannot claim it, which is what makes 'no implicit promotion' structural rather than a rule someone has to remember. PRESENCE IS A COUNT, NOT A FLAG, and the falsification is what forced that: with a flag, the `!gmPresent` guard was unreachable and neutralising it reddened nothing - which turned out to be hiding a real defect. A GM holding TWO connections (a second tab, or a reconnect landing before the old socket's close is processed) paused a session they were still sitting in the moment either one closed. Now paused only when the LAST GM connection goes. THE PAUSE BITES: `CampaignProposal`, `CampaignDecision`, and `CampaignHostIntent` are refused while paused with `MATCH_PAUSED` / `campaign-paused-gm-absent` - a code that already means exactly this, so none was minted. Join, grant-join, and acknowledgement stay OPEN on purpose: those are how a participant gets back and how the GM returns to resume, and refusing them would make the pause unrecoverable. Five rows pin the behaviour: the GM dropping pauses; a player's command is refused while the GM is absent; a player RECONNECTING does not resume it; the same GM reconnecting does; and a non-GM connection closing does not pause at all. Falsified 1:1: dropping the pause assignment reds four rows, dropping the resume reds one, and forcing the refusal helper to admit reds one. WHY THIS BOX STAYS OPEN, EXACTLY: (1) DURABILITY. The paused state lives on the in-memory session, so a process restart forgets it - a restarted server rehydrates the campaign unpaused with no GM connected. Making it durable means a column on the session row and a read on rehydration, which is 9.1's schema work. (2) AUDITED PARTICIPANT REMOVAL. Not built at all: revocation exists in the participant store (6.1) but there is no GM command that performs it and no audit record of who removed whom. (3) 'MAY resume after cursor convergence' from the spec scenario is NOT implemented - the resume here is immediate on the GM's return, and gating it on convergence is 9.4's gate, which does not exist. Verification: typecheck 0 errors; oxlint 0 errors / 78 warnings unchanged; bind suite 25/25; the three suites 9.5 names run green together under `--runInBand` (3 suites / 43 tests); FULL unit suite 2415 suites / 33122 passed / 0 failed. The spec's second scenario ('Player cannot inherit GM authority') holds independently of this work and was pinned in #1356 - `handleCampaignHostIntent` compares against the registered host, and the upstream identity guard binds that comparison to the verified principal.
- [x] 9.4 Gate scenario progression on convergence of all retained participants while allowing eligible committed delivery to healthy clients.
  - Receipt (2026-08-28, ENFORCED — the gate now refuses real progression; box CHECKED with its delegated remainder named): #1395 built the ledger, #1402 made 'behind' mean behind-on-DELIVERY, and both merged enforcing nothing — zero production callers, `retained` empty in every live session, a finding the adversarial review put in capitals. #1411 is the wiring. Every session-world join path — `joinMember`, `joinGuest`, and a new resync branch for a rejoiner quoting `lastSeq` (current room code required, exactly the newcomer rule) — carries the connection's verified playerId, so participants are RETAINED. The client acks every applied `CampaignEvent` over a new `CampaignAck` frame in the session's own event-log revision space — deliberately never the delivery-epoch grant cursor, which numbers a privacy-FILTERED view; feeding one number space into the other is the replay off-by-one class (#1399) rebuilt an abstraction higher, and the wiring charter forbade it by name. Envelope identity is safe because `handleInbound` closes any socket whose envelope playerId is not the connection's verified principal, before dispatch. PROGRESSION IS GATED AT EVERY ROUTE TO A COMMIT: the host's own `AdvanceDay` intent, the GM's approval of a pending `AdvanceDay` proposal, and — in auto-approve mode, where submission IS the commit — the submission itself, read off a new one-line `arbitrationMode` accessor. Refusal is typed `CAMPAIGN_NOT_CONVERGED` naming who is behind and the revision they must reach, and the day does not advance (asserted on state, not on the error frame). THE OTHER HALF OF THE TASK'S SENTENCE IS ALSO PINNED: non-progression intents commit fine while a participant lags (`SpendFunds` control row), so eligible committed delivery to healthy clients keeps flowing. A REVIEW FINDING CLOSED IN THE SAME PR, found by the landing reviewer rather than the implementing worker: the large-gap resync baseline is stamped `sequence: -1` and the client acked only `sequence >= 0`, so a rejoiner past `RESYNC_SNAPSHOT_GAP` could NEVER converge until an unrelated live event happened to land — AdvanceDay deadlocked with nothing anyone could do. After applying a snapshot the client genuinely holds the state at `payload.revision`, so it now acks that revision; `resyncGuest` still never reseeds `acknowledged`, so convergence remains something only the participant's own ack can claim. EVIDENCE: implemented by a grok-4.6 CLI worker from a bounded charter, failing-test-first — six rows red for the stated reasons before any wiring; reviewed, extended, and re-verified independently. Falsified 1:1 (neutralizing the gate consult reds exactly the five gate rows, controls green) plus a five-mutation matrix each killing exactly its named row: drop `joinMember`'s id; disable the decision-route consult; disable the auto-approve-route consult; disable the snapshot ack; neutralize the gate. Typecheck clean; oxlint 0 errors / 77 warnings (the post-merge-wave pin); multiplayer+campaign battery 258 suites / 3355 tests; full suite 2438 suites / 33302 passed / 0 failed; 30/30 CI. WHY THE BOX CLOSES WITH TWO THINGS STILL NAMED OPEN IN THE FILE: the HOST is not retained (snapshot+subscribe path) — the host is the author of every commit this gate protects, so 'retained participants' excluding the author is the design, not a gap; and grant-served guests (`handleRoomCodeGuestJoin` with a replicaStore) never enter this ledger — that join path belongs to the delivery-epoch world, whose convergence story the wave map delegates to `design-campaign-authority-and-sync` (campaign portions of section 9, wave-map row 5). Per wave-map rule 3 the umbrella acceptance still gates the program: if the leaf's convergence lands weaker than this, section 21-25 acceptance fails the program, not this receipt.

  - Wave provenance (2026-08-28): see the note of the same date under task 5.3 - shared review/remediation/verification cycle for this wave.
  - Progress (2026-08-28, THE GATE IS BUILT AND CORRECT; box STAYS OPEN because NOTHING CALLS IT): #1395 added the convergence ledger and `evaluateScenarioLaunch` to `CampaignSyncSession` - retained participants, per-participant acknowledgement cursor, behind-list with required revision. The review found a NaN acknowledgement slipped BOTH guards and permanently self-converged a participant (unrepairable, because Math.max(x, NaN) is NaN); the remediation added the `invalid-revision` outcome (NaN/Infinity/1.5/-1 all refused, probed live) and DELETED the Math.max seed that made a bad value permanent - a provably equivalent mutant replaced by a plain assignment and an invariant comment. #1402 replaced the commit-head ceiling with a per-participant DELIVERED watermark, closing the review's honesty finding that 'ahead-of-delivery' never checked delivery (probed live: a participant sent literally nothing could converge by naming the head; now refused), including a frame committed DURING hydration (race constructed in a test with a mid-hydration commit). STILL OPEN, and the next PR: `evaluateScenarioLaunch` and `noteParticipantAcknowledged` have ZERO production callers, and neither production join site passes a participantId, so `retained` is empty in every live session and the gate is unconditionally open. THE WIRING SEAM, verified so the next author does not repeat the review's finding 3: the ack must quote the baseline event's `payload.revision` (log-head space) - NOT `entry.revision`, which is sampled once at registration, never advanced in production (`advanceRevision` has no callers), and would make the gate refuse every launch forever. KNOWN HAZARD for the wiring, verified live: an ID-LESS rejoin (exactly what production does today) leaves the delivered watermark stale and fail-closes later acknowledgements - the wiring must pass the verified playerId on EVERY join path, including reconnect and `resyncGuest`.
- [x] 9.5 Verify with `npm.cmd test -- --runInBand src/lib/multiplayer/server/__tests__/CampaignSyncSession.test.ts src/lib/multiplayer/server/__tests__/CampaignGmArbiter.test.ts src/lib/multiplayer/server/__tests__/CampaignMatchHost.test.ts`.
  - Receipt (2026-08-29, run on main b63808758 - CLOSES this box on its literal command): 3 suites / 56 passed / 0 failed. HONESTY NOTE, per the 3.4 precedent: sibling 9.3 (durable GM-loss pause without implicit promotion) remains open and is NOT covered by these suites' current rows - this box verifies what its named command verifies, and 9.3 carries its own future receipt. The section's shipped halves (9.1 durable membership fields, 9.2 readiness, 9.4 convergence gate) are what these suites pin today.


## 10. Awaited Campaign Creation and Canonical Unit Adoption

- [ ] 10.1 Make campaign creation/adoption await authoritative campaign, genesis branch, GM membership, player-slot placeholders, force ownership, and unit-reference commit before returning success, implementing `Campaign Creation Has an Awaited Authority Checkpoint`.
- [ ] 10.2 Preserve customized-unit identity, weight, tech base, engine, gyro, armor, equipment, critical slots, and temporal metadata through campaign adoption and player ownership.
- [ ] 10.3 Materialize both tactical player forces from the active campaign branch/revision and reject stale force or ownership revisions before encounter creation.
- [ ] 10.4 Verify with focused campaign creation, serialization, customizer-handoff, and encounter-materialization tests plus `npm.cmd test -- --runInBand src/lib/campaign/persistence/__tests__/serializeCampaign.test.ts src/lib/campaign/encounter/__tests__/materializeCampaignMissionEncounter.test.ts`.

## 11. Pre-Serialization Viewer Projection

- [x] 11.1 Introduce one server-side viewer projector used before `JSON.stringify` for live match and campaign frames, implementing `Viewer Projection Occurs Before Serialization` and private authority-sequence separation.
  - Receipt (2026-08-29 - CLOSES this box; the campaign half its receipts named is delivered): CAMPAIGN frames now pass a per-recipient scope boundary BEFORE serialization. The finding that drove the shape: design D3 has stamped an access scope (gm | campaign | team | player:<id>) on every campaign event since it shipped, the emission table explicitly anticipates GM-hidden facts - and NOTHING on the delivery side ever read it: CampaignSyncSession fanned every envelope to every sink, so the FIRST gm-scoped producer would have broadcast its secret to the whole session. (No leak exists on today's wire - every current producer stamps scope campaign, grep-verified - the boundary is the prevention the vocabulary always assumed.) campaignScopeAdmits is the pure guard (GM sees all; campaign flows to everyone; player:<id> targets its player plus the GM; team fails closed to GM-only until a membership source exists; an unproven viewer gets only campaign scope), applied by admitToWire at the session's sink boundary - joinMember hydration and live subscribe, resyncGuest tail, baseline, and live - so nothing reaches JSON.stringify unguarded. A withheld frame still advances delivery bookkeeping, deliberately: convergence must not wait forever on a fact scope holds back, proven by the ack row ('applied', where skipping the recording would answer ahead-of-delivery). Integration rows drive the REAL commit/hydration/fan-out via a _commitEventsForTests seam (the anticipated GM-hidden facts have no producer yet) and were RED before the wiring - gm-scoped withheld from players live AND in resync tails, player-scoped targeting exactly its player plus GM, campaign flowing to all. Mutations: bypassing the boundary at ONE site (resyncGuest) dies at the resync row - the single-application-point law; stripping the GM too dies at six GM-keep rows across the pure matrix and integration. Gates: tsc 0; oxlint 0 errors / 77 warnings; campaign+bind suites 4/96; multiplayer tree 110 suites / 852 passed. DEFERRAL, DOCUMENTED IN THE MODULE ITSELF: the envelope's authority `sequence` stays on player campaign frames - the legacy campaign client resumes by that number, and the delivery-epoch mapping that replaces it is delegated to design-campaign-authority-and-sync (wave map row 5), exactly as this box's own receipts anticipated. Until it lands, concealment arithmetic over sequence gaps becomes possible the moment something is actually withheld - stated here rather than silently shipped, and the match half's separation (delivery-space frames, sequence stripped per viewer) remains fully in force.

  - Progress (2026-08-28 later, THE ENVELOPE STOPPED COUNTING TOO; box still open only for campaign frames): #1429 closed the negative sweeps' second finding - ReplayStart/End carried authority-space bounds, so `(toSeq - fromSeq + 1) - totalEvents` handed a player the exact number of fog-hidden events in the span, the same arithmetic the inverted guard kills at the event level surviving one envelope higher. Envelope projection now rides the SAME publication boundary as event fields: player envelopes carry delivery-space bounds only, where span equals items delivered BY CONSTRUCTION and the arithmetic yields nothing; GM and authority envelopes keep `fromSeq`/`toSeq`, and over-stripping them is a caught failure. The sweeps' finding row flipped `it.failing` to `it` - red first with the honest number (expected 0, received 1). The client needed no authority bounds already; a new row proves recovery completes when the envelopes carry none at all. Reviewer re-run: re-emitting authority bounds to players reds four rows. Full suite 2443/33360/0. STILL OPEN here: campaign frames outside the boundary, and the sweeps' schema finding (`EventMessageSchema` tolerating `event.sequence` via `z.unknown()`) stays a deliberate `it.failing` until the protocol decision is taken.
  - Progress (2026-08-28, THE CONCEALMENT LEAK IS DEAD; box STAYS OPEN for the campaign half): the authority-sequence separation this task names is now enforced, in two slices plus a review addition (#1422/#1423 and its predecessor). SLICE A made the client delivery-first and dual-keyed, proven against a real fog server whose frames were post-processed to DELETE event.sequence - exactly-once by id, gap report, resync, recovery, all without the number. Its most valuable output was naming the protocol gap: ReplayStart/End bounds were authority-space and could neither number replay items nor prove a delivery hole filled. SLICE B closed that gap and cut over. Replayed events ARE deliveries: each per-player replay item carries the viewer's deliverySequence - reusing the existing index for events already in the record, assigning through the SAME cursor for events never delivered, so the record stays gapless and later resumes still answer. ReplayEnd carries a delivery-space bound for pin release. `sequence` joined AUTHORITY_ONLY_EVENT_FIELDS at the single projection point, PER-VIEWER: GM and authority projections keep it, and over-stripping them is a caught failure (the mutation that strips GM too reds the authority-keep row). `viewerSequenceConcealmentLeak.test.ts` is INVERTED per its own header: what measured the violation now fails the suite if any player-projection frame carries event.sequence or exposes an authority gap. THE REVIEW ADDITION, because the worker's honest stop surfaced it: `isGameEvent` rightly demands a numeric sequence (engine events always carry one), so a real player's mirror would have hydrated at 'loading match...' forever against the stripped stream - while every hook test stayed green on locally-built events that still carry sequences, the suite-green-product-broken shape this program keeps finding. The bridge is the client: within a player's mirror, `sequence` already means position-in-the-local-log (locally-appended events use events.length), and the delivery number IS that position, so the client stamps it onto sequence-less wire events at emission - a local number, never an authority one, and the code says so. The live-shape row (real client, real fog server, sequence-stripping duplex, mirror hydrates) was red before the stamp. One premise corrected on the way: emitted events do not carry NO number, they carry the local stamp - duplicate-free and contiguous once a late recovery lands, arrival order deliberately not ascending because the recovered frame keeps its original number. EVIDENCE: grok-4.6 worker on a bounded charter, failing-first (the inverted guard red before the strip); reviewed, extended, re-verified independently. Worker mutations: fresh-assign on replay reuse reds the resume rows (the #1406 defect class, guarded); strip-GM-too reds the authority-keep row; dropping the ReplayEnd bound reds the pin-release row. Reviewer re-runs: neutralizing the mirror stamp reds the hydration and stripped-stream rows; re-stripping GM reds authority-keep. Typecheck 0; oxlint 0 errors / 77 warnings; battery 95 suites / 734; full suite 2440 suites / 33337 passed / 0 failed; 30/30 CI. Scope disclosed in the commit: 21 files, over the 15-file convention, 7 production - the assertion sweep of an atomic cutover, declared rather than silent. STAYS OPEN, and it is the task's own second noun: CAMPAIGN frames do not route through the publication boundary - `CampaignSyncSession` / `bindCampaignSyncConnection` serialize outside the projector. That half is this box's remaining work; the wave map's delegation of campaign portions to design-campaign-authority-and-sync is the likely home for its delivery-epoch side, and whichever change takes it must run this same per-viewer separation there.

  - Wave provenance (2026-08-28): see the note of the same date under task 5.3 - shared review/remediation/verification cycle for this wave.
  - Progress (2026-08-28, ONE PROJECTOR NOW DECIDES WHAT LEAVES THE SERVER for live-match frames; box STAYS OPEN for the sequence removal and campaign frames): #1398 introduced `ViewerFrameProjector` behind the publication boundary as the single application point before serialization, with `AUTHORITY_ONLY_EVENT_FIELDS` as the declared removal list. The review's blocking finding - an always-true `redacted` flag collapsed replay pagination from 64-event chunks to ONE frame - was fixed and the remediation added the row that reds when pages re-collapse, plus a by-reference identity-fast-path pin (removing the fast path had left all 44 tests green while the docblock promised the original object back). #1400 added wire-level proofs that a removed field is absent on all three surfaces (live, replay, resync). STILL OPEN, stated plainly: (1) `event.sequence` is STILL on player frames - the concealment leak is not closed, `viewerSequenceConcealmentLeak.test.ts` still passes and must be INVERTED when the removal lands; the removal needs the client resuming from its per-viewer deliverySequence first (dedupe and fork detection re-keyed off the same number) - the delivery-cursor half of that landed in the 5.3 wave, the client re-key has not; (2) campaign frames (`CampaignSyncSession` / `bindCampaignSyncConnection`) do not route through the publication boundary at all - the task says live match AND campaign, and only the live-match half is done; (3) the removal list has one entry (`payload.intentId`).
  - Progress (2026-08-26, first slice — the per-viewer sequence EXISTS and is consumed; box STAYS OPEN): this box has two halves and the projector half was already built - `MATCH_WIRE_PUBLICATION_BOUNDARY.guardLiveEvent` runs per recipient BEFORE `safeSend` does the `JSON.stringify`, so `Viewer Projection Occurs Before Serialization` holds today. What did not exist was PRIVATE AUTHORITY-SEQUENCE SEPARATION: the wire carried only the global authority sequence, so a fog viewer's slice of it was full of holes. `ViewerDeliveryCursors` now assigns each viewer their own gapless `deliverySequence`, and the number is taken AT SEND TIME, after every `continue` that fog or the guard can take - which is precisely what makes it gapless for them while the authority sequence in the same frame is not. A send that FAILS does consume its number, deliberately: the resulting hole is a true signal that something was lost, which is the signal the authority sequence could never give. THE SLICE IS CONSUMED, not shipped inert - the client tracks the number and reports a `delivery-gap` on a hole, while STILL APPLYING what arrived. That last part is the whole lesson of the reverted regression: holding events behind a gap stalls a fog client forever on a sequence it may never receive, so the signal is advisory and resync is the response, not a stall. Three server rows (gapless per viewer while the authority sequence is not; the two viewers numbered independently; a withheld frame consumes no number) and three client rows (reports a gap and still applies; does NOT cry gap over a sparse authority sequence; says nothing about pre-rollout frames that carry no number). Falsified 1:1 on both halves: removing the server stamp reds all three server rows, and making the client's detector always return false reds the gap row alone. TWO TEST-AUTHORING MISTAKES WORTH RECORDING, both caught before merge and both of the same family. The first control asserted the two viewers received DIFFERENT NUMBERS of frames - they received the same count (10 each), losing a different event apiece, so it was comparing the wrong thing; it now compares what actually arrived. The second was worse: the client harness sent `ReplayStart`/`ReplayEnd` without their required fields, so the frames failed schema validation, the client never left its replay window, and both control rows passed because NOTHING WAS PROCESSED AT ALL. They now assert the applied count too. WHY THIS BOX STAYS OPEN, EXACTLY: the concealment leak is NOT closed. The authority sequence is still on the wire beside the new number, so `viewerSequenceConcealmentLeak.test.ts` still passes - a player can still count what was hidden from them. Closing it means REMOVING the authority sequence from player frames and resuming replay from the delivery cursor rather than `lastSeq`, which touches replay, mirror hydration, and reconnect together. That is the next slice, and doing it in the same step as this one would have been a change nobody could review. Verification: typecheck 0 errors; oxlint 0 errors / 78 warnings unchanged; new suites 6/6; FULL unit suite green.
- [x] 11.2 Store GM previews and private reasons in separately authorized server-only records; ensure player-safe facts contain no private fields or correlatable private identifiers.
  - Receipt (2026-08-30 - CLOSES this box; the evidence map had shown the consumer machinery real and the PRODUCER wiring absent, and the producer now exists): GmPrivatePreviewRecordWriter is the server-only ingress - a GM preview persists as a gm-draft private record (content + derived summary) and a private reason as a separate gm-reason record, both through authenticated repository writes with the access audit recording write/granted; a player write is DENIED, creates nothing, and records write/denied. No public marker, frame, or history row is emitted by the writer; finalized-correction ROUTING stays with 14.1 by boundary, and this is the ingress it will call. NO CORRELATABLE IDENTIFIERS: the projector now strips privateRecordRef from player projections recursively (top-level AND nested) while the GM projection retains it, and the 11.4 raw sweep gained the planted-opaque-ref row proving a 32-hex ref appears in no player frame. DEFECT FOUND AND FIXED EN ROUTE: the private-access audit storage rejected the new write purpose, and its broad constraint classifier mislabeled that failure as an opaque-ref collision - additive migration 22 extends the purpose set with an append-only trigger, and collision handling is narrowed to actual PK conflicts. Red-first throughout; mutations killed in both runs: disabling recursive ref-stripping dies at the sweep row (reviewer re-run confirming); removing GM-role enforcement dies at the GM-only-write row. Gates, both runs agreeing: tsc 0; oxlint 0 errors / 77 warnings; focused privacy/history suites 6/51; full multiplayer tree 110 suites / 857 passed. 10 files.

- [x] 11.3 Implement sealed Player 1/Player 2 choices and authoritative reveal while keeping ordinary public combat facts on immediate committed publication.
  - Receipt (2026-08-30 - CLOSES this box, SEALING SECTION 11 at 5/5): sealed choices exist on the wire. The seal set - MovementDeclared, AttackDeclared, PhysicalAttackDeclared - is declared solely in MatchWireAudienceCatalog (the single policy point) as SEALED_TO_ACTOR_UNTIL_REVEALED, evaluated by MatchWireSealedChoices, satisfying the spec letter: a tactical player's sealed choice SHALL be visible only to that player and the GM until an authoritative reveal or phase-finalization event. Reveal semantics per finalizer: weapon attacks reveal on AttacksRevealed; movement and physical attacks finalize on the PhaseChanged out of their phase (movement has no dedicated reveal marker - found, not assumed). The public finalizer publishes FIRST, then the withheld originals are DELIVERED to newly eligible viewers in committed order through the SHIPPED delivery machinery - ViewerDeliveryCursors identity mapping, normal delivery numbering, exactly once - and replay/resume applies the same audience policy, so E2E-22's reveal-from-committed-delivery-streams letter is the mechanism, not a paraphrase. Pre-reveal the sealed-out player's frames carry neither the choice nor a delivery-number gap (the fog no-gap law extended); ordinary public facts keep immediate committed publication (PhaseChanged control row). Proven on a real seated 1v1: actor-immediate delivery, opponent omission with no destination leak and no number gap, exactly-once full originals after finalization. Failing-first throughout, including a REAL finding: the actor-delivery path first keyed on a lobby owner id that mismatched the authoritative principal id - red until corrected. Mutations: leaking a declaration at commit dies at TWO boundary rows (reviewer re-run - the first reviewer probe hit the movement-only integration suite and survived, the boundary suite is where attack coverage lives, both suites now cited); skipping reveal delivery dies at the exactly-once row; sealing a public fact dies at the control. Gates, both runs agreeing: tsc 0; oxlint 0 errors / 77 warnings; focused 5 suites / 31; full multiplayer tree 111 suites / 860 passed. 9 files, +762/-68.

- [x] 11.4 Add schema/object tests and raw-frame negative tests for GM-private reason, hidden metadata, private IDs, authority sequences, and inferable gaps; fail closed on any projection error.
  - Receipt (2026-08-28, PR #1427; box CHECKED - the sweeps exist for all five categories, the fail-closed clause was already load-bearing, and the two truths the sweeps surfaced are pinned as failing rows rather than papered over): one suite drives real hosts and asserts on the RAW WIRE STRINGS. Authority sequences: absent on every player surface, PRESENT on the GM stream - the discrimination is part of the property, and the mutation that strips GM too reds the GM-keep row. Private ids: an explicit documented allowlist, anything id-shaped outside it fails (a stamped `rowid` reds exactly that row). GM-private reasons: a sentinel planted on a fog-concealed event appears in no player frame. Hidden metadata: the concealed event's literals swept against every frame. Inferable gaps: every NUMBER in every player frame swept - a stamped derived authority position reds the sweep. Five deliberate-leak mutations, five named deaths; the reviewer independently re-ran keep-sequence-everywhere (three rows died, restored green beside the inverted guard). FAIL CLOSED ON PROJECTION ERROR is the #1398 boundary's standing law, not new work: a projector throw returns a typed failure with NO raw fallback (ViewerPublicationBoundary's own contract and rows), so a projection error suppresses the frame rather than leaking the unprojected one - re-read this session, not assumed. TWO FINDINGS, as `it.failing` rows that flip green when fixed, deliberately not fixed in a test-only slice: (1) `EventMessageSchema` admits `event.sequence` through `z.unknown()` - the wire schema tolerates what the projector strips, so schema validation alone cannot catch a projector regression; tightening it is a protocol decision. (2) THE REPLAY ENVELOPE STILL LEAKS A COUNT: on a player resync, `(ReplayEnd.toSeq - ReplayStart.fromSeq + 1) - ReplayStart.totalEvents` equals the number of fog-hidden events in the span (measured 1). The per-event sequences are gone (#1423), but the authority-space bounds let a player COUNT what was concealed - the same arithmetic the inverted guard kills at the event level, surviving one envelope higher. The fix direction is already named on the schema fields: player replay bounds fully into delivery space. That fix is 11.1-remainder work and the next production slice. Gates: typecheck 0; oxlint 0 errors / 77 warnings; multiplayer battery 96 suites / 743 tests; one file, 778 lines.

- [x] 11.5 Verify with `npm.cmd test -- --runInBand src/lib/multiplayer/server/__tests__/ServerMatchHost.fogOfWarIntegration.test.ts src/lib/multiplayer/server/__tests__/CampaignSyncSession.test.ts src/lib/multiplayer/server/__tests__/ServerMatchHost.test.ts`.
  - Receipt (2026-08-29, run on main 7d3a9040b - CLOSES this box on its literal command, per the 3.4/9.5 precedent): 3 suites / 40 passed / 0 failed - fog integration, the campaign sync session (now carrying the 11.1 scope boundary), and the match host (now carrying the serialized executor). HONESTY NOTE: siblings 11.2 (GM-private server-only records) and 11.3 (sealed choices) remain open and are not covered by these suites' current rows; each carries its own future receipt. What this section HAS delivered and these suites plus the 11.4 sweeps pin: the projector before serialization on both frame families, per-viewer sequence separation for matches, the campaign scope boundary, and the five-category raw-wire negative sweeps.


## 12. Replay Snapshot Timeline and Export Parity

- [ ] 12.1 Reuse the pre-serialization projector for replay, resync, snapshot, cold recovery, audit timeline, and export, implementing `Visibility Is Equivalent Across Surfaces` and `Timeline and Export Use the Same Viewer Projection`.
  - Progress (2026-08-30, six-surface projection inventory by a fresh-context worker, suites re-verified 110/856 - box STAYS OPEN, and the inventory says its letter needs an owner decision before code): the surfaces do not share one projector BY DESIGN, and forcing them onto one would break proven contracts. LIVE/REPLAY/RESYNC: the wire publication boundary, proven (11.x). GRANT-CHANNEL SNAPSHOT: buildScopedCampaignSnapshot - a deliberately SEPARATE policy that excludes journal sequence/revision/digest fields so a restricted snapshot cannot leak identities; replacing it with the wire projector would assign sequences to withheld identities (projectCampaignStreamForGrant states this in its own contract). COLD RECOVERY: rehydrates a host and serializes nothing itself - reconnect reaches the guarded replay path. AUDIT TIMELINE: audit-row metadata mapped per viewer role, not event projections - no event identity or digest exists to unify. EXPORT: the stream half uses ViewerProjectionService; its embedded timeline uses the audit mapper. FINDING, the one genuinely unprojected surface: the LEGACY campaign socket CampaignSnapshot serializes entry.host.buildSnapshotPayload() directly (bindCampaignSyncConnection ~759) - full folded campaign state to any admitted viewer, no projection; the 11.1 scope boundary filters EVENTS, but folded state in a baseline can carry the effects of scope-withheld facts. The scoped-grant snapshot machinery exists precisely for this; routing the legacy baseline through it (or its policy) is the fix direction and belongs with the delivery-epoch delegation (design-campaign-authority-and-sync) beside the sequence deferral already recorded there. 12.2 cross-surface parity digests are blocked on the same fork: audienceDigest speaks IGameEvent+IGameState and cannot digest campaign snapshots or audit rows - a shared projection/digest contract is a DESIGNED slice, not a test-authoring afternoon. WHAT WOULD CLOSE 12.1 as written: either the owner narrows its letter to per-surface-policy-proven (mostly true today, legacy snapshot excepted) or approves the shared-contract slice. Recorded here rather than guessed at.

- [ ] 12.2 Add per-viewer projection digests and parity tests across live/replay/snapshot/cold-recovery/timeline/export for GM, Player 1, and Player 2.
- [x] 12.3 Add authorization tests proving a player cannot query a private GM audit identifier or infer hidden authority events from delivery gaps.
  - Receipt (2026-08-29, reconciled against existing shipped evidence on main 61d862840, suites re-run 3/20 green - CLOSES this box; both named properties were already proven by the history/audit suites and the 11.4 raw-wire sweeps, and this receipt cites the rows rather than re-authoring them): CANNOT QUERY A PRIVATE GM AUDIT IDENTIFIER - ViewerHistoryService.test rows: unknown principal refused on every entrypoint before any store read; wrong-scope refused with a CONSTANT message and no foreign load; the audit timeline hides other principals and committed revisions from a player while the GM sees both; private records return PAYLOAD-FREE shapes to a player even when the player asks includePrivate, with the DENIAL RECORDED in the access audit; a typed failure carries no partial facts and no private content. ThreeContextPrivacyEvidence adds the indistinguishability law: a player presenting ANOTHER player's cursor matches a fabricated-unknown-id response byte-for-byte, and cross-principal scope escalation is typed with pinned audit and journal counts. CANNOT INFER HIDDEN AUTHORITY EVENTS FROM DELIVERY GAPS - the service pages visible facts with GAPLESS durable sequences across hidden events; player replay envelopes carry delivery-space bounds where span equals items BY CONSTRUCTION (the 11.1 envelope receipts); and the 11.4 sweeps assert every number in every player frame against inferable-gap arithmetic, with the stamped-derived-position mutation dying there. The three-context evidence suite's export rows extend the same guarantees to timeline and export buffers.

- [ ] 12.4 Verify with focused replay, recovery, fog, audit, and export suites and `npm.cmd run verify:qc:replay-recovery`.

## 13. Durable Combat Outcome Receipt Boundary

- [x] 13.1 Replace process-local terminal publication as the authority boundary with a combat outcome outbox written in the terminal combat transaction, implementing `Terminal Combat Outcome Uses Durable Outbox and Inbox`.
  - Receipt (2026-08-29 - CLOSES this box, and the two 4.5-review residuals it structurally owned): the terminal journal-authority command writes a write-once mp_combat_outcome_outbox row (outcomeId, outcomeVersion, canonical outcome) INSIDE the same batch transaction that commits its events - the started-fact pattern applied to the outcome, in both stores behind the IMatchStore contract. Post-verify publication reads the ROW, fires CombatOutcomeReady from it, and marks published_at; recovery drains an unpublished terminal row before accepting new work (the resumePendingPublications pass-0 pattern). The bus remains the in-process notification; the ROW is the authority boundary. RESIDUALS CLOSED: the enabled path's missing post-verify publication (the #1455 suppression removed the scratch's accidental pre-commit publish; the row is its truthful replacement) and the stale constructor-session publisher (publication no longer touches the session the journal path replaced). The legacy event-at-a-time path keeps its engine-primary publish unchanged - it has no terminal transaction to join; the boundary upgrade rides the cutover path like the rest of section 4's machinery, stated in code. Proofs on real SQLite: reopen shows both-or-neither for events+row (out-of-transaction mutant dies there); write-once second insert throws; non-terminal batches write no row; live publication exactly once; crash-before-publication then recovery publishes exactly once from the row (skip-resume mutant dies there); shadow/scratch paths write nothing. The reviewer's NOVEL never-mark-published mutant dies at two rows - the suite watches the marking, not just the firing. Spec SHALLs quoted in the PR. Gates, both runs agreeing: tsc 0; oxlint 0 errors / 77 warnings; focused 8 suites / 79; full multiplayer tree 110 suites / 856 passed. 13 files, +451/-13. The campaign inbox and (outcomeId, outcomeVersion) receipt are 13.2's own work - the consumer lands separately.

- [x] 13.2 Add a campaign inbox and unique `(outcomeId, outcomeVersion)` receipt that commits campaign consequences atomically.
  - Receipt (2026-08-29 - CLOSES this box): campaign_combat_outcome_inbox (migration 21, ladder pins moved 20->21 together) holds one receipt per outcomeId with its version, consuming command identity, stream revisions, and commit positions. Consumption is ATOMIC in the journal's own extension transaction: check-receipt -> append the consequence events -> insert the receipt, one transaction - a receipt-insert failure rolls the consequences back (its own test row). The receipt is the dedupe authority: a second delivery of the same (outcomeId, outcomeVersion) answers 'duplicate' with the original receipt and applies NOTHING; a DIFFERENT version of the same outcomeId answers a typed outcome-version-conflict, never a silent apply. The consumer topology found and wired: co-op battle reconciliation (reconcileCoopBattle -> CampaignMatchHostOutcomeInbox.commitCampaignOutcomeConsequences) routes SQLite-backed server hosts through the durable inbox; CoopBattleConsequences carries outcomeVersion on the wire (optional for compatibility). Worker-begun (codex, capped mid-task before gates - next window Aug 30 1:50 AM), orchestrator-finished. Mutations, reviewer-run: skipping the receipt check dies at the duplicate AND restart-idempotency rows; skipping the receipt INSERT dies at FOUR rows including rollback-on-receipt-failure. Gates: tsc 0; oxlint 0 errors / 77 warnings; multiplayer+campaign+persistence trees 285 suites / 3603 passed. 13 files.

- [x] 13.3 Prove duplicate delivery, crash-before-receipt, restart, and campaign ingestion retries apply one consequence batch.
  - Receipt (2026-08-29 - CLOSES this box, proven by the 13.2 suite's own rows on real SQLite): DUPLICATE DELIVERY commits one consequence batch and one durable receipt (second delivery answers duplicate, applies nothing); CRASH-BEFORE-RECEIPT rolls back the consequences with the failed receipt insert and a redelivery applies exactly one batch; RESTART plus outbox replay stays idempotent against the durable receipt; INGESTION RETRIES are the same redelivery path, receipt-answered. Each proof drives the real journal extension transaction; the mutation runs above are the falsifications.

- [ ] 13.4 Enforce `Combat-Only Rewind Stops at Campaign Receipt` with a typed closed-boundary response.
- [x] 13.5 Verify with `npm.cmd test -- --runInBand src/lib/multiplayer/server/__tests__/bindCampaignSyncConnection.reconcile.test.ts src/lib/multiplayer/server/__tests__/CampaignMatchHost.test.ts` and focused `InteractiveSession.outcome` tests.
  - Receipt (2026-08-29, run on main eb500af3a - CLOSES this box on its literal command plus the focused outcome suite): reconcile + CampaignMatchHost + InteractiveSession.outcomeBus, 3 suites / 16 passed / 0 failed - the reconcile path now routing through the durable inbox, the host carrying the outcome-consequence commitment, and the bus suite pinning the engine-side publish guard beside the suppression law. HONESTY NOTE: sibling 13.4 (Combat-Only Rewind Stops at Campaign Receipt) remains open - its enforcement point is the rewind command surface owned by add-authoritative-history-branches (wave map row 7), and the typed closed-boundary response lands with it; the receipt table it will consult exists as of 13.2.


## 14. Immutable Combat Correction and Rewind Kernel

- [ ] 14.1 Route finalized combat corrections through server-authored authoritative commands while keeping preview non-mutating and GM-private, implementing `Combat Intervention Has Distinct Preview and Commit Phases`.
- [ ] 14.2 Add immutable combat branch, supersession, trusted-checkpoint, and stale-branch command semantics without changing existing combat reducers.
- [ ] 14.3 Reject commands during rebuild with retryable `PROJECTION_REBUILDING`; do not queue them invisibly.
- [ ] 14.4 Rebuild combat state, RNG provenance, fog, sealed choices, and viewer projections; activate the replacement branch atomically only after verification.
- [ ] 14.5 Verify with GM intervention suites, deterministic replay tests, fog integration tests, and `npm.cmd run verify:qc:gm:campaign-ledger`.

## 15. Cache-Only Checkpoints and Branch Recovery

- [ ] 15.1 Implement immutable checkpoints keyed by branch, authority head, reducer version, and digest, satisfying `Checkpoints and Compaction Are Cache-Only`.
- [ ] 15.2 Prove compatible checkpoint-plus-tail equals full replay for authoritative state and all three viewer digests.
- [ ] 15.3 Prove reducer-version or digest mismatch rebuilds from an earlier base or blocks truthfully without publishing partial recovery.
- [ ] 15.4 Add per-session corruption quarantine for broken sequence, lineage, receipt, or digest while a healthy control session remains available.

## 16. Retroactive Campaign Rebuild

- [ ] 16.1 Extend campaign intervention preview to declare affected date, missions/contracts, finances, transactions, loans, reputation, rewards, salvage, repairs, inventory, roster, unit/pilot/personnel, markets, receipts, scenario artifacts, activity, audit, and viewer-projection families.
- [ ] 16.2 Implement backward time and retroactive correction as replacement-branch replay from a trusted base, not a negative forward-day loop, satisfying `Retroactive Campaign Changes Rebuild Declared Families`.
- [ ] 16.3 Reject commands during rebuild with `PROJECTION_REBUILDING`; activate only after every declared state, artifact, receipt, audit, and viewer projection verifies.
- [ ] 16.4 Invalidate stale scenario drafts, force selections, outcomes, and other externalized artifacts explicitly and reject their later use.
- [ ] 16.5 Verify with campaign day-pipeline, campaign intervention control-plane, mission-launch, and time-cascade tests plus `npm.cmd run verify:qc:gm:time-cascade`.

## 17. Coordinated Post-Receipt Outcome Correction

- [ ] 17.1 Implement a distinct higher-version coordinated outcome-correction command for the post-campaign-receipt boundary, satisfying `Coordinated Outcome Correction Supersedes Across Journals`.
- [ ] 17.2 Execute coordinated correction as a durable saga: atomically record combat supersession, invalidation manifest, and replacement outcome outbox in the source authority, then idempotently record the target-scoped campaign replacement receipt and deterministic consequence batch in the target authority; expose pending/blocked state and do not claim a cross-store transaction.
- [ ] 17.3 Gate scenario N+1 on the active outcome receipt, replacement artifacts, active branch, and convergence of every retained participant.
- [ ] 17.4 Prove retry, reconnect, and restart cannot apply the coordinated correction twice and preserve cross-journal audit causality.

## 18. Role-Scoped Audit Timeline

- [ ] 18.1 Add branch/effective-head/supersession rendering and cross-journal outcome causality to the audit timeline, implementing `Timeline Preserves Branch and Supersession Lineage`.
- [ ] 18.2 Add separate authorized GM-private audit lookup and player-safe history/export; record private-record access and append exactly one private rejection-audit row before each terminal rejection without creating gameplay journal, outbox, cursor, replay, or export facts.
- [ ] 18.3 Add rewind-impact preview showing affected domains and artifacts without mutating state.
- [ ] 18.4 Add GM and player timeline/export parity tests plus negative privacy scans for private fields and hidden-event identifiers; prove rejected-command retries retain one authorized audit row while all player surfaces and gameplay authority remain unchanged.

## 19. Accessible Lifecycle and Recovery UX

- [ ] 19.1 Add stable UI states and locators for pending, sealed, finalized, syncing, reconnecting, behind, rebuilding, rewound, and blocked across campaign, combat, and GM surfaces.
- [ ] 19.2 Gate command surfaces during rebuild, stale branch, blocked recovery, and required convergence; provide actor-safe typed conflict and recovery actions.
- [ ] 19.3 Add keyboard-complete rewind preview/confirmation, focus management after errors and decisions, persistent text, live-region announcements, and narrow-viewport primary actions.
- [ ] 19.4 Verify affected component tests, `npm.cmd run verify:qc:viewport-sweep`, and accessibility assertions for every lifecycle state.
  - Progress (2026-08-24, STAYS OPEN — see the blocker at the end): found and fixed a real accessibility defect while writing the assertions this task asks for. `CampaignSyncStateBanner` — the strip that tells a player whether to trust what is on screen — was a plain `<p>` with NO live region. A sighted player watches it flip from "up to date" to "reconnecting"; a screen-reader user got nothing at all, because a status that changes outside a live region is announced to nobody. It now carries `role="status"`, `aria-live="polite"`, and `aria-atomic="true"`. POLITE DELIBERATELY, including for `blocked`: assertive cuts off whatever the reader is currently speaking, a sync posture is never more urgent than the sentence the player is in the middle of, and the blocked state also disables the controls so it is discoverable by trying rather than only by hearing. ATOMIC because without it a reader may announce only the CHANGED words, turning "you are up to date" into a fragment that means nothing alone. New a11y suite (`CampaignSyncStateBanner.a11y.test.tsx`, 14 rows, jest `a11y` project): axe-clean for all SIX existing states rather than one representative, because the per-state tone classes are exactly the kind of thing that silently breaks contrast for a single posture - and the broken one would be a failure state, which is when the message matters most; live-region announcement asserted BY ROLE rather than by testid, so it pins the accessibility tree a screen reader actually walks instead of a hook only tests can see; an explicit row that the region is never assertive; and a row that no state's announcement contains a DIGIT, because a spoken "12 events behind" rebuilds the same inference channel tasks 3.2/3.4 closed for the printed one. Falsified 1:1: changing `role="status"` to `role="presentation"` turns 8 of the 14 rows red. Verification: tsc clean; oxlint 0 errors / 78 warnings unchanged; a11y project 7 suites / 53 tests; campaign-coop + replica units 15 suites / 77 tests; **`npm run verify:qc:viewport-sweep` 51 passed (3.5m)**, which is the second of this task's three named gates; FULL unit suite 2410 suites / 33061 passed / 0 failed. **WHY THIS BOX STAYS OPEN:** the task requires a11y assertions for EVERY lifecycle state, and 19.1 names nine — pending, sealed, finalized, syncing, reconnecting, behind, rebuilding, rewound, blocked. Four of those cannot be asserted because they do not exist to render: a case-insensitive grep over non-test `src/components` and `src/lib/campaign` finds `sealed` in 0 files and `rewound` in 0 files, with `finalized` (1) and `rebuilding` (2) present only outside the lifecycle UI. Writing assertions for states nothing renders would be theatre. The six states that DO exist are now covered end to end; 19.4 closes when 19.1 builds the remaining four, and this note is the record of exactly which ones are owed.


## 20. Three-Context Sandbox Foundation

- [x] 20.1 Add `e2e/fixtures/gmTwoPlayerCampaign.ts` with isolated contexts reserved for the future non-playing GM, Player 1, and Player 2, plus a harness-owned server, per-run database, distinct identities, and deterministic seeds. The foundation proves isolation first; Task 9 durable membership later binds those contexts to actual roles and must pass `membership-smoke` before role admission is claimed.
- [x] 20.2 Add test-only fault controls guarded by `NODE_ENV === 'test'`, E2E run ID, explicit session scope, and one-shot consumption; make production startup reject enabled controls.
  - Receipt (2026-08-24): `src/lib/testing/faultControls.ts`. A fault-injection switch is a remote kill switch wearing a helpful name, so arming one requires FOUR things to line up and each closes a different leak: the build must be `NODE_ENV === 'test'`; a RUN id, so one Playwright run cannot arm a seam another run then trips over (which is what makes parallel runs safe); a SESSION scope, so the fault hits one session and a healthy control session stays available to prove the failure was CONTAINED rather than global; and ONE-SHOT consumption, because an armed-until-disarmed switch outlives the test that armed it and fails the next one for no visible reason. Arming outside a test build THROWS rather than no-opping - a silent no-op would let a fault test "pass" on a production build having proven nothing at all - and consumption returns false for every near miss (wrong run, wrong session, wrong seam, already consumed, not a test build), because a seam that fires on an approximate match is worse than no seam. A partially-scoped control is refused outright: it looks armed and its blast radius is whatever the missing field would have narrowed. PRODUCTION STARTUP REFUSES, and refuses at the REAL entrypoint: `server.js` now exits 1 with `FAULT_CONTROLS_IN_PRODUCTION` when `MEKSTATION_FAULT_CONTROLS` is set outside a test build. Exiting rather than ignoring is deliberate - a process that boots and merely declines to honour fault config is one refactor away from honouring it, while one that refuses to start cannot rot that way. The guard is a hand-written MIRROR because `server.js` is CommonJS and cannot import the TS module, so a sibling test SPAWNS `server.js` itself rather than trusting the mirror; drift between the two would otherwise be invisible. The guard is "not a test build", not "is production", so a dev server carrying fault config is refused identically. Environment is INJECTED rather than read globally, so exercising the production path cannot disturb other tests sharing the worker (it also removed a `NODE_ENV` readonly type error rather than casting past it). Falsified 1:1: neutralising the startup guard turns both rejection rows red while the no-config CONTROL row stays green - without that control every rejection row would pass against a server that simply fails to boot for an unrelated reason, which on a machine with no build it otherwise would. Verification: tsc clean; oxlint 0 errors / 78 warnings unchanged; fault-control suites 19/19 (16 scoping + 3 real-process startup); FULL unit suite 2408 suites / 33037 passed / 0 failed. NON-CLAIM: this ships the CONTROL, not the seams. No production call site consumes a fault yet - wiring `store.commit`, `outbox.insert`, and head-update seams belongs to the tasks that build those paths (plan todo 28 and leaf-4 task 2.4), and `MEKSTATION_FAULT_CONTROLS` is currently read only by the startup refusal.

- [x] 20.3 Add a dedicated SQLite evidence reader opened `readonly: true, fileMustExist: true`; never instantiate the production store for read-only proof.
  - Receipt (2026-08-24): `e2e/fixtures/sqliteEvidenceReader.ts` opens its own connection with `readonly: true, fileMustExist: true` and imports NOTHING from the production persistence layer. Both options are load-bearing for a reason worth stating: the production store CREATES its file and runs migrations, so pointed at a database the run never produced it would manufacture an empty one and report zero rows - evidence of absence conjured from nothing, which is the exact shape of a false pass. `fileMustExist` turns that into a thrown `EVIDENCE_DB_MISSING`, and the row proving it asserts the file was NOT brought into existence, not merely that the open threw. `readonly` makes immutability a database-level guarantee rather than a convention, and the byte-equality row compares the file BEFORE and AFTER a full probe (count + select + tables) as bytes rather than row counts, because a probe that rewrote a page while leaving rows alone would still make the artifact describe a different file than the run produced - which is what the plan's evidence gate means by identical hash and schema. Two refusals are named rather than left to the driver: a non-SELECT statement is rejected as `EVIDENCE_WRITE_REFUSED` with the offending SQL (the connection would refuse it anyway, but as "attempt to write a readonly database", which does not say what was attempted), and a table name that could carry SQL is rejected outright because table names cannot be bound as parameters. WIRED, NOT SHELVED: the fixture exposes `openEvidence('app' | 'multiplayer')` over the run-owned paths `assertRunOwnedPath` already validated, and the `fixture-smoke` gate now reads its own database through it and asserts the hash is unchanged - so the reader is proven against REAL run databases end to end, not only against synthetic ones. Falsified 1:1: flipping `fileMustExist`/`readonly` off turns the missing-database row red. A SELF-CORRECTION worth recording: the guard proving no production dependency first banned the string `DurableMatchStore` anywhere in the file, and failed on the module's own comment EXPLAINING why it is not used; it now scans import lines only, so it fails on a dependency rather than on prose. Verification: tsc clean; oxlint 0 errors / 78 warnings unchanged; reader suite 6/6; `fixture-smoke` and the 20.5 ownership spec both green on real builds; FULL unit suite 2405 suites / 33011 passed / 0 failed. NON-CLAIM: this ships the reader and one live use of it; the per-table durable-row assertions the program's evidence bundle needs (receipts, batches, outbox, participants, cursors, branches, outcome receipts) arrive with the tasks that create those tables.

- [x] 20.4 Add role-labeled traces, screenshots, raw socket transcript, pre-serialization projection capture, latency timestamps, durable-row export, state/projection hashes, environment manifest, and cleanup log under `test-results/gm-two-player/<run-id>/`.
  - Receipt (2026-08-24): `e2e/fixtures/gmTwoPlayerEvidence.ts` writes the declared bundle under `test-results/gm-two-player/<run-id>/`, role-labeled in the FILENAME so three contexts are distinguishable from a directory listing rather than by opening every file, with a sha256 and byte count per artifact. Two properties make such a bundle trustworthy and both are easy to lose silently. (1) IT CANNOT WRITE OUTSIDE ITS RUN - every path goes through the same `assertRunOwnedPath` the sandbox already uses, because a writer that escapes clobbers another run's evidence or a developer's files and the resulting artifact describes a run that never happened; a `../../../` name is refused. (2) IT CANNOT CARRY SECRETS - the bundle captures raw socket transcripts and pre-serialization projections, which is exactly where a bearer token or password lives, and an evidence file is written to disk, uploaded as a CI artifact, and read by people who were not in the run, so a leak there is a leak everywhere. The scan matches the SHAPE of a credential (password/token fields, `Bearer`, private keys) rather than known values, because a list of known secrets contains only the ones already known about; the refusal does NOT echo the match into its error, since an error message is itself logged and repeating it would defeat the refusal it just performed. A CONTROL row writes ordinary prose that merely mentions "token" and must still be accepted - a guard that refused every transcript containing the word would be switched off within a week. (3) THE MANIFEST DECLARES INTENT, not inventory: `recordMissing` puts an artifact the run could not capture into the manifest with a reason, so a missing file is a DETECTABLE gap; a manifest listing only what exists cannot distinguish "never captured" from "captured and lost", which is the difference between a gap and a bug. WIRED AND PROVEN LIVE: the fixture hands out `openEvidenceBundle()` and the `fixture-smoke` gate now writes a per-role artifact and finalizes a manifest; verified by reading the real bundle off disk after the run (3 role-labeled files plus `manifest.json`, every sha256 64 hex chars, environment carrying node version and run id). Falsified 1:1: neutralising the secret scan and the run-owned path check turns 6 rows red while the 4 structural rows stay green. Verification: tsc clean; oxlint 0 errors / 78 warnings unchanged; bundle suite 10/10; `fixture-smoke` green end to end on a real build; FULL unit suite 2409 suites / 33047 passed / 0 failed. SECTION 20 IS NOW COMPLETE (20.1-20.5). NON-CLAIMS: the bundle is the WRITER plus one live use - actually capturing traces, screenshots, socket transcripts, latency series, and durable-row exports happens in the tasks that produce those things (Playwright tracing/screenshots ride Todo 26's runner; socket transcripts and pre-serialization projections need the seams of sections 5 and 11; durable-row export needs the tables of section 2). The secret scan is a shape heuristic, not a proof of absence: it catches the credentials this program actually handles and would not catch an unrecognised encoding.

- [x] 20.5 Add ownership-scoped cleanup that preserves ambient browser tabs, Chrome processes, unrelated servers, databases, and user artifacts.
  - Receipt (2026-08-24): writing the proof found a REAL defect rather than confirming the implementation. The context half was already scoped correctly - `cleanup()` closes only the three contexts it opened - but the IDENTITY half was not, and nothing had ever asserted it. Seeding an identity through `POST /api/e2e/vault-identity` calls `repository.setActive`, whose SQL is `UPDATE vault_identities SET is_active = 0` across the WHOLE table; the sandbox seeds three, so it deactivated whatever identity the machine had, and then deleted its own three on teardown. End state: NO active identity at all. `getActive()` returned null and `/api/multiplayer/auth/token` answered `404 No vault identity configured` - a developer's own vault session silently stopped working after running this suite, which is exactly the "preserve ... user artifacts" this task forbids. FIX: the fixture now reads the prior active id BEFORE its first seed (new read-only `GET /api/e2e/vault-identity`, id-only because the caller needs to restore the identity, not learn about it) and hands it back through `DELETE`'s new `restoreActiveId`, which reactivates AFTER the deletes so the final state is the one the caller asked for even when the restore target was also in the delete list. A restore target that no longer exists is REPORTED (`restored: false`), not thrown - the harness cannot know whether something else removed that identity meanwhile, and failing teardown over it would turn a tidy-up into a test failure. PROOF (`e2e/gm-two-player-cleanup-ownership.spec.ts`, 2/2 on a real build): an ambient context and an ambient active identity survive a full fixture lifecycle; the fixture's own three contexts are gone and the bystander's is not; the ambient PAGE is re-navigated rather than merely found in the list, because a context object can outlive its pages; and a second `cleanup()` destroys nothing further (specs that clean up in both `finally` and an error path call it twice). A control asserts the fixture actually ADDED contexts, so the preservation rows cannot pass against a fixture that created nothing. Falsified 1:1 - dropping `restoreActiveId` turns both rows red. HONEST NOTE ON METHOD: the first version of this spec probed identity liveness by minting a token and asserted an "unrelated identity survives", which failed - and the failure was mine, not the fixture's: identities are a collection with ONE active row, so `getActive()`-backed auth answers about activation, not existence. Chasing that wrong probe is what surfaced the actual defect. Verification: tsc clean; oxlint 0 errors / 78 warnings, unchanged from main at `c7fba3db7` (the count moved 77 to 78 when PRs #1342-#1343 landed, not here); api/vault/scripts batteries 117 suites / 1784 tests; the endpoint's own unit suite grew 4 to 8 rows covering the GET, the null case, a successful restore, and an unrestorable target; the `fixture-smoke` gate still passes end to end; FULL unit suite 2404 suites / 33005 passed / 0 failed. NON-CLAIMS: Chrome PROCESS preservation and unrelated-server/database preservation are asserted only insofar as the fixture never targets them - there is no positive process-level assertion, and adding one needs the fault-control seams of task 20.2; this closes the browser-tab and user-artifact halves of 20.5 with evidence and leaves the process half named rather than implied.


## 21. Strict Acceptance Scenarios E2E-01 Through E2E-45

- [ ] 21.1 Implement and pass E2E-01 through E2E-18 for durable creation, order, atomicity, idempotency, replay/live recovery, cursor integrity, slow-client isolation, restart, reauthentication, active routing, and heartbeat.
  - Progress (2026-08-31 - box stays OPEN; FIRST SCENARIO LANDED + FINDING #12): E2E-18 is green and honest - three real browser contexts on the campaign channel sit QUIET for the full HEARTBEAT_TIMEOUT_MS plus a server period (a genuine ~75s hold; the first draft's expect.poll resolved on first success in 23s and was rejected as vacuous - survival BEYOND the interval is the letter), heartbeats flowing both ways, all three still connected after; registered as runner group authority-pack1 ('authority' stays reserved for the complete pack per 21.4). MARATHON FINDING #12, instrumented server-side across six live probe runs: a committed campaign command's event reaches NO connected campaign client. The campaign socket has THREE join arms with THREE different live-delivery behaviors - the host arm subscribes a direct unscoped sendCampaignEvent; the legacy newcomer arm subscribes via the session's admitToWire-scoped sinks; the grant arm attaches snapshot-gated epoch-PROJECTION subscribers that push no raw frames - and in the live fixture an AdvanceDay commit broadcast to 3 subscribers produced exactly ONE wire send (readyState 1) that the browser never received (raw message count stayed 1), zero admitToWire evaluations, zero errors anywhere. Which arm a client lands on depends on server state (hostPlayerId match, issuedInvite, grant channel), which is precisely the Visibility-Equivalent-Across-Surfaces violation the 12.x owner fork names; E2E-03/08 (their letters and their spec scaffolding retained in the pack file) are BLOCKED on this delivery unification, not on test authoring. The instrumentation lives in the session transcripts; the probe sequence (intent-arrival -> result-ok -> subscribers=3 -> one-send -> raw=1) is reproducible via MEKSTATION_E2E_SERVER_COMMAND log capture.

  - Progress (2026-08-30 - box stays OPEN): the acceptance drive's first authoring attempt opened with the campaign-socket subset (E2E-03/08/17/18) and the worker's STOP clause immediately earned its keep - E2E-08's letter could not pass because the campaign socket had NO intentId-keyed idempotency (command ids were sequence-derived, so a client retry after a lost ack double-committed; marathon defect #10, found by refusing to weaken the scenario). Fixed same-day: stable campaign-intent:<campaignId>:<intentId> command ids feeding the journal's existing duplicate detection, duplicate replays prior events without re-append/re-apply/re-broadcast, divergent reuse is typed intent-identity-conflict, and the flag-off production store carries the same receipt contract. Recon banked for the pack: HEARTBEAT_TIMEOUT_MS=60s makes E2E-18 a real ~70s idle; invite expiry is the registry's null-invite state from the 6.4 arc; evidence reads follow the readDurableParticipants read-only pattern. E2E-01/02 remain gated on the genesis-branch machinery (branches leaf), 05/06 on a scoped fault-injection harness, 09-12 were unblocked by the section-5 client work.

- [ ] 21.2 Implement and pass E2E-19 through E2E-30 for pre-serialization privacy, sealed choices, immediate public facts, fog projections, live/replay/export parity, unauthorized access, veto/timeout, and simultaneous proposal attribution.
- [ ] 21.3 Implement and pass E2E-31 through E2E-45 for player ownership, deterministic concurrency, server RNG, disconnect policy, outcome receipts, immutable correction, pre-receipt combat rewind, branch convergence, fog restoration, and coordinated post-receipt correction.
- [ ] 21.4 Register an exact command `npm.cmd run verify:qc:gm-two-player-campaign -- --group=authority` and make failures retain the complete evidence bundle.

## 22. Strict Acceptance Scenarios E2E-46 Through E2E-80

- [ ] 22.1 Implement and pass E2E-46 through E2E-60 for time cascade, retroactive rebuild, invalidation, scenario continuity, customized-unit fidelity, ten-scenario restarts, checkpoint equivalence, receipts, provenance, and impact preview.
- [ ] 22.2 Implement and pass E2E-61 through E2E-70 for malformed/replay attacks, atomic failure, fail-closed projection, GM loss, partition catch-up, stale branch, quarantine, bounded replay, and socket failure.
- [ ] 22.3 Implement and pass E2E-71 through E2E-80 for latency, catch-up, backpressure, accessible lifecycle UX, confirmation, typed conflicts, evidence completeness, cleanup, and exact-main post-merge audits.
- [ ] 22.4 Register `--group=campaign`, `--group=failure`, `--group=performance`, and `--group=all` selectors without weakening strict backing requirements.

## 23. Performance and Long-Run Gates

- [ ] 23.1 Commit the controlled fixture configuration: 20 warm-up commands, at least 200 measured representative commands, monotonic correlated clocks, nearest-rank percentile calculation, named runner class, 100-event/512-KiB replay chunks, 256-envelope/1-MiB queue bound, 128-MiB server post-warm memory-growth ceiling, and 64-MiB post-warm ceiling per controlled browser client.
- [ ] 23.2 Gate p95 at 250 milliseconds, p99 at 750 milliseconds, and 1,000-event cold catch-up at 2 seconds on the recorded controlled loopback runner; keep the 2,000-millisecond Playwright wait as a functional timeout only.
- [ ] 23.3 Run ten sequential scenarios with periodic player reconnects, GM reconnect, server restart, correction, pre-receipt rewind, post-receipt coordinated correction, and checkpoint/full-replay comparison.
- [ ] 23.4 Verify `npm.cmd run verify:qc:gm-two-player-campaign -- --group=performance` and `npm.cmd run verify:qc:campaign-long`; archive latency and memory JSON with the run.

## 24. Cutover Rollback and Exact-Main Regression

- [x] 24.1 Prove shadow dual-write state and audience digests match before enabling journal authority for new sandbox sessions.
  - Receipt (2026-08-29 - CLOSES this box; both clauses delivered, sealing SECTION 24 at 4/4): the shadow comparison now computes PER-AUDIENCE digests - gm and player:<id> for both seats - by running the decided events and the live events through the REAL fog filter (fresh visibility cache, real match config) and the REAL viewer field-projection policy (ViewerFrameProjector's own removal rules, GM keeping sequence), with seat identities threaded from the SAME getMatchMeta source the live broadcast uses - no parallel seat authority invented. Any audience mismatch lands in the ShadowComparisonRecord as audience-digest-mismatch:<audience> and flips equal=false, which the 4.2 admission tripwire already consumes - an audience divergence now refuses journal authority for new matches automatically. The divergence surface this adds over event equality is exactly fog state: the proof row corrupts SCRATCH-ONLY visibility ownership while keeping events and the authority digest equal, and the audience digest catches it. SANDBOX RUN: a real host in mode shadow with fog ENABLED drives real commands and every recorded comparison is equal across GM, Player 1, and Player 2 - the shadow-equality run for controlled sandbox sessions; the production mode flip remains the deployment act. Mutations, worker-run then reviewer-re-run: digesting events without state dies at the fog-state row; dropping Player 2 dies at the audience-completeness row; the reviewer's NOVEL mutant - stripping sequence from the GM audience through the refactored projector policy - dies at the authority-keep row, proving the 11.x contract survived the refactor. Gates, both runs agreeing: tsc 0; oxlint 0 errors / 77 warnings; focused 5 suites / 40 (reviewer subset 3/25); server tree 98/707; multiplayer tree 109 suites / 836 passed. This machinery is also 12.2's per-viewer digest foundation - the parity-across-surfaces rows remain that box's own work.

- [x] 24.2 Cut over new sessions behind the reviewed feature flag, preserve legacy completed-log read compatibility, and refuse ambiguous active-session migration.
  - Receipt (2026-08-29, combat scope - campaign cutover stays delegated to design-campaign-authority-and-sync per wave-map row 5; verified on main 293790e09): per-match admission at creation behind the off-by-default reviewed flag; legacy reads schema-compatible (DurableMatchStore suites green in both reconciliation runs); ambiguity REFUSED on two independent axes - an imported legacy stream is refused journal authority before any flag is consulted, and recovery selects its reader from durable facts alone (matchRollbackReaderSelection.ts, #1451), the options.recovered bypass deleted. The leaf's five SQL-corruption rollback proofs (#1453) pin the refusal shapes against a real store.

- [x] 24.3 Document rollback that stops new admission, preserves journal/branch/receipt/audit rows, and returns to a schema-compatible reader without destructive history edits.
  - Receipt (2026-08-29, combat scope, verified on main 293790e09): documented normatively in the leaf's delta spec (adopt-combat-event-journal-authority/specs/event-store/spec.md, 'Combat Rollback Preserves Journal Authority' - rollback SHALL stop admission, preserve rows/receipts/head/generation/recovery state, and never substitute an incompatible log), implemented as the pure fact-driven selector wired through recovery, and PROVEN against real SQLite with SQL-written corruptions: blocked states refuse new command/effect admission with typed errors, serve no legacy substitution (digest-asserted), and leave a byte-identical store dump. NOTE: the delta text merges into main specs at the leaf's archive; the normative content exists and is implemented today.

- [x] 24.4 After the pre-harness ABI-checker merge, archive its exact-main native-store regression. Land the isolated three-context fixture next, then after every later major merge update the evidence ledger and run the applicable staged `npm.cmd run verify:qc:gm-two-player-campaign -- --group=<slice>` against exact main before the next dependent PR: `fixture-smoke` before role admission, `membership-smoke` when durable GM/P1/P2 roles land, and the evolving `smoke` subset thereafter.
  - Receipt (2026-08-29, second entry - CLOSES this box; the staged ladder is complete and exercised): the evolving `smoke` group now exists as the composition of both landed groups in ONE runner plan (fixture + membership specs, multi-file forwarding proven by the 3.4-erratum probe), grown one line per landed group inside buildRunPlan so the subset can never silently narrow elsewhere - and NARROWING IT IS CAUGHT: dropping either spec from the list reds the plan row (mutant killed). First smoke run on merged main 2185b5e3c: both specs green in one invocation (2 passed). All three named groups have now run green against exact main today; every unimplemented group still throws typed NOT_IMPLEMENTED (7/7 runner suite). The box's per-merge discipline (update the evidence ledger, run the applicable slice before the next dependent PR) continues as a standing landing ritual, the same way the exact-main regression rituals do - the deliverable machinery and its first exercised pass are what this box names, and both exist.

  - Progress (2026-08-29, the membership-smoke clause is DELIVERED; box stays open for the evolving smoke subset): the membership-smoke group now exists and runs - a three-context spec (e2e/gm-two-player-membership.smoke.spec.ts) driving the production match API and campaign WebSocket with the fixture's three authenticated identities: GM seat, two tactical seats, a seeded FOURTH identity refused campaign-tactical-seats-full with NOTHING streamed, durable campaign_session_participant rows verified through the fixture's read-only evidence reader, role-labeled evidence bundle finalized. Green via BOTH invocation paths (direct playwright and the runner --group=membership-smoke); fixture-smoke regression green through the runner. BEFORE ITS FIRST GREEN RUN this group surfaced THREE product defects, each fixed under its own law: (1) the co-op join streamed the CampaignSnapshot before the durable seat cap refused (#1461 - seat-before-stream, order-restore mutant); (2) the socket module graph's SQLite singleton was never initialized - tsx-required modules hold a DIFFERENT singleton than Next's API bundle, so every durable-membership dispatch threw 'Database not initialized' and closed the socket (server.js now initializes at loadMultiplayerRuntime; idempotent, shared DATABASE_PATH, WAL-safe); (3) the grant-channel served arm NEVER consulted the durable seat system at all - two shipped membership systems had not met, so seats never filled on the live path and the cap could not fire (handleRoomCodeGuestJoin now binds the tactical seat before any grant/snapshot/attach, refusing typed on full). Mutations, both killed live: the stranger control asserted admitted reds at the real refusal; the durability read starved to an empty result reds at the participant-row assertion. Gates: tsc 0; oxlint 0 errors / 77 warnings; runner+socket suites 59/59 (bindCampaignSyncConnection's 41 intact); the spec 1/1 post-oxfmt. Every other runner group still throws NOT_IMPLEMENTED - the evolving smoke subset remains this box's open clause.


## 25. Final Verification and Documentation

- [ ] 25.1 Run focused changed-area Jest and Playwright suites, then `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run format:check`, `npm.cmd run verify:qc:multiplayer:contracts`, `npm.cmd run verify:qc:multiplayer:browser`, `npm.cmd run verify:qc:coop-campaign-journey`, `npm.cmd run verify:qc:encounter-combat-continuity`, `npm.cmd run verify:qc:replay-recovery`, and the complete GM/two-player command.
- [ ] 25.2 Run `openspec validate harden-gm-two-player-campaign-sessions --strict --no-interactive`, `npm.cmd run qc:openspec-ci:validate`, and LSP diagnostics for every modified TypeScript/TSX file.
- [ ] 25.3 Update operator and contributor documentation for authority versus delivery sequence, membership/reauthentication, host loss, rewind boundary, recovery/quarantine, evidence bundle, performance methodology, and exact-main post-merge audit.
- [ ] 25.4 Reconcile all 80 acceptance scenarios to test identifiers and evidence paths, leave no unchecked implementation task without a linked blocker, and archive the change only after every applicable strict gate passes.
