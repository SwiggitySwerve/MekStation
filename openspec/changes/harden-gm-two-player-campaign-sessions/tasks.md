# Tasks: Harden GM + Two-Player Live Campaign Sessions

The PR order, dependency graph, ownership boundaries, exact-main regression cadence, and review evidence are maintained in `implementation-plan.md`. The event-history implementation sections are decomposed and frozen according to `event-history-wave-map.md`; execute their leaf-change tasks rather than implementing a duplicate umbrella seam. Each implementation PR MUST reference the requirement names, leaf task IDs, and umbrella acceptance IDs it closes.

## 1. Durable-Baseline Preflight

- [x] 1.1 Verify the repository-supported Node version and rebuild/reinstall the native `better-sqlite3` dependency until `npm.cmd test -- --runInBand src/lib/multiplayer/server/__tests__/DurableMatchStore.test.ts src/lib/multiplayer/server/__tests__/hardenedTransport.test.ts` passes; record Node, ABI, and command output in `test-results/gm-two-player/preflight/` before any durability implementation.
- [ ] 1.2 Add RED unit/contract tests for `Atomic Command Event Batches`, `Commit Precedes Recipient Publication`, and crash-before/after-commit behavior at `IMatchStore.ts`, `DurableMatchStore.ts`, `InMemoryMatchStore.ts`, and `ServerMatchHostIntent.ts`; prove partial-batch and false-success behavior fail before implementation.
- [ ] 1.3 Add RED protocol/client tests for `Stable Intent Identity Survives Retries`, `Authority and Viewer Sequences Are Separate`, replay/live overlap, sequence collision, and `Heartbeat Is Bidirectional` in `src/types/multiplayer/Protocol.ts`, `src/lib/multiplayer/client.ts`, and their existing test suites.
- [ ] 1.4 Keep SUPERHEAVY gyro serialization, zero-BV campaign materialization, Quick Game bootstrap recovery, and fail-open determinism CI in separate changes; document only their external prerequisite status in `implementation-plan.md`.

## 2. Additive Authority Schema and Store Contracts

- [ ] 2.1 Add additive SQLite migrations and TypeScript records for command receipts, command batches, recipient-neutral outbox, campaign sessions, participants, viewer cursors, branches, supersession, checkpoints, outcome inbox/receipts, private GM audit references, and append-once private rejection-audit records keyed by session/command identity, implementing `Atomic Command Event Batches`, `Campaign Journal Migration Is Additive and Idempotent`, and `Branches Preserve Immutable Supersession Lineage`.
- [ ] 2.2 Extend `IMatchStore.ts` and the campaign event-store boundary with atomic batch, receipt lookup, outbox claim/acknowledgement, branch, participant, and cursor operations; update `InMemoryMatchStore.ts` only as a contract-compatible test/dev adapter.
- [ ] 2.3 Implement the additive schema and atomic operations in `DurableMatchStore.ts` and the selected campaign persistence adapter with uniqueness and foreign-key constraints for session-scoped idempotency, outcome versions, authority sequence, and branch activation.
- [ ] 2.4 Add migration/idempotency/rollback contract tests, including repeated backfill, ambiguous ownership fail-closed behavior, and a schema reader compatible with preserved materialized snapshots; run `npm.cmd test -- --runInBand src/lib/multiplayer/server/__tests__/DurableMatchStore.test.ts src/lib/campaign/persistence/__tests__/campaignMigration.test.ts`.

## 3. Atomic Batch Append API

- [ ] 3.1 Implement command-receipt digesting and atomic multi-event append in the store adapters, satisfying `Atomic Command Event Batches` and `Command Receipts Persist for the Authority Lifetime`.
- [ ] 3.2 Add controlled no-network/no-sleep store seams that fail before commit, during a middle event, during head update, and during outbox insert; assert full rollback and no successful receipt.
- [ ] 3.3 Add restart tests proving an identical retry returns the prior receipt and a reused identity with different actor, branch, kind, or payload returns a typed integrity conflict.
- [ ] 3.4 Verify with `npm.cmd test -- --runInBand src/lib/multiplayer/server/__tests__/InMemoryMatchStore.test.ts src/lib/multiplayer/server/__tests__/DurableMatchStore.test.ts src/lib/multiplayer/server/__tests__/MatchStoreStress.test.ts`.

## 4. Per-Session Serialized Command Cutover

- [ ] 4.1 Add one bounded serialized executor per match/session and route `ServerMatchHostIntent.ts` validation, reducer execution, and batch creation through it, implementing `Per-Session Command Execution Is Serialized`.
- [ ] 4.2 Split atomic store capability from host cutover behind a disabled-by-default shadow/cutover flag; compare existing and journal-derived results before the new path becomes authoritative.
- [ ] 4.3 Prove concurrent Player 1/Player 2 commands form deterministic non-interleaved batches while commands in unrelated sessions continue independently.
- [ ] 4.4 Verify with `npm.cmd test -- --runInBand src/lib/multiplayer/server/__tests__/ServerMatchHost.test.ts src/lib/multiplayer/server/__tests__/ServerMatchHostEngineDispatch.test.ts src/lib/multiplayer/server/__tests__/MatchStoreStress.test.ts`.

## 5. Stable Intent and Viewer Delivery Protocol

- [ ] 5.1 Extend production `Protocol.ts` and `client.ts` envelopes with stable command/idempotency identity, branch, gapless per-viewer `deliverySequence`, projection digest, acknowledgement, and typed gap/collision receipts, implementing the `multiplayer-sync` requirements without exposing private authority sequences.
- [ ] 5.2 Persist pending command identity client-side until a terminal receipt and reuse it across reconnect attempts; clear only the matching committed, rejected, vetoed, timed-out, or integrity-conflict state.
- [ ] 5.3 Replace max-high-water application with contiguous exactly-once reducer application, replay/live buffering, gap recovery, and sequence-collision blocking.
- [ ] 5.4 Verify with `npm.cmd test -- --runInBand src/lib/multiplayer/__tests__/client.test.ts src/lib/multiplayer/server/__tests__/replayStream.test.ts src/lib/multiplayer/server/__tests__/reconnectionFlow.test.ts`.

## 6. Durable Admission, Active Routing, and Reauthentication

- [x] 6.1 Persist participant-to-campaign-session and participant-to-match bindings, and require authenticated durable membership before `bindMultiplayerSocketConnection.ts` or `bindCampaignSyncConnection.ts` registers a replay/fan-out recipient, implementing `Authenticated Membership Precedes Socket Attachment`.
  - Receipt (2026-08-24, fourth step — CLOSES this box: BOTH named binders are gated): the requirement names two binders, so the earlier notes deliberately left it open while only the campaign one was done. The combat binder turned out to be already gated, by work the ARCHIVED `add-authority-audit-and-privacy-proof` leaf delivered - verified rather than assumed by tracing the chain: `bindMultiplayerSocketConnection` calls `host.admitSocket`, which resolves through `AuthorizedViewerResolver` backed by `MatchSeatMembershipSource(IMatchStore)`, i.e. the DURABLE match store, and `attachSocket` runs only after `resolve()` succeeds. Its 29 admission tests already pin the substance, including 'a verified non-member is refused with typed frames only and never attaches', 'membership derives from durable seats - a store change is the revocation', and 'the seat membership source is durable-state-only'. WHAT WAS GENUINELY MISSING was the BINDER's own half of that contract - nothing proved a refusal actually STOPS the bind rather than the socket being wired up anyway and merely lacking a viewer. Two rows now cover it: a refused admission returns null, never calls `attachSocket` (so never becomes a fan-out recipient), and releases the connection slot rather than leaking it; and an inbound message after a refusal is never dispatched, because the message handler is registered BEFORE admission resolves and waits on a promise that only settles on success - so a refusal leaves inbound traffic permanently undelivered rather than merely unauthorized. The mock's `admitSocket` had to be widened to production's `IAuthorizedViewer | null`, because a mock that can only succeed cannot express the case the binder has to handle. Falsified 1:1: neutralising the binder's null check reds both new rows. Verification: tsc clean; oxlint 0 errors / 78 warnings unchanged; binder suite 7/7, admission suites 3 suites / 29 tests; multiplayer battery 69 suites / 496 tests; FULL unit suite 2411 suites / 33078 passed / 0 failed. Both halves of the requirement now hold: participant bindings are PERSISTED (campaign_session_participant for campaigns, durable match seats for combat) and authenticated durable membership PRECEDES registration as a replay/fan-out recipient on both sockets.
  - Receipt (2026-08-24, third step — the record is now CONSULTED on the CAMPAIGN socket; box STAYS OPEN for the combat binder): `bindCampaignSyncConnection` takes membership as an optional PORT and production supplies the store-backed adapter from `server.js`. Two properties exist now that did not before. A REVOKED member is refused even holding a valid room code - previously revocation lasted exactly as long as they stayed disconnected, because the code was the whole check, so reconnecting undid it. A RETURNING member is routed by their membership rather than by re-presenting the invite, so a rotated or expired code no longer locks out someone already admitted. The room code keeps its real job as the NEWCOMER invite, and a newcomer it admits is recorded durably (host as `gm`, room-code guest as `player`) so it is not needed again. ABSENCE OF THE PORT IS THE STRUCTURAL FLAG: a socket bound without it keeps the pre-6.2 behaviour exactly, and all ten pre-existing tests in that suite pass unchanged with the port absent - which is the evidence that the fallback really is the old path rather than a subtly different one. The adapter is supplied by production rather than defaulted inside the bind function, because a default would reach for SQLite from every test that binds a socket and those tests have no database. TWO DESIGN NOTES: `isRevoked` is a SEPARATE question from `isActive` rather than inferred from it, because the store deliberately reports a revoked row as absent - through that lens a newcomer and a revoked member are indistinguishable, and only one of them may be admitted; and the port does NOT escalate the store's seat refusals, because this call records an admission the socket layer already granted by its own rules, and turning a bookkeeping refusal into a disconnect would drop a player the session had just accepted (enforcing seat limits AT admission is task 9, where the refusal has somewhere to go). A durable member whose live session cannot take them falls through to the ordinary paths rather than failing - not an authorization problem, and those paths report the real reason. Falsified 1:1: neutralising the revocation check reds the revoked-member row, neutralising the durable-routing check reds the wrong-room-code row. Verification: tsc clean; oxlint 0 errors / 78 warnings unchanged; bind suite 14/14; multiplayer battery 69 suites / 494 tests; FULL unit suite 2411 suites / 33076 passed / 0 failed. **SCOPE OF THIS CLOSURE, STATED EXACTLY:** the requirement names TWO binders and only `bindCampaignSyncConnection.ts` is done. `bindMultiplayerSocketConnection.ts` (the combat socket) was not audited and is not claimed - it is tracked under task 6.2's remaining routing work rather than silently folded in here.
  - Progress (2026-08-24, second step — the durable RECORD now exists; see both notes): `campaign_session_participant` (SQLite migration 17) plus `CampaignSessionParticipantStore` persist one row per (campaign, session, participant) with the seat they hold. REVOCATION IS A TIMESTAMP, NEVER A DELETE - deleting would destroy the fact that they were ever a member (what the audit timeline needs) and make revoke-then-rejoin indistinguishable from never having left. ONE ACTIVE GM per session is enforced by a PARTIAL unique index rather than by application care, because a second GM is an authority split; partial so a revoked GM does not block their replacement, which is the whole reason the revoked row is kept. TWO TACTICAL SEATS is a count SQLite cannot express as a constraint, so it sits beside the insert and INSIDE the same transaction - counting free seats then inserting as separate statements would let two simultaneous joins each see one seat left and both take it. A reconnecting active member is `already-bound` (not an error, not a second seat); a revoked one is REFUSED rather than reinstated, or revocation would last exactly until they tried again; a second revoke returns false so a caller can tell "I revoked them" from "they were already out" without racing another revoker. Migration-ladder pins moved together: MAX(version) 16->17 and COUNT 15->16 (they differ because the ladder skips version 8). Falsified 1:1 - dropping the partial index reds the GM row, dropping the seat count reds the third-player row. Store suite 10/10; persistence migrations 8 suites / 87 tests; FULL unit suite 2411 suites / 33072 passed / 0 failed. STILL OPEN: nothing CONSULTS this record yet - the socket admission path still decides from the room code, so wiring it in is 6.2 and cold reauthentication from it is 6.3.
  - Progress (2026-08-24, first step — the ordering fix): found and fixed a live fan-out leak that this requirement's own title forbids. `addSocketToMatch(matchId, socket)` was the FIRST statement of `handleCampaignJoin`, executing before the room code was ever checked. A guest presenting the WRONG room code was answered `UNKNOWN_MATCH` and the handler returned - but the socket was already in `socketsByMatch`, nothing removes it, and `broadcast()` fans out to every socket in that set. So a client holding a valid auth token but no right to the campaign was refused entry and then received the campaign's events anyway: a refusal that refused nothing. MEASURED, not theorised - a RED test drove a rejected join and then a real host proposal, and the refused socket received **27 further messages** including `CampaignEvent` frames after its `UNKNOWN_MATCH`. FIX: registration now happens on each ACCEPTED path and never before - the host branch (admitted by identity, since `role` is host precisely because the verified player id matches the registry's host), the grant-channel `served` branch, and the room-code branch after `joinGuest` succeeds. The rejection paths simply never register. All nine pre-existing tests in that suite still pass unchanged, which is what shows the legitimate host and guest fan-out was not narrowed along with the leak. Verification: tsc clean; oxlint 0 errors / 78 warnings unchanged; `bindCampaignSyncConnection` suite 10/10; multiplayer battery 69 suites / 490 tests; FULL unit suite 2410 suites / 33062 passed / 0 failed. **WHY THIS BOX STAYS OPEN:** the requirement has two halves and only the ORDERING half is done. There is still no PERSISTED participant-to-campaign-session or participant-to-match binding - admission is decided per connection from the room code and the registry's in-memory host identity, so membership does not survive a restart and cannot be revoked out of band. Task 6.2 ("replace active room-code routing with durable campaign-session/match routing") and 6.3 (cold reauthentication from durable membership) are the other half, and this box closes with them. What is true today is narrower and worth stating exactly: a socket is no longer a fan-out recipient before its join is accepted. `bindMultiplayerSocketConnection.ts` was NOT audited here and is not claimed.

- [ ] 6.2 Replace active room-code routing with durable campaign-session/match routing after join while preserving invite expiry for newcomers.
- [ ] 6.3 Implement cold reauthentication from account/vault identity plus durable membership and mint scoped session tokens outside URLs; reject revoked or mismatched identities before payload disclosure.
- [ ] 6.4 Verify with `npm.cmd test -- --runInBand src/lib/multiplayer/server/__tests__/bindMultiplayerSocketConnection.test.ts src/lib/multiplayer/server/__tests__/bindCampaignSyncConnection.test.ts src/lib/multiplayer/server/__tests__/auth.test.ts` and a focused Playwright cold-reload test.
  - Progress (2026-08-24, STAYS OPEN — the Playwright half is not written): the three named suites pass together under `--runInBand` (3 suites / 29 tests). Writing the cold-reconnect proof found an AUTHORIZATION DEFECT that predates this program's work. The guest join path resolved the invite as `envelope.roomCode ?? entry.roomCode`, so a client sending NO room code had the session's OWN correct code substituted and the join always succeeded - omitting the invite was equivalent to presenting the right one, and any authenticated player who knew a match id could join a campaign they were never invited to. The room code protected nothing against them. Introduced in `27e1ee448` ("Prove live co-op campaign play through server transport"), found now only because a CONTROL row asserted that a stranger presenting no code is refused, and it was not. Fallback removed: a newcomer must actually present a code, and a durable member never reaches that line because the membership path admits them first - which is what makes the removal safe rather than a regression for people who legitimately have no code to present. The paired rows are the point: one proves a member can cold-reconnect on a brand-new socket with NO room code (the situation after a page reload or client restart, which before durable membership was simply a rejected join), the other proves a stranger doing the same is refused. Without the second, the first would pass equally against a server that had stopped checking anything at all. Falsified 1:1: restoring the fallback reds the stranger row. Verification: tsc clean; oxlint 0 errors / 78 warnings unchanged; bind suite 16/16; multiplayer battery 69 suites / 498 tests; FULL unit suite 2411 suites / 33080 passed / 0 failed. **WHY THIS BOX STAYS OPEN:** the task names a focused PLAYWRIGHT cold-reload test and that is not written. Stated honestly, a browser cannot observe the property that matters here - a real client re-presents whatever room code it cached, so the durable path and the invite path are indistinguishable from outside. A meaningful browser version needs the invite to be invalidated between joins (room-code rotation or launch-time clearing), which is task 6.2's remaining "preserve invite expiry" work; the Playwright row should be written once that exists, rather than written now against a scenario that cannot fail.


## 7. Durable Publication, Heartbeat, and Backpressure

- [ ] 7.1 Publish committed results from durable outbox rows after transaction commit and resume pending publication after restart, implementing `Commit Precedes Recipient Publication`.
- [ ] 7.2 Complete the bidirectional heartbeat contract in `Protocol.ts`, `ServerMatchSocketLifecycle.ts`, and `client.ts`; prove quiet healthy sessions remain connected and dead peers transition through reconnect.
- [ ] 7.3 Add bounded per-connection queues to `ServerMatchBroadcaster.ts`; on saturation mark only that participant behind, continue healthy delivery, and resynchronize from the durable viewer cursor.
- [ ] 7.4 Add tests for one socket-send failure, slow-client isolation, bounded queue/memory behavior, and convergence-gated progression; run `npm.cmd test -- --runInBand src/lib/multiplayer/server/__tests__/ServerMatchSocketLifecycle.test.ts src/lib/multiplayer/server/__tests__/reconnectPersistence.integration.test.ts src/lib/multiplayer/server/__tests__/hardenedTransport.test.ts`.

## 8. Durable Campaign Journal Migration and Cutover

- [ ] 8.1 Replace `CampaignHostRegistry.ts`'s unconditional in-memory event-store selection with an environment-aware durable server campaign journal while preserving a loud dev/test adapter.
- [ ] 8.2 Add idempotent backfill from existing materialized campaign snapshots into a genesis branch, preserving snapshots during migration and blocking ambiguous ownership for GM remapping.
- [ ] 8.3 Make campaign activity a role-scoped authoritative projection rather than a browser-global log and remove whole-envelope 409 retry as a conflict strategy.
- [ ] 8.4 Implement command-based conflict handling: revalidate disjoint stale commands, reject same-field stale commands, and return current branch/revision plus safe recovery action.
- [ ] 8.5 Verify with `npm.cmd test -- --runInBand src/lib/multiplayer/server/__tests__/CampaignHostRegistry.test.ts src/lib/multiplayer/server/__tests__/CampaignMatchHost.test.ts src/lib/campaign/persistence/__tests__/campaignMigration.test.ts`.

## 9. Non-Playing GM and Two-Player Membership

- [ ] 9.1 Extend campaign membership to one non-playing GM and two tactical player slots with durable role, ownership, readiness revision, branch, acknowledgement cursor, and revocation state, implementing `Co-op Campaign Has One Non-Playing GM and Two Tactical Players`.
- [ ] 9.2 Validate unit, force, proposal, readiness, and launch commands against durable ownership; clear only affected readiness when an authoritative force or revision changes.
- [ ] 9.3 Implement durable GM-loss pause with no implicit player promotion; allow resume only by the same reauthenticated GM and participant removal only through an audited GM command.
- [ ] 9.4 Gate scenario progression on convergence of all retained participants while allowing eligible committed delivery to healthy clients.
- [ ] 9.5 Verify with `npm.cmd test -- --runInBand src/lib/multiplayer/server/__tests__/CampaignSyncSession.test.ts src/lib/multiplayer/server/__tests__/CampaignGmArbiter.test.ts src/lib/multiplayer/server/__tests__/CampaignMatchHost.test.ts`.

## 10. Awaited Campaign Creation and Canonical Unit Adoption

- [ ] 10.1 Make campaign creation/adoption await authoritative campaign, genesis branch, GM membership, player-slot placeholders, force ownership, and unit-reference commit before returning success, implementing `Campaign Creation Has an Awaited Authority Checkpoint`.
- [ ] 10.2 Preserve customized-unit identity, weight, tech base, engine, gyro, armor, equipment, critical slots, and temporal metadata through campaign adoption and player ownership.
- [ ] 10.3 Materialize both tactical player forces from the active campaign branch/revision and reject stale force or ownership revisions before encounter creation.
- [ ] 10.4 Verify with focused campaign creation, serialization, customizer-handoff, and encounter-materialization tests plus `npm.cmd test -- --runInBand src/lib/campaign/persistence/__tests__/serializeCampaign.test.ts src/lib/campaign/encounter/__tests__/materializeCampaignMissionEncounter.test.ts`.

## 11. Pre-Serialization Viewer Projection

- [ ] 11.1 Introduce one server-side viewer projector used before `JSON.stringify` for live match and campaign frames, implementing `Viewer Projection Occurs Before Serialization` and private authority-sequence separation.
- [ ] 11.2 Store GM previews and private reasons in separately authorized server-only records; ensure player-safe facts contain no private fields or correlatable private identifiers.
- [ ] 11.3 Implement sealed Player 1/Player 2 choices and authoritative reveal while keeping ordinary public combat facts on immediate committed publication.
- [ ] 11.4 Add schema/object tests and raw-frame negative tests for GM-private reason, hidden metadata, private IDs, authority sequences, and inferable gaps; fail closed on any projection error.
- [ ] 11.5 Verify with `npm.cmd test -- --runInBand src/lib/multiplayer/server/__tests__/ServerMatchHost.fogOfWarIntegration.test.ts src/lib/multiplayer/server/__tests__/CampaignSyncSession.test.ts src/lib/multiplayer/server/__tests__/ServerMatchHost.test.ts`.

## 12. Replay Snapshot Timeline and Export Parity

- [ ] 12.1 Reuse the pre-serialization projector for replay, resync, snapshot, cold recovery, audit timeline, and export, implementing `Visibility Is Equivalent Across Surfaces` and `Timeline and Export Use the Same Viewer Projection`.
- [ ] 12.2 Add per-viewer projection digests and parity tests across live/replay/snapshot/cold-recovery/timeline/export for GM, Player 1, and Player 2.
- [ ] 12.3 Add authorization tests proving a player cannot query a private GM audit identifier or infer hidden authority events from delivery gaps.
- [ ] 12.4 Verify with focused replay, recovery, fog, audit, and export suites and `npm.cmd run verify:qc:replay-recovery`.

## 13. Durable Combat Outcome Receipt Boundary

- [ ] 13.1 Replace process-local terminal publication as the authority boundary with a combat outcome outbox written in the terminal combat transaction, implementing `Terminal Combat Outcome Uses Durable Outbox and Inbox`.
- [ ] 13.2 Add a campaign inbox and unique `(outcomeId, outcomeVersion)` receipt that commits campaign consequences atomically.
- [ ] 13.3 Prove duplicate delivery, crash-before-receipt, restart, and campaign ingestion retries apply one consequence batch.
- [ ] 13.4 Enforce `Combat-Only Rewind Stops at Campaign Receipt` with a typed closed-boundary response.
- [ ] 13.5 Verify with `npm.cmd test -- --runInBand src/lib/multiplayer/server/__tests__/bindCampaignSyncConnection.reconcile.test.ts src/lib/multiplayer/server/__tests__/CampaignMatchHost.test.ts` and focused `InteractiveSession.outcome` tests.

## 14. Immutable Combat Correction and Rewind Kernel

- [ ] 14.1 Route finalized combat corrections through server-authored authoritative commands while keeping preview non-mutating and GM-private, implementing `Combat Intervention Has Distinct Preview and Commit Phases`.
- [ ] 14.2 Add immutable combat branch, supersession, trusted-checkpoint, and stale-branch command semantics without changing existing combat reducers.
- [ ] 14.3 Reject commands during rebuild with retryable `PROJECTION_REBUILDING`; do not queue them invisibly.
- [ ] 14.4 Rebuild combat state, RNG provenance, fog, sealed choices, and viewer projections; activate the replacement branch atomically only after verification.
- [ ] 14.5 Verify with GM intervention suites, deterministic replay tests, fog integration tests, and `npm.cmd run verify:qc:gm:campaign-ledger`.

## 15. Cache-Only Checkpoints and Branch Recovery

- [ ] 15.1 Implement immutable checkpoints keyed by branch, authority head, reducer version, and digest, satisfying `Checkpoints and Compaction Are Cache-Only`.
- [ ] 15.2 Prove compatible checkpoint-plus-tail equals full replay for authoritative state and all three viewer digests.
- [ ] 15.3 Prove reducer-version or digest mismatch rebuilds from an earlier base or blocks truthfully without publishing partial recovery.
- [ ] 15.4 Add per-session corruption quarantine for broken sequence, lineage, receipt, or digest while a healthy control session remains available.

## 16. Retroactive Campaign Rebuild

- [ ] 16.1 Extend campaign intervention preview to declare affected date, missions/contracts, finances, transactions, loans, reputation, rewards, salvage, repairs, inventory, roster, unit/pilot/personnel, markets, receipts, scenario artifacts, activity, audit, and viewer-projection families.
- [ ] 16.2 Implement backward time and retroactive correction as replacement-branch replay from a trusted base, not a negative forward-day loop, satisfying `Retroactive Campaign Changes Rebuild Declared Families`.
- [ ] 16.3 Reject commands during rebuild with `PROJECTION_REBUILDING`; activate only after every declared state, artifact, receipt, audit, and viewer projection verifies.
- [ ] 16.4 Invalidate stale scenario drafts, force selections, outcomes, and other externalized artifacts explicitly and reject their later use.
- [ ] 16.5 Verify with campaign day-pipeline, campaign intervention control-plane, mission-launch, and time-cascade tests plus `npm.cmd run verify:qc:gm:time-cascade`.

## 17. Coordinated Post-Receipt Outcome Correction

- [ ] 17.1 Implement a distinct higher-version coordinated outcome-correction command for the post-campaign-receipt boundary, satisfying `Coordinated Outcome Correction Supersedes Across Journals`.
- [ ] 17.2 Execute coordinated correction as a durable saga: atomically record combat supersession, invalidation manifest, and replacement outcome outbox in the source authority, then idempotently record the target-scoped campaign replacement receipt and deterministic consequence batch in the target authority; expose pending/blocked state and do not claim a cross-store transaction.
- [ ] 17.3 Gate scenario N+1 on the active outcome receipt, replacement artifacts, active branch, and convergence of every retained participant.
- [ ] 17.4 Prove retry, reconnect, and restart cannot apply the coordinated correction twice and preserve cross-journal audit causality.

## 18. Role-Scoped Audit Timeline

- [ ] 18.1 Add branch/effective-head/supersession rendering and cross-journal outcome causality to the audit timeline, implementing `Timeline Preserves Branch and Supersession Lineage`.
- [ ] 18.2 Add separate authorized GM-private audit lookup and player-safe history/export; record private-record access and append exactly one private rejection-audit row before each terminal rejection without creating gameplay journal, outbox, cursor, replay, or export facts.
- [ ] 18.3 Add rewind-impact preview showing affected domains and artifacts without mutating state.
- [ ] 18.4 Add GM and player timeline/export parity tests plus negative privacy scans for private fields and hidden-event identifiers; prove rejected-command retries retain one authorized audit row while all player surfaces and gameplay authority remain unchanged.

## 19. Accessible Lifecycle and Recovery UX

- [ ] 19.1 Add stable UI states and locators for pending, sealed, finalized, syncing, reconnecting, behind, rebuilding, rewound, and blocked across campaign, combat, and GM surfaces.
- [ ] 19.2 Gate command surfaces during rebuild, stale branch, blocked recovery, and required convergence; provide actor-safe typed conflict and recovery actions.
- [ ] 19.3 Add keyboard-complete rewind preview/confirmation, focus management after errors and decisions, persistent text, live-region announcements, and narrow-viewport primary actions.
- [ ] 19.4 Verify affected component tests, `npm.cmd run verify:qc:viewport-sweep`, and accessibility assertions for every lifecycle state.
  - Progress (2026-08-24, STAYS OPEN — see the blocker at the end): found and fixed a real accessibility defect while writing the assertions this task asks for. `CampaignSyncStateBanner` — the strip that tells a player whether to trust what is on screen — was a plain `<p>` with NO live region. A sighted player watches it flip from "up to date" to "reconnecting"; a screen-reader user got nothing at all, because a status that changes outside a live region is announced to nobody. It now carries `role="status"`, `aria-live="polite"`, and `aria-atomic="true"`. POLITE DELIBERATELY, including for `blocked`: assertive cuts off whatever the reader is currently speaking, a sync posture is never more urgent than the sentence the player is in the middle of, and the blocked state also disables the controls so it is discoverable by trying rather than only by hearing. ATOMIC because without it a reader may announce only the CHANGED words, turning "you are up to date" into a fragment that means nothing alone. New a11y suite (`CampaignSyncStateBanner.a11y.test.tsx`, 14 rows, jest `a11y` project): axe-clean for all SIX existing states rather than one representative, because the per-state tone classes are exactly the kind of thing that silently breaks contrast for a single posture - and the broken one would be a failure state, which is when the message matters most; live-region announcement asserted BY ROLE rather than by testid, so it pins the accessibility tree a screen reader actually walks instead of a hook only tests can see; an explicit row that the region is never assertive; and a row that no state's announcement contains a DIGIT, because a spoken "12 events behind" rebuilds the same inference channel tasks 3.2/3.4 closed for the printed one. Falsified 1:1: changing `role="status"` to `role="presentation"` turns 8 of the 14 rows red. Verification: tsc clean; oxlint 0 errors / 78 warnings unchanged; a11y project 7 suites / 53 tests; campaign-coop + replica units 15 suites / 77 tests; **`npm run verify:qc:viewport-sweep` 51 passed (3.5m)**, which is the second of this task's three named gates; FULL unit suite 2410 suites / 33061 passed / 0 failed. **WHY THIS BOX STAYS OPEN:** the task requires a11y assertions for EVERY lifecycle state, and 19.1 names nine — pending, sealed, finalized, syncing, reconnecting, behind, rebuilding, rewound, blocked. Four of those cannot be asserted because they do not exist to render: a case-insensitive grep over non-test `src/components` and `src/lib/campaign` finds `sealed` in 0 files and `rewound` in 0 files, with `finalized` (1) and `rebuilding` (2) present only outside the lifecycle UI. Writing assertions for states nothing renders would be theatre. The six states that DO exist are now covered end to end; 19.4 closes when 19.1 builds the remaining four, and this note is the record of exactly which ones are owed.


## 20. Three-Context Sandbox Foundation

- [x] 20.1 Add `e2e/fixtures/gmTwoPlayerCampaign.ts` with isolated contexts reserved for the future non-playing GM, Player 1, and Player 2, plus a harness-owned server, per-run database, distinct identities, and deterministic seeds. The foundation proves isolation first; Task 9 durable membership later binds those contexts to actual roles and must pass `membership-smoke` before role admission is claimed.
- [x] 20.2 Add test-only fault controls guarded by `NODE_ENV === 'test'`, E2E run ID, explicit session scope, and one-shot consumption; make production startup reject enabled controls.
  - Receipt (2026-08-24): `src/lib/testing/faultControls.ts`. A fault-injection switch is a remote kill switch wearing a helpful name, so arming one requires FOUR things to line up and each closes a different leak: the build must be `NODE_ENV === 'test'`; a RUN id, so one Playwright run cannot arm a seam another run then trips over (which is what makes parallel runs safe); a SESSION scope, so the fault hits one session and a healthy control session stays available to prove the failure was CONTAINED rather than global; and ONE-SHOT consumption, because an armed-until-disarmed switch outlives the test that armed it and fails the next one for no visible reason. Arming outside a test build THROWS rather than no-opping - a silent no-op would let a fault test "pass" on a production build having proven nothing at all - and consumption returns false for every near miss (wrong run, wrong session, wrong seam, already consumed, not a test build), because a seam that fires on an approximate match is worse than no seam. A partially-scoped control is refused outright: it looks armed and its blast radius is whatever the missing field would have narrowed. PRODUCTION STARTUP REFUSES, and refuses at the REAL entrypoint: `server.js` now exits 1 with `FAULT_CONTROLS_IN_PRODUCTION` when `MEKSTATION_FAULT_CONTROLS` is set outside a test build. Exiting rather than ignoring is deliberate - a process that boots and merely declines to honour fault config is one refactor away from honouring it, while one that refuses to start cannot rot that way. The guard is a hand-written MIRROR because `server.js` is CommonJS and cannot import the TS module, so a sibling test SPAWNS `server.js` itself rather than trusting the mirror; drift between the two would otherwise be invisible. The guard is "not a test build", not "is production", so a dev server carrying fault config is refused identically. Environment is INJECTED rather than read globally, so exercising the production path cannot disturb other tests sharing the worker (it also removed a `NODE_ENV` readonly type error rather than casting past it). Falsified 1:1: neutralising the startup guard turns both rejection rows red while the no-config CONTROL row stays green - without that control every rejection row would pass against a server that simply fails to boot for an unrelated reason, which on a machine with no build it otherwise would. Verification: tsc clean; oxlint 0 errors / 78 warnings unchanged; fault-control suites 19/19 (16 scoping + 3 real-process startup); FULL unit suite 2408 suites / 33037 passed / 0 failed. NON-CLAIM: this ships the CONTROL, not the seams. No production call site consumes a fault yet - wiring `store.commit`, `outbox.insert`, and head-update seams belongs to the tasks that build those paths (plan todo 28 and leaf-4 task 2.4), and `MEKSTATION_FAULT_CONTROLS` is currently read only by the startup refusal.

- [x] 20.3 Add a dedicated SQLite evidence reader opened `readonly: true, fileMustExist: true`; never instantiate the production store for read-only proof.
  - Receipt (2026-08-24): `e2e/fixtures/sqliteEvidenceReader.ts` opens its own connection with `readonly: true, fileMustExist: true` and imports NOTHING from the production persistence layer. Both options are load-bearing for a reason worth stating: the production store CREATES its file and runs migrations, so pointed at a database the run never produced it would manufacture an empty one and report zero rows - evidence of absence conjured from nothing, which is the exact shape of a false pass. `fileMustExist` turns that into a thrown `EVIDENCE_DB_MISSING`, and the row proving it asserts the file was NOT brought into existence, not merely that the open threw. `readonly` makes immutability a database-level guarantee rather than a convention, and the byte-equality row compares the file BEFORE and AFTER a full probe (count + select + tables) as bytes rather than row counts, because a probe that rewrote a page while leaving rows alone would still make the artifact describe a different file than the run produced - which is what the plan's evidence gate means by identical hash and schema. Two refusals are named rather than left to the driver: a non-SELECT statement is rejected as `EVIDENCE_WRITE_REFUSED` with the offending SQL (the connection would refuse it anyway, but as "attempt to write a readonly database", which does not say what was attempted), and a table name that could carry SQL is rejected outright because table names cannot be bound as parameters. WIRED, NOT SHELVED: the fixture exposes `openEvidence('app' | 'multiplayer')` over the run-owned paths `assertRunOwnedPath` already validated, and the `fixture-smoke` gate now reads its own database through it and asserts the hash is unchanged - so the reader is proven against REAL run databases end to end, not only against synthetic ones. Falsified 1:1: flipping `fileMustExist`/`readonly` off turns the missing-database row red. A SELF-CORRECTION worth recording: the guard proving no production dependency first banned the string `DurableMatchStore` anywhere in the file, and failed on the module's own comment EXPLAINING why it is not used; it now scans import lines only, so it fails on a dependency rather than on prose. Verification: tsc clean; oxlint 0 errors / 78 warnings unchanged; reader suite 6/6; `fixture-smoke` and the 20.5 ownership spec both green on real builds; FULL unit suite 2405 suites / 33011 passed / 0 failed. NON-CLAIM: this ships the reader and one live use of it; the per-table durable-row assertions the program's evidence bundle needs (receipts, batches, outbox, participants, cursors, branches, outcome receipts) arrive with the tasks that create those tables.

- [x] 20.4 Add role-labeled traces, screenshots, raw socket transcript, pre-serialization projection capture, latency timestamps, durable-row export, state/projection hashes, environment manifest, and cleanup log under `test-results/gm-two-player/<run-id>/`.
  - Receipt (2026-08-24): `e2e/fixtures/gmTwoPlayerEvidence.ts` writes the declared bundle under `test-results/gm-two-player/<run-id>/`, role-labeled in the FILENAME so three contexts are distinguishable from a directory listing rather than by opening every file, with a sha256 and byte count per artifact. Two properties make such a bundle trustworthy and both are easy to lose silently. (1) IT CANNOT WRITE OUTSIDE ITS RUN - every path goes through the same `assertRunOwnedPath` the sandbox already uses, because a writer that escapes clobbers another run's evidence or a developer's files and the resulting artifact describes a run that never happened; a `../../../` name is refused. (2) IT CANNOT CARRY SECRETS - the bundle captures raw socket transcripts and pre-serialization projections, which is exactly where a bearer token or password lives, and an evidence file is written to disk, uploaded as a CI artifact, and read by people who were not in the run, so a leak there is a leak everywhere. The scan matches the SHAPE of a credential (password/token fields, `Bearer`, private keys) rather than known values, because a list of known secrets contains only the ones already known about; the refusal does NOT echo the match into its error, since an error message is itself logged and repeating it would defeat the refusal it just performed. A CONTROL row writes ordinary prose that merely mentions "token" and must still be accepted - a guard that refused every transcript containing the word would be switched off within a week. (3) THE MANIFEST DECLARES INTENT, not inventory: `recordMissing` puts an artifact the run could not capture into the manifest with a reason, so a missing file is a DETECTABLE gap; a manifest listing only what exists cannot distinguish "never captured" from "captured and lost", which is the difference between a gap and a bug. WIRED AND PROVEN LIVE: the fixture hands out `openEvidenceBundle()` and the `fixture-smoke` gate now writes a per-role artifact and finalizes a manifest; verified by reading the real bundle off disk after the run (3 role-labeled files plus `manifest.json`, every sha256 64 hex chars, environment carrying node version and run id). Falsified 1:1: neutralising the secret scan and the run-owned path check turns 6 rows red while the 4 structural rows stay green. Verification: tsc clean; oxlint 0 errors / 78 warnings unchanged; bundle suite 10/10; `fixture-smoke` green end to end on a real build; FULL unit suite 2409 suites / 33047 passed / 0 failed. SECTION 20 IS NOW COMPLETE (20.1-20.5). NON-CLAIMS: the bundle is the WRITER plus one live use - actually capturing traces, screenshots, socket transcripts, latency series, and durable-row exports happens in the tasks that produce those things (Playwright tracing/screenshots ride Todo 26's runner; socket transcripts and pre-serialization projections need the seams of sections 5 and 11; durable-row export needs the tables of section 2). The secret scan is a shape heuristic, not a proof of absence: it catches the credentials this program actually handles and would not catch an unrecognised encoding.

- [x] 20.5 Add ownership-scoped cleanup that preserves ambient browser tabs, Chrome processes, unrelated servers, databases, and user artifacts.
  - Receipt (2026-08-24): writing the proof found a REAL defect rather than confirming the implementation. The context half was already scoped correctly - `cleanup()` closes only the three contexts it opened - but the IDENTITY half was not, and nothing had ever asserted it. Seeding an identity through `POST /api/e2e/vault-identity` calls `repository.setActive`, whose SQL is `UPDATE vault_identities SET is_active = 0` across the WHOLE table; the sandbox seeds three, so it deactivated whatever identity the machine had, and then deleted its own three on teardown. End state: NO active identity at all. `getActive()` returned null and `/api/multiplayer/auth/token` answered `404 No vault identity configured` - a developer's own vault session silently stopped working after running this suite, which is exactly the "preserve ... user artifacts" this task forbids. FIX: the fixture now reads the prior active id BEFORE its first seed (new read-only `GET /api/e2e/vault-identity`, id-only because the caller needs to restore the identity, not learn about it) and hands it back through `DELETE`'s new `restoreActiveId`, which reactivates AFTER the deletes so the final state is the one the caller asked for even when the restore target was also in the delete list. A restore target that no longer exists is REPORTED (`restored: false`), not thrown - the harness cannot know whether something else removed that identity meanwhile, and failing teardown over it would turn a tidy-up into a test failure. PROOF (`e2e/gm-two-player-cleanup-ownership.spec.ts`, 2/2 on a real build): an ambient context and an ambient active identity survive a full fixture lifecycle; the fixture's own three contexts are gone and the bystander's is not; the ambient PAGE is re-navigated rather than merely found in the list, because a context object can outlive its pages; and a second `cleanup()` destroys nothing further (specs that clean up in both `finally` and an error path call it twice). A control asserts the fixture actually ADDED contexts, so the preservation rows cannot pass against a fixture that created nothing. Falsified 1:1 - dropping `restoreActiveId` turns both rows red. HONEST NOTE ON METHOD: the first version of this spec probed identity liveness by minting a token and asserted an "unrelated identity survives", which failed - and the failure was mine, not the fixture's: identities are a collection with ONE active row, so `getActive()`-backed auth answers about activation, not existence. Chasing that wrong probe is what surfaced the actual defect. Verification: tsc clean; oxlint 0 errors / 78 warnings, unchanged from main at `c7fba3db7` (the count moved 77 to 78 when PRs #1342-#1343 landed, not here); api/vault/scripts batteries 117 suites / 1784 tests; the endpoint's own unit suite grew 4 to 8 rows covering the GET, the null case, a successful restore, and an unrestorable target; the `fixture-smoke` gate still passes end to end; FULL unit suite 2404 suites / 33005 passed / 0 failed. NON-CLAIMS: Chrome PROCESS preservation and unrelated-server/database preservation are asserted only insofar as the fixture never targets them - there is no positive process-level assertion, and adding one needs the fault-control seams of task 20.2; this closes the browser-tab and user-artifact halves of 20.5 with evidence and leaves the process half named rather than implied.


## 21. Strict Acceptance Scenarios E2E-01 Through E2E-45

- [ ] 21.1 Implement and pass E2E-01 through E2E-18 for durable creation, order, atomicity, idempotency, replay/live recovery, cursor integrity, slow-client isolation, restart, reauthentication, active routing, and heartbeat.
- [ ] 21.2 Implement and pass E2E-19 through E2E-30 for pre-serialization privacy, sealed choices, immediate public facts, fog projections, live/replay/export parity, unauthorized access, veto/timeout, and simultaneous proposal attribution.
- [ ] 21.3 Implement and pass E2E-31 through E2E-45 for player ownership, deterministic concurrency, server RNG, disconnect policy, outcome receipts, immutable correction, pre-receipt combat rewind, branch convergence, fog restoration, and coordinated post-receipt correction.
- [ ] 21.4 Register an exact command `npm.cmd run verify:qc:gm-two-player-campaign -- --group=authority` and make failures retain the complete evidence bundle.

## 22. Strict Acceptance Scenarios E2E-46 Through E2E-80

- [ ] 22.1 Implement and pass E2E-46 through E2E-60 for time cascade, retroactive rebuild, invalidation, scenario continuity, customized-unit fidelity, ten-scenario restarts, checkpoint equivalence, receipts, provenance, and impact preview.
- [ ] 22.2 Implement and pass E2E-61 through E2E-70 for malformed/replay attacks, atomic failure, fail-closed projection, GM loss, partition catch-up, stale branch, quarantine, bounded replay, and socket failure.
- [ ] 22.3 Implement and pass E2E-71 through E2E-80 for latency, catch-up, backpressure, accessible lifecycle UX, confirmation, typed conflicts, evidence completeness, cleanup, and exact-main post-merge audits.
- [ ] 22.4 Register `--group=campaign`, `--group=failure`, `--group=performance`, and `--group=all` selectors without weakening strict backing requirements.

## 23. Performance and Long-Run Gates

- [ ] 23.1 Commit the controlled fixture configuration: 20 warm-up commands, at least 200 measured representative commands, monotonic correlated clocks, nearest-rank percentile calculation, named runner class, 100-event/512-KiB replay chunks, 256-envelope/1-MiB queue bound, 128-MiB server post-warm memory-growth ceiling, and 64-MiB post-warm ceiling per controlled browser client.
- [ ] 23.2 Gate p95 at 250 milliseconds, p99 at 750 milliseconds, and 1,000-event cold catch-up at 2 seconds on the recorded controlled loopback runner; keep the 2,000-millisecond Playwright wait as a functional timeout only.
- [ ] 23.3 Run ten sequential scenarios with periodic player reconnects, GM reconnect, server restart, correction, pre-receipt rewind, post-receipt coordinated correction, and checkpoint/full-replay comparison.
- [ ] 23.4 Verify `npm.cmd run verify:qc:gm-two-player-campaign -- --group=performance` and `npm.cmd run verify:qc:campaign-long`; archive latency and memory JSON with the run.

## 24. Cutover Rollback and Exact-Main Regression

- [ ] 24.1 Prove shadow dual-write state and audience digests match before enabling journal authority for new sandbox sessions.
- [ ] 24.2 Cut over new sessions behind the reviewed feature flag, preserve legacy completed-log read compatibility, and refuse ambiguous active-session migration.
- [ ] 24.3 Document rollback that stops new admission, preserves journal/branch/receipt/audit rows, and returns to a schema-compatible reader without destructive history edits.
- [ ] 24.4 After the pre-harness ABI-checker merge, archive its exact-main native-store regression. Land the isolated three-context fixture next, then after every later major merge update the evidence ledger and run the applicable staged `npm.cmd run verify:qc:gm-two-player-campaign -- --group=<slice>` against exact main before the next dependent PR: `fixture-smoke` before role admission, `membership-smoke` when durable GM/P1/P2 roles land, and the evolving `smoke` subset thereafter.

## 25. Final Verification and Documentation

- [ ] 25.1 Run focused changed-area Jest and Playwright suites, then `npm.cmd run typecheck`, `npm.cmd run lint`, `npm.cmd run format:check`, `npm.cmd run verify:qc:multiplayer:contracts`, `npm.cmd run verify:qc:multiplayer:browser`, `npm.cmd run verify:qc:coop-campaign-journey`, `npm.cmd run verify:qc:encounter-combat-continuity`, `npm.cmd run verify:qc:replay-recovery`, and the complete GM/two-player command.
- [ ] 25.2 Run `openspec validate harden-gm-two-player-campaign-sessions --strict --no-interactive`, `npm.cmd run qc:openspec-ci:validate`, and LSP diagnostics for every modified TypeScript/TSX file.
- [ ] 25.3 Update operator and contributor documentation for authority versus delivery sequence, membership/reauthentication, host loss, rewind boundary, recovery/quarantine, evidence bundle, performance methodology, and exact-main post-merge audit.
- [ ] 25.4 Reconcile all 80 acceptance scenarios to test identifiers and evidence paths, leave no unchecked implementation task without a linked blocker, and archive the change only after every applicable strict gate passes.
