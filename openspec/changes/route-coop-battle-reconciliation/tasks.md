# Tasks: route-coop-battle-reconciliation

> Execution is **Codex-first and strictly sequential** — parallel agent fan-out has failed on this repo. Each task is bounded to 1–3 files and lands with its own verification before the next begins. Claude orchestrates, packages context, and final-reviews. Pre-commit runs a full `next build` (~3–5 min) — commit via background shell. After any type/schema edit run oxfmt + the affected validator + its jest wrapper (formatter/oxfmt quote race, R3). Reference: wave-2 precedent `emitCoopDayAdvancedEvent` at `src/stores/campaign/useCampaignStore.dayActions.ts:318-361`.

## 1. `ReconcileBattle` host-intent type + wire schema

- [ ] 1.1 Add the `ReconcileBattle` host-intent type. In `src/types/campaign/CampaignSync.ts` add `ICampaignReconcileBattleIntent` — `{ readonly kind: 'ReconcileBattle'; readonly campaignId: string; readonly intentId: string; readonly payload: ICoopBattleConsequences }` (import `ICoopBattleConsequences` from `src/lib/campaign/coop/reconcileCoopBattle.ts`, or relocate that interface to `CampaignSync.ts` if an import cycle appears — record the choice in the file comment). Do **NOT** add `'ReconcileBattle'` to `CampaignIntentKind`, `ICampaignIntentPayloadMap`, or `validateCampaignIntent` (D1 QC-pin containment).
  - Files: `src/types/campaign/CampaignSync.ts` (+ possibly `src/lib/campaign/coop/reconcileCoopBattle.ts` for the interface move)
  - Acceptance: `ICampaignReconcileBattleIntent` is exported and carries the full `ICoopBattleConsequences`; the guest-facing `CampaignIntent` union is byte-for-byte unchanged
  - QA: `npm run typecheck` clean; `git diff` shows no change to `CampaignIntentKind` / `validateCampaignIntent`
- [ ] 1.2 Widen the host-intent wire frame. In `src/types/multiplayer/Protocol.ts` add `CampaignReconcileBattleIntentSchema` (zod, mirroring the type in 1.1 with a `z.literal('ReconcileBattle')` discriminant) and a **NEW** `CoopBattleConsequencesSchema` payload (create it — no such schema exists today; reuse `rosterUnitSchema` from `src/types/campaign/campaignSyncSchemas.ts` for the roster-change entries, and make `fundsDelta` a `z.number().finite()` so negative debits are legal). Then change `CampaignHostIntentSchema.intent` from `CampaignIntentSchema` to `z.union([CampaignIntentSchema, CampaignReconcileBattleIntentSchema])`. Add a **failing-first** jest round-trip: a `ReconcileBattle` `CampaignHostIntent` frame parses, and a frame with an unknown host-intent `kind` is rejected (no silent `default`).
  - Files: `src/types/multiplayer/Protocol.ts`, new/updated `src/types/multiplayer/__tests__/Protocol.campaignHostIntent.test.ts`
  - Acceptance: test fails on `main` (schema rejects `ReconcileBattle`), passes after the union widening; `CampaignIntentSchema` is unchanged
  - QA: `npm.cmd test -- --watchAll=false --runTestsByPath src/types/multiplayer/__tests__/Protocol.campaignHostIntent.test.ts --runInBand`; then oxfmt + `npm run typecheck` (R3)
- [ ] 1.3 Widen the client transport signature. In `src/lib/campaign/coop/campaignSyncTransport.ts` widen `sendHostIntent`'s parameter type (interface `ICampaignSyncTransport.sendHostIntent`, line ~28, and the impl at ~125) to `ICampaignIntent | ICampaignReconcileBattleIntent`. No runtime change — the impl already wraps the intent in a `CampaignHostIntent` envelope verbatim.
  - Files: `src/lib/campaign/coop/campaignSyncTransport.ts`
  - Acceptance: a `ReconcileBattle` intent type-checks through `sendHostIntent`; `AdvanceDay`/`SpendFunds` callers still type-check
  - QA: `npm run typecheck` clean

## 2. Server handler branch + per-match idempotency guard

- [ ] 2.1 Add the per-match reconciled-set guard state. On the `CampaignHostRegistry` entry (`src/lib/multiplayer/server/CampaignHostRegistry.ts`) hold a bounded per-match set of reconciled battle ids (pattern precedent: `src/lib/multiplayer/server/reconnection/AcceptedIntentTracker.ts`). Expose `hasReconciledBattle(matchId)` / `recordReconciledBattle(matchId)` (or an equivalent tracker on the entry). Keyed on the resolved-battle `matchId` inside `ICoopBattleConsequences`.
  - Files: `src/lib/multiplayer/server/CampaignHostRegistry.ts`
  - Acceptance: the guard is scoped to one registry entry and survives across frames for the match's lifetime; a fresh entry starts empty
  - QA: `npm run typecheck` clean
- [ ] 2.2 Branch `handleCampaignHostIntent` on `ReconcileBattle`. In `src/lib/multiplayer/server/bindCampaignSyncConnection.ts:246-282`, after the existing `envelope.playerId === entry.hostPlayerId` host check, branch: if `envelope.intent.kind === 'ReconcileBattle'`, consult the 2.1 guard on `intent.payload.matchId` — if already reconciled, ack as a no-op; otherwise call `reconcileCoopBattle(entry.host, intent.payload)`, then `recordReconciledBattle(...)`. Non-`ReconcileBattle` intents keep the existing `entry.host.applyHostIntent(envelope.intent)` path. Write a **failing-first** jest test asserting: a `ReconcileBattle` frame drives `reconcileCoopBattle` and the funds/salvage/roster events broadcast to a subscribed guest socket; a **second identical** frame commits **zero** additional events.
  - Files: `src/lib/multiplayer/server/bindCampaignSyncConnection.ts`, `src/lib/multiplayer/server/__tests__/bindCampaignSyncConnection.reconcile.test.ts`
  - Acceptance: test fails on `main` (no `ReconcileBattle` branch), passes after; duplicate frame is a proven no-op (D2)
  - QA: `npm.cmd test -- --watchAll=false --runTestsByPath src/lib/multiplayer/server/__tests__/bindCampaignSyncConnection.reconcile.test.ts --runInBand`; `npm run typecheck` clean

## 3. Route the outcomes seam over the transport (host live path)

- [ ] 3.1 Route reconciliation over the transport in host mode. In the reconciliation seam (`src/lib/campaign/coop/coopHostRegistry.ts` `reconcileCoopOutcomeForCampaign`, and/or `src/stores/campaign/useCampaignStore.outcomes.ts` `enqueueCampaignOutcome`): when `campaign.coopSession?.mode === 'host'`, derive `ICoopBattleConsequences` (reuse `deriveCoopBattleConsequences`), resolve `getActiveCampaignSyncTransport(matchId)`, and if `transport?.role === 'host'` send `sendHostIntent({ kind: 'ReconcileBattle', campaignId, intentId: \`coop-recon-${matchId}\`, payload: consequences })` — **instead of** `reconcileCoopBattle` against the browser-local `getActiveCoopHost`. Mirror `emitCoopDayAdvancedEvent` (matchId resolution + `role === 'host'` guard) exactly. **New return contract for `reconcileCoopOutcomeForCampaign`**: it currently returns `Promise<ICoopReconciliationResult | null>` and existing callers/tests assert `result?.ok`. Change it to a `sent | degraded` discriminant (recommended: `{ outcome: 'sent' } | { outcome: 'degraded' } | null`), OR keep `ICoopReconciliationResult` and return the `{ ok: true, events: [] }` sentinel for the `sent`-over-wire case (events now commit server-side, so the local `events` array is empty) — pick one and update every `result?.ok` assertion accordingly. `null` stays the return for guest / single-player (unchanged). Write a **failing-first** integration test: with a host-role transport present, an enqueued co-op outcome sends exactly one `ReconcileBattle` intent carrying the derived consequences and does **not** call the browser-local host.
  - Files: `src/lib/campaign/coop/coopHostRegistry.ts`, `src/stores/campaign/useCampaignStore.outcomes.ts`, test under `src/lib/campaign/coop/__tests__/` or `src/stores/campaign/__tests__/`
  - Also rework the two suites that pin the OLD browser-local-host behavior and fail by design after this rewire (do NOT delete — repoint their assertions): `src/stores/campaign/__tests__/coopBattleReconciliationWiring.test.ts` (assert the `ReconcileBattle` send against a fake host-role transport instead of the local-host mutation) and `src/__tests__/integration/coopCampaignPlay.test.ts` (the `reconcileCoopOutcomeForCampaign` registry-gate assertions around `:264-309` — repoint to the wire send, or to the 3.2 degrade assertion when no transport is present).
  - Acceptance: test fails on `main` (routes to browser-local host), passes after (sends the wire intent); matchId dedup guards at `outcomes.ts:48-49` still precede the send; the two reworked suites are green against the new contract
  - QA: `npm.cmd test -- --watchAll=false --runTestsByPath <the new integration test> src/stores/campaign/__tests__/coopBattleReconciliationWiring.test.ts src/__tests__/integration/coopCampaignPlay.test.ts --runInBand`; `npm run typecheck` clean
- [ ] 3.2 Add the no-transport degrade notice. When `mode === 'host'` but no host-role transport is connected, keep the local persist (already done by `enqueueCampaignOutcome`) and surface a warning notice mirroring `dayActions.ts:333-341` ("reconciliation saved locally but the live host connection is unavailable; guests may need to refetch"). Do **NOT** fall back to mutating the browser-local `CampaignMatchHost`. Guest (`mode === 'guest'`) and single-player (`coopSession === undefined`) paths return early exactly as today.
  - Files: same as 3.1
  - Acceptance: no-transport host reconciliation emits the notice and writes nothing to `getActiveCoopHost`; guest/single-player behavior byte-for-byte unchanged
  - QA: extend the 3.1 test with a no-transport case; `npm run typecheck` clean
- [ ] 3.3 Verify the live host reconciliation path no longer depends on the host-mount runtime. In `src/components/campaign/coop/CampaignCoopRouteSurfaceConnected.tsx` confirm no reconciliation dependency on the host-mount `openCoopRuntimeSession` instance remains after tasks 3.1/3.2 (reconciliation now routes over the transport, not through `getActiveCoopHost`). **KEEP `openCoopRuntimeSession` mounted** — it still drives `subscribeCoopPendingProposals` and the `decideGuestProposal` fallback; do NOT read this task as license to remove the mount. This task is a verification + narrow cleanup (drop only a genuinely dead local-arbiter subscription if 3.1/3.2 orphaned one), not a removal of the runtime.
  - Files: `src/components/campaign/coop/CampaignCoopRouteSurfaceConnected.tsx`, affected `__tests__` under the co-op / dashboard folders
  - Acceptance: the production host reconciliation no longer depends on `getActiveCoopHost`; `openCoopRuntimeSession` stays mounted for proposals; the existing co-op suites pass (with explicit single-graph setup only where a suite relied on the reconciliation-via-mount path, R4)
  - QA: `npm.cmd test -- --watchAll=false --runTestsByPath src/components/campaign/coop/__tests__/GuestProposalSurface.test.tsx src/__tests__/pages/gameplay/campaigns/index.coop.test.tsx src/components/gameplay/pages/campaigns/dashboard/__tests__/CampaignDashboardPage.coopPersistenceRegression.test.tsx --runInBand`; `npm run typecheck` clean

## 4. Live two-browser verification

- [ ] 4.1 Prove the guest mirror converges on a host-resolved battle **live**. Extend `e2e/coop-campaign-two-browser-journey.spec.ts` (or add a sibling spec) with: host + guest in two contexts join a co-op campaign; the host resolves a co-op battle; assert the **guest** dashboard's rendered state converges on the host's post-battle values **without a guest reload** (assert the live push per R5, not post-rejoin state). Assert against rendered guest UI backed by the mirror store — never a JS-heap bridge.
  - **Produced-set correction**: the live derivation hardcodes `missionPayout: 0` (`src/lib/campaign/coop/coopHostRegistry.ts:116`), so `fundsDelta` is `0` and **no `FundsChanged` is producible live** — do NOT assert a funds *change*. Assert instead: guest funds **EQUAL** host funds (no divergence), the **salvage pool CHANGED** (destroyed OpFor credits it), and the **roster CHANGED** (a destroyed player-side unit). The `FundsChanged` leg is proven at the jest level in task 2.2, not here.
  - **Guest-mirror salvage surface must be ADDED**: the guest mirror UI currently renders **no** salvage pool (`guestMirrorSummary` omits `salvagePool`). Add a rendered guest-mirror salvage-pool surface (with a stable testid) so the e2e can assert the pool moved — otherwise there is nothing to observe. Also add a guest-mirror unit-count/roster testid if none exists.
  - **Sanctioned battle trigger**: use the store-bridge trigger pattern from `e2e/campaign-round-trip.spec.ts` (`state.enqueueOutcome(outcome)` in page context as the TRIGGER; rendered UI as the ASSERTION). Build the outcome with **destroyed OpFor units** (so `deriveCoopBattleConsequences` credits salvage) and **one destroyed player-side unit** (so the guest-mirror unit count visibly moves).
  - Files: `e2e/coop-campaign-two-browser-journey.spec.ts` (+ helpers under `e2e/helpers/` as needed), `src/components/campaign/coop/CampaignCoopRouteSurfaceConnected.tsx`, `src/components/campaign/coop/CampaignCoopRouteSurface.tsx` (add the rendered guest-mirror salvage-pool + roster surface)
  - Acceptance: spec green locally, two contexts; guest funds equal host funds, guest-mirror salvage pool changed, guest-mirror roster/unit-count changed — all live, no reload
  - QA: `node scripts/playwright/run-playwright.mjs test --project=chromium e2e/coop-campaign-two-browser-journey.spec.ts --workers=1`

## Final Verification Wave

- [ ] F1 Typecheck + lint + format clean across all touched files: `npm run typecheck && npm run lint && npm run format:check`.
- [ ] F2 The wave's contract/integration suites pass: the host-intent frame schema round-trip (1.2), the `bindCampaignSyncConnection` reconcile routing + idempotency (2.2), the outcomes-path send-on-transport + degrade (3.x), and the single-graph jest suites (3.3) — all green.
- [ ] F3 The two-browser live verification is green: `node scripts/playwright/run-playwright.mjs test --project=chromium e2e/coop-campaign-two-browser-journey.spec.ts --workers=1` — the guest mirror converges live on the host-resolved battle.
- [ ] F4 `npx openspec validate route-coop-battle-reconciliation --strict` passes, and every SHALL in the delta is backed by a test/QA scenario (host live push → 2.2 + 4.1; idempotency → 2.2; degrade → 3.2; single-player unaffected → 3.1/3.2 early-return coverage).
- [ ] F5 Confirm no regression to the guest-facing `CampaignIntent` union or the combat `GameEventType`: `git diff` shows `CampaignIntentKind` unchanged in `src/types/campaign/CampaignSync.ts`, `validateCampaignIntent` unchanged in `src/lib/multiplayer/server/CampaignMatchHostIntent.ts` (that is where it lives — not `CampaignSync.ts`), and no new `GameEventType` member (D1).
