# Tasks: add-sp-combat-determinism

> Execution is **Codex-first and strictly sequential** — parallel agent fan-out has failed on this repo. Run one group at a time; each lands with its verification before the next. Claude orchestrates, packages context, and final-reviews. Pre-commit runs a full `next build` (~3–5 min) — commit via background shell. **Non-goals are load-bearing: do NOT touch MP dice behavior (`ServerMatchHostBootstrap`, `MatchRecovery` semantics, `RollCapture`), do NOT change `runToCompletion` / `QuickResolveService` rule-off stream composition (the ONE sanctioned exception is task 1.4's MaxTech heat-scale location roller, which draws only when that optional rule is on — design D7), do NOT persist roll counters, and no test may pin a seed→roll-sequence or seed→outcome golden value (design D6).** Line numbers below were verified on `main@b4c2d8571` — re-verify against the current tree before editing. After any spec-delta edit, run the formatter + `npx openspec validate add-sp-combat-determinism --strict` (design R6). Ledger registration for this active change ships with the declarative PR stage, not with these implementation tasks.

## 1. Engine — persist the seed and inject the seeded dice stream (game-engine-orchestration, dice-system)

- [x] 1.1 Grep all current-tree call sites of `new InteractiveSession(` and `.createInteractiveSession(` (production + test) and record them, so the constructor-arg addition in 1.2 and the injection in 1.3 cover every caller and provably skip the MP path (`ServerMatchHostBootstrap.ts` constructs directly and must be untouched).
  - Files: read-only grep across `src/`.
  - Acceptance: a written call-site list; MP construction sites confirmed outside the edit set.
- [x] 1.2 Add `readonly seed?: number | null` to `IGameConfig` (doc-commented, `encounterId` style). Stamp `seed: this.seed` into `runToCompletion`'s inline `gameConfig` (`GameEngine.ts:128-137`). Extend `InteractiveSessionConstructorArgs` with a trailing optional `seed?: number` (12th positional) and thread it through `buildInteractiveSessionGameConfig` (`InteractiveSession.setup`) into the session config. Callers that omit it persist no seed (MP/tests unchanged).
  - Files: `src/types/gameplay/GameSessionUnitTypes.ts`, `src/engine/GameEngine.ts`, `src/engine/InteractiveSession.ts`, `src/engine/InteractiveSession.setup.ts` (or wherever `buildInteractiveSessionGameConfig` lives — confirm by grep).
  - Acceptance: both engine execution modes produce sessions whose `config.seed` equals the engine's resolved seed; direct constructions without the arg produce configs with no seed.
- [x] 1.3 In `createInteractiveSession` (`GameEngine.ts:244-257`), replace the literal `undefined` 9th argument with `new SeededD6Roller(this.seed).asD6Roller()` (dedicated stream — design D1; import from `@/simulation/core`), and pass the new `seed` arg from 1.2. Rewrite the stale `d6Roller` doc comment at `InteractiveSession.ts:146-155` (engine callers now always inject; the `Math.random` fallback remains only for roller-less direct constructions). Leave the vibro-claw fallback line (`InteractiveSession.ts:570`) as-is (design D2).
  - Files: `src/engine/GameEngine.ts`, `src/engine/InteractiveSession.ts`.
  - Acceptance: an engine-created interactive session's resolvers all consume the injected roller; no `Math.random`-backed roll is reachable from `createInteractiveSession` output; MP direct-construction behavior byte-identical.
- [x] 1.4 Seed the MaxTech heat-scale critical location roller (design D7 — the one resolver-level `Math.random` escape live in BOTH execution modes). Add `createLocationIndexRollerFromD6(d6: D6Roller): () => number` to `gameSessionHeat.helpers.ts` (exactly uniform over 0–7, consuming only the given d6 stream — reference scheme: three draws, `((a-1)*36 + (b-1)*6 + (c-1)) % 8`, doc-commented with the 6³ = 216 ≡ 0 (mod 8) uniformity argument). Pass `maxTechCriticalLocationRoller` at both production `resolveHeatPhase` call sites: `GameEngine.ts:211` (derive from the `runToCompletion` `d6Roller` closure) and `InteractiveSession.phases.ts:228` (derive from `context.d6RollerForResolvers()`; pass `undefined` when no roller injected so roller-less direct constructions keep the legacy default). The `Math.random` default at `gameSessionHeat.ts:48-50` stays in place (containment mirror of D2). Unit-test the helper: same stream ⇒ same index sequence; all values in 0–7; no `Math.random` call.
  - Files: `src/utils/gameplay/gameSessionHeat.helpers.ts`, `src/engine/GameEngine.ts`, `src/engine/InteractiveSession.phases.ts`, helper test under the existing gameplay `__tests__` layout.
  - Acceptance: with `maxtech-heat-scale` enabled, no `Math.random` location roll is reachable in either engine mode; with the rule off, the draw sequence in both modes is byte-identical to pre-change (the location roller is constructed but never draws).
- [x] 1.5 QA — `npm run typecheck` clean; existing engine + resolver + heat suites green (targeted: `npx jest --selectProjects unit --runInBand` on the `src/engine/__tests__/` suites, the `gameSessionHeat` suites, and any suite named in 1.1's call-site list).

## 2. Recovery — re-seed from the persisted seed, position zero (game-engine-orchestration)

- [x] 2.1 In `fromSessionAsync` (`InteractiveSession.ts:322-346`): when `session.config.seed` is a finite number, construct `new SeededRandom(session.config.seed)` for the `random` param and `new SeededD6Roller(session.config.seed).asD6Roller()` for the `d6Roller` param; when absent, pass exactly the current values (`new SeededRandom(0xc0ffee)`, `undefined`) — byte-identical legacy/MP behavior (design D4, R1).
  - Files: `src/engine/InteractiveSession.ts`.
  - Acceptance: recovered sessions with a persisted seed get seeded AI random + seeded dice re-seeded from position zero; sessions without one behave exactly as today.
- [x] 2.2 Verify the persistence round-trip: a jest that creates an interactive session with a known seed, persists/serializes its event log, rehydrates via the same path `recoverInteractiveSession` uses (`hydrateRecoverableSessionFromMatchLog` → `fromSessionAsync`), and asserts `config.seed` survives (R2). If the `GameCreated` payload or hydrator prunes the field, fix the creator/hydrator in this task.
  - Files: new or extended test under `src/engine/__tests__/`; possibly the `GameCreated` event creator / `hydrateGameSessionFromEvents` if pruning is found.
  - QA: the round-trip jest green.
- [x] 2.3 Recovery-determinism jest — prove the recover-twice invariant on the seam this change modifies (design D4 proof obligation; spec scenario "Identical rehydrations produce identical continuations"). Create ONE seeded interactive session, drive it partway (so recovery starts mid-battle, not at turn 0), persist its match log, then rehydrate it TWICE through the same chain the live route mounts (`hydrateRecoverableSessionFromMatchLog` → `fromSessionAsync` — the composition of `recoverInteractiveSession`, `InteractiveSession.ts:660-665`; do NOT shortcut via a hand-built session object), drive both recovered sessions through identical `advancePhase` loops to completion, and assert deep-equality of the two **normalized** post-recovery event streams (same normalizer as 3.1). No golden values: the assertion is run-to-run equality only. Rationale from repo history: "jest passing while live fails means the test skipped the route-mount recovery path" — W2's seed-then-goto rides exactly this factory.
  - Files: new or extended test under `src/engine/__tests__/` (e.g. `interactiveSessionRecoveryDeterminism.test.ts`).
  - QA: the recover-twice jest green; a divergence here means the re-seed wiring in 2.1 is incomplete (e.g. a recovery-path RNG consumer not re-seeded from `config.seed`) — fix the wiring, never the normalizer.
- [x] 2.4 QA — MP non-drift proof: MP recovery + transport suites green unchanged (the `src/lib/multiplayer/server/` `__tests__` suites covering `MatchRecovery` / `ServerMatchHost`); grep confirms no MP construction site passes the new seed arg.

## 3. Determinism jest proof — same seed ⇒ identical normalized event streams (dice-system)

- [ ] 3.1 New jest: build two `GameEngine`s with the same seed and identical unit inputs, create spectator-mode interactive sessions from each, drive both through identical `advancePhase` loops to completion (turn-limit-bounded), and assert deep-equality of the two **normalized** event streams (strip wall-clock timestamps and generated ids; keep order, types, phases, roll-bearing payloads, final outcome — design D6/R3). Run the same-seed pair TWICE: once with default rules and once with `optionalRules: ['maxtech-heat-scale']`, so the optional-rule heat-crit dice path (design D7) is inside the detector's coverage — a default-rules-only proof cannot see a leak in code the rules gate off. Add a different-seed third run asserting only validity (divergence expected, NOT asserted). **No golden sequences**: the test must contain no expected roll values for any seed.
  - Files: `src/engine/__tests__/interactiveSessionDeterminism.test.ts` (new).
  - QA: `npx jest --selectProjects unit --runInBand --runTestsByPath src/engine/__tests__/interactiveSessionDeterminism.test.ts` green; a same-seed divergence here means a resolver still reaches `Math.random` (design R4) — fix the resolver, never widen the normalizer.
- [ ] 3.2 QA — full engine + gameplay-resolver targeted suites green after the proof lands (catch any resolver routed through the context roller in R4 triage).

## 4. Launch surfaces — thread `?seed=N` (game-engine-orchestration)

- [ ] 4.1 `pre-battle.tsx` (`src/pages/gameplay/encounters/[id]/pre-battle.tsx:158-159` query destructure): parse `seed` from `router.query` (first value, `Number.parseInt(..., 10)`, `Number.isFinite` guard — mirror `server.js:590-595`) and pass `seedOverride?: number` into both hooks. In `usePreBattleLaunch.ts:224` and `usePreBattleSkirmish.ts:181`, change `seed: Date.now()` to `seed: seedOverride ?? Date.now()` (threaded through their options types).
  - Files: `src/pages/gameplay/encounters/[id]/pre-battle.tsx`, `src/components/gameplay/pages/preBattle/usePreBattleLaunch.ts`, `src/components/gameplay/pages/preBattle/usePreBattleSkirmish.ts`.
  - Acceptance: `?seed=N` on the pre-battle route reaches `new GameEngine({ seed: N })` for auto, interactive, and skirmish launches; absent/invalid seed falls back to `Date.now()`.
- [ ] 4.2 Quick-game surface: same parse on `src/pages/gameplay/quick/index.tsx`; thread `seedOverride` into `startBattle` / `startSpectatorMode` / `startInteractiveSkirmish` so `createEngineForQuickGame` (`useQuickGameStore.actions.ts:26-38`) resolves `seed: seedOverride ?? Date.now()`. The scenario-generator seed at `useQuickGameStore.actions.ts:136` is NOT touched (non-goal).
  - Files: `src/pages/gameplay/quick/index.tsx`, `src/stores/useQuickGameStore.actions.ts`, `src/stores/useQuickGameStore.types.ts` (if action signatures live there).
  - Acceptance: `?seed=N` on the quick page reaches the engine for all three start actions; no-param behavior unchanged.
- [ ] 4.3 QA — `npm run typecheck` clean; existing hook/store suites green; manual-equivalent check that a garbage `?seed=abc` neither throws nor NaN-seeds the engine.

## 5. Live e2e proof — same `?seed=N` twice ⇒ identical outcome

- [ ] 5.1 New e2e spec: drive the pre-battle route with a fixed `?seed=N` through auto-resolve twice (two independent page loads) and assert identical winner, turn count, and event count between the runs. Equality **between the two same-seed runs** is the assertion — no expected literal winner/turn values appear in the spec (invariant, not golden trace).
  - Files: new spec under `e2e/` (follow the naming style of `e2e/quick-play.spec.ts`).
  - QA: the new e2e green locally via the repo's e2e lane; existing combat e2e specs untouched and green.

## 6. Verification battery

- [ ] 6.1 `npm run typecheck && npm run lint && npm run format:check` clean across touched files.
- [ ] 6.2 Full unit lane green: `npx jest --selectProjects unit --ci` (or the sharded equivalent), including the new determinism proofs (default-rules + `maxtech-heat-scale` variants), the recovery-determinism recover-twice jest, the seed round-trip test, the location-roller helper test, and the untouched MP suites (R1 non-drift).
- [ ] 6.3 e2e lane green including the new seed-reproducibility spec; the pre-existing tolerant assertions in `quick-play.spec.ts` / `playtest-sp-smoke.spec.ts` / `encounter-combat-continuity.spec.ts` are NOT tightened in this change (that is W2's journey work).
- [ ] 6.4 `npx openspec validate add-sp-combat-determinism --strict` passes; spec-delta requirements carry backing evidence from 3.1 (fresh-session invariant proof, both rule variants), 2.3 (recover-twice invariant proof), and 5.1 (live e2e proof).
