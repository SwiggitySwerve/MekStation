# Tasks: reconcile-campaign-economy-surfaces

> Execution is **Codex-first and strictly sequential** — one worker, one task at a time; parallel agent fan-out has failed on this repo. Each task is bounded to 1–3 files where possible and lands with its own verification before the next begins. Tests are **failing-first**: write the assertion, watch it fail against current `main`, then implement until green. Pre-commit runs a full `next build` (~3–5 min) — commit via background shell. After any spec/validator edit, run oxfmt + the validator + its jest wrapper (formatter/oxfmt quote race, R5). Do NOT touch co-op transport (that is the separate `route-coop-battle-reconciliation` change).

## 1. M4 — Post the contract payout at the outcome-application commit point

- [ ] 1.1 Hoist `applyContractClosure` into a shared module. Move the private `applyContractClosure` (`src/lib/campaign/processors/contractProcessor.ts:85-125`) plus its `statusToPayoutOutcome` helper into a new `src/lib/campaign/processors/contractClosure.ts`, exported. Update `contractProcessor` to import from there. Pure refactor — no behavior change.
  - Files: new `src/lib/campaign/processors/contractClosure.ts`, `src/lib/campaign/processors/contractProcessor.ts`
  - Acceptance: `contractClosure.applyContractClosure` builds the credited balance + `TransactionType.Income` transaction (id `tx-contract-close-<id>-<iso>`) + `contract_closed` day event exactly as before; `contractProcessor` behavior is unchanged
  - QA: existing contract-processor coverage green — note `src/lib/campaign/processors/__tests__/contractProcessor.test.ts` does NOT exist; the real suites are `src/lib/campaign/__tests__/processors.test.ts` and `src/__tests__/integration/phase4CampaignRoundTrip.test.ts` (`npm.cmd test -- --watchAll=false --runTestsByPath src/lib/campaign/__tests__/processors.test.ts --runInBand`); `npm run typecheck` clean

- [ ] 1.2 **Failing-first**: payout posts at apply. In `src/lib/campaign/processors/__tests__/postBattleProcessor.retryIdempotency.test.ts` (or a sibling `postBattleProcessor.*.test.ts`), add a test: applying an outcome that flips a contract to terminal SHALL credit the balance, append exactly one final-payment income transaction, and record the contract id in `processedFulfilledContractIds` (not `pending`). Pin the discriminator: the transaction keeps `TransactionType.Income` with id `tx-contract-close-<contractId>-<iso>` (the existing `applyContractClosure` output) — do NOT switch it to `TransactionType.ContractPayment` (`'contract_payment'`), which is a DIFFERENT enum member used only in fixtures. "CONTRACT_PAYMENT"/`contract_payment` here names the *day-event* type and the payout concept, not the transaction enum member. Watch it fail. Then implement in `postBattleProcessor.applyOutcome` (`src/lib/campaign/processors/postBattleProcessor.ts:114-261`): when `contractDelta?.flippedToTerminal`, call the shared `applyContractClosure`, write `finances.balance` + `transactions` onto `updatedCampaign` (`:189-202`), stamp `processedFulfilledContractIds` instead of `pendingFulfilledContractIds` (`:171-179`, `:200`), and push a `contract_payment` day event alongside the existing `contract_fulfilled` event (`:217-229`). Keep the balance write + id stamp in the same returned immutable object, preserving the D-5 commit-point ordering (`:230-245`).
  - Files: `src/lib/campaign/processors/postBattleProcessor.ts`, its test
  - Acceptance: the new test is red before the edit and green after; the finance write and the `processedFulfilledContractIds` stamp are in the single returned object
  - QA: `npm.cmd test -- --watchAll=false --runTestsByPath src/lib/campaign/processors/__tests__/postBattleProcessor.retryIdempotency.test.ts --runInBand`; `npm run typecheck` clean

- [ ] 1.3 **Failing-first**: exactly-once across apply + advance (double-pay guard). Add a test: apply an outcome that posts a contract payout, then run the day pipeline (`postBattleProcessor` at `DayPhase.MISSIONS - 50` then `contractProcessor` at `DayPhase.MISSIONS`) and assert **no** second final-payment `TransactionType.Income` transaction and an unchanged balance from closure. Watch it fail if 1.2's stamp is wrong. Then verify `contractProcessor`'s `alreadyClosed` guard (`contractProcessor.ts:156,164`) skips the already-`processedFulfilledContractIds` id.
  - Files: `src/lib/campaign/processors/__tests__/`, `src/__tests__/integration/phase4CampaignRoundTrip.test.ts` (update, do NOT loosen)
  - Acceptance: payout posts once total across the apply and the following advance; `phase4CampaignRoundTrip` asserts payment at the new apply commit point, not in `contractProcessor`
  - QA: `npm.cmd test -- --watchAll=false --runTestsByPath src/__tests__/integration/phase4CampaignRoundTrip.test.ts --runInBand`; `npm run typecheck` clean

- [ ] 1.4 **Failing-first**: legacy-save drain fallback. Add a test constructing a campaign whose `pendingFulfilledContractIds` contains a fulfilled contract that is absent from `processedFulfilledContractIds` and has no posted payment (a pre-change save shape). Advancing a day SHALL have `contractProcessor` close it exactly once and move the id into `processedFulfilledContractIds`. Watch it fail if the drain was broken by 1.2, then confirm the drain still closes un-posted pending ids.
  - Files: add the drain-fallback cases to `src/lib/campaign/__tests__/processors.test.ts`, OR create `src/lib/campaign/processors/__tests__/contractProcessor.test.ts` (new file) — `contractProcessor.test.ts` does NOT exist today, so do not reference it as an existing suite
  - Acceptance: legacy pending-but-unposted contract drains once; already-posted contract is skipped
  - QA: `npm.cmd test -- --watchAll=false --runTestsByPath src/lib/campaign/__tests__/processors.test.ts --runInBand` (or the new file's path if created); `npm run typecheck` clean

- [ ] 1.5 Emit the `finances` activity-log entry at closure so Recent Activity shows the income — on BOTH the day-advance path AND the interactive apply path. Wire the `contract_payment`/`contract_closed` closure event into the activity-log append path (`emitDailyActivityEntries` / the activity-log surface in `src/stores/campaign/useCampaignStore.dayActions.ts:117-146`). **Decision (load-bearing):** `applyPostBattle` (`postBattleProcessor.ts:262-268`) returns only `{campaign, summary}` and DISCARDS `applyOutcome`'s `events`, so the interactive apply path (`useCampaignStore.outcomes.ts:156-196` `retryCampaignOutcomeApplication`; `review.tsx:284`) never sees the closure event. Widen `applyPostBattle`'s return to surface the events (or recompute the activity entry in the store layer from the applied summary) so the `finances` activity entry lands on interactive apply too, not just day-advance. The day pipeline (`postBattleProcessor.process`) already returns events, so that path needs only the emit wiring.
  - Files: `src/lib/campaign/processors/postBattleProcessor.ts` (widen `applyPostBattle` return), `src/stores/campaign/useCampaignStore.outcomes.ts`, `src/stores/campaign/useCampaignStore.dayActions.ts` (or the activity-log emit path it calls)
  - Acceptance: a posted contract payout produces a `finances` activity-log entry on both the day-advance and the interactive review/GM apply path
  - QA: note `src/stores/campaign/__tests__/useCampaignStore.dayActions.test.ts` does NOT exist — add the assertion to `src/stores/campaign/__tests__/useCampaignStore.test.ts` or create the dayActions test file (new); `npm.cmd test -- --watchAll=false --runTestsByPath src/stores/campaign/__tests__/useCampaignStore.test.ts --runInBand`; `npm run typecheck` clean

## 2. M2 — Derive dashboard economy widgets from canonical state

- [ ] 2.1 Hoist the active-contract predicate and define the composed selector. Move the `getActiveContracts` predicate (`src/lib/campaign/processors/scenarioGenerationProcessor.ts:73-92` — `isContract` + future/undefined `endDate`, **endDate-only, NO status check**) into `src/stores/campaign/campaignCommandSelectors.ts` **unchanged**, so `scenarioGenerationProcessor` keeps identical behavior consuming the hoisted predicate. Then define the shared `selectActiveContract(campaign)` (and/or `selectActiveContracts`) as the hoisted predicate **AND** the Missions-tab active filter (`status === MissionStatus.ACTIVE`) — the status check is what the pipeline predicate lacks and what the dashboard needs to agree with the Missions tab (`missions.tsx:188,243`). The dashboard uses `selectActiveContract`; the pipeline uses the raw hoisted `getActiveContracts`.
  - Files: `src/stores/campaign/campaignCommandSelectors.ts`, `src/lib/campaign/processors/scenarioGenerationProcessor.ts`
  - Acceptance: `scenarioGenerationProcessor` consumes the hoisted `getActiveContracts` with identical behavior; `selectActiveContract` returns only ACTIVE-status contracts that also pass the endDate predicate; the pipeline's existing tests stay green
  - QA: `npm.cmd test -- --watchAll=false --runTestsByPath src/lib/campaign/processors/__tests__/scenarioGenerationProcessor.test.ts --runInBand`; `npm run typecheck` clean

- [ ] 2.2 **Failing-first**: dashboard/Missions-tab active-contract agreement. Add a **NEW hook-level test** (against `useCampaignDashboardSummary`) driven by a campaign whose `missions` hold an ACTIVE contract: `selectActiveContract(campaign)` and the dashboard active-contract card SHALL surface that contract, and the operations queue SHALL NOT emit the "Choose a contract" item. Also assert the post-terminal case (a contract flipped to a terminal status is NOT reported active). Watch it fail. Then replace `extractActiveContractSummary` (`src/lib/campaign/hooks/useCampaignDashboardSummary.ts:147-177`) with `selectActiveContract(campaign)` and remove the "Choose a contract" operations item keyed off `campaign.activeContract` (`:377-386`). Note: `CampaignOperationsQueueCard.test.tsx` is fixture-driven, never exercises the hook, and contains NO "Choose a contract" assertion — do NOT "rewrite" a stale assertion there; at most refresh its fixture if the card's props shape changes.
  - Files: `src/lib/campaign/hooks/useCampaignDashboardSummary.ts`, a new/extended hook test under `src/lib/campaign/hooks/__tests__/`; `src/components/campaign/dashboard/__tests__/CampaignOperationsQueueCard.test.tsx` only if a fixture refresh is needed
  - Acceptance: new hook test red before, green after; dashboard active contract == Missions-tab active contract; terminal contract not reported active
  - QA: `npm.cmd test -- --watchAll=false --runTestsByPath src/components/campaign/dashboard/__tests__/CampaignOperationsQueueCard.test.tsx --runInBand`; `npm run typecheck` clean

- [ ] 2.3 **Failing-first**: resolve the `600 vs 200` daily-cost contradiction. Add a NEW assertion (none exists today — the current `useCampaignDashboardSummary.test.ts` only covers `extractForceSnapshot`): a campaign with 4 pilots + 4 roster units + 0 force-assigned units SHALL have the dashboard finance card's daily-cost equal `selectDailyCostProjection` (option-aware, loan-aware). Watch it fail against the hardcoded `50/100` estimate. Then replace `extractFinancesSummary`'s hardcoded math (`useCampaignDashboardSummary.ts:223-232`) with `selectDailyCostProjection` (`campaignCommandSelectors.ts:146`), including loan repayment. **Pin the pilot-count input:** `selectDailyCostProjection(campaign, pilotCount)` takes a pilot count — the dashboard MUST pass the SAME billable filter the Finances page uses (`pilots.filter((p) => p.status !== CampaignPilotStatus.KIA)`, `finances.tsx:49-54`), otherwise a campaign with KIA pilots still contradicts. Prefer hoisting that billable-pilot filter into a shared selector consumed by both surfaces.
  - Files: `src/lib/campaign/hooks/useCampaignDashboardSummary.ts`, `src/stores/campaign/campaignCommandSelectors.ts` (optional shared billable-pilot selector), a new assertion in the hook test
  - Acceptance: dashboard daily cost == Finances page projection for the same campaign (including a KIA-pilot case); hook test asserts the projection with the billable-pilot count, not the hardcoded constants
  - QA: `npm.cmd test -- --watchAll=false --runTestsByPath src/lib/campaign/hooks/__tests__/useCampaignDashboardSummary.test.ts --runInBand`; `npm run typecheck` clean

- [ ] 2.4 Count force-tree-assigned units in `forceSnapshot`. Change `extractForceSnapshot` (`useCampaignDashboardSummary.ts:252-277`) so `mechCount` reflects force-tree assignment (via `getAllUnits(rootForce)`) matching the Forces tab, or surface roster and assigned counts explicitly. Add/adjust a test asserting agreement with the Forces tab count.
  - Files: `src/lib/campaign/hooks/useCampaignDashboardSummary.ts`, its hook test
  - Acceptance: dashboard forces count matches the Forces-tab force-tree assignment count
  - QA: `npm.cmd test -- --watchAll=false --runTestsByPath src/lib/campaign/hooks/__tests__/useCampaignDashboardSummary.test.ts --runInBand`; `npm run typecheck` clean

- [ ] 2.5 Deprecate the write-less `ICampaignActiveContract` field with read-tolerant deserialization. Mark `ICampaignActiveContract` (`src/types/campaign/CampaignCommandExtensions.ts:78`) deprecated; confirm no production reader remains (D2 removed the last one). Keep deserialization read-tolerant so a legacy save carrying `activeContract` still loads (`useCampaignStore.persistence.ts:145,298,391`; `serializeCampaign.ts:195,263`). Add a test loading a legacy save with `activeContract` present.
  - Files: `src/types/campaign/CampaignCommandExtensions.ts:78`, `src/types/campaign/SerializedCampaign.ts:249` (the serialized-envelope `activeContract` field — keep it optional/read-tolerant), persistence/serialize read paths (`useCampaignStore.persistence.ts:145,298,391`; `serializeCampaign.ts:195,263`), a persistence test
  - Acceptance: legacy save with `activeContract` loads without error; no production code reads the field
  - QA: `npm.cmd test -- --watchAll=false --runTestsByPath src/lib/campaign/persistence/__tests__/serializeCampaign.test.ts --runInBand`; `npm run typecheck` clean

## 3. Live smoke + final verification

- [ ] 3.1 **Live smoke**: complete a mission → ledger shows income immediately → dashboard matches Finances page. Using the running app / existing e2e harness, complete a campaign mission (apply the outcome via the review path, NOT a day advance), then assert (a) the finance ledger shows the final-payment `TransactionType.Income` transaction immediately, (b) Recent Activity shows the income, and (c) the dashboard finance card daily-cost matches the Finances page. **Co-op scope (decidable form only):** assert the payout lands on the host-authoritative `campaign.finances` after `applyOutcome` and persists via `saveCampaign`. Do NOT assert a guest mirror sees the balance change — on the current tree `reconcileCoopOutcomeForCampaign` fires at enqueue with a hardcoded `missionPayout: 0`, so guest-visible `FundsChanged` propagation does not exist yet; wiring it is owned by `route-coop-battle-reconciliation` (iter 2). Do not modify co-op transport here.
  - Files: e2e/smoke wiring as needed (no product co-op transport changes)
  - Acceptance: income posts at completion with no day advance; dashboard and Finances page agree; payout is present on host-authoritative persisted `campaign.finances` (guest-visibility explicitly out of scope, deferred to iter 2)
  - QA: run the smoke command; capture the ledger + dashboard state

## Final Verification Wave

- [ ] F1 Typecheck + lint + format clean across all touched files: `npm run typecheck && npm run lint && npm run format:check`.
- [ ] F2 All unit + integration suites for the change pass: contract closure, post-battle apply idempotency, contractProcessor legacy drain, dashboard summary hook, operations queue, and serialize/persistence round-trip (Tasks 1–2 QA commands) all green.
- [ ] F3 The three failing-first behaviors are proven: payout posts exactly once across apply + advance (1.2/1.3), legacy-save drain fallback (1.4), dashboard/Missions-tab agreement + `600 vs 200` resolution (2.2/2.3).
- [ ] F4 `npx openspec validate reconcile-campaign-economy-surfaces --strict` passes, with the `contract-types` and `campaign-hud` deltas' SHALL coverage backed by the tests above.
