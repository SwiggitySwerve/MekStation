# OMO Council — Recommendations after the roadmap loop reached terminal (2026-09-17)

**Headline:** The loop is not stalled on four owner decisions but on one: rule PR #1797's head and hold both ticks, because the two "non-claim" genesis clauses are unbuilt (the only writer of a campaign genesis branch row is the match-journal mirror, whose combat-journal mode is the literal `off`), and everything else the program needs next is agent-executable today under existing nodes except one narrow client-side ownership gap.

**Brief:** After U0–U1d landed and U2/U3 parked owner-gated (`--next` = NONE-ADMISSIBLE: 2 owner-gated, 0 blocked, main 12466da33), what should the owner rule, what should agents do without a ruling, and what must change in the loop before the next run?

**Option space:** per packet: rule/tick, rule/merge-hold, reject, defer rows to a node; ownership: widen R2.authority-live, re-own to R4.rewind, add a client node, use R6.umbrella/R2.combat as-is; genesis: bisect, probe, or treat as gated; process: codify checks vs document them.

**Variant:** Lean+ (Phase 0 Metis; Phase 2 Hephaestus, Oracle, Librarian, Explore-Deep, Momus; Phase 3 both proposers). Metis reframed the brief: the packets are not independent, the PK-u3 tick may be the forbidden "narrow the letter" move, the D2-vs-U3 genesis discrepancy was unreconciled, the ownership gap is narrow, and "next increment" is whatever `--next` prints after rulings.

## Decision

### A. Owner rulings (in this order, one sentence each)

1. **PK-u3-21-1-tick-and-merge → option (b).** "I rule head `935fe0423e9eef2c806ab8b4e012c9a9a8d6c929`; PR #1797 merges; row 21.1 stays open." Reason: the genesis clauses of E2E-01/02 cannot pass at any head today. `campaignCreationCheckpoint.ts` `awaitGenesisBranch` only reads a marker (Oracle P3, verified); the sole non-test caller of `backfillGenesisBranches()` is `MatchStreamJournalMirror.ts:264`, and `DurableMatchStore.mirrorCommittedBatch` returns when `getCombatJournalAuthorityMode() === 'off'`, which `matchJournalAuthority.ts:18-19` sets as the literal `'off'` (Oracle P3, Explore-Deep E1). Task 5.7 of design-campaign-authority-and-sync is that flip. Ticking 21.1 would pass the validator (it matches `[x]` by source id and text only, `validate-roadmap.mjs:393-398`, Momus) and would still be GOAL.md forbidden move #1. Option (c) is unjustified: the PR is harness-only, 31 checks green, Lane A APPROVE. Add the `owner-ruled` label (absent today, Momus).
2. **PK-privacy-21-2-tick → hold, receipt the ten rows.** The packet's stated blocker was wrong in both directions: the GM combat-rewind commit half IS wired end to end (#1588; Hephaestus), but E2E-25 needs the *correction* pair (`onPreviewHostGmCorrection`/`onApproveHostGmCorrection`), which `[roomCode].tsx:388-404` never passes (Explore-Deep E2, confirmed by Hephaestus P3), and no client collects a private reason (`REWIND_COMMIT_REASON` is a constant, `rewindCommitDeps.ts:38`; Oracle P3). E2E-19's letter is disjunctive and the rewind preview now records a private draft (#1578), but `tasks.md:448` says a preview creates no draft, so the object of the assertion is still missing (Hephaestus P3). Both rows also sit behind the combat-journal mode flip (404 `no-authoritative-history` on every rewind route while mode is `off`). A tick on ten of twelve rows is the same partial-letter narrowing as PK-u3 option (a). **Dissent preserved (Oracle):** tick on ten with a deferral of 19/25 to R4.rewind re-founded on two source-quotable gates (`matchJournalAuthority.ts:18-19`; the constant reason). The council majority holds that every existing deferral moved a whole open row and that partial-row ticks need an owner-level rule first.
3. **PK-rewind-branch-reader → option (a), teach the reader, but it is off the critical path.** A successor unit is admissible today under R2.authority-live, whose `ownershipPaths` include the exact string `"src/lib/multiplayer/server"` (the reader lives at `src/lib/multiplayer/server/history/matchStoreBranchSegmentReader.ts`) and whose dependency is main-verified; two nodes may share a path; no node state or `dependsOn` edit is needed (Momus; Oracle conceded; Hephaestus corrected the path wording: the unit must declare the directory string, not the file, per `validate-roadmap.mjs:313`). `--next` prints nothing for the eight held rows until a planned unit references them (packet `blocks: []`).
4. **PK-e2e-80-ci-wiring → the qc contract run by the loop** (`scripts/qc/validate-exact-main-regression-ladder.mjs` reading `SPEC_BY_GROUP`), no CI minutes, inside R6.umbrella paths. Correction to the packet text: R1.ci does own `.github/workflows`, but it sits behind an archived dependency and is not admissible (Oracle).

### B. Ownership (agent-draftable now, owner approves the node)

- Harness units (E2E-19/25 rows, E2E-16 diagnosis, the authorization row) file under **R6.umbrella** as-is.
- The pending-intent fix (structural: no lobby intent ever settles because `ILobbyUpdated` and the lobby `Error` frames carry no `intentId`; smallest fix is an optional `intentId` stamped at `ServerMatchHostLobbyIntents.ts:208-215` plus `settlePendingIntent` in `client.ts:413-415`; Explore-Deep E4) can file under **R2.combat**, which owns the exact string `"src/lib"` with a satisfied dependency (Hephaestus P3). Coarse but legal; Lane B classes apply (wire, idempotency).
- One new node is genuinely required for the lobby page and components: **R2.multiplayer-client** (dependsOn R2.authority-live; paths `src/pages/multiplayer`, `src/components/multiplayer`, `src/pages-modules/multiplayer`; taskKeys []). Precedent: R2.authority-client/host were added mid-program with empty taskKeys; the admission snapshot checks packages and tasks, never nodes (Oracle, verified). Do not widen R2.authority-live (a main-verified receipt would retroactively cover lobby UI) and do not re-own to R4.rewind (its chain R3.combat → R3.activation is planned/blocked, so `--next` can never admit it; Oracle, Hephaestus withdrew).
- The stale-head lever (`src/pages/api/campaigns/[id]/commands.ts:57-60`, ~10 product lines; the pipeline already accepts `expectedRevision`) touches a path no node owns; it needs the same ownership ruling.

### C. Genesis clauses: disclosure the owner should demand

- Explore-Deep traced that the campaign genesis branch row never comes from the campaign genesis path; it appears only when the drive's own `AdvancePhase` match commit fires the stream-agnostic backfill (`SQLiteService.historyBranches.migration.ts:104-119`, disclosed as D-S1-4). Hephaestus P3 adds that the backfill only covers streams that already exist, so a campaign stream must exist first; with combat-journal mode `off`, neither condition is met. U3's drive restored the match commit (human host seat) and the rows stayed red, consistent with this.
- **Unreconciled fact:** `tasks.md:493` (DECISION D2, 2026-09-03) records both clauses green ("4 passed / 0 failed") on 5d5307125. No seat could explain that from current code, and the bisect the council first proposed was withdrawn as unnecessary for the ruling. Recommendation: annotate D2's receipt with this council's finding rather than run a bisect; if the owner wants the discrepancy explained, one production build at 5d5307125 with `--group=authority-recovery` settles it (Hephaestus, ~10 minutes). Hephaestus also notes `waitForRelaunch` polls an endpoint that is already 200 if the server never died, which can pass vacuously; file it as a harness finding.

### D. Loop and process fixes (agent-executable, three harness PRs, no owner ruling)

1. Idle check as `scripts/qc/machine-idle.mjs` with a jest pin: match process images (`next-server`, `node server.js`, Playwright, Chromium) not command lines, and **throw** when the enumeration tool is absent (the `wmic` check was silently vacuous). Exclude the ladder's own server when called from inside a run (Momus).
2. Junction guard as an npm `preinstall` script refusing to run when `realpath(cwd)` is not the repo root (npm/cli#3669: no flag prevents pruning through a linked node_modules; Librarian).
3. Log preservation helper that copies a run's logs into the ledger evidence dir with sha256 **before** `git worktree remove`, refusing removal otherwise (DELIVERY.md already says so; it needs enforcement, not prose).
4. oxlint: measure the finding count for `e2e`/`scripts` first; add a `lint:units` target with its own pin rather than un-ignoring paths that existing receipts pin (Hephaestus). 
5. E2E-80 qc contract (A4).
6. Worker routing: keep the Grok 4.6 directive; on exhaustion mid-unit record the failure, rotate one variable per WORKERS.md, re-run local from the existing red receipt, re-run Lane A; **the parent must not finish a worker's edits**; split `implementerModel` into implementer/finisher fields. U3's receipt stands for this unit (honestly recorded, sonnet reviewed a harness-only head); the protocol change applies going forward (Oracle).
7. CI minutes: per-run `actions/runs/{id}/timing` or runs listed by `head_sha` need only repo read scope (Librarian); the `user`-scoped endpoint the loop hit is the wrong one.
8. E2E-16 diagnosis unit under R6.umbrella: `routeWebSocket` only sees sockets created after installation, and `setOffline` may not sever an open socket (Librarian); nothing in a00b816dc..12466da33 touched the token pack, client, lobby or socket route (Explore-Deep E3), so it is pre-existing nondeterminism.
9. Umbrella acceptance ("all reachable E2E-01..80") is already unsatisfiable today (E2E-16 red; E2E-19/25 absent); it deserves its own packet as a program-contract question rather than riding on PK-privacy (Oracle, Explore-Deep E5).

**Survival score:** Decision survived Phase 3 intact on A1, A3, A4, B and C (B was contested in Phase 3: Hephaestus proposed re-owning product units to R4.rewind and withdrew it on the dependency evidence; Oracle's new-node proposal was corrected on its path list; R6.umbrella and R2.combat as hosts for the harness and pending-intent units were Hephaestus's Phase 3 contribution and stood); A2 survived by majority with Oracle's dissent preserved; both proposers withdrew positions under evidence (Hephaestus: R4.rewind ownership, "commit half is built", the genesis probe; Oracle: the regression/bisect claim, the rewind packet's criticality, the client node's path list).

**Cruxes left open:** why D2 recorded the genesis clauses green; whether E2E-77's "the UI SHALL identify" clause accepts a route-level 403 for the authorization class; whether partial-letter ticks are ever allowed (program rule the owner sets once).

Synthesis verified by omo-judge (2-pass, both VERIFIED).
