# OMO Council — Review: what needs validation and what is left (2026-09-17, second council)

**Headline:** The umbrella's remaining rows are gated on one hardcoded constant that no unit owns, not on an owner cutover: `COMBAT_JOURNAL_AUTHORITY_MODE` is the literal `'off'` with no e2e arm, so one Short unit that mirrors the campaign flag's two-key arm makes the two genesis clauses provable, which is why the council withdraws its own proposal to change the tick rule and instead keeps this morning's rulings, fixes U4's admission (the schema it must edit sits outside its node and zod strips the field it adds), and mints one owner-ruled packet per sensitive unit.

**Brief:** After the first council's rulings were applied (main 9913f06e7, `--next` = U4), which recorded claims are not yet validated, what is left to close harden-gm-two-player-campaign-sessions honestly, which planned units are mis-specified, what should the loop validate that it does not, and what should the owner know.

**Variant:** Lean+ without Librarian (external evidence not load-bearing): Phase 0 Metis; Phase 2 Hephaestus, Oracle, Explore-Deep, Momus; Phase 3 both proposers. Metis reframed the brief: validation mostly means checking receipts against the tree (the validator already does the structural half); the umbrella cannot close until PK-umbrella-acceptance is ruled; the sizing risk is companion files, not the 500-line ceiling; growing the validator is a tradeoff.

## Decision

### A. What is validated, what is not (Explore-Deep, Oracle; all by git diff, no reruns)

- **Validated by empty diffs since their proofs:** U1, U1c, U1e, U1d, U3 (product files untouched after each main proof; qc-core edits since are additive group registrations), and the ten privacy rows of U2 (`git diff a00b816dc..9913f06e7` over the privacy and proposal packs, helpers and multiplayer src is empty).
- **Not validated: U1b.** PR #1797 (U3) hardcoded `hostSeatKind: 'human'` into `postCampaignMatch` in `e2e/helpers/gmTwoPlayerAuthorityRecovery.ts`, which `e2e/gm-two-player-evidence.pack.spec.ts` uses through `openRecoverableCampaign`; U1b's main proof predates that. Rerun `--group=evidence-smoke` on main and re-stamp U1b's mainProof (Short). Second-order: a shared-helper edit invalidates every group that imports it and the ledger does not record that fan-out (validator item D5).
- **D2's genesis green (tasks.md line 494, DECISION D2, 2026-09-03, run d2-proof-1 on 5d5307125) is falsified by git without a build.** Captain-verified commands: `git cat-file -e 5d5307125:src/lib/multiplayer/server/MatchStreamJournalMirror.ts` reports the path does not exist in that commit; `git grep -n backfillGenesisBranches 5d5307125 -- src` returns only the definition at `src/lib/events/journal/SQLiteEventHistoryBranchStore.ts:69` (no caller); `git diff --stat 5d5307125 HEAD -- e2e/helpers/authorityRecoveryEvidence.ts` is empty, and at that commit the helper already asserted `expect(rows).toHaveLength(1)` at line 229 over `event_history_branches` (line 102). So the assertion existed with no code path able to satisfy it. Annotate D2; do not run the probe build (Oracle; Metis's proposed measurement withdrawn).
- **Ledger against GitHub (Momus):** all 52 receipt paths resolve; PR #1797 state, merge e3fc54384, head 935fe0423, label `owner-ruled` and comment 5710327985 match the ledger; PR #1800's merge is 9913f06e7. All checked by hand: the validator never opens a receipt file, reads GitHub, or parses a Playwright verdict.
- **Citation drift in finding prose (Explore-Deep):** FN-u1d-authorization names `src/lib/multiplayer/Protocol.ts` (real file `src/types/multiplayer/Protocol.ts:225-227`); FN-u1d-stale-head names `src/lib/campaign/.../commands.ts` (real file `src/pages/api/campaigns/[id]/commands.ts`). Cite by task id and quoted text, never by tasks.md line (the file is append-heavy; D2 is now at line 494).

### B. The rulings stand; the rule change is withdrawn

- Oracle proposed a program-level "ledger-complete" tick rule (tick when every scenario is proven or named in a deferral citing its gate) and **withdrew it in Phase 3**: the named gate would be a constant no unit owns, which is a tick bought with prose. PK-u3 option (b) and PK-privacy's hold stay as ruled; both rows tick honestly after the arm unit below.
- **Disclosure (Momus):** the OWNER-RULING comment on #1797 was posted by the loop from the owner's GitHub account on a general chat instruction. DELIVERY.md says the owner posts it out of band. Record it as a deviation in PK-u3's ruling receipt and tighten DELIVERY.md: the owner posts each ruling personally, or authorizes a named head in chat before the loop posts it; the sensitive-class gate is only as strong as the hand that writes the comment.

### C. Planned units: corrections before admission (Hephaestus, Oracle, both verified)

1. **Sensitive units need one packet each** (validate-roadmap.mjs:369-376: a packet whose `blocks` names the unit and whose `ruling.ruledHead` equals that unit's merge head). Mint packet shells now for U4, U5a, U5b, U8, U10 with `blocks` set and `ruling: null`, no `ownerGate` (that would block the unit); the owner rules each on its PR head. Adding `blocks:["U10"]` to PK-rewind-branch-reader is insufficient: it carries no ruling and one ruling cannot cover different merge heads.
2. **U4 cannot succeed as admitted:** `LobbyUpdatedSchema` is in `src/types/multiplayer/Protocol.ts:864-871` (not under R2.combat's `src/lib`), and `ServerMessageSchema.safeParse` at `client.ts:785-787` strips unknown keys, so a stamped `intentId` never reaches the handler. Fix: add `src/types/multiplayer` to R2.combat's paths (the narrowest new string, unowned today); shrink U4 (the Error frame already carries `intentId` at Protocol.ts:803 and settles at client.ts:394; missing are the schema field, the two LobbyUpdated build sites `ServerMatchHostLobbyIntents.ts:209` and `ServerMatchHostReplay.ts:331`, and the handler settle); the red-first must enter through `handleSocketMessage` (client.ts:780) with a JSON frame, plus an assertion that the parsed schema keeps `intentId`. Set U4 `implementing` now: `planned` while a lane runs is a double-admission gap (Momus).
3. **U5a retarget, not delete:** the private draft record already ships (`rewind-preview.ts:224-249` writes via `GmPrivatePreviewRecordWriter` on the preview branch, #1476), so its red-first is already green; but `includePrivate` exists only in `ViewerHistoryService.ts:294-318` and the export route says it is not accepted, so **no GM HTTP read route exists** and E2E-19 cannot assert on the record. Retarget U5a to that read exposure (privacy class), paths `src/pages/api/matches` and `src/pages-modules/api`, which **no node owns**: add both to R2.authority-live in the same roadmap edit.
4. **U8 red-first** must use a colliding-field intent (`resolveCommandBase` serialises disjoint-field stale commands) and pass the `STALE_REVISION` code through, since the 409 body carries none.
5. **Sizing:** every merged unit was 4-6 files and 64-369 lines; the floor broke, never the ceiling. U9 is the only real overage risk (needs a companion inside `scripts/qc`); U12's `preinstall` must gate on manifest directory versus cwd realpath, not "is this the main repo"; U14 needs a `scripts/qc` wrapper rather than a second root config no node owns.
6. **Serialisation:** U9/U11/U12/U13/U14 share `scripts/qc`, `scripts/__tests__`, `package.json`; they run one at a time.

### D. The unlock and the new units

1. **Combat-journal e2e arm (highest value).** Task 5.7 of design-campaign-authority-and-sync is already `[x]` and the authority-recovery group already arms the campaign flag; the genesis clauses fail because `matchJournalAuthority.ts:18` hardcodes `'off'` with only a jest-only override. **U15a** (R2.authority-live): `src/lib/multiplayer/server/combatJournalAuthorityEnabled.ts` mirroring `campaignJournalAuthorityEnabled.ts` (two keys: `NEXT_PUBLIC_E2E_MODE` and `MEKSTATION_E2E_COMBAT_JOURNAL_AUTHORITY_MODE`), consumers routed through `getCombatJournalAuthorityMode()`; red-first jest on the two-key arm. **U15b** (R6.loop-harness): the qc-core env arming and `playwright.config.ts` forwarding; `playwright.config.ts` is owned by no node and must be added to R6.loop-harness. After both, the two genesis rows are provable and 21.1 can tick under PK-u3 (b)'s own terms; E2E-19/25 then need U5a/U5b and rows.
2. **Validator unit U16 (R0.plan, owns the planning directory; U0's precedent; insert ahead of U4 in array order):** `--evidence` default-on for the cheap checks (every receipt path exists; `mainProof.runtime` parses as `N failed / M passed`; `expectedReds` is an array whose length equals the failed count, gated to product units, with the five earlier product units backfilled), `--github` opt-in (PR state, `owner-ruled` label, OWNER-RULING head for sensitive units at rank merged or above), prefix-containment `pathFree` (today exact-string, so `src/lib` never blocks `src/lib/multiplayer/server`), and later a mainProof invalidation when a later merge touches the unit's proof inputs.
3. **Loop scripts into the repo (R6.loop-harness):** the closure, fold and main-proof generators exist only in the parent's scratchpad; the loop is not reproducible from main. U13 depends on them and cannot land first.
4. **Docs (R0.plan):** disclose the ruling deviation, tighten DELIVERY.md's Lane B wording, fix the two citation drifts, annotate D2, record the U1b invalidation.

### E. What is left to close the package (after the above)

Rows held: 21.1 (U3, ticks after U15a/b prove genesis and U6 settles E2E-16), 21.2 (U2, ticks after U5a/U5b and two new rows), the eight rewind rows (PK-rewind-branch-reader: U10 plus the combat-journal cutover boxes 1.7/3.2, whose six `@until-journal-cutover` rows in `gm-two-player-rewind.pack.spec.ts` are strict `test.fail`), five rows deferred to R4.rewind (blocked chain R3.combat, R3.activation), eight to R6.B7 (behind R5.E1-E10), two to R2.journey (behind R2.camp-8, blocked). Under the literal acceptance reading the umbrella closes behind 18 of the 30 planned nodes; PK-umbrella-acceptance-unsatisfiable remains the owner's call and this council recommends the openspec amendment that names the gates.

### F. Worker routing (parent's observation, for the owner)

The Grok CLI answers one-line prompts but hung twice on the U4 charter (a file-read tool call ran past five minutes; the lane logged startup only, with a zero prepaid balance in its billing record); the parent rotated U4 to codex exec (gpt-6) per WORKERS.md's one-variable rule and the accepted D6 protocol. Lane A stays sonnet. The owner may redirect.

**Survival score** (intact means the position survived the Phase 3 attack, not that every crux is closed): intact on A, C and D after real cross-attack (Hephaestus withdrew "U4 is fine under src/lib" and "E2E-19 is only a row"; Oracle withdrew the tick-rule change and the D2 build; both corrected their own citations); B survived by both proposers' agreement in Phase 3 with the earlier dissent recorded. Open cruxes: whether E2E-77's "the UI SHALL identify" accepts a route-level 403; whether the genesis row should keep depending on a combat commit (arm now, correct fix tracked); and PK-umbrella-acceptance's reading.

Synthesis verified by omo-judge (3-pass: VERIFIED, REVISE on the D2 proof and wording, VERIFIED after revision).
